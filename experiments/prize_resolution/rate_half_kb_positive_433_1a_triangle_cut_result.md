# Positive 433-1a target-free triangle-cut pilots

These experiments refine the positive `433-1a -> O0b` outside interface.
They are finite-field evidence and algorithm diagnostics, not route
deletions.

## Specialized scalar cuts

Substitution of the explicit common kernel into one `ef` scalar edge cut
gave product-coefficient supports of `44,56,44` terms and squared-sum
supports of `441,957,1122,957,441` terms in each of four root-sign rows.
All four compact resultant expansions reached the 75-second cap.  Modal:
`ap-L6jmDq6TmutoMId17LuGB0`.

The exact cell-5 normalization

```text
A0-b*A2=alpha*(W-r^2)*(W-t^2)
```

reduced product supports to `28,40,28`, but left the squared-sum supports
unchanged and again reached the cap.  Modal:
`ap-D6qITfQuoEmOY4DuAhsi7F`.  Thus cofactor sparsification alone does not
make the scalar resultant route viable.

## Residual-sum trace

The exact `F_29`, cell-5, cycle-`+1` replay enumerated every product lift
that already passed the missing-mate sum.  In each of four root-sign rows,
both aligned and near-aligned modes had four target triples and four unique
product-compatible source assignments.  Every assignment passed exactly
zero of the six residual squared-sum rows:

```text
assignment pass histogram = [4,0,0,0,0,0,0].
```

The four triples use exactly two oriented matching templates, A and B in
the companion proved node.  Representative A data are

```text
(b,c,d,e,f)=(14,12,9,16,24), xi record=ef,
outside products=(28,1,13,16,7,21,27),
source labels=(7,5,19,22,20,10,24),
sum residuals=(4,26,15,21,0,11,7) mod 29.
```

The zero residual is the already-imposed missing-mate row; all six others
are nonzero.  Modal: `ap-3rKm0cMDPrCQBq9glyA83m`.

## Target elimination and common geometry

Templates A and B eliminate `d,e,f` exactly, giving compact cuts in the
common maps `F,H` and three source deck labels.  An over-relaxed type-A cut
alone still has ten admissible roots across eight common points at `F_29`,
so the complete three-pair product chain is essential.  Modal:
`ap-9zpZGiNq6PRIRiZJER0AUt`.

Direct expansion of either five-variable target-free cut reached 85
seconds before producing an expanded polynomial.  Modal:
`ap-hiw5WgQAWd21qUlGGxugnw`.  This rejects expansion, not the identities.

Over the deployed field, the cell-5 common chart generated by three
incident minors has dimension two before localization because of guard
components.  Localizing the exact common/source guard product leaves a
dimension-one ideal with a 23-element standard basis and `vdim=-1`:

```text
UNSAT dim=2 basis=112
SAT   dim=1 basis=23
```

Modal: `ap-3NNIpulALnODMHqWkGTzM3`.

The unsaturated ambient seven-variable type-A matching standard basis then
hit the 120-second function cap (`ap-5LekROrgmIeQwn2fIpVvVy`).  The next
algorithm should reduce the three product-chain equations and one compact
sum cut in the localized common-curve coordinate ring or its function
field.  Do not retry raw ambient degree-order expansion.
