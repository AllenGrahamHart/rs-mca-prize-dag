# Deleted-pair harmonic characteristic screen

Modal app `ap-YVKd2kCRyMVnpUDLR9id5x` completed all 32 shards of the
pre-registered official-interval screen.

```text
2^40 | p-1,
3*2^128 <= p^2 < 4*2^128,
p=1+k*2^40,
29058991 <= k < 33554432.
```

Every one of the `4,495,441` congruence classes was tested. For each modulus,
the trace recurrence

```text
c_0=4,       c_(j+1)=c_j^2-2 mod p,       1<=j<=38
```

had no zero. There were no hits, even before restricting the moduli to primes.

This packet is usable only after the algebraic reduction from the harmonic
Möbius branch `r^2-4r+1=0` to the trace recurrence. Together with the direct
order-six exclusion of `r^2-r+1=0`, it removes `q=-1` from the official
split-quadratic deleted-pair branch. It says nothing about the three
nonharmonic remainder-square systems.
