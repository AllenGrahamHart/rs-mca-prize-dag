# Primary/secondary fourth-root rigidity sweep

The falsification target was the tempting statement

```text
nondegenerate primary gap
  ==> scaled pure fourth-root denominator.
```

It is false. Exhaustive normalized censuses at `H=3,...,12`, using two
characteristics with `2N | p-1` for every `N=8H-8`, covered `680,500`
quartets. There were 22 nondegenerate primary survivors, twelve of which were
non-pure. Representative witnesses are stored in the JSON result and replayed
by the checker.

All twelve non-pure primary survivors failed the exact secondary square gate.
The six primary-plus-secondary survivors were pure fourth-root quartets. This
leaves the sharper statement

```text
primary gap + secondary square gap
  ==> scaled pure fourth-root denominator
```

unfalsified on these twenty rows. It is not proved and is not installed as a
DAG premise. If it were proved at the official parameters, the proved pure
fourth-root exclusion would close the normalized generic `c=2` chamber before
canonical span.

This result argues for an algebraic attack on the combined two-window square
identity. Extending the raw census to larger bounded rows would not establish
the official quantifier and is not a contributor compute request.
