# pcf_evaluation_flatness
- **status:** TARGET
## Statement
The CORRECTED flatness target (post falsification #20): for every central
level-j profile M, the TRUE valid-skew count satisfies
B_j(M) = #{d in D_j(M) : Q_d(zeta^{2l-1}) = 0 in F_q for all 1 <= l <= L_j}
<= q^{-L_j + eta_j} U_j(M), with sum_j eta_j = o(t) (prize budget
q^{-t+H} W_cen <= 2^122). The conditions are the ACTUAL odd evaluation
equations at the specific roots — L_j genuinely independent F_q-conditions —
NOT the folded-resultant divisibility, which collapses to ONE condition for
2-power moduli (Res(X^N+1, Q_{d,r}) = Res(X^N+1, Q_{d,1}) for all odd r —
the collapse identity, proved+verified). Surviving levers: the second-moment/
character-sum route (untouched by the falsifier), the divisor-pair bilinear
route, and any structure that remembers WHICH roots are prescribed.
