# ATTACK - e22_cross_scale_duplicate_control

Status: conditional.

The algebraic recovery part is now proved in
`e22_cross_scale_rootset_recovery`: equal staircase locators have equal root
sets, and at each fixed modulus the tail and selected fibers are recovered by
`e22_fixed_scale_staircase_injectivity`.

The active leaf is now `e22_minimal_scale_pricing_compatibility`: the
canonical support-scale relation and minimal-scale selector are proved, and
the remaining issue is compatibility with the pricing column.
