# frontier: e22_cofactor_common_tail_kernel_invariance

Conditional.

The cyclic-group criterion reducing full fibers to kernel invariance is
proved. The certificate semantics for the common-tail invariance package are
also proved. The remaining work is `e22_common_tail_invariance_payload`: the
E22-specific proof of a bounded common tail and local kernel invariance from
the cofactor divisor constraints.
