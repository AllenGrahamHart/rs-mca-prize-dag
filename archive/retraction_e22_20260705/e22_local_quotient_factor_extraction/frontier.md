# frontier: e22_local_quotient_factor_extraction

Conditional.

The formal facts that quotient factors `X^M-z` are exactly full quotient
fibers, that full fibers are equivalent to kernel invariance, and that
quotient factors pass through after tail removal are proved. The remaining
work is `e22_cofactor_common_tail_kernel_invariance`: derive the common tail,
tail bound, dyadic local moduli, and non-tail kernel invariance from the
sunflower cofactor divisibility constraints.
