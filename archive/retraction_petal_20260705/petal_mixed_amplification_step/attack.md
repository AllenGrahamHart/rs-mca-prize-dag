# ATTACK - petal_mixed_amplification_step

This node is now conditional on `petal_residue_line_uniformity`, not the
refuted `route_exact_rank` formula.

The remaining work is to prove the residue-line uniformity predicate itself.
Once that holds, the transition estimate follows by adding the uniformly
bounded new residue-line extras at the `c+1` layer.
