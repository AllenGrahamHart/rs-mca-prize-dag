# petal_mixed_amplification_step

- **status:** CONDITIONAL
- **closure:** proof

## Statement

Assuming residue-line uniformity, the full-petal bound at excess `c` implies
the corresponding bound at excess `c+1` with polynomial constants controlled
uniformly in `c` throughout the corridor range.

## Falsifier

An induction coefficient or residue-line transition whose size grows
super-polynomially with the corridor parameters or unboundedly with the excess
parameter `c`.

## Conditional decomposition

This node is conditional on `petal_residue_line_uniformity`, now interpreted
as exact realizable-extra uniformity under the proved linear residue-kernel
bound, not as literal ambient-kernel flatness.
