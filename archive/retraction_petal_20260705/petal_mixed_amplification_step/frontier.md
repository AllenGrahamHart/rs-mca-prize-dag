# frontier: petal_mixed_amplification_step

Decomposed.

Conditional on `petal_residue_line_uniformity`, the uniform growing-excess
step is formal: each transition adds only the uniformly bounded residue-line
extras. The remaining open petal leaf is therefore
`petal_residue_line_uniformity`.
