# ATTACK - petal_squarefree_classification_ledger_payload

This is now the live Petal squarefree-kernel leaf.

Needed output:

- enumerate the structural families of squarefree realizable locator points
  inside the residue-line kernels;
- partition them into charged and uncharged records;
- cite the paid ledger for every charged record;
- prove a polynomial bound with exponent independent of `c` for every
  uncharged record; and
- prove the number of uncharged records is independent of `c`.

The formal rule converting such a ledger into the old classification payload
is proved in `petal_squarefree_classification_ledger_soundness`.
