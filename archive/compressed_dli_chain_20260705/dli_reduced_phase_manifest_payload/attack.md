# ATTACK - dli_reduced_phase_manifest_payload

This is now the live DLI leaf.

Needed output:

- enumerate the tuple set: central profiles, nonzero frequencies, DLI
  harmonics, and relevant square-root components;
- compute the Artin-Schreier-reduced local expansion of
  `P_lambda(sigma(y))` for each tuple;
- exhibit a positive pole order not divisible by `p`;
- record a certified upper bound for the reduced polar divisor of that same
  tuple;
- prove the manifest covers every tuple exactly once or by declared disjoint
  row classes; and
- prove the harmonic total of the majorants is `o(t)`.

This is analytic/algebraic local expansion work, not a broad local compute
task.
