# frontier: dli_odd_phase_polar_obstruction_payload

Conditional.

The reduced-phase manifest soundness rule is proved. The remaining work is
`dli_reduced_phase_manifest_payload`: after Artin-Schreier reduction, every
odd-evaluation phase on every relevant square-root component must retain a
positive pole order prime to `p`, with the majorant row recorded for the same
tuple.
