# ATTACK - dli_odd_evaluation_discrepancy

Status: conditional.

The deterministic transfer is proved in `dli_truncated_log_transfer`. The
active mathematical leaf is `dli_peak_mass_discrepancy`.

Likely routes for that leaf:

- a Weil/exponential-sum discrepancy estimate for the odd-evaluation sequence;
- a direct peak-repulsion lemma for low-degree odd polynomials on the
  square-root section;
- a collision-energy proof that rules out peak concentration indirectly.

The seen-coordinate lever is not enough by itself: Dirichlet peaks survive
arithmetic averaging, so the proof must control the geometric mean.
