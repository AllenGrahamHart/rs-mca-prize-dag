# midlarge_h_certification

- **status:** TARGET
- **closure:** proof
- **refs (legacy):** ['experimental/notes/roadmaps/h_window_derivation_audit.md']

## Statement

With H_max = A (audit verdict), the certification pipelines cover: C1a direct MITM h = 4(-5); C1b descent h <= ~8-10 (band saturation). The range h in (~10, A] (A = 67/133/261 per rate) is covered by NEITHER — and the crude route (value_set_partner_cap x C(n,h) cores) reduces back to the terminal bound. ROUTES: (a) a large-h emptiness/exclusion THEOREM (first moment empty by thousands of bits; h-1 obstruction conditions overwhelm the C(n,h) pattern entropy above h ~ some explicit H0 — find the worst-case tool); (b) a scalable certificate valid at all h (the descent is not); (c) consumer-side: resolve the h <= A vs 2h <= A micro-lemma and check whether large-h trades enter R_PTE at full weight; (d) per-h budget arithmetic: 261 x ~1e5 << n^3 fits, so per-h bounds suffice — no aggregation cleverness needed. GAP PINNED (micro-lemma 5/5): h <= A reading PROVED (2h <= A wrong) — the certification gap is the full (10, A] with A = 67/133/261; its (100, A] tail additionally lies outside the frozen W3 grammar window at rates 1/4 and 1/8 (nonempty both), empty at 1/16. FRONTIER UPDATE (C1b proved + route-a autopsy): the gap is now h in (6, A] — C1b certifies h = 5, 6 (bottom census 2^38 at n=1024), C1a h = 4. Route (a) is DEAD (m1_trade_bijection: a bijection, not a bound). Live assets: the ~3.3e3-active-cores-per-h budget target (quantified), the M2 lever at extension rows, the first-moment margin (thousands of bits). This node is now the single largest open item on the (A) side.

## Attack surface

the Fable C1b brief carries this as its second part: prove the injection lemma (h <= 10) AND map/attack the large-h routes

## Falsifier

a trade found at h in (10, A] at any calibration row with q >= n^3 (none ever observed)

## Ledger

C1a VERDICT SHARPENS THE SPLIT: direct MITM is h=4-only (h=5 measured ~2.2yr infeasible). The descent (C1b) must carry h = 5..~10; the gap above ~10 unchanged.
