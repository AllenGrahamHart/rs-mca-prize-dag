# PRO THREAD T5 — "M720-CONDUCTOR" (fresh window)

*Self-contained. A structural conductor/height bound. Brute-force census is
infeasible (exponential MITM: 73/79 deployed cases hit the probe ceiling), so a
STRUCTURAL proof is required.*

## Setting
Official-shape primitive rows F_q, q=p or p^2, n | q-1. K=Q(zeta_n), O=Z[zeta_n].
For an anchored 2h-support R subset mu_n(K), C_R(X)=prod_{x in R}(X-x); S_R = the
monic deg-h polynomial matching the top h coefficients of C_R; E_R = S_R^2 - C_R;
O_j(R)=[X^j]E_R (1<=j<=h-1). The cleared algebraic integers (banked x83 denominator
control) are A_j(R) = 2^{4h-2} O_j(R) in O.

## What is proved (black boxes)
- **P4-HB structural theorem:** a non-toral anchored survivor => all A_j(R) ≡ 0
  mod P (P|p) => (char-0 candidates are toral/paid, X24) some A_j != 0 with
  p | N_{L/Q}(A_j). If |N(A_j)| <= B^{[L:Q]} < p, a nonzero integer < p can't be
  divisible by p => NO survivor.
- **Verified crude height:** log2 B_h — h=7: 71.9, h=13: 167.3, h=20: 290.7. So the
  gate closes any official case where the nonzero obstruction sits in a field L with
  291*[L:Q] < log2 p.
- **Census evidence:** every case that exhausted (n=16,32 h=7) had ZERO survivors;
  73/79 larger cases incomplete (probe ceiling) — no counterexample anywhere.

## The ask (target: m720_conductor_compression)
> Prove EITHER (compression) every non-toral anchored cleared obstruction A_j(R)
> relevant to h in [7,20] lies in a SMALL subfield L with 291*[L:Q] < log2 p (so
> p ∤ N_{L/Q}(A_j) at official scale), OR (certificate) the C2 per-row
> gcd(p, D(n,h)) = 1 for the official prime values, where D(n,h) is the product of
> ideal norms of the cleared obstructions over anchored non-candidates.

- **(A)** The compression lemma. Levers: (i) the anchored-core obstruction actually
  lives in the field generated by the SPECIFIC 2h anchored roots, not the full
  Q(zeta_n) — bound [L:Q] by the support size, not phi(n); (ii) a toral/non-toral
  dichotomy forcing non-toral obstructions into a low-conductor subfield; (iii) print
  the D(n,h)-GCD certificates for the official rows directly.
- **(B)** an official case with p | D_pt(n,h): a genuine bad-reduction prime with a
  non-char-0 anchored survivor whose cleared obstructions all vanish mod P.
- **(C)** conditional on a clean [L:Q] bound for the anchored-core norm.

Downstream: m720 manifest -> midlarge norm-gate column -> the mca_safe staircase.
