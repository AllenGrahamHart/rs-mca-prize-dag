# Attack

1. Use LS6 exactly; do not replace its guarded split-divisor set by every
   vector in the ambient linear slice.
2. Preserve first ownership while summing over source, touched triple,
   defect `a`, and cross-ratio `lambda`.
3. Test max-to-mean, quotient/field-drop, and collective split-divisor routes
   against the explicit Johnson-negative tail.
4. A per-cell polynomial whose exponent grows with `ell` is not a payment.
