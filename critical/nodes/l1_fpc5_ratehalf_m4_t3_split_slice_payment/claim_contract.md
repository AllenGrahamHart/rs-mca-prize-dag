# Claim contract

- **Input:** the exact Johnson-nonpositive rate-half `M=4,t=3` LS6 atoms.
- **Output:** one aggregate first-owner polynomial/profile payment.
- **Must preserve:** source, triple, `a`, `lambda`, split divisor, exactness
  guard, and multiplicity.
- **Nonclaim:** each fiber has size at most two; linear dimension implies a
  split-divisor count; an unguarded prefix-flatness theorem applies.
