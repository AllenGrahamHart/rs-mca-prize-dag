# Lineage

This theorem extracts the reserve-independent structural argument from the
proved local node `pma_sigma_one_first_layout_domination`. That earlier node
also depends on sigma-one paired-core normalization and three finite payment
formulas. None of those ingredients is used here.

The only algebraic input is the core-defect argument already represented by
`l1_core_defect_reduction`, rewritten after subtracting an arbitrary source
base polynomial `Q`. The result applies at every petal size and leaves all
one-layout payment questions open.
