# Conditional exact-list first-match assembly

- **status:** CONDITIONAL
- **closure:** proof

## Statement

At every official `x4` row and transported quotient row, fix one
support-wise first-match priority map on each full locator-prefix fiber.
Partition its actual list supports into

```text
paid background | quotient/dihedral | structured moment/U2 | primitive star.
```

Conditional on the wired inputs:

1. the paid theorem handles bound their declared classes;
2. `u2c_exact_slice_extras_budget` gives `U_MT<=n^3` after expansion and
   coalescing;
3. `x4_primitive_star_u1_coverage` gives the direct local bound
   `U_prim<=16n^3`; and
4. `x4_exactlist_summed_budget` proves the one list-side integer sum is at
   most `B*=floor(|F|/2^128)`.

Then every exact list at the row has size at most `B*`. The same statement
transports only to quotient rows explicitly included in the summed table.
QA.22's MCA slope arithmetic is evidence; it is not a per-word list bound.

The historical quotient, dihedral, and moment-trade counterexamples remain
load-bearing: they show why the classes must be separated before the
primitive residue is counted.

## Attack surface

Close the three typed leaves separately: structured-column population,
the local primitive general-star budget, and the list-side summed budget.
The quotient and dihedral terms retain their exact staircase formulas.

## Falsifier

A support omitted or multiply owned by the priority map, a primitive star
record outside the `u1` ledger, or a consumed row whose complete list-side sum
exceeds `B*`.

## Ledger

The node remains a conditional assembly theorem. The August 2026 ownership
audit replaces the stale `PrimitiveExactList<=n^2` prose and prevents two
unsupported conversions: null-generator census to arbitrary primitive prefix,
and MCA per-pair slopes to LIST per-word members.
