# Full staircase supports determine their codewords

- **status:** PROVED
- **role:** numerator reduction for petal G3

In the petal route, G2 classifies the contributor's full agreement set `S`,
the E22 canonical support is `R=S`, and the profile parameter is
`A=|R|>=k+sigma>=k`. For a fixed received word `U`, at most one degree-`<k`
Reed-Solomon codeword agrees with `U` on a fixed canonical support `R`.

Hence G3 has no residual codeword multiplicity inside one full support class.
Its only remaining numerator is the number of G1 charts charging the same
canonical support/codeword, in aggregate across the quotient profile.

