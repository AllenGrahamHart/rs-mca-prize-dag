# Claim contract

- **Claim:** the post-strip exact-slice residual consumed by `x4` is at most
  `N^3` on every official row.
- **Quantifier:** all four rates and every admissible official field/domain.
- **Depth:** with agreement numerator `A` and code dimension `K=rho N`,
  `t_XR=A-K` comes from the exact corridor equation, not the full-subset
  F2 balance depth.
- **Counted object:** only non-coset-union blocks and unpriced trade families
  remaining after the named first-match strips.
- **Near-tail pin:** the paid widths are `15,14,13,12` at rates
  `1/2,1/4,1/8,1/16`; the target begins immediately beyond them.
- **Nonclaims:** no generated-field guard override; no transport through
  `f1/ext`; no claim that the guarded F2 mass target is false.
- **Falsifier:** a certified official post-strip residual larger than `N^3`.
