# Claim contract

- **Claim:** the heaviest post-strip exact locator-prefix fiber consumed by
  `x4` is at most `N^3` on every official row.
- **Quantifier:** all four rates and every admissible official field/domain.
- **Depth:** with agreement numerator `A` and code dimension `K=rho N`,
  `t_XR=A-K` comes from the exact corridor equation, not the full-subset
  F2 balance depth.
- **Counted object:** distinct residual supports/list members in each full
  elementary-prefix fiber after the named first-match strips; take `max_z`.
- **Null-route evidence:** the paid widths `15,14,13,12` at rates
  `1/2,1/4,1/8,1/16` apply only after a max-to-null bridge is supplied.
- **Nonclaims:** no generated-field guard override; no transport through
  `f1/ext`; no claim that the guarded F2 mass target is false.
- **Falsifier:** a prefix `z` with a certified official post-strip fiber
  larger than `N^3`.
