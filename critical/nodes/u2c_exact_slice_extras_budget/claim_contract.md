# Claim contract

- **Claim:** the heaviest structured moment/null pullback bucket is at most
  `N^3` actual list supports on every official row.
- **Quantifier:** all four rates and every admissible official field/domain.
- **Depth:** with agreement numerator `A` and code dimension `K=rho N`,
  `t_XR=A-K` comes from the exact corridor equation, not the full-subset
  F2 balance depth.
- **Counted object:** distinct supports assigned by first match to the
  structured non-quotient moment/U2 pullback bucket in each full
  elementary-prefix fiber, after every staircase expansion and coalescing;
  take `max_z`.
- **Null-route evidence:** the paid widths `15,14,13,12` at rates
  `1/2,1/4,1/8,1/16` apply only after maximum-prefix transport and an exact
  expansion ledger are supplied.
- **Nonclaims:** no generated-field guard override; no transport through
  `f1/ext`; no claim that the guarded F2 mass target is false.
- **Nonclaim:** a bound on the primitive star-PTE residue owned by `u1`.
- **Falsifier:** a prefix `z` with a certified official structured bucket
  larger than `N^3`, or a staircase expansion missing from the count.
