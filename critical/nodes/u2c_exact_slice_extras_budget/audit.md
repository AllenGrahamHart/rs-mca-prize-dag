# Audit

1. The target copies the `u2c/x4` count and budget; it does not invent a new
   asymptotic surrogate.
2. The depth is the proved `xr_radius_arithmetic` corridor depth.
3. `f1/ext` prices the MCA safe-side extension stratum. No theorem routes
   guard-rejected generating exact-slice rows through that chain.
4. The exact-value F2 diagnostic under `(T*)` is evidence of difficulty, not a
   contradiction and not a guarded consumer obligation.
5. No numerical experiment is promoted to proof.
6. The lower-rate near-tail overreach is removed: each rate uses only the
   width proved by `b2b_near_tail_bound`.
