# Audit

1. The target uses the exact locator-prefix map realized by admissible
   polynomial boundary words and retains the `x4` residual budget.
2. The depth is the proved `xr_radius_arithmetic` corridor depth.
3. `f1/ext` prices the MCA safe-side extension stratum. No theorem routes
   guard-rejected generating exact-slice rows through that chain.
4. The exact-value F2 diagnostic and near-tail theorem concern the null/p-free
   route. They are evidence, not a maximum-prefix theorem.
5. No numerical experiment is promoted to proof.
6. Raw mode-at-null is explicitly fenced by the proved `F_17` counterexample.
   A strip-aware exchange-compression theorem is not ruled out, but must be
   stated and proved.
