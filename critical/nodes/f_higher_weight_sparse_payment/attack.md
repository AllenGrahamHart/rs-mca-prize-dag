# Attack

1. Start from the exact higher-weight output of `f_sparse_rank_split`; do not
   re-prove the rank or weight partition.
2. Compile the syzygy-flat transport into actual locator-section membership
   and retain the support-wise first owner.
3. Reuse the exact coefficient atlases in `xr_highcore_collision_count` only
   after proving their received-word and owner transport for this flat.
4. A local circuit classification is useful only if its support embeddings
   and cross-core multiplicity are summed in the same currency.
5. Either obtain one absolute polynomial payment or exhibit a counterfamily
   and narrow `f_many_sparse_structure` accordingly.
