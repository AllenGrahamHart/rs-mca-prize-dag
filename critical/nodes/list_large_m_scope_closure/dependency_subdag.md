# Dependency sub-DAG

```text
list_adjacency_closing [CONDITIONAL; consume m=1 instance] ---+
                                                              +--> list_large_m_scope_closure [CONDITIONAL]
list_subsqrt_interleaving_collapse [PROVED] ------------------+
```

Supporting alternatives, not required premises:

```text
list_codegree_closed_form -----------+
list_interleaved_support_census -----+--> large-arity diagnostics and alternate bounds
list_extension_interleaving_identity +
```

No new open proposition was introduced.
