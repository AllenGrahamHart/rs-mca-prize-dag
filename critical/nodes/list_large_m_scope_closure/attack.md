# Attack disposition

The omitted arbitrary-arity scope is conditionally closed. The only open
truth content consumed here is the base (`m=1`) adjacent crossing already
carried by `list_adjacency_closing`.

The proved sub-square-root projection theorem transports the safe side to
every `m`; diagonal embedding transports the unsafe side. No separate
large-arity envelope or adjacent search remains.

The earlier routes remain useful outside this implication:

1. `list_codegree_closed_form` gives a finite-depth general upper ledger;
2. `list_interleaved_support_census` gives exact support and average anchors;
3. `list_extension_interleaving_identity` identifies interleaving with an
   extension-field ordinary list while preserving the base denominator.

They are evidence edges, not logical premises of this node.
