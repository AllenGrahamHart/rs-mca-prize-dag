# conditional proof: rate_half_band_closure

- **status:** CONDITIONAL (decomposed 2026-08-09, user-directed;
  plan notes/band_decomposition_plan_20260809.md)
- **gate:** all

## Premises

- `rate_half_band_structural_surplus` — the K3/workboard arm: the
  enumerated structural-supply cap at the deployed rows (the
  positive-433 board completes in distinct affine slopes, with
  independent review).
- `rate_half_band_crossing_location` — (RH-AC): the located adjacent
  crossing a_RH(q) at every admissible row with n = 2^41, k = 2^40,
  q prime, 2^167 < q < 2^256 (child pose as WIDENED 2026-08-10;
  round-30 F6 propagation — this line previously said "razor row"
  only), within the proved bracket [k + 2^34, 3n/4].

## Claim

Conditional on the two children, the rowwise adjacent certificate of
(RH-ADJ) holds at every admissible prime-q row in 2^167 < q < 2^256
with the crossing located (round-30 F4 FLAG, unresolved: rows with
q = p^e, e >= 2, inside the item-13 admissible family and inside
this range — including exhibited razor-slice instances — are covered
by NEITHER child; resolution surfaced), and the enumerated arm
certifies the absence of unaccounted structural supply at the active
deployed KoalaBear row (n = 2^21, F_(p^6), p = 2130706433 — the
singular row of the K3 manifests; round-30 F5 correction: the K3
chain touches no other deployed row, so the former plural "deployed
rows" is narrowed to this exhibit row).

## Proof

The two children partition the node's open content exactly as the
round-27 rebuild established it. Child 2 supplies the located
crossing: B_mca(a_RH) <= B* < B_mca(a_RH - 1) with a_RH pinned.
[ROUND-30 F1 PROPAGATION of the banked round-28 P0 correction,
which this file had missed: the former sentence here — "the far-CA
half is discharged unconditionally at razor rows by the Hankel
layer (B* >> n), and the child's content is the S_sparse
localization" — is FALSE. The Hankel layer's scope is r < 2^39,
i.e. a > 3n/4 only; on the open bracket [k+2^34, 3n/4) the far-CA
term is BINDING and S_sparse is dominated. THE CHILD'S OPEN CONTENT
IS THE FAR-CA CROSSING; both halves of B_mca = max(B_ca^far,
S_sparse) remain the child's obligation.] Child 1 supplies the
enumerated supply cap at the active deployed KoalaBear row in K3's
unit of account (an exhibit-scoped certificate under
official_row_primes_pinning; the WP5 no-transport verdict stands —
round-30 F2/F3: child 1 serves no consumer bar of this node, and
its gate-all req status vs an ev-grade role is a SURFACED
coordinator/user decision, recorded in statement.md). Together they give the
consumers what they consume from this node: adjacency_closing takes
the located adjacent pair (both halves of RH-ADJ); mca_safe takes
the safe half AT THE LOCATED INDEX (round-28 quantifier audit,
2026-08-10: the named premise-weakening follow-up onto HD1 is
RETIRED AS UNSOUND — HD1 is an upper bracket END at 3n/4, and
B_mca is nonincreasing, so it bounds nothing at the crossing; the
consumer's a_safe is the shared crossing symbol, not a free
choice); list_adjacency_closing no longer consumes this node's MCA
content (owner moved at wave 10).

No random-word quantity appears in either premise (the F3 zero-power
declaration is load-bearing on child 2; the FLOOR v2 history and its
round-27 forced correction are preserved in statement.md).
