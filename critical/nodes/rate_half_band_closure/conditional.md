# conditional proof: rate_half_band_closure

- **status:** CONDITIONAL (decomposed 2026-08-09, user-directed;
  plan notes/band_decomposition_plan_20260809.md; RESTRUCTURED
  2026-08-10, user-delegated coordinator ruling on round-30 F2/F3:
  the K3 arm demoted req -> ev, see below)
- **gate:** all (over the single remaining premise)

## Premise

- `rate_half_band_crossing_location` — (RH-AC): the located adjacent
  crossing a_RH(q) at every admissible row with n = 2^41, k = 2^40,
  q prime, 2^167 < q < 2^256 (child pose as WIDENED 2026-08-10;
  round-30 F6 propagation — this line previously said "razor row"
  only), within the proved bracket [k + 2^34, 3n/4].

## Evidence arm (not a premise — the F2/F3 ruling, 2026-08-10)

- `rate_half_band_structural_surplus` — the K3/workboard arm: the
  enumerated structural-supply cap at the active deployed KoalaBear
  row (n = 2^21, F_(p^6), p = 2130706433), wired as evidence_for
  this node. RULING RATIONALE: the arm is an exhibit-scoped
  certificate under official_row_primes_pinning; the standing WP5
  verdict (2026-07-10, quoted in this node's shard) adjudicated it
  "an ev-edge upgrade, not an amber"; and the round-30 seam hunt
  verified it serves no consumer bar of this node (every consumer
  consumes the located crossing only). It therefore evidences this
  node without gating it. RE-PROMOTION CONDITION, pre-registered:
  a proved parameter-to-carrier bridge + labels-to-slopes conversion
  + row transport theorem (the WP5 (F2) mismatch discharged) would
  restore the req edge — that is the K3 arm's path back to a gate.

## Claim

Conditional on the premise, the rowwise adjacent certificate of
(RH-ADJ) holds at every admissible prime-q row in 2^167 < q < 2^256
with the crossing located. [Round-30 F4 FLAG on the e-axis, RULED
2026-08-10 (user-delegated): the pose STAYS prime-q; blind widening
to q = p^e is RULED OUT (it would assert unaudited transport of the
rounds-27..29 instruments to extension rows — the exact seam shape
this campaign keeps catching); the per-instrument primality-
sensitivity audit is commissioned as a round-31 pilot, after which
the choice (widen with proof vs a separate extension-row child)
gets made on evidence. Until then rows with q = p^e, e >= 2 in this
range — including the exhibited razor-slice instances — are
UNCOVERED and every consumer reading "each admissible row" must
count them so.]

## Proof

The premise supplies the located crossing: B_mca(a_RH) <= B* <
B_mca(a_RH - 1) with a_RH pinned. [ROUND-30 F1 PROPAGATION of the
banked round-28 P0 correction, which this file had missed: the
former sentence here — "the far-CA half is discharged
unconditionally at razor rows by the Hankel layer (B* >> n), and
the child's content is the S_sparse localization" — is FALSE. The
Hankel layer's scope is r < 2^39, i.e. a > 3n/4 only; on the open
bracket [k+2^34, 3n/4) the far-CA term is BINDING and S_sparse is
dominated. THE CHILD'S OPEN CONTENT IS THE FAR-CA CROSSING; both
halves of B_mca = max(B_ca^far, S_sparse) remain the child's
obligation.] That is exactly what the consumers consume from this
node: adjacency_closing takes the located adjacent pair (both
halves of RH-ADJ); mca_safe takes the safe half AT THE LOCATED
INDEX (round-28 quantifier audit, 2026-08-10: the named
premise-weakening follow-up onto HD1 is RETIRED AS UNSOUND — HD1 is
an upper bracket END at 3n/4, and B_mca is nonincreasing, so it
bounds nothing at the crossing; the consumer's a_safe is the shared
crossing symbol, not a free choice); list_adjacency_closing no
longer consumes this node's MCA content (owner moved at wave 10).
The K3 evidence arm strengthens confidence in the structural-supply
side at its exhibit row and gates nothing (previous text read "The
two children partition the node's open content" — under the F2/F3
ruling the open content is the premise's alone, and the arm is
corroboration at a different row).

No random-word quantity appears in the premise (the F3 zero-power
declaration is load-bearing on the child; the FLOOR v2 history and
its round-27 forced correction are preserved in statement.md).
