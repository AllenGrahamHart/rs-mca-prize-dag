# Wave-67 addendum: positive 433-1b cell-4 xi3/xi4 pairing-3/6 closure (2026-08-08)

The first non-fixed matching-exchange orbit is now closed. The direct PROVED
node

```text
rate_half_kb_m2_r4_coordinate_positive_433_1b_cell4_xi3_pairing3_reciprocal_square_exclusion
```

excludes missing `df` at canonical matching 3.

## Sign-free reciprocal-square cut

With `m=df`, `s=(d+f)^2`, `z=1/d`, and `y=z^2`, the missing equation is

```text
M(y)=1+(2m-s)y+m^2y^2=0.
```

The colored pair `C(z)=Pair(bmz,sigma_c cmz)` is quartic. Writing
`C(z)=E(y)+zO(y)` gives the necessary sign-free quartic

```text
K(y)=E(y)^2-yO(y)^2=C(z)C(-z).
```

Dividing `K` by `M` in the exact four-basis source tower leaves a linear
remainder in all eight source-sign/`sigma_c` rows. Its division-free
common-root cut gives

```text
candidate r roots             60
guarded source points         16
common z lifts                 8
antipodal q roots per lift     2
common outside-pair q roots    0
target boundary / witness    0 / 0
free branch / unresolved     0 / 0
```

The independent verifier recomputes every degree-1112 norm root union,
rebuilds all source lifts, and intersects the original quartics in both `z`
and `q`. Final full-census Modal app: `ap-6XSVCn7sZu2uToXIzZ9J3E`.

## Two-transport payment

The separate PROVED composition node

```text
rate_half_kb_m2_r4_coordinate_positive_433_1b_cell4_xi3_xi4_pairing3_6_transport_exclusion
```

uses the parallel-`DE` exchange to pay `xi=3`, matching 6, then uses the
universal outside-role involution to pay both `xi=4` partners. This closes
four labels, two parallel-`DE` quotient orbits, and 64 raw cases.

## New campaign boundary

Cell 4 is now

```text
paid labels                   85 / 105
live labels                   20
paid quotient orbits          50 / 60
live quotient orbits          10
paid raw cases               640
```

The independent live obligation is ten missing-`df` labels in five
matching-exchange pairs: `{4,9}`, `{5,12}`, `{7,10}`, `{8,13}`, and
`{11,14}`. Each direct payment transports to three partners. No complete
cell, route, K3 value, or Prize endpoint closes at this stage.
