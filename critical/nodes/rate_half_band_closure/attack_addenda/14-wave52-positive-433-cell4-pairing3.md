# Wave-52 addendum: positive 433-1b cell-4 pairing-3 closure (2026-08-08)

The first genuinely new matching orbit after the first-pair block now has an
exact proof payment.

## Pairing-3 nested-quadratic theorem

The PROVED node

```text
rate_half_kb_m2_r4_coordinate_positive_433_1b_cell4_de_pairing3_complete_exclusion
```

closes all three parallel-`DE` omissions at canonical matching
`3=((0,2),(1,3),(4,5))`. A FLINT-backed four-basis tower evaluates 32 rows
for `xi in {0,2}`; identical-positive-copy transport supplies `xi=1`.
The exact direct ledger is:

```text
computed rows                 32
transported rows              16
raw cases                     48
candidate r roots            312
guarded source points         272
all (u,v) pairs               816
missing-sum failures          752
missing-sum survivors          64
nonzero colored terminals      96
f=0 target boundaries          16
witnesses / unresolved        0 / 0
```

Every root of the norm numerator, norm denominator, and four inverse-guard
fractions is lifted through the original source equations. The independent
verifier reconstructs every finite quadratic root list and terminal cut.

Final Modal app: `ap-VnOKofCGaEWi6SM8IN26qj`.

## Honest quotient composition

The PROVED composition node

```text
rate_half_kb_m2_r4_coordinate_positive_433_1b_cell4_de_pairing3_6_orbit_exclusion
```

uses the parallel-`DE` involution only at its exact scope. The two newly paid
orbits are

```text
{(0,3),(1,3)},  {(2,3),(2,6)}.
```

Thus the direct 48 cases transport to 16 additional cases at `(xi,pairing)
=(2,6)`, for 64 raw cases across four labels. The involution fixes matching
indices when exchanging `xi=0,1`, so `(0,6)` and `(1,6)` remain open. A
proposed six-label count was rejected before DAG insertion.

Combined with the nine first-pair labels, the cell-4 ledger is now

```text
paid labels                   13 / 105
live labels                   92
paid quotient orbits           8 / 60
live quotient orbits          52
```

The nearest exact action is to run the same nested-quadratic compiler for
the two positive omissions at pairing 6, or prove an independently audited
target-swap transport. No cell, route, K3 value, or Prize endpoint closes at
this stage.
