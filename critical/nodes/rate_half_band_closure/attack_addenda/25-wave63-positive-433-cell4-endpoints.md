# Wave-63 addendum: positive 433-1b cell-4 endpoint closure (2026-08-08)

Both source-fixed endpoint roles are now paid before any matching equation is
imposed. The PROVED node

```text
rate_half_kb_m2_r4_coordinate_positive_433_1b_cell4_xi5_xi6_endpoint_compatibility_exclusion
```

closes missing `bf` (`xi=5`) and missing `sigma_c cf` (`xi=6`) across all
15 canonical matchings and all four target lanes.

## Source-only theorem

Let `m` be the missing product, `s` its squared-sum record, and put `x=b` at
`xi=5` or `x=c` at `xi=6`. The signed second endpoint is `m/x`, so every
guarded target must satisfy

```text
(x^2+m)^2-s*x^2 = 0.
```

The exact four-basis norm and complete exceptional-root lift give

```text
computed source/role rows          8
compatibility roots               40
candidate r roots                 56
r-guard terminals                 40
t-guard terminals                  8
no-base-field-b terminals          8
guarded source points              0
compatible / unresolved          0 / 0
paid labels                       30
paid quotient orbits              18
paid raw cases                   480
```

The independent verifier reconstructs every norm and inverse-guard root,
solves every base quadratic, and checks every no-`b` discriminant. Final
Modal app: `ap-e2M3bp83ckyLVeO9wqmAtd`.

## New campaign boundary

Together with the 45-label parallel-`DE` layer, cell 4 is now

```text
paid labels                   75 / 105
live labels                   30
paid quotient orbits          42 / 60
live quotient orbits          18
```

The live labels are exactly missing `df` and missing `sigma_o ef`,
`xi in {3,4}`, across all 15 matchings. No complete cell, route, K3 value,
or Prize endpoint closes at this stage.
