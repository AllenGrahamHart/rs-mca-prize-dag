# Wave-65 addendum: positive 433-1b cell-4 xi3/xi4 pairing-0 closure (2026-08-08)

The first independently live `df` matching is now closed. The PROVED node

```text
rate_half_kb_m2_r4_coordinate_positive_433_1b_cell4_xi3_pairing0_reciprocal_square_exclusion
```

uses the three exact `paired(q,q)` branches and a reciprocal-square
resultant in `y=1/d^2` to exclude missing `df` at canonical matching 0.

## Exact direct census

For `q=de`, `m=df`, and `s=(d+f)^2`, the target equations reduce to

```text
q = B_i/A_i,
1 + (2m-s)y + m^2 y^2 = 0,
paired(-q, sigma_o q m y) = 0.
```

The division-free resultant of the last two quadratics is normed through
the exact cell-4 source tower. Complete exceptional-root replay gives

```text
branch rows                    24
candidate r roots             200
guarded source points          72
empty q-branch points          24
reciprocal-square candidates   64
final paired evaluations      128
target boundary / witness     0 / 0
free branch / unresolved      0 / 0
```

The independent verifier reconstructs all 200 candidate roots from the
printed norm and guard polynomials, then rebuilds every source and target
lift from the pinned structure and kernel files. Final full-census Modal
app: `ap-ymGV72om2LOXCwxR8zKKlr`.

## Transport payment

The separate PROVED corollary

```text
rate_half_kb_m2_r4_coordinate_positive_433_1b_cell4_xi4_xi3_pairing0_transport_exclusion
```

applies the universal outside-role involution to pay the corresponding
missing `sigma_o ef` label at matching 0. It introduces no duplicate
computation.

## New campaign boundary

Cell 4 is now

```text
paid labels                   77 / 105
live labels                   28
paid quotient orbits          44 / 60
live quotient orbits          16
paid raw cases               512
```

The independent live obligation is the 14 missing-`df` labels at matching
indices 1 through 14, grouped into eight matching-exchange orbits. Each
future `df` payment transports to the corresponding `ef` label. No complete
cell, route, K3 value, or Prize endpoint closes at this stage.
