# Wave-64 addendum: universal positive 433-1b xi4/xi3 transport (2026-08-08)

The PROVED node

```text
rate_half_kb_m2_r4_coordinate_positive_433_1b_universal_xi4_xi3_outside_role_transport
```

shows that missing `sigma_o ef` and missing `df` systems are exactly
bijective at fixed common role cell, source sign, target lane, and canonical
matching.

## Universal signed involution

For a target lane `(sigma_c,sigma_o)`, set

```text
(D,E,F)=(sigma_o e,sigma_o d,f).
```

This transposes exactly product/squared-sum atlas rows 3 and 4. The other
five rows, compact residual order, all 15 matching indices, target guards,
and both sign-cycle invariants are fixed. The outside map acts trivially on
the five common roles, so a complete role-cell census verifies that all 15
source role cells are fixed.

The primary sparse-polynomial certificate covers

```text
15 role cells * 4 source signs * 4 target lanes * 15 matchings
= 3,600 exact system bijections.
```

An independent integer audit replays 31,104 coordinate/lane rows without
importing the primary implementation.

## Cell-4 consequence

Wave 63 left 30 live labels in 18 quotient orbits at `xi in {3,4}`. The
universal transport pairs every `xi=4` label with the same matching at
`xi=3`. Hence only 15 labels in nine matching-exchange orbits require an
independent exclusion proof. A complete `xi=3` close will then transport to
all `xi=4` labels.

No cell-4 label is promoted by the transport alone, and no complete cell,
K3 value, or Prize endpoint closes at this stage.
