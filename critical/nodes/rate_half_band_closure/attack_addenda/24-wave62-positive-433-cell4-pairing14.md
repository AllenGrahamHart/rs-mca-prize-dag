# Wave-62 addendum: positive 433-1b cell-4 pairing-14 closure (2026-08-08)

The two retained positive-`DE` omissions at matching 14 are now paid by an
exact common-`f` exclusion. This completes every parallel-`DE` omission in
cell 4.

## Matching-14 theorem

The PROVED node

```text
rate_half_kb_m2_r4_coordinate_positive_433_1b_cell4_positive_de_pairing14_complete_exclusion
```

closes `xi in {0,1}` at `14=((0,5),(1,4),(2,3))`. For computed omission
`xi=0`, the first two paired cuts and final colored cut are

```text
P_b(f)=Pair(-de,bf),
P_c(f)=Pair(de,sigma_c*cf),
final=Pair(df,sigma_o*ef).
```

The division-free quadratic resultant is normed over the four-basis source
tower, and every norm or inversion-exception root is lifted through the
original equations. The exact ledger is

```text
computed rows                 16
transported rows              16
raw cases                     32
candidate r roots            152
target roots                 120
guarded source points          80
nonboundary quartic rows      128
nonzero colored terminals     128
target boundaries               0
witnesses / unresolved       0 / 0
```

The independent verifier recomputes both scalar quadratic root sets, their
intersection, every even-quartic root through `u^2`, and the final colored
cut. Final Modal app: `ap-pw5ZlqWXn640JjkVYlv221`.

## Complete parallel-DE layer

Wave 61 supplied `(0,11),(1,11),(2,11),(2,14)`. The direct theorem adds
`(0,14),(1,14)`, closing all six labels in
`{0,1,2} x {11,14}`. Therefore every one of the `3*15=45` parallel-`DE`
labels is now proved empty. The cumulative cell-4 ledger is

```text
paid labels                   45 / 105
live labels                   60
paid quotient orbits          24 / 60
live quotient orbits          36
```

The live labels are exactly the four nonparallel missing roles
`xi in {3,4,5,6}` across all 15 matchings. No complete cell, route, K3 value,
or Prize endpoint closes at this stage.
