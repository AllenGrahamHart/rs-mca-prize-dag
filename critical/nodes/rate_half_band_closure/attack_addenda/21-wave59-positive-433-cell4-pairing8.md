# Wave-59 addendum: positive 433-1b cell-4 pairing-8 orbit (2026-08-08)

The parallel-`DE` matching-8 orbit is now paid by an exact degree-eight
exclusion and the previously proved quotient involution.

## Matching-8 theorem

The PROVED node

```text
rate_half_kb_m2_r4_coordinate_positive_433_1b_cell4_de_pairing8_complete_exclusion
```

closes `xi in {0,1,2}` at `8=((0,3),(1,5),(2,4))`. The four-basis
compiler evaluates `xi in {0,2}` directly and transports `xi=1` from the
identical positive-`DE` copy at `xi=0`. With `u=ef`, its paired quadratics
and final colored cut are

```text
P_u(u)=Pair(de,sigma_o*u),
P_f(f)=Pair(second_de,sigma_c*cf),
Pair(df,bf),
```

where `second_de=-de` for `xi=0` and `second_de=de` for `xi=2`. The exact
ledger is

```text
computed rows                 32
transported rows              16
raw cases                     48
candidate r roots            320
target roots                 288
guarded source points         288
all (u,f) rows              1,040
missing-relation failures     960
nonzero colored terminals      64
f=0 target boundaries          16
witnesses / unresolved       0 / 0
```

Every norm, denominator, and inversion-exception root is directly lifted
through the original source and target equations. Final Modal app:
`ap-rpCUgXeECUEAWWOjI2Ma2S`.

## Quotient payment

The direct theorem pays `(0,8),(1,8),(2,8)`. Under the exact parallel-`DE`
involution the first two labels form one orbit, while `(2,8)` transports to
`(2,13)`. Thus four labels, two quotient orbits, and 64 raw sign/lane cases
are paid. Composing with the Wave-58 ledger gives

```text
paid labels                   37 / 105
live labels                   68
paid quotient orbits          20 / 60
live quotient orbits          40
```

The positive-`DE` labels `(0,13),(1,13)` are not supplied by this quotient
action and remain open. Their direct matching-13 exclusion is the next
target. No complete cell, route, K3 value, or Prize endpoint closes here.
