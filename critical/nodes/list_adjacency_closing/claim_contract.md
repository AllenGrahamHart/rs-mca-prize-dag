# Claim contract: base-list adjacent crossing interface

```text
claim id: list_adjacency_base_scope_repair
mathematical statement: the lower-level corridor obligation is only the
  ordinary-code crossing L_1(a0)>B*>=L_1(a0+1); under the official field cap,
  list_subsqrt_interleaving_collapse transports that same adjacent pair to
  every m>=1
quantifiers and row scope: every admissible official row; base arity m=1 in
  list_adjacency_closing and every constant m in list_large_m_scope_closure
consumer and exact slot: list_large_m_scope_closure supplies the arity
  transport required by list_grand
current status: interface repair; the transport implication is proved and the
  base crossing remains conditional on the existing corridor predicates
dependencies already proved: rules_freeze; list_subsqrt_interleaving_collapse
new open content: only the ordinary-code adjacent crossing
falsifier: a row with a base crossing but an arity m whose safe value exceeds
  B* or whose diagonal unsafe witness disappears
proof route being attempted: projection-collision collapse on the safe side
  and diagonal embedding on the unsafe side
replay command: tools/ramguard tiny -- python3
  critical/nodes/list_large_m_scope_closure/verify.py
upstream hard-input mapping: complete profile-envelope comparison with the
  target; arity transport is no longer an independent analytic input
```

The repair weakens an open child to the exact premise consumed by the proved
transport theorem. It does not promote the base crossing or any mathematical
leaf.
