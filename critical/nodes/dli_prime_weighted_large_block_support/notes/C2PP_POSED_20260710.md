# CONJECTURE C2″ — POSED (2026-07-10, fresh-context amber audit)

Successor of the refuted C2 (`dli_level_factorization`); the WP4 crux Y2;
sibling of Face 1's TR (the K2 joint-reserve clause — K2 duplication catch
#35: cite BOTH `rigidity_kernel` AND `rk_rigidity_kernel`). Posed by the
2026-07-10 audit with fresh calibration data; ZERO adversarial rounds so far
— per the node-pinned F-round rule it is NOT promotable to a wired predicate
until it survives its own rounds (M1 below).

## Setting (all proved/pinned)

The b2b nested tower at an admissible row (q prime, q ≡ 1 mod n, n = 2^s;
official schedule 34 levels, Σ_j L_j = t, 33 junctions); junction j
conditions level-(j+1) skew variables on the state-dependent domain
G(state_{<j}); tower total = Σ_states skewcount(G(state)) (TEST-1 exact, 8
rows). At each junction, partition level-state profile classes into:
COSET classes (the packet's QuotientStaircase/coset column: k = 0 and full
mu_M-coset dressings), ACCIDENT classes (noncoset classes whose
class-conditional mean exceeds mean-field by a factor > theta = 2), and
BULK (the rest).

## Clause (i) — coset routing (definitional)

Coset-class contributions are routed through the packet's exact coset
accounting at every junction; they are budget arithmetic, never
correlation. (Exact at every replayed row; the q = 60289 dressing histogram
is exact — replay: ../f2b_replay_perclass_20260710.py and the audit's
bweak-exp1 slice.)

## Clause (ii) — bulk reserve (the conjecture proper)

At every official prize row, under the generated-field normalization
(catch #13 pin), the coset-routed, accident-decomposed joint loss satisfies

    E_U[ prod_j rho_j ]_reduced  <=  2^R_joint * prod_j E_U[ rho_j ],
    R_joint = 21 bits

(the catch-#40-corrected 2^121/2^100 slack; display convention:
geometric-mean junction allowance 2^(21/33) = 1.554 (last digit corrected
at M4 banking, catch #164) — the aggregate form is
primary; NO per-junction uniform constant is asserted).

## Clause (iii) — accident pricing (counted, absolute)

Accident-class mass is charged at its exact weights against the same
21-bit reserve, with the frequency model the proved q-independent window
law (expected mass C(N,w)·2^(w−N−L−1) per window; Poisson over
orbit-dressing quanta). No accident is absorbed into a uniform constant,
and no charge scales with iid mass (this dodges window-law catch #11).

## PRE-REGISTERED FALSIFIER (fixed at pose time, before any new data)

C2″ is refuted by any of:
- (F-a) sustained super-polylog growth of the COSET-STRIPPED,
  ACCIDENT-PRICED junction ratios: after clauses (i)+(iii) with exact
  accounting, a residual bulk trend exceeding C·log q per junction for
  every C, sustained across >= 3 increasing q-scales at >= 2 depths of the
  packet's nested toy towers;
- (F-b) a replayable tower whose total clause-(ii)+(iii) charge,
  transported by the packet arithmetic, exceeds the full 21-bit reserve;
- (F-c) an accident census (>= 30 rows at fixed shape) whose exceedance
  frequencies reject the clause-(iii) Poisson window law at p < 10^-3.

NOT falsifiers: raw unstripped conditional ratios (F2b's 4.25x/8.40x — the
coset column is exact accounting); single orbit-quantized spikes consistent
with the exact window mean (the 6.6x/9.4x classes); failures of per-level,
per-junction-uniform, or factorized proxies.

## Survival against the exact record that killed the old amber
## (demonstrated at pose time — calibration: ../c2pp_calibration_20260710.py)

- vs F2b: C2″ asserts nothing about the raw ratios that fired (GM 2.139);
  the bulk object it constrains measures 0.998 / 1.010 / 0 / 0 / 1.033 /
  1.066 / 0.760 / 0 at the 8 exact rows (GM 0.967 < 1, the anti-correlated
  direction), with the four accident classes orbit-quantized (masses
  128/128/64/128, exact multiples of 32) and window-priced. At the four
  sparse rows the stripped conditional mass is IDENTICALLY ZERO — the
  three-part shape is what the exact data does, not a modeling choice.
- vs the antipodal-collapse landmine: posed on the proved
  nested-conditional structure, not shared cells.
- vs BWEAK-EXP-1's 43.9x artifact: clause (i) routes the dressing before
  any trend can be read (replayed exact at q = 60289).
- vs the C1 normalization flaw: clause (iii) charges absolute exact
  weights.
- A naive UNIFORM coset-stripped per-junction constant is ALREADY REFUTED
  by the banked record (stripped junction ratios 2.752 at t=2, q=8353 and
  2.874 at t=4, q=97 vs the 1.534-1.566 allowance) — the three-part
  counted form above is the only surviving shape; posed accordingly.

## Honest gaps at pose time

No multi-junction joint measurement beyond t=4/n=32 (n=64 towers need the
queued sharded-MITM redesign — M1); theta = 2 is a pose-time convention
(results insensitive for theta in [2,4] at the 8 rows); the clause-(iii)
Poisson model has 8-row + 35-job support, not a 30-row census.

## Path to predicate status (M-ledger, ordered by cost)

M5 constant re-pin 2^122 -> 2^121 (catch #40 — banked as a statement
addendum, maintainer confirmation owed) < M6 pro_floor1 artifact
(catch #41 — repaired) < M3 ENDPOINT-EXC posed as a certificate rule <
M2 C1′ re-pose with level-scaled w_max (Finding C: as written it
degenerates to bare uniform-K at 33 of 34 production levels) + one
adversarial round < M4 the assembly verifier (exact-rational) < M1 the
C2″ adversarial round (F-a/F-c on fresh n=64 sharded-MITM rows; Modal).
After M1-M5: standard surgery (petal template) — predicates
{C1′-re-posed, C2″, ENDPOINT-EXC}, green skeleton (A1-PROD sieve, moment
transfer, Lemma 1/D2/D3, the dual kills), pins P-CONS/P-FIELD/P-ROWS.
