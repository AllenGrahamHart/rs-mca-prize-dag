# Claim contract - PMA rate-half core triple-excess reduction

## Proven

1. A fixed full three-petal PMA layer injects into the printed core RS list.
2. The core-list Johnson denominator is exactly the preceding `J`.
3. Three tail contributors force an equality bonus at least
   `2(K_0-1)+3a`.
4. Removing their common factor gives a nonconstant rational map with three
   disjoint core fibers of total size at least `2r+3a`.
5. Absence of such a source-coupled certificate pays the whole tail by `8n`.

## Still open

Classify or price the source-coupled positive three-fiber certificates. The
arbitrary smooth-core property is false, so the PMA CRT word, first-match
layout, and natural-scale profile must remain in that theorem.

## Consumer effect

Use this node as theorem evidence for `petal_mixed_amplification`. It does not
become a logical requirement of `imgfib`, and it creates no new conjectural
critical leaf.
