# QUALITY CERTIFICATE (red-quality program, charter r2, 2026-07-10)

## Red: ww_lower_witnesses (W2)

Q-SUFF: slot W2 of the worst_word amber (KB #19). Scope-exact: planted
lower witnesses are FREE and PROVED (planted_count = M, exact); the
predicate covers challenger-column spends only, and constructive-by-
necessity is forced (exclusion-type kernels cannot supply lower witnesses —
WP7 verdict).

Q-SURV: DOSSIER — toy exemplars exist and replay (the E15 gate, k = 2/4/8,
PASS). QUEUED: (1) the SPEND-MAP CHECK — enumerate at which official rows
the consumer's unsafe side actually spends the challenger column; W2 may be
VACUOUS at some rows (that would shrink the obligation — cheap, decisive,
run first); (2) an E15-successor construction attempt at the first
spending row (falsifier: an unsafe row where every explicit family
undershoots the spent value).

Q-MIN (annotation): conditional-scope minimality — the obligation exists
only where the spend map says so; the spend-map check IS the minimality
refinement.

## EXECUTION LOG (2026-07-10, the spend-map trace — outcome of record)
- EXECUTED: full spend map computed exactly (one spending cell per
  clean-rate row, t_u = n/N*-1; n-uniform per rate; vacuity thresholds
  T* = 5/13/29 n-independent). The vacuity hope INVERTED (catch #49):
  non-vacuous at every clean-rate official row — but the spend is
  covered by the PROVED qcore staircase count with margins
  +75.15/+7.23/+40.82 bits. Falsification yield: the rate-1/2 scope
  hole (catch #53 — unscoped W2 is falsified-pending there; clean-rate
  pin applied) and the receiver contradiction (catch #50 — a
  receiver-local W2 makes W1^W2^W3 unsatisfiable; own-receiver pin
  applied).
- CERTIFICATE VERDICT: Q-SUFF re-certified under the repaired statement
  (scope + receiver pins); Q-SURV EXECUTED (this trace + QA.22 27/27 +
  E15 gate + corridor verifiers post path-fix). Honest open residue:
  the endpoint-conventions seam (WP-0.1/F1, #493-adjacent) + the X-4
  taxonomy identification. WATCH NUMBER: rate-1/8's 7.23-bit coverage
  margin.
