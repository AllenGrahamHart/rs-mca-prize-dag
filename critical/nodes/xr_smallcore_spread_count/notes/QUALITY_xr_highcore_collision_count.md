# QUALITY CERTIFICATE (red-quality program, charter r2, 2026-07-10)

## Red: xr_highcore_collision_count (P-A)

Q-SUFF: slot P-A of the xr amber (KB #24): high-core stratum <= 8n^3, with
8 + 8 = 16 exact (M6 composition, three independent implementations).
POSEDNESS pin (catch #42) inside the statement: slope count, NOT the
collision moment (the moment can sit at ~n^6 while the floor holds — np1:
moment 130,370 vs 98 rays); quantifier = per-pair sup (consumer-forced).

Q-SURV: DOSSIER — E27 remainder at FM-normalized rates, all classifiers at
chance, no fifth mechanism (executed census, banked); rung-2b forcing
replayed EXHAUSTIVELY at banking (4,662 pairs, 0 violations, t=3 control);
the np1 moment-vs-count datum. EXECUTED: the E27 census + the audit's
exhaustive forcing replay (both banked). QUEUED: E27/P9 high-core census
at scaled official-like rows (falsifier: post-strip high-core count >
8n^3 transported, sustained; FM-band-consistent counts and moment values
alone do NOT refute).

Q-MIN (annotation): SPLIT FREEDOM — the 8/8 partition of the consumer's
16n^3 is a chosen reserve; any (a,b) with a+b <= 16 works. Re-balanceable
without re-surgery (recorded as a pin; re-run the banked composition
script on re-balance).

PROVED ALL-ZERO BRANCH (2026-07-13):
`xr_affine_core_all_zero_charge` pays full-domain selector ranks
`4,4,4,11,11,10`. In particular it closes the catch-#158 RowC `1/16`
rank-four family without assuming collision-line coverage. Every open RowC
selector now has rank at least five.

PROVED POST-STRIP PENCIL CHARGE (2026-07-13):
The strip core cap `kappa=k` bounds each kernel-basis pencil by
`floor(R/h)`. Combined with the cogirth basis count, this pays selector ranks
`4,4,4,15,15,14`; every remaining prize selector has rank at least
`16,16,15`.

PROVED RANK-FIVE EXTENSION-LIST REDUCTION (2026-07-14):
After puncturing `P_0`, every surviving RowC rank-five selector is a
distinct-quotient-class list in a five-dimensional extension of its
four-dimensional polynomial kernel. The exact parameters are length
`R+4+u+v`, agreement `4+h+u+v`, and pair cap `4+u+v`. At `u=v=0` the kernel
is `GRS_4`; this identifies the missing theorem but does not pay the list.

PROVED BASE REUSE CORE (2026-07-14):
A peeling count on four-bases shows that any P-A counterexample at `u=v=0`
contains a nonempty subfamily with minimum collision-line degrees
`125,123,31` out of `126,126,35` possible bases. Per-line populations are
still capped by `153,179,320`; the resulting near-saturated core is not yet
paid.

PROVED RICH-LINE HIERARCHY (2026-07-14):
The peeling charge extends to every line threshold `r>=2`. The exact minimum
3-rich-line degrees are `123,120,27`, and positive forced cores persist
through line populations `74,40,9`. This excludes pair-only models but does
not count the surviving representable arrangements.

PROVED RATIO/MINOR DICTIONARY (2026-07-14):
Each rich collision line is a heavy-fiber problem for `z/(q-I_Tq)`, and each
fiber collision is a maximal-minor zero of the single global rank-six lift.
Optimized thresholds force `23560,12600,228` external zero-minor records per
retained member. The aggregate zero-minor census remains open.

PROVED DIVIDED-DIFFERENCE CLIQUE BRIDGE (2026-07-14):
The `r=2` deficits `1,3,4` force `[S]q!=0` on every selected five-set.
Consequently every retained agreement block is a distinct-color
monochromatic clique for `Phi(S)=[S]U/[S]q`, and the ratio/minor records are
its equal-color stars. Bare five-set packing remains above budget; the open
content is the coherent split-pencil clique census.

PROVED DIRECTION STAR CONFIGURATION (2026-07-14):
The cubic interpolation points `I_Tq` are the vertices of a simple
four-dimensional hyperplane arrangement. Degree-`d` evaluation has exact
minimum distance `C(4+h-d,4)`. In particular the `r=2` reused directions
are not contained in a hypersurface through degrees `4,4,2`; turning this
local rigidity into a global rich-point count remains open.

PROVED `u=1` SPLIT-LOCATOR NONBASIS BOUND (2026-07-14):
The one-defect quartic kernel has at most `42,42,20` nonbasis four-sets per
official agreement block and hence at least `168,168,50` bases. This imports
the sharp locator-flat mechanism with an exact XR source bridge. The current
high-core aggregation still does not cross budget.

PROVED SPLIT-PENCIL RANK/PLUCKER CERTIFICATE (2026-07-14):
For the shared GRS-four census, a block family forces the stack
`[H_i|-gamma_i H_i]` to have a ninth kernel direction beyond the cubic-pair
kernel, with noncubic second projection. Five-face colors on every six-set
are fractional-linear in the omitted coordinate and therefore either all
distinct or all equal. This is the correct coherent rank-defect interface;
the aggregate count remains open.

PROVED MAXWELL-CORE EXTRACTION (2026-07-14):
Every P-A base counterexample contains a dimension-minimal block core of size
at most `308,359,640`. Proper subfamilies are Maxwell-sparse, each block has
at most `2,2,1` private coordinates, and the full parity stack has an
anomalous left dependence beyond row overdetermination. The next task is the
bounded circuit/degeneration classification, not further global packing.

PROVED DUAL-TRADE CORE (2026-07-14):
Every anomalous left relation is a bounded dual-product-code trade. Nonzero
block words have locator form and weight at least five; every active
coordinate carries at least three slopes with explicit dual-Vandermonde
coefficients. The open classification is now the primitive `(5,3)` trade
residue after quotient/tangent charging.

PROVED TRADE-RANK LOCALIZATION (2026-07-14):
Rank one is excluded by the block intersection cap. Rank two is supported on
at most `12,12,9` active coordinates. This is a bounded-chart route, not yet
a payment; an exact first-match audit must show where those charts enter the
existing strip/tangent ledger.
