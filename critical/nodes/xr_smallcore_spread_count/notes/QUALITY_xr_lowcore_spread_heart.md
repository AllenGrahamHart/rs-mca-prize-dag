# QUALITY CERTIFICATE (red-quality program, charter r2, 2026-07-10)

## Red: xr_lowcore_spread_heart (P-B)

Q-SUFF: slot P-B of the xr amber (KB #24): low-core stratum <= 8n^3
(singletons included), same exact composition. Sup-form pin declared
(toy evidence is mean-flavored; evidence strength != predicate strength).

Q-SURV: DOSSIER — E27: the spread remainder sits exactly in the FM band
(unstructured mult >= 2 events at FM-normalized rates, classifiers at
chance) — K2 clause (ii) holds empirically for the remainder; validity-
automatic + strip arithmetic proved around it. EXECUTED: the E27 census
(banked). QUEUED: E27 spectrum scan at scaled rows (falsifier: post-strip
spread remainder over the transported budget, sustained); the
f2_effective_energy_dichotomy transfer (portable verbatim per its
statement) as a structural attack; SCK adjacency.

Q-MIN (annotation): same split freedom as P-A. This is the
anti-concentration heart — the axis-Q instance of K2 Face 3; a kill here
is a kill of the K2 clause at this face (maximum information).

PROVED BRANCH (2026-07-13): `xr_all_lineray_affine_core_bound` pays the slope
target whenever there exists a one-per-slope selector of affine error rank at
most three. The open anti-concentration family must therefore have rank at
least four for every selector. The P9 collision jobs test P-A rather than this
low-core stratum, but confirm that high-transversal-rank post-strip families
can occur in the shared LineRay object.

PROVED COGIRTH BRANCH (2026-07-13):
`xr_affine_core_cogirth_ray_bound` pays full-domain selector ranks through
`4,4,3,11,11,10` on the six rows. Hence a P-B counterexample has minimum
selector rank at least `5,5,4,12,12,11`, respectively. No collision premise is
used in this branch.

PROVED ALL-ZERO STRENGTHENING (2026-07-13):
`xr_affine_core_all_zero_charge` charges every nonpersistent zero for each
kernel basis and replaces the final factor `n-s+1` by `r+1`. It pays
full-domain selector ranks `4,4,4,11,11,10`; hence a P-B counterexample now
has minimum selector rank at least `5,5,5,12,12,11`. No collision premise is
used.

PROVED POST-STRIP PENCIL CHARGE (2026-07-13):
`xr_poststrip_affine_pencil_charge` uses the low-core cap `kappa=k-1` to
bound every kernel-basis pencil by `floor((R+1)/(h+1))`. It pays selector
ranks `4,4,4,15,15,14`, so a P-B counterexample has minimum selector rank at
least `5,5,5,16,16,15`.

PROVED RS COMMON-ROOT WEIGHTING (2026-07-13):
`xr_rs_common_root_basis_charge` counts nonloop zeros rather than assigning
the same cogirth minimum to every error. Global RS common roots are bounded by
`k-a`, persistent ones localize every basis, and each remaining common root
can be consumed at only one selected slope. The resulting endpoint envelope
pays selector ranks `4,4,4,16,16,14`, so a P-B counterexample has minimum
selector rank at least `5,5,5,17,17,15`.

PROVED RS FLAT-NULLITY REDUCTION (2026-07-13):
After global-root factorization, `xr_rs_flat_nullity_basis_charge` bounds
every proper rank-`j` flat by `j+u` and combines the resulting ordered-basis
count with persistent-root subset packing. At RowC rank five the possible
P-B deficiency pairs collapse from `32,131 / 7,875 / 1,891` to
`8 / 49 / 274`, with maximum `(u,v)` coordinates `(7,1)`, `(29,2)`, and
`(60,6)`. This is a residual classification, not a rank payment.

PROVED RANK-FIVE EXTENSION-LIST REDUCTION (2026-07-14):
Puncturing `P_0` turns every surviving RowC rank-five selector into a
distinct-quotient-class list in a five-dimensional extension code. Its exact
parameters are length `R+4+u+v`, agreement `4+h+u+v`, and P-B pair cap
`3+u+v`. The `u=v=0` kernel is `GRS_4`. This is a precise primitive-list
target, not a payment of the low-core count.

PROVED BASE REUSE CORE (2026-07-14):
At `u=v=0`, the P-B cap three prevents any four-basis reuse. Unique-basis
packing gives exact upper bounds `116548571,215520801,1021697885`, all below
`8*1024^3`. Thus this base pair is independently paid; the listed P-B
flat-nullity residual already begins outside it.

PROVED `u=1` SPLIT-LOCATOR NONBASIS BOUND (2026-07-14):
For the one-defect quartic kernel, a four-set is a nonbasis exactly when its
split quartic locator lies in the kernel hyperplane. A triple-completion count
and the sharp split-cubic locator line theorem bound nonbases in an official
agreement block by `42,42,20`, improving the kernel-basis floors to
`168,168,50`. The independent packing/basis minimum remains above budget, so
the `u=1` residual is narrowed but not paid.

PROVED `(u,v)=(0,1)` GRS DEFECT REDUCTION (2026-07-14):
The loop coordinate is an agreement for at most one slope. Removing that
possible exception from a family above budget leaves at least `8n^3` regular
members with GRS-four kernel, block sizes `10,10,8`, and pair cap four.
Peeling forces `209,207,66` reused directions; the blocks are coherent
divided-difference cliques, direction rigidity holds through degrees `5,5,3`,
and optimized zero-minor floors are `97650,52530,1782`. This deficiency pair
now lands on the shared split-pencil census but is not paid.

PROVED SPLIT-PENCIL RANK/PLUCKER CERTIFICATE (2026-07-14):
The one-loop residual uses the same matrix `[H_i|-gamma_i H_i]` as the
high-core base case. A genuine family gives an extra kernel class with
noncubic second projection. On every six-set its defined five-face colors
are either pairwise distinct or all equal. Paying the resulting projected
rank-defect census is still required.

PROVED MAXWELL-CORE EXTRACTION (2026-07-14):
Every one-loop P-B counterexample contains a dimension-minimal block core of
size at most `256,299,480`. Proper subfamilies are Maxwell-sparse, each block
has at most `2,2,1` private coordinates, and the full parity stack has one
more left dependence than ordinary overdetermination explains. Classifying
these bounded anomalies remains open.

PROVED DUAL-TRADE CORE (2026-07-14):
The one-loop anomaly also yields a bounded dual-product-code trade. Every
active block word has weight at least five, every active coordinate carries
at least three distinct slopes, and both coefficient directions have exact
low-degree locator forms. The primitive trade classification remains open.

PROVED TRADE-RANK LOCALIZATION (2026-07-14):
Rank one is impossible and rank two uses at most `13,13,10` active
coordinates. The one-loop branch may discard it only after these bounded
charts are matched to an existing paid first-match cell; that audit remains
open.


> [w10-X2 CORRECTION 2026-07-18 (forced): the `16,16,15` prize ranks above are stale — the audited frontier since wave-8 is `17,17,15`; the line stands as history, this note is the correction of record.]
