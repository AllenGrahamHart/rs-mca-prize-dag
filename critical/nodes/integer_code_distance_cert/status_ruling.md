# Status ruling

`TARGET`. Exact finite search is a total decision procedure, and the C-4 toy
certificate validates a format. Neither fact is the row-specific certificate
consumed by `lattice_cone_certificate`. The banked execution report contains
no official or adopted exhibit-row matrix, full support bound, allowed
cyclotomic-relation basis, or replayable full-row certificate.

The node may return to `PROVED` only after that literal certificate is banked
with a deterministic checker, or after the consumer is formally narrowed to
a proved procedure-totality statement that no longer claims row injectivity.

The order-128 branch with `p>253^32` is now paid analytically by
`integer_code_distance_high_field_folded_box_exclusion`. This does not repair
the missing row registry or certify lower-field/other-order rows, and it does
not prove that the resulting quotient value set exceeds `B*`. The `TARGET`
ruling therefore remains unchanged.
