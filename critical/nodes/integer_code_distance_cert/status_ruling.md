# Status ruling

`TARGET`. `official_row_primes_pinning` proves that the challenge supplies no
finite official-prime registry. Exact finite search is a total decision
procedure, and the C-4 toy certificate validates a format. Neither fact is a
uniform collision-free theorem or an exhibit-scoped certificate consumed by
`lattice_cone_certificate`. The banked execution report contains no pinned
consumer row, full support bound, exact cell count and budget, allowed
cyclotomic-relation basis, or replayable full-row certificate.

The node may return to `PROVED` only after a family-uniform theorem covers all
rows assigned to this route, or after a literal exhibit certificate is banked
with a deterministic checker and every consumer is formally narrowed to that
same exhibit. A procedure that may return a collision does not establish the
no-vector conclusion.

The order-128 branch with `p>253^32` is now paid analytically by
`integer_code_distance_high_field_folded_box_exclusion`. The four pinned Proth
prize exhibits have 167--171-bit primes and are below its threshold. The
256-bit corridor prime is a reproducibility exhibit with no lattice-route
value-set consumer. Thus the theorem does not cover the universal row family
or prove that a consumed quotient cell exceeds `B*`; the `TARGET` ruling
remains unchanged.
