# Claim contract

- **Claim:** one disjoint list-side column sum is at most `B*`.
- **Rows:** every official or quotient row actually consumed by `x4` and
  `TR`.
- **Units:** list members per received word and full prefix, after coalescing.
- **Inputs:** actual bounds from the structured and primitive leaves; their
  proposed `n^3` and `16n^3` allowances are not assumed proved here.
- **Nonclaims:** QA.22 supplies the list table; equal numerical thresholds
  identify MCA and LIST currencies; six candidates cover 29 or quotient
  rows.
