# QUALITY CERTIFICATE (red-quality program, charter r2, 2026-07-10)

## Red: f2_growing_order_myerson (the parity summit)

Q-SUFF: slot = the SOLE open input of f2_conditional_close (KB #16-#17;
consumer arithmetic machine-verified: ANY 2^{o(n)} alignment bound flips F2
"with astronomical room"). The statement's tolerance 2^{1.05e12}
(equivalently per-condition extras <= 2^15 on the p-free ladder) is the
calibrated consumable form. Quantifier: max-to-mean over the Frobenius
tower's moving sectors at official rows — matches the consumer's contract
axis (B-RESOLVED, satellite 30: holds at every mid-band size b).

Q-SURV: DOSSIER — the F2 campaign record (114 cycles, 2026-07-09/10):
alignment verified at 9.4k-44M states, 21+ rows, ZERO anomalies; sign field
certified pseudorandom by five independent test families (Walsh,
multiplicative-character, component spectra, Zolotarev, CF-depth); the
pre-registered kill line (per-orbit constant > 10 at 3+ scales) NEVER
approached (observed band ~[0.03, 3]); deep-regime exactness THEOREM frames
the far corner (1247/1247 primes exact at n=8). EXECUTED (this program,
2026-07-10): fresh W3 transition-band octave on Modal — populations to
~8e9 (200x prior watch frontier), 13 primes, pre-registered reads —
OUTCOME: see EXECUTION LOG below. STANDING WATCH: low-frequency octaves
(campaign posture, banked scripts in this directory).

Q-MIN (annotation): the consumer accepts any 2^{o(n)}; the summit's stated
tolerance leaves ~2^{1.05e12} room — attackers have essentially unlimited
slack; only the SHAPE (sub-exponential alignment) is load-bearing.

## EXECUTION LOG
- 2026-07-10 W3 transition octave (f2_watch_w3_transition_modal.py): PENDING — fill on completion.
