# Proof — petal_growth (full-petal extras poly(n) uniformly in c)

Discharged 2026-07-13 on the maintainer's P1 floor confirmation
(conditional.md tail). The conditional packet (conditional.md, retained
as the assembly of record) proves the claim from eleven wired
hypotheses, all PROVED:

- **Band scope (pin P1, now fixed):** this theorem is the top-defect band
  `d >= ell(M-2)` (floor form, layout-anchored per catch #168). The old
  prose claim of a complete off-band induction is not consumed. The floor
  band is the widest proved atlas form: it dominates the
  narrower (t-1)M scan reading, and any wider reading is refuted by
  counting (C(63,32) = 2^59.67 > (121/128)·128^6 = 2^41.92).
- **On-band payment (the G1/G2/G3/K4 predicate split, all PROVED):**
  G1 clause (P) — the word-independent, layout-anchored floor-band
  atlas (petal_g1_layer_maps/proof.md; rigidity census n^4/96 against
  the n^6 budget, margin 2^88.5; empty band at rates <= 1/4); G2
  support forcing; the G3 pricing compiler (charge exactly once,
  aggregate inflation 1 <= 719; petal_g3_pricing_multiplicity/proof.md)
  fed by the proved conversion identity and full-support injectivity;
  K4's per-chart m+1 primitive bound; the small-scale staircase census
  gate (COL/clause (D) route) for the periodic branch.
- **Suppliers:** the four E22 dictionary/cofactor nodes, the v13
  planted lower count, and the fiber-locator saturation node (all
  PROVED; G4/G5 pins in conditional.md).

Assembly: exactly the conditional packet's, with every predicate now a
theorem — no step is re-proved here; conditional.md sections G1-G5 +
the r1 history are the proof body, this file is the artifact of record
for the status flip.

## Standing scope and tripwires

- The proved claim is the top band only. Mixed-petal and below-top are
  SEPARATE obligations: the #171 wide-minus-floor lift
  mass (~2^59.67 at (128,64)) is this node's open below-top ledger
  line, untouched by this promotion. The exact residual is now named
  `l1_full_petal_fpc5_payment` after the proved band/root composition.
- TRIPWIRE (P)-3 / re-surgery criterion 4: a future re-resolution of
  the band constant re-opens the chain (pre-registered).
- The six re-surgery criteria of the 2026-07-10 amber design remain
  live as demotion triggers.
