# QUALITY CERTIFICATE (red-quality program, charter r2, 2026-07-10)

## Red: petal_g1_layer_maps (G1)

Q-SUFF: slot G1 of the petal amber (KB #20, route of record link 1).
Clause (iii) (joint chart control) is pinned INSIDE the statement because
the audit showed the assembly FAILS without it (Finding D: the C(k-1,d)
chart union destroys the budget) — the wired form is the assembly-
sufficient one, quantified over the floor's band (pin P1).

Q-SURV: DOSSIER — upstream proves the concrete-word injection and the
distinct-label residual-free charts (with the joint form) UNCONDITIONALLY,
and states the compiler reductions become theorems exactly when the supply
exists; F4-A1 7-rung ladder + F4-A2 c-sweep saw no coverage failure at any
rung (top-band counts exactly the binomial column — consistent with full
chart coverage). EXECUTED: the F4 ladder/c-sweep scans (banked, 2026-07-07).
QUEUED: a direct chart-coverage search at the sweep cells (falsifier: a
top-band contributor covered by NO chart, or super-polynomial chart
multiplicity).

Q-MIN (annotation): upstream's own framing makes G1 the named missing
supply — the compiler is written to consume exactly this.

## ATLAS RE-POSE (2026-07-12, catch #79)

The r1 clause (iii) form was UNSOUND as an assembly input (overlap+K4 do
not imply the total line — N disjoint singleton charts). Re-posed as the
first-match atlas with the TOTAL census clause |A_U| <= n^{b1}; four
falsifiers pinned; the degenerate one-chart-per-codeword atlas forbidden;
F4 scans honestly downgraded (they do not test arbitrary-word coverage).
Q-SURV queued run updates to: atlas-coverage search at sweep cells against
the four pinned falsifiers.