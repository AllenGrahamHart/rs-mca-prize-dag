# QUALITY CERTIFICATE (red-quality program, charter r2, 2026-07-10)

## Red: petal_k4_primitive_bound (K4)

Q-SUFF: slot K4 of the petal amber (KB #20, the kernel). Stated at the
band-restricted chart level the chain consumes (pin G5 makes the
primitive notion identical to G2's aperiodic class — no gap between the
branches); the honest-scope clause records that the row-level upstream
form is neither consumed nor implied (a future row-level proof needs the
band-restriction bridge first).

Q-SURV: DOSSIER — v13_capf_planted_lower_count fixed the target's correct
form (the quotient-periodic branch MUST be charged separately — charge
proved NECESSARY, exact column match); F4-A2b: below-top accidents decay
with p (4->0, 2->0) while only the exactly-priced staircase column
persists — the primitive part has never shown growth; the floor's budget
sits 9.5 bits under n^6 with the primitive family never observed above
the binomial column. EXECUTED: the F4-A2b two-regime scans (banked).
QUEUED: chart-family census growth scan over the auxiliary lists at
scaled rows (falsifier: super-polynomial aperiodic lists above the
reserve).

Q-MIN (annotation): the kernel of the kernel-basis program (K4* pool with
G2); band-restricted = exactly the consumed geometry, deliberately not the
stronger row-level form.

## CLOSED (2026-07-13, wave-4): the chart-level K4 is PROVED

Codex's close (retained-zero factorization -> CRT touched-set injection ->
EXACT tail collapse Sigma C(m,t) = m+1) audited link-by-link: valid,
row-agnostic, 4 verifiers with live mutation controls. Landed as a package
with G1's weighted-atlas re-pose (catch #117: either piece alone breaks the
ledger; together (64/63)(121/128) = 121/126 < 1 exactly). The row-level
form lives on in the successor red (the census gate's lemma). Certificate
CLOSED — the second red retired by proof.
