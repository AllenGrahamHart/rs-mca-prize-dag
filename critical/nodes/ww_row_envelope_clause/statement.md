# Direct worst-word challenger envelope

- **status:** TARGET
- **consumer:** `worst_word_challenger_pricing`

For an official-convention planted-sunflower cell `c`, let `N_chal(c)` be the
actual number of non-planted degree-`<k` codewords agreeing with its received
word on at least `k+sigma` points. The field is the generated field of the cell;
base-valued extension strata are transported to their base row by the proved
beta-normalization bridge.

Let `B_chal(c)` be the exact conservative residual integer budget obtained from
the row target

```text
B*=floor(q_line/2^128)
```

after subtracting every already-paid planted, tangent, quotient/staircase, and
other column in the consumer's QA.22 convention. If #493 offers multiple
allowance branches, use their minimum unless and until the convention is
resolved.

The binding claim is

```text
N_chal(c) <= B_chal(c)
```

for every official row and all its cells, layouts, scalar choices, and words.
This direct statement retires the undefined intermediate `K_cell`, the
heuristic `C*K_cell/q^sigma` law, and all unspecified polynomial factors.


---

## IMPORT REPAIRS (2026-07-12 master banking; cluster audit)

R1 (binding semantics): B_chal(c) is quantified PER RECEIVED WORD and PER
FIRST-MATCH CELL — the row-level target defines the allowance, but the
asserted inequality N_chal(c) <= B_chal(c) holds at each cell of each
word's first-match decomposition; the aggregate/mean reading is DEAD (the
banked adversarial layout-sup datum stands). R2 (certificate obligation,
promoted from attack.md into the binding statement): a proof of this node
must supply an INDEPENDENTLY CHECKABLE upper certificate per official row
(complete cell descriptor; exact B* and every paid-column subtraction;
soundness across layouts/scalars/words) — the consumer's word 'certified'
discharges HERE; a nonconstructive proof of the bare inequality does not
close the node.
