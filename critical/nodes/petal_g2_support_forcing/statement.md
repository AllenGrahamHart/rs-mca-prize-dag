# petal_g2_support_forcing

- **status:** see `dag.json` (single source of truth)

Every nonempty agreement support `S` on an official power-of-two cyclic domain
is either aperiodic (`c(S)=1`) or is a union of complete fibers at the dyadic
scale `c(S)>=2`, hence is a strict staircase with empty tail at that scale.

This is a structural partition only. Pricing all periodic scales, including
`c(S)<=t`, belongs to G3 and the P3 quotient-profile emission.
