# Pro brief v6 (same thread) — the hereditary closure-entropy bound (L3)

*Your round-5 critique was adjudicated and banked: gaps 1-5 accepted (gap 3 as
the standout — QF.12's own Non-claims say exactly what you said); your descent-
stability formula VERIFIED (1800/1800 random checks over F_17) and banked as a
proved lemma; the audit flag on f_many_sparse_structure's w>=3 Face-4 routing
is now sharpened in the ledger. The four-lemma decomposition is adopted as the
node's structure. This brief asks for L3, with L2 as a secondary target.*

## Confirmed inputs you may use as proved
- **L1 (stability, now PROVED):** descent through a forced divisor ell_A
  convolves the syndrome: S^A_m = sum_r e_r S_{m+r}; M_S(ell_A g) = M_{S^A} g.
  Every state in the tree is a Hankel kernel (t unchanged, j -> j - |A|), so
  displacement rank <= 2 persists hereditarily.
- **gcd-strip and multiplicative scale recursion** (both proved).
- **The constant-threshold weight-2 inverse (E35 restatement, proved):** if a
  post-strip flat carries >= 4 DISJOINT minimal weight-2 dual supports (the
  MATCHING count), it is contained in a projective pullback X^e g(X^M) — i.e.
  PAID/routed. Consequence you should exploit:
      EVERY UNPAID STATE HAS MATCHING NUMBER <= 3.
- **QF.12 accounting** (states = saturated root-closures; chains have length
  <= dim P + degree drop; the identity bounds states by generated closed sets
  and nothing more — as you correctly insisted).
- **NOT usable hereditarily:** f_many_sparse_structure's w >= 3 branch (routes
  to wall machinery — your gap 1, confirmed).

## The target (L3)
For rate-1/2 Hankel-kernel flats over mu_n, after gcd-strip and paid
Luroth/rational-pullback routing (multiplicative, Chebyshev/dihedral,
mixed-tail, PTE forms): the number of reachable UNPAID PRIMITIVE saturated
states in the sparse descent is n^{O(1)}.

## A concrete route to consider
At an unpaid state, branching = the number of DISTINCT ROOT-CLOSURES of its
sparse atoms (saturation merges same-closure atoms). Matching <= 3 bounds the
disjoint atoms, but overlapping atoms may be many — near-pencils reach raw 6
with matching 2 — so the question is: bound the number of distinct closures
generated by a weight-<=w atom family with matching <= 3 in a Hankel kernel
(displacement rank <= 2, hereditary by L1). Then compose: bounded closure-
branching x QF.12's short chains (budget drops per edge) => poly states.
The w >= 3 atoms are the residual danger: either (i) prove a Hankel-specific
w >= 3 inverse (displacement rank should forbid many independent w >= 3
exceptional circuits without pullback structure), or (ii) show w >= 3 atoms
at unpaid states collapse into few closures under saturation.

## Deliverables (choose)
- **(A)** Prove L3 (any polynomial exponent; explicit branching/chain bounds).
- **(B)** Break it: an explicit unpaid primitive Hankel descent tree with
  super-polynomially many DISTINCT saturated states — post-saturation, post-
  Luroth-routing. (Your round-5 verdict was that no old-style counterexample
  survives; a genuine one here would kill the descent route for real.)
- **(C)** Conditional: L3 modulo one clean named hypothesis (e.g. "an unpaid
  primitive Hankel state has <= C distinct atom-closures"), with the
  implication proved.
- **Secondary (L2):** the dihedral/Chebyshev REDUCTION at the Hankel level:
  the y = X + 1/X analogue of the multiplicative recursion — an explicit
  syndrome transform mapping a Chebyshev-quotient-structured Hankel state to a
  strictly smaller quotient instance (your stability formula is the template).

Admissibility: no prefix-map fiber counts anywhere (circularity guard).
