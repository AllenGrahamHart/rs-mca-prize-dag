# bsr_findings — BRANCH-SPLIT RE-POSE designer (petal demand structure, post second T-KILL)
# 2026-07-13. Catch ledger continues from #144. All artifacts bsr_-prefixed, this scratchpad.

## TURN 1 — reading pass (verbatim sources)

- critical/nodes/petal_g1_layer_maps/{statement,claim_contract,attack,audit,frontier}.md
  + notes/g1a_findings.md (falsifier theorem F4, #138-#144, PROVED window #143,
  the exact instances) + dag.json node (FALSIFIER FIRED block, the #144 re-pose
  direction: column-priced periodic branch, n^6 primitive branch).
- background/nodes/petal_chart_carrying_descent/: csp_statement.md (Claims 1
  S1-S6 / 2 U0-U2 / 3 L0+C, reading-A pin #129, normalization pin #133, margin
  pin #134, band parity #135), conditional.md (the now-falsified single
  hypothesis wiring), csp_findings.md (#133-#135), ccd_findings.md (#128-#132,
  the #131 fiber-rich census 36,646 @ (64,16,193), volumes table).
- dag.json: petal_small_scale_staircase_census (full history: mint #80, #101
  falsifier re-pose, PROVED reduction, #106 residual, #109/#110, T-KILL #124,
  SUCCESSOR-A re-promotion, #144 second demotion), petal_growth (+
  conditional.md r2: pins P1/G4/G5/P3, 719 allowance, route of record),
  petal_k4_primitive_bound (+statement.md: chart-level close b4=1, package
  ledger note #117), imgfib, dyadic_profile_evaluation (Q_H, QA.22, first-scale
  dominance), intrinsic_scale_geometric_ledger (statement: 64/63 arithmetic).
- critical/nodes/petal_growth/notes/cg_petal_census.py + results JSON (372
  cells; column definition Q_M(A) = C(n/M-1, A/M); B719 = 719*Q; band readings
  edge/own/all; layout fiber_aligned pin) + QUALITY_petal_small_scale_staircase_census.md.
- critical/nodes/petal_small_scale_staircase_census/notes/qrl_packet_20260713/qrl_statement.md
  (T1/T2/T3 suppliers, still true; A >= k+M divisibility; multiplicity-1
  theorem; the refuted successor's residual band).
- critical/nodes/petal_g1_layer_maps/notes/g1a_check3.py (instance arithmetic).

## TURN 1 — the design-driving structural findings (algebra first)

### D1. VERIFY-item of task A(i): B(c*)'s mass is ALL scale >= 2 at the PARENT row
By the g1a theorem's own construction, every class counted in B(c) has
y_nf in S' (the consumed set), i.e. g(y_nf) = 0, i.e. (csp S4) the official
lift support is S = pi^{-1}(S') — K_2-invariant, scale >= 2. NONE of the
falsification mass is aperiodic at the parent row. BUT (D2) the primitive
clause has its own, previously unrecorded exposure.

### D2 (NEW; to be machine-verified as catch #145). THE ODD-LIFT APERIODIC MASS
At any fiber-aligned chart word U (k even), every k'-subset S'' of
H^2 \ {y_nf} has a UNIQUE deg <= k'-1 interpolant g of u1 (NO consistency
condition — interpolation through exactly k' points always solves), and when
g(y_nf) != 0 and agr(g,u1) = S'' exactly, the lift f = (X - x_nf) g(X^2) is an
official contributor with EXACT support S = {x_nf} u pi^{-1}(S''), |S| =
2k'+1 = k+1 = k+sigma, APERIODIC (odd size), FULL-PETAL, and in the
per-member/chart band d >= ell(m-2) ALWAYS (d = 2(k'-1-z), m = k'-z). The
population is therefore ~ C(n'-1, k') realized aperiodic top-band full-petal
classes — Q-FREE (no label-averaging, no field condition; the split point
x_nf is a free agreement and fiber pairs halve interpolation cost, so the
q-suppression that kills generic (k+1)-supports never engages).
CONSEQUENCE: an "aperiodic within (121/128)n^6" clause quantified at the
per-member band is PRE-FALSIFIED by pure counting at rate-1/2 rows n >= 128
(C(63,32) ~ 9.2e17 > (121/128)*128^6 ~ 4.2e12) — for EVERY q. Under the
FLOOR's deep band d >= ell(t_ch - 2) (t_ch = chart petal count) the same
population is capped at sum_{z <= z0} C(k'-1,z) C(t_ch, k'-z), z0 =
k'-1-(t_ch-2): poly at rate 1/2, EMPTY at rate <= 1/4. So the primitive
clause's band pin is LOAD-BEARING and must be the floor band (or carry an
explicit split-point carve-out). This sharpens what is at stake in pin P1's
owed maintainer line. -> measure at (16,8,97), (32,16,97), (32,12,97).

### D3 (to be verified as catch #146). LEMMA COL: the gate's column form is a
### ~10-line unconditional theorem — for ARBITRARY received words
For ANY word U at a 2-power row, any exact scale M >= 2, any A with M | A:
realized exact-agreement classes at profile (M,A) <= C(n/M, A/M)
[stabilizer partition (PROVED petal_g2_support_forcing): S = union of A/M
M-fibers; |S| >= k+1 pins the codeword; classes = supports] and EXACTLY
C(n/M, A/M) = [n/(n-A)] * Q_M(A) (rational identity C(n',h) =
C(n'-1,h) n'/(n'-h)). In the gate's consumed P1-OWN band A = k+M with
M <= t: n/(n-A) = n/(n-k-M) <= n/t = 2n/(n-k) <= 4 at rates <= 1/2.
M <= t is EXACTLY the nondegeneracy boundary (h <= n'-1 <=> A <= n-M <=>
M <= (n-k)/2 = t). Word-free, layout-free, odd-k-inclusive, q-free.
This is catch #101's observation ("classes <= C(N,h) caps the ratio at n/M")
taken to its sharp form: the cap is n/(n-A) <= 4 IN SCOPE, not n/M — so the
719 line holds at ALL row sizes at the consumed band, and the gate's
supplier problem dissolves. (The #101 falsifier "literal excess reachable at
n >= 2^11" lives in the widest-ALL band cells h -> n'-1, which the gate
explicitly does NOT consume — catch #113 pin.)

### D4 (to be verified as catch #147). Clause (ii) closes UNCONDITIONALLY via
### the DD-atlas at the column price
Per consumed profile a' in {k'+1, k'+2}: the DD-consistent minimal-chart
atlas (#143(a), explicitly defined from u1) has cardinality
|A'_{a'}| <= #{a'-supports containing y_nf} = C(n'-1, a'-1)
= [a'/(n'-a')] * Q_2(2a') <= 3-5 x column at rate 1/2 small k', and
<= ~1.002 x column in G1's actual scope n >= 256. It covers every realized
band class (the class's own support is DD-consistent; minimal chart =
(W = S' n core'', P = S' \ core''), a legal reading-A band chart; support
recoverable from (W,P) so charts are distinct). The consumed composition is
CARDINALITY (classes <= |A'| via csp U1 uniqueness), NOT the weighted census
— the weighted form was load-bearing only for the primitive branch (K4 pays
m_chi+1); pricing clause (ii) by weight would need C_col ~ k' (catch #141)
and no consumer needs it. With C_col = 719 the margin in scope is ~700x, and
the g1a floor mass B(c*) ~ column/(4q(k'+1)) sits ~q(k'+1) BELOW the line.
THE PERIODIC HALF OF G1 BECOMES A THEOREM once priced at the column — #144's
"the demand was wrong" in its strongest form.

### D5 (to be verified as catch #148). Ledger fate under the split
The 64/63 factor of the K4 package ((64/63)(121/128) = 121/126) existed to
pay official periodic classes through quotient-row n'^6 atlases (exact-scale
partition -> aperiodic classes at row n/M). g1a makes that supply
IMPOSSIBLE at descended chart words (the quotient-row band class census is
exponential), i.e. the geometric ledger's hypothesis ("scale-M class injects
into a primitive quotient-row family of size <= C(n/M)^6") is now known
UNSATISFIABLE on the petal lane. Under the split the periodic branch charges
the profile clause instead; the primitive branch is current-row only:
(121/128) n^6 < n^6 with NO 64/63 — slack improves 121/126 -> 121/128.
The ledger node stays PROVED but loses its consumer (edge retirement, not
node deletion); the #102 dedup concern dissolves into the exact-scale
partition's disjointness (each class charged once at its own (c(S),|S|)
cell).

### D6. Consumer landing (task D skeleton; exact numbers from bsr_check)
- Allowance uniformity: C_col = 719 = floor(n^6/C(n+6,6)) at all four
  official maximal rows — the SAME constant as the M > t composite's line
  and the floor's integer allowance; one uniform "719 x Q_M(A) per consumed
  profile cell" line across M in [2,t] (gate) and M > t (composite).
- The extension of dyadic_profile_evaluation to M <= t (route (a) of the
  gate's mint) is the one owed consumer-side computation; first-scale
  dominance makes the added mass <= (1+eps) x the M=2 columns (exact sums in
  bsr_check).
- No consumer of the OLD n^6 form survives unfixed: K4 package -> improved
  (D5); census gate -> COL (D3); SUCCESSOR-A -> clause (ii) (D4); the
  ledger edge -> retired (D5).

## PRE-REGISTERED EXPECTATIONS (before bsr_check runs)

- E1: all 372 banked records, all 6 band tags, all profile cells satisfy
  classes <= C(n/M, A/M) = [n/(n-A)] Q exactly; max classes/Q ratio = 4/3
  (the banked (8,3,17) cell); multiplicity 1 everywhere (codewords == classes).
- E2: g1a instances (128,64,65537), (4096,2048,q~2^26), (256,128,2^128),
  (512,256,2^256): OLD form 3*B_floor > (121/128)(2n')^6 REPRODUCED-VIOLATED;
  NEW column form B_floor <= 719 * Q_2(k+2) fits with >= ~30-bit margins
  (margin ~ log2(4*719*q*(k'+1)) + log2((n'-1-k')/(k'+1))).
- E3: odd-lift census: (16,8,97,consec): wide-band aperiodic ~ <= C(7,4)=35
  realized (minus accidentals), floor-band <= 13; (32,16,97,geom5): wide
  ~ <= C(15,8)=6435, floor-band <= 57; (32,12,97,geom5): floor band EMPTY
  (deep band unreachable at rate 1/4: 2(t_ch-2) = 16 > max d = 10).
- E4: scale>=2 banked pins reproduce exactly: 3 / 63 / 47.
- E5: dominance sum_{M<=t} Q_M(k+M) <= (1+2^-10) Q_2(k+2) at s = 13..20 all
  rates; clause-(ii) trivial ratios <= 719 across the official grid with max
  ~ (k'+1)/(n'-1-k') ~ 1.001 at s = 13 rate 1/2; gate COL cap <= 4.
- E6: mutations MC1-MC4 all trip (C_col=1; shifted column; corrupted u1
  breaks S2 identity + the 63-pin; old-budget line fails at g1a instances).

## TURN 2 — bsr_check.py executed (ramguard tiny, ~40 s): ALL CHECKS PASS

Every pre-registered expectation confirmed; deviations from expectation: NONE
material (details):

- E1 CONFIRMED: 514 profile cells across 372 records x 6 tags; zero COL
  violations; the rational identity C(n/M,h) = Q * n/(n-A) exact at every
  cell; multiplicity 1 everywhere; max classes/Q = 4/3; max SUPPORT
  saturation classes/C(n/M,A/M) = 2/3 (the banked tightest cell is 4 of 6
  possible fiber-unions — COL is not even saturated there).
- E2 CONFIRMED at all four instances (s=13 field resolved to q = 33710081):
  old form violated by 54..3976 bits; column form fits with margins 33 / 47 /
  147 / 275 bits. The clause-(D) per-profile caps hold at every instance.
- E3 CONFIRMED and now MEASURED (the #145 numbers):
  (16,8,97,consec):  wide-band aperiodic 24 realized (22 at |S|=k+1, 2 at
    k+3) of supply C(7,4) = 35; floor-band 8 <= cap 13.
  (32,16,97,geom5):  wide-band 6012 of supply C(15,8) = 6435 (93 percent
    saturation!); floor-band 53 <= cap 57 (z0 = 1).
  (32,12,97,geom5):  wide-band 4514 of supply C(15,6) = 5005; floor band
    EMPTY by arithmetic (z0 = -3) and measured 0.
  Every odd lift verified END-TO-END at the official row: |S| = 2|agr|+1,
  aperiodic, full-petal, wide-band membership always. Brute cross-check at
  (16,8,97): total aperiodic top-band full-petal classes = 25, of which 24
  are odd lifts — the aperiodic full-petal top band IS the lift family up to
  one accidental.
- E4 CONFIRMED: banked member pins 3 / 63 / 47 reproduced exactly by the
  fresh independent enumeration (quotient-side, method-independent).
- E5 CONFIRMED: 719 allowance exact at s = 41..44; dominance excess ~2^-691
  (s = 13..20 exact bigint), log2(Q_4/Q_2) < -5.4e11 at maximal rows;
  clause-(ii) trivial ratios max 1.000977 / 1.001955 over the s = 13..44
  grid; gate COL cap max = 4.0000 exactly (rho = 1/2, M = t).
- E6 CONFIRMED: MC1 216 trips, MC2 350 trips, MC3/MC3b trip (corrupted
  census 62 != 63), MC4 = the P2 old-form failures.

Bonus measurement (P4): the DD-atlas is SHARP against realized classes at
generic cells — B = N_DD at (16,8) and (32,16) (every DD-consistent band
support is realized there), while (32,12) shows strict slack (46 < 53 at
a' = 7): N_DD - B counts supports whose interpolant has EXTRA agreements
(they belong to bigger-support classes). Consistent with the L1/first-moment
picture; no consumer depends on sharpness.

## CATCHES #145-#149

- **#145 (ODD-LIFT APERIODIC MASS — the primitive clause's band pin is
  load-bearing; q-FREE).** At any fiber-aligned chart word (k even), the
  S4 dichotomy's OTHER branch — lifts f = (X - x_nf) g(X^2) with
  g(y_nf) != 0, g an exact-k'-agreement interpolant of u1 — supplies
  aperiodic official contributors with |S| = k+1 (and k+3 at |agr| = k'+1),
  full-petal, ALWAYS in the wide/per-member band d >= ell(m-2). The supply
  is Vandermonde-complete: sum_z C(k'-1,z) C(t_ch,k'-z) = C(n'-1,k')
  subsets, each yielding an interpolant with NO consistency condition (the
  split point is a free agreement; fiber pairing halves interpolation cost —
  the q-suppression that starves generic (k+1)-supports never engages).
  MEASURED: 93 percent of supply realized at (32,16,97); every lift verified
  end-to-end. CONSEQUENCE: an "aperiodic within (121/128)n^6" clause at the
  WIDE band is pre-falsified at (128,64) for EVERY q (C(63,32) ~ 2^59.7 >
  2^41.3) — no label averaging, no field condition. Under the FLOOR band
  d >= ell(t_ch-2) the family is capped at poly (993 at (128,64); measured
  53 <= 57 at (32,16,97)) and EMPTY at rho <= 1/4 (z0 < 0; measured 0).
  G1' clause (P) is therefore posed at the floor band, with a pre-registered
  decision-gate falsifier if P1 resolves wide (bsr_falsifiers F(P)-3). The
  owed P1 maintainer line should be requested WITH this table.
- **#146 (LEMMA COL — the gate's column form is a support-count theorem,
  word-free).** For ANY received word, #realized classes at consumed profile
  (M,A) <= C(n/M, A/M) = [n/(n-A)] Q_M(A) (stabilizer partition + supports
  pin codewords + Pascal). At the P1-OWN cells with M <= t the cap is
  n/(n-A_own) <= 4 UNIFORMLY IN n (max exactly 4 at rho = 1/2, M = t), so
  the 719 line holds at ALL row sizes; M <= t is exactly the column's
  nondegeneracy boundary (A <= n-M <=> M <= t). Sharpens #101 (whose n/M cap
  left the falsifier reachable at n >= 2^11 — that excess now lives only in
  the unconsumed widest-ALL cells, i.e. at the #113 seam). Covers odd k,
  all layouts, both fp filters. Verified on all 514 banked profile cells.
  The gate's supplier problem DISSOLVES; the multi-node descent/ESP/QRL
  scaffold is no longer load-bearing for the gate.
- **#147 (CLAUSE (D) CLOSES UNCONDITIONALLY — the column was the right
  demand).** The DD-consistent minimal-chart atlas covers the consumed band
  by construction and its per-profile cardinality obeys
  |A'_{a'}| <= C(n'-1,a'-1) = [a'/(n'-a')] Q_2(2a') <= 1.002 Q in G1's
  scope (grid max 1.00196) — so C_col = 719 holds with ~350x margin against
  the trivial bound and 2^33+ against every g1a instance. The consumed
  quantity is CARDINALITY (uniqueness pays 1 class/chart); the weighted
  census is NOT consumed on this branch (#141 honored — a weighted form
  would need a k'-sized factor, and pricing it constant was exactly the
  falsified mistake). The second T-KILL thereby converts to: periodic half
  PROVED at the column price, primitive half still red but now correctly
  scoped (#145).
- **#148 (LEDGER RETIREMENT).** The intrinsic_scale_geometric_ledger's
  petal-lane hypothesis (scale-M classes inject into C(n/M)^6 quotient
  families) is unsatisfiable post-g1a; the edge to the census gate retires;
  the K4 package's 64/63 rider is deleted — primitive-only coefficient
  121/128 < 1, an IMPROVEMENT over 121/126; the #102 dedup dissolves into
  the exact-scale partition's disjointness. Node-local notes owed at surgery
  (node-local-notes rule).
- **#149 (ALLOWANCE UNIFORMITY + THE ONE OWED LANDING).** 719 =
  floor(n^6/C(n+6,6)) exactly at s = 41..44, and the SAME 719 works as the
  single profile-clause allowance across the M > t composite, the gate's
  M <= t cells, and clause (D)'s atlas. First-scale dominance makes the
  M <= t extension of dyadic_profile_evaluation cost one M = 2 column +
  2^-691 (exact, s = 13..20; log2(Q_4/Q_2) < -5.4e11 at maximal rows). The
  single remaining consumer-side item is the QA.22 re-run with the extended
  cells and the 719 multiplier against imgfib's reserve (gate mint route
  (a)); everything else in the landing is pre-verified.

## STATUS: DESIGN-COMPLETE (ready for surgery)

Deliverables (this scratchpad):
- bsr_g1prime_statement.md   (A: G1' two-clause re-pose; clause (P) TARGET at
                              the floor band with the #145 pin; clause (D)
                              proposed PROVED, column-priced cardinality)
- bsr_successor_rewire.md    (B: 3-C re-wiring; the conditional dissolves)
- bsr_gate_repromotion.md    (C: Lemma COL + supplier map + re-promotion,
                              conditional only on the D3 landing item)
- bsr_consistency.md         (D: exact consumer arithmetic; old-form consumer
                              audit — K4 package improved, ledger retired,
                              nobody orphaned)
- bsr_falsifiers.md          (E: live falsifiers with executability status;
                              dead ones declared dead and re-posed)
- bsr_check.py               (verifier, ramguard tiny, ALL CHECKS PASS:
                              372-cell replay, 4 g1a instances, 3 fresh
                              censuses incl. the odd-lift measurement, grid
                              arithmetic s = 13..44, mutations 4/4)

Surgery order suggested: (1) mint G1' (both clauses; clause (D) with
audit-pending flag), (2) rewrite SUCCESSOR-A conditional.md, (3) mint Lemma
COL as its own small node (supplier of the gate; consumer petal_small_scale_
staircase_census), (4) gate re-promotion + petal_growth r3 route-of-record
edit + ledger edge retirement + node-local notes, (5) queue the
dyadic_profile_evaluation M <= t extension (the one owed landing, D3/#149),
(6) request the P1 maintainer line with the #145 table attached.

Design gaps: NONE named beyond the two explicitly owed items that are
pre-existing house debts, not design holes: (a) the D3/#149 QA.22 landing
re-run (computation in a PROVED node's machinery), (b) the P1 maintainer
line (now a decision-gate with quantified stakes). Fresh-context audit of
clause (D) + COL owed per house law before any PROVED status lands.
