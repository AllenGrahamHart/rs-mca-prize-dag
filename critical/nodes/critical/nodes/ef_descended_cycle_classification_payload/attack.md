# ATTACK - ef_descended_cycle_classification_payload

This node is now conditional. The live EF leaf is
`ef_descended_cycle_inventory_payload`.

The descent node proves that a pole-free full orbit descends to a `B`-defined
cycle in the pole complement. The soundness node proves that an exhaustive
classification into base, tower, or noncontainment-degenerate cases excludes
hidden leakage.
The inventory soundness node proves that a complete verified inventory gives
the classification payload.

Needed output:

- classify every `B`-defined horizontal cycle in the extension-pole complement;
- show base-descended cycles are paid;
- show non-base cycles are proper-subfield/tower-confined, or else
  noncontainment-degenerate;
- leave no non-base, non-tower, nondegenerate pole-free cycle.

The banked pole arithmetic prices cycles that meet the pole divisor; it does
not by itself classify pole-free cycles.
