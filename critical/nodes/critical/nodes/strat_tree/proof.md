# proof

Direct from stratification_partition_thm (PROVED): the executable first-match tree terminating in the residual leaf L is a partition of the pair space; the dedup convention is the nine-cell family {T0..T7, L}. Fuzz record wp2_3 section 4 reproduced exhaustively by the leaf's verify.py.
