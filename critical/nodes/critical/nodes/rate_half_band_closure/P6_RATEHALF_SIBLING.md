# PRO WINDOW P6 — "RATEHALF-SIBLING"

*Self-contained. THE rate-1/2 battlefield node. The AQB route was REFUTED; a
genuinely NEW covering mechanism (or a bracket-grade verdict) is needed.*

## Setting
Prize grand challenge at rate 1/2, razor slice log2 q in (255.900, 256). The
MCA/list determination is PROVED clean at rates 1/4, 1/8, 1/16 (margins < -121).
At rate 1/2 there is a residual band of radius 2,978,147 between
    M_max = 2^33   and   sigma* = 8,592,912,738,
i.e. 2^33 < sigma <= sigma*, that no currently-priced mechanism covers: quotient
windows and integrality both fall short, and the AQB (averaged quotient box)
route is REFUTED.

## Why AQB died (proved, do not retry)
Convex-combination obstruction: the proved averaging transfer needs the family
AVERAGE (a convex combination of single-box fiber counts <= the max box), so the
box-sharing log|F| saving is cancelled EXACTLY by the /|F| normalization. A gain
would need a heavy-fiber theorem, which is circular/forbidden. The deficit side
is proved (B_I(256) = 429,645,546.77; Q_crit = 255.90000002); the safe side
ABOVE sigma* is proved. Only the band 2^33 < sigma <= sigma* is open.

## The ask (a third mechanism; bracket is a partial)
> Cover the band 2^33 < sigma <= sigma* at rate 1/2 by a NEW priced mechanism,
> using a route that is not AQB and not the refuted dihedral-sibling certificate.

A proof that some band radius is provably uncoverable by any priced mechanism is
a valuable bracket-grade partial, but it does not close `rate_half_band_closure`
as consumed by the full-determination parents.

Candidate mechanisms (all un-built):
- **Extended quotient scales:** giant moduli M beyond M_max=2^33 — does an
  averaged conversion at giant M price the band without the AQB circularity?
- **B2b-style balance analysis:** the safe-side sigma* balance (the same balance
  that closed the safe side above sigma*) pushed DOWN into the band.
- **Averaged conversion at giant M:** a convex-safe averaging that does NOT rely
  on box-sharing (which AQB showed cancels).
- **(B) Bracket verdict:** prove NO priced mechanism can cover some band radius
  (an unconditional obstruction), so rate-1/2 lands bracket-grade — an
  acceptable, honest prize outcome (the determination brackets rate 1/2).
- **(C)** conditional on a clean feasibility criterion for giant-M conversion.

Downstream: rate_half_band_closure -> the rate-1/2 grand-challenge determination
(the last rate). This is the single node standing between us and a full-rate MCA
determination.
