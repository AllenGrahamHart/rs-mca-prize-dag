# frontier: e1_official_prime_exception_control

Conditional.

The collision mechanism and almost-all-primes density statement are proved.
The folded-certificate soundness and two-cell manifest assembly are also
proved. The missing steps are now
`e1_folded_no_vector_certificate_128_payload` and
`e1_folded_no_vector_certificate_256_payload`: print direct folded no-vector
certificates over the proved named field/root data for both open cells.
