# xr_smallcore_spread_count — CONDITIONAL packet (amber surgery 2026-07-10)

- **status:** CONDITIONAL (amber)
- **route of record:** generic-branch scoping -> strips + rung-2a/2b forcing
  (pairwise cores <= k) -> exhaustive high/low-core dichotomy ->
  {P-A: `xr_highcore_collision_count` <= 8n^3 | P-B: `xr_lowcore_spread_heart`
  <= 8n^3} -> 8 + 8 = 16 exact -> R_post <= 16n^3 (consumer-exact).
- **audit:** fresh-context amber audit 2026-07-10, verdict AMBER-READY on the
  two-predicate stratum route (the K2-reduction route stays refused per
  WP2_FACE3_VERDICT.md — different question; both answers stand). The
  WP2-named blocker (M6 symbolic-constants composition) was done exactly:
  no proved stratum consumes budget inside R_post at the candidates, so the
  full 16 splits 8 + 8 with zero hidden constants.
- **verification record:** shipped verifiers replayed
  (clean_poly_forcing 52/52, budget_audit 50/50, rungs_2a_2b 39/39, qa22
  staircase 27/27, eliminant fleet exit-0) + two from-scratch implementations
  banked in notes/ (audit_consumption_replay_20260710.py 69/69 — replayed
  again at banking; audit_p8p9_local_20260710.py 88/88, incl. exhaustive
  rung-2b forcing on 4,662 pairs and a t=3 control). Constants audited for
  orbit provenance: none found.

## Pins (definitional; details in the dag statement)

P1 slope-count form (same-slope population = the LIST lane's debt; M1 not
consumed here). P2 post-strip = the gate's B_quot/B_tan ledgers, tangent
staircase ambiguity at worst case n-A+1 (#147). P3 dihedral + extension
INSIDE the predicates (M7 dissolved; E30 odd-j anti-reciprocal caveat).
P4 row pins + the I2 q = p^k caveat (0.089-bit sliver). P5 per-pair sup
quantifier (consumer-forced; toy evidence is mean-flavored — declared).
P6 FM-witnessing caveat (Face-3 evidence at FM = Theta(1)..107; candidate
rows are extrapolation).

## Posedness catch (#42, load-bearing)

P-A is posed on the SLOPE COUNT, not the collision moment: the moment can
sit at ~n^6 while the floor holds (np1 datum: moment 130,370 vs 98 high-core
rays). A moment-posed predicate could be falsified without killing the floor
— spurious re-surgery. The W-collision moment identity stays the instrument.

## NOT consumed, with reasons

`shared_census_kernel` + `f5_npb_conditional_close` (near-pencil band is
inside the generic-branch scoping at the candidates; beyond it NPB's budget
exceeds 16n^3 astronomically — evidence for P-B only);
`spread_exception_classification` (taxonomy route not on record, stays
CONJECTURE/ev); L2/L3/dimension-cap machinery + `f5_toy_window_theorem`
(vacuous at official A — catch #10); `u1_pullback_dichotomy`,
`x4_exactlist_staircase_split` (retracted-route terminals, ev);
`dihedral_staircase` (deep-regime scope only — NOT applicable at the
candidates by its own note; the gate-side conditional.md overstatement is
flagged there); `v13_sp_shift_pair_residual`, sov/m720 fans (advisory), all
REFUTED aqb/pcf nodes (ev/archive). `rigidity_kernel`/`rk_rigidity_kernel`:
ev context on the predicates only (K2 instances by shape, not by consumed
reduction).

## Re-surgery criteria (demote to TARGET if any fires)

1. Either predicate falsified as stated.
2. Candidate-pin change (deciding scales, B*, I2 q-pin ruling, #493/QA.22
   conventions re-attribution) — re-run notes/audit_consumption_replay and
   re-pin.
3. A post-strip live cross pair with core in [k+1, A-2] surviving the
   tangent ledger at any scaled row (breaks the rung-2b step).
4. B_tan resolved above n-A+1 or a B_quot convention change.
5. Maintainer rejects the two-predicate grain (governance call; precedent:
   worst_word W1-W3, petal G1-K4).

---

## Archived route (pre-2026-07-05 retraction; historical — the packet this
## file previously contained)

The old packet claimed CONDITIONAL on: same-slope leg
(`xr_sameslope_list_crossover`), partial-forcing band
(`xr_partial_tangent_band`), far-spread core (`xr_fresh_codim_dichotomy`),
plus `x4_exactlist_staircase_split` / `u1_pullback_dichotomy` terminals.
NOTE (2026-07-10 audit, catch #43): `xr_partial_tangent_band` and
`xr_fresh_codim_dichotomy` do not exist in the DAG — the packet cited
absent nodes (same shape as worst_word's stale conditional). The surviving
content: the same-slope leg is pin P1; the partial-forcing band is the
rung-2b step (now PROVED and carried by `xr_strip_classification_rungs`);
the far-spread core is P-B's open content.
