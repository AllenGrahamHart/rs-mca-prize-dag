# dependency sub-DAG: e22_residual_profile_generating_function

Edges are directed from dependency to consumer.

```text
e22_overlap_nested_fiber_residual_identity [PROVED]
    -> e22_residual_profile_generating_function [PROVED]
    -> e22_overlap_residual_profile_formula [CONDITIONAL]
```

This node counts canonical scale-`M_i` data passing the raw `M_j` residual
budget before the lower-scale minimality and pricing-multiplicity filters.
