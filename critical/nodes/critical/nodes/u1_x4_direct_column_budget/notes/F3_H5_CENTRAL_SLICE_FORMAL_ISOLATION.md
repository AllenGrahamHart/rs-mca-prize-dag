# F3 h=5 central slice formal isolation

Status: PROVED LOCAL ALGEBRA LEMMA, NOT GLOBAL h=5 CLOSURE.

This packet records the all-orders consequence of the central weighted-slice
tangent computation.  It replaces the informal phrase "no infinitesimal fixed
branch" by the precise completed-local statement.

## Statement

Work on the central weighted slice

```text
l5 = bar_l5 = 1
```

with local variables

```text
l6,l7,l8,l9.
```

Let `E_i=l_i-F_i(l)` be the relaxed fixed-point equations obtained by
conjugating the central graph.  At the normalized origin, the tangent compiler
proves

```text
dE = (3/4) I_4,
det(dE) = 81/256.
```

In every official row characteristic this determinant is a unit: official
rows have `n=2^s`, `s>=13`, and any row prime satisfies `p=1 mod n`, hence
`p` is odd and `p != 3`.

Therefore, in the completed local ring at the normalized origin,

```text
K[[l6,l7,l8,l9]] / (E_6,E_7,E_8,E_9)  ~=  K.
```

Equivalently, the origin is a simple isolated fixed point of the relaxed
central sliced graph, with no formal branch through it to any order.

## Proof

Let `m=(l6,l7,l8,l9)`.  Since the linear part matrix of the four equations is
invertible, the images of `E_6,E_7,E_8,E_9` form a basis of `m/m^2`.  Hence

```text
m = (E_6,E_7,E_8,E_9) + m^2.
```

By Nakayama's lemma in the complete local ring, `m` is generated by the
`E_i`.  The quotient by the fixed equations has zero maximal ideal, so it is
the residue field.

## Role

This does not prove the central chart is globally empty.  It proves only that
the normalized origin is not the source of a local h=5 branch.  Any remaining
central-chart obstruction must either be away from this local origin or require
a global/saturated argument.

## Replay

```bash
python3 critical/nodes/u1_x4_direct_column_budget/notes/f3_h5_central_slice_formal_isolation.py
```

Expected digest:

```text
H5_CENTRAL_SLICE_FORMAL_ISOLATION_PASS
```
