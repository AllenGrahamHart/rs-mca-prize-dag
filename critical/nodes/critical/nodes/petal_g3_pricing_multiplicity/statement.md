# Petal G3 pricing multiplicity

- **status:** PROVED (2026-07-13 — the G1 hypothesis discharged at the clause-(P) banking; see proof.md discharge note)
- **consumer:** `petal_growth`

The profile-conversion clause is proved. The remaining charging theorem is a
proved implication conditional on G1 supplying its covering atlas.

Order the supplied charts and assign every canonical staircase support/codeword
pair to its first covering chart. With the exact class-to-quotient conversion
from `petal_g3_profile_conversion_identity`, the total charged multiplicity in
each consumed quotient profile is at most

```text
719 * (number of canonical support classes in that profile).
```

The constant 719 is the complete finite allowance:
`floor(n^6/C(n+6,6))=719` at `n=2^41,2^42,2^43,2^44`. It jointly includes
chart overlap and per-class codeword multiplicity. The former unspecified
`n^{b3}` formulation is retired; no positive polynomial factor fits this
staircase allocation.

The per-class codeword factor is proved to be one. G2 passes the full
agreement support `R` with `|R|>=k` into the canonical profile, so interpolation
determines the codeword uniquely. The first-match selector makes the chart
factor one as well. Thus G3 is not an independent red leaf; its only premise is
the explicitly wired G1 atlas supply.
