# Proof of the conditional implication

Assume `petal_g1_layer_maps`. For a fixed official row and received word `U`,
G1 supplies a finite family of sunflower charts covering every top-band
full-petal contributor. Chart data consist of finite subsets and field data,
so fix any deterministic total order on them.

G2 passes each periodic contributor's full agreement set `R` to the E22
canonical staircase machinery. The cross-scale selector canonically fixes its
intrinsic representative. By `petal_g3_full_support_codeword_injectivity`,
`|R|>=k` implies that at most one degree-`<k` codeword belongs to `(U,R)`.

Assign this canonical support/codeword pair to the least G1 chart that covers
it. Coverage makes the set of eligible charts nonempty, and total ordering
makes the selected chart unique. Therefore every canonical class contributes
exactly one charged pair to the first-match atlas. The aggregate charged count
in every profile is consequently at most the number of canonical classes in
that profile.

Finally, `petal_g3_profile_conversion_identity` converts the canonical-class
count to the quotient-profile convention exactly, with no hidden weight. Thus
the joint inflation factor is `1`, which is below the exact finite allowance
`719` proved by `petal_staircase_allowance_719`. This proves G3 conditional on
G1.


---

## Discharge note (2026-07-13)

`petal_g1_layer_maps` is PROVED (clause-(P) banking:
`../petal_g1_layer_maps/proof.md`, worker packet cp_packet_20260713,
fresh-context audit SOUND). The atlas supplied is word-independent —
strictly stronger than the per-received-word supply assumed above — and
layout-anchored (#168 pin), which the audit verified is this compiler's
consumption semantics. At rates <= 1/4 the floor band is empty and
coverage holds vacuously; the small-scale census carve-out is priced by
the PROVED `petal_small_scale_staircase_census`. The implication above
therefore fires unconditionally; the node is PROVED. Inherited
tripwire: G1's (P)-3 (maintainer P1 wide-resolution re-opens the
chain).
