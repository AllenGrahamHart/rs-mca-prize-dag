# ATTACK - e1_folded_no_vector_certificate_128_payload

This is the remaining live mathematical payload for the `N'=128` E1 cell.

Needed output:

- bind the certificate to the prime field and `rho_128` proved in
  `e1_pocklington_250bit_exhibit_field`;
- give the folded kernel equation in the certificate schema;
- provide a proof log, exact checker, or imported off-laptop transcript for
  the `64`-coordinate box `{-2,-1,0,1,2}^64`;
- prove the transcript is complete; and
- return zero nonzero non-cyclotomic folded vectors.

Do not run a broad local WSL enumeration for this cell.
