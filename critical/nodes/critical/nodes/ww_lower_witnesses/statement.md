# ww_lower_witnesses

- **status:** see `dag.json` (single source of truth)

At every clean-rate official row where the unsafe ledger spends the challenger
column, an explicit quotient-core received word has at least

```text
floor(|F|/2^128)+1
```

distinct codewords at the exact spent agreement. The witnesses use their own
received word, as allowed by the `sup_U` lower-bound quantifier.

Rate `1/2` is not part of this node.

R3 (closed-ball pin, import repair): the endpoint conversion inherits the
CLOSED-BALL reading of Lambda already load-bearing in the PROVED MCA
mirror (adjacency_closing) and the corridor tables — no new assumption;
recorded so a convention re-ruling re-opens this node explicitly.
