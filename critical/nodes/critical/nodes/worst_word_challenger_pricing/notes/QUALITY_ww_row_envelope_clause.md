# QUALITY CERTIFICATE (red-quality program, charter r2, 2026-07-10)

## Red: ww_row_envelope_clause (W3)

Q-SUFF: slot W3 of the worst_word amber (KB #19). The audit FORCED the
sup-form (mean form makes the assembly FALSE by 1.263 bits — the banked
adversarial layout-sup 2.417x median vs the 0.9-bit line) and the
generated-field normalization (q^sigma form refuted by the 24/24 census,
catch #12) — both pinned INSIDE the statement, so the wired form is exactly
the assembly-sufficient one. #493 branch: both branches precise; operative
one selected by the pending conventions ruling (re-surgery criterion).

Q-SURV: DOSSIER — the adversarial layout sweep that produced the 2.417x
sup datum (executed pre-surgery; it is what forced the sup form); f7_beta
24/24 census (K_cell equal across strata); open-window small-q closure
(sigma = 2 closes by q = 97); the codimension-drop vs new-component
dichotomy route lead. EXECUTED: the pre-surgery adversarial sweep (banked).
QUEUED: wider official-convention cell sweep at generated-field
normalization (the pre-registered falsifier's exact shape; bg <= 1 cells
only — open-window small-q and bg >= 2 do not count).

Q-MIN (annotation): W3 is the K2 door (SCK clause (ii) instance) — proving
it likely proves the K2 clause at this face; the #493 branch-B (poly form)
is the weaker admissible variant if granted.

## DIRECT RE-POSE UPDATE (2026-07-12)

W3 is now the DIRECT per-word/per-cell count-vs-allowance claim with the
certificate obligation absorbed (repairs R1/R2) and the min-#493 branch
baked in (a ruling can only relax it). Q-SUFF: re-certified under the
re-derived assembly (import audit link 15). Q-SURV: prior record stands
(the 2.417x layout-sup datum + 24/24 census); the queued wider cell sweep
now targets the DIRECT form's falsifier: an official-convention cell at
min-branch allowance with N_chal > B_chal.

## AGGREGATE ALLOCATION REPAIR (2026-07-12)

The sup quantifier remains per received word. The binding payment is now the
sum of disjoint first-match challenger counts against one row residual, or
equivalently cell allocations whose sum fits that residual. The former demand
that every cell separately fit the whole residual had no composition theorem.
Q-SUFF is restored by the explicit allocation law; no averaging is admitted.

## PAID/RESIDUAL OWNERSHIP REPAIR (2026-07-12)

The paid columns now precede residual challenger cells in one binding
first-match order. This prevents the asserted E15/quotient-staircase class from
being subtracted in `P_paid` and charged again to `B_chal`. A closing
certificate must emit and verify this ownership map. The full-scope
E15/staircase identification remains unproved and is not consumed as a close.
