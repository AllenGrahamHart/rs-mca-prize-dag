# CONDITIONAL close — route of record (2026-07-10 amber surgery)

Superseded content notice: this file previously named the ARCHIVED
predicate e22_challenger_staircase_pricing (pre-retraction,
2026-07-05); that route is dead. The live conditional is:

  IF ww_row_envelope_clause (W3; direct actual-count bound against the exact
     conservative residual budget, sup-form and generated-field normalized)
  AND ww_lower_witnesses (W2)
  THEN the floor, cell-conditionally on planted-sunflower
  receivers at official crossing radii — composing with PROVED
  links ww_lemma_a_exhaustion, the catch-#12 stratum join, and
  the QA.22 budget fit.

The former W1 `K_cell` certificate premise is retired: `K_cell` and its matrix
were never defined, and W3 now states the direct mathematical upper bound that
any future certificate must prove.

Re-surgery criteria and the evidence-citation repair (F7-A2 drift
figures) are in the dag.json statement. Full audit: the 2026-07-10
amber-audit record (kernel_basis KB log #19).

---

## IMPORT ADDENDUM (2026-07-12 master banking; cluster audit)

R4 (re-surgery criterion 1 restated): W3-direct bakes in the MINIMUM #493
branch, so a conventions ruling can only RELAX the predicate — criterion 1
now reads "W3-direct falsified as stated at the min branch", and a #493
grant triggers a RE-PIN (loosen B_chal), never a demotion.

Audit record: the assembly implication (IF W3-direct AND W2 THEN floor)
was re-derived independently at import — per-word first-match column
accounting under QA.22 gives list <= B* directly (tighter than the old
C*K_cell/q^sigma two-step); no sup/mean or per-cell/aggregate drift. Note
the implication is verified at prose rigor on both sides (link 15) — a
machine assembly compiler for this amber is a queued nicety, not a gap
regression.
