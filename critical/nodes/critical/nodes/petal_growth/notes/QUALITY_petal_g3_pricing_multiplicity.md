# QUALITY CERTIFICATE (red-quality program, charter r2, 2026-07-10)

## Red: petal_g3_pricing_multiplicity (G3)

STATUS: CERTIFIED (deferral resolved by the fifth-branch replay,
2026-07-10 — verdict: replay-clean-parametric, G3 NOT discharged;
catches #44-#47).

Q-SUFF: slot G3 of the petal amber (KB #20) — prices the staircase branch
into imgfib's quotient-profile budget (pin P3). STATEMENT REPAIRED by the
replay: clause (b) as originally worded ("W is exactly the
dyadic_profile_evaluation weight") was UNPROVABLE-AS-STATED — the two
weights live on different objects (canonical support classes vs quotient
cosets; exact ratios 8/5, 640/3 — catch #47); re-posed as the explicit
conversion identity raw_M = Q_M * N/(N-h) * [x^b]H_M(x)^{N-h} plus the
w = 1-per-class declaration. The repaired form is exactly what the
assembly consumes (the P3 emission arithmetic). Clause (a) unchanged:
per-U sup over canonical data of the codeword count, sub-k parts the open
content (>= k parts have multiplicity <= 1 by interpolation — proved).

Q-SURV: EXECUTED — the fifth-branch replay itself was a falsification-
grade stress on this red's fastest claimed close: exhaustive n = 8/16
ground truth (all 2^n supports) + sampled n = 32, fresh verifiers with
mutation controls, and it KILLED the discharge claim while confirming
every parametric identity (the census/denominator side is sound; the
conversion identity verified exactly on every tabulated cell — harness
banked at notes/g3_replay_20260710/g3_confront_qa22.py). Support-level
multiplicity exactly zero-hidden (7-lemma composite, replayed twice).
DOSSIER: the branch's dedup soundness = the denominator bookkeeping the
predicate will consume; no super-polynomial multiplicity ever observed at
any tested cell. QUEUED: the per-U charging-map probe (codewords-per-datum
at small rows with adversarial U — clause (a)'s direct falsifier: a
(U, canonical support) pair with super-polynomial codeword multiplicity).

Q-MIN (annotation): clause (a) is the honest numerator obligation the
chain cannot avoid (the charging map lives in G2/imgfib territory);
clause (b) after re-pose is a finite algebraic identity — likely the
easiest red on the board to close partially.

## EXECUTION LOG
- 2026-07-10 fifth-branch replay (fresh context): discharge claim KILLED,
  parametric scaffold SURVIVED, conversion identity verified exactly at
  every tabulated cell; catches #44 (annotation undercount — mine), #45
  (vacuous selector verifier), #46 (IE domain drift — statement corrected
  on the critical surface), #47 (unfillable placeholder — clause (b)
  re-posed). G3 stays red; petal amber unchanged at four predicates.

## COMPILER RESOLUTION (2026-07-12)

Clause (b): PROVED IN GENERAL (petal_g3_profile_conversion_identity —
the identity this certificate re-posed; catch-#47 cells reproduce
exactly). Clause (a): discharged by full-support injectivity +
first-match (per-(U,R) multiplicity = 1; sub-k no-go guards the
re-anchoring). G3 -> CONDITIONAL on G1's atlas, with the small-scale
census carved out to its own gate node. The queued per-U charging-map
probe is superseded by the atlas falsifiers. Certificate CLOSED as
compiler; open risk transfers to G1-atlas and the census gate.