# QUALITY CERTIFICATE (red-quality program; 2026-07-13)

## Red: petal_chart_carrying_descent (SUCCESSOR-A, re-posed M=2 + K_2-invariance)

Q-SUFF: the census gate's route of record post-#124 (word-uniform routes
refuted; chart-carried untouched — the refuting family are not petal
contributors, now MEASURED: the fiber-rich mass inside charts is 100%
multiplicity-1).

Q-SURV: EXECUTED (first attack, 2026-07-13): ~85 cells, ~100k members,
two independent enumeration methods matching exactly, 3/3 mutation
controls; NEITHER falsifier clause fired. Yield: the structure theorem
(#132), P3 uniqueness (#128 — two-line degree argument, m'+1 safe by a
factor of m'+1), the M>ell mechanism refutation + clean re-pose (#130),
the reading-A pin (#129), the quantified G1 cost transfer (#131).
QUEUED: the structure-theorem + uniqueness proof packet (algebraic —
short, the arguments are already sketched); n >= 128 volumes; sigma > 1.

## AUDITED -> CONDITIONAL (2026-07-13)

PACKET-SOUND at the full import bar. Claims 1/2/L0 PROVED; the node is
now amber on the single G1-coverage hypothesis (G1's own quotient-closed
clause). Certificate transfers its open risk entirely to G1-atlas.
