# Frontier

## Proved

- Worst-list size is integer-valued and monotone in agreement.
- The cyclically rotated prefix construction is ordinary-list unsafe through
  excess `17,179,869,183=2^34-1` at the prize-max razor row.
- The exact balanced-incidence Johnson theorem supplies a bigint-computable
  safe anchor `a_IJ(C)`. At the prize maximum it ranges from `3n/4` for
  `B*=1,2,3` down to `1554944255988` once `B*>=332114441762`.
- Explicit two- and three-codeword predecessor constructions meet that safe
  anchor for `B*=1,2`. Hence both complete budget branches have the exact
  crossing `a_L=3n/4` and list radius `1/4`.
- This is the unique largest cap-uniform maximal-prefix specialization of that
  cyclic construction.
- Diagonal tuples preserve every ordinary unsafe witness for all constant
  interleaving arities.
- Once an ordinary safe value is at most `B*`, the official field cap gives
  `B*^2<q`, and the sub-square-root interleaving theorem preserves that safe
  value for every arity.
- The four quadratic B*=3 chambers are primitive full-rank balanced scrolls.
  Their coprime generators have exact degrees `(d-2,d-1)` or `(d-2,d-2)`, and
  `<alpha,Xalpha> intersect <beta,Xbeta>={0}`. Thus they cannot collapse to a
  hidden constant split-unit branch.
- On the maximal `n=2^41`, B*=3 row, every admissible field is prime or
  quadratic: `e=1, p=1 mod 2^41`, or `e=2, p=+/-1 mod 2^40`. The cubic and
  higher-extension branches are empty.
- None of the nine linear B*=3 chamber relations can arise by completing each
  related block to one degree-`d` quotient fiber and removing only its
  exceptional points. The resulting at-most-eight geometric Vandermonde
  vectors are independent for `d>=8`.
- The fixed exponent-support pattern of the known `F_17` path witness has a
  full cyclotomic minor of norm `2^170 17^4`. It is full rank in every odd
  characteristic other than `17`, which is unavailable at the prize-max
  order under the field cap. This isolates the known witness but not the full
  path chamber.
- Equal-multifiber completions by one monomial quotient map are excluded in
  all four cycle and both path chambers whenever the quotient fibers have
  size at least four. The one-root-deleted locators expose independent
  Vandermonde columns in their top coefficient block. At the prize maximum,
  only fiber sizes one and two survive this direct quotient construction.
- The multideletion rational-series extension excludes equal complete fibers
  of size at least eight in all nine linear chambers. The additional direct
  residual is fiber size four in linear `K_4-e`, `K_4`, and
  triangle-plus-singleton; mixed/partial/primitive and quadratic-scroll
  configurations remain outside the theorem.
- The fiber-four residue-rank gate excludes linear `K_4-e` and
  triangle-plus-singleton at the remaining scale. Linear `K_4` survives only
  if four reciprocal quadratic deleted-root locators span dimension two over
  `F(X^4)`. Antipodal pairs realize the algebraic rank condition, but no
  official block construction is known.
- The antipodal rank-two component descends to four pairwise-coprime
  degree-`2^37-1` factors in one two-dimensional base-field pencil. Their
  product is `(Y^(2^39)-1)/product_i(Y-b_i)` for four named subgroup roots.
  This identifies the exact quotient split-pencil census still to be paid.
- The complete first nonconstant quotient boundary (`d=8` over `F_97`) has
  `192` antipodal pencil assignments with nonzero relation coefficients.
  Their graph condition is Möbius, so the residual is genuinely populated at
  small scale and requires an official-scale rigidity argument.
- The two quotient-pencil relations force the four pencil parameters to be
  one Möbius image of the four deleted-pair parameters. After a base change,
  the residual is one quartic norm equation
  `product_i(R+a_iS)=kappa (Y^d-1)/product_i(Y-a_i^2)`.
- Reverse contact forces the degree-drop direction to have degree at least
  `2^36-2` on the maximal row, improved to `(2^38-4)/3` when `e_2=0`.
  If `q` is the first nonzero centered coefficient, the differential residual
  `T=dDU-Y(D'U+4DU')` has exact degree `r+4-q(r-deg V)`. It is linear on the
  lowest generic boundary and quadratic on the lowest intermediate boundary,
  and it contains every repeated/deleted-divisor/zero root of `U`.
- On either low-residual boundary, `U` is uniquely determined by `D`: its
  reverse is the degree-`r` truncation of `E(z)^(-1/4)` for
  `E=z^4D(z^-1)`. The generic boundary requires two consecutive zero omitted
  coefficients at `r+1,r+2`; the intermediate boundary requires the first
  omitted coefficient at `r+1` to vanish. The exact four-term recurrence makes
  this a degree-four coefficient-gap problem rather than an official-degree
  unknown-polynomial problem.
- On the generic floor, the normalized reverse of `V` is also canonical: it
  is the truncation modulo `z^h` of a square-root series computed from `E` and
  the canonical reverse of `U`. Its degree `h-3` forces a second two-term gap
  at indices `2^36-1,2^36`. The generic boundary therefore requires four
  simultaneous printed coefficient vanishings and has no free
  official-degree polynomial directions.
- The primary double-gap condition is genuinely populated beyond the
  order-eight coset exception. Over `F_193` at order `64`, the noncoset orbit
  of exponents `{0,1,3,-2}` has `(a_16,a_17,a_18)=(0,0,94)`. Its secondary
  forbidden coefficients are `102,24`, so the proved secondary gate rejects
  it. Since `193>64`, this rules out primary nonvanishing under the generic
  `char(F)>d` hypothesis. At the first prime above the deliberately stronger
  threshold `p>=d^2`, namely `4289`, the same pattern has primary triple
  `(2769,3151,2930)`. A complete scan over the first eight such primes finds
  single-gap counts `64,64,128,64,192,0,0,128` but no double gap. The strong
  threshold is not a uniform official premise: the prime and quadratic field
  branches have different characteristic scales. This is bounded route
  evidence only.
- After the primary gap, the secondary gate is exactly the two-window square
  congruence `L T=a_(2h)C^2 mod z^h`, with `deg C<=h-3`. The shifted tail of
  `E^(-1/4)` also obeys a first-order differential equation with linear
  forcing. This removes the nested square-root implementation and all
  official-degree polynomial variables, but the dyadic square congruence
  remains open.
- At order `128`, complete scans of the first eight split prime fields find
  primary double gaps only over `F_257` and `F_641`, with `192` packets in
  each, and no simultaneous secondary pass. Each positive field has one
  ordinary scaling orbit and one orbit of two deleted antipodal pairs. This
  selects a parity/univariate algebraic sublane but has no official-order
  transport; larger raw sweeps are not part of the attack.
- That parity sublane is now exact. For `d=16M`, normalize the squared pair
  ratio to `t in mu_(8M)\{1}`. One primary and one secondary zero are
  automatic; the remaining gate is `F_(2M)(t)=G_M(t)=0` with
  `F_(2M+1)(t)!=0`. Proving coprimality with official-characteristic control,
  or direct nonvanishing on dyadic torsion, would remove this entire sublocus.
- A candidate surviving those equations and the canonical span has `beta=0`
  by parity. It splits the punctured `8M`-torsion binomial into two coprime
  degree-`4M-1` factors `B_0^2+lambda w^(2M+1)C_0^2`. This is the preferred
  downstream target: classify the two root cells under the existing primitive
  and Möbius constraints rather than compute larger coefficient resultants.
  Their inverse-root Fourier moments agree for frequencies `1,...,2M`, with
  a printed first difference at `2M+1`, giving a direct prefix-flatness
  interface.
- A primitive-resultant/Parseval bound now excludes complete deleted-pair
  survivors in the official prime-field and `p=-1 mod 2^38` quadratic
  branches. For this sublocus, only the split quadratic branch
  `q=p^2`, `p=1 mod 2^40` remains. Ordinary primary-gap orbits and the other
  generic/intermediate/pure strata are unaffected. All quotient-pencil and
  Möbius data in the surviving branch are already defined over `F_p`; the
  quadratic extension remains only in the ambient evaluation domain.
- In original half-degree coordinates, every complete deleted-pair survivor
  has `D=D_0(Y^2)`, `U=YU_0(Y^2)`, and `V=V_0(Y^2)`. Its exact linear
  residual becomes one constant-forcing first-order ODE. For fixed `D_0`,
  this determines at most one monic `U_0` and leaves one terminal coefficient
  equation. The zero root of `U` is forced and simple, and no other root is
  repeated or shared with `D`. The simultaneous square-pencil equation still
  needs classification.
- The source lifts can be normalized to `(1,iota,r,iota r)` with `r^4=t`.
  Pulling back the outer antipodal pairing gives exactly three reciprocal
  equations for `q=mu/lambda`. Hence each `r` permits at most three unordered
  outer ratios, each itself in `mu_N`, including the explicitly retained
  harmonic alternatives.
  The remaining classifier should shard these three ratio systems rather
  than enumerate point permutations or leave `q` free.
- After `U_0` and `q` are fixed, Euclidean division of
  `R=(x^N-1)/D_0-(xU_0^2)^2` by `A=xU_0^2` reconstructs the only possible
  scaled square `V_0^2`. The nonharmonic branch is one exact quotient-square
  and remainder identity; the harmonic branch is one fourth-power test.
  Consequently no polynomial coefficient or common outer scale remains free.
- The harmonic ratio `q=-1` is empty on the official maximal row. Two source
  pairings are excluded by incompatible orders; the third reduces to a
  length-38 trace recurrence. A complete screen of all `4,495,441` official
  congruence classes has no zero. Only nonharmonic quotient-square tests
  remain for this sublocus.
- The nonharmonic outer ratio is no longer a search variable. For pairing
  `j`, the Mobius equation and Euclidean remainder combine to
  `4b_jT=a_jS^2`, while `y=4b_j/a_j-2` recovers the reciprocal ratio through
  `X^2-yX+1`. The length-38 recurrence `y_(m+1)=y_m^2-2` is an exact `N`-torsion
  gate, and the final polynomial-square test is invariant under reciprocal
  root choice. Thus only three one-variable certifiers in `r` remain.
- In the source trace `chi=r+r^(-1)`, the three scalar multipliers are the
  explicit squares `h_0^2,h_1^2,h_2^2`, with
  `h=(1/(2(chi-1)),(chi-2)/(2(chi+2)),chi/(2(chi-4)))`. Once the matching
  scalar identity `T=(h_jS)^2` holds, the old root-dependent square test is
  equivalent to `T` being an exact fourth power. No outer root needs to be
  constructed even for the final polynomial verdict.
- The first coefficient of those scalar identities is already exact. If
  `sigma=S(0)` is obtained as the terminal coefficient of
  `R_rev/A_rev mod z^(2M-1)`, the three branches require respectively
  `t sigma^2+4(chi-1)^2=0`,
  `t(chi-2)^2sigma^2+4(chi+2)^2=0`, or
  `t chi^2sigma^2+4(chi-4)^2=0`. This precedes every polynomial gcd or
  fourth-power test.
- The constant ODE differentiates the Euclidean residual exactly:
  `2xD_0R'=(2N+kappa x^2U_0^3)+2(ND_0-xD_0')R`. If `T=W^4` on a scalar
  branch, then `W^2` divides `R`, so `W` divides
  `P=2N+kappa x^2U_0^3`. Every survivor therefore satisfies
  `S|P^2` and `deg gcd(S,P)>=M-1`, a root-free rejection gate.
- Once those gaps hold, the entire generic norm equation is one exact
  two-dimensional span test `S in span{z^hBCbar^3,z^(2h)Cbar^4}`. Its two
  coefficients are uniquely read at degrees `h,2h`; the resulting centered
  quartic must split into four distinct parameters with the proved Möbius
  matching. This is a deterministic exact certifier depending only on four
  subgroup roots, not a new official-degree component decomposition.
- On the intermediate floor, normalized `V` is the unique Hensel solution of
  `H=C^3(1+u z^hC/B)`. Its first forbidden coefficient gives
  `3kappa-uDelta=0`: this uniquely determines `u` when `Delta!=0`, rejects
  when `Delta=0,kappa!=0`, and leaves exactly one scalar only on the printed
  `Delta=kappa=0` branch. Polynomiality and the split/Möbius check remain.
- In the pure-quartic stratum `e_2=e_3=0`, the degree-drop direction has exact
  degree `2^37-2`; both pencil directions are squarefree and the remaining
  second-derivative Wronskian factor is linear.
- The same pure stratum forces a non-antipodal harmonic quadruple of
  order-`2d` deleted-root lifts. After normalizing one lift, the fourth is an
  explicit rational function of two subgroup variables. The polynomial
  problem is exactly a coprime Fermat decomposition `Q=B^4+Z^4` with
  `B(0)=1`, `ord_0Z=1`, and both degrees at most `r`. Harmonicity is populated
  at small finite orders, so the matched decomposition is the honest target.

## Open

For every remaining branch `B*>=3`, locate the first safe agreement inside
the now-proved bracket. In the razor case,

```text
k+2^34 <= a_L(C) <= a_IJ(C).
```

For `B*=3` and all larger budgets, the integer Johnson test failing at
`a_IJ-1` is not evidence of unsafety.
A closure still needs a stronger upper profile theorem and a matching
predecessor witness at the same candidate.

The existing clean-rate corridor ledger does not cover rate `1/2`, and the
rate-half MCA node is a different counting object.
For the quadratic B*=3 branch, the remaining object is the product of four
independent affine-linear combinations of the coprime generator pair, times
the exact four- or six-root exceptional factor.
The maximal-row arithmetic can be attacked separately in its prime and
quadratic field branches; smaller domains retain their own extension degrees.
All nine line chambers exclude the one-fiber completion already covered by
the partial-fraction theorem. In the six cycle/path chambers, equal complete
multifiber blocks of fiber size at least four are now excluded as well;
the direct fiber-four linear residual is now only the rank-two `K_4` locus.
Fiber sizes one and two, mixed-map, partial-fiber, and primitive
configurations remain.
For the antipodal component, classify the generic linear-residual boundary
by excluding its two-coefficient fourth-root gap, and classify the
intermediate boundary by excluding its one-coefficient gap. Then treat the
higher exact-residual-degree strata and the separate pure-quartic locus.
For the generic boundary, retain the proved secondary square-root gap; the
primary fourth-root gap alone is no longer the full necessary condition.
On the deleted-pair sublocus, impose the half-degree constant ODE and its
single terminal equation before any square-pencil elimination; do not
saturate away the forced simple zero of `U`.
Then replace the free outer ratio by the three proved Möbius-ratio branches;
the exact router retains the harmonic branch before the official
characteristic theorem excludes it.
Finally apply the exact Euclidean remainder-square router; the open statement
is uniform rejection of its nonharmonic deterministic torsion-indexed tests.
After all four gaps, prove uniform rejection of the full canonical span and
split/Möbius certifier; prefix agreement is not enough.
For the intermediate boundary, reject the unique nondegenerate Hensel
candidate and separately resolve the exact `Delta=kappa=0` one-parameter
branch.
For the pure boundary, classify harmonic lift orbits and exclude their matched
Fermat decompositions; retain the proved squarefree and linear-Wronskian
constraints.
