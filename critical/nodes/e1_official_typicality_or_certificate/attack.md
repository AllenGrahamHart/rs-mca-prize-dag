# ATTACK - e1_official_typicality_or_certificate

This is now an assembly node, not the live E1 leaf.

The live leaves are `e1_folded_no_vector_certificate_128_payload` and
`e1_folded_no_vector_certificate_256_payload`. The selected closure route is:

- produce complete folded no-vector certificates for `N' in {128,256}` over
  the proved common exhibit field.

Uniform typicality remains a possible alternate route in principle, but it is
not the currently wired live critical leaf.

Do not promote from birthday-scan evidence alone.

There is no hidden list of official row primes to recover; see
`official_row_primes_pinning`.
