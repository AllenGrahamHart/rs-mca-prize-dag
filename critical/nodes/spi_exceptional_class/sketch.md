# sketch: spi_exceptional_class (RESTATED per the wording correction; Pro G)
CORRECTED STATEMENT (the wording trap): the GENERIC HORIZONTAL component may
have dimension Theta(n) and is NOT paid — it is the aperiodic incidence core
whose D_j-count is spi_point_counting / R2 (a separate node, honestly open).
The classification claim is for everything ELSE: every rank-excess or
super-slope component outside the generic horizontal one is (i) tangent-paid,
(ii) empty, (iii) degenerate-excluded (noncontainment gate), (iv) quotient/
extension-paid, or (v) an APOLAR-PRINCIPAL SCROLL — the new named class:
generically row-deficient pencils with ker M(Z) = q_Z(X) K(Z)[X]_{<=D-1}
(catalecticant/secant stratum of the rational normal curve), priceable via
R(Z) = Res_X(q_Z, X^n - 1): either R == 0 (forces a fixed H-root -> tangent-
paid; VERIFIED 1600 fibers) or deg R = O(nt) -> only O(n^2) supported slopes.
PROOF ROUTE (complete; write-up pending): vertical fibers at rank-drop slopes
are row-deficient Hankel kernels -> principal q K[X]_{<=D-1} (the PROVED
rank-profile packet): q has an H-root -> tangent-paid; root outside H or a
bare boundary monomial -> D_j-intersection EMPTY; degenerate syndrome ->
excluded. The row-deficient horizontal case: the R(Z) dichotomy above. Not
fiber-count circular (resultant degrees only).
