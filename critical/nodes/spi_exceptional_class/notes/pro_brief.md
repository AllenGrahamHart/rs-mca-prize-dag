# Pro brief G — the exceptional-component classification (crystallization core)
*Recommended for the Hankel-thread instance: this is the same "dense alignment
=> paid structure" mathematics you just used to close the Hankel node, one
level up — at the alignment-variety level.*

## THE MAP AROUND YOU (read this before optimizing your target)
This node is ONE OF FOUR alternative routes into a gate (payment_completeness:
any single route suffices; the others are enumeration-style and stalled). The
campaign's paid taxonomy has grown twice before under counterexample pressure:
the affine-net class (P3) and the dihedral/Chebyshev class (E30) were both
DISCOVERED as "counterexamples" and then absorbed as named paid classes. So:
if you find a component family outside tangent/quotient/extension, that is
NOT failure — name it, characterize it, and the ledger grows (route stays
alive if the new class is priceable). The only bad outcome is an UNNAMEABLE
unstructured family — that would genuinely kill this route (three others
remain). Your Hankel work is the model: dense sparse-atom structure was
routed to paid classes via rational-defect normal forms; expect the same
shape here.

## Setup (self-contained)
F_q, mu_n, RS_k as usual (rate 1/2, n = 2^41 scale; toys F_17/F_193). For a
pair (u, v) of received words, the ALIGNMENT VARIETY X_{u,v} = the locus of
(slope, support-configuration) data where locator alignment occurs — cut out
by determinantal conditions on the Hankel syndrome system of u + Zv (the same
M(Z) from the Hankel thread; alignment = rank drop at slope Z). Components of
X_{u,v} and its intersections with the structure loci (tangent: common-divisor
planes; quotient: Luroth/rational-pullback strata incl. multiplicative,
Chebyshev, PTE; extension: subfield/extension strata) are the objects.

## The two-part target
1. (spi_component_control) BEZOUT/NOETHERIAN CONTROL: the number of
   irreducible components of X_{u,v} (and its structure-locus intersections)
   is bounded by an explicit poly(n) — degree-product bounds on the
   determinantal presentation suffice; the presentation is by t x t minors of
   a Hankel matrix with entries linear in the syndrome window.
2. (spi_exceptional_class) CLASSIFICATION: every component of dimension above
   the poly-count threshold lies in a tangent, quotient(-tower), or extension
   stratum — OR is a new named class. Your rational-defect normal form is the
   suggested tool: a high-dimensional component supplies many independent
   alignment relations; push them through the defect/separation dichotomy.

## Deliverables: (A) both parts; (B) part 1 alone (it gates part 2 and is
pure algebraic geometry); (C) a new named component class with its defining
structure (= taxonomy growth, valuable); (D) conditional on a clean geometric
hypothesis. Constraint: no fiber-count circularity.
