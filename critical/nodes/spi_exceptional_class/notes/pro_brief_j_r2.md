# Brief J (Hankel/G thread) — assemble R2 point-counting from Conjecture F
NEW FACT since your classification: CONJECTURE F IS PROVED (the full chain
closed via your rank-profile + pinned-value packets): linear-plane sections
of D_j have poly(n) unpaid points (tangent/quotient escapes priced).
Your own scroll theorem says the generic horizontal component's fibers ARE
linear kernel planes: fiber(Z) = P(ker M(Z)) cap D_j. The ask: assemble
spi_point_counting (= R2, the last safe-side wall): bound
#(D_j cap H_generic \ Paid) — the unpaid aligned mass over the row-full
horizontal component — using conj_f fiber-wise. The known obstacle: q ~ 2^256
slopes, so a naive fiber-sum is useless; the count must run over SUPPORTED
slopes (those with nonzero unpaid fiber) — bound their number (your R(Z)/
resultant-degree method? the displacement structure? the alignment variety's
degree <= j+t+1?). MAP: this is the deepest remaining safe-side node; a
conditional reduction to one named slope-count estimate is a full deliverable;
a proof closes the r2 chain feeding mca_safe.
