# Statement

Conditional on the three wired premises, the active row and first-match
manifest have the literal finite ledger

```text
U_K3 = U_positive + U_geometry <= U_K3_allocation.
```

The closed saturated `c2(1,1,2)`, `c2(2,0,2)`, and negative-coordinate
branches contribute zero as proved evidence. `U_positive` is the exact
disjoint sum over all thirteen positive routes; `U_geometry` is the exact
disjoint sum over the remaining source-line, biquadratic source-cover, and
trivial-stabilizer terminals from the orientation compiler. The third premise
prints and checks the exact active-allocation comparison.

Every nonzero moving-root payment must prove its chart is a genuine pencil
and count relevant lines exactly. Every higher-dimensional survivor needs a
proved ray compiler with exact multiplicity. All terms retain the same
balanced-core qualifier, received-line owner, bridge multiplicity, and active
add-back chronology.

## Premises

- `rate_half_kb_m2_r4_coordinate_positive_complete_payment`
- `rate_half_kb_m2_r4_k3_orientation_assembly`
- `rate_half_kb_m2_r4_k3_allocation_inequality`

## Falsifier

A missing or overlapping owner, incorrect integer, non-genuine pencil,
uncounted line, unpaid higher-dimensional component, or total above the
active allocation.
