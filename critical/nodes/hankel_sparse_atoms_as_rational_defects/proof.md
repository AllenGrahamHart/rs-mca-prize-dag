# proof: hankel_sparse_atoms_as_rational_defects
Pro rounds 6-7 (refereed): the RS-dual identity (Lemma D, matching-composition
note) gives Ann_E(P) = GRS_{n_E-j-1}(E,lambda) + omega RS_t(E); atoms = defect
sets of rational approximants -lambda g/h to omega. Separation: eliminate
omega, F = g1 h2 - g2 h1 vanishes off T1 u T2, deg F <= n_E - j + t - 3, so
distinct approximants force |T1 u T2| >= j - t + 2 (VERIFIED: 233,130 pairs,
0 violations). Same-approximant atoms are rescalings of RS_s on the defect
block: punctured-GRS/MDS forces ONE saturated closure (verified round 3 at
the pair-family; general weight identical). Hereditary via the proved
stability lemma (omega -> omega * ell_A).
