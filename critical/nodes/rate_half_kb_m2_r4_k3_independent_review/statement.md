# Statement

Fresh independent review must cover the final load-bearing K3 sub-DAG at
pinned commits. It must replay the certificate custody checks, inspect the
mathematical implications rather than only script exits, verify the active
row and partition digest, and confirm same-owner distinct-slope chronology.

This node remains `TARGET` until the review record names the reviewer,
commits, commands, verdicts, and any resolved objections.
