# Attack

1. Write the equality-wall `Q,s,u` invariants directly from the canonical
   source certificate; do not infer them from the string `bcCertified`.
2. Construct the endpoint producer before applying any component
   classification or workboard theorem.
3. Prove divisibility, source-star, and actual-component hypotheses at the
   original slope and support.
4. Canonicalize endpoint records and compute exact finite fibers.
5. Only then invoke the already proved K3 decomposition chain.

The existing K3 packets begin with a supplied endpoint record and explicitly
disclaim this realization. More raw-cell deletion cannot prove this node.
