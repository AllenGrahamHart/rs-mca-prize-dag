# Pro Brief B result (the certified partial; refereed + sanity-checked)

L1 (spread/rank-defect dichotomy): at every point of P cap D_j, either the
incident functionals admit (1-alpha)^{r-1} C(m,r) independent r-subsets
(greedy span-avoidance) or > alpha m of them lie in a proper subspace,
yielding a positive-dimensional subflat carrying the shared divisor.
L2 (spread bound): spread points <= C(n,r)/[(1-alpha)^{r-1} C(j,r)] by
double counting through independent r-tuples (numerically tight at r=2).
L3 (descent): division by g_S is linear; degree and dimension both drop.
r=3 at alpha=2/3, with the twin/pencil-branch split. CORRECTION: coincidence
= RANK DEFECT, not equal hyperplanes (the pencil-through-a-line refutation).
NOT certified: the full F_{r-1} => F_r with absolute B_F — the global
packing step (absorbing the n^r spread numerator) remains open.
