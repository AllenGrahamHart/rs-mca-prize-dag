# Status ruling

`CONDITIONAL`. The implication in `conditional.md` remains proved: a valid
`generator_economy` construction combined with `cluster_certificates` gives
the claimed global separation. The construction premise is open, so the
unconditional statement is not proved.
