# Wording flag 2026-08-02: the cascade "removes" sentence over-claims

`conditional.md:16` states "The proved `xr_pencil_cascade` removes the
large-core pencil regime." Per the 2026-08-02 payment audit
(`notes/pilots_20260802/xr_cascade_payment_audit/`, coordinator-
verified), `xr_pencil_cascade` proves forcing + cascade only; the
regime is CLASSIFIED, not removed — no banked column charges the
core = A-1 tier (T2 does not fire on it; the injection column's
hypothesis excludes it; F5-OS is quantified at <= A-2). Consequently
`conditional.md:23-24` ("the remaining unproved contribution is
precisely the small-core spread remainder") is false as written: the
A-1 tier is a second unproved contribution. To be repaired in the same
coordinated change as the band-repair route (Route W currently
favoured); this node stays CONDITIONAL either way.
