# Fiber-rigidity scope counterexample, 2026-08-06

The exact scaled fixture in
`notes/pilots_20260804/fiber_rigidity/REPORT.md` disproves the broad claim
that primitive deficiency, maximality, tangent saturation, active-defect
locality, and the normative selector force every selected block to be a
union of complete `phi=[P:Q]` fibers plus at most one point.

Both selected blocks in the fixture have profile `(2,1,1,1)` across five
two-point fibers.  A constructor and an independent full checker agree after
exhausting all `C(64,4)` interpolation anchors and all projective slopes.

This does **not** refute this node: the fixture is not an official prize row,
has affine target dimension zero, and is outside the explicitly unpaid
`s=11,11,10` post-envelope strata.  It is a proof-route restriction.  Any
usable `(FR)` statement must print and use the official-row, first-unpaid, or
post-envelope hypothesis rather than infer fiber completion from the local
primitive equations alone.
