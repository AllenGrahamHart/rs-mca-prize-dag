# frontier: e1_official_prime_exception_control

Target.

The collision mechanism and almost-all-primes density statement are proved.
What remains is not two exhibit computations. It is the uniform quantifier:
every admissible field/domain/rate instance in the `N'=128,256` cells must
obey the required exceptional-incidence budget, or a complete input-sensitive
replacement must be wired into the prize route.

The former named-field certificate branch is background evidence. Its two
no-vector payloads remain unresolved at exhibit scope, but closing them would
not close this target.

The next route-deciding step is algebraic: either construct a prize-admissible
exceptional family and reroute `e1_fullness`, or derive a pointwise bound from
the explicit norm-divisor structure that reaches every admissible row.
