# Result

The bridge is `PROVED` as an exact case split:

```text
globally generic:     P-A1 (8n^3) + P-B (8n^3),
globally nongeneric:  P-A2 (combined 16n^3).
```

The former standalone mismatch target is absorbed verbatim as P-A2. No
stronger per-stratum bound is imposed and no numerical red estimate is
assumed.
