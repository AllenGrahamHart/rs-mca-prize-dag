# Result

The near-tail payment now matches the official exact-list ensemble at every
rate. The unpaid half-band starts at `t_XR+16`, `t_XR+15`, `t_XR+14`, and
`t_XR+13` for rates `1/2`, `1/4`, `1/8`, and `1/16`, respectively.
