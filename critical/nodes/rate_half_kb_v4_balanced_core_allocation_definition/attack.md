# Attack

1. Bind the exact row and partition digest from the tangent-source atom.
2. Prove active-partition values for `U_Q` and `U_new`; legacy values do not
   qualify without an owner bridge.
3. Subtract with exact integers and print `U_K3_allocation` and its provenance.
4. Reject the shortcut `U_K3_allocation=B*-U_paid` unless both sibling cells
   are proved empty.
