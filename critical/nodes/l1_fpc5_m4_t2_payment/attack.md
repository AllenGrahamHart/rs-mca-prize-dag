# Attack

1. Work with the coprime cofactor-pair petal envelope and retain the
   background-root and exact-cell guards.
2. Start with the apparent rate-half codimension-two boundary. The forced
   full background collapses it to a guarded locator slice of dimension
   `ell-1` and codimension `ell-1`.
3. Preserve source and touched-pair first ownership in any split-divisor sum.
4. Count core-split locators in the guarded congruence kernel, and test
   quotient or common-pencil ownership before proposing a uniform random-split
   heuristic.
