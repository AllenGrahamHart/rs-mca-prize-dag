# Attack

1. Do not reopen rate quarter; pair uniqueness plus first-layout domination
   pays it by the absolute bound `6+4`.
2. Work only on the rate-half child, using one fixed maximal source plus the
   four-anchor remainder.
3. Retain the background-root and exact-cell guards inside the coprime
   cofactor-pair envelope.
4. At the apparent codimension-two boundary, use the dimension-`ell-1`
   guarded congruence kernel.
5. Count core-split locators after internal tangent/quotient first ownership;
   source layouts are no longer an allowed multiplicity loss.
