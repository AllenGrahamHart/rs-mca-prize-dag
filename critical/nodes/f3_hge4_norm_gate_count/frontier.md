# Frontier

## Proved structure

- x81 and x83 classify every F-4-minimal record as a characteristic-zero
  full-fiber trade or a p-specific norm-gate event.
- x24 pays every characteristic-zero full-fiber trade.
- Therefore the stripped HGE4 residual is a subset of the raw p-specific
  norm-gate population.
- The proved primitive shift-pair adapter gives
  `N_h^strip<=SP_h^prim` and sums the explicit per-width tolerance
  `7000n max(1,B_h)` below `14n^3` over the entire official width band.
- The proved orbit router gives `SP_h^prim=nO_h^prim`; hence the single
  quadratic census `sum_h O_h^prim<=14n^2` is sufficient. It also proves that
  naive uniform max-fiber summation would have to show primitive emptiness.
- The proved near-square union router gives the exact anchored identity
  `A_h^union=hO_h^prim` and removes the union-partition factor
  `binom(2h-1,h-1)` from any fixed-row generator.
- The proved swap odd-moment router makes the antipodal-swap branch empty at
  even `h`. At odd `h` it replaces a union/partition scan by a signed odd
  Vandermonde kernel on `h` antipodal pairs and gives
  `A_h^swap=hV_h^swap<=binom(n/2-1,h-1)`.
- The proved half-order square descent removes the odd sign search as well:
  on `Y subset mu_(n/2)`, test deterministically whether
  `(L_Y+prod(Y))/Z` is a monic square. Its primitive scaling-orbit count is
  exactly `V_h^swap`.
- The proved straight-line lift replaces each fixed swap divisibility cell by
  a sparse cubic repeated-squaring scheme and proves that it has an integer
  bad-characteristic certificate endpoint. At `n=8192`, widths `5,7,9` have
  `88/90`, `127/130`, and `149/153` variables/equations respectively.
- The proved non-full near-square lift now covers the free class as well. A
  selector for a nonzero intermediate coefficient of `S` removes exactly the
  paid characteristic-zero full-fiber locus; repeated squaring gives one
  cubic fixed-cell scheme or an `h-1` chart cover, each with an integer
  bad-characteristic endpoint.
- The proved near-third Belyi theorem turns every `0<e<h` pair into a
  central-star map, and the reciprocal complement supplies a second gap in
  `Z^(2/3)`. Its differential recurrence proves
  `E_h^prim(m,p)=0` whenever `7h>=2m+1`. Thus the live exact-level band ends
  at `floor(2m/7)`; the exclusion costs no orbit budget.
- In the retained near-third strip, the same complement has the exact form
  `W=ZS+lambda y^(h+e)`. Consequently the midpoint satisfies
  `S | 1-(1-a^2lambda)y^m`. Over a finite base field its factors have one
  Kummer degree dividing `h`; a nontrivial degree would stabilize both
  outside members and violate primitivity. Thus every retained candidate is
  a base-field-split three-member divisor pencil, not an arbitrary
  central-star necklace or extension-field midpoint.
- The cyclotomic-norm quarter-width theorem supersedes that retained-strip
  classification as a consumer obligation. Odd power-sum congruences force
  `p^floor(h/2)` into one nonzero cyclotomic norm, while odd-frequency
  Parseval supplies an incompatible upper bound under `p>m^2`. It deletes
  every exact-level width `m/4<=h<m/3`. Hence every live width satisfies
  `4<=h<=m/4-1`, equivalently `e=m-3h>=h+4`.
- In the exact band `h=m/4-d`, put `X=4(d+1)log m` and let `B` be the least
  power of two at least `X`. If `B<h` and
  `BX<m^(1-(4d+8B)/m)`, the dyadic Haar iteration forces zero antipodal
  defect. Every candidate is therefore swap-stabilized: even widths are
  empty and odd widths route to the half-order perfect-square support count.
  The free class is absent in `9223` top widths at `m=2^41`; the simpler
  constant-`64` sub-band reaches `6521`.
- The swap-norm theorem proves the necessary inequality
  `m^(h-1)<h^(m/4)` for every odd primitive swap pair. Thus the complete swap
  class is empty for `h=m/4-d` whenever
  `d<=floor(m/(2log_2(m)))-1`; at `m=2^41` this reaches
  `d=26,817,356,774`.
- The Vandermonde-defect theorem uses the guarded cubic norm ceiling
  `L<Y_3`, where
  `Y_3=4((d+1)R-d)-8(d+1)^2R^2/m+(32/3)(d+1)^3R^3/m^2`, and the consecutive
  even moments force every nonzero defect to have squared mass at least
  `floor((h-1)/2)+2`. Under `4(d+1)R/m<=1`, this completely deletes
  `2,677,220,820` top widths at `m=2^41`. This band lies inside the swap-norm
  band and supersedes the Haar cutoff for production.
- The multiscale Haar norm-product theorem folds every even moment through
  one shared dyadic energy ledger and prices a structurally zero fold by its
  exact cyclotomic power of two. Its sharp quarter-orbit endpoint closes
  `h=4,5` at `m=64`; the existing ambient and upper-width gates close the
  rest. Hence the complete `m=64` exact level has zero debit on every
  official row, and the surviving exact-level problem begins at `m=128`.

## Evidence

The banked deep census contains 68 cells. Every cell satisfying `p>=n^2` has
raw non-toral count zero: complete `n=16` rows over six primes, `h=4..8`
slices at five `n=32` primes, and `h=4,5` slices at four `n=64` primes. The
only nonzero raw cell is the deliberate sub-threshold control `(16,17,h=4)`.

## Open content

Starting at exact level `m=128`, prove the primitive orbit aggregate `(OAR2)`,
its equivalent anchored near-square form `(NSU4)`, RAW-NG, the weaker stripped
aggregate, or the explicit per-width estimate `(PSA3)` uniformly over the full
corridor and all live exact-level widths through `m/4-1`. This requires a bound on the
free-union class and, if
the swap route is retained, the equivalent half-order perfect-square support
count. The free class now has a fixed-cell certificate architecture but no
certificate family or count. Fixed-cell integer certificates are useful only
with a uniform prime-factor or summed-cell argument. The finite census is
route evidence only; it does not supply width-uniformity or an official-row
certificate family. The near-third Kummer and scalar-gcd packets are now
conformance fixtures only. The Vandermonde-defect band is fully deleted.
Between its cutoff and the swap-norm cutoff only the free aggregate remains;
below the latter both free and swap aggregates remain open. No generator or
positive debit remains at `m<=64`.

## Provenance addendum (2026-07-21, wave-18 integration)

The original master-minted frontier read: "Prove RAW-NG or the weaker
stripped aggregate uniformly over the full corridor and all widths through
`H_max`. The finite census is route evidence only; it does not supply
width-uniformity or an official-row certificate family." Superseded by the
width-cap frontier above (live band now the lower quarter, cap `m/4-1`);
preserved per custody #104.
