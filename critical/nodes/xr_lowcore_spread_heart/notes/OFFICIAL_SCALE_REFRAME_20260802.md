# Official-scale reframe 2026-08-02: the partition never compresses; the obligation is |Gamma|

**Status of this note: surfaced strategic finding (model extrapolation,
not a measurement); adjudication with coordinator/maintainer.** Source:
`notes/pilots_20260802/pb_fm3_mechanism/{REPORT,FABLE_AUDIT}.md`
(parameter-free greedy-depletion model, validated at 15 shapes with
frozen pre-registered predictions 5/6 PASS; corroborated by the
model-free population partner count Pi).

Findings bearing on this node:

1. The compression-selector collapse (Gamma_lo -> ~0) that survived
   K1 at every computable scale is driven by the greedy block deficit
   K - L*, where L* depends on density/rate but NOT on n. Since
   K = rate*n, the deficit grows linearly in n: at official RowC
   scale (K - chi)/sigma ~ 18-20, P[core >= K] ~ 2^-83, and
   Pi = 2^-73.5 — >=K-core pairs are absent from the POPULATION, so
   no selector can build a Gamma_hi. **At official scale Gamma_hi is
   empty and Gamma_lo = Gamma for every order.**
2. |Gamma_hi| + |Gamma_lo| = |Gamma| identically — the R2 partition
   routes, it never compresses. At computable scales the mechanism
   routes ~everything into P-A1; at official scale everything lands
   HERE. Either way the binding constraint behind the 8+8 assembly is
   a bound on |Gamma| itself.
3. Consequence for this node's campaign: re-target from partitioning
   Gamma to BOUNDING |Gamma| for split-fibre pencils. The adversarial
   audit's M = 129,948,699,009 vs 16n^3 = 1.7e10 (RowC 1/4) must be
   re-examined on that footing — noting |Gamma| <= q automatically,
   so rows with pinned q below the budget are safe by field size
   alone; the exposure is confined to rows whose q exceeds 8n^3.
   Row-by-row q-pin adjudication queued (coordinator pass, then Pro).
4. The n = 44/48 native-enumerator fleet item is re-scoped: its
   target is Pi (pre-registered: Pi = 2^-4.4 < 1 at n=44 rate 1/2
   q=1.33e6, hence Gamma_lo = Gamma for EVERY order including lex) —
   one measurement that confirms or destroys the extrapolation.

Caveats: 5-doubling extrapolation (conservative in direction); the
equidistribution assumption (H) fails measurably at the one
officially-shaped toy point (Q11, x55, designed-fibre artifact that
provably decays); nothing here is proved. This note changes no status.
