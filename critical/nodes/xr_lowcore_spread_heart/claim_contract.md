# Claim contract

- **claim id:** `xr_lowcore_spread_heart`
- **status:** TARGET
- **quantifier:** every globally generic received pair and its fixed
  support-wise first-match post-strip selector at each official clean-rate row
- **currency:** distinct slopes
- **predicate:** every selected support intersection is at most `K-1`
- **budget:** `8n^3`
- **inside:** singleton, dihedral-symmetric, and extension-type generic slopes
- **outside:** quotient and genuinely recovered-line tangent slopes already
  first-matched and paid; globally nongeneric mismatches route to P-A2
- **consumer:** `xr_smallcore_spread_count`
- **paid shift-pair range:** every oriented difference fiber with `t>=K`
  has multiplicity at most one; the more general terminal Plotkin bound is
  `8(h+1)` and lies inside this range on all six rows
- **open shift-pair range:** `h+1<=t<=K-1`, with RS realization and
  first-match ownership retained
- **terminology guard:** this is a Boolean indicator-difference energy split,
  not a bound on CAP25's local locator-SPI degree
- **paid puncture compiler:** each repeated difference becomes two smaller
  generic low-core RS-line instances with unchanged `h`; affine rank at most
  `K-t+1` pays `t=K-1,K-2` per fixed difference
- **open aggregate:** count/first-match the oriented differences and control
  reduced dimensions `K-t>=3`
- **falsifier:** one official or transport-valid scaled pair whose selected
  low-core slope population exceeds `8n^3`
