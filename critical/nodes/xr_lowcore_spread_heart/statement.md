# XR low-core spread heart

- **status:** TARGET
- **scope:** the six clean-rate candidates
- **consumer:** `xr_smallcore_spread_count`

For every globally generic-branch received pair `(u,v)`, fix the
support-wise first-match post-strip selector. Let `Gamma_lo` contain the live
slopes whose selected agreement support intersects every other selected
agreement support in at most `K-1` coordinates. Then

```text
|Gamma_lo|<=8n^3.
```

Singletons are included. Dihedral-symmetric and extension-type slopes remain
inside the predicate allocation.

Pairs with a joint explanation on another `A`-support route to P-A2's
combined mismatch clause; they are not silently tangent-paid and are not an
extra obligation of P-B.

The all-LineRay affine-core theorem and later covering-free charges pay the
currently recorded low selector ranks in this generic scope.

On support indicators, every oriented shift fiber of width `t>=K` has
multiplicity at most one. This includes the complete terminal Plotkin range
on all six rows. Repeated oriented differences `h+1<=t<=K-1` and the slope
count remain open. This is a Boolean additive-energy split, not CAP25's local
locator-SPI degree.

For every repeated difference in that range, full-side locator division gives
two smaller globally generic P-B instances with parameters
`(N-2t,K-t,A-t)` and unchanged excess `h`. The resulting affine-rank bound
pays the two boundary widths `K-1,K-2` per fixed difference. Cross-difference
aggregation and reduced dimensions at least three remain open.

The later `xr_lowcore_near_k_difference_packing` supplies the first
cross-difference aggregate below `K`. At width `t=K-c`, residuals intersect
in at most `c-1`, so disjoint ownership of their `c`-subsets gives the exact
fiber bound

```text
floor(binom(N-2K+2c,c)/binom(H+c-1,c)).
```

Charging this bound through the global difference-energy identity pays the
codimension prefixes `2,1,1,6,5,4` on the six clean-rate rows. The unresolved
repeated ranges now end at `K-3,K-2,K-2,K-7,K-6,K-5`, respectively.
This narrows the energy concentration range but does not prove the slope
count.
