# DSP8: the split-pencil / line-fiber correlation bound

- **status:** TARGET (minted 2026-07-19 at the C36 amber ceremony; alt leaf, gate=any)
- **consumer:** `f3_h3_mobius_excess_half` (alt)

At every official row in the open range n^2 <= p <= 6^{n/4}:

    10 J_25^0 + 17 J_25^A <= 892 n^2

where J = 8D counts the integer correlation between disjoint split
members of Q_(t,r)(X) = X^2 - (1+r-t)X + r and the affine line fiber
(pin: (X-r)Q_(t,s) - (X-s)Q_(t,r) = t(s-r)X). This is EXACTLY the E=6
disjoint-support moment (wave-14 audited equivalence, factor exact) —
closing it closes the C36 red on the analytic route.

PRE-REGISTERED F-ROUND 1 (queued): measure J_25^0 / J_25^A exactly at
analogue rows across >= 3 scales; KILL = any exact row exceeding the
bound, or a monotone trend extrapolating past it at official aspect.
FALSIFIER: one official row with 10J^0 + 17J^A > 892 n^2.
