# interleaved_floor_import

- **status:** see dag.json (single source of truth; dag status PROVED) [header retrofit 2026-07-10, catch #69 — was: TARGET]
- **closure:** proof

## Statement
ext_pole_floor's list-window mechanism (N(L) = ceil(L(|F|-|B|)/(|F|-|B|+kL)))
covers the e-INTERLEAVED base-list multiplication-slice witness (not only
ordinary B-valued base lists): every e-interleaved base-list floor witness meets
the extension-pole divisor D_E. This is the SOLE remaining seam for
ef_fullfield_bridge after Pro's interleaved reduction. If it holds, no S9 exists
and ef_pole_free_cycle_exclusion is unconditional; if it fails, the residue is a
genuine NEW extension-valued/interleaved S9 ledger class (trivial stabilizer,
non-tower, non-pole, F-valued word source, NOT covered by the base-list floor).

## Falsifier
An e-interleaved base-list witness that provably escapes ext_pole_floor's N(L)
mechanism -- the S9 extension-valued class.
