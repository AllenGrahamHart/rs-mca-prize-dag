# conditional: unsafe_at_crossing

PROMOTED red -> amber (Codex batch-3 defect completed by this audit): the collision-free branch = certified_valueset_lower (the all-pairs-distinct witness family) — was prose-only (my earlier fix silently no-op'd on an existing ev edge); now wired req alongside the collided branch (averaged_slope_conversion, RIPE). The two-branch split (collision-free vs collided) is exhaustive by definition; granting both branches, the adjacent-point witnesses exist per row — assembly statement.
