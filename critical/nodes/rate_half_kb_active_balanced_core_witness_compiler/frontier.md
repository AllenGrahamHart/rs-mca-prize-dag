# Frontier

Do not enumerate more certificates: canonical selection is complete. Use the
stored lattice/support/explaining fields to test the `Q=6,s=6,u=2` hypotheses
one by one, or map the same source record directly into upstream's current
spread/large-owner/exception routing interface.
