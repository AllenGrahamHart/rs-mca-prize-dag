# Claim contract: XR P-A

- **claim id:** `xr_highcore_collision_count`
- **scope:** the six `(RowC, prize) x (1/4,1/8,1/16)` candidate rows
- **quantifier A1:** per globally generic received pair `(u,v)`
- **quantifier A2:** per received pair having a joint `A`-support explanation
- **currency:** distinct post-strip live slopes
- **budget A1:** `8n^3` for high-core slopes
- **budget A2:** `16n^3` combined across every retained support mismatch
- **consumer:** `xr_smallcore_spread_count`; generic routing uses P-A1 plus
  P-B as `8+8`, while nongeneric routing uses P-A2 as one combined `16`
- **inside:** dihedral-symmetric and extension-type slopes
- **outside:** same-slope, quotient, tangent, and other explicitly stripped
  columns
- **falsifier A1:** one sustained transported generic high-core count above
  `8n^3`
- **falsifier A2:** one official nongeneric pair whose total retained
  support-mismatch population exceeds `16n^3`
- **nonclaim:** neither raw W-collision moments nor per-chart counts are the
  target
- **paid atlas:** canonical collision components partition the target slopes
  and carry one residual-union chart each
- **paid rank branch:** the existence of one one-per-slope selector of affine
  error rank at most three fits the full `8n^3` allocation
- **paid collision-line ledger:** independent core minors inject distinct
  collision lines into `(s-1)`-subsets; basis-poor lines put the whole selector
  in a small GRK chart
- **paid cogirth branch:** kernel-slice information sets pay full-domain
  selector ranks `4,4,3,11,11,10` on the six rows
- **covering-free rank close:** the all-zero, post-strip affine-pencil, and
  RS common-root basis charges pay selector ranks `4,4,4,16,16,14`; the
  first open ranks on P-A1 are `5,5,5,17,17,15`, so catch #158 is closed
- **P-A2 low-rank close:** support-local LineRay transversality pays selector
  rank at most three everywhere and rank four at RowC rate `1/4` from the
  combined reserve; first open P-A2 ranks are `5,4,4,4,4,4`
- **paid uniform compiler:** any over-budget uniform cell yields a bounded
  split-pencil trade; at minimum rank-two union the collapsed branch is
  impossible and regular Plucker face syzygies are quotiented
- **uniform residual:** surviving rank-two trades have active union at least
  `a+3`; the first shell is exactly a quadratic-involution pullback with at
  most one singleton zero fiber; every shell has an exact zero-mass/arity and
  primitive-Maxwell-deficit router; trade rank at least three is also open
- **primitive shell payment:** full-core rank two is absent at every prize
  kernel rank through shell depths
  `22,428,333;19,217,048;4,478,600`; the complete primitive first shell is
  empty because its only remaining rank-three edge-zero form also has
  positive deficit
- **local rank-two atlas:** every rank-two trade decomposes into four-block
  Mobius/Plucker and five-block full Segre circuits
- **intratrade owner:** the lexicographically first three/four-column basis
  gives every non-anchor block one unique fundamental four/five-block circuit
  and those vectors form a basis of the full scaling kernel
- **three-anchor scalar close:** coefficient rank three is exactly
  `s_i(P+gamma_i Q)` with `s` a full-support dual `GRS_3` word parameterized
  by one `H` of degree below `t-3`; owner weights are explicit barycentric
  ratios
- **four-anchor coefficient close:** every non-anchor is a support-three/four
  point on an explicit smooth split anchor quadric, its coordinates are the
  owner, and all such coordinates have centroid `(-1,-1,-1,-1)`
- **dual support-extension close:** every row has one unique common cofactor
  `R_i` between its active zero fiber `Z_i` and selected-block extension
  `T_i`, with forced extension size and an exact dual-codeword converse
- **received-pair interaction close:** the three-anchor polynomial plane has
  an alternating `2x2` pairing with `(b,q)`, while the four-anchor plane is
  orthogonal to both directions; this is exact for the selected trade row
- **actual-block extension close:** for each fixed support/slope, unique
  support interpolation gives an external residual zero set `E_i`; actual
  extensions are exactly its forced-size subsets and are counted binomially
- **extension-family compatibility close:** inside active-zero reuse and
  outside extension collisions obey an exact pairwise slack and summed
  multiplicity ledger equivalent to all pairwise block caps
- **next theorem A1:** count realized support embeddings from the dual-`GRS_3`
  and quadric-centroid atlases together with solutions of the extension
  packing ledger, and prove first-core ownership; classify rank-at-least-three
  circuits or nonpositive-deficit higher shells, extend to `u>=1` nonuniform
  cells, and bound the cross-core aggregate by `8n^3`
- **next theorem A2:** aggregate the canonical full-zero mismatch charts and
  low-core explanation fibers within the combined `16n^3` reserve
