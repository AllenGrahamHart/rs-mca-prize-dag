# Widening cost pass 2026-08-02: what survives {core in [k, A-2]}

**Status of this note: evidence banked; the repair fork is SURFACED,
no predicate change applied.** Full report and coordinator audit:
`notes/pilots_20260802/p_a1_widening_cost/{REPORT,FABLE_AUDIT}.md`.
Context: the bridge adjudication
(`notes/pilots_20260802/xr_bridge_semantics/`) forces R2 on
`xr_tangent_support_mismatch_bridge` jointly with widening THIS node's
P-A1 predicate from "size-`k` core" to "core >= k" — unless the band
`[k+1, A-2]` is instead charged upstream (Route T below).

Inventory verdict (coordinator-replayed):

- SURVIVES VERBATIM: T1-T4 moment identities, the d-adaptive sunflower
  cap (self-adjusts: (n-k)/(t-d) at d = w-k equals (n-k)/(A-w)), H1
  (the pinned verifier already reads `J >= k`), GRK, DDR, SEL, ACG,
  AZC, the entire rank-two coefficient tower (Segre atlas, anchors,
  extension certificates), and L_max = 384,448,960.
- SURVIVES WITH EDITS, CONSTANTS DEGRADE: component-union atlas (edge
  relation -> `|E u E'| <= R`, chart padding), collision-line ledger
  (population cap becomes L(w) = floor((n-w)/(A-w)) — the printed
  floor(R/h) is REFUTED on a planted, verified-generic band fixture),
  PSP/CRB/FN (parametric in kappa; prize paid ranks drop
  15,15,14 -> 11,11,10; CLB3/CLB4 dead on every row).
- BREAKS: the uniform-cell / Maxwell / shell-exclusion fan (~10 PROVED
  background nodes) — every strict contradiction needs h < 4 or h < 3;
  h = 5, 5, 3, ~2^33, ~2^33, ~2^32. Includes this node's own
  statement.md:36-40 collapsed-face paragraph (the "k+2 zeros vs cap
  k" argument), dead on five of six rows.

**Catch (wording defect in the dag statement of this node):** "PROVED
AROUND IT: post-strip pairwise cores <= k ... 4,662 forced pairs, 0
violations" — the pinned replay verifies the FORCING identity, and
4,662 is the count of core->=k+1 cross pairs in that fixture: the
number is evidence that cores EXCEED k, cited as if the opposite. Same
genre as the bridge "=K" mis-transcription. To be corrected in
whichever repair route lands.

**The fork (surfaced, undecided):**
- Route W: widen + demote the invalidated PAID entries + open
  `xr_band_core_slope_count` (band slopes <= 4n^3, funded from the
  13n^3 consumption headroom; NEVER by splitting 8n^3 — the AZC
  discharge of catch #158 at RowC 1/16 has 0.5005% margin and dies at
  4n^3).
- Route T: prove the graded tangent band charge (archived
  `xr_partial_tangent_band` mission); band pairs charged before the
  generic branch; this node keeps its exact-k form, zero demotions.

Gate for both: audit whether `xr_pencil_cascade`'s TANGENT-PENCIL
payment is actually charged inside B_tan <= n-A+1 — if not, the A-2
ceiling itself is unsourced.
