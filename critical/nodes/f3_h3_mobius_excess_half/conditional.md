# conditional close: f3_h3_mobius_excess_half (the C36 amber)

- **status:** CONDITIONAL — flipped 2026-07-19 at the maintainer-directed
  ceremony; route of record = f3_h3_dsp8_correlation_bound (req); reserve
  = f3_h3_official_order_template_survivor (ev)

## Assembly

The audited reduction chain (waves 10-14, every component PROVED): the
critical target 17 X_18 <= 300 n^2 reduces via the double-accident
chain to Y_18, whose two routes are (analytic) the DSP8 correlation
10 J_25^0 + 17 J_25^A <= 892 n^2 — proved at wave-14 to be EXACTLY the
E=6 disjoint-support moment — and (fixed-order) the coupled-ideal sieve
+ template survivor bound. Hence: DSP8 ==> the red closes. The reserve
route is equally sufficient; the surface gate law pins one req route.

## Standing discipline

DSP8 F-round 1 is PRE-REGISTERED and queued (the C1' lesson). If DSP8
is falsified, the amber re-wires to the reserve route (forced
correction) or re-opens.
