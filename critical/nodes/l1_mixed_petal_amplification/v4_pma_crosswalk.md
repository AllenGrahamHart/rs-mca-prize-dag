# v4 PMA campaign crosswalk (wave-8 audited, 2026-07-16)

How Codex's v4 "petal mixed amplification" campaign maps onto THIS node
(guard (c) of the wave-8 audit: FAITHFUL, no goalpost shift, but NOT the
same bucket — v4 re-poses the post-top-band SUPERSET, which includes
full-petal strata master already owns via clause (P)).

## What pays PART of this node's bucket (imported, PROVED, ev-wired)

- `pma_petal_pattern_root_pinning_ledger` — every bounded u+e
  mixed/partial region.
- `pma_saturated_mixed_support_kernel` — the fixed-support charge
  (saturated-kernel/Hankel lemma).
- `petal_reserve_rich_fiber_reduction` — THE SHARPEST STRUCTURAL FENCE:
  any reserve-scale counterexample needs an Omega(n/log^2 n)
  source-coupled value fiber concentrated in ONE petal.
- The sigma-one payment/owner suite (M<=3 all cells; M=4/t=4;
  rate-quarter M=4/t=3; positive-denominator rate-half).

## Mandatory lower fences (imported refutations)

- `pma_sigma_one_variable_defect_exact_hit_floor` — exponential
  primitive wide-sub-Johnson residual at sigma=1 on poly-field rows
  (Linnik/Xylouris); OUTSIDE imgfib's entropy reserve (guard (b):
  verified — imgfib survives CONDITIONAL), but any finite-row ledger
  must respect this floor.
- `pma_sigma_one_d4_generic_source_obstruction` — kills n^6 Post
  allocation (2^104 > 2^97 at (65537^2, 65536, 32768)).
- `pma_sigma_one_d3_reciprocal_quadratic_obstruction` — Theta(n^7)
  diffuse extras from n = 2^22.

## This node's open content after the import

u+e -> infinity mixed/partial profiles WITH the rich-fiber forcing:
a counterexample must concentrate an Omega(n/log^2 n) source-coupled
value fiber in one petal. That is the sharpened target; everything
bounded is paid.

## Guard record

pma_wide_residual is REFUTED (both readings) — nothing may condition
on it (all wiring ev/history). The v4 conditional closure of
petal_mixed_amplification was retired for exactly this reason.

## Wave-9 addendum (2026-07-17)

The v4-side rate-half full-petal tail is now an exact ONE-PARAMETER
guarded split-divisor atom (LS6) with lambda cross-ratio normalization
(the five-node pma_ratehalf ladder, audited + imported). This is NOT
new open content for the l1 red (it is a clause-(P)-side stratum per
the wave-8 guard (c)); the atom is a BC/SCK-type object (ev edge into
shared_census_kernel) — a potential shared attack surface.
