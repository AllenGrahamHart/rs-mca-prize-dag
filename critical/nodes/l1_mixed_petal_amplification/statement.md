# L1: mixed-petal / diffuse partial-petal amplification

- **status:** TARGET (posed 2026-07-14 at the upstream audit's finding, catch #212)
- **consumer:** `imgfib` (req — the missing bucket of the image-fiber count)

## Statement

At every official row and for every received word U, the mixed-petal /
diffuse partial-petal contributors to `#ImgFib_U(k+sigma)` (supports
touching at least one petal in fewer than two points, or diffuse
core-heavy supports outside the full-petal stratum) are polynomially
bounded above the corrected reserve, with the quotient profile budgeted.

## Provenance

The bucket the proved chain does not cover. Clause (P) + K4 + the census
gate close full-petal top-band, full-petal below-top (Lemmas 13/15/16),
primitive, and periodic. The program note labels this bucket CONJECTURAL;
its naive induction was retracted 2026-07-05 (dim K grows with c — the
residue-line growth IS the obstruction). Posed at the upstream audit
`l1_imgfib_crosswalk_audit.md` (41/41 verifier), which identified B11's
residual frontier as growing-excess full-petal (closed by clause (P))
PLUS this diffuse partial-petal case.

## Known evidence

- Catch #176: mass is real — 43 mixed-petal floor-band contributors vs
  10 full-petal at (16,8,97) (4x at the smallest cell); no growth law
  was then measured in either direction. Roadmap round N10 now extends the
  exact-support count to `n=32,64` on two scalar schedules. The counts are
  `43,2879,109391` (consecutive) and `33,2857,108600` (powers of five), with
  the second doubling factor about `38` in both. This resists the registered
  super-polynomial-growth falsifier on those chart families but is not a
  theorem over arbitrary received words; see
  `experiments/prize_resolution/l1_balanced_mixed_growth_census_result.md`.
- The 2026-07-05 retraction record (petal_growth/RETRACTION_MANIFEST.md)
  kills the naive induction route, not the statement.
- Codex's v4 PMA campaign (~133 commits, 2026-07-13..15) appears to
  attack exactly this allocation problem — survey/audit pending.

## Falsifier

A word family whose mixed-petal image-fiber count grows
super-polynomially above the corrected reserve; or a route-kill proof
that mixed-petal mass at official rows exceeds every n^B.

## Known evidence — addendum (2026-07-16, wave-8 import)

The v4 PMA campaign landed (audited): all bounded u+e mixed/partial
regions are PAID (root-pinning ledger + saturated kernel + the sigma-one
suite); the open content narrows to u+e -> infinity profiles with an
Omega(n/log^2 n) source-coupled rich fiber in one petal
(petal_reserve_rich_fiber_reduction). The naive-induction killer is now
complemented by the collision-free-emptiness theorem. See
v4_pma_crosswalk.md.

## KNOWN-MASS WITNESS (re-appended 2026-07-20, w15-L1-1 — restoring catch #176's datum that a wave-15 in-place consumer rewrite dropped)

The mixed-petal bucket is non-empty and its mass is measured: 43
mixed-petal floor-band contributors vs 10 full-petal at (16,8,97)
(catch #176; 4x the full-petal mass at the smallest cell). The v7 L1
payment ladder PAYS a large frontier (ev-wired) but does NOT close the
red — residual = growing-p, balanced sub-Johnson strip, growing
source-core/defect polarity, and internal non-intrinsic rechart payment, each keeping its
Omega(n/log^2 n) rich-fiber forcing from a PROVED node. Node stays
TARGET.

## FIRST-MATCH SCOPE PIN (2026-07-21)

`l1_first_match_totality_scope_pin` imports the set-theoretic content of
upstream's kernel-checked `prefixFibreAtlas_total` theorem and proves the
multivalued carrying-chart form directly. Every image contributor carried by
at least one finite source chart has a unique least owner under any fixed
chart order; the owner cells are disjoint and exhaustive. Thus "canonical
first-match supply" is not an independent coverage gap.

The source-layout part of the former fourth mechanism is now retired by
`l1_general_first_layout_domination`. Fixing the first admissible maximal
source layout carries every listed non-anchor; all later source-layout mass is
among its `M=O(log n)` planted anchors. What remains is narrower: prove a
polynomial bound on the **sum of valid owner-cell payments** for unanchored or
refined internal maps, growing signed polarity, and arbitrary petal locators
inside this one source. First-match totality alone still gives no bound on
those residual keys or their budgets. This does not change the node's TARGET
status.

## JOINT BACKGROUND-ANCHOR PAYMENT (2026-07-21)

`l1_maximal_background_anchor_injection` imports and independently proves
upstream Lemma B3 and Proposition B4. In every exact maximal-chart support
cell, the contributor count is at most

```text
q^max(0,d-max(r,a_*)+1),
```

using the better of the largest petal and exact background agreement as an
anchor. Its explicit `(t,u,r,E)` ledger must be charged before any residual
census. The remaining issue is still aggregate: unanchored/refined internal
maps, growing signed polarity, and arbitrary-locator first-owner payment may
escape the fixed-stratum bound.

Raw summation of these cell bounds is formally ruled out as a closure route.
`l1_raw_support_ledger_exponential_route_fence` gives a balanced legal formal
profile on which the root-pinning ledger is at least `2^(n/4)` and one
background-anchor allowance is at least `q^(n/8)`. This is not a realized
counterexample; it proves that the missing ingredient must remove
algebraically infeasible supports or inject many support patterns into one
shared payment.
No red status changes.

## GENERAL FIRST-LAYOUT DOMINATION (2026-07-21)

`l1_general_first_layout_domination` proves, for arbitrary `ell`, that one
admissible maximal source layout universally carries every listed codeword
except its own `M` anchors. Therefore a global bound follows from the complete
non-planted payment in that one layout plus `M`; no census of non-intrinsic
maximal source layouts is needed. Growing source-layout polarity may still
obstruct an internal quotient normal form, but it is no longer a chart-count
multiplier.

## FIXED-SOURCE QUOTIENT-PARTITION ANCHOR CENSUS (2026-07-22)

`l1_fixed_source_quotient_partition_anchor_census` removes the quotient-
polynomial part of the remaining internal rechart multiplicity. In the fixed
first source, a monic degree-`ell` quotient polynomial carrying a whole source
petal `T_i` satisfies `P-a_i=L_(T_i)`. Thus one petal determines the level-
fiber partition up to additive shift, least-petal ownership gives at most
`M` partitions, and all complete-fiber role keys cost at most
`M 3^(n/ell)=poly(n)`. Combined with the proved bipolar per-key payment, every
fixed-polarity genuine quotient common-pencil box is polynomial across
non-intrinsic anchored partitions. The node stays TARGET: Forney coefficient
keys inside one partition, smaller-fiber refinements or a theorem supplying a
whole-petal anchor, growing polarity, and arbitrary-locator cells remain open.
Dense support on a source petal is not itself such an anchor.

## ANCHORED TRIPLE-POLARITY CLOSURE (2026-07-22)

`l1_fixed_source_anchored_triple_polarity_closure` composes the anchor census
with the strict-window and thin-edge CRT bounds. It allows arbitrary partial
source cores and explicitly charges core and defect points outside the
complete quotient fibers. Across every anchored partition and canonical
source chart, a fixed layout/core-defect/petal-polarity box costs at most

```text
M (R_0+1)(B_0+1)(P_0+1)
  16^(n/ell) n^(R_0+B_0+P_0) q^(2P_0)=poly(n).
```

Thus all numerator/Forney multiplicity is paid in this scope; it is not a
remaining first-match key. The node stays TARGET because an internal quotient
map need not carry a whole first-source petal, polarity may grow, and arbitrary
petal locators need not lie in one common pencil.

## TAME FIXED-PETAL REFINEMENT CENSUS (2026-07-22)

`l1_tame_fixed_petal_refinement_census` now removes quotient-polynomial
multiplicity from the tame part of the smaller-fiber branch. If a degree-`s`
map partitions one whole `ell`-point source petal into complete fibers, then
its locator is `F(P)` with `deg F=ell/s`. When the characteristic does not
divide `ell/s`, the top `s-1` locator coefficients determine `P` up to an
additive constant. Hence all tame refinement-map classes in the fixed source
number at most `M tau(ell)<=n`.

This does not promote the node. At small `s`, a known partition still has
`n/s` fibers and the raw role count `3^(n/s)` can be exponential. Wild
divisors, unanchored maps, growing polarity, and arbitrary locators also
remain. The exact `F_9` additive fixture in the proved node shows that wild
right-component uniqueness is false without extra multiplicative-domain
geometry; it is not an admissible-domain counterexample.

`l1_tame_refinement_periodic_owner_obstruction` also rules out the immediate
payment shortcut. On an exact `F_17^*` chart, one source petal is three
complete fibers of the tame affine quadratic `X^2+3X`, but the contributor's
13-point agreement support has trivial multiplicative stabilizer. Therefore
the existing exact-periodic owner and cyclic quotient descent do not absorb
all tame refinements. The missing small-scale theorem must own general
polynomial pullbacks or pay their aperiodic exact supports collectively.

`l1_general_pullback_interleaving_descent` now supplies the owner interface
for the fully fiberwise part. Every degree-below-`k` polynomial has a unique
`sum_(j<s)X^j g_j(P)` form, and Vandermonde inversion on complete `P`-fibers
turns agreement into one common-support interleaved quotient list. If `b`
complete labels are present, the only map-to-evaluation multiplicity is the
exact factor

```text
q^kappa,       kappa=sum_j max(0,k_j-b).
```

For a full complete-fiber partition, `kappa=0`; sub-square-root interleaving
then collapses to the ordinary quotient list. This still does not promote L1:
the realized label domain need not be a smooth prize domain, and partial
fibers, sparse complete-fiber coverage, and the required ordinary quotient-
list bound remain live.

`l1_full_pullback_divisibility_johnson_closure` pays a sharp part of that
ordinary-list residual. On official dyadic rows, a nontrivial fully fiberwise
support cannot have exact size `k+ell-1`; divisibility moves it to the next
quotient agreement. For `s<=k`, every full-partition tame map is then paid
whenever

```text
(k+ell)^2>(k-s)n,
```

while `s>k` is automatic. A direct intersection count gives at most
`(n/s)^2` codewords per map and at most `n^3` across all tame maps, without
assuming smooth quotient labels. The remaining full-partition class is now
the explicit nonpositive Johnson gate, in addition to partial fibers, sparse
coverage, wild maps, and unanchored/arbitrary locators.

The later `l1_full_domain_pullback_intrinsic_rigidity` retires even that
nonpositive full-partition gate. If complete degree-`s` fibers partition the
multiplicative domain, then `X^n-beta=F(P)` places `K(P)` in the cyclic
extension `K(X)/K(X^n)` and forces `P=X^s+c`. Hence the partition is
intrinsic and every fully fiberwise exact support is already exact-periodic.
The genuine non-intrinsic pullback residual must have incomplete domain
fibers, partial agreement in a fiber, or no whole-petal anchor. The affine
`F_17` obstruction survives precisely through incomplete and partial fibers.

`l1_partial_pullback_johnson_router` pays the positive-gate part of that
escape without enumerating partial patterns. If `z` is the number of
agreement points outside fully agreed complete degree-`s` fibers, then the
quotient tuple has at least

```text
h_Z=ceil((k+ell-1-Z)/s)
```

agreements whenever `z<=Z`. With `b` complete labels, quotient degree
`d=ceil(k/s)-1`, and sparse-coverage kernel `kappa`, the strict gate
`h_Z^2>bd` costs at most `q^kappa b^2` per map. Fixed `kappa` is polynomial
across every tame anchored map. The live partial-pullback branch is now the
nonpositive gate, unbounded `kappa`, wild decomposition, or missing anchor.
The affine `F_17` witness has `z=5`, `kappa=0`, and remains sub-Johnson.

`l1_pullback_coverage_kernel_tradeoff` removes `kappa` as an independent
escape. The residue-class dimensions sum exactly to

```text
kappa=max(0,k-sb),       kappa<=max(0,z-ell+1).
```

Thus `z<=ell-1` forces `kappa=0`, and
`z<=ell-1+K` automatically supplies `kappa<=K` to the router. The exact
partial-pullback frontier is now the nonpositive Johnson gate, partial-loss
excess `z-ell+1` escaping every fixed cap, wild decomposition, or missing
whole-petal anchor.

## EXACT-SHELL PREFIX/HANKEL BRIDGE (2026-07-21)

`l1_exact_shell_prefix_hankel_bridge` gives a second, global formulation of
the remaining payment. For each exact agreement size `a>k`, the zero fiber of
the depth-`a-k` interpolation-prefix map, after requiring that the support is
the complete agreement set, is in bijection with the degree-below-`k`
codewords having exactly `a` agreements. These exact shells partition
`#ImgFib_U(k+sigma)` without the raw-support overcount.

Inside any sunflower source chart, fixing the missed core `D` and noncore
support `X` carries that global zero-prefix condition unitriangularly to the
existing saturated Hankel equation `T_(X,|D|)(L_D)=0`, including the guards
that exclude extra agreements. Thus there are now two honest closure routes:

1. prove a new row-sharp prefix-flatness theorem directly for the primitive
   received-word exact shells and pay quotient-periodic shells separately; or
2. keep one maximal source and prove that all residual internal owner-cell
   payments aggregate polynomially.

The bridge proves equivalence of the objects, not either payment theorem.
The toy power-sum Q map and the special F2 Myerson statement are not L1
inputs without an explicit map/owner transport theorem. The node remains
TARGET.

`l1_received_word_barycentric_q_scope_fence` identifies the missing transport
precisely. The dual equations for a support `A` have weights
`U(x)/L_A'(x)`, which move with the complete support. A depth-one `F_7`
witness has two supports with the same locator prefix and different
received-word prefixes, plus a nonzero exchange defect excluding a literal
fixed-column representation. Therefore upstream `def:q-row-atom` is not
already the global L1 theorem; the needed input is received-word barycentric
prefix flatness or a new normalization of those moving weights.

`l1_exact_shell_complement_toeplitz_normal_form` supplies that normalization
on every multiplicative-coset domain. Writing `M=L_(H\A)`, the barycentric
moment at depth `j` is exactly `[Z^(n-j-1)]UM`; hence each exact shell is a
primitive monic split divisor `M|Z^n-beta` in one fixed length-`a-k`
Toeplitz coefficient gap, with a cofactor gcd guard for exactness. The map is
unchanged by adding a degree-below-`k` codeword to `U`. The global red is now
precisely flatness of these realized Toeplitz divisor sections, or a budgeted
transport from them to upstream prefix-affine divisor atoms.

`l1_exact_shell_fixed_cofactor_prefix_transport` sharpens this separation.
After translating `U` by a codeword and writing `h=deg U`, the shell of size
`a` is empty for `a>h`; otherwise its error cofactor has forced degree
`e=h-a`. At `e=0`, the exact shell is **exactly one** locator-prefix Q fiber.
For fixed `e>0`, fixing the cofactor determines one deeper locator prefix and
there are `q^e` possible cofactors. The proved
`l1_cofactor_depth_budget_cancellation` gives the exact accounting for
`e<k`: at depth `d=a-k+e`, `q^e` cancels against the `e` extra ambient prefix
coordinates. Under image-normalized Q it instead leaves the exact effective-
image factor `q^d/L_(a,d)`, and integer rounding contributes less than
`q^e`. This is decisive at the deployed rows: the deeper ambient slice is
already sparse at `e=2` for KoalaBear and `e=1` for Mersenne-31, exactly when
the raw possible-cofactor union first exceeds the corresponding row budget.
The active upstream Q theorem is not uniform in `(a,d)`, and no collective
graph-intersection theorem is proved, so the node remains TARGET.

The possible targets themselves are now normalized exactly.
`l1_cofactor_prefix_pade_graph_normal_form` reverses the high coefficients
and writes the product equations as

```text
Qhat(T)Lhat(T)=Uhat(T) mod T^(a-k+e+1).
```

As `Qhat` varies, the locator prefixes form one `q^e`-point codimension-
`a-k` polynomial graph; projection to the first `e` locator coordinates is
bijective and recovers `Qhat`. Exact shells are precisely split locators
meeting this graph, with the complement gcd guard. Thus the sparse-image
problem is not an arbitrary occupied-target census: it is row-sharp
transversality of the split-divisor prefix image against every realized
received-word Pade graph. This theorem identifies the open algebraic object
but does not bound its intersection.

The fixed-syndrome target cannot in general be collapsed to one actual
locator-prefix fiber. `l1_fixed_syndrome_multiprefix_route_cut` independently
proves the field-generic fixed-multipartial polynomial-fold source and replays
an `F_17^*` pair with the same partial agreement and error remainders but
different actual locator prefixes in both orientations. Its deployed
`(u,v)=(1,1)` specialization has an exact source floor `1,693,898`, so a
universal 29-member profile cap is false. The arbitrary-word object is
therefore the complete attained-target sum for `Y-P=L_S H`, with first-match
ownership; `max_z N_Y(z)` alone is not a payment. This is a route cut and
lower source, not an L1 upper bound, so the node remains TARGET.

The cap at `e=k` is now only a chart transition.
`l1_full_locator_pade_section_all_cofactors` uses the full reversed locator
and, for every `e>=0`, characterizes the shell by the same `w=a-k` equations

```text
[T^(e+1..e+w)] Uhat/Lhat=0.
```

Below the cap this section is the prefix Pade graph times `F^(k-e)` and has
exactly `q^k` ambient locator points. At and above the cap the reciprocal
series continues beyond the last locator coefficient; the section remains
exact, but no dimension or cardinality is asserted. Thus `e>=k` is no longer
an untyped list-interior bucket. It is the continued-reciprocal part of the
same split-divisor/Pade-section transversality target.

Primitive local rank collapse is now excluded.
`l1_pade_remainder_jacobian_tangent_dichotomy` identifies the Pade section
with the low-remainder locus `deg(U mod L)<k` and proves

```text
d Rem_U|_L(D)=-(QD mod L).
```

If `gcd(L,Q)=1`, multiplication by `Q` modulo `L` is invertible, so the `w`
section equations have Jacobian rank exactly `w`; the section is smooth of
local codimension `w`. Every rank-deficient point lies on `Res(L,Q)=0`.
Together with the complement exactness guard, exact shells split canonically
into primitive transverse members `gcd(Q,Omega)=1` and tangent members with
`D=gcd(L,Q)!=1` supported inside the agreement roots. An exact `F_17`
witness shows projected rank loss really occurs on the tangent branch. Thus
the remaining primitive problem is global split-point/component
concentration on a smooth section, while tangencies require a separate
first-match payment.

The same-locator tangent locus now has an exact root-set census.
`l1_tangent_hasse_root_pinning_census` assigns a tangent member to the unique
owner `D=gcd(L,Q)` of degree `r`.  Fixing `D` is equivalent to forcing

```text
D^2 | U-P,
```

so `P` must match both the value and first Hasse derivative of `U` at all
`r` roots.  The confluent evaluation map has rank `min(k,2r)`, giving at
most `q^max(k-2r,0)` members per exact owner and
`binom(n,r)q^max(k-2r,0)` in the raw degree-`r` stratum.  Moreover the Pade
Jacobian loses at most `r` ranks.  This removes fixed-root multiplicity and
support-subset overcount, but raw summation over `D` can still be exponential.

That raw owner sum is now superseded by a collective field-independent
precharge.  `l1_tangent_confluent_packet_packing` proves, for every
`0<=j<=floor(k/2)`,

```text
sum_(P:r_P>=j) binom(r_P,j) binom(a-j,k-2j)
 <= binom(n,j) binom(n-j,k-2j).
```

The packets consist of doubled value/first-Hasse conditions at `j` tangent
roots and value conditions at `k-2j` other agreement roots.  Distinct
degree-below-`k` codewords cannot share one.  In particular `j=1` bounds the
entire tangent population without enumerating any of the `binom(n,r)` formal
owners.  In the balanced band, putting `omega=n-a` and `s=n-2a+k`, the
weighted intersection law also gives

```text
sum_(P:r_P>=j) binom(r_P,j) binom(omega,s-j)
 <= binom(n,j) binom(n-j,s-j).
```

Its `j=0` case is complement packing, while positive `j` sharpen
high-tangency tails by exchanging complement marks for Hasse marks.  The
successive ceiling ratio is `(w+j+1)/(r_0-j)`, so complement packing remains
optimal through `r_0<=w+1`; above that threshold the optimum is explicit at
`j=ceil((r_0-w-1)/2)` subject to the legal endpoints.  These
ceilings can still be exponential in the linear band; profiles exceeding the
reserve still require quotient absorption or sharper primitive control.

Each fixed tangent owner also has an exact one-step descent.
`l1_tangent_double_root_descent_to_primitive_shell` sets
`P_D=U mod D^2` and writes `P=P_D+D^2R`.  For `2r<=k`, this bijects the
exact-`D` tangent stratum with a primitive exact shell on the punctured
domain, under

```text
(n,k,a,e,w) -> (n-r,k-2r,a-r,e-r,w+r).
```

For `2r>k` there is at most one member per owner.  This consumes all
tangency in one step and improves the shell surplus, but arbitrary puncturing
need not preserve the smooth-domain hypotheses.  After the confluent-packet
precharge, only profiles whose exact packet ceiling still exceeds the reserve
need further puncture-stable or quotient-owner control.

The global Pade object is now identified exactly with upstream's support
moment.  `l1_pade_split_section_support_moment_inversion` proves, for
`m>=k`,

```text
C_m(U)=#(unguarded split points on the full Pade section)
      =sum_(a>=m) binom(a,m) Z_a(U),
```

where `Z_a` is the exact-shell codeword count.  The complement gcd guard
selects `Z_m`, and finite binomial inversion recovers every `Z_a` from the
support censuses.  Therefore Paper D v13.2's shifted-lattice/split-pencil
`cen(U;m)` is the same unguarded object and any valid upper bound on it
directly bounds the L1 exact shell.  The remaining cross-tree work is the
primitive/quotient owner and base-field finite-budget interface, not an
object-level identification.

The near-rational part of that support census contributes nothing to band
exact shells.  `l1_exact_shell_balanced_shifted_lattice_reduction` imports
the shifted interpolation module and proves that, when
`2m<=n+k-1`, a minimal shifted degree `d_1<=w=m-k` gives either no support or
one unique codeword with exact agreement `n-d_1>=m+1`.  Its potentially huge
`binom(n-d_1,m)` support moment is therefore deleted by the exact-shell gcd
guard.  Every nonempty exact level-`m` shell lies in the balanced profile

```text
w+1<=d_1<=d_2<=omega=n-m
```

and is a guarded point of one two-generator split pencil with coefficient
dimension `omega-w+1`.  The remaining band theorem is consequently the
owner-normalized balanced split-pencil count, not payment of the
near-rational binomial mass.

The complementary deep range is fully paid.
`l1_deep_exact_shell_johnson_closure` proves that at

```text
m_J=floor((n+k-1)/2)+1
```

the ordinary Reed--Solomon Johnson bound contains the entire list in at most
`n^2` codewords.  Every shell with `2m>n+k-1` lies in this one list, so all
deep exact shells together cost at most `n^2`, not one allowance per level.
Over the full prize box this is at most `2^88`; it is a polynomial L1
payment, not a standalone finite-reserve inequality.  Consequently the global
exact-shell residual has complete range coverage: the deep tail is green,
the near-rational band branch is empty after exactness, and only the guarded
balanced split-pencil band remains open.

The transition strip on the band side of Johnson is also paid.
`l1_band_complement_dimension_packing` puts

```text
s=omega-w=n-2m+k
```

and observes that this is exactly the projective dimension of the balanced
complement pencil.  Distinct exact codewords have complement root sets
intersecting in at most `s-1` points, so

```text
Z_m(U)<=floor(binom(n,s)/binom(omega,s)).
```

At linear complement density this costs `exp(O(s))`; fixed `s` is polynomial,
and `s=o(n)` is subexponential with reserve absorption under
`s=o(R log |B|)`.  The deployed frontier has linear `s`, so the theorem does
not close BC there; it removes the sublinear projective-dimension strip before
any profile census.

The band census now has the same exact off-by-one split as Paper D.
`l1_boundary_shifted_lattice_affine_q_cell` proves that the profile
`d_1=w+1,d_2=omega` is projectively one point at infinity, costing at most
one complete codeword, plus one guarded affine Q cell `g_2+A g_1`.  When
`W_1=1` this is exactly a locator-Q fiber with the top `w+1` coefficients
prescribed; for nonconstant `W_1` it remains a quotient/residue affine Q
cell and is not silently transported to that fixed-column atom.  Hence the
remaining band obligation is disjoint: boundary Q at `d_1=w+1`, and
guard-pruned exact interior BC only at `d_1>=w+2`.  Neither row-sharp bound
is proved, so the node remains TARGET.

The raw base-field BC floor does not survive that exact guard at its printed
level.  `l1_interior_bc_floor_higher_shell_q_routing` proves that Paper D's
strict-interior construction at `d_1` consists of codewords with complete
agreement

```text
m'=k-1+d_1>=m+1.
```

Each contributes `binom(m',m)` unguarded level-`m` supports, but every proper
sub-support has an agreement cofactor dividing its complement locator and is
deleted.  Binomial inversion cancels the same mass from `Z_m`.  The codeword
is counted once at level `m'`, where `d_1=(m'-k)+1` is boundary Q.  Thus
Paper D's realized `M_B^disc` floor remains real for raw support and MCA
consumers, but is higher-shell Q mass for L1.  Its `M_B^soft` term remains a
raw-census upper baseline, not identified exact-L1 mass.  The remaining L1
interior obligation is a separate `BC_exact_guarded` census; other exact
level-`m` members may exist, so no interior emptiness or field independence
is claimed.

The entire below-cap polynomial-led interior family now reduces to deeper Q,
not only its deleted floor rays.  `l1_polynomial_led_interior_to_deeper_q_curve`
proves for `1<=e<=k` that a monic degree-`m+e` word has

```text
Z_m(U)=sum_(s in B^e)
  #{T in Fib_(m,w+e)(theta_z(s)):gcd(R_s,Omega/L_T)=1}.
```

The first `e` locator coefficients determine the cofactor `R_s`
triangularly; the next `w` are a recursively determined, injective curve of
depth-`w+e` Q targets.  Thus ambient-normalized deeper-Q density cancels the
`|B|^e` slices exactly.  A discrete max-fiber estimate still leaves an
additive `|B|^e` term, which is already unaffordable in relevant finite
rows unless curve occupancy is controlled collectively.  Polynomial-led
below-cap BC is therefore Q plus a curve-occupancy problem.  The genuinely
separate interior residual is narrowed to nonconstant minimal vectors and
the above-cap primitive section.

Cross-level ownership inside every split pencil is now canonical.
`l1_split_pencil_content_exact_shell_descent` proves that for any
interpolation-module basis and any raw level-`m` member

```text
(W,N)=A g_1+B g_2,       N=Wc,
```

one has

```text
agr(U,c)=m+deg gcd(A,B).
```

The coefficient ideal is invariant under polynomial-unimodular basis
changes.  Hence the exact guard is precisely `gcd(A,B)=1`, while division by
the full content sends every nonprimitive member uniquely to the same
codeword at its true higher shell.  This generalizes the planted-floor
routing and removes same-codeword ray coalescing for nonconstant minimal
vectors as well.  The remaining exact BC wall is the row-sharp count of
**coprime** coefficient pairs in the primitive split pencil, together with
its Q/priority interface; no primitive count is supplied by this identity.

The nonconstant boundary vector now has a canonical planted reduction.
`l1_boundary_q_planted_root_descent` derives

```text
W_1P-N_1=c_P L
```

for every affine exact member.  Thus `D=gcd(W_1,Omega)` divides every complete
agreement locator and all its roots are fixed agreements.  Dividing them out
leaves a root-free rational-Q cell on the punctured domain; if `deg D>=k` it
costs at most one codeword, while otherwise locator degree and codeword
dimension both fall by `deg D`, preserving depth `w`.  The owner is fixed by
`g_1`, so no planted-root subset sum remains.  What is still open is a
row-sharp bound or numerator-preserving quotient transport for the root-free
nonconstant multiplier; arbitrary puncturing is not assumed smooth.

That root-free residual is now identified exactly with Paper D's projective
Conjecture-F language.  `l1_rootfree_rational_q_projective_packing` sets

```text
j=m-r,   d=k-r,   V=span(G,W_1 F[X]_<d)
```

and proves that the exact cell is the full gcd-trivial intersection
`P(V) intersect Dloc_j(H')`; its hyperplane at infinity has no split point.
Distinct root sets meet in at most `d-1` points, giving the exact ceiling

```text
floor(binom(n-r,d)/binom(m-r,d)).
```

This pays every fixed residual dimension `d` and recovers dimension-one
voting without a new conjecture.  At linear locator density the same cap is
`exp(O(d))`, so `d=o(n)` is subexponential and is reserve-absorbable when
`d=o(R log |B|)`.  It can be exponential for `d=Theta(n)`, so the active
boundary obligation is linear-residual-dimensional root-free Conjecture-F
flatness and its first-owner coalescing.

The sparse-image/occupied-cofactor branch now has an exact second-moment
interface.
`l1_growing_cofactor_decorated_shift_pair_compiler` sends every ordered pair
of distinct shell members to disjoint split tails `A,B` and cofactors
`Q_1,Q_2` with

```text
AQ_1-BQ_2=R,       deg R<=deg(A)-(a-k)-1.
```

At scalar cofactor this is exactly the locator-Q shift-pair ledger. If
`gcd(Q_1,Q_2)=1` and `e<=a-k`, each fixed ordered `(A,B)` admits at most one
cofactor decoration. Thus this large region has no hidden `q^(2e)`
multiplicity. The unpaid content is the primitive split support-pair census,
the common-cofactor/tangency branch, and `e>a-k`.

The common-cofactor branch itself now descends canonically. If
`c=deg gcd(Q_1,Q_2)`, division lowers cofactor degree to `e-c` and raises the
effective prefix depth to `(a-k)+c`; every domain gcd root lies in the common
agreement core. Primitive uniqueness therefore extends through
`e<=(a-k)+2c`. The complementary reduced high-cofactor branch is genuinely
different: `l1_decorated_shift_pair_gcd_descent_sharpness` gives ten exact
`F_13` witnesses at the first endpoint `e'=w'+1` on one support-pair shape
while varying `U`. This does not refute L1, but proves that the reduced high
branch must retain its received-word/Toeplitz owner.
