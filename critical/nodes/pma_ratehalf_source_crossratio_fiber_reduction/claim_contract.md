# Claim contract - PMA rate-half source cross-ratio fiber reduction

## Proven

1. Affine changes of the three source labels preserve every agreement set.
2. Distinct labels normalize to one `lambda notin {0,1}`.
3. The source quotient code modulo `RS(C,K_0)` is two-dimensional.
4. The `lambda` coefficient of `P_*` has only `ell-1` possible core zeros.
5. Every three-contributor tail cell forces at least `2b+3a-2` weighted
   nonexceptional votes for its single source cross-ratio.

## Still open

Bound or profile-own the weighted fiber in (XR6), uniformly over the tail
parameters, contributor pair-equality loci, cores, and petal layouts.

## Consumer effect

Use this node as theorem evidence for `petal_mixed_amplification`. It does not
promote the target or become a direct requirement of `imgfib`.
