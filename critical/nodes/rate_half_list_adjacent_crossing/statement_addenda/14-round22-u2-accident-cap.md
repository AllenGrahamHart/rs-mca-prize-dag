# Round-22 addendum (2026-08-07, coordinator-applied on replay): PROPOSITION U2 — the first accident UPPER bound of record; the crux relocated

Source: notes/pilots_20260807/bb_nu_transport/ (PROOFS.md;
transport.py, 1550 checks / 0 failures, fail-closed exits 1;
coordinator-replayed).

**THEOREM BB's method graded for the upper-bound direction** (the
nine-step anatomy): the three quantitative steps — SM(1) shell
concentration, THEOREM AC's Cauchy-Schwarz, the max >= mean
pigeonhole — are all strictly lower-bound-directed and do NOT
transport; concentration is the SOURCE of the accident problem.
The scaffolding (LEMMA DS/FREE bijection + LEMMA SL shell-index
identity) does transport and yields:

**PROPOSITION U2.** On the deep stratum, for every shell gamma,

    Xdeep(gamma) <= M(2L, L-2) = ( C(2L, L-2) + C(L, (L-2)/2) ) / (2L),

attained at odd gamma (Ramanathan/Lehmer with gcd(2L, L-2) = 2 —
the gcd = 1 coprime case on the structural sub-family is banked in
gamma_shell/PROOFS.md; this is its unconditioned-superset
refinement; novelty LOW, value FIRST-OF-RECORD: no upper bound on
the shell population existed anywhere). Verified against
exhaustive toy censuses at 17 cells (three independent counters);
the measured loss factor tracks Q = p (exactly the discarded
relation condition).

**At the witness row** (p = 3*2^41+1, e = 6, exact integers):
U2 = 2^117.0820 vs B* = 2^127.5098 at w = 2^35 — margin +10.4278
bits; BELOW B* throughout v in [35, 39]; VACUOUS at v = 34
(consistent with BB's proved budget break there). Consistency:
U2's heuristic truth brackets gamma_shell's banked proved floor
2^73.061 at +1.436 bits.

**U2 is p-FREE**, so it reaches the e = 1 prime rows BB provably
cannot: coverage of the live prime window [2^129.585, 2^256) is
0% (v=34), 8.64% (35), 58.15% (36), 82.42% (37), 94.15% (38),
99.67% (39).

**CRUX RELOCATED.** In X_w(gamma) <= S(v) + Acc_deep + Acc_shallow,
the first two terms are now supplied (S(v) exact via SM(2); Acc_deep
via U2). The remaining crux is Acc_shallow + aperiodic S — via the
banked LEMMA Y/MW equality W_w = BCH_w (w <= p, true at every
official razor row), this is a CONSTANT-WEIGHT POPULATION CAP for
BCH_w in a prescribed sig class. The sharp deep-stratum route is
gated by the ternary minimum-l1-weight instrument
(integer_code_distance_cert) — the same missing instrument as
mystery 5's GE-WEAK and the F2 lane's non-local obligation
(round-22 f2_rlocality): the four-lane convergence point.

Anti-transport note for the M route: THEOREM AT (banked on
averaged_slope_conversion) — occupancy concentration kappa >= 3
forces nu(A) <= 0, and BB's deep-stratum kappa is 2^33; the
round-21 "BB's method shape matches the M-route's need" lead is
REVERSED.
