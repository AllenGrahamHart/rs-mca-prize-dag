# Round-23 addendum (2026-08-07, coordinator-applied on replay): the constant-weight pricing — what the shared instrument does and does not buy this node

Source: notes/pilots_20260807/cw_shared_target/ (130/29
coordinator-replayed; every FAIL an itemized registered-prediction
miss).

- **The deep stratum is settled territory at v >= 35 and needs no
  new instrument:** the trivial ternary bound U1 = C(128,62) =
  2^124.0820 < B* already suffices; CONJECTURE Z-CEILING (the
  candidate upper companion of THEOREM Z-FLOOR, see the round-23
  addendum on f2_z1_mass_knife_edge) would improve margins by
  32.67 bits at v = 35 but changes NO verdict there.
- **At v = 34 the ceiling would de-vacuum ONLY the e = 1 prime
  rows** (+2.0947 bits below B* at C = 2, reference 2^129.585) —
  exactly the rows recorded as unreachable by BB's method — and
  correctly does NOT de-vacuum the tower rows (by 84.9053 bits),
  in agreement with THEOREM BB. CONDITIONAL on the unproved
  conjecture; labelled as such.
- **THE FIRST OFFICIAL-ROW DATUM on the shared functional:**
  composing THEOREM BB's proved 2^199.575 break-region floor with
  the LEMMA TC identity yields EXCESS >= 2^-11.84 for the official
  deep-stratum object — the true ternary theta sits 11.84 bits
  BELOW its volume heuristic. Consistent with Z-CEILING.
- **Acc_shallow IS NOT BRIDGED — the divergence point of record:**
  the TC fold is a bijection exactly on the 2-adically periodic
  strata; Acc_shallow + aperiodic S are by definition their
  complement; the only remaining bridge (collision/Cauchy-Schwarz)
  loses a measured 0.31-0.50 of kappa*log2 p, i.e. >= 4.565e11
  bits at the official row against this node's 54.45-bit
  tolerance (ratio 8.4e9). The crux stays exactly where the
  round-22 addendum put it: a PRIMAL constant-weight population
  cap on BCH_w in a prescribed sig class — and it is a SEPARATE
  target from mystery 2's ternary-theta terminal. One object, two
  targets.
