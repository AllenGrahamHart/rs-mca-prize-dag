# Frontier

## Proved

- Worst-list size is integer-valued and monotone in agreement.
- The cyclically rotated prefix construction is ordinary-list unsafe through
  excess `17,179,869,183=2^34-1` at the prize-max razor row.
- The exact balanced-incidence Johnson theorem supplies a bigint-computable
  safe anchor `a_IJ(C)`. At the prize maximum it ranges from `3n/4` for
  `B*=1,2,3` down to `1554944255988` once `B*>=332114441762`.
- Explicit two- and three-codeword predecessor constructions meet that safe
  anchor for `B*=1,2`. Hence both complete budget branches have the exact
  crossing `a_L=3n/4` and list radius `1/4`.
- This is the unique largest cap-uniform maximal-prefix specialization of that
  cyclic construction.
- Diagonal tuples preserve every ordinary unsafe witness for all constant
  interleaving arities.
- Once an ordinary safe value is at most `B*`, the official field cap gives
  `B*^2<q`, and the sub-square-root interleaving theorem preserves that safe
  value for every arity.
- On the pure-quartic budget-three boundary, the exact Euler identity
  `TU^3+d=e_4V^3C` has derivative `(TU^3)'=U^2V^2L` with `deg L=1`.
  Consequently `TU^3` has at most three finite critical values, and all
  repeated-factor defects are localized at one point.
- The Euler factor is exactly the second-Wronskian factor divided by `d`.
  Pure survivors split into four printed Belyi defect passports and one
  generic almost-Belyi passport; there are no other ramification cases.
- A passport polynomial has a unique candidate pure norm packet. Its
  coefficientwise Euler lift must pass one degree-four gcd, two exact
  fourth-power tests, base-field splitting, and the harmonic lift matching.
- Harmonic lift matching is one symmetric degree-`24` condition on the four
  roots of the deleted quartic. It factors into three explicit radical-free
  pairing norms, so all eight lift-sign classes can be tested without square
  roots or a symbolic expansion.
- For the spectral gcd `D_Phi`, each pairing norm is one explicit
  difference-of-squares identity in the coefficients of its two quadratic
  factors. The spectral gcd, fourth-power quotients, scalar split, and three
  such evaluations now form one exact acceptance packet.
- On the generic deleted-pair branch, the six exact trace gcd triples now
  have degree at most `2^35`, down from `2^36`, after an even-Jacobi quadratic
  norm reduction. No triple is yet proved trivial.
- In both path-plus-singleton chambers, a direct common-`X^2` completion is
  impossible. The tight-triangle parity kernel forces the three residual
  half-degree factors to coincide up to scalars, contradicting pairwise
  coprimality. Together with the Vandermonde theorem, this excludes every
  direct equal-complete-fiber path construction with fiber size `m>=2`.
- Every direct common-`X^2` four-cycle completion descends to one of three
  quartic pencils indexed by `c=0,1,2` antipodal deleted-root pairs. Added
  roots are automatically distinct. The
  `c=0` denominator is the matched antipodal norm; `c=1,2` replace one or two
  repeated coefficient-square roots by exceptional-pair squares. All have
  primitive map degree `2^38-1` and lower pencil degree at least `2^37-2`.
- The complete boundary compiler transfers to all three cycle strata through
  canonical span. At order `2^40`, both generic and intermediate floor
  residuals are linear and both have primary zeros at `2^38,2^38+1` followed
  by a nonzero coefficient at `2^38+2`. The generic secondary zeros are at
  `2^37-1,2^37`.
- On the two-antipodal-denominator subbranch of the matched `c=0` generic
  cycle boundary, the order-`2^39` signed-cell resultant leaves only
  `q_field=p^2`, `p=1 mod 2^40`. Although this does not always split all
  `2^41`st roots a priori, Frobenius conjugation of the three Mobius-ratio
  equations excludes the anti-invariant source lifts. Every surviving
  normalized lift, outer root, and matching is over `F_p`.
- The same matched parity branch is nonharmonic: the prior complete trace
  levels `1,...,38` plus a new zero-hit census of all `2,247,720` split
  level-`39` classes exclude `q_out=-1`. Its exact post-field classifier now
  runs through the ODE, three scalar branches, trace depth `39`, the corrected
  reciprocal-invariant gate `T/q_out=W^4`, the constant/Legendre gate, and a
  degree-`2^36-1` gcd floor. The old unscaled `T=W^4` gate is invalid for
  exact-order-`2^39` outer ratios in the nonsplit field class.
- Its trace/Jacobi exclusion interface also transfers. For each fixed
  `epsilon`, six degree-`2^37` trace gcds reduce to one coverage-equivalent
  six-gcd degree-`2^36` Jacobi-norm packet, so there are at most twelve signed
  branches total. The torsion prefilter is one order-`2^39` minus norm and a
  37-level plus tower through order `2^38`, with equivalent degree-`2^36`
  trace factors. These norms are specified but not evaluated.
- The two denominator-mismatch completion-root couplings are now exact finite
  packets. The `c=1` packet has 24 binary-quartic invariant tests and the
  `c=2` packet has six. Each test is one scalar equality in the canonical
  outer coefficients and the four roots of `D_*`; no Mobius variable or
  root-permutation search remains.
- Those 30 indexed tests now reduce radical-free to twelve quadratic norms
  for `c=1` and one degree-`6`/degree-`3` pair-trace resultant for `c=2`.
  The latter is symmetric in the coefficients of `D_*` and has an outer
  cubic with nonzero discriminant leading coefficient.
- Every `c=2` survivor also passes an outer-only order-`2^40` trace gate.
  Forty degree-two squarings modulo the outer cubic must end with a
  nonconstant gcd against `Q_40-2`; no dense trace polynomial is needed.
- Intersecting that trace gate with the degree-six denominator pair resolvent
  selects actual pair traces. Common scaling then gives one role-labelled
  `(t,S,P)` chamber modulo
  `(t,S,P)->(t^-1,S/t,P/t^2)`, rather than six labelled pair searches.
- The terminal torsion recurrence already forces the complementary quadratic
  to split over `F_q` and every normalized root to be a square. The official
  field algebra is either prime-field fixed or reciprocal Frobenius with
  `E_j^p=E_(4-j)/E_4`.
- The c2 primary and secondary gaps now have one exact four-term recurrence
  and an equivalent differential divisibility of degree at most `2H-2`.
  The minimal structural target is whether distinct torsion quartets passing
  both gaps must be two-antipodal. Gap-only parity is false; official-torsion
  parity remains unproved.
- If a c2 denominator already has one antipodal pair, its primary gap and
  complementary-root torsion reduce exactly to a sign-free two-variable
  `(X,P)` circuit. Here `X=0` is the second antipodal pair, while the live
  exactly-one-pair branch has `F_H=G_H=0` and a 39-step terminal trace/product
  equation. This removes root labels and splits C2-PAR into one-pair and
  antipodal-free exclusions; neither circuit is yet proved empty.
- In preferred one-pair coordinates `P=cd`, `Z=c/d+d/c`, one has
  `X=P(Z+2)`. Torsion is exactly the coupled half-order sign gate
  `P_39^2=1`, `Z_39=2P_39`, and distinctness is
  `(Z^2-4)(1+P^2-PZ)!=0`. The trace `Z` always descends to `F_p`; only `P`
  may lie on the reciprocal norm-one torus. This removes the square-root
  search but does not prove the circuit empty.
- The twelve `c=1` norms are one coefficient-only iterated resultant.
  A divided quartic indexes exactly the ordered repeated/unused-root pairs,
  and the resultant is `e_4^36` times the old packet product.
- Modulo canonical subgroup scaling, the `c=1` packet is one role-labelled
  `(S,P,c)` chamber with repeated square one. Pair and unused-root torsion
  are equivalent to a forty-step three-scalar recurrence ending at
  `(2,1,1)`.
- On its two-antipodal-denominator subbranch, `c=1` has exactly two source
  role patterns and six outer-trace Mobius tests. Two harmonic classes are
  excluded algebraically, and a complete two-trace characteristic certificate
  excludes the two remaining classes. The parity chamber is nonharmonic.
- On the maximal-degree selected-antipodal `c=2` collision shard, the
  reciprocal quadratic-field affine intersection has one fixed candidate:
  `y=(7+4a)/9`, with affine images `r,-r` for `r=(2a-1)/3`. Its three
  subgroup tests are exactly one forty-squaring scalar recurrence. A
  registered 16-shard screen covered all `4,495,442` progression moduli and
  found no hit, excluding this reciprocal field shard.

## Open

For every remaining branch `B*>=3`, locate the first safe agreement inside
the now-proved bracket. In the razor case,

```text
k+2^34 <= a_L(C) <= a_IJ(C).
```

For `B*=3` and all larger budgets, the integer Johnson test failing at
`a_IJ-1` is not evidence of unsafety.
A closure still needs a stronger upper profile theorem and a matching
predecessor witness at the same candidate.

For the pure-quartic boundary, the remaining task is narrower: classify the
harmonic-matched covers in those five passports, retaining the deleted
quartic `D`; the spectral reconstruction removes auxiliary variables but
does not prove that any passport fails its exact tests. The support norm is
an exact entry gate, not an emptiness theorem: it must still be coupled to the
fourth-power quotients or a complete succinct passport classification.

For the path chambers, only the structure-free `m=1`, mixed-map,
partial-fiber, and primitive locator routes remain from the direct common-map
fiber analysis. The four-cycle fiber-two route remains through one matched
primitive quotient-pencil census and two denominator-mismatch censuses; it is
no longer an unclassified small-fiber geometry.

For the mismatch censuses, the completion-root coupling and its
constant-degree radical elimination are complete. The next open theorem is
to combine the twelve `c=1` norms and the `c=2` pair-trace resultant with the
doubled-order coefficient gaps and canonical span identity. The old
one-parameter norm gate is not a complete compiler for `c=1,2`. The
reconstructed Mobius pole and complementary-factor Pell identities add no
independent condition. On `c=2`, apply the cheap outer torsion-trace gcd
before recovering an actual denominator pair; a passing trace is still only
a necessary class, not a cycle. On `c=1`, substitute the single
coefficient resultant rather than factor `D_*` and loop over twelve root
choices. For theorem work, use the normalized `(S,P,c)` chamber and retain
its split/square conditions; proving its norm nonzero on the canonical gap
locus remains open. On the parity subbranch, use the six trace equations;
discard `q_out=-1` by the completed field/certificate packet and retain
only the nonharmonic tests.

For `c=2`, use the normalized `(t,S,P)` chamber and do not retain splitting
or square class as separate gates. The terminal torsion recurrence proves
both. The live theorem is C2-PAR in the fixed and reciprocal-Frobenius field
chambers. A proof routes all survivors through the existing three-trace
parity/Jacobi interface; it does not by itself evaluate the shared CR-002
norms.

If a proposed c2 survivor already contains an antipodal pair, do not send it
through the general three-parameter chamber. Normalize that pair to
`{1,-1}` and use the proved `(P,Z)` product/ratio trace circuit. Its first
necessary gate is the common-root resultant of `F_H,G_H`, but product torsion
alone is insufficient: the same packet must pass the coupled half-order sign
gate `Z_39=2P_39` as well. The
remaining general C2-PAR attack may therefore assume the quartet is
antipodal-free after the one-pair circuit is excluded.

The reciprocal selected-antipodal maximal-degree collision packet is empty
by the fixed-element recurrence and complete no-hit certificate. Do not
repeat the screen or retain that field shard. Fixed-field, degree-deficient,
and non-selected-antipodal packets are outside this exclusion.

For the matched `c=0` two-antipodal-denominator census, the mathematical
compiler and exclusion interface are complete through the scalar,
twisted-fourth-power, constant/Legendre, gcd, trace-Jacobi, and cyclotomic-norm
stages at `M=2^36`. What remains is a compressed norm algorithm with a
measured pilot, streaming checker, resource estimate, and cost ceiling, or an
analytic nonvanishing proof. The other matched denominator geometries remain
outside this interface.

The existing clean-rate corridor ledger does not cover rate `1/2`, and the
rate-half MCA node is a different counting object.
