# Attack surface

The complete branches `B*=1,2` are closed at `a_L=3n/4` by the proved
low-budget exact-crossing theorem. On the remaining branches `B*>=3`, the
unsafe side at the prize-max razor row is already proved by
`rate_half_cyclic_rotated_prefix_floor`. The safe side now has the proved
exact-integer Johnson anchor `a_IJ(C)`. A closure must narrow the interval
between those two bounds until one candidate has both a safe proof and an
unsafe predecessor.

Selected routes:

1. strengthen the Johnson anchor with rate-half image-fiber/codegree structure
   while preserving its exact integer endpoint;
2. derive a direct profile-envelope theorem for the non-polynomial received
   words left outside the truncated-locator moment-map census;
3. move the safe point upward if necessary and pair the new upper theorem with
   a matching unsafe witness at its predecessor.

On the pure-quartic budget-three branch, use the Euler router before any
coefficient search. The follow-on passport theorem leaves exactly five
cases: four Belyi defect profiles and one almost-Belyi profile with one
moving simple branch point. Work case-by-case with the harmonic lift condition,
the deleted quartic `D`, and the Fermat factor assignment retained. Do not
enumerate arbitrary degree-`4r` low-critical-value maps or treat the passport
as a cover classification.

Before either passport classification or the Fermat decomposition, apply the
proved binary-quartic harmonic support norm. Its three radical-free pairing
norms reject all eight lift-sign classes using only base-field arithmetic on
the four roots of `D`; a zero factor prints the surviving harmonic pairing.
The harmonic-torsion characteristic sieve remains valid and eliminates the
nondegenerate locus in characteristic zero, but its `126`-variable unit-ideal
certificate is not the selected first computation. Aspect-matched finite
counts indicate an abundant harmonic locus, so a valuable successor must
couple the support norm directly to the Fermat decomposition or to one of the
five succinct passport families.

For a succinct candidate `Phi`, reconstruct rather than solve for the norm
variables. Apply the Euler multiplier `(d-m)^(-1)` coefficientwise, compute
the symbolic gcd with `Y^d-1`, and test the two exact fourth-power quotients.
Keep the split-scalar and harmonic-lift tests separate. At official degree,
this route requires a recurrence or other succinct representation; a dense
coefficient vector is outside the compute policy.

After the degree-four gcd is known, use the proved harmonic spectral
quadratic gate. Factor `D_Phi` in each of its three pairings and evaluate the
printed difference-of-squares identity in the two quadratic coefficient
pairs. This is the combined spectral/Fermat/harmonic acceptance packet; a
candidate family that does not preserve these quadratic factors is not yet a
useful passport parametrization.

Do not request the expanded degree-`24` support polynomial: the pairing norm
is already an exact certificate. Any external pure-boundary computation must
be recorded in `notes/PRIZE_COMPUTE_REQUESTS.md` with a finite completeness
boundary, a measured pilot, total cost and storage ceilings, an independent
checker, and explicit PASS/FAIL DAG outcomes.

For the generic deleted-pair branch, use the even-Jacobi norm router before
any subresultant calculation. Work in `w=2x^2-1` modulo the degree-`2^35`
Jacobi primary polynomial and construct each quadratic norm modulo that
polynomial. Keep all six `s`-signed branches; the norm removes only the sign
of `x`. A dense degree-`2^35` batch is still outside the local compute policy.

For the fiber-two denominator-mismatch branch, start from the radical-free
trace-resolvent packet. The `c=1` condition is a union of twelve printed
quadratic norms. The `c=2` invariant and torsion conditions must be
intersected at the same actual-pair trace through

```text
G_2=gcd(K_O,R_D mod K_O,Q_40-2).
```

A coprime `G_2` excludes the branch without factoring `D_*`; a nonconstant
`G_2` now identifies exactly the passing actual-pair trace classes. Normalize
one selected root to one and continue only in

```text
D_A(Y)=(Y-1)(Y-t)(Y^2-SY+P)
```

modulo `(t,S,P)->(t^-1,S/t,P/t^2)`. Do not replay six labelled pair cases.
Use the normalized gap-span compiler: generate the coefficients of
`E^(-1/4)` with its signed four-term recurrence, enforce the primary gap and
the equivalent secondary differential divisibility (which removes the high
window), reconstruct `alpha=4c,beta,gamma` from canonical span,
and only then apply the scalar invariant equation at `z_t=(1+t)^2/t`.
The open task is to compress or exclude this exact interface, not to derive
another root/lift parametrization. The pure fourth-root denominator is already
excluded at the official odd `H`: its coefficient `a_(2H-2)` is nonzero.
Do not conjecture primary-only fourth-root rigidity; twelve explicit non-pure
primary survivors refute it on bounded smooth rows. The minimal unpromoted
route is to prove or falsify

```text
primary gap + secondary square gap
  + official mu_(2^40) root torsion + distinctness
  ==> two-antipodal denominator.
```

It has survived `680,500` normalized quartets in twenty bounded rows. On that
parity chamber use the three trace classes `0,2+r+r^(-1),2-r-r^(-1)` and the
shared CR-002 Jacobi norms; do not create a new norm campaign. The stronger
conclusion that the denominator is pure fourth-root would close official
`c=2` immediately by the pure exclusion, but is not needed for parity
routing. Do not drop the official hypotheses: a split squarefree `F_53`
counterexample passes both gaps but has root orders `52,13,13,52` and mixed
square classes. Thus a gap-only differential proof is impossible. These are
evidence-backed attack directions, not DAG premises. On
`c=1`, use the
coefficient-only iterated resultant: the divided quartic
already removes `A=C`, and ordered pairs preserve the repeated/unused-root
roles. Do not factor `D_*` merely to replay the twelve norms.

No `c=2` official-order run is currently coverage-defined. Large contributor
work belongs under pre-request `CR-002-C2N` until a compressed implementation,
official field ledger, pilot, checker, and dollar ceiling exist.
The torsion-field router removes splitting and square class from that ledger.
Treat the remaining fields symbolically as two chambers: all parameters in
`F_p`, or reciprocal Frobenius `x^p=x^-1` with
`E_j^p=E_(4-j)/E_4`. Do not identify the latter conjugate packet with the
orientation involution.

Split off the exactly-one-antipodal c2 stratum before attacking full C2-PAR.
After normalizing the known pair to `{1,-1}`, use `P=cd` and
`Z=c/d+d/c`, so `X=(c+d)^2=P(Z+2)`. The primary equations are
`F_H(P(Z+2),P)=G_H(P(Z+2),P)=0`. Complementary torsion is the coupled
half-order gate `P_39^2=1`, `Z_39=2P_39`; independent product and ratio
torsion admit false positives. Work over `Z in F_p`, with `P in F_p` in the
fixed chamber and `P^p=P^-1` in the reciprocal chamber. Once this circuit is
excluded, the residual parity theorem is purely antipodal-free.
For a direct theorem attack, normalize the repeated root to one and work in
the role-labelled `(S,P,c)` chamber. Enforce pair torsion by the coupled
`T,P` recurrence, not by a trace condition alone, and retain distinctness.
If the denominator is two-antipodal, branch immediately into the two printed
`R/P` source patterns and use their six outer-trace equations. Do not
import the matched-cycle source tuple. The dedicated `c=1` field ledger and
two-trace certificate now exclude the harmonic value; begin with the
nonharmonic Euclidean/scalar consequences.
The nonharmonic Frobenius router now proves that every accepted
`R1,R2,P0,P1,P2` source lift lies in `F_p`; do not allocate top-lift extension
branches for them or repeat the completed `-8,6/5` characteristic screen.
For the sole residual `R0` role, remove the lift using
`K_t(Y)=t(Y-2)^2+4(t-1)^2`, enforce trace and scalar selection with one common
quadratic gcd, and apply the lift-free constant gate
`4t(1+tH^2)^2+(t-1)^2=0`. Use the proved Jacobi compression below before
attempting to reject these packets; do not materialize the official-degree
Euclidean or Legendre polynomials.
For the `R0` primary/torsion/constant subpacket, that compression is now
proved: use the two degree-`2^36` Jacobi gcds with
`F_epsilon=(1+epsilon zQ^2)^2+z-1`. Their torsion-only prefilters are the
same `R_-,R_+` norms already assigned to the matched `c=0` packet. Do not
request or implement a separate `R0` norm run; extend the shared norm
certificate so a compatible divisor is replayed against both the matched
signed polynomials and `F_epsilon`.
The other five source roles now share the same norm prefilter as well. Use
the exact four source-resultant families `R0`, `R1/R2`, `P0`, `P1/P2` and
their second `x`-sign norms. For each torsion sign there are exactly seven
Jacobi tests: `F_R0,epsilon` and `F_R,F_P0,F_P` for both
`s^2=-epsilon`. A no-divisor result for the shared `R_-,R_+` pair therefore
removes the full generic two-antipodal `c=1` parity chamber; a divisor must be
replayed against all relevant scalar families before any Euclidean work.
Do not pursue the reconstructed Mobius pole or the complementary-factor Pell
identities as extra gates: the converse theorem makes the former sufficient
bookkeeping, and the latter are automatic identities in `U,V`.

An upper bound at an unrelated agreement is not a close. The Johnson anchor
is a rigorous safe bracket, but its failed test one step below is not an
unsafe witness.

The direct falsifier for a proposed remaining-branch certificate is one admissible rate-half
row and received word with more than `B*` codewords at its asserted safe
agreement. Numerical survival is evidence only.
