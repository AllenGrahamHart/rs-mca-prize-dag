# Attack

1. Start from the `imgfib` assembly branches, not from family names in the
   old Conjecture-F notes.
2. Mark exactly where `conj_f` is invoked after full-petal, periodic, PMA,
   and mixed-petal first-owner removals.
3. Reuse the exact root-free rational-Q projective cell and the coordinate
   MDS interface where their hypotheses match.
4. For every remaining branch, either emit an exact linear descriptor or
   remove the unsupported `conj_f` charge and expose its true missing lemma.
5. Mutation-test omitted branches, owner-swapped punctures, and replacement
   of the nonlinear Pade section by its tangent space.
