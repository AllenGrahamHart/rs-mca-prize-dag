# axis4_predicate

- **status:** PROVED
- **closure:** proof
- **refs (legacy repo):** ['proof_sketch/s4_reserve_dictionary.md#2']

## Statement

Resolve: is ABF's Delta_S((f1,f2), C^{==2}) > 0 exactly the same-support degenerate-pencil predicate (a = b = 0 stratum)? Quote-level audit.

## Ledger (migrated notes)

Root authority identified: ePrint 2026/680 (ABF26 survey) — the audit compares against ITS text; repo authors have the PDF (our fetch is Cloudflare-blocked).
