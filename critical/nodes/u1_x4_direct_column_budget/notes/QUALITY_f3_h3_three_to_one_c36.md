# QUALITY CERTIFICATE (red-quality program, charter r2, 2026-07-10)

## Red: f3_h3_three_to_one_c36 (C36')

Q-SUFF: slot R_3 of the U16 amber — C36' => T_3 < n^3 by the exact repaired
identity (audit, 32 checks; the close is f3_h3_direct_floor_conditional_close).
Pure counting statement, full-corridor quantifier (F-1 compliant), no
mechanism asserted. Catch #71 applied at mint: threshold 16n^(4/3), the only
form whose supporting envelope is true.

Q-SURV: EXECUTED (pre-stressed at proposal time): exhaustive 12-prime
n=8192 official-prefix Modal sweep — worst count 66,933,997 = 2.774% of the
repaired threshold; counting method proved sound by brute-force equality at
three small rows; N_3to1/n^2 DECREASING across the prefix (0.9974 -> down).
The audit's counterexample hunt for the envelope also exercised the gate at
(7937,64) exactly. QUEUED: extend the sweep to a second order (n=16384) and
deeper primes; the falsifier is executable at boundary rows (demonstrated).

Q-MIN (annotation): envelope caveat — C36' is deliberately STRONGER than
the h=3 floor needs (it counts unfiltered tuples); falsifying it would NOT
kill the floor (re-route via the ACT(4096) alternate or the {P,-P}
tightening). The 16n^(4/3) subtraction is what the repaired envelope
consumes exactly; a proved {P,-P} strip would restore 8n^(4/3).

## RED RETIRED TO SUCCESSOR (2026-07-12)

C36' -> CONDITIONAL on f3_h3_mobius_excess_half via the proved compiler
chain (cluster audit: every implication re-derived exactly, exact rational
radical bounds at all 29 orders). C36' remains the consumer contract; its
falsifier and 12-prime Q-SURV seed stay live (falsifying C36' falsifies
the successor by modus tollens).
