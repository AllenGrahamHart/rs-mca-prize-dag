# QUALITY CERTIFICATE (red-quality program, charter r2, 2026-07-10)

## Red: f3_hge4_aggregate_budget (HGE4-14)

Q-SUFF: slot Sigma_{h>=4} of the U16 amber — the aggregate 14n^3 reservation
composes 0+1+1+14 = 16 exactly (F-3 compliant, no double-spend; mutation
control 14->15 fails the compiler). The uniform-in-h shape is FORCED by the
band derivation (KB #27: no proved per-h cap for h >= 4; ~2^40 strata at
prize rows). Amendments A3 (full-corridor p), A4 (slice falsifier
semantics), A5 (five-strips convention + F-4 minimal-trade pin) applied at
mint.

Q-SURV: DOSSIER — the banked budget probe (PASS at 1.5% of budget with
positive control, rows h=3..8); h=5/6/7 finite certificate banks at
n=32/64/96/128 (sampled-row evidence, conjugation-free); the first-moment
vacancy heuristic (H_vac = 8..16) with its known norm-gate violator class
(SP-CENSUS-2: A_8 = 976 at F1153/mu32). QUEUED (first executed attempt,
audit-specified): exact fully-stripped h>=4 aggregate census vs scaled
14n^3 at small analogue rows INCLUDING the SP-CENSUS-2 adversarial class +
the n=64 boundary primes — local, exact, and it exercises the stripped
convention that is the gate's soft spot (F-4).

Q-MIN (annotation): the 14 is a compiler allocation, not a predicted sharp
constant — re-balanceable against the h=2/h=3 reservations without
re-surgery (split freedom, the xr pattern); only the total 16 is
consumer-pinned.

## ROUTE 3 + CATCH #95 (2026-07-12, dichotomy replay)

The upstream complete dichotomy is PROVED and replayed (see the node's
route-3 addendum): stripped records are norm-gate events, so HGE4 reduces
to NG-COUNT — an explicit arithmetic object with ~9 orders of magnitude of
room at the binding row under the naive model. CORRECTION (#95): this
certificate's queued census "including the SP-CENSUS-2 adversarial class"
must be re-read — that class is BAND-currency (A_8^nt = 0 exhaustively in
minimal currency at the hero row); the vacancy model is UNVIOLATED in the
F-4 currency, and route 1 (the width cap) is back in play. NG-ZERO remains
NOT consumable (catch #96: predicted false at boundary rows for
4 <= h < H_vac; empirical zeros are all in the vacant zone).

## EXECUTION LOG (2026-07-12): FIRST ATTEMPT SURVIVED; NODE NOW CONDITIONAL

Deep (F-4-pinned) aggregate EXACTLY 0 at every scanned cell: complete-row
certificates at n=16 (6 primes, full band h=4..H_max), h in [4,8] slices
at n=32 (5 primes incl. the adversarial F1153/F1249 boundary rows), h in
{4,5} at n=64. The p=17 sub-threshold control's 60 non-toral trades fully
consumed by strips + minimality. Catch #99 (load-bearing-for-truth): the
UN-pinned shallow mass grows x4.3-5.5 per h and extrapolates across 14n^3
near h~12 — the F-4/A5 re-open clause is now a KILL trigger. Catch #100:
U2/DLI strips have no in-repo operational analog (3/5 implementable);
kill-claims must define them first. HGE4 is now CONDITIONAL on the minted
NG-COUNT red (route 3, dichotomy bridge); this certificate transfers to
QUALITY_f3_hge4_norm_gate_count as its Q-SURV seed.
