# u1_x4_direct_column_budget

- **status:** see dag.json (single source of truth)

U16 (the consumer's true form, re-posed 2026-07-10): at the official
corridor rows, the post-strip primitive active-core residue satisfies
R_post < 16 n^3 — the allowance x4_exactlist_staircase_split actually
consumes (poly-forcing 52/52 + QA.22 27/27 at the six decision candidates;
the 29-row gate table is an owed artifact, catch #75). The former <= n^3
headline is retained in dag.json as a stronger historical evidence line.
Full statement, band pin, route of record, and re-surgery criteria:
dag.json (authoritative).
