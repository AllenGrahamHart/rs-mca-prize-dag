# Dependency sub-DAG

```text
concrete layer injection [PROVED] --------+
distinct-label union recovery [PROVED] ---+--> arbitrary-word coverage [OPEN]
quotient-scale chart dictionary [PINNED] -+--> bounded total atlas [G1 TARGET]
printed exponent fit [OPEN] --------------+

bounded total atlas + per-chart K4 --> primitive total n^(b1+b4)
bounded total atlas + first-match --> staircase chart overlap factor 1
```
