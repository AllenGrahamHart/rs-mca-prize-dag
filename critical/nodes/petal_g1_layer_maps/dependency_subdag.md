# Dependency sub-DAG

```text
concrete layer injection [PROVED] --------+
distinct-label union recovery [PROVED] ---+--> arbitrary-word coverage [OPEN]
quotient-scale chart dictionary [PINNED] -+--> paid weighted atlas [G1 TARGET]
weighted chart ledger [OPEN] -------------+

weighted atlas (`sum(m+1)<=121n^6/128`) + proved K4 --> primitive total `<n^6`
finite covering atlas + first-match --> staircase chart overlap factor 1

quotient-closed atlas + K4 + proved `121/126` reserve compiler
    --> small-scale staircase census [CONDITIONAL only on G1]
```

Upstream `(A2)/(CAT)/(UNIF)` is not a dependency in this sub-DAG. It has only
an analogy edge until the three-part instance map in `upstream_crosswalk.md`
is proved.
