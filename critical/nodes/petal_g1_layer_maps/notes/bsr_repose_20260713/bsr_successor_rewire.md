# SUCCESSOR-A re-wiring — petal_chart_carrying_descent conditional.md (DRAFT)

Replaces the falsified wiring of background/nodes/petal_chart_carrying_descent/
conditional.md (whose single hypothesis was G1 coverage WITHIN THE n^6-CAPPED
WEIGHTED BUDGET — falsified, catches #138-#144).

## The re-wired packet

- **status:** proposed PROVED (pending the house fresh-context audit) — the
  conditional DISSOLVES: Claims 1 (S1-S6), 2 (U0-U2), 3-L0 were already
  PROVED+AUDITED; the single open hypothesis (G1 coverage) is replaced by
  G1' clause (D), which is itself proposed PROVED (bsr_g1prime_statement.md).
  Nothing else in the csp packet changes.
- **the discharged hypothesis (was: G1 coverage, n^6-capped):** for the
  descended word u1 of each fiber-aligned sigma = 1 chart word at an official
  row (k even), G1' clause (D) supplies the DD-consistent minimal-chart atlas
  A'(u1) of reading-A band charts covering every contributor class S' with
  y_nf in S' and |S'| in {k'+1, k'+2}, with per-profile cardinality
  |A'_{a'}| <= C(n'-1, a'-1) <= 719 * Q_2(2a').
- **the re-wired composition (3-C, now unconditional):** per received chart
  word U,

  ```text
  #{classes S of contributors to U with c(S) >= 2, |S| in {k+2, k+4}}
      =  #{consumed-band classes S' of u1}          (S4 bijection, PROVED)
      <=  |A'(u1)|                                   (coverage + U1 uniqueness)
      <=  C(n'-1, k') + C(n'-1, k'+1)                (clause (D) line 2)
      <=  719 * [Q_2(k+2) + Q_2(k+4)],
  ```

  and by L0 this single count simultaneously prices EVERY dyadic scale
  2 <= M <= t. This is EXACTLY the census gate's original column form
  ("<= 719 x planted column per consumed profile"), delivered per-profile:
  scale->=2 classes at official profile (2, 2a') <= 719 * Q_2(2a'), and the
  aggregate over all scale-M cells, M in [2, t], bounded by the same count
  (each scale-M class is K_2-invariant, hence counted once at M = 2 descent).
- **what changed vs the falsified wiring (explicit):** the budget clause
  consumed from G1 moves from "sum(m_chi+1) <= (121/128) n^6" (FALSE at
  fiber-rich cells — the necessary census is >= 3B(c*) ~ 3C(n'-1,k')/(4q(k'+1)))
  to "cardinality <= 719 x column per profile" (TRUE with ~2^33+ margin at
  every named falsification instance). No weighted form is consumed (#141).
  The m'+1 fallback line (S6 tail collapse) is retained untouched as the
  safety line; #136's consumption map is unchanged.
- **pins carried verbatim:** reading A (#129); word-vs-layer normalization
  (#133); margin-exactly-1 (#134) — clause (D) supplies CONCRETE descended
  charts (images deg <= d'-1), not generic auxiliary layers, so U1 applies;
  band parity |S| in {k+2, k+4} (#135); census nonemptiness (#137).
- **scope (unchanged, honest):** k even; sigma = 1 fiber-aligned chart words;
  M > 2 through L0 only (#130). For the census gate these scope-outs are
  immaterial (Lemma COL prices all words, all layouts, odd k included —
  bsr_gate_repromotion.md); SUCCESSOR-A remains the CHART-CARRYING supplier:
  it is what gives the gate's count a per-chart structure (one class per
  chart, first-match assignable) for the G3 charge-once compiler, and it is
  the route that survives if the gate's count semantics is ever re-posed
  from classes to chart-carried objects.
- **aperiodic bucket:** still NOT this node's business (clause (P)/K4). NEW
  cross-reference (catch #145): the odd lifts (X - x_nf)g(X^2) with
  g(y_nf) != 0 — the SAME structure theorem S4, other branch of the
  membership dichotomy — are aperiodic official contributors and populate
  the WIDE band at binomial scale; they are clause (P)'s problem and the
  reason its band pin is the floor band. Any future attempt to widen this
  node's band beyond {k+2, k+4} must first re-read #145.

## Re-surgery criteria

1. The audit replay contradicts any line of the composition above.
2. G1' clause (D) falsified as stated ((D)-1/(D)-2 of bsr_falsifiers.md).
3. The census gate's consumption re-posed away from class counts at the
   consumed band (then re-wire through the m'+1 line or re-open).
4. Pin P1 resolved to the wide band without the odd-lift carve-out (#145
   tripwire — affects the sibling clause (P), and the gate's aperiodic
   bucket wiring must be re-checked before this node's count is consumed
   as "the small-scale part is priced").
