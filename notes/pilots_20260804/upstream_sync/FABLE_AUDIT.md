# Coordinator audit — upstream-sync pilot

**Auditor:** Fable, 2026-08-04. **Verdict: BANKED — headline finding
VERIFIED; addenda accepted in principle, wiring deferred one unit.**

Verified by the coordinator: all SIX aligned_positive_unramified
diagonal-c2-112 nodes are PROVED in our dag — Scott's #1149 is
attacking a block we closed and never exported; upstream's
remaining_unramified=6 is stale (our fault for not exporting). Flag
S-5 CONFIRMED: the six are DIAGONAL cells, so the diagonal-export
hold's premise ("Scott has NOT covered the diagonal cells") is dead —
the hold converts to an URGENT export item (ship before he spends
more compute). Flag S-1's discipline (mapping likely-not-established;
no identity assertion) is endorsed — the export must present our
closure on its own terms, not as a claim about his cells.

The 14 addenda are zero-status/zero-edge/zero-census prose additions;
per the freeze and honest context budgeting, their mechanical
application is DEFERRED to the immediately-next unit (line-audit of
exact texts + apply via statement.md appendices). The proposal is
banked as-delivered. Pilot protocol slip (bare python3 for JSON
formatting) recorded. EXPORT ORDER ADOPTED: (1) the six-cell diagonal
closure, (2) cells 3+6, (3) the band-flip narrative.
