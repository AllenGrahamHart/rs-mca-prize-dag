# Coordinator audit — F2 opening pilot (round 14)

**Auditor:** Fable, 2026-08-04. **Verdict: BANKED — the round's
biggest single-mystery advance. Mystery 2's obligation list shortens
from FOUR items to TWO, and its last analytic-number-theory
dependency is REMOVED.**

Replay: V1-V12 ALL PASS (66 checks; digest reproduced). Hand-checked
the load-bearing steps: THEOREM A's surjectivity (the evaluation
matrix = diag(y_i).Vandermonde(y_i^2), squares distinct because
y -> y^2 has antipodal fibres — exact independence, hence
E[exp S_c] = 2^{n/2} ON THE NOSE with o(n) = 0; brute-forced over all
390,625 frequencies at p = 5); THEOREM B's positivity argument
((O2) <= (O1) via nonnegative integer counts — the obligation is NOT
independent); LEMMA 4's three-line pullback power law ((O3) exact:
PP5.0 carries 2^d as a POWER, not a factor); LEMMA 3's necessary
condition (dim L >= m/log2 p, tight 3856 >= 3855, official margin
7.89x — thin and now load-bearing, CATCH-4 adopted: pin t exactly);
THEOREM C's refutation of uniform T3 (at t >= n_j the folded
frequency is arbitrary — constructive exhibits at three primes; the
1-delta ask collapses to 1.5e-4 by rung 13). The V10 negative (the
parity certificate is the WRONG functional for mass — 675x mass
variation at identical certificate output) correctly kills the
tempting internal route.

ADOPTED:
- (O1) + (O2) DISCHARGED EXACTLY at official rungs 1-13; (O2) implied
  by (O1); (O3) an exact lemma. Remaining for a complete (O1):
  SL-1 (rungs 14-16 — no low-weight ternary vanishing odd-power-sum
  relation; prediction w >= t/2) and SL-2/CATCH-3 (the average-vs-sum
  |K1| normalisation seam — settle WITH the PP5.0 freeze).
- T3-UNIFORM REFUTED; T3 re-posed IN MEASURE (a second-moment
  computation — internal). THE F2 LANE HAS NO ANALYTIC-NT DEPENDENCY.
  The board's mystery-2 description must be rewritten accordingly.
- The banked 1.74/1.98/2.22 drift REPRODUCED EXACTLY and closed (a
  punctured single-condition artefact; the floor holds with c = 0
  included) — the mint-prep auditor's live falsifier direction is
  CLOSED.
- CATCH-1 (the "20k sample" label vs exact match) queued for the
  hygiene pass; CATCH-2 (naming collisions) -> the naming-convention
  alias layer; CATCH-4 (pin t) -> a task.
- THEOREMS A/B/C + LEMMAS 3/4 are MINT-SHAPED — queued for mint-4's
  line-audit alongside the held window-descent package. No status
  flips now (correct posture: "discharged at rungs 1-13" is the
  honest headline).

Round-14 state: three of four pilots banked (crossing opened with 3
theorems + the shared terminal; C1 mapped with the Delta route dead;
F2 mostly discharged). Awaiting: fr_fiber_rigidity.
