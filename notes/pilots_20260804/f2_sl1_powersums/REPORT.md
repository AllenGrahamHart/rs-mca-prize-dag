(RECOVERED 2026-08-06: this pilot's final-message report was never persisted at bank time — a systematic coordinator defect caught by the round-16 es_boundary_adversary pilot. Recovered VERBATIM from the session transcript by task-id; the opening sanity line matches the ledger's bank entry. The FABLE_AUDIT.md in this dir remains the audit of record.)

Clean re-run from scratch under the `tiny` profile (256M/60s): **85/85 PASS**, exit 0, digest reproduced. Re-read the ask — all three routes, the pre-registration, CATCH-4, and the cross-lane verdict are covered.

## REPORT — F2 SL-1 (round 15)

**VERDICT: SL-1 PROVED**, non-asymptotically, at every rung and under every live value of `t`.

**THEOREM SL-1.** Over *any* field, for `W ⊆ μ_n` antipodally closed with representatives `y_1..y_m`, if `Λ` contains `R` consecutive odd exponents then every nonzero `ε` with `Σᵢ εᵢ yᵢˡ = 0` for all `l ∈ Λ` has `wt(ε) ≥ R+1`. Proof: apply `f2_opening` LEMMA 2's own factorisation `diag(yᵢ^{2a+1})·Vandermonde(yᵢ²)` to the **w×w minors** instead of the full `m×m` matrix; the `yᵢ²` are distinct because `y↦y²` has antipodal fibres. With `R = ceil(t/2)` this gives `wt ≥ ceil(t/2)+1 &gt; t/2` — the round-14 prediction confirmed and beaten. `R+1` is `Ω(m)` at rungs 14-16 (fractions 0.509 / 0.255 / 0.127), so no `o(m)`-weight relation exists. It also **subsumes THEOREM A** (the `R ≥ m` case), so one law now covers rungs 1-13 and 14-16.

**THE TRUE WEIGHT LAW** (39 live configurations, full meet-in-the-middle enumeration, 24 attaining it): `wt ≥ min(2R+1, max(p, R+1))`, both branches sharp. At the official row `p = 2^31 ≪ R+1 ≈ 3.5e10`, so it collapses to exactly `R+1` — Theorem SL-1 is predicted **sharp** there.

**PINNED t — and it pins as UNDETERMINED.** `t` has no definition in the repo: a bare literal `t = 7e10` at `f2_opening/verify.py:958` and `:1038`, from the banked product `t·log2 q ≈ 2.15e12` divided by `log2 p`. I reproduced LEMMA 3's 7.89x margin **exactly** (7.892x). But the competing exact value `t* = 8,592,912,739` (`xr_radius_arithmetic/proof.md:41-58`) **violates** LEMMA 3 at rung 16 at 0.9687x — a *sign flip of a proved necessary condition for (O1)*, not a shrinkage, and it shortens LEMMA 2's band from rungs 1-13 to **1-10**. `PROOFS.md:235-237` says only "an order of magnitude" would violate it; the real competitor is 8.15x and already does.

**CROSS-LANE VERDICT — two answers, and the second is the bigger one.**
- The route I was asked to check (crossing LEMMA Y): **shared LENS confirmed, sixth reduction REFUTED.** Both are cyclic codes with consecutive defining sets (machine-checked), but alphabet (0/1 vs ternary), weight regime (BCH vacuous vs BCH everything — opposite ends of the same weight enumerator), and question type (count vs distance) each independently block a drop-in reduction.
- The route I found instead: **SL-1 was already PROVED, stronger, in the DLI/WCL lane** — `background/nodes/dli_wcl_newton_short_window_exclusion` gives `w ≥ 2R+1` for exactly this object, and **no F2 file has ever cited it**. Its `char &gt; w` hypothesis fails at the official row (`p ≈ 2^31` vs `w` up to `2^38`), which is why the characteristic-free proof was still needed. I also produced **6 explicit counterexamples** showing that hypothesis is *necessary*: once `p ≤ 2R` the true minimum weight drops to exactly `p`.

**OBLIGATION LIST RE-PRICED.** SL-1 is a *distance* statement; (O1) at rungs 14-16 needs the *mass*, which is a **count**. I proved new bounds `Z(L) ≤ 2^{m-R}` and `Z(L) ≤ 1 + 3^{m-R}2^{-(R+1)}`, giving a new discharge criterion `t ≥ 1.2263m` (1.631x weaker than LEMMA 2's `t ≥ 2m-1`) — but it does not move the rung cutoff. The sharpest finding: the exact first-moment law `E[Z(L)] = 1 + (2^m-1)(p^{m-d}-1)/(p^m-1)` shows **LEMMA 3 sits exactly at the first-moment threshold for the mass**, and the threshold for killing the ternary dual outright is the same formula in base 3 — the two differ by exactly `log2 3 = 1.585`. New residual **SL-1b**: prove `dim L ≥ m·log_p 3`.

**Honest negatives:** my pre-registered falsifier F6 did not fire in its literal ternary form (underpowered — reported as such, with the honest form S7b firing on 1210 witnesses). §5 (no structural relations) turned out **banked** at `bounded_coeff_norm_gate` — cited, not claimed.

**Files** (all absolute): `/home/u2470931/smooth-read-solomin/prize/notes/pilots_20260804/f2_sl1_powersums/PREREG.md`, `.../PROOFS.md`, `.../verify.py`, `.../results/VERIFY_LOG.txt`, `.../results/sl1_results.json`.

**Flags:** (1) **maintainer-level** — CATCH-4: `q = p^k` is pinned nowhere and it decides whether rung 16 is discharged; (2) missed cross-lane reduction F2 ↔ DLI/WCL; (3) report the `char &gt; w` counterexamples back to the DLI/WCL lane; (4) internal contradiction `m_16 = 2^38` (`f2_opening/PROOFS.md:233`) vs `2^39` (`PREREG.json:58`) — under `t*` the second reading violates LEMMA 3 at rungs 15 *and* 16; (5) mystery 2's residual and mystery 4's heart are the same species of counting problem — a shared terminal.

result: SL-1 PROVED (85/85 machine checks) — every nonzero ternary vanishing odd-power-sum relation has weight ≥ ceil(t/2)+1, beating the predicted t/2, via a characteristic-free Vandermonde-minor/BCH bound that also subsumes THEOREM A; true law measured as min(2R+1, max(p,R+1)); the crossing cross-lane link is a shared lens not a sixth reduction, but SL-1 was found already proved (stronger, uncited) in the DLI/WCL lane; t pins as UNDETERMINED and the competing t*=8,592,912,739 flips LEMMA 3 at rung 16 from a 7.89x margin to a 0.969x violation.