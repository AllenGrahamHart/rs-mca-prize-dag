# Coordinator audit — F2 SL-1 pilot (round 15)

**Auditor:** Fable, 2026-08-04. **Verdict: BANKED — SL-1 PROVED
(characteristic-free, subsuming THEOREM A); ROUND 15 COMPLETE (4/4).
One MAINTAINER-LEVEL catch surfaced: the unpinned t.**

Replay: 85/85 (digest reproduced). Hand-checked: THEOREM SL-1's
minor argument (LEMMA 2's diag.Vandermonde factorisation applied to
w x w minors; distinct squares from antipodal fibres — the same
mechanism as THEOREM A, run locally: weight >= ceil(t/2)+1, BEATING
the round-14 prediction t/2, non-asymptotic, every rung, any field).
One law now covers rungs 1-16 (SL-1 subsumes THEOREM A at R >= m).
The true weight law min(2R+1, max(p, R+1)) measured sharp both
branches (39 configs, 24 attaining).

ADOPTED:
- SL-1 PROVED -> mystery 2's mathematical residual reduces to the
  MASS side: the new Z(L) bounds (2^{m-R}; the 1 + 3^{m-R}2^{-(R+1)}
  refinement; discharge criterion t >= 1.2263m) + the named SL-1b
  (dim L >= m log_p 3 — the base-3 first-moment threshold, exactly
  log2 3 from LEMMA 3's). Mint-shaped with the F2 queue.
- CROSS-LANE: the sixth reduction REFUTED honestly (shared lens;
  three independent blockers) — but SL-1 was ALREADY PROVED STRONGER
  (w >= 2R+1) in dli_wcl_newton_short_window_exclusion, UNCITED by
  any F2 file, with char > w failing at the official row (p ~ 2^31
  vs w up to 2^38) — the characteristic-free proof was genuinely
  needed, AND the pilot's 6 counterexamples showing char > w is
  NECESSARY go back to the DLI/WCL lane as a scope gift. The F2 <->
  DLI/WCL crosswalk joins the mint queue.
- **THE t CATCH (maintainer-level, SURFACED):** t has NO definition
  in the repo (a bare literal 7e10); the competing exact
  t* = 8,592,912,739 FLIPS LEMMA 3 at rung 16 from a 7.89x margin to
  a 0.9687x VIOLATION — a sign flip of a proved necessary condition
  for (O1) — and shortens the surjectivity band to rungs 1-10. Plus
  the m_16 = 2^38-vs-2^39 internal contradiction. RESOLUTION
  REQUIRED before mystery 2's "discharged at rungs 1-13" headline
  can stand: pin q = p^k and t from the rules freeze (a derivation
  task, not compute). Until pinned, the F2 discharge claims carry a
  dated caveat in the ledger (applied here).
- The honest F6-underpowered self-report and the banked structural-
  relations subtraction accepted.

ROUND-15 NET (4/4): A1 LOSSY (W = 5 rollback surfaced, recommend
ratify); (BC) refuted (three-way -> the terminal); the terminal
UNIFIED as (ES) with the coding-theory label refuted by theorem and
Ax-Katz named; SL-1 proved with the t-pin crisis surfaced. BOARD
AFTER ROUND 15: mystery 2 = {the t/q pin, SL-1b, the |K1| seam};
mysteries 1+3+4's deep residuals = (ES) (four-lane) + the W-5
rollback execution + CR-W5-ELL1. NEXT ROUND: the Ax-Katz transfer
pilot on (ES); the t/q pin derivation; the rollback execution on
ratification; mint-4 (a very large queue).
