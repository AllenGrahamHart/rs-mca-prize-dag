# Coordinator audit — (BC) block census pilot (round 15)

**Auditor:** Fable, 2026-08-04. **Verdict: BANKED — (BC) REFUTED as a
uniform bound (the eleventh pre-registered refutation); round-14's
empirical |Bset| = 2 WITHDRAWN; the syzygy leaf's honest residual
ROUTES TO THE SHARED TERMINAL.**

Replay: all four verifiers 0 failures (34,332 exact checks; the FR
positive control reproduces round-14's audited gate ceiling exactly,
and the new bucketed slope-free gate does it 2,618x cheaper).

ADOPTED:
- THE REFUTATION: |Bset| = 4 = 2|Tau| at fully admissible fixtures
  with EXHAUSTIVE tangent gates, above AND below Johnson (no regime
  split — route 3 dead). Round-14's |Bset| = 2 was a PIGEONHOLE
  ARTIFACT: at its pinning, |Tau| >= 2 is impossible by margin
  exactly -(t-1) — the measurement was forced, not informative; it
  is WITHDRAWN from the record (stronger than keeping it empirical).
- THE REUSE LAW: reuse is a FREE PARAMETER at |Tau| = 2 (each in-D
  point carries two scalar dof; two targets consume both) — the gate
  is indifferent. Only from the third target is anything forced, and
  the M >= 3 case is honestly out of toy reach (k >= 17 collapses the
  bucketing) — NOT claimed.
- ROUTE 2's DEATH AT THE ROWS: K = k - ell exceeds e = |D| by
  55-112x at every prize row, so the punctured code is ALL of F_q^D
  and every partition realises. The list-count route is dead by two
  orders, not epsilon.
- THE CIRCULARITY CATCH (pre-registered): the ordered-pair (BC) form
  is IDENTICALLY 2|Tau| — literally the statement the lane exists to
  prove. My re-pose was circular at the rows; recorded as the
  coordinator's fourth statement-level defect caught by a pilot.
- THE HONEST RESIDUAL: the non-circular content is the OFF-D
  agreement statement (|{x in H\D : tau(x) = m_x}| = k+d — how many
  tau hit a prescribed value pattern on H\D at exactly k+d points),
  which IS the mu_n anti-concentration terminal. THE SYZYGY LEAF NOW
  ROUTES THERE TOO: three surfaces (band fullrank, crossing, syzygy)
  converge on ONE terminal question.
- MINT-SHAPED: the bucketed slope-free tangent gate (2,618x speedup,
  positive-controlled) + (CORE-LENS)/(2-TARGET)/(PACK)/(D-LOCAL
  CEILING). sigma > 0 scope limit carried.

BOARD CONSEQUENCE: mystery 1's syzygy branch loses its independent
obligation — (WTB) stays a proved-but-unpaid lemma; the s <= 11/11/10
range is NOT paid; and the program's convergence onto the mu_n
terminal is now THREE-WAY. The in-flight mun_anticoncentration pilot
is now the single most important open thread of the campaign.
