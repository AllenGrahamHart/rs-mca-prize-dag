# Coordinator audit — (FR) fiber rigidity pilot (round 14)

**Auditor:** Fable, 2026-08-04. **Verdict: BANKED — (FR) REFUTED as
stated (the campaign's tenth pre-registered refutation); the counting
consequence survives; the leaf's obligation RE-POSED as (BC).
ROUND 14 COMPLETE (4/4).**

Replay: all four verifiers 0 failures (47,111 + 1,716 + 36 + 8). The
witness is fully audited: the pinned smallest boundary shape
(h=25, ell=3, sigma=0), 36/36 admissibility clauses incl. the
exhausted tangent gate (max agreement = A exactly over 230 slopes x
52,360 competitors), the ell > sigma+2 exception NOT invoked — the
counterexample sits inside (FR)'s intended domain, and 1,710 of 1,716
boundary partitions violate it. The six survivors are exactly the
packed blocks (6t = 12 reproduced) — the conjecture was the packed
case mistaken for the general law.

ADOPTED:
- The NEW STRUCTURAL LAYER: the master two-ray syzygy
  (R_2 L_1 - R_1 L_2 = -delta.rho — the correct home for the
  forced-common-root instinct), self-fiber avoidance (D = the
  psi != phi locus), the SELECTION LENS (W_nu independent of tau —
  the block census IS a punctured-RS agreement census on D), and
  GATE-r. Mint-shaped; queued for mint-4's line-audit.
- **(BC) BLOCK CENSUS** as the leaf's obligation of record
  (|Bset| <= X for fixed (u,v); by the lens a well-posed punctured-RS
  list count; the sharp unmeasured sub-question = block REUSE across
  targets, since (WTB) is non-vacuous only under reuse). The
  empirical |Bset| = 2 (8 runs, one shape) stays EMPIRICAL.
- The brief-staleness catch AGAINST MY BRIEF (THEOREM R
  mischaracterized — BM excludes deficiency, it does not force
  locators; same class as round 13's catch; the anchor-grep rule now
  extends to theorem paraphrases: quote statements verbatim in
  briefs).
- The label fix: the coordinator's conjecture renames (FIB-RIG) to
  clear the banked (FR) collision.
- THE REGIME CAVEAT, prominently: the witness sits ABOVE the Johnson
  radius; the prize rows sit BELOW it — a prize-regime-only rigidity
  mechanism is NOT excluded. (BC) inherits this as its first
  falsifier axis.

ROUND-14 NET (4/4 banked): crossing OPENED (3 theorems + the shared
terminal with mystery 1); C1/C2'' MAPPED (Delta route dead; A1 window
audit = next; CR-W5-ELL1 gated); F2 MOSTLY DISCHARGED ((O1)+(O2)
exact at rungs 1-13; T3-uniform refuted; analytic-NT dependency
removed); (FR) refuted -> (BC). All four mysteries moved in one
round. NEXT-ROUND CANDIDATES: (BC) pilot (with the reuse
sub-question); A1 window-extension audit; F2's SL-1; the mu_n
anti-concentration terminal (the shared deepest question — now
consumed by BOTH mystery 1 and mystery 4; external-shaped);
mint-4 (a large queue: window-descent, 3-drop, UPB, F2 theorems,
crossing theorems, the FR structural layer).
