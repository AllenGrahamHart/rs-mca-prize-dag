# Coordinator audit — fullrank divisor count pilot (round 13)

**Auditor:** Fable, 2026-08-04. **Verdict: BANKED — PARTIAL accepted;
ROUND 13 COMPLETE. The two-leaf landscape is now exactly mapped.**

Replay: all five verifiers rerun (213 checks; the 4 recorded failures
are the pilot's own mis-specified first forms with corrected forms
passing — the honest pattern). Six refuted/corrected predictions
recorded openly, incl. P3 (the recorded coset adversary is NOT
full-rank — THEOREM SHIFT routes it to the sibling syzygy leaf) and
E3 (a band-proper in-scope fixture EXISTS for the route cut, which
STRENGTHENS it).

ADOPTED: (1) the dual form — the leaf is a statement about ONE point
of Gr(2, n-k) (2-plane containment in syndrome spans; tangent gate =
near-MDS position; the O(C(n,k).n) all-members agreement ceiling);
(2) THEOREM SHIFT (rank J_d = d + min(|delta|, d), equality at 25
fixtures) + the adversary routing corollary; (3) the ROUTE CUT: full
rank buys NO anti-concentration (120/11,440 divisors on a full-rank
window, 33 bits above equidistribution, at a band-proper in-scope
fixture) — the node's own no-go now has a witness; (4) LEMMA X0: the
transversality reading (raw inflation = the dependent-union regime;
the maximal currency = transversal; k-packing re-derived dually);
(5) the exact prize-row survival region (M = 2..2^20; ONE depth at
the top scale) + E1' (the general coset class is FULL-rank in
285/288 — the structured residual belongs to THIS leaf);
(6) the M^2-SLACK SELF-REDUCTION: any quotient count <= the same
absolute 0.68n^2 discharges the whole structured class — with the
TRANSFER GAP (maximality/liveness/strip survival through THEOREM D's
bijection) flagged as the prerequisite; (7) the aperiodic residual's
obstruction re-confirmed as THE program's terminal question
(arithmetic anti-concentration / sub-Johnson RS list size on mu_n) —
all four lenses provably equivalent.

ROUND-13 NET: the band promotion now rests on exactly TWO named
obligations — (FR) fiber rigidity (the syzygy leaf, closes everything
at s <= 11/11/10) and the fullrank leaf's pair {transfer-gap check +
quotient count (M^2 slack)} / {aperiodic anti-concentration}. Next
round candidates: (FR) pilot; the transfer-gap check (small,
mechanical); the approximate-shift closure question (flag 4 — decides
the leaf boundary); the s >= 12 mechanism.
