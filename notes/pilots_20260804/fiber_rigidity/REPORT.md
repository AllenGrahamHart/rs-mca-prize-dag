# XR fiber-rigidity boundary fixture - report

- **date:** 2026-08-06
- **critical consumer:** `xr_band_forced_commonroot_syzygy_count`
- **verdict:** exact counterexample to field-independent `(FR)`; official
  post-envelope `(FR)` remains open
- **DAG effect:** none

## Result

The preregistered predictions P1--P4 pass.  The scope restriction P5 also
fires: this is a scaled smooth row with affine target dimension zero, not an
official first-unpaid stratum.  It therefore rules out the broad local
mechanism but does not refute the critical target or the narrower conjecture
adopted by the round-13 coordinator audit.

The exact fixture is

```text
q=193, n=64, H=mu_64, k=4, d=13, h=A-k=18,
A=22, ell=2, r=5, r'=47, sigma=0,
P=X^2+1, Q=X, phi=P/Q=x+x^(-1).
```

Its active defect is five complete inverse-pair fibers.  The two selected
five-point blocks are disjoint, cover the defect, and each have fiber profile

```text
(2,1,1,1).
```

Thus each selected block splits three complete two-point fibers and is not a
union of complete fibers plus at most one point.

The exact stacked depth-13 window has rank `25/26` and one-dimensional left
kernel generated by `(Z_D P,Z_D Q)`.  The reconstructed pair has a joint core
of size `17=k+d`, residual support exactly `D`, tangent ceiling exactly
`A=22`, and exactly two selected core-containing live slopes.  The
lexicographic first-match selector returns the planted support on both.

The primary scan exhausted all `C(64,4)=635376` interpolation anchors and
all 194 projective slopes.  It retained 631,833 canonical codeword pairs,
found one high-core pair, and found live slopes 64 and 192.  Certificate
SHA-256:

```text
91248465187ab72abd9cbb4e9debe6e0feef9e52d26afed7fb568a0826680ec2
```

## Algebraic scope

For a primitive family

```text
(f_tau,g_tau)=(f_*+Q tau,g_*-P tau)
```

and slope `lambda=[alpha:beta]`, every active block point satisfies

```text
tau(x)=(alpha E_*(x)+beta E'_*(x))/(alpha Q(x)-beta P(x)).   (BE)
```

The denominator is nonzero on the block because the invariant residual is
active there.  Equation `(BE)` is a level/interpolation condition for
`tau`; it does not force divisibility by complete `phi`-fibers.  The fixture
shows that primitive deficiency, exact maximality, active-defect locality,
the tangent gate, `sigma=0`, and the normative first-match selector do not
repair that gap by themselves.

Any viable `(FR)` replacement must explicitly use at least one hypothesis
absent here: the official subgroup arithmetic, the first unpaid affine
dimension `s=11,11,10`, or the post-envelope residual profile.  In
particular, a THEOREM-R-style proof from only the local primitive equations
cannot establish the coordinator's shorthand.

## Independent audit

`verify.py` does not import the constructor.  It independently rebuilds the
finite-field arithmetic, domain, interpolation, stacked rank, syzygy,
residual support, fibers, blocks, slopes, tangent maxima, and first-match
supports.  Its full scan agrees on every one of the 194 per-slope maxima and
supports and rejects twelve hostile certificate mutations.

Primary Modal app `ap-z6h81Tc1oAr9HAKqIdbkxZ` and independent-audit app
`ap-wQGFHF5dPs7XXgZEBMq9cC` both completed and stopped normally.  The earlier
app `ap-3mwM41d1SzL3tjsBGyEZoG` failed during packaging before mathematical
work began and was stopped.  The exact pass markers are:

```text
XR_FIBER_RIGIDITY_BOUNDARY_COUNTEREXAMPLE_PASS seed=20260806 subsets=635376 canonical_pairs=631833 live=2 Lp=2 profiles=2,1,1,1/2,1,1,1
XR_FIBER_RIGIDITY_INDEPENDENT_AUDIT_PASS full_scan=true mutations=12
```

## Adjudication

This packet is a **route fence**, not a critical-node falsification.  Keep
`xr_band_forced_commonroot_syzygy_count` at its current status.  Future `(FR)`
work must print its affine and post-envelope scope before proof or experiment;
generic local fiber forcing is no longer an admissible route.
