# Coordinator audit — C1/C2'' sharpest-leaf pilot (round 14)

**Auditor:** Fable, 2026-08-04. **Verdict: BANKED — the lane map is
the deliverable; the Delta route-death is accepted; the (1,5) census
re-price is the actionable output.**

Replay: controls PASS (P1 symbolic identity + the banked engineered-
prime reproduction + Burnside recomputation); the P2 supporting-prime
lemma's 61/61 constructive verification stands on its logged record
(sample_main.json). The Delta verdict is correctly labeled: the
PREMISE is proved, the 4.4e8-bit size is an EXTRAPOLATION from 68
classes surviving 44x pessimism — route-dead for planning purposes,
not a theorem, and the pilot says so.

ADOPTED:
- LANE MAP: ten TARGET slot leaves, minimal set = ALL ten (zero-event
  forced — a single relation anywhere blows the 1/32 budget by
  32-64x; no trade-offs exist); the r3 window extension +5 -> +7 is
  the fault line (it created the six unroutable slots).
- ROUTE LEDGER: max-norm gate DEAD (banked), Minkowski DEAD (banked),
  Delta certificate DEAD (this pilot), census alive ONLY at (1,5)
  (re-priced from "too expensive" to 65-130 CPU-hours — the 2026-07-13
  fence is STALE) and marginally at (1,6). EIGHT OF TEN LEAVES HAVE
  NO VIABLE ROUTE. The v_2-blindness argument (the gate lives on the
  prime field, invisible to the divisor geometry) is banked as the
  structural reason — any ideal-theoretic route is intrinsically
  blind; certificates here MEAN the census.
- A1 ADOPTED AS THE LANE'S NEXT OBLIGATION: audit the +5 -> +7 window
  extension (cheap, derivation-only, highest leverage — a rollback
  takes the board from ten leaves/8 unroutable to four/1 closable).
  A2 (re-pose WCL-ZONE; the W_ext = 0 overkill gap) queued behind it.
- CR-W5-ELL1 RECORDED as a compute request (65-130 CPU-hours, ~2 h on
  64 workers; certificate ~450 MB sharded; falsifier = one eligible
  prime, equal value either way) — GATED ON A1 per the pilot's own
  recommendation; SURFACED to the user, not launched.
- Hygiene flags 1-6 accepted (stale three-layer statement text on the
  zone node; the mislabeled refutes edge; schema drift on two ell4
  shards; __pycache__ pollution) — queued for the next hygiene pass.
- The v_2 ladder update (max observed 20 vs gate 41) and the A4
  large-deviation calibration handoff noted.

Honest scope kept: (1,5) stays TARGET; nothing claimed about the
other nine slots or the consumers.
