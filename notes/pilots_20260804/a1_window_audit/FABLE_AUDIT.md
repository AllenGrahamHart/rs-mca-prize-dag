# Coordinator audit — A1 window-extension audit (round 15)

**Auditor:** Fable, 2026-08-04. **Verdict: BANKED — LOSSY accepted;
W_min = 5 exactly; THE ROLLBACK IS SURFACED TO THE USER (a board
restructure, not a forced correction).**

Replay: 7/7 controls PASS (slot-count law verified through W = 9).
The forensic chain is damning and precisely documented: no inequality
forces 7 (the only inequality in the derivation is a CEILING); the
"7" is one numeral under two semantics (the absolute calibration 7
already meant L+5); the sole argument for L+7 was denominated in the
K' >= 1 instrument that the same document set had already condemned
and retired one round later (flags A1-F1..F4 — an inverted citation,
a mislabeled success, and an intermediate window that was surfaced
but never priced, for 18 days). The binding inequality is now exact:
W_ext > env/2 - 1 = 0.500029 at the unique accident carrier
q = 918552577, payable only by its w6 orbit => W >= 5, and the
margin/slot curve (computed here for the first time) shows +6 -> +7
buys 0.033 K'-units for 3 slots and +7 -> +8 buys ZERO.

ADOPTED: the audit itself; the A2 re-scope (the 21.4x / 1.2112-bit
overkill gap is STERILE — retires zero slots, buys zero allowance
units; A2 demoted to "stop overclaiming"); flags A1-F5 (true
headroom 49%, not 3100%) and A1-F6 (the inverted-fix note) queued for
the hygiene pass. The pilot's honest prediction scoring (verdict
class right, W_min wrong at 6 vs 5) recorded.

**SURFACED DECISION — THE W = 5 ROLLBACK.** For: board 10 leaves/8
routeless -> 4 leaves/1 closable NOW (CR-W5-ELL1 un-gates) + 3 with
small classifiers; consumer arithmetic bit-identical (control C7);
the pre-extension statement text becomes correct again. Honest costs:
census margin 2.854x -> 2.299x; amber-2 watch headroom 42.7% ->
14.9%; the C1-ZERO extractor needs a shorter relation (real, unpriced,
currently unpaid-for either way); W = 5 must be REPLAYED as a
re-posed conjecture, not inherited. W = 6 hedge: not recommended
(most of the cost, little benefit). COORDINATOR RECOMMENDATION:
RATIFY THE ROLLBACK — the costs are watch-line, the benefit is the
difference between a routeless lane and a closable one.
