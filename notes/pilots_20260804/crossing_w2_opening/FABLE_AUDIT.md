# Coordinator audit — crossing w >= 2 opening pilot (round 14)

**Auditor:** Fable, 2026-08-04. **Verdict: BANKED — the crossing
lane's first three theorems; the MERGE CHECK resolved in its honest
form: a confirmed SHARED TERMINAL, not an edge deletion.**

Replay: 152 checks total (74/3-recorded + 59/0 + 19/0; the three
round-1 failures are openly resolved — two choice-of-c artefacts, one
the pilot's own pre-registered iff refuted at (16,6,4,3)).

ADOPTED:
- **LEMMA X** (general-T product equidistribution; MC-3's Lemma 5 is
  the coset instance; removes BOTH the coset and gcd hypotheses; at
  n = 2^41 it applies to ALL 5.3e11 bracket values of w where MC-3
  covered 6). It also RECONCILES PK2's calibration exactly (the
  two-fibre law explains the 29/7-vs-30/8 discrepancy).
- **THEOREM Q** (the w >= 2 dependence is on the CHARACTERISTIC only
  — "per-field certification" is repriced to per-characteristic, now
  a theorem; the shell is tower-invariant while B* grows).
- **LEMMA Y/MW** (W_w <= BCH_w always; equality at w <= p; the
  prize-row arithmetic p >= 2^39+1 > w makes the window conditions
  LINEAR at every official razor row — the crossing heart is a
  constant-weight count in a length-2^41 cyclic code; the MC window
  is a codimension-w COORDINATE subspace — upgrading the band lane's
  route cut from measurement to theorem-grade witness).
- **The q-free mixed-modulus floor** (beats MC-3 up to 12x; honestly
  flagged as no-bite at the binding 2-power row).
- **MERGE VERDICT**: crossing and the band divisor count are the SAME
  counting problem (the LEMMA W == MC-1 identification was already
  machine-checked upstream; Lemma MW gives the shape), with FOUR
  load-bearing blockers to a drop-in reduction (quantifier, arity,
  filters, direction) — and ONE CONFIRMED SHARED TERMINAL: arithmetic
  anti-concentration over mu_n (A5/A6 = the band lane's recorded
  terminal verbatim). Mysteries 1 and 4 share their deepest question.
- The anchor list A1-A8 (A5/A6 EXTERNAL-shaped; A7/A8 Codex-lane
  candidates; F-A7/F-A8/F-Q2 pre-registered with predictions).
- (UP)-side honesty: no L_1 bound for general words — the safe side of
  (RHL-ADJ) is untouched; the crossing red stays red.

Subtraction ledger verified thorough (incl. the L1-lane BCH false
friend correctly not claimed). This is the model opening: three
proved theorems, one route-cut upgrade for a sibling lane, the heart
precisely named, and the external boundary drawn honestly.
