# Coordinator audit — commonroot syzygy pilot (round 13)

**Auditor:** Fable, 2026-08-04. **Verdict: BANKED — NARROWED accepted;
the (FR) fiber-rigidity conjecture ADOPTED as the leaf's named next
obligation; the brief-staleness adjudication ENDORSED.**

Replay: both verifiers rerun (41,077 exact checks; the dimension scan
and tuple-family exhaustion tables reproduced — X = 118/136 at s =
11/10, X = 0 one dimension higher, collapse ratio exactly N/w =
297/596). The pilot's correction of MY brief was right (hard law 5
working against the coordinator again): the leaf already carries 15
proved supplier routers, and the B-M/forced-structure layer was
banked upstream — attacking as posed would have re-derived it. It
attacked the residual the node and the worker's own burn-down
actually name.

ADOPTED: (1) the (WTB) weighted two-block ledger — a proved lemma,
honestly scoped as the abstraction of the (P4F4) double count with a
SINGLE budget for mixed families (reproduces all eight published cap
integers); (2) the exact block budget X (118/136, exact threshold);
(3) THE NO-GO: the affine-dimension ladder caps at s = 11/11/10 —
even |Bset| = 1 exceeds the budget one dimension up, so NO
block-scarcity theorem of any strength iterates upward; the whole
(APT1)/(FSP6)/(FSP7)/(CRE2)/(P4F4) incidence-counting family is
exhausted, and "iterate the census upward" plans are rejected on
sight; (4) the near-packed extension + the NEW EXCEPTION (a tail
fiber reaching size ell breaks 6t rigidity — the ell > sigma+2
hypothesis is load-bearing and must travel); (5) the exact residual
boundary (the split-profile exhibit at ~1e18 x X shows counting alone
provably cannot finish); (6) **(FR)**: every selected block at the
tuple-incidence boundary is a union of full phi-fibers plus at most
one point — (FR) => beta <= 6t < X closes every stratum at s <=
11/11/10 with (WTB) alone. (FR) is the correct home for the
forced-common-root instinct and becomes the leaf's obligation of
record, replacing profile-counting. No status flip (correct — (WTB)
pays nothing without (FR)).

NEXT ANCHORS from this audit: an (FR) pilot (THEOREM-R-style forcing
onto complete pencil fibers); the s >= 12 mechanism question routed to
the signed support-moment interface (must beat ~300 per dimension).
