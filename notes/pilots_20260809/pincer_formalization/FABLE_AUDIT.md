# FABLE_AUDIT — pincer_formalization (round 27)

Coordinator: Fable. Date: 2026-08-09. Pilot: Opus (task ae39f5f59adc10f37,
~17 min, 54 tool uses, 3 interpreter invocations — a reading-heavy
audit, exactly what D0 demanded). Quarantine marker: ledger line 4062,
observed.

## Verdict

**BANKED. D0 = BROKEN, and the stop rule worked: the pilot built
nothing on the broken foundation, fired CATCH-24A against its own D1
mandate, and returned the sharpest correction this node has received.
FLOOR v2's own pre-registered falsifier fires by THEOREM (the
structural-surplus direction, rho in [53.77, 79.88] across all 38
determined scales, plus directly at the razor rows via sigma_0 >
sigma*). The failure is a max-vs-mean type error that the node's own
statement has carried unreconciled since wave 9 — the wave-9 text
recording sigma_0 > sigma*+1 as unsafe sits in the same statement
field as FLOOR v2's "safe above sigma*". The banked survivals +1..+4
were never evidence for the claim: every cell lives at q < 2^128
where B* = 0, measuring the mean object. The "missing formalization"
(WP5's flag) has existed since wave 9 as (RH-ADJ)/a_RH — a stale
flag, not missing mathematics; the custody miss (the wave-9
supersession banners never landed) is repaired with this bank.**

## Coordinator verifications (mine, not the pilot's word)

| what | result |
|---|---|
| the node's own wave-9 refutation text | CONFIRMED in node.json statement field: "sigma_0 = 8,594,128,895 > sigma*+1 is also list-unsafe via the exact simple-pole conversion ... > 2^-83" — coexisting with FLOOR v2, never reconciled |
| HD1 | CONFIRMED verbatim: B_mca(3n/4) <= n <= floor(q/2^128) (rate_half_half_distance_safe_bracket, PROVED) |
| banner absence | CONFIRMED: 0 "superseded" hits in both P6_RATEHALF_SIBLING.md and pro_brief_razor.md before this bank |
| mca_safe's bar text | CONFIRMED verbatim ("the safe half of the rowwise adjacent certificate"; simple-pole "is not an upper input") |
| d0d2.py replay | EXACT: gaps 1,216,157 and 8,586,956,445; ratio 63.978x; rho window [53.77, 79.88] |
| esc.py replay | 6/6 escape suite (8 MATCH/OK lines), exit 0 |
| board r5 exposure | checked: the mystery-1 board entry does NOT carry the FLOOR v2 framing — no board correction needed |

## Audit judgements

- **The binding deliverable order earned its keep**: had D1 run
  first, the round would have manufactured a duplicate of a PROVED
  object on top of a refuted foundation. The D0-first design was the
  right call and the pilot executed it exactly.
- **The re-classification of survivals +1..+4 is the deepest lesson**:
  four falsification-survival entries were banked against a claim
  their instrument could not see (max-vs-mean). The successor pose's
  F3 (pre-declaring the zero-power instrument class) is the
  structural fix and should become standard practice on any pose
  whose claim quantifies over a max.
- **The pilot's honesty ledger is exemplary**: a registered constant
  that was wrong but landed in-window declared as "luck, not
  correctness"; the lgamma substitution for Modal-dependent
  verifiers named as a substitution; the "fourth bookkeeping claim"
  framing corrected to "stale flag" after checking dates; a
  considered-and-dropped trigger swap disclosed as a near-miss.
- **Impact on the running siblings**: staircase_extension is
  UNAFFECTED (it works the (RH-ADJ) program directly — now confirmed
  as the real object); nonpoly_flank_census's fiber-reduction census
  stands on its own (list-side objects); cancellation_recon's D1
  will hit the same consumer texts and D2 (Z-FLOOR transport)
  retargets naturally at S_sparse floors. No mid-flight re-brief
  sent; reconciliation at their banks.
- **The decomposition STOP-GATE FIRED** (the plan's own clause):
  the surplus/collapse-around-sigma_FM design is dead as written.
  Re-surfaced to the user with this bank.
- **Compliance clean**: quarantine held (two above-marker grep hits
  disclosed; sibling dirs listed by path only), compute law total
  (3 invocations, all ramguard; RAMGUARD_TIMEOUT=280 documented),
  scratch-copy rule followed, draft-only held, no subagents.

## Corrections applied (forced; the node stays TARGET — pose, not status)

1. critical/nodes/rate_half_band_closure/statement.md — the round-27
   FORCED CORRECTION section (six particulars; FLOOR v2 superseded by
   its own falsifier; (RH-AC) recorded as DRAFT not adopted; the
   mca_safe lead flagged unverified).
2. critical/nodes/rate_half_band_closure/P6_RATEHALF_SIBLING.md —
   the wave-9-mandated supersession banner, landed.
3. critical/nodes/rate_half_band_closure/notes/pro_brief_razor.md —
   likewise.

## Follow-ups filed (not executed)

- THE LEAD: does HD1 discharge mca_safe's rate-1/2 bar at razor rows?
  A dedicated read of mca_safe/conditional.md's a_safe quantifier —
  if free, a consumer bar closes on a PROVED node. High value, cheap.
- The (RH-AC) adoption + the revised decomposition design — surfaced
  to the user (the plan's gate fired).
- WP5's open item should be marked discharged-stale in a future
  kernel_basis note (low priority; the node correction supersedes).
- F3-style zero-power declarations on max-quantified poses — a
  standing-rule candidate for the next round's briefs.
