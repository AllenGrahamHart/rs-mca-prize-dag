# FABLE_AUDIT — nonpoly_flank_census (round 27)

Coordinator: Fable. Date: 2026-08-09. Pilot: Opus (task ada97dab53f92c0a7,
~48 min, 64 tool uses). Quarantine marker: ledger line 4062, observed
(one below-marker grep line-number hit disclosed, content not read).

## Verdict

**BANKED. The named residual hunt space is closed as a question and
opened as an object: the flank is exactly the positive-slack strata
(top stratum = arbitrary received words), the first-moment law
transfers to it with zero correction (the falsifier does NOT fire —
58/58 exact), and THEOREM CAP is scope-limited to slack 0, with a
real, char-0, two-field, two-scale structural surplus above the
plateau and an explicit clean maximizer (the antipodal-pair
locator). Three laws proved, the prescribed-sum escape closed at
three scales, the sporadic price verified and de-mystified. The
honest residual is sharp: the arbitrary-word maximum's scaling is
undetermined between a collapsing mechanism and a growing one, and
the deciding n=32 maxscan is priced (Modal-class).**

## Replays and verifications (all mine)

| what | result |
|---|---|
| S1 n=8 exhaustive census (31 cells, via the pilot's own driver + the cell list reconstructed from the banked data) | **BYTE-IDENTICAL** — the mean law, histograms, F_MAX, plateau comparisons all reproduce |
| the two-field argmax | verified in BOTH banked maxscan files: MAX_F_SUBSET_over_all_words = 46 at q=10177 AND q=10193, argmax_W1 = 0 in both |
| the argmax probe detail | F_SUBSET 46 / F_LIST 19 / profile {9:16, 10:3} confirmed in s2_probe_argmax.json; the third-field (q=10009) low score is the field-dependence the pilot's own self-correction 4 explains |
| the price | lg C(255,128) = 250.67284 re-derived; 2^-5.2272 / 2^-5.3272 at the slice ends — the two banked figures ARE one quantity |
| the deficit | lg C(127,64) = 123.1714; 4.7286-4.8286 bits re-derived (my first two attempts botched the binomial arguments — my errors, caught against the pilot's numbers, then confirmed exactly) |

Not replayed in full: the n=16 exhaustive S1 (3000s batch; same driver
as the byte-identical n=8 replay, banked per-cell data self-consistent),
the two maxscans (2x2400s; their summary claims cross-checked between
the two banked files + the probe), the 900+ probe batch.

## Audit judgements

- **The falsifier discipline was exact where it matters most**: a
  non-empty flank could easily have been headlined as a falsifier
  fire. The pilot correctly separated the mean object (the
  falsifier's subject — held 58/58) from the max object (the new
  finding — 2 scales, not 3), and reported its OWN registered
  reduction as refuted by its own census, first.
- **The parameterization is the lasting contribution**: "non-poly"
  is now exact (positive slack), the fiber reduction's completeness
  on slack 0 is established (not just for the X^k L_T0 shape), and
  the window-shift form makes the flank computable forever after.
- **The named residual is genuinely undecided and honestly framed**:
  two lower-bound curves with opposite trends; no extrapolation
  claimed; the deciding run priced and named. This slots directly
  into the (RH-AC) picture: the flank surplus is a supply-side
  object bounded far below the 4.83-bit witness need at accessible
  scales, with the top-stratum scaling as the open question.
- **Self-corrections handled correctly**: the subset-vs-list
  near-miss (46 vs 19) caught before reporting; the RNG cross-field
  invalidity caught and all char-0 claims rebuilt on
  field-independent words; a Poisson-consistent value withdrawn.
- **Compliance clean**: quarantine held, compute law total (11
  documented RAMGUARD_TIMEOUT batches, no wall hit), scratch copies
  md5-checked, draft-only held, no subagents.

## Corrections applied

- critical/nodes/rate_half_band_closure/statement.md — round-27
  flank-census addendum: THEOREM CAP scope-limited to slack 0; the
  flank parameterization + the window-shift reduction + the three
  proved laws + P4; the two-field surplus with the explicit
  maximizer; the named residual + the deciding Modal-class run; the
  price verified with the two-figures-one-quantity resolution; the
  witness-supply vs LB-rung deficit disambiguation. No status flip.

## Follow-ups filed (not executed)

- THE DECIDING RUN: exact n=32 t=1 whole-word-space maxscan
  (Modal-class) — resolves the arbitrary-word maximum's scaling,
  the one number that decides whether the flank can ever matter at
  the razor need.
- The antipodal-pair-locator family deserves a closed-form attempt
  (its count profile 16x9+3x10 at n=16 looks structured enough to
  prove).
- The window-shift reduction + P4 are mint candidates (small,
  self-contained, exhaustively verified).
