# FABLE_AUDIT — cancellation_recon (round 27)

Coordinator: Fable. Date: 2026-08-09. Pilot: Opus (task a3c905d5323d8c35c,
~26 min, 55 tool uses, 8 interpreter invocations). Quarantine marker:
ledger line 4062, observed (the ledger never opened at any line).

## Verdict

**BANKED. The recon did exactly what a recon should: it found the
transport already exists (Z-FLOOR's mechanism IS the proved
simple-pole floor — hard law 5 fired on the pilot's own 0.55
registered prediction), killed the weakest usable next-rung floor
twice over at the REAL parameters (no extrapolation), and mapped the
barrier exactly — 11.87 bits at a provably tight normalizer, with
the counting/construction dichotomy named. The blind convergence
with pincer_formalization (quarantined siblings deriving the same
[2^34, 2^39] gap, the same exact factor-2 surplus, the same
mca_safe upper-half reading) is the strongest cross-validation this
node's picture has ever had.**

## Replays and verifications (all mine)

| what | result |
|---|---|
| escape.py / cap.py / attack.py / frontier.py | all four outputs **BYTE-IDENTICAL** to banked |
| the transport claim | rate_half_cyclic_simple_pole_mca_floor/proof.md:42-56 read by me: verbatim the Z-FLOOR skeleton (degree-bound second-moment ceiling + Cauchy-Schwarz) |
| mca_safe bar text | verified in bank 1/4 (same quote, independent read) |
| CATCH-B's target quote | the x28.4 / 4.73-4.83-bit deficit is in the node's own statement blob (witness-hunt section) — confirmed present, now corrected |

The class-profile DP (Result 2, the 9-decimal tightness) and the
family optimization (Result 1, max reach 2^34-1 at (2^33,1)) are
inside the byte-identical frontier.py/attack.py replays.

## Audit judgements

- **CATCH-B matters strategically**: the campaign has been quoting a
  4.8-bit deficit as the band's distance-to-close; the real number
  at a rung that would actually move the reach is 11.87 bits with
  the recovery route dead by exact computation. Anyone pricing a
  band-closure attempt from the old number would have mis-budgeted
  by a factor ~130.
- **The economic self-correction (2) is the report's best moment**:
  the pilot caught that its own registered repair rationale was
  economically wrong BEFORE running, ran the registered test anyway,
  and the test killed the route independently and oppositely
  (conversion goes lossless as q grows). Both layers reported.
- **The declared deviation (P5/P6/P7 not run) is acceptable**: exact
  computation at the real rung parameters strictly dominates the
  registered small-scale ladder for the questions asked; the
  unresolved registered predictions are named, not buried.
- **The barrier map converges with WP7's clean-rate finding** and
  quantifies rate-1/2 as the closest instance (11.87 vs 212 bits) —
  useful triage for any future external-instrument decision.
- **Compliance clean**: quarantine total, compute law total (8
  invocations, no RAMGUARD_TIMEOUT needed), scratch copies used,
  draft-only held, no subagents.

## Corrections applied

- critical/nodes/rate_half_band_closure/statement.md — round-27
  cancellation_recon addendum: the blind-convergence note, the
  self-subtracting transport, CATCH-B/C (live deficit 11.87 bits,
  the 7 unrecoverable), the double kill of the next rung, the named
  barrier + sharpest bracket, the sharpened consumer-bar map, K5
  discharged-stale. No status flip.

## Follow-ups filed (not executed)

- The mca_safe/HD1 lead (bank 1/4's flag) is now REINFORCED from a
  second direction: this pilot independently confirms mca_safe
  consumes the upper half only. The dedicated read of its a_safe
  quantifier is the cheapest high-value item on the board.
- K5's kernel re-mint ((2^34-1, a_RH-k-1]) — fold into the (RH-AC)
  adoption decision.
- The barrier statement ("counting cannot cross n/log2 q; only
  atypical algebraic clustering can") is the honest input to any
  decision about external instruments or a Przemek consultation on
  the band lane.
