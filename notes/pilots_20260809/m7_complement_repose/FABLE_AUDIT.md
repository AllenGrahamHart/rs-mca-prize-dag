# FABLE_AUDIT — m7_complement_repose (round 25)

Coordinator: Fable. Date: 2026-08-09. Pilot: Opus (task a80b02f14f87e6bd4,
~32 min, 62 tool uses). Quarantine marker: CAMPAIGN_LEDGER line 3731,
observed (pilot never opened the ledger, never read another round-25 dir,
dispatched no subagents).

## Verdict

**BANKED. The pilot's headline holds on replay: the symmetric-difference
re-pose is correct mathematics, ALREADY PROVED in-repo in both
orientations, and DEAD as a mystery-7 route with an exact efficacy
threshold (sigma < 2a) that every FPC5 rate-half cell fails permanently
by its own arithmetic (sigma/a -> 2.5). Bonus decided item: the
(CJ2)/(CJ3) chart hypotheses TRANSFER at M >= 5 — claim (ii) of the
large-source node is FALSE (71/408 rows partially rescued, 1.97% of
d-mass).**

## Replays (all by me, under ramguard, from repo root)

| script | profile | result |
|---|---|---|
| d3_pricing.py | tiny | output BYTE-IDENTICAL to banked d3_pricing.txt |
| d4_cj3_audit.py | local | output BYTE-IDENTICAL to banked d4_cj3_audit.txt (71 rows, fraction 0.01969549, hypothesis ledger 0/1/0/82/0/0/0) |
| d2_arm_a.py 4 97 32 20260809 full | local | summary EXACT MATCH (only elapsed_s differs): OVL hists, NSPLIT, configs_used 32, all_exact_full_chart, all_maxpack_exhaustive |
| d2_arm_b.py 31 16 9 8 20260809 | local | summary IDENTICAL: 8/8 draws EXTRA=0, MISSING=0, OVL_HIST {8: 224} (pure sunflower) |

Not replayed: ell=4 q=193 and ell=5 q=127 arms (same code path as the
replayed ell=4 q=97 arm; banked JSONs consistent), LS6 (pilot re-ran the
round-23 ls6_probe.py unchanged and reproduced the banked 92x figure),
Arm-B t=6/t=8 (same code path as the replayed t=9).

## Hard-law-5 verification (my own reads, not the pilot's word)

Confirmed all four citations: xr_lowcore_near_k_difference_packing
statement.md:24-35 IS (PC3') in difference coordinates with the ownership
argument and a prize-row payment table (NK4/NK6);
l1_band_complement_dimension_packing:27 IS the complement orientation
(CP2) with the matching Theta(n) scope caveat;
l1_official_max_split_value_complement_census:54-56 (MSC4); the m4_t3
fixed-owner bound after removing G IS the common-core orientation. The
round-25a subtraction line on l1_rootfree was therefore FALSE — my
error in the 25a harvest audit, now corrected in place with an inline
marker + dated correction section.

Admissibility arithmetic at (4,1,1) verified by hand: b=1 < 7; the
a-window is empty (floor((1-3)/4) = -1); J = 4(4·1-1+2)+1+2-4 = +19 > 0.
All three fail — the pilot's flag is correct.

## Corrections applied (forced; no status flips)

1. background/nodes/l1_rootfree_rational_q_projective_packing/statement.md —
   inline FALSE marker on the 25a subtraction line + round-25 correction
   section: the three PROVED prior statements, the 723/105/7.7-bit
   decomposition of the "2^836-vacuous" figure (723 bits = ground-set
   error), the mechanism qualification (root-sharing TRUE at the M31
   flats, FALSE at our guarded FPC5 cells — mode-0 overlap, kappa=0,
   PENCIL_MAX=1 in 63/63 exact configs; the sunflower stratum is deleted
   by our own guards), the sigma < 2a kill, the registered falsifier,
   and the first-moment qualification on the "EXACT (2^0)" calibration.
2. critical/nodes/l1_fpc5_large_source_payment/statement.md — claim (ii)
   FALSE marker + round-25 forced-correction section with the honest
   scope (71 partial rows, 0 full, no rate-1/16, CJ4 payments <= n^3).
3. critical/nodes/l1_fpc5_ratehalf_m4_t3_split_slice_payment/statement.md —
   round-25 audit flag: the owner-quality cell (4,1,1) is out of the
   node's parameter family; FLAGGED not resolved.

## Pilot self-corrections, accepted as reported

Seven, all disclosed unprompted: (1) one bare-python3 heredoc patch
(compute-law violation, isolated, all runs complied); (2) a misleading
merged histogram corrected mid-pilot by disaggregation; (3) P9's
registered H5 collision was wrong (careless single-file read, caught by
its own measurement); (4) P5 refuted at tested scale, mechanism found
(first-moment law, ratio 0.980); (5) Q2 refuted (kappa=0 at #1148, no
free sharpening); (6) the ell=6 arm produced zero usable configs and
carries no claim; (7) the 723-bit ground-set decomposition corrects the
calibration of record. Predictions registered before compute: 9
confirmed, 2 refuted, 1 split — the refutations are reported first in
the pilot's own table. This is what a compliant pilot looks like.

## What did NOT change

No status flips. Mystery 7 stands as ratified (the wall is re-described,
not moved; membership and the max-to-mean classification are untouched).
The m4_t2 node's banked 2^(1.61 ell) sharpening is independently
reconfirmed (1.609 recomputed). The 408-row residual count stands. The
LS6 Bonferroni-3 stands (exact where defined; the annulus orientations
are strictly worse there).

## Follow-ups filed (not executed)

- The registered falsifier's cheapest hunting ground: overlap structure
  of the M >= 5 large-source charts legalized by D4 (b can approach ell
  there) — UNTESTED by this pilot, says so explicitly.
- The (4,1,1) repair decision (re-measure at an admissible cell vs
  re-scope the finding) — surfaced in the audit flag, needs no user
  decision until someone wants to spend the chart cost.
- The slice-dimension theorem (dim V = e+1) mint candidate is
  STRENGTHENED by D4 (the t >= 4 rows' flats now have a legal J-sieve
  AND a transfer-audited background guard); still queued for minting.
