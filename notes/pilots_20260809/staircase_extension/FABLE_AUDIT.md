# FABLE_AUDIT — staircase_extension (round 27)

Coordinator: Fable. Date: 2026-08-09. Pilot: Opus (task ab55964a731d5763a,
~50 min, 83 tool uses). Quarantine marker: ledger line 4062, observed
(the pilot read the node statement's mid-session round-27 growth as
primary text — legitimate under the rules; sibling dirs never read).

## Verdict

**BANKED. The residual is diagnosed to the unit (the counting layer
is exactly one slope short, provably at its limit), the truth-vs-proof
question is closed on the officially relevant prime-field axis (the
only violation is a smallest-field artifact at q=17; the formula holds
at every prime field probed, and a PROVED node forces the residual
onto prime fields), the razor determination is established as a NEW
THEOREM rather than a computation, the one field-independent threat
to budget 2^39+1 is killed with a clean realizability law, and the
D4 cross-link re-prices the residual as the gate on the bracket top.
The round's four reports now assemble into a single coherent picture
of the analytic half.**

## Replays (all mine)

| what | result |
|---|---|
| d1_realizability.py (the key certificate set: non-Hankel saturation, nullity-0 certificates) | **BYTE-IDENTICAL** |
| d1_cyclotomic_threat.py (the threat family across N/e/q; the realizability law) | **BYTE-IDENTICAL** |
| escape_anchors.py | exact: B_Q = 389,500,552,609; the a_RH formula anchors; the four Proth-row checks PASS |
| d1_stratum_count.py | exact: 687,194,767,358 ~ 2^39.322 open strata |

Not replayed in full: d3_scale_field_census + d1_robustness (the
q < 5000 field sweep — long; corroborated by the two byte-identical
certificate replays sharing the same machinery, and the banked
results files are self-consistent); the incomplete runs the pilot
itself disclosed (core-line q=97 killed at 430s; the R=12 dead-scale
row) carry no claims.

## Audit judgements

- **The one-slope diagnosis is the round's sharpest single number**:
  the residual's difficulty is now "beat a provably attained cap by
  exactly one," with the m=1 fence proving the whole incidence
  family cannot do it. Combined with the truth probe (prime fields
  clean), the honest posture is: the residual is TRUE, and the proof
  needs the apolar origin — a named, narrow mathematical target.
- **The cyclotomic-threat kill is exemplary falsification work**:
  the pilot found the one structural family that COULD refute budget
  2^39+1 (divisibility holds at the official profile — a genuine
  scare), tested it at every accessible analogue, and banked a clean
  law (realizable iff not over target) with certificates.
- **The D4 cross-link changes the residual's price**: closing budget
  2^39+1 buys the bracket top on a 2-bit q-window, not just the
  residual's own sliver. This should drive the next round's
  prioritization.
- **The compute-law breach (one un-ramguarded patch heredoc) is
  disclosed and isolated** — same class as the round-25
  m7_complement pilot's slip; the pilot switched to Edit afterwards.
  Noted; no pattern (12/13 compliant).
- **Honest incompleteness**: P8's A=1 e-window census not run
  (out of reach) and said so; two killed/unfinished runs carry no
  claims; the out-of-domain (ERC2) evaluation was caught by its own
  absurd output and corrected with the proved branch.
- **Compliance otherwise clean**: quarantine held, scratch copies
  staged correctly (dag.json symlink-reached inside the subprocess
  only), RAM discipline held (the 1G cap fired once and the
  algorithm was redesigned — the guard working as intended).

## Corrections applied

- critical/nodes/rate_half_band_closure/statement.md — round-27
  staircase addendum: the unit-deficit diagnosis + exact sizing, the
  per-layer boundary verdicts, the prime-field truth probe, the
  non-Hankel saturation certificate, the cyclotomic kill + law, the
  D4 cross-link re-pricing. No status flip.

## Follow-ups filed (not executed)

- THE APOLAR TARGET: the residual's proof needs exactly what the
  incidence family lacks — the apolar origin of the Hankel system.
  A narrow, named theorem target; the natural round-28 anchor
  alongside the (RH-AC) adoption.
- The A=1 e in [2,4] census (P8's unrun half) — needs degree-2..4
  curve machinery; price before attempting.
- The realizability law (cyclotomic family) is a mint candidate
  with its certificates.
