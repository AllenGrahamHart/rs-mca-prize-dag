# Circle-domain scope audit (2026-07-07) — VERDICT + one named seam

## 1. Circle domains: NO HOLE in our claim

The OFFICIAL definition (ePrint 2026/680 / open-proximity.tex line 321):
"A set L ⊆ F is smooth if it is a multiplicative coset of a subgroup
H ⊆ F* whose order is a power of two." The word "circle" appears NOWHERE
in the official statement; the grand challenges quantify over smooth =
multiplicative 2-power cosets. Our rules_freeze node already banked this
citation with a drift detector. Upstream's Mersenne-31 CIRCLE rows are
EXTRA-OFFICIAL practice rows on two axes: circle domain (not officially
smooth) AND target 2^-100 (official = 2^-128). Their circle machinery
(twin cosets, Chebyshev fibers, torus uniformization) is value-add beyond
the official prize — importable if scope ever drifts, NOT a required
import for our claim. Of upstream's four deployed adjacent pairs, only
the two KoalaBear rows are official-shaped.

## 2. Extension fields: COVERED BY ARCHITECTURE (better than feared)

The official family admits extension fields (|F| < 2^256, no primality
restriction), and KoalaBear-sextic (q = p^6 ~ 2^186, p ~ 2^31, n = 2^21,
rate 1/2) is exactly a NON-GENERATING row (q_line = q_gen^m). Our DAG has
a DEDICATED lane for precisely this: axis8_generating (PROVED — the
official family admits such rows; q_gen < 2^128 towers live),
f1_classification trichotomy (B-pencil-rational / pole-type /
subfield-confined, each priced), f1_case_tower (PROVED — recursive
per-level pricing), f1_full_field_pole_forcing (PROVED — genuinely
F-valued bad slopes force the pole mechanism), the ef_* extension-fiber
exclusion lane (PROVED — no unpaid hidden-fiber leakage beyond poles +
subfield pullbacks), wild_row_audit (PROVED — explicit Mersenne-power
extension rows audited), m2_antinorm_lever (q = p^2 rows). The fear
"our assembly is prime-only" is FALSE at the architecture level.

## 3. THE NAMED SEAM (the real residual audit item)

At non-generating rows the BASE field is small (KoalaBear p ~ 2^31) while
the budget is large (B* = floor(2^186/2^128) = 2^58). Consequences:
- the e1/zone-(b)/kernel-lattice machinery (which assumes zeta in F_p,
  heights vs p) runs at the BASE prime where |value set| <= p ~ 2^31 <
  B* = 2^58: the certified_valueset_lower strategy is IMPOSSIBLE there;
  base-field pigeonhole FORCES collision mass — this is exactly
  upstream's PROVED subfield census floor M_B phenomenon (their
  beta = log2|B| envelope dependence).
- our assembly prices this through the f1/ef pole + subfield ledgers —
  the AUDIT ITEM is whether those per-level denominators reproduce the
  magnitudes of upstream's PROVED M_B(d1) floors at a KoalaBear-shaped
  row. If yes: independent confirmation of both. If no: one side's
  ledger is mis-normalized (this is homework #1, base-field audit, now
  sharpened to a single concrete comparison).
- bookkeeping: knife_edge_census / census_window_arithmetic speak of
  "admissible primes"; extend the window arithmetic to prime powers
  (mechanical; the Diophantine windows gain the subfield strata).

## 4. Consequences for the correspondence PR

- Our conditional claim covers the OFFICIAL family (incl. KoalaBear-type
  extension rows via the f1/ef lanes); their finite program pins 2
  official rows exactly + 2 extra-official circle rows.
- The M_B-vs-f1-ledger comparison (item 3) is the highest-value
  technical content the PR can add: it either welds the two programs'
  extension-row accounting together or exposes a mis-normalization
  before a referee does.
- The F2<->Q bridge statement must carry the caveat: our F2 evidence is
  prime-row (B = F); their head-depth theorems are base-field-normalized
  — the bridge is exact at prime rows, and at extension rows routes
  through the M_B comparison above.
