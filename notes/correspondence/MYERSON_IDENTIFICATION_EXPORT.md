# EXPORT DRAFT (for user review before any PR): the Myerson
# identification — grounding the shared census kernel in recognized
# open mathematics
# (2026-07-10, from the F2 campaign capstone, log #79-80)

## The finding, in upstream's language

The row-sharp Q problem (prob:row-sharp-q) at the ZERO PREFIX is,
by an exact classical identity, the norm theory of Gaussian periods:
Myerson's combinatorial framing (via Habegger, arXiv:1611.07287,
eq. (2)) states that the deviation of a subgroup linear-equation
census from its flat count equals ((p-1)/p) times a product of
Gaussian-period values. The identification chain, each link proved
or machine-verified in our fork's campaign record:

  row-sharp Q  =  shared census kernel  =  F2's zero-prefix instance
  (f2_zero_prefix_q_equivalence, two independent DPs, 20 cells)
  =  subgroup-census deviation  =  Gaussian-period norms
  (Habegger eq. 2, Myerson's framing).

## The regime map (why this matters for the program)

Every known result (Habegger Thm 1: fixed odd prime length f,
p -> infinity, rate O(p^{-1/(5(f-1)^2)}); Duke; Myerson's cases
f <= 4) and Myerson's conjecture itself are FIXED-f statements. The
deployed/official rows need f growing with p (f ~ p^{0.77}+ in the
Frobenius-tower coordinates). So: the kernel's hard case is a
GROWING-ORDER GENERALIZATION of a recognized open conjecture — the
sharpest placement the kernel has had, and a warning against
expecting elementary closes.

## What our campaign proved around it (replayable, digests in-fork)

- The t-null dichotomy holds unconditionally on the empty band
  (b <= t) and the edge band (extras = 0), any characteristic.
- The Frobenius-fixed sector and the symmetric sector at every tower
  level reduce to proved prime-field machinery (per-condition
  1 + o(1) via a threshold-free Halasz + subgroup Gauss bounds).
- Nine method-classes are fenced by no-gos with quantified failure
  margins; the LSV ergodic corner covers exactly the full-group
  anchors and no more.

## Suggested upstream consumption

(a) cite the identification in the Q ledger (it grounds prob:
row-sharp-q's difficulty); (b) the fixed-f results ARE consumable at
any auxiliary fixed-order rows in the finite certificates; (c) the
growing-order gap is the honest statement of the kernel's remaining
content for the asymptotic-to-finite program.

STATUS: DRAFT — user review required before any PR machinery.
