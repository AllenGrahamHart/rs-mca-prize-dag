# What upstream has that we don't (harvest pass, 2026-07-07)

## A. Proved theorems importable as-is
1. UNIVERSAL UNSAFE CAP (v13 main): emca > 2^-86 >> 2^-128 on
   [1-rho-2^-9, 1-rho) for ALL |F| < 2^256, all four rates, pigeonhole
   only. Unconditional.
2. SAFE-SIDE ANCHORS (we have NONE of our own): MCA error <=
   (floor(delta n)+1)/q below (1-rho)/3 for ALL linear codes;
   MCA-from-CA to half distance; self-contained half-Johnson CA;
   optional BCIKS import. Bolt-in PROVED nodes; check what they
   discharge in our assembly.
3. EXACT SECOND-MOMENT STRATIFICATION: Sum_z N_w(z)^2 = C(n,m) + Sum P_e
   with sp_w shift-pair strata — replaces/sharpens our empirical
   dual-spectrum instruments with an exact identity.
4. **SP DOWNSTREAM FROM Q** (grande finale) + BC->Q chart discharges
   (PR #393): their three inputs have PROVABLY collapsed to essentially
   ONE — the row-sharp Q atom bound R_Q^max <= 2^Delta_Q. Their entire
   conditional program = one number per row vs one margin.
5. Head-depth exactness (w <= 22 KoalaBear): fibers = C(N,m)p^-w(1+eps)
   two-sidedly — PROVED strata of the F2<->Q object.
6. Toolbox: saturation identity for codeword rays; quotient descent;
   one-parameter moving-root theorem; prefix-fiber rigidity (colliding
   fibers differ in >= w+1 points/side); composite-scale collapse
   (divisor hierarchy — organizes what we found as lift identities).

## B. Barriers / negative knowledge
1. **MOMENT-ORDER FLOOR (proved no-go):** any finite-Q proof via the
   moment criterion needs order r >= w*log2|B|/Delta_Q — i.e. 641,593
   (M31 MCA) / 680,397 (M31 list). Every fixed-moment route is
   numerically incapable at the binding rows. Applies to OUR planned
   moment attacks on F2-finite/Q.
2. Their identity-prefix floors REFUTED the broad aperiodic-band
   conjecture below the envelope (exponential aperiodic mass). AUDIT
   DEBT: check whether any of OUR aperiodic-column budgets assume
   flatness in a regime their floors penetrate.
3. Binding-row arithmetic: the whole prize (their route) reduces to
   "max prefix fiber <= 8.42x mean" at M31 list — 3.07 bits. Q atom
   targets printed per row (exact integers).

## C. Instruments / numerics to graft
1. Newton-recursion exact fiber-statistic computer (non-expansive,
   float-safe, per-depth E(s)/C(N,m)) — upgrades our F1/F2 fleet.
2. Group-algebra DP for exact joint fiber censuses.
3. Calibration constants: primitive character maxima decay exponents
   (2.26 -> 0.31, depths 2-10); divisor-hierarchy jump magnitudes;
   max-to-mean tables incl. Poisson-boundary growth.
4. Lean integer-certificate pattern + frontier-adjacent packet JSONs.

## D. Big ideas
1. Entropy-subfield envelope g*(rho, beta) — a PREDICTIVE closed form
   for delta*; we have none. Consistency-check our floors against it.
2. Base-field normalization (beta = log2|B|) + proved M_B subfield
   floors (our audit debt).
3. Mode-at-null framing: extremal-witness factorization as the
   alternative to our budget factorization; testable by our fleet.
4. **Circle/map-smooth/stereographic machinery** (twin cosets, Chebyshev
   fibers, torus uniformization, genus-one lacunarity): the M31 CIRCLE
   rows are one of the two deployed challenge families. CRITICAL AUDIT:
   does OUR assembly cover circle-type domains at all, or is our
   conditional claim multiplicative-only? Potential scope hole in OUR
   claim; their transport machinery may be REQUIRED imports.
5. First-match ledger discipline; row-sharp atom localization.

## Convergence datum
Their frontier collapsed to Q (prefix-fiber flatness). Our campaign's
frontier ranked F1/F2 (the cancellation kernel) as the deep floors.
Same kernel, reached independently by both factorizations — the
correspondence PR's central scientific claim.
