# Fable audit of the sublinear-rank adversarial pilot — 2026-08-02

**Verdict: ACCEPTED — K2: the accounting corrects (per-RAY, not
per-datum), the K_V construction legitimately beats the sunflower
(the banked builder had excluded it by fiat — an instrument catch),
three banked claims are refuted, AND the ratified 0.68n^2 bound
SURVIVES with margin >= 2.9e19, as does N_d <= n/2.** Replayed
stage1 (104/104) and arith_repricing (the two FAILs = the documented
deliberate negatives at the degenerate h=3 row). Hand-checks: the
ray-implication argument (difference of ray rows over a shared core
yields the core rows — sound); M <= C(V,2) definitional; the
support-3 induction step's genuine use of k-packing and the honest
support-4 stopping point; the per-ray floor arithmetic (rho >= ~1.6
suffices vs proved h).

Adopted:
1. **The occupancy accounting is RESTATED per-ray** (amendment to the
   occupancy-v2 audit): the cost theorem's object is the ray system;
   the design ceiling's 191/223/479 are RAY counts with datum counts
   binomial; SHARP-OCC's strong law is REFUTED (K_V, 5.25x), its
   weak form N_d <= n/2 SURVIVES at all six rows; the ratified
   0.68n^2 and the 13n^3 column are UNTOUCHED (margins >= 2.9e19).
   No change to the ratified decisions.
2. **The band lane's named heart RESTATES one more level down**: from
   "sublinear-rank families" to "admissible pairwise-intersecting ray
   systems carrying a SUPPORT-4 RELATION" — with the sufficient
   per-ray charge now 2 vs the proved h. This is the cleanest form
   the gap has ever had; support <= 3 is proved zero.
3. Instrument catch recorded: the banked family builder's
   distinct-forced-slopes clause silently excluded the extremal
   configuration — future battery code must not encode admissibility
   guesses as builder constraints.
4. Mint queue: the per-ray cost theorem + the support-<=3
   transversality lemmas (joining occupancy T1/T2 at minting); K_V
   as the standing calibration adversary alongside MC.
5. Open lane recorded: group-theoretic amplification outside the P3
   strip (the brief's item 2, honestly not attempted).

Caveats endorsed: RAYCAP empirical; upper bound conditional on the
support-4 gap; toy scale; prize figures are exact budget formulas.
