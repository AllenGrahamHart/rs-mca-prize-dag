# Fable audit of the WCL count-bounds pilot — 2026-08-02

**Verdict: ACCEPTED — the named lever is decided: structurally wrong
for the slots (two independent proofs), correctly restated for the
M-bound, and the whole route reduced to one scale-invariant constant
(kappa) whose unconditional bound IS the banked fence.** Replayed
verify.py (25/25). Hand-checks: LAT1 (shift isometry => all Z-minima
equal — immediate and devastating for the second-minimum framing);
LAT5 (flat polynomials give kappa = 1 exactly — the fence is tight,
so no (h,det)-only improvement exists); the 1.507 cap arithmetic
(6^128/q0)^{1/128}; the q_min observation (slot statements quantify
over ALL admissible q — the 43-bit prime is the hardest case, and
prior deficit figures understated by ~214 bits).

Adopted:
1. **Correction of record appended to dli_norm_gate/REPORT.md**: the
   "Minkowski second minimum" lever transfers to the M-BOUND (over
   the ring action = the banked resultant gate), NOT the slots.
   The standing planning line extends: slots need v_2-aware
   certificates (the sparse-certificate route, now confirmed from a
   second direction) — not max-norm gates AND not count bounds.
2. **kappa adopted as the lane's scale-invariant currency** (the
   fence = kappa >= 1 = tight); the doubling sublattice (LAT6) and
   the orbit-granularity invariant (LAT2 — also a proven soundness
   detector, catching a composite in a hand list) join the mint
   queue with the norm-gate package.
3. **The named falsification experiment queued** (weight-5 256-bit
   engineered witness — cheap, would kill the route's last residue).
4. Third independent census reproduction recorded (the strongest
   multi-path validation in the program: four code paths now agree).

Caveats endorsed: h <= 16 exhaustive scope; the h=256 cap's ell=1
scope; the probed-maxnorm conditional rows flagged.
