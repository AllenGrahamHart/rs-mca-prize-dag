# Fable audit of the P-B (H4) hunt pilot — 2026-08-02

**Verdict: ACCEPTED.** The design space collapses (degree-A pencils =
affine lines of AG(h,q); the moment map is the subset-sum map of the
moment curve) and was searched EXHAUSTIVELY at five shapes: the
richest admissible line is always split-fibre, always Gamma_lo = 0.
F1 did not fire — **P-B is not endangered by any construction found**
— and three proved structures land: the shortened-dual lemma L1
(dim(C_S ^ C_T) = max(0, |S^T|-K), 1599/1599), the DESIGN CEILING
(M <= (2(n-K)-1)/(h-1): at most ~960 members of any P-B
counterexample can be independently designed — 2^23-2^117 below
budget; a bankable corollary), and the BLOCK DICHOTOMY (coset blocks
cannot be spread with a live slope-direction — DERIVING
SF-SELFCOLLISION's range). F2 fired honestly (group-orbit pencils:
rank deficit without collision — the counting theorem is not a
lane-closer; but symmetry buys deficit, not excess), and F3 confirmed
designed placements cap at 2 pencil-model supports.

Checkpoint files inspected (EXPA/B/C/D/E/F + official); the exhaustive
enumerations and lemma tables are in the record; hand-verified: the
L1 dimension count, the ceiling arithmetic (Mh vs 2(n-K) with
RS_K x RS_K in the kernel), the coset power-sum vanishing behind the
dichotomy, and the selector nu-logic.

Adopted:
1. **THE SELECTOR CATCH (amendment to the K1 closure):** Gamma_lo = 0
   for split-fibre is a JOINT identity + support-keyed-selector
   theorem, not an identity consequence — adjacent partners live at
   other slopes; a uniform selector leaves ~q e^{-nu} survivors
   (official RowC 1/4 nu = 3.0 => ~2^187), while support-lex gives 0
   at 18/18 across nu in [0.05, 30]. The banked K1-closed conclusion
   SURVIVES (verified at the official ratio) but the (PB-SUPPLY)
   skeleton and the P-B lane note must state the selector hypothesis,
   and K1 re-couples to the PP4.0 A1 fork — one more reason the
   compression-class freeze must be ratified. Dated amendment
   appended to pb_gamma_exposure/FABLE_AUDIT.md.
2. **Gauge-invariance scope correction (for the Route T edit + P-B
   contracts):** the strip/genericity gates read off words are
   VACUOUS under degree-<K gauge; they must be stated on (alpha,
   beta) mod RS_K. Queued with the coordinated edit.
3. **(H4) refined to (H4')** (|Gamma_lo| <= mu + sqrt(4(h-1) mu ln q)
   + O(h log q)); the FRAGILE RowC 1/16 row's 0.77-bit slack is NOT
   consumed by the constant (2^-11.2 there). The precise remaining
   open content: "the only rich lines are block-composition lines" —
   an equidistribution statement whose block-composition exception is
   exactly the (self-colliding) split-fibre locus. That + the q-scope
   call is what remains of P-B; both go to Pro.
4. Mint queue: L1, the design ceiling + forcedness corollary, the
   block dichotomy (with SF-SELFCOLLISION as its corollary).

Caveats endorsed: pencil-model exhaustion is a 2h-dim slice of the
word model (the largest gap, covered only by the DOF theorem +
fixtures); lambda measured at small mu; spread-block infeasibility is
first-moment; the n=24 spread-faithful case did not finish and is
honestly absent from the record.
