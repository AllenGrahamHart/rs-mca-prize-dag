# Fable audit of the DLI norm-gate pilot — 2026-08-02

**Verdict: ACCEPTED — the C1 and C2'' lanes MERGE at junction 0, and
the merged frontier gains a proved lemma chain, an exact splitting
LAW (not a conjecture), an official-scale support-forcing theorem,
and a decisive negative that saves future effort.** The measured
1/phi(n) is discharged as an identity (S1: Galois is a signed
permutation of the ternary set, so the double count is exact) plus a
bounded multiplicity correction (S3), with 0 violations across 1930
rows. The AM-GM ceiling transfers verbatim to arbitrary integer
coefficients (LN4 — the banked proof never used ternariness), and at
the official 256:1 schedule the router criterion collapses to
q <= E^128 uniformly over junctions: **every t-null state at
production q has |S_0| = 0 or >= 4**. The WCL fence law
(q > w^128, ell-independent) explains the lane's own history —
4^128 = 2^256 is EXACTLY the official cap, which is why weight 4
cost 1.4 billion polynomials — and proves no open slot (w >= 5) is
reachable by max-norm gates: the remaining lever is count bounds.

## Independent verification record

- Replayed core.py self-test (4/4, incl. the signed-permutation
  lemma) and analysis.py (the consolidated S1/S2/S3/LN6 audits; the
  printed deviation table shows every deviating row with max_m = 2
  and q^{o+1} < maxnorm — S3's condition catches all 53 with zero
  exceptions).
- Hand-verified the lemma chain: LN1 (alpha in ker pi => q = N(p)
  divides Norm; nonvanishing from the basis-range hypothesis — which
  I confirm is the load-bearing clause, matching the banked
  no-opposite-pairs condition); LN2 (simple transitivity => the o
  primes are distinct); LN4 (the sandwich Claim-2 proof with w -> E:
  correct — Parseval gives h.||a||^2 with no ternariness anywhere);
  S1's double count (sigma_a bijects W_w because a signed monomial
  map preserves support size and coefficients up to sign). All sound.
- Checked the official arithmetic: q^{L_j} <= E^{128 L_j} <=>
  q <= E^128 (the 256:1 ratio); E_min = 4 iff q > 3^128 = 2^202.875;
  4^128 = 2^256 exactly (the cap coincidence is arithmetic fact, not
  numerology); the (1,4) conditional fence 14^64 = 2^243.7.
- The junction-0 identification: U_0 = {1}, cells = the half-system
  basis range — a skew solution IS a C1 half-section relation. The
  byte-identical censuses (three independent code paths now: C1
  pilot, C2'' L13, this bridge) close the loop.
- The pilot's process hygiene was exemplary AND honestly reported:
  a frozen pre-registration with one scored MISS (R3), and a real
  mid-pilot bug (sieve bound) caught by its own cross-check and
  recorded rather than buried.

## Findings adopted (binding on the merged C1/C2'' frontier)

1. **The lanes are formally MERGED at the junction-0 interface**; the
   census bridge, splitting law, and energy ceiling are shared
   infrastructure. Node-local recording deferred to the mint (below).
2. **Mint queue (next boundary): the norm-gate package** — LN1/LN2
   (the forward gate + o-fold upgrade), LN4 (energy ceiling), LN7
   (the splitting law S1-S3), and the OFFICIAL SUPPORT-FORCING
   THEOREM (|S_0| >= 4 at q > 3^128) — all proved, all with cheap
   exact verifiers derivable from the pilot's scripts. This is the
   most mint-ready package any pilot has produced.
3. **Decisive negative adopted as lane law**: no open WCL slot is
   reachable by max-norm gates (all have w >= 5; even the
   conditional fence needs q > 2^289). Effort redirects to COUNT
   bounds (Minkowski second minimum). Recorded so nobody re-attempts
   the dead route.
4. **A new consumer for the C1 doubling-law conjecture**: the (1,4)
   WCL slot follows in two lines IF the law holds at w = 4 to
   N = 256. This sharpens what the C1 conjecture is FOR — and the
   honest flag stands (the pattern is verified to 2N = 128; its
   proposed mechanism is refuted; treat as an extrapolated pattern).
5. **The C2'' reserve currency identity**: the junction-0 mean rho is
   the C1' raw-ledger orbit sum up to an explicit constant — the
   "588x blindness" is exactly the census's low-weight witnesses.
   The C2'' Pro ask updates: the norm-gate residual framing is now
   GROUNDED (identity-level bridge + exact splitting law); the
   remaining C2'' content is count bounds at w >= 5 + the PP2.0 seam.
6. **Caveat discipline**: the variance (~2x clustered) and the
   norm-multiset under-determination (R3) are the two named
   refinements any distributional claim must address; the basis-range
   hypothesis is THE fragile clause if any future seam folds cells
   onto opposite pairs — recorded as a standing check for seam edits.

## Caveats kept (endorsed)

- Census identification proved at junction 0 only (shape proved
  generally, tower-validated at j = 0,1,2).
- E >= 4 needs q > 3^128 (production covered; small official q gives
  E_min = 2).
- o > 1 splitting: small sample (9 rows).
- Official-scale statements are exact consequences of proved lemmas,
  not measurements at 2^41.
