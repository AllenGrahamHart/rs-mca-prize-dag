# Fable audit of the two-slope occupancy v2 pilot — 2026-08-02

**Verdict: ACCEPTED — the occupancy lemma survives its own two-slope
attack, and round 5 closes with the band question settled from BOTH
sides.** Replayed cost.py (59/59) and arith.py (36/36). Hand-checks:
Theorem 1's rank bookkeeping (the block-sum argument via
C_{S_1} ^ C_{S_2} = C_Z — sound); Theorem 5's quantization
(|T ^ T'| <= r'-M gives joint agreement <= k+w-M <= k — one line,
and it independently corroborates THEOREM BP via a different
argument, with the adjudication pilot's parity form correctly
acknowledged as stronger); Theorem G (overlap >= k+1 <=> shared dual
rank <=> a complementary-depth band pair — the self-referential
grading is a genuine structural discovery); the exact calibration
(383/447/959 = the sunflower law at all three prize rows, ratio
1.00000 — the strongest calibration any pilot has produced).

Adopted:
1. **The occupancy lemma is now PROVED-MODULO-ONE-NAMED-CLASS**
   (sublinear-rank two-slope families), with the sufficient condition
   quantified as 10^12 weaker than one-condition-per-pair, every
   known structured mechanism barred/priced/quantized-out, and the
   designed half exact (191/223/479, linear). Together with THEOREM
   BP this is the band question settled from both the construction
   and the cost side. SHARP-OCC (N_d <= n/2 at every band depth)
   adopted as the lane's sharpened conjecture.
2. **Route T re-prices DOWN**: under the measured law the third
   column costs ~0.57n^2, not 13n^3 — recorded for the ratification
   memo (the 13n^3 headroom remains the SAFE budget; the n^2 figure
   is the expected cost).
3. **The factor-2 correction** (family-rank, not per-pair charge) and
   the two honest negatives (core independence not a law; O(1)
   saturation deficits) are recorded as standing constraints on any
   future rank-based proof attempt.
4. Mint queue additions: Theorem 1 (two-slope cost), Theorem 5
   (MC quantization — merge with BP at minting), Theorem G, the
   generalized sunflower (closes banked caveat 5).
5. The remaining gap (the sublinear-rank class) is the band lane's
   single named heart; first adversarial-queue item after Gamma-in--H.

Caveats endorsed: toy scale; heuristic first moments (labeled, not
load-bearing); the deliberate-FAIL reporting discipline is exemplary.
