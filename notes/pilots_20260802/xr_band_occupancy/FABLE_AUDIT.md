# Fable audit of the band-occupancy pilot — 2026-08-02

**Verdict: ACCEPTED — the Route T heart RE-ROUTES into the program's
own species.** Theorem 2 (high-depth injectivity of the pencil
projections, proved via the k-packing on agreement-set complements)
converts 96.9% of the band ledger's cost into a single-word RS
list-size bound at agreement tau = k + ceil(h/2) — 0.8-1.6% above
rate, 25-50% of the Johnson radius, min over the q+1 pencil members.
That is the SAME SPECIES as the prize's positive target #1: the band
heart and the lane's existing list-size frontier converge instead of
multiplying. F1 did not fire (best admissible construction exactly
LINEAR in n, ~21 orders below the requirement); the master ledger is
worst-case TIGHT (slack exactly 1.0 and 2.0 on the extremal families);
and two banked claims are corrected (Route S un-killed; the tangent
gate must include the (0:1) direction).

## Independent verification record

- Replayed val.py (banked V1 fixture reproduced, including the gate's
  rejection of the over-agreeing seed), reduce.py, and read
  aggregate.json directly: 0 violations of k-packing / T1 / T2 / T3 /
  per-ray spread / line cap / fibre identity on admissible fixtures;
  EXACTLY 15 per-slope spread violations (the re-selection finding);
  the 8 T1 "violations" all on INADMISSIBLE fixtures — the gate is
  precisely the theorem's hypothesis, demonstrated empirically.
- Hand-proved Theorems 1 and 2: T1's union bound via the shared
  projection agreeing with the pencil word (sound; the extension to
  z = 0, inf is genuine — the banked statements never covered pairs
  sharing a component); T2's complement-packing count (|Z ^ Z'| <=
  k-1 <=> complements in the a-set intersect in <= h-2d-1, distinct
  (h-2d)-subsets across members, impossible for two members at
  2d >= h). Both correct.
- Hand-verified the sunflower construction's mechanism: differences
  of two pairs interpolating cores through a common (k-1)-set vanish
  there, hence are constant multiples of its vanishing polynomial,
  hence proportional — the shared slope and the free live ray at
  support size k + 2d + 1 = A (at d = (h-1)/2) follow from the banked
  union-agreement identity. Sound, and the L_P = m-1 = line-cap
  saturation is forced.
- Checked Proposition 5's arithmetic (slope-side routes need
  |Gamma_band| <= 1.32n^2, n/6 stronger than P-B's own budget) and
  the count 384/448/960 = 2(n-k+1)/(h+1) at the prize rows.

## Findings adopted

1. **The band occupancy heart is RESTATED** (ledger updated): the
   binding statement is now "some pencil member has <= ~0.7n^2
   codewords at agreement k + ceil(h/2)" — a beyond-Johnson RS
   list-size bound, one variable, side-condition-free, in the form
   the literature attacks, and convergent with positive target #1
   (L1/#106). The low band (3.1% of cost) is free at any bound up to
   n^2. Route T's feasibility is UNCHANGED (the occupancy conjecture
   stands; its head is now better-shaped); the ratification memo
   inherits this framing.
2. **MANDATORY GATE before banking the reduction**: the literature
   check the pilot flagged — do subspace-polynomial superpolynomial-
   list constructions (BSKR-style, additive domains) transfer to the
   smooth MULTIPLICATIVE domain at 25-50% of Johnson? Queued as a
   coordinator/next-round item against notes/literature_map_20260726
   and for the Pro relay. If they transfer, the reduced statement is
   FALSE and Route T's high band needs a different split.
3. **Two corrections applied to the graded-band-ledger audit**
   (dated amendment): (a) "Route S has no purchase" is REFUTED —
   re-selection freedom occurs at 15/76 admissible fixtures via
   birthday collisions among forced band slopes; every rigidity/coset
   statement must be keyed on RAYS, never slopes (Theorem 3). (b) The
   "arbitrarily lossy" master-ledger caveat is corrected: the
   inequality is worst-case TIGHT (attained at slack 1.0; the max
   family sits at exactly 2.0) — slope-counting reformulations buy at
   most a factor 2.
4. **Gate-completeness item for the Route T edit**: the tangent gate
   must be stated pencil-wide over P^1(F_q) INCLUDING the (0:1)
   direction (bandlib's scan omits it; no banked fixture is
   retroactively invalidated — 0/76 — but Theorem 2's z = inf
   instance requires it).
5. **Mint-queue revision**: Theorem 1 SUPERSEDES the queued banked
   Theorems 4+5 mint (it contains them, extends to z in {0, inf},
   and weakens the hypotheses); mint T1 + T2 (+ T4's partial-linear-
   space bookkeeping) instead, with the Route T edit. Deza corollary
   recorded as a proved sub-case awaiting its covering argument.
6. **Prop 5's no-go adopted**: no slope-side route to the occupancy
   lemma — codeword-pair side only. Closes a whole class of future
   attempts cheaply.

## Caveats kept (endorsed)

- Nothing proves the occupancy lemma; the reduction re-shapes it.
- The toy regime sits at/above the Johnson radius (A/sqrt(kn)
  0.78-1.08 vs prize 0.25-0.50) — it cannot in principle exhibit the
  blow-up the reduced statement must exclude; the sunflower
  arithmetic extrapolates, the absence of better constructions does
  not.
- The sunflower family is verified at d = (h-1)/2 (odd h) only.
- Hill-climbing was shallow; a structurally different search could
  find a non-sunflower family.
