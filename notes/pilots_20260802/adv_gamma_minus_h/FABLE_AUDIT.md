# Fable audit of the Gamma-in--H adversarial pilot — 2026-08-02

**Verdict: ACCEPTED — the ideal adversarial outcome: PROOF where the
ratified pricing needed it (j=1: THEOREM Y, unconditional at all
parameters including the official rows; the tangent gate proved with
no q-threshold — TWO standing caveats close), REFUTATION where the
claim over-reached (j>=2: 18 certified gate-intact counterexamples),
and the discovery that the excess index equals the published
gate-admissibility threshold to two decimals — two hypotheses were
one.** Replayed t4 (50/50) and t6 (10/10 CERTIFIED). Hand-checks:
the (X+z) factorisation trick (S = T u {-z} converts the mixed
member's window system into MC-1 with one free point + one extra
equation — elegant and correct); THEOREM Y's coset argument (product
condition pins -z to one mu_n-coset; realizability aligns it with
-H); the mixed-member ceiling (m_0 = 0 impossible); the
z = +-1/e_j(T^{-1}) identity's coset-confinement failure at j >= 2.

Adopted:
1. **The ratified fold-in's pricing is now UNCONDITIONAL for the
   class that matters** (j=1; N_{h-1} <= n/2 and the B_tan overflow
   figures stand; a total failure would have cost +163 bits — the
   attack was necessary and the result is the strongest possible).
2. **Lemma amendment adopted** (into the coordinated edit): state the
   confinement as the j=1 coset theorem + realizability; the
   cardinality bound |Gamma| <= n separately from the set claim
   (more robust); j >= 2 classes inherit from the gate inequality
   log2 q > log2 C(n,A)/w — now recognized as THE single
   admissibility hypothesis.
3. Collateral corrections into the definitions glossary: item 8 gains
   the competition note (non-MC rays lower selected counts — safe
   direction, now explicit); the degenerate q=17 fixture noted
   (banked coverage 5, not 6).
4. Mint queue: THEOREM X/Y + the ceilings join the MC family package.
5. The honest-failure discipline (two falsified pre-registrations
   left in the record, one in a docstring) is exemplary.

Caveats endorsed: j >= 2 has no hard bound (open; the calibrated
first moment is labeled); w = M >= 4 super-critical covered by
argument not counterexample; toy scale.
