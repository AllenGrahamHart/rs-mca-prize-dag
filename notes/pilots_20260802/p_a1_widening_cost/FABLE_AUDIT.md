# Fable audit of the P-A1 widening cost pass — 2026-08-02

**Verdict: ACCEPTED.** The gate has done its job: the widening is NOT the
cheap statement-level companion edit the bridge adjudication assumed. It
splits the banked P-A1 ledger into verbatim survivors (the k-set-keyed
moment layer and every core-agnostic charge), parametric survivors with
destroyed prize constants (PSP 15,15,14 -> 11,11,10; CLB3/4 dead), and a
broken fan of ~10 PROVED background nodes (the uniform-cell / Maxwell /
shell-exclusion program, whose contradictions all need h < 4 or h < 3
and die at h = 5 / h ~ 2^33). The repair fork this opens is a GENUINE
PLANNER/MAINTAINER DECISION and is being SURFACED, not executed.

## Independent verification record

- Replayed all four scripts this session under ramguard local:
  `row_arith.py` (banked constants reproduced exactly at kappa = k:
  PSP 4,4,4,15,15,14; ACG 4,4,3,11,11,10; CLB3 4,4,7/4,5,10;
  L_max 384,448,960 — then the widened blow-ups), `band_measure.py`
  (18 fixtures, 0 FAILS), `planted_band.py` (all headline numbers
  reproduced: genericity verified exhaustively, current predicate 0 vs
  widened 3, banked cap floor(R/h)=2 VIOLATED by L=3, widened cap
  floor((n-w)/(A-w))=3 tight, d-adaptive sunflower 0/63 vs d=0 form
  63/63), `clb_recompute.py` (thresholds + the 13n^3 headroom).
- Hand-checked the two kill conditions: collapsed-face needs
  k+2 > A-2 <=> h < 4 (dead on RowC 1/4, 1/8 and all three prize rows;
  alive only at RowC 1/16, h=3); face-syzygy needs a+1 > a+h-2 <=>
  h < 3 (dead on all six). Both match the report exactly.
- Hand-checked the sunflower self-adjustment: a k-subset W of a size-w
  core has d >= w-k pencil-matching points outside W, so the banked
  d-adaptive cap (n-k)/(t-d) evaluates to (n-k)/(A-w) — exactly the
  repaired line cap. This single mechanism is why group A survives and
  group B's frozen-kappa constants do not. Sound.
- Verified the new catch by direct reading earlier today: F1 in the
  pinned audit checks the FORCING identity on core >= k+1 cross pairs
  and counts 4,662 of them (`audit_p8p9_local_20260710.py:188-204`) —
  the P-A1 dag statement cites that count as evidence cores stay <= k.
  False-green wording confirmed (third of its genre today).

## Adopted posture — the fork is SURFACED

The bridge R2 edit is safe unconditionally (routing-only) but I am NOT
executing any part of the surgery yet, because the P-A1 side is now a
genuine two-route decision that changes what the tree claims:

- **Route W (widen + demote + open P-A1b):** honest accounting today.
  Costs: prize paid ranks drop to 11,11,10 (first open 12,12,11);
  ~10 PROVED background nodes re-scoped to the exact-k sub-stratum;
  a new red target `xr_band_core_slope_count` (<= 4n^3, from the 13n^3
  headroom — NOT by splitting 8n^3, which reopens catch #158 through
  the 0.5005% AZC margin). Buys: unconditional soundness of the
  generic-branch routing with zero new mathematics.
- **Route T (graded tangent band charge):** resurrect the archived
  `xr_partial_tangent_band` mission; band pairs are CHARGED before the
  generic branch, P-A1 keeps its exact-k form, zero demotions. Costs:
  a genuinely open theorem (the graded ledger), B_tan above n-A+1
  (13n^3 slack exists), and caveat 1 below must be closed first.

My recommendation: decide the fork AFTER (a) the pencil-cascade payment
audit (caveat 1 — if the A-2 ceiling is unsourced, Route W's widened
predicate has no upper bound and Route T becomes forced-ish), and
(b) a Pro adversarial round on both routes. Either route keeps P-B
verbatim and the bridge R2 edit is common to both. What I am NOT
willing to do is leave the tree as-is silently: the bridge unsoundness,
the strip item-3 over-claim, and the 4,662 false-green sentence are
now three documented wording defects on PROVED/critical surfaces; if
the fork is not decided promptly, the minimum honest action is a
wording-only pass (flag sentences, no predicate change) which I will
propose at the next wave boundary.

## Catches (running ledger)

- The 4,662 catch (above) — false-green wording on the P-A1 dag
  statement; the cited count is evidence of the opposite.
- The pinned t=2 evidence-base catch: the machine evidence for the
  cap-k prose comes overwhelmingly from a t=2 row where the band
  [k+1, A-2] is arithmetically EMPTY. The organic corpus cannot see
  the band at all; only the planted fixture can.
- The AZC 0.5005% margin: any future proposal to split the 8n^3
  budget reopens catch #158 at RowC 1/16. Recorded as a hard
  constraint on budget surgery.

## Caveats kept (endorsed)

- Pencil-cascade payment (B_tan accounting) NOT re-audited — the
  natural next flag, and it gates BOTH routes' ceilings.
- Planted fixture = realizability + cap refutation at toy scale, not
  band population at official A.
- Pinned verifiers not re-run at kappa = A-2 (reimplementations
  validated at kappa = k on four independent constants); a committed
  edit must re-run them.
- The rank-five / split-pencil / prize fans were sampled, not
  exhausted; both samples break, so assume the worst until audited.
- kappa is overloaded (cap vs circuit vector) — grep discipline.

> **[AMENDED 2026-08-02 — cascade payment audit, coordinator-verified.]**
> Caveat 1 fired: the REPORT's "Cascade ceiling verified ... from a
> clean source" sentence is WRONG — `xr_pencil_cascade`'s payment is
> unsourced (classification into a CONJECTURE-status taxonomy; nondeep
> pair-level form REFUTED in-tree; deep condition fails on all six
> rows). The honest generic-branch ceiling is A-1, independently
> sourced (genericity + strip forcing algebra). All Group A/B/C
> classifications of this pass SURVIVE; the widened predicate's kappa
> moves A-2 -> A-1 (line caps exactly x2 on all six rows; PSP already
> priced at A-1; the collapsed-face exclusion loses its last surviving
> row, 6/6 dead; F5-OS and the xr_smallcore dag statement must move to
> A-1 in the same edit). Route T additionally forces B_tan > n-A+1
> (slot saturated, multi-pencil overflow) = re-surgery trigger 4. Fork
> tilts to Route W. See
> `notes/pilots_20260802/xr_cascade_payment_audit/`.
