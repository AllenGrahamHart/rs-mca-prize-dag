# Fable audit of the F2 deployed-windows pilot — 2026-08-02

**Verdict: ACCEPTED — the sharpest architectural catch of the
campaign.** The tower's deployed windows are parity-homogeneous BY
CONSTRUCTION at every rung: the ANTIPODAL DESCENT LEMMA (2-power
towers over p with v_2(p-1) = e make every rung's Frobenius pairs
antipodal {y, -y}, so the linear character forces every Delta even)
is proved, elementary, and verified at the actual KoalaBear prime for
all 16 rungs. Consequence: mode k = p is slice-dead at 100% of
frequencies of the banked model, the b-resolved cancellation is
pinned at log2 p ~ 31 bits per rung against budgets of 1.95e5-6.4e9
bits, and NO window-selection step can repair it (every sub-window
inherits the degeneracy). The repair is a FREQUENCY-space case split
— parity-pure frequencies carry no signed content in the moving
sector and route to the already-PROVED fixed-sector argument —
plus the hypothesis restatement (H-flat*): k = p is a UNIVERSAL named
owner at deployed windows, not an exceptional one.

## Independent verification record

- Replayed verify.py: A1-A8 ALL PASS (LTE at the KoalaBear prime
  rungs 0-16; genuine elements all trace-zero with y^p = -y;
  all-Delta-even exhaustive over every non-vacuous frequency at three
  primes; the true-weight Z[zeta_p] identity |e_b(p)| = E_b; the
  degeneracy law at 194 rows, 0 violations; A7's residual-flatness
  scale showing k = p is the SOLE dead mode).
- Hand-derived the antipodal law in full: LTE gives
  v_2(q_{j-1} - 1) = e + j - 1; an element of order 2^{e+j} has
  y^{2^{e+j-1}} = -1, so y^{q_{j-1}-1} = (-1)^{odd} = -1 and
  y^{q_{j-1}} = -y. The Frobenius pair is {y, -y}; s^- = -s^+; Delta
  even. Elementary and airtight — and it explains, in one line, why
  the F2A.5 census saw the trace-zero line as a thin accident (it
  sampled the FULL group) while the tower lives entirely on it.
- Checked the self-diagnosis (F3): the banked pilot family's silent
  choice of the single linear character placed all its measurements
  in class K1 — an honest and important catch about OUR OWN
  instruments, of the same genre as the t=2 blindness catches.
- The convention audit (E9) is exemplary: the pilot found that the
  banked half-system labelling manufactures a spurious 2/pi
  residual-|R_k| plateau and correctly refused to bank any
  residual-flatness number — only the flip-invariant statements
  carry weight.

## Findings adopted (binding on the F2 lane)

1. **The theorem draft is AMENDED in place** (dated addendum on
   F2_SLICE_THEOREM_DRAFT.md, since it sits in the Pro relay):
   hypothesis becomes (H-flat*) — flatness over odd k != p, plus the
   k = p mode as a named UNIVERSAL owner carrying its exact
   Krawtchouk contribution +-E_b/p — and the theorem's scope is the
   GENERIC-frequency class only (measured flat 0.55-0.60, 100% clear
   1/43, 1/3 unreachable at deployed windows for any m).
2. **The F2 architecture acquires a frequency-space case split** (not
   a window selection — proved impossible): generic frequencies ->
   the slice theorem; parity-pure frequencies (K1: even part
   trace-zero; K2: odd part zero — where the moving sector carries NO
   signed content) -> the fixed-sector argument. **NEW NAMED
   OBLIGATION (open): prove the fixed sector mu_{2^24} absorbs the
   parity-pure class.** This joins PP5.0 and the F2A.1 seam as the
   lane's upstream gates and goes to Pro with the amended draft.
3. **Two proved gems queued for the mint list** (with the eventual F2
   node work, not today): the antipodal descent lemma (T1) and the
   parity-defect certificate (T2 — an exact (H-flat) certificate,
   currently for the undeployed full-group window; its value is as
   the template for certifying whatever windows the case split
   actually sends to the slice theorem).
4. **Instrument correction recorded**: all banked F2A.2/A.5/A.5b
   measurements used the linear character = class K1; their
   structural laws stand (they are frequency-general) but their
   window statistics describe the full group, not the tower. Future
   F2 pilots must use the multi-condition character model
   (moment.py's K1/K2/G classification) and state the class.
5. **PP5.0's load-bearing status is reinforced**: the selection-cost
   arithmetic (pays only from rung 13) and the whole budget table
   ride the pessimistic composition — the case split's price cannot
   be finalized before the seam freezes.

## Caveats kept (endorsed)

- The per-rung window/frequency definitions were reconstructed
  (flagged, with the two readings priced); the antipodal law is
  robust to the ambiguity.
- The law requires 2-power n (holds at official rows; fails with odd
  parts).
- Exact V_b to m <= 128; official scale enters only through pure
  arithmetic (LTE at the actual prime), the exact k=p identity, and
  the analytic M_p slope — the cleanest extrapolation posture
  available.
- The fixed-sector coverage obligation is OPEN — nothing here
  discharges it.
- Nothing mintable today; no status flips.
