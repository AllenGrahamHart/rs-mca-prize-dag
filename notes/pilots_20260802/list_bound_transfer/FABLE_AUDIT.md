# Fable audit of the list-bound transfer pilot — 2026-08-02

**Verdict: ACCEPTED — V1: the band reduction's reduced statement is
FALSE, and the mandatory gate did exactly what it was built for:
it killed the reduction BEFORE it was banked.** Replayed verify_mc
(161), verify_pencil (30), incidence (4004) — all 0 fails; 7792
machine checks total in the pilot's record. Hand-verified: MC-2's
ceiling (the PK1(A) argument at general w), Theorem I (fibre
partition — one line), and the KEY LEMMA (interpolant linearity =>
A(S) + zB(S); rank dichotomy => below cascade <=> no shared agreement
sets across pencil members) — all sound, and the KEY LEMMA is a
genuinely valuable structural theorem independent of the refutation.

Adopted:
1. **The band-occupancy REDUCTION is retired as a route** (dated
   amendment on xr_band_occupancy/FABLE_AUDIT.md): Theorem 2 there is
   correct but bounds a two-slope object by a one-slope object,
   discarding ~2^1.6e12 of structure. The occupancy lemma itself
   STANDS as the target; any new split must retain the two-live-slope
   condition. The BSKR literature question is ANSWERED (does not
   transfer; irrelevant — our own banked mechanism e22/#1051 does the
   damage, made exact/q-free/ceiling-controlled here as MC).
2. **THE TOP ADJUDICATION (mine, queued as the next coordinator
   pass): is depth d = h-1 inside the band column or the cascade
   tier?** MC produces 2^130-2^197 pairwise non-interacting
   cascade-depth pairs on an explicit gate-admissible received pair.
   Under the cascade audit's honest ceiling (A-1), if d = h-1 is
   band, the occupancy lemma ITSELF is refuted; if cascade-tier, the
   (unpaid!) cascade tier is astronomically populated and BOTH repair
   routes' extension terms need re-pricing. This intersects the
   payment audit, the band ledger, and the W/T fork — a dedicated
   adjudication with the banked semantics, then Pro.
3. **Subtraction discipline held**: MC's lower-bound half is banked
   (e22 + #1051 boundary case + PK1); the pilot's citations of OUR
   materials corrected two of MY brief's literature attributions
   (2025/2055 = BCHKS, not Goyal-Guruswami; 2308.13424 = alphabet
   bound) — literature map updated with the eventual edit.
4. **Two theorems queued for the mint list**: the pencil mass
   identity (Theorem I/I') and the KEY LEMMA (pencil support
   dichotomy — "below cascade <=> no shared agreement supports"), both
   exhaustively verified; the MC family theorems join the crossing
   lane's PK1 package (same mechanism family).

Caveats endorsed: the (R1) concentration step is sketched, not
carried (R2 is the certified half); the (H2) one-step gap at the MC
pencil is 2-adically forced and closed only via (R1)+toy search;
"strip-free" lacks a written definition (adjudication item);
paywalled JH used via restatements.
