# Fable audit of the C2'' nullity-structure pilot — 2026-08-02

**Verdict: ACCEPTED — and the lane is RE-POSED.** The pilot found the
exact structural law of the cross-junction nullity (every junction
matrix is a diagonally scaled Vandermonde, so delta = sum (L_j -
|S_j|)^+ — purely combinatorial, field-independent) and the law kills
the replacement currency: the nullity's top stratum IS the archived
coset class (already routed exactly and priced by the marginal
account), while the joint excess R lives in the delta = 0 stratum
where the invariant is identically 1 and empirically never correct
(0 of 65,535 states). Both R = E[q^delta] and its dominating form die
with exact rational counterexamples. By the adversarial audit's OWN
kill line, the NUL0-NUL4 rank-defect route as posed is refuted at the
level-junction seam. The compensating positives are substantial:
PP2.1 (the exact multi-junction compiler) is delivered and validated
against every banked ground truth; the schedule ell_j is DERIVED, not
assumed; and the residual is identified as an ARITHMETIC
norm-divisibility event, routing C2'' into the norm-gate machinery
the lane already owns.

## Independent verification record

- Replayed both gate suites this session: fixtures.py (42/42 PASS —
  every banked trap reproduced exactly, including the 32-wise
  moment-curve trap at the gate prime with all 33 subfamilies checked)
  and validate_dli.py (23/23 PASS — the MITM census reproduces all 12
  banked SCAN rows; the rank law at 7194 supports, 0 violations).
  The discipline held: nothing else ran before these passed.
- Hand-verified the rank law's mechanism: v_i = zeta^i .
  (1, w_i, ..., w_i^{L_j-1}) with distinct nodes w_i is a scaled
  Vandermonde — rank min(L_j, |S|) for every support is standard and
  correct. L2 and L3 follow directly; the L10 seam arithmetic
  ((|S_j| - L_j)^+ = 255 L_j at the official 256:1 ratio) is
  immediate.
- Checked the L6 counterexample arithmetic: 198158383604301824 /
  76427240573639025 = 2.5928 > 12979091/8388608 = 1.5472. Exact
  rationals as claimed.
- Checked the O3 threshold: the coset contribution
  2^{2^33(log2 q - 256) + 128} exceeds 2^21 iff
  2^33 (256 - log2 q) < 107 — the named 256-bit exhibit sits
  O(2^-175) below 256 bits, giving the 107-bits-above-reserve figure.
- The falsification design is honest: the seam is the pilot's own
  (PP2.0 open) and every seam-relative verdict (L5-L7) is labelled;
  the seam-independent results (L1-L4, L10-L13, O1-O4) carry the
  weight.

## Findings adopted (binding on the C2'' lane)

1. **The rank-defect route (NUL0-NUL4) is RETIRED as posed** — per
   the adversarial audit's pre-registered kill line, on a compiler
   validated against every banked fixture and census. Formally a
   third seam could evade (PP2.0 is open), but BOTH canonical seams
   fail in opposite directions (0 vs 255t), so the burden has
   flipped: any future nullity claim must first specify its seam and
   show it does better. PP2.0's role is upgraded from Gate-0 to
   DECISIVE.
2. **The lane re-targets to the ARITHMETIC residual**: rho =
   q^delta + Rem with Rem a norm-divisibility event in Z[zeta_n]
   (exact certificate; 1/phi(n) splitting measured exactly at three
   primes). Next theorems live in the norm-gate/resultant machinery
   the lane already owns — NOT in matroid/Bellman work. This is the
   third lane this campaign whose heart turned out to be arithmetic
   (after C1's resultant router and F2's spectral flatness).
3. **PP2.1 banked as delivered**: the exact multi-junction compiler
   with the derived schedule (L_j = #{r <= t : v_2(r) = j}) and the
   independent confirmation N_j = 256 ell_j = h_{j+1}. The delta
   histograms and the coset-atom tail law are the lane's new exact
   evidence base.
4. **Two named exhibits adopted**: the 256-bit admissible prime
   q = 2^256 - 191315023233023 (one nullity stratum = 2^128 — the
   reserve-arithmetic calibration point), and the junction-0
   rho-spread table (the invariant's blindness on the delta = 0
   stratum, worst case 588x), which also independently re-falsifies
   the archived profile-constancy conjecture.
5. **Relay to Pro**: this report + the F2 theorem draft + the
   crossing packet report form the round-3 package; the C2'' ask
   becomes "attack the norm-gate residual framing; adjudicate PP2.0's
   seam" instead of the retired NUL ladder.

## Caveats kept (endorsed)

- Seam-relativity of L5-L7 (PP2.0 open; the pilot's seam is the
  unique mean-one normalisation, but packet-specific bookkeeping
  could differ).
- Exhaustive scale n <= 32 (ratio 16 vs official 256); official-scale
  statements are analytic consequences of the validated
  reconstruction, with a rigorous lower bound only for E[q^delta].
- t = 2^33 vs 2^33+1 off-by-one unreconciled (structural claims
  unaffected); n = 2^41 inferred from the banked pin, not quoted.
- BPSW primality on the exhibits; L13's splitting ratio conjectural
  beyond n = 16.
