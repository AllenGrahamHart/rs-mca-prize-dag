# FABLE_AUDIT — rh_sat3_realizability (round 33)

Coordinator: Fable. Date: 2026-08-11. REPORT.md persisted (WROTE
verified). Replays: d1 exhaustive census + d2 Hankel realization
both IDENTICAL to banked results. ZERO compute-law breaches (5/5
ramguard — the round's only fully clean pilot); one disclosed
quarantine deviation (traversal-with-output-filter, nothing
surfaced) — accepted with flag.

## Verdict

**BANKED — ROUTE DECIDED: (SAT3) IS REALIZABLE; the vacuity branch
is DEAD.** Exhaustive at m=1, q=17 (16 families, ALL carrying
non-degenerate column-far Hankel realizations at generic rank rho,
(SAT1)-(SAT5) satisfied exactly), reproduced at five more fields
incl. designed-domain first-draw hits at q ~ 10^4. THE COUNTING
STACK IS SIMULTANEOUSLY TIGHT on the witness: (AO1) = (MI2) =
(ERC2) = (ERC4) = 5 = T, all equalities on one object — the 9/4 is
a route ceiling, not slack. CATCH-24C SETTLED: the two lanes count
the SAME T (PROVED via (HS3)) but LB1 does NOT transfer (its petal
structure forces d_x = r, violating d_x <= e; corank-0 branch) —
the pilot's own registered R2 overturned by its own D1, correctly.
THE REGRESSION-TEST PRINCIPLE: any proof of the strict target must
be m-dependent and must FAIL at m=1; the witness is the test.
(NEWCAP) survives it (Lmin(0) vanishes at m=1). THE T-CAP
CONJECTURE (TCAP-DIM), posed not claimed: realizable exactly for
m <= 2 (excess -13, -1), unrealizable m >= 3 (+35, +95, ...) —
with its own blind spot named (the naive-count failure mode, the
pb_design_ceiling precedent quoted against itself) and the
Kummer-family warning (structured families beat dimension counts;
its own e=1 family is the proof). THE DECISIVE EXPERIMENT NAMED AND
NOT RUN: settle m=2 — reduced to a finite algebraic system (40
parameters vs 39 rank conditions, the 9-vertex 31-edge design
exists). F1 premise now LIVE for the first time but m=1 is
degenerate for it (window = one point). e=m is the entire
difficulty (the e=1 ladder stalls at 4 vs rho+2 for m >= 2;
(ERC2) already closes e < m).

## Audit judgements

- The definitional reconciliation executed first, as briefed, and
  overturned the pilot's own registered prediction — the discipline
  at its best. The corank dichotomy (LB1 corank-0 vs (SAT)
  corank-1) cleanly explains why two lanes could both be right.
- The m >= 2 honesty is the report's spine: every m >= 2 object
  built has e = 1 (a closed range), declared calibration-not-
  progress. The false-negative ladder bug (basis-vectors-only scan)
  self-caught and disclosed.
- Cross-bank picture: bank 3 KILLS the vacuity shortcut that would
  have obsoleted banks 1+2; banks 1+2's layer-A instrument must now
  be m-dependent (the regression test). Coherent.

## Follow-ups

- G2 (settle m=2) is a ROUND-34 PRIMARY ANCHOR — cheap, decisive,
  reduced to linear algebra + a finite search.
- The e=m structured-family question (G4) — the way TCAP-DIM dies.
- Mint candidates: the m=1 realization theorem + witness (a fence
  node: "the strict target is FALSE at m=1; proofs must fail
  there"); the corank dichotomy note on the reconciliation.
