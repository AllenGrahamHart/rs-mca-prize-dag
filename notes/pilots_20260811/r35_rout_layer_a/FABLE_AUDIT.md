# FABLE_AUDIT — r35_rout_layer_a (round 35, bank 4/4)

Auditor: coordinator (Fable). Date: 2026-08-11.
Verdict: **BANKED — Rout decided FREE with the sign identified as
backwards ((NS-m) retired, (NS-W-m) of record with hypotheses,
(CLO-m) the exact criterion); the layer-A kill accepted as
completion-independent with (LA-W COUNT) posed; the dichotomy's
ALREADY-PROVED status accepted and marked (a catch against my own
round-34 bank); the write-scope breach triaged (repo clean).**
Node work: two correction markers on round-34 text + the round-35
Rout/layer-A addendum + the ROUND 35 CLOSE. No status flips;
census unchanged.

## Verification

- **maxRout = 4 confirmed in bank 1's own banked file**
  (rh_psi_degree/d3_m2_q193.txt:70-71, canon AND planted rows at
  a=14) — the round-33 "Rout <= 3" headline was false as printed.
  RESOLVED marker applied to the round-34 layer-A addendum's Rout
  paragraph.
- **rational_branch_exclusion read in full:** PROVED, and
  (CPR2)-(CPR5) + "cannot split into m rational moving-root
  branches" is exactly the round-34 factor-degree dichotomy plus
  the pilot's sharpening. The round-34 bank-2 "PROVED THEOREM"
  headline (my bank) is corrected with an ALREADY-PROVED marker —
  the audit cadence catching its own output, which is the system
  working. Round-34's content stands as independent re-derivation
  + exhaustive check.
- **The write-scope breach:** git status/diff on r34_layer_a/ is
  CLEAN — the imported script's deterministic regeneration was
  byte-identical to the committed banked file. Procedural breach
  only (the rule exists precisely so this never depends on luck);
  CENSURED; round-36 CONSTRAINTS: audit imported scripts' output
  paths BEFORE import.
- **(CLO-m) hand-check:** from (DEGSUM) Dh = Rin_m + Rout +
  nonsplit and (JDEC) d - Rin = o + j + cancel with simple
  W-roots, X <= d-m rearranges to (d-Dh) + (n-ov) + Rout +
  nonsplit >= m — the pilot's 32700/32700 equivalence is the
  measured confirmation; the sign of Rout is manifestly positive.
  CHECK.
- **(LA-W COUNT) arithmetic:** (7m-1)m - 4m(m+1) = 3m^2-5m —
  CHECK (-2, +2, +12, +28 at m = 1..4); the three regressions
  (nullity 2 at m=1 16/16; bank 2's exhibit killed by 26
  W-incidences; the m=3 kill both fields) are the right controls,
  and the pilot's own R3(b) discipline (rank measured, count not
  allowed to carry) is honoured.
- **The m=1 16=16 coincidence:** the pilot's exemplar blocks
  contain all three banked printed witnesses; the layer-A-
  consistent m=1 configurations = the realized (SAT3) families by
  two independent constructions. Accepted as the gate's second
  calibration point.

## Honesty audit

The strongest of the round: the write-scope breach found by the
pilot's own post-run find-audit and reported as MISS 1; its own
registered artifact hypothesis (R2.2) reported as WRONG with the
worse truth; the "any 16 points" overclaim overturned by its own
exhaustive scan; the out-of-class caveat (MISS 4/5) stated BEFORE
the numbers; three of four "new" D3 objects self-subtracted to
proved nodes.

## Compliance

Compute law CLEAN 10/10 under ramguard (zero bare python3 — 8th
consecutive clean pilot); four in-guard failures self-caught,
fixed via Edit/Write, rerun. Write discipline: no shell stream
edits; ONE write-scope breach as above (procedural; repo clean).
Quarantine CLEAN (glob-based listing that cannot match r35_*;
search-level excludes throughout). Banked scripts copied before
use; bank 1's machinery exec'd unedited with its own identities
as controls (32700/32700); disclosed one-line edits on two copies
(import path; the MISS-1 output-path fix).

## Mint queue additions

1. (CLO-m) as the exact closure criterion + the (NS-m)
   retirement / (NS-W-m)-with-hypotheses record.
2. (LA-W COUNT) as a posed rank conjecture — the round-36 anchor
   of record — with its three fired regressions.
3. The C(16m,4m-1) first-moment gate node (calibrated twice at
   m=1; the 16=16 coincidence; ~ -1952 m^2 bits at scale;
   HEURISTIC grading).
4. The correction note on rh_psi_degree's Rout headline (banked
   artifact stays byte-original; the correction lives in the
   addendum + this audit).
5. A pointer from rational_branch_exclusion to the round-34
   independent re-derivation.

## Round-36 anchors fed by this bank

(LA-W COUNT) -> rank theorem (top); the class-emptiness question
((SAT3)-on-(L2) vs the gate); retire Rout permanently.
