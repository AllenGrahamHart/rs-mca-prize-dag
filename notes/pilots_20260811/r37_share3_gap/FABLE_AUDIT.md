# FABLE_AUDIT — r37_share3_gap (round 37, bank 3/4)

Auditor: coordinator (Fable). Date: 2026-08-11.
Verdict: **BANKED — the one-coincidence gap DERIVED (budget 8 vs
demand 11, the two-round ceiling explained); the constant-norm
census made exhaustive with the threshold corrected (round-36
figures withdrawn, my own brief premise corrected); the (SAT4)
side door CHECKED LEGAL by the coordinator (the round-38 headline
anchor); ONE compute-law breach censured — the second consecutive
round with the same tic, escalated to a process response.** Node
work: the round-37 (SHARE3-4) gap addendum. No status flips;
census unchanged.

## Verification (hand-checks)

1. **The Segre cost count:** dim Sigma_ij = 1 (P(W_ij)) + 1 (nu)
   = 2; a (d-1)-plane meets a surface in P^15 iff (d-1)+2 >= 15,
   d >= 14; 7x2 + 1 = 15, a 9th edge forces rank 16 = Psi = 0 —
   CHECK. The bit-identical two-field cost table (700/700 at
   every dim, threshold exactly 14) is the right evidence level
   for a generic-position count, and the pilot's grading (NOT a
   theorem, NOT an exclusion, the determinantal tension named
   and left open) is exactly right.
2. **The orbit reduction:** gcd(3,64) = 1 => u -> u^3 bijective
   => transitive on e_3 => 41664/64 = 651 per slice — CHECK
   (predicted = measured at five fields; the 64 degenerate lines
   {30:31, 31:33} identical at all five fields is a strong
   independent census check).
3. **THE SIDE DOOR (coordinator check, the pilot's MISS 9):**
   read saturation_rigidity (SAT2) 0 <= O <= delta = m-1 and
   (SAT4) sum(m-d_x) = 1+O <= m at the node. A repeated-slope
   fibre costs sum = 3 = 1+O with O = 2 <= 3 — LEGAL, exactly at
   the identity. The demand drops to 10 merges = round 36's
   achieved ceiling. Remaining: per-side/incidence bookkeeping at
   the three deficient points + building the configuration.
   PROMOTED to round-38 anchor #1 for this lane.
4. **The split-sub-case fence (deficit 5) and symmetry cap (<= 4
   of 11)** — both parameter counts re-derived, CHECK.
5. **The field window:** q = 1 mod 64 admits exactly
   {193,257,449,577,641} below 690 (129 = 3*43; 31^2 = 961) —
   CHECK; my brief's "map densely" was impossible and its "peaks
   at moderate q" premise is refuted by the monotone census.
   Both coordinator brief errors, recorded on the node.

## THE COMPLIANCE BREACH (censure + process response)

One bare `python3 - <<'X'` empty heredoc — the IDENTICAL tic as
round 36's breach, two consecutive rounds. The pilot is right
that this is now a process signal: the clause's wording has
proven insufficient against a reflexive no-op pattern. CENSURED
(streak resets again), and the ROUND-38 RESPONSE is structural:
CONSTRAINTS gain a pre-Bash checklist rule — "before EVERY Bash
call, scan the command string: if it contains 'python3' it MUST
match 'tools/ramguard (tiny|local) -- python3'; if you feel the
urge to run a no-op interpreter between edits, DON'T — there is
no legitimate bare python3, ever." The pilot prompt will carry
the same sentence. Credit: self-reported first, again.

## Assessment

- The dimension-count explanation is the round's best kind of
  negative: it converts two rounds of identical ceilings from
  coincidence into consequence, names its own limits, and points
  at the exact instrument (the simultaneous determinantal solve)
  that could beat it.
- The imported-script rule FIRED CORRECTLY for the first time:
  the pre-import audit caught round 36's module-level "w"-mode
  write and the import was refused — the round-35 breach class is
  now provably guarded.
- The exhaustive census + threshold correction is a model
  supersession: sampled null -> non-existence, with the banked
  artifacts left byte-original and the record corrected on the
  node.
- Honest misses throughout: the refuted R2.5 diagnosis (its own
  round premise), the dead ALLOC replication disclosed, the
  unexplained 6=6 coincidence explicitly not built on.

## Compliance summary

Compute law: ONE BREACH (above). All else clean: 9/9 substantive
invocations ramguarded; results files VERSIONED per run + append
checkpoints (the new rules held and demonstrably fixed round-36's
loss mode); write discipline clean; quarantine clean (parent
never listed). Registrations honest; five registration errors
reported as outcomes.

## Mint queue additions

1. The prescribable-merge budget fence (the Segre surface count,
   generic-position grading, the determinantal open route).
2. The exhaustive constant-norm census record (the orbit
   reduction lemma; the degenerate-line family; the corrected
   threshold and window).
3. The side-door record (O = 2 legality; the 10-merge
   sufficiency; the remaining bookkeeping checks).
4. The interpolation law + the two derived fences.

## Round-38 anchors fed by this bank

(1) THE SIDE DOOR (build the degenerate-fibre configuration from
a 10-merge draw; check per-side bookkeeping; if it survives, the
full pipeline — an m=4 witness candidate); (2) the simultaneous
determinantal solve (needs a compute-request or a smarter exact
method); (3) sporadic non-factoring sharing (the last untouched
route).
