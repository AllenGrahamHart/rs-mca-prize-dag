# FABLE_AUDIT — rh_bivariate_system (round 33)

Coordinator: Fable. Date: 2026-08-11. REPORT.md persisted verbatim
(WROTE verified). Replays: d1 analytic controls (all four predicted
nullities exact), d4 exhibit (two fields), d5 layer-A kill — green.

## Verdict

**BANKED — NOT KILLED, WITH A WITNESS. The overdetermined bivariate
system does not exclude the failure configuration: an explicit m=2
exhibit (T = rho+2 = 9, a = w* = a* = 13, every banked incidence
axiom + the bivariate system satisfied, nullity 1 with admissible
kernel, TWO FIELDS) fences this instrument the way wave-57's node
fenced incidence. The system's real content is (BIV-CURVE) — the
type-2 slope classes on W are fibres of a degree-(3m-3) linear
series of dimension m (at m=2: a degree-3 pencil) — its per-slope
content is exactly banked (C2), WEAKER than the proved FR-canonical
2m-2; the honest counts are 7m^2-9m+2 conditions on 3m^2-2m
coefficients (my brief's "overdetermined by O(m)" premise corrected:
at m=64 only 895 of 16962 equations are consumed). THE POINTER OF
RECORD: layer A (the full-domain bidegree-(rho,m) Q(Z,x); deficit
12m^2-4m, ~3x this instrument) KILLS the pilot's own exhibit with
all three controls passing — and layer A IS the sibling (NS-m)/psi
lane. Cross-pilot convergence: banks 1+2 independently point every
remaining type-2 resource at the same full-domain object. Also new:
(BIV-CURVE) at m=2 is VERBATIM the xr lane's fibres-of-a-pencil
predicate (xr_pencil_forcing_t0) — a cross-lane transport candidate.
The m >= 3 feasibility of (BIV-CURVE) is open either way (the
first-moment heuristic says infeasible for m >= ~16 and the m=2
witness says the heuristic fails at small m — both honestly
labelled).**

## Audit judgements

- Exemplary negative-result discipline: the pilot killed its own
  headline three times (the inadmissible D2(c) cap, the
  double-counted parametrisation that made the K_7-star look
  consistent for free, the near-shipping of zero-power random
  censuses as evidence) and reports each ahead of results. The
  MISS-4 lesson generalizes: random-embedding censuses have
  q^{-Theta(m^2)} detection power — construction or nothing.
- The K_7-star result is elegant: bivariately consistent IFF the
  block map is a Mobius restriction, and REQUIRES the unsaturated
  exception (suppress the extra root, nullity 0) — the saturation
  scope caveat proved load-bearing exactly as briefed.
- COMPLIANCE: two bare-python3 patching breaches (same class as
  bank 1's seven; second pilot this round) — accepted with the
  same censure; the round-34 law text upgrade is now mandatory.
  One quarantine-boundary event (one ledger line surfaced by a
  root-anchored grep, discarded) — noted.

## Follow-ups

- Layer A is THE type-2 instrument (with bank 1's (NS-m) as its
  statement form) — round 34's primary anchor.
- The m=3,4 constructive (BIV-CURVE) search (degree-6/9 series
  with prescribed fibres) — decisive for whether the m=2 witness
  is an artifact of small m.
- The xr_pencil_forcing_t0 transport — coordinator look.
- Mint candidates: (BIV-G)/(BIV-CURVE) + the counts + the
  per-side slack-(m-1) rigidity; the m=2 exhibit as a fence node.
