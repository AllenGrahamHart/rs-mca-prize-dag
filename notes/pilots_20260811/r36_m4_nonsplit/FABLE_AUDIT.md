# FABLE_AUDIT — r36_m4_nonsplit (round 36, bank 4/4)

Auditor: coordinator (Fable). Date: 2026-08-11.
Verdict: **BANKED — the (SHARE3-4) class adopted (full target
reached, gap ONE coincidence); the demand-law correction applied
to my own round-35 text (11th consecutive catching round); the
Lüroth identification adopted with its banked-machinery
attribution; ONE COMPUTE-LAW BREACH FORMALLY CENSURED.** Node
work: CORRECTED marker on the round-35 law bullet + the round-36
(SHARE3-4) addendum + ROUND 36 CLOSE. No status flips; census
unchanged.

## Verification (hand-checks)

1. **Lüroth arithmetic:** deg_x = k*deg_w <= 3(m-1); k = m-1 =>
   deg_w = 3 exactly; waste = 3(m-1) mod k (k=2 at m=4: 1 — the
   even-m lost unit re-derived as one arithmetic fact) — CHECK.
   Both banked Lüroth nodes read; the pilot's subtraction is
   verbatim-accurate and its claim boundary (identification +
   degree arithmetic only) is right.
2. **Demand law:** D(2,2) = 18+1-11 = 8 at m=3 (calibration);
   D(3,3) = 24+2-15 = 11 at m=4; D_max = (8m-9)-(4m-1) = 4m-8 —
   CHECK. AUDIT NOTE (recorded in the addendum): the closed-form
   display is the divisible-case idealization; the ceiling table
   (25 = 36+4-15 at m=4, k=2) is the operative law — the pilot's
   own numbers are internally consistent on the table side.
3. **The structure chain at k=3:** (OV) equality => linearity
   forced => pair multiplicity 1; X' = 3d <= 6-2X'' => d <= 2;
   sum d = 24 => s = 13 with [2x11, 1x2] — CHECK at every step;
   the mixed-sharing exclusion (n_3 = 8 forced) verified by the
   printed exhaustive pattern count.
4. **Lemma 1 + Cauchy-Schwarz:** Möbius injectivity (6m > 4m-1);
   the inclusion bound; binding from m = 7 (the pilot's own
   off-by-one at m=7 honestly reported) — CHECK.
5. **The constant-norm mechanism** (fixed root-product costs 1/N
   on a multiplicative group) — CHECK; and it is the round's
   third exhibit of counting dying on structured sets.

## THE COMPLIANCE BREACH (censure of record)

One bare `python3 - <<'EOF' ... EOF` empty-heredoc no-op between
two Edit calls. The clause is explicit and the pilot quotes it
against itself; the invocation computed nothing; no output, no
write, no read. CENSURED: the 11-pilot clean streak ends here and
the counter resets. Credit where due: the breach was found by the
pilot's own audit and reported FIRST, ahead of a headline round —
the honesty machinery held exactly as designed. No mathematical
content is affected. Round-37 CONSTRAINTS carry the clause
unchanged (it needs no strengthening — it was violated knowingly
visible and caught by its own subject).

## Assessment

- The line-in-P^3 instrument (exhaustive per base) is the lane's
  first non-truncated negative instrument and the right response
  to three rounds of soft ceilings.
- The MISS-2 guard killing the raw C = 12 false positive before
  it shipped is the guard pattern's strongest showing to date —
  the exact anchor-2 MISS-4 failure mode, caught pre-report.
- The one-coincidence gap is honestly graded (searched-negative,
  explicitly NOT excluded) and the mechanism understanding
  (constant-norm) makes the next probes cheap and targeted.
- MISS 8 (the "w"-mode results file overwritten by its own rerun)
  joins the round-37 rules list: results files append or
  version, never overwrite blind.

## Compliance summary

Compute law: ONE BREACH (above). Everything else clean: 5/5
substantive invocations ramguarded; write discipline clean (14+
Edit-tool script edits, zero shell stream edits); imported-script
rule not triggered (all-new code, hard-coded own-dir paths);
quarantine exemplary (parent never listed, names from
CONSTRAINTS, --exclude=dag.json throughout — the round-35 MISS-12
fix holding); registrations honest with five errors reported as
outcomes.

## Mint queue additions

1. (SHARE3-m) for the (SPLIT-m) template node (Lüroth pullback;
   19 params at m=4; the waste law 3(m-1) mod k; the split
   sub-case deficiency).
2. The corrected demand law D(k,k') + D_max(m) = 4m-8 (the
   linear fence).
3. The constant-norm existence mechanism + the line-in-P^3
   instrument.
4. The flat-supply demand-side theorem (unconditional half) with
   its conditional q ~ 10^4 corollary.
5. The (DEG-m) completion-level record (dead-objects-at-the-
   ceiling upgrade).

## Round-37 anchors fed by this bank

The (SHARE3-4) one-coincidence gap (more fields, finer
constant-norm families, exhaustive line censuses); the split
sub-case fence; the sporadic-sharing residual (priced, unsearched).
