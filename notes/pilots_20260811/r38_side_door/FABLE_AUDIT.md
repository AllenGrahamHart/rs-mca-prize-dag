# FABLE_AUDIT — r38_side_door (round 38, bank 1/4)

Auditor: coordinator (Fable). Date: 2026-08-11.
Verdict: **BANKED — the ledger's closure adopted (my legality
check completed to every axiom with margins); the
budget-neutrality adopted as the central result; the
demand-minimality theorem adopted; DOOR B promoted to round-39
anchor #1; the 9th-fibre fence adopted; the pipeline gating
discharged; TWO coordinator errors corrected by the pilot (my
brief's biv_core warning — re-verified false, 0 hits; the q=257
ceiling 15 -> 14).** Node work: the round-38 side-door addendum.
No status flips; census unchanged.

## Verification

- The (OUT-m) aggregate hand-checked (3 symmetric-difference
  points x charge (m-2) = 6 <= 9); the equalities at the
  per-side/(OV)/eps caps re-derived; the demand-minimality
  enumeration logic checked (unit-3 placements, (SAT4) identity
  cap m); the tangency dimension count (span{v,v'} fibre =
  surface, dim 2) checked against the round-37 Segre frame.
- biv_core.py re-grepped by the coordinator: 0 hits for
  open(/write/flush — the pilot's correction of MY brief stands;
  the round-37 offender was share3_pencil.py. Brief-error
  recorded on the node.
- The pipeline numbers: two-implementation rank agreement (56 =
  56) is the right gating standard; the |Z| = 18 > T = 17
  disclosure (MISS 13) is exactly why the survival is banked as
  class-evidence, not witness-evidence.
- The pilot's three self-caught instrument errors (the
  common-root collapse reproducing round 36's artefact on 100%
  of draws; the verifier missing the (SAT4) budget ITSELF; the
  synthetic zero-power near-misread) are the guard system
  working; all three led to fixes before results.

## Assessment

- Budget-neutrality is the round's lesson in its purest form:
  the door's saving and its cost are the same object in the
  Segre currency. The board's romance with the side door ends
  honestly — and Door B, which was invisible until this ledger
  was written, is strictly better (deficit 2, no new objects
  needed). The (SAT2) c_gamma conditionality is the honest
  asterisk on the whole family and goes to round 39 with Door B.
- The 9th-fibre fence explains retroactively why two rounds of
  slope-layer success produced no completions — and it stacks
  with the census into a hard q-ceiling on the whole class.
- The un-run solve routes (MISS 8) are the round's real
  shortfall: a third measurement of a known fence instead of the
  registered attack. The determinantal solve remains with the
  r38_sporadic_det pilot.

## Compliance

CLEAN 7/7 — the pre-Bash checklist held on its first outing (the
two-round breach recurrence ends). Results files versioned +
append. The imported-script rule executed at the exemplary
standard (pre-import audit, dont_write_bytecode, post-run
pycache mtime check). The /dev/null judgment call RULED
COMPLIANT and codified. Quarantine exemplary.

## Mint queue additions

1. The side-door closed ledger + demand-minimality theorem.
2. The tangency-cost/budget-invariance row (round-37 fence
   extension).
3. The 9th-fibre completion fence (+ the census stack).
4. The mu(x) sharpened form + the forced-mu prediction.
5. The (OUT-m) inside-deficiency corollary extension.

## Round-39 anchors fed by this bank

DOOR B (the bookkeeping decision — deficit 2 with existing
objects); the (SAT2) c_gamma Hankel-layer check; the registered
solve routes (kernel-of-Vandermonde) actually implemented; layer
A on the Door-A object.
