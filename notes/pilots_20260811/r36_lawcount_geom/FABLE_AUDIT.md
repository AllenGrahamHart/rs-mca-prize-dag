# FABLE_AUDIT — r36_lawcount_geom (round 36, bank 1/4)

Auditor: coordinator (Fable). Date: 2026-08-11.
Verdict: **BANKED — the unconditional rank target RETIRED by
(LA-EQ) + two constructive rung refutations; the generalized
fence adopted; the (RIC3) mechanism attribution adopted; the
layer-A/realizability convergence recorded as the board event.**
Node work: RETIRED marker on the round-36 anchor-1 text + the
round-36 (LA-EQ) addendum + a cross-pointer addendum on the fence
node. No status flips; census unchanged.

## Verification

- **(LA-EQ) hand-checked** against the cited PROVED lines
  (saturation_rigidity: >= 15m saturated points, |A_x| = m,
  parameter degree exactly m with distinct roots in Z; RNC:
  nonzero bidegree-(m,rho) kernel biform): restriction to any
  7m-1 saturated points is a saturated configuration and vec(Q)
  lies in its layer-A kernel — nullity >= 1. Sound; correctly
  priced by the pilot as a reading (five lines from PROVED
  material), not new mathematics.
- **The H1 closed form hand-checked:** Q(g,.) = a(g-h)sigma_g and
  Q(h,.) = b(g-h)sigma_h are immediate; the supports are split
  degree-7 by construction. The 4047/4047 + 4426/4426 nullity-1
  measurements and the two-code-path H1+H2 exhibits (polynomial-
  identity check on all 26 incidences + Gaussian nullity) are the
  right verification level; controls (unsolved shape 60/60
  nullity 0; random 40-60/cell nullity 0) show the locus is thin
  and the construction is doing real work.
- **The generalized fence hand-checked at m=3** (x -> x^6 on
  mu_48: image mu_8, fibres of 6; 4 fibres = 24 >= 20 = a;
  Gamma = 12 cube roots + 1 = 13 = T; kernel contains A(X)Q_0,
  deg A <= 5 => nullity >= 6; measured exactly 6, three fields).
- **(RIC3) and the row-surplus fence read:** both PROVED, exactly
  as subtracted — the pilot's MISS 2 (mechanism not new) is
  correct and correctly placed ahead of results.
- Disclosed artifacts accepted as declared: the stale %d line in
  d2_ladder_results.txt (re-measured honestly in d4 rather than
  patched); the d3 off-by-one self-caught against the registered
  formula (the registration was right, the code wrong — the
  right failure mode).

## Assessment

- The repricing is correct and I apply it: my wave-59 re-pose of
  anchor #1 was itself still over-priced — the "theorem" was
  always the exclusion-plus-more, and the "more" is now
  constructively false at the reachable rungs. Layer A's
  per-witness kills stand untouched.
- **The board event is the convergence:** the ladder terminates
  at (SAT2)/block completion, and passing it IS (SAT3)
  realizability. Two of this round's four pilots are now provably
  working the same question from two sides; the closed-form
  starting variety transports to the B-design search (recorded in
  the addendum for the sat3_on_l2 bank to pick up at
  reconciliation — the pilots themselves stay quarantined).
- The pilot's honesty ledger is strong: the mis-priced R1.1/R1.2
  (a calibration failure it could have avoided — the derivation
  was available at registration) reported as MISS 1; H3/H4
  shortfall stated plainly; the sibling-name find output
  disclosed.

## Compliance

Compute law CLEAN 4/4 (zero bare python3 — 9th consecutive clean
pilot; the registered expectation of a ramguard failure did NOT
fire, reported for calibration). Write discipline CLEAN.
Imported-script rule VACUOUSLY satisfied (no banked script used;
the one banked object re-typed from a PROVED statement and
validated against the node's own printed properties — the right
pattern). Quarantine held (the closing find audit printed sibling
NAMES already pre-listed in CONSTRAINTS — the pre-listing rule
working as designed; no content). Registrations honest, no
addenda, execution in registered order.

## Mint queue additions

1. The generalized fence + classification table (as an upgrade or
   sibling of the wave-59 fence node; cross-pointers to (RIC3)
   and the row-surplus fence — three faces, one mechanism).
2. (LA-EQ) as a recorded reading on the endpoint accounting.
3. The H1/H1+H2 counterexample families with the closed form (the
   starting-variety transport is the valuable half).
4. The (LA-PADE)/(LA-DEG) exact nullity formula as an all-block
   extension note on (RIC3).

## Round-37 anchors fed by this bank

The single converged question: (SAT2)-completable saturated
configurations = (SAT3) realizability at m >= 2, now with the
starting variety; everything else in this lane is downstream.
