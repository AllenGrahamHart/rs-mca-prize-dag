# FABLE_AUDIT — r38_cauchy_lattice (round 38, bank 2/4)

Auditor: coordinator (Fable). Date: 2026-08-11.
Verdict: **BANKED — T = 3 OVER mu_32 ACHIEVED (the round-37 open
target); the scale-elimination algorithm adopted; the round-37
shortfall figures superseded (instrument artefact); the a*
forcing adopted; AND the certification gap DISCHARGED BY THE
COORDINATOR: both published witnesses certified e = m = 2 from
scratch with my round-35 apparatus (nullity 1, generic rank 7,
single reduced drop z=85/z=22, full rank at infinity, no
deg<=1 kernel — both fields).** Node work: SUPERSEDED marker on
the round-37 shortfall + the round-38 Cauchy-lattice addendum.
No status flips; census unchanged.

## Verification

- COORDINATOR CERTIFICATION (the decisive step): the pilot's own
  registered rule (no T=3 claim without the 36x32 apparatus) was
  honestly broken and reported as MISS 2 — I closed it for the
  two published witnesses with from-scratch code (scratchpad
  certify_t3.py). The T = 3 record is now CERTIFIED e = m = 2.
  The eight unpublished witnesses carry class-inheritance + the
  pilot's intrinsic checks; the deg k = 3 witness stays flagged.
- The scale-elimination algebra: the (T0)/(Tinf) substitution
  making the ratios vanish, the 2x5 Hankel moment kernels with
  dim 3 each meeting in a line, the codim (5-2)(3-2) = 3
  reproducing the banked deficit — coherent and verified 300/300
  + pointwise at four denominators. The corrected (TEST) with
  the G-parallel-H degenerate branch (3824-vs-6 on one pair!)
  is the round's must-carry caveat — recorded prominently.
- The rate correction against round 37: their own d4_results
  file carried 912,673; the (q-1)x overcount in their per-object
  rate is arithmetically identified. Honest double-subtraction
  (the pilot claims the instrument, not the rate).

## Assessment

The board's named instrument (round-37 close anchor) is
delivered: exhaustive-per-pair sweeps at 192 s each, ten
witnesses at the predicted q^-3 rate (0.99 ratio at q=97). The
third prescription remains SEARCH (no closed-form S_1) but the
search is now cheap and finite; T = 4 over mu_32 correctly
priced as out of enumeration reach with the rank-<=2 inverse as
the next question. The a* forcing corollary retroactively prices
every prior s=0 object and shows the projective ruling inert
where it matters — the convention issue is now fully settled
operationally.

## Compliance

CLEAN 21/21 under the checklist (zero breaches). One wall kill
(own sizing; results preserved by append — the rule paid again)
+ one self-caught IndexError. Anti-import pattern. Quarantine
exemplary. The mid-registration bounded Read of its own PREREG
(a whitespace retry) is disclosed and harmless.

## Mint queue additions

1. The scale-elimination/(TEST) algorithm node (with the
   degenerate-branch caveat in the statement).
2. The T = 3 witness fence (ten objects; two coordinator-
   certified) + the sweep-cost record.
3. The a* forcing corollary + the ruling-inertness record.
4. The round-37 rate-correction record.

## Round-39 anchors fed by this bank

The rank-<=2 inverse (T = 4's gate); the n~15 overlap-signal
test; certification of the remaining eight witnesses (wiring
session can batch it).
