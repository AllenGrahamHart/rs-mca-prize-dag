All six registered runs are done. Full report follows.

---

# REPORT — r36_hrlow (round 36)

## VERDICT

**R-HRLOW: THE h_r BAND IS CLASSIFIED, AND THE BRIEF'S PREMISE IS HALF-REFUTED. `h_r` is not the right coordinate. The exact dictionary is `h_r = rho + deg L`, where `L` is the ratio of the two syndrome-generating errors on their common support — and the common support is not an assumption but a THEOREM (any column-far pencil with >= 2 bad slopes is generated by two errors on `S_1 u S_2`, verified 12/12 random pencils, two fields). LB1 IS the unique `h_r = rho+1` structural family and its `T_1 = r+1` is FORCED, not chosen. But the `h_r = rho+2` band is NOT moment-bound: it carries a structural floor that REACHES `r+1`, so "LB1-limited" is well-posed only as a value, never as a uniqueness claim. And the round's real find is a third thing neither the brief nor round 35 anticipated: on a negation-closed evaluation domain — which the official row's multiplicative subgroup IS — there are column-far razor-faithful pencils with `T = 95` against `r+1 = 9`, field-size independent, beating their own first moment by a factor 750 at `mu_1 = 1.26e-7`. That mechanism is identified exactly, counted exactly (`C(m-1, r/2-1)`, 84/84 and 330/330), and killed exactly (it needs `ceil(rho/2) = 1`; at the razor `rho = 2^34` over-determines it by 8,589,934,591 conditions).**

Five results, each two-field-or-better:

1. **`h_r = rho + d` with `d = deg L`, exactly, 210/210** (`f1_results.txt`, all rows; `dim K_0 = r+1-rho-d` also 210/210), across 5 razor-faithful shapes and 5 fields — and `h_r` is INDEPENDENT of the support size `s` (`f3_results.txt:69-131`, `s = 9..16` at fixed `(deg e_0, deg e_1)`). `h_r` measures the ratio map, not the support.

2. **The common-support theorem.** Any column-far pencil with two bad slopes `gamma_1 != gamma_2` and locators `S_1, S_2` satisfies `y_0 = syn(e_0)`, `y_1 = syn(e_1)` with `e_0, e_1` supported on `W = S_1 u S_2`. Verified by explicit reconstruction at 12/12 random column-far pencils, `q in {349, 1009}` (`f3_results.txt:54-66`, `recon syn(e_0)=y_0 True syn(e_1)=y_1 True` at every row).

3. **D1 classification, positive.** Column-farness forces `|W| >= r+1`; one bad slope forces `|W| <= r+d`. So `d = 1` forces `|W| = r+1` exactly, a degree-1 `L` is injective, and `T_1 = r+1` is FORCED. Measured and exactly verified (every structural slope re-checked by exhibiting its locator and testing the kernel) at 210/210 rows.

4. **D2, negative for the brief's hypothesis.** `h_r = rho+2` has a structural floor: `T_1 = r+1` for an injective quadratic ratio (5 shapes x 5 fields), `T_1 = ceil((r+1)/2)` for a 2-to-1 one. Even `h_r = 2rho` — FG's own value — carries `T_1 = r+1` at `d = 3, rho = 3` (`f1_results.txt`, H4/H5 `d3-cube` rows). **The absence of a floor is not a property of `h_r`.**

5. **The negation-closure excess, and its death.** `T = 98` at `q = 65537` and `T = 95` at `q = 999983` for the `h_r = rho+2` family on `D = {+-1..+-10}` (`f3_results.txt:8,32`), against a first moment of 1.92 and 0.126. Control on `D = {1..20}`: `T = 10` (`f4_results.txt:27`). Mechanism: even locators `sigma(x) = Q(x^2)` collapse the odd-index Hankel rows; count `= C(m-1, r/2-1)` **exactly** (84/84 at H1, 330/330 at H3, two fields, `f4_results.txt:7-11`); zero at `rho = 3` (H4, H6, H7) and `rho = 4` (H8), two fields each (`f4_results.txt:13-23`).

`B_ca^far(k+2^34) < 2^128`: **NO.** I add no upper bound. I do add the first *named and counted* mechanism by which the razor-shape count exceeds `r+1` at zero field-size cost — and the exact reason it cannot survive to `rho = 2^34`.

---

## MISSES FIRST

1. **I KILLED MY OWN THIRD RUN WITH A `head` PIPE AND LOST ITS RESULTS FILE.** `f3_probe.py` completed part A to screen, then SIGPIPE'd before writing `f3_results.txt`; no file existed. I re-ran it with `tail` (which buffers) and it wrote. **That cost one of my six registered interpreter invocations for nothing.** Exactly anchor 1's miss 9 in a new costume, and I had read that miss before registering. Disclosed, not hidden.

2. **MY REGISTERED CAP A-5 IS FALSE AS I MEANT IT, AND TRUE ONLY AS I WROTE IT.** I registered (R2e/A-5) "no common-support cell, any `d <= rho`, any field, has `T_1 > r+1`" at P = 0.80. The literal claim HOLDS (`T_struct <= r+1` at every row of `f1_results.txt`, `f2_results_H1.txt`, `f2_results_H2.txt`). But the conclusion I intended to draw from it — that the total bad-slope count is `<= r+1` — is **FALSE**: `T = 95 > 9 = r+1` at `f3_results.txt:32`, column-far, razor-faithful, `mu_1 = 1.26e-7`. My fibre argument bounds only the slopes whose locator contains the support; it says nothing about locators that reach the syndrome through a codeword. **I registered the cap as if it were the whole count. It is not.**

3. **MY `p*(d)` LAW HAS FIVE MEASURED FAILURES AND I FOUND THEM ONLY BY GREPPING MY OWN OUTPUT.** `p* = max(rho+d, floor((R+1+d)/2))` holds 205/210 (`f1_results.txt`); the 5 failures are all H2's `d2-2to1` row, at 5/5 fields, `p* = 6` against a predicted 7 — field-size independent, so a genuine geometric dependence on `T`, not noise. **`p*` is not a function of `(R, rho, d)`.** My registered A-2 named the injective-quadratic values (6,7,7,8,8) and those are 5/5 correct; the law as a *law* is not.

4. **I DID NOT MEASURE THE SECOND SYMMETRY MECHANISM AT `rho >= 3`, AND MY DERIVATION SAYS IT SURVIVES THERE.** At H2, `q = 65537`, the symmetric-`T` quadratic family has `T = 336` against a moment of 7.59 (`f2_results_H2.txt:13`), and the `s = r+2` symmetric row is larger still. This is a *different* mechanism from the even-locator one (here the ERROR is even on a symmetric support, so half the syndrome moments vanish identically). My parity derivation says it leaves `floor(rho/2)` conditions on one unknown, hence survives at `rho = 3` and dies only at `rho >= 4`. **My `f4` scan tested only the one-sided-`T` variant.** I claim ZERO POWER on the symmetric-`T` variant at `rho >= 3` (ZP-11) and flag it as the round-37 first item. The razor conclusion is unaffected (`rho = 2^34 >= 4`), the *shape* of the claim is.

5. **"IS LB1 UNIQUE" WAS THE WRONG QUESTION AND I ANSWERED IT ANYWAY.** The brief asks whether LB1 is the unique beat-the-moment object. Within the fibre structure the answer is a clean yes-at-`h_r = rho+1`. But EVERY common-support pencil beats the first moment by ~`6.7e11` bits at the razor, because the moment is `2^{-6.7e11}` and any structural floor `>= 2` beats it. **"Beats the moment" is not a discriminating predicate in this lane.** The discriminating predicate is the *value* of the floor, and I report values throughout.

6. **`d3-cube` AT H1 SHOWS `h_r != rho+d` AND THAT IS MY OWN SCOPE, NOT A REFUTATION.** `f3_results.txt:24,48` print `h_r=4(=rho+d False)` for `d = 3` at `rho = 2`. R2a explicitly required `d <= rho`; at `d > rho` the reduction mod `tau_T` wraps and `h_r` saturates at `2rho`. Correct behaviour, misleading label in my own output. Recorded rather than silently dropped.

7. **`ctrl-s=r` IS COLUMN-CLOSE AND ITS "STRUCTURAL SLOPES" ARE MEANINGLESS.** The `s = r` control reports `r` structural slopes at every row of `f1_results.txt`, but `f2_results_H1.txt:4-34` shows it is column-CLOSE at every field (common locators 1-3). It is a *control*, and it did its job (it confirms `s >= r+1` is forced); its slope counts must not be read as far-CA data.

8. **ONE ROW OF MY OWN CENSUS IS COLUMN-CLOSE BY ACCIDENT.** `d1-x` at H1, `q = 101` has 1 common locator (`f2_results_H1.txt:4`) — `mu_1 = 12.3`, so accidental common locators are expected. That row is not a valid far-CA instance and is excluded from every conclusion. Column-far at every `q >= 349` (`f2_results_H1.txt:11,18,25,32`). Anchor 1's miss 10, reproduced at a different field.

9. **NO RAZOR-SCALE MEASUREMENT, AND A DISCLOSED WIDENING.** ZP-1 registered `q <= 65537`; I went to `q = 999983` because the excess had to be separated from the first moment and `q = 65537` still leaves `q*mu_1 = 1.9`. A deviation in the permissive direction, disclosed. Every razor number is a closed form.

10. **NO `rho >= 3` EXHAUSTIVE TOTAL-`T` CENSUS EXISTS.** `C(26,10) = 5,311,735` put it out of reach in stdlib Python inside 290 s, exactly as anchor 1's miss 8. The `rho = 3` and `rho = 4` evidence is the even-locator scan plus the structural verification, not a total-`T` count. Declared (ZP-10), not hidden.

11. **NO BOUND.** `B_ca^far(k+2^34) < 2^128` remains **NO**. Registered at 0.03; the round adds a mechanism and a kill, not an addition.

---

## CATCH-24A SUBTRACTIONS (own-repo greps before every novelty claim)

Every recursive grep carried, at search level: `--exclude-dir=r36_lawcount_geom --exclude-dir=r36_sat3_on_l2 --exclude-dir=r36_m4_nonsplit --exclude-dir=pilots_20260802 --exclude-dir=prize-codex-work --exclude-dir=.git --exclude-dir=__pycache__ --exclude=dag.json` (plus my own dir). Sibling names were taken from CONSTRAINTS, never from an `ls`.

| # | claim | grep | banked? | verdict |
|---|---|---|---|---|
| 1 | `h_r`, `dim K_0 = r+1-h_r`, increment `h_r-rho <= rho` | `-rnE "h_r \| stacked rank"` | **YES** — `critical/nodes/rate_half_band_crossing_location/statement.md:3103-3105` (round 34) | **SUBTRACTED** |
| 2 | `p*(LB1) = max(rho+1, floor((R+2)/2))`, `h_r(LB1) = rho+1`, `dim K_0 = r-rho` | `-rniE "p\* *= *max\(\|floor\(\(R\+2\)/2\)\|max\(rho\+1"` | **YES** — `crossing_location:3129-3132` (round 35, scope-correction) | **SUBTRACTED.** My additive part is only the `d >= 2` extension |
| 3 | R-HRLOW as the load-bearing residual, "razor pencils with `h_r` near `rho`" | `-rnE "h_r"` | **YES** — `crossing_location:3637` (round 35's flag, already banked) | **SUBTRACTED — this brief's own provenance** |
| 4 | **`T <= r+1` is dead as a universal bound at razor shape** | `-rnE "<= *r\+1"` | **YES** — `crossing_location:3112` ("the measured `T = q` kills `T <= rho`, `T <= p`, `T <= r+1` as candidate bounds", round 33), narrowed at `:3133-3135` ("the saturation tracks `mu_1 = C(n,r)/q^rho`, not FG membership") | **SUBTRACTED — the KILL is round 33's.** **ADDITIVE:** the banked kill is a `mu_1 >= 1` saturation artefact and was explicitly narrowed to one; mine is at `mu_1 = 1.26e-7` with the moment at 0.126 and `T = 95` (`f3_results.txt:32`) — a violation that SURVIVES `mu_1 -> 0`, with a named mechanism and an exact count |
| 5 | `B_ca^far(n-r) <= r+1` proved unconditionally | `-rnE "B_ca\^?far.*<="` | **YES** — `critical/nodes/rate_half_band_closure/statement.md:162` ("the Hankel suite's unconditional layer `B_ca^far(n-r) <= r+1` (every `r <= 2^39-2`)"), tightness at `crossing_location:657` | **SUBTRACTED — and it is a SCOPE FENCE:** that bound is for `r <= R/2-2`, the official row's OWN shape. My cells have `r = 0.8R > R/2`. **My `T = 95 > r+1` does NOT contradict it** and I say so explicitly |
| 6 | `B_ca^far(k+2^34) >= r+1 = 2^39.9773`, LB1's construction | `-rnE "<= *r\+1"`, `:650-665` | **YES** — `crossing_location:654-656` | **SUBTRACTED.** I reproduce `r+1 = 1,082,331,758,593 = 2^39.977280` exactly (`f4_results.txt:32`) as an independent check |
| 7 | orbit-invariant locators `L_B(X)G(X^M)` from fibres of `x -> x^M` in the multiplicative-subgroup domain | `-rniE "multiplicative subgroup\|even locator\|automorphism of D"` | **YES** — `critical/nodes/e22_tail_coset_locator_algebra/proof.md:3`, `node.json:8` | **SUBTRACTED — the locator ALGEBRA is banked (E22 lane).** **ADDITIVE:** its deployment as a far-CA **bad-slope** mechanism, the odd-row collapse identity on a negation-closed `D`, the exact count `C(m-1, r/2-1)`, and the `rho`-threshold that kills it |
| 8 | the official `D` is a multiplicative subgroup of power-of-two size (hence negation-closed) | same grep | **YES** — `critical/nodes/rules_freeze/proof.md:13` (verbatim paper quote), `critical/nodes/axis8_generating/proof.md:19` | **SUBTRACTED.** The *consequence* for far-CA bad slopes is mine |
| 9 | the coset/qcore supply plateau `C(127,64) = 2^123.1714`, capped by Lam-Leung + nesting | `-rnE "B_ca\^?far.*<="` (hit in `rate_half_band_closure/node.json:9`) | **YES** — `rate_half_band_closure/node.json:9` (witness-hunt recon, 2026-07-12) | **SUBTRACTED, and flagged as a CORRESPONDENCE to check** (D3.4): my orbit-invariant count at `M = rho` is `C(n/rho, r/rho) = C(128, 63)`, one binomial step from the banked plateau. I did **not** compute it (ZP-12) |
| 10 | **`h_r = rho + deg(ratio)` as a dictionary; `h_r` independent of the support size** | `-rniE "h_r *= *rho *\+\|ratio (poly\|function\|map)\|degree of the ratio"` | hits only `deg L=1` in unrelated curve lanes (`rate_half_list_adjacent_crossing/frontier.md:22`, `statement_sections/06-...:670`) — **ZERO far-CA hits** | **ADDITIVE** |
| 11 | **the common-support theorem** (>= 2 bad slopes ⟹ both syndromes from errors on `S_1 u S_2`) | `-rniE "common[- ]?support\|joint[- ]?support\|S_1 *(u\|cup\|union) *S_2\|two (bad )?slopes .*span\|pencil is (spanned\|generated)"` on `critical/nodes background/nodes` | hits are all the *interleaving* "common-support diagonal" (`interleaved_floor_import/proof.md:6`, `rate_half_list_adjacent_crossing/.../10-wave12-pin-of-record.md:25`, etc.) — a different object in a different lane; the far-CA `W` at `background/nodes/rate_half_bivariate_locator_extension_kernel_reduction/statement.md:56-62` is explicitly scoped OUT ("They do not enforce the root sets of `Q_gamma` outside `W` ... or column-farness") | **ADDITIVE** |
| 12 | **the fibre cap `T_struct <= (r+f)/f` with `f = |W|-r`, hence `<= r+1`** | same greps as 5 and 11 | anchor 1's `(C3) T_1 <= e+1` is the SAT-lane ledger and is under a scope fence I am forbidden to import | **ADDITIVE — and derived from scratch, not imported** (see ZP-6) |

**Genuinely additive this round:** (a) `h_r = rho + deg L`, `dim K_0 = r+1-rho-d`, and the `s`-independence of `h_r`; (b) the common-support theorem and its 12/12 reconstruction; (c) `r+1 <= |W| <= r+d`, hence the D1 classification `d=1 => |W|=r+1 => T_1 = r+1` FORCED; (d) the fibre cap `T_struct <= r/f + 1 <= r+1` and the fibre floor `T_1 >= ceil((r+1)/d)`; (e) `p*(d) = max(rho+d, floor((R+1+d)/2))`, 205/210, with its 5 named failures; (f) the negation-closure excess: mechanism, exact count `C(m-1,r/2-1)`, control, and the `ceil(rho/2) = 1` threshold that kills it; (g) the second (symmetric-`T`) mechanism, measured at `rho = 2` and declared unmeasured at `rho >= 3`.

---

## D1 — THE `h_r = rho+1` STRATUM, CLASSIFIED

### D1.1 The dictionary: `h_r` measures the ratio, not the support

Setup (anchor 2's conventions, `statement.md:7-33`): `v_x = 1/prod_{y != x}(x-y)`, `y_m = sum_x e(x)v_x x^m`, `M_r(y) = (y_{i+j})_{0<=i<rho, 0<=j<=r}`, `rho = R-r`.

Let both syndromes come from errors on a common `T`, `|T| = s`, with `e_0 == 1` and `e_1 = L` on `T`. Writing `W_dual = {w : sum_x w_x x^i = 0, i < rho} = {(h(x)/tau_T'(x))_x : deg h <= s-1-rho}`:

```
K_0  ~=  { h : deg h <= r-rho , deg(L h mod tau_T) <= r-rho }        (s = r+1)
```

and for `d = deg L <= rho` there is no wraparound, so the condition is `deg h <= r-rho-d`:

**`dim K_0 = r+1-rho-d`, `h_r = rho + d`.**

**Measured 210/210 rows** — `h_r` matches `rho+d` at 210/210 and `dim K_0` at 210/210 — across shapes H1..H5 (`f1_results.txt:8,95,182,269,366`, all FAITHFUL: `4rho<R`, `a>R+1`, `a-1>r` True at every header) and fields `{101, 349, 1009, 10007, 65537}`. At `d = 1` this reproduces anchor 1's banked LB1 values (`h_r = rho+1`, `dim K_0 = r-rho`) from a derivation rather than a fit.

**And `h_r` does not see `s`.** At H1, `q in {1009, 10007}`, with `(deg e_0, deg e_1)` fixed and `s` swept over `9, 10, 11, 12, 16` (`= r+1 .. 2r`), `h_r` is constant in `s` at every one of the 12 degree pairs (`f3_results.txt:69-131`): `degA=0,degB=1 -> h_r=3` at all five `s`; `degA=1,degB=2 -> h_r=3`; `degA=2,degB=1 -> h_r=4 = 2rho` (ratio not a polynomial); `degA=1,degB=1 -> h_r=2 = rho` (degenerate pencil). **The law is `h_r = rho + deg(e_1/e_0)` when that ratio is a polynomial of degree `<= rho`, and `2rho` otherwise.** Two fields.

### D1.2 The common-support theorem (this is what makes the classification well-posed)

By (HS1) (`rate_half_ca_hankel_split_pencil_equivalence/statement.md:23-25`), a bad slope `gamma_i` means `y_0 + gamma_i y_1 = syn(u_i)` for some `u_i` of weight `<= r` supported on the locator's root set `S_i`. If `y_0, y_1` are independent and there are two distinct bad slopes, then `(y_0+gamma_1y_1)` and `(y_0+gamma_2y_1)` span the same plane as `(y_0,y_1)`, so

```
e_0 = (gamma_2 u_1 - gamma_1 u_2)/(gamma_2-gamma_1),   e_1 = (u_1-u_2)/(gamma_1-gamma_2)
```

are supported on `W = S_1 u S_2`, `|W| <= 2r`, and `syn(e_0) = y_0`, `syn(e_1) = y_1`.

**Verified by explicit reconstruction at 12/12 random column-far pencils**, `q in {349, 1009}`, H1 (`f3_results.txt:54-66`): every row reads `recon syn(e_0)=y_0 True syn(e_1)=y_1 True`. Two fields. **So "common support" is not a family I chose; it is forced.**

### D1.3 The classification theorem

Write `f = |W| - r`. With `chi : W -> P^1`, `x |-> [e_1(x) : -e_0(x)]`, the slope-`gamma` error vanishes exactly on `chi^{-1}(gamma)`:

- **column-far ⟹ `f >= 1`.** If `|W| <= r` any `r`-set containing `W` is a common locator. Confirmed: the `s = r` control is column-CLOSE at 7/7 measured (cell, field) rows (`f2_results_H1.txt:4,11,18,25,32`; `f2_results_H2.txt:6,16`).
- **one bad slope ⟹ `f <= d`,** since `L` has fibres of size `<= d`. Confirmed at the two `s = r+2, d = 1` rows: **0** structural slopes at every field (`f1_results.txt`, `d1-s+1` rows, `f = 2 > d = 1`).
- **`d = 1` ⟹ `f = 1` ⟹ `|W| = r+1`, and a degree-1 `L` is injective, so `T_1 = r+1` EXACTLY.** Not chosen — forced.

**Measured, and every structural slope verified by exhibiting its locator and testing `(M_0 + gamma M_1)sigma = 0`: 210/210 rows have `structural slopes = VERIFIED exactly`** (`f1_results.txt`, all rows), with `T_1 = r+1 = 9,10,11,11,12` at H1..H5 for both `d=1` families, 5 fields each.

**ANSWER TO D1: yes — LB1 is the only `h_r = rho+1` column-far family with a bad slope, up to the choice of `(T, e_0, e_1)`, and its count is `r+1` exactly.** And the exhaustive census confirms there is nothing else there: at `h_r = rho+1`, `T = 9 = r+1` with **zero** accidental slopes at `q = 65537` AND `q = 999983` (`f3_results.txt:6,30`). Two fields, deeply subcritical.

### D1.4 The `p*` converse: half true, and I refute the strong form

`Ann(V)_i := {P : deg P <= i, P*F[x]_{<=r-i} subset K_0}`; `p* = min{i : Ann(V)_i != 0}`. The lattice `{(g,s) : s = E g mod tau_T}` has determinant degree `r+1` and shift `rho+d`, so the shifted minimal indices sum to `(r+1)+(rho+d) = R+1+d`:

```
p*(d) = max( rho+d , floor((R+1+d)/2) ) ,   d* = min(a-1, that index)
```

At `d = 1` this **is** anchor 1's banked law, and my implementation reproduces its 5/5 rate-half values `p*(LB1) = 6,6,7,7,8` at `R = 10..14` (`f1_results.txt`, `d1-x` rows) — an independent replication of `crossing_location:3132`.

- **Forward direction HOLDS:** `h_r = rho+1 ⟹ p* = max(rho+1, floor((R+2)/2))` at 50/50 measured `d=1` rows (5 shapes x 5 fields x 2 families).
- **The CONVERSE is REFUTED.** `p*` does not determine `h_r`: at H1, `q = 101`, the `d=1` family has `p* = 6, h_r = 3` and the `d=2` family has `p* = 6, h_r = 4` (`f1_results.txt:10,14`). Same `p*`, different stratum, 5/5 fields.
- **And the general law itself has 5 failures**, all H2 `d2-2to1`, `p* = 6` vs predicted 7, at 5/5 fields (miss 3). Overall **205/210**.

**`p*` is a coarser invariant than `h_r` and must not be used as a stratum label.**

---

## D2 — THE `h_r = rho+2` BAND

### D2.1 The two-increment analogue exists, and it is a quadratic ratio (not two point sets)

The brief asked for "two fixed point sets? a 2-dimensional locator pencil over a fixed support". Neither. The `h_r = rho+2` analogue of LB1 is **one** fixed `(r+1)`-point set `T` with a **quadratic** ratio `L`, i.e. `e_1 = x^2 e_0` (equivalently `y_1` is the double shift of `y_0`). `f = 1` still, so every value of `L` on `T` is a bad slope, and the fibre structure of a degree-2 map decides the count:

| family | `s` | `d` | `h_r` | `dim K_0` | `T_1` measured | predicted |
|---|---|---|---|---|---|---|
| `d2-inj` (`L = x^2`, `T` one-sided) | `r+1` | 2 | `rho+2` | `r-rho-1` | **`r+1`** | `r+1` (`L` injective on `T`) |
| `d2-2to1` (`L = x^2`, `T` symmetric) | `r+1` | 2 | `rho+2` | `r-rho-1` | **`ceil((r+1)/2)`** | `ceil((r+1)/2)` |
| `d2-s+1inj` | `r+2` | 2 | `rho+2` | `r-rho-1` | **0** | 0 (all fibres size 1 < `f = 2`) |
| `d2-s+1sym` | `r+2` | 2 | `rho+2` | `r-rho-1` | **`floor((r+2)/2)`** | fibres of size exactly 2 |
| `d3-cube` (`rho = 3` only) | `r+1` | 3 | `rho+3 = 2rho` | `r-rho-2` | **`r+1`** | `r+1` (`x^3` injective on `T`) |

All values at 5 fields; `h_r` and `dim K_0` at 210/210; `T_1` verified exactly at 210/210 (`f1_results.txt`). Concretely at H5 (`n=28, k=14, R=14, rho=3, r=11, a=17`): `d2-inj` has `h_r = 5 = rho+2 < 2rho = 6` and `T_1 = 12 = r+1`.

**ANSWER TO D2: the `h_r = rho+2` band DOES carry a structural floor, it is field-size independent, and it REACHES LB1's value `r+1`.** The floor is `ceil((r+1)/d)` in general and `r+1` exactly when the ratio is injective on `T` — which is always arrangeable (`T` avoiding the involution pairs in odd characteristic; automatic in characteristic 2 with `L = a x^2 + c`, **unmeasured**, ZP-5). **The band above LB1 is not moment-bound, and "the extremal count is LB1-limited" is false as a uniqueness statement and true only as a value statement — subject to D2.2.**

### D2.2 The `mu_1` sweep, and the excess the brief did not anticipate

Exact total-`T` census (every one of the `C(n,r)` split locators swept once; a locator pins at most one slope, and `u = w = 0` means column-close):

| `q` | `mu_1` | `q*mu_1` | `d1-x` `T` / `T_1` | `d2-inj` `T` / `T_1` | far? |
|---|---|---|---|---|---|
| 101 | 12.349 | 1247 | 101 / 9 | 101 / 9 | `d1` CLOSE (miss 8) |
| 349 | 1.0342 | 361 | 225 / 9 | 253 / 9 | far |
| 1009 | 0.12373 | 125 | 120 / 9 | 186 / 9 | far |
| 10007 | 0.0012579 | 12.6 | 22 / 9 | 104 / 9 | far |
| 65537 | 2.9329e-05 | 1.92 | **9 / 9** | **98 / 9** | far |
| 999983 | 1.2597e-07 | 0.126 | **9 / 9** | **95 / 9** | far |

(`f2_results_H1.txt:4-34`; `f3_results.txt:5-8, 29-32`.) `mu_1 = 12.349, 1.0342, 0.12373, 0.0012579, 2.9329e-05` reproduce anchor 1's H1 row exactly.

**`d1-x` converges to the floor: `T = T_1 = r+1` with zero accidentals at the two large fields.** LB1 is exactly `r+1`, nothing more.

**`d2-inj` does not converge to `r+1`. It converges to ~95.** The moment predicts 1.92 and 0.126; the measurement is 98 and 95. **This is a column-far, razor-faithful pencil whose bad-slope count exceeds `r+1` by a factor 10.6, at zero field-size cost.**

### D2.3 The mechanism, named and counted

`D = {+-1,...,+-m}` is closed under `x -> -x`, so `v_{-x} = (-1)^{n-1}v_x`; with `L = x^2` the pencil error `e_gamma = P_gamma(x^2)` is even. Take an **even** locator `sigma(x) = Q(x^2)`, root set `S = A u (-A)`. Using `sum_{x in D} v_x g(x) = 0` for `deg g <= n-2`, the odd-index rows of the pencil collapse to the orbits `T` does not meet:

```
row i odd  =>  condition becomes  sigma(x_0) P_gamma(x_0^2) = 0  for the orbits x_0 off T
```

So if `S` contains every orbit off `T`, **all odd rows are automatic**, and only the `ceil(rho/2)` even rows remain as genuine conditions on the single unknown `gamma`:

- **`rho = 2` -> 1 condition, 1 unknown -> exactly one slope per such locator.** Count `= C(m-1, r/2-1)`.
- **`rho >= 3` -> >= 2 conditions, 1 unknown -> nothing.**

**Measured exactly** (`f4_results.txt:5-23`), two fields (`q = 65537`, `q = 999983`) at every cell:

| cell | `rho` | `r` | `C(m-1, r/2-1)` | bad even locators | distinct slopes | all covering the off-`T` orbits? |
|---|---|---|---|---|---|---|
| H1 (20,10) | 2 | 8 | 84 | **84 / 84** | 84 | 84/84 yes |
| H3 (24,12) | 2 | 10 | 330 | **330 / 330** | 329 | 330/330 yes |
| H4 (26,13) | 3 | 10 | 495 | **0** | 0 | — |
| H6 (30,15) | 3 | 12 | 2002 | **0** | 0 | — |
| H7 (34,17) | 3 | 14 | 8008 | **0** | 0 | — |
| H8 (36,18) | 4 | 14 | 12376 | **0** | 0 | — |

Every cell FAITHFUL (`4rho<R`, `a>R+1`, `a-1>r` all True, printed per row). Column-far (`common=0`) at 12/12 rows.

**Two-field control, and it is decisive.** The same `d=2` family on `D = {1,...,20}` — same `n, k, r, rho, T, L, q`, only `D` no longer negation-closed — gives `T = 10` with `T_struct = 9`, `T_acc = 1` (`f4_results.txt:27`), against `T = 98, T_acc = 89` on the negation-closed `D` (`f4_results.txt:26-27` states the comparison inline). **The excess is caused by the domain's symmetry, not by `h_r`, not by `d`, and not by the field.**

**A second, larger mechanism exists and I only measured it at `rho = 2`** (miss 4): with `T` itself symmetric the error is even on a symmetric support, half the syndrome moments vanish identically, and at H2 `q = 65537` the count is `T = 336` against a moment of 7.59 (`f2_results_H2.txt:13`). Parity bookkeeping leaves `floor(rho/2)` conditions, so it should survive to `rho = 3` and die at `rho >= 4`. **Unmeasured at `rho >= 3` (ZP-11).**

### D2.4 Why neither mechanism reaches the razor

At the razor (`R = k = 2^40`, `rho = 2^34`, `r = R-rho`, `n = 2^41`; FAITHFUL: `4rho<R`, `a>R+1`, `a-1>r` all True, `f4_results.txt:30-31`):

```
#even locators = C(n/2, r/2)               = 2^1,099,317,984,625.2820
residual conditions on the one unknown gamma = ceil(rho/2) = 8,589,934,592
over-determination                          = 8,589,934,591
```

(`f4_results.txt:45-46`.) The carrier is astronomically large; the *condition count* is what kills it. **The `rho = 2` (and, by parity, `rho <= 3`) excess mechanism is dead at the razor by 8,589,934,591 conditions.** This is the honest reason the counterexample does not propagate — and it is a `rho`-threshold, not a field-size threshold, so no amount of `q` restores it.

---

## D3 — THE STRUCTURAL-FLOOR DICHOTOMY, POSED

### D3.1 The dichotomy

> **POSE (R36-D).** Let `(y_0,y_1)` be a column-far Hankel pencil at rate-half razor-faithful shape with `>= 2` bad slopes, and let `W, e_0, e_1, chi` be its forced common-support data (D1.2), `f = |W|-r`, `d = deg(e_1/e_0)` when that ratio is a polynomial. Then the bad-slope set splits as `T = T_fib + T_sym + T_rand`, where
> - **`T_fib`** (fibre/structural) `= #{gamma : |chi^{-1}(gamma)| >= f}`, and satisfies **`ceil((r+1)/d) <= T_fib <= r/f + 1 <= r+1`**, with `T_fib = r+1` **iff** `f = 1` and `chi` is injective;
> - **`T_sym`** (symmetry) is nonzero **iff** the pair `(D, chi)` is invariant under a nontrivial automorphism `x -> zeta x` of the evaluation domain of order `M`, and is then carried by the `M`-orbit-invariant locators, requiring **`ceil(rho/M) = 1`**, i.e. **`M >= rho`**;
> - **`T_rand`** is first-moment governed, `~ C(n,r)/q`, and is `2^{-6.7e11}` at every admissible official row.
>
> **Column-far razor pencils have a structural `T`-floor iff `f = 1` (i.e. `|W| = r+1`), and the floor exceeds `r/f+1` iff the pair `(D, chi)` carries an automorphism of order `>= rho`.**

**`h_r` is NOT the named condition** — that is the round's correction to the brief. `h_r = rho + d` prices only the *decay rate* `ceil((r+1)/d)` of the fibre floor; the floor's *existence* is `f = 1`, and the possibility of exceeding `r+1` is a symmetry condition on `D`.

**Falsifiers, pre-registered in form:**
- **F-1** any column-far razor-faithful pencil with `f >= 2` and `T_fib > r/f + 1`. (Would kill the fibre cap. Not observed: `T_struct <= cap` at 12/12 random pencils, `f3_results.txt:54-66`, and `T_struct <= r+1` at every row of `f1/f2`.)
- **F-2** any `T_sym > 0` at `rho >= 4` on a one-sided `T`, or at `rho >= 5` on a symmetric `T`. (Would kill the `M >= rho` threshold. Not observed at `rho = 3,4`: 0 at 8/8 rows, `f4_results.txt:13-23`.)
- **F-3** any `h_r = 2rho` pencil with `f = 1` and injective `chi` and `T_fib < r+1`. (Would restore `h_r` as the discriminator. Refuted already: `d3-cube` at `rho = 3` has `h_r = 2rho` and `T_1 = r+1`, `f1_results.txt` H4/H5.)

### D3.2 The relation to `(C3)`'s `e+1` cap — attainment is a CONSEQUENCE, not the mechanism

I registered B-4a/B-4b and **B-4b fired.** The `f = 1` configuration has `|W| = r+1` and every point of `W` on `r` of the `r+1` locators, so `d_x = r`, `e = r`, and `T_1 <= e+1 = r+1` is attained. But the *reason* is the pigeonhole `#{fibres of size >= f} <= (r+f)/f`, maximised at `f = 1` — **`(C3)`-attainment is the shadow of `f = 1`, not its cause.** This matters operationally: `(C3)` cannot be imported into the open bracket (anchor 1's D2.2 fence, ZP-6), whereas the fibre cap is a two-line pigeonhole with no ledger behind it. **I derived it from scratch and imported nothing.**

### D3.3 What an upper-bound campaign must prove

> **STATEMENT U (exact).** Let `n = 2^41`, `k = R = 2^40`, `rho = 2^34`, `r = R-rho`, `a = k+rho`, `D` the order-`n` multiplicative subgroup of `F_q^*`, `2^128 < q < 2^256`. Let `(y_0,y_1)` be column-far. Then every bad slope `gamma` admits a locator `S_gamma` with `S_gamma subset W`, where `W = S_1 u S_2` for two fixed bad slopes.
>
> **If U holds, then `B_ca^far(k+2^34) = r+1 = 1,082,331,758,593 = 2^39.977280` exactly** — floor from LB1 (banked, `crossing_location:654-656`), cap from the fibre pigeonhole of D3.1.

U is exactly the statement that `T_rand = 0` and `T_sym = 0`, i.e. **that no bad slope reaches the pencil syndrome through a codeword rather than through the support.** Its two failure modes are the two I measured: the symmetry mode (killed at the razor by `ceil(rho/2) = 2^33` conditions, D2.4) and the random mode (priced at `2^{-6.7e11}` by a model with zero power, ZP-3). **U is the whole residual, and it is stateable — B-5 HIT.**

Banked instruments that could carry U: `background/nodes/rate_half_ca_hankel_split_pencil_equivalence` (HS1/HS3, the exact incidence — already the frame); `critical/nodes/e22_tail_coset_locator_algebra` (the orbit-invariant locator algebra `L_B(X)G(X^M)` — the exact carrier of `T_sym`, needs only the `M >= rho` threshold added); `background/nodes/rate_half_bivariate_locator_extension_kernel_reduction` (`W`-local extension equations, explicitly scoped as NOT enforcing root sets outside `W` or column-farness, `statement.md:56-62` — i.e. it is the right object with exactly the missing hypothesis U supplies). **`(C2)/(C3)/(C4)` are vacuous on the bracket and must not be used** (anchor 1 D2.2, ZP-6).

### D3.4 A correspondence to check (flagged, NOT computed)

The `T_sym` carrier at `M = rho` has size `C(n/rho, r/rho) = C(128, 63)` — one binomial step from the banked qcore plateau `C(127,64) = 2^123.1714` that `rate_half_band_closure/node.json:9` prices as 4.73-4.83 bits short of the razor need and CAPS by Lam-Leung + nesting. **If these are the same object, the upper-bound campaign inherits a proved cap on `T_sym` for free.** I did not compute `C(128,63)` and I make no identification (ZP-12). **Flagged for the coordinator.**

---

## D4 — VERDICT AND THE FAR-CA RESIDUAL MAP

### D4.1 Verdict

**R-HRLOW: classified, and the priority inverted again.** Not resolved as a bound. What *is* resolved:

- **`h_r` is the wrong coordinate.** `h_r = rho + deg(ratio)` (210/210), independent of the support size (2 fields, 5 support sizes). It prices the floor's decay rate, not its existence.
- **LB1 is unique at `h_r = rho+1`, and forced.** `d=1 => |W| = r+1 => T = T_1 = r+1` with zero accidentals at `mu_1 = 1.26e-7` (2 fields). The brief's D1 is answered YES.
- **The `h_r = rho+2` band is NOT moment-bound.** It reaches `r+1`. The brief's D2 hypothesis ("if the low-`h_r` band above LB1 is moment-bound, the extremal count is LB1-limited") has a **false antecedent**; the conclusion survives only as a value, and only conditionally on U.
- **`T <= r+1` is FALSE at razor-faithful shape, field-size independently.** `T = 95` vs `r+1 = 9`. The banked kill of `T <= r+1` (`crossing_location:3112`) was attributed to `mu_1` saturation and explicitly narrowed at `:3133-3135`; **that narrowing is too generous** — there is a saturation-free counterexample. It does **not** touch the banked `B_ca^far(n-r) <= r+1` for `r <= 2^39-2` (`rate_half_band_closure:162`), which is a different shape.
- **The counterexample cannot reach the razor**, and I say exactly why: `ceil(rho/2) - 1 = 8,589,934,591` surplus conditions.
- **The residual is now a single statement, U**, whose two failure modes are both named and one of which is already killed at razor `rho`.

`B_ca^far(k+2^34) < 2^128`: **NO.**

### D4.2 The far-CA residual set after this round

- **R-HRLOW — RESOLVED as a classification, DISSOLVED as a stratum.** The `h_r`-indexed family is `h_r = rho + deg L`; the floor is a `f = |W|-r` property. Recommend retiring `h_r` as a residual index and replacing it with `(f, chi, Aut(D,chi))`.
- **R-U (new, and now the whole thing).** Statement U (D3.3). Closing it gives `B_ca^far(k+2^34) = r+1` exactly. Its two modes: **U-sym** (orbit-invariant locators; already killed at razor `rho` by condition counting, needs the `rho >= 4` symmetric-`T` case measured — see below) and **U-rand** (codeword-mediated locators; the first moment has zero power).
- **R-FG-RAZOR — further downgraded.** `h_r = 2rho` is not a floor-free label: `d3-cube` at `rho = 3` has `h_r = 2rho` and `T_1 = r+1`. FG's floorlessness is a property of witness B's particular `K_0`, not of its `h_r`.
- **R-PSTAR-INTERMEDIATE — retire as a stratum.** `p*` does not separate `h_r = rho+1` from `rho+2` (both `p* = 6` at H1, 5/5 fields).
- **R-KER, R-DEEP, R-LINEDEGREE** unchanged.

### D4.3 FLAGS FOR THE COORDINATOR (AUDIT-AND-DRAFT — no surgery applied)

1. **`crossing_location:3133-3135` — the narrowing of round 33's `T = q` is too generous.** It says the saturation "tracks `mu_1`, not FG membership". There is a **`mu_1`-free** violation of `T <= r+1`: `T = 95` at `mu_1 = 1.26e-7` (`f3_results.txt:32`), column-far, razor-faithful, mechanism identified. Suggested repair: add "…and there is additionally a symmetry-carried, field-size-independent excess on negation-closed `D`, requiring `ceil(rho/M) = 1`; dead at `rho = 2^34`." **I flag; I do not apply.**
2. **`crossing_location:3103-3105` — `h_r` should be annotated with its meaning.** Add: `h_r = rho + deg(e_1/e_0)` on the common support (210/210, 5 shapes, 5 fields), `2rho` when the ratio is not a polynomial; `h_r` is independent of `|W|`. This turns a rank statistic into a computable invariant.
3. **`crossing_location:3129-3132` — extend the `p*` law and record its failure mode.** `p*(d) = max(rho+d, floor((R+1+d)/2))`, 205/210, failing at H2's symmetric-`T` quadratic (5/5 fields, off by 1). And record that `p*` does NOT determine `h_r` (H1: `p* = 6` at both `h_r = 3` and `h_r = 4`).
4. **`background/nodes/rate_half_ca_hankel_split_pencil_equivalence:44-47` — add the shape fence.** "`B_ca^far(n-r) <= r+1`" is stated there at the official candidate's own shape (`r <= R/2`) and is banked as PROVED for `r <= 2^39-2` (`rate_half_band_closure:162`). It is **false as a universal statement at `r > R/2`** on a negation-closed `D`. Worth one sentence so no future round transports it into the open bracket.
5. **Round-37 first item (from my own miss 4):** measure the symmetric-`T` symmetry mechanism at `rho = 3` and `rho = 4`. My parity derivation predicts survival at `rho = 3` and death at `rho >= 4`; I measured neither. If it survives further than predicted the `M >= rho` threshold in D3.1 needs re-deriving — the razor conclusion has 2^33 conditions of slack, so it is not at risk, but the dichotomy's statement is.
6. **Check the D3.4 correspondence** (`C(128,63)` vs the banked `C(127,64) = 2^123.1714` plateau). If they coincide, `T_sym` inherits a proved cap.

### D4.4 Cross-pilot flag (I did NOT read any sibling)

For whoever holds the domain-structure / coset lanes: **the official evaluation domain's multiplicative-subgroup structure is not neutral for far-CA counting.** Negation-closure alone (which every even-order subgroup has) manufactures `C(m-1, r/2-1)` column-far bad slopes at `rho = 2` that are invisible to every first-moment model — 84/84 and 330/330, exact, two fields. The general carrier is the banked orbit-invariant locator algebra `L_B(X)G(X^M)` (`e22_tail_coset_locator_algebra/proof.md:3`). **Any far-CA counting argument that treats `D` as a generic point set is unsound at small `rho`.** What saves the razor is only that `ceil(rho/M) = 1` demands `M >= rho = 2^34`.

---

## PREDICTIONS vs OUTCOMES

| id | registered | outcome |
|---|---|---|
| B-1 | LB1 unique at `h_r = rho+1` (as the `d=1` family), P = 0.60 | **HIT.** `d=1 => f=1 => T = T_1 = r+1` forced; zero accidentals at 2 large fields |
| B-1' | a different `h_r=rho+1` mechanism with `T_1 >= 3`, P = 0.20 | **Correctly priced low — none found.** Scoped by ZP-4 |
| B-2 | the `p*` converse holds, P = 0.55 | **MISS — refuted.** `p* = 6` at both `h_r = 3` and `h_r = 4`, H1, 5/5 fields |
| B-2' | my `p*(d)` law confirmed at `d=2`, >= 2 shapes, >= 2 fields, P = 0.60 | **PARTIAL HIT — 205/210**, 5 named failures (H2 `d2-2to1`) |
| B-3 | an `h_r = rho+2` structural floor exists, P = 0.75 | **HIT** — 5 shapes, 5 fields |
| B-3' | the floor is `>= ceil((r+1)/2)` and attains `r+1` for injective quadratic, P = 0.55 | **HIT — both halves**, exactly |
| B-4 | the dichotomy poses cleanly, P = 0.70 | **HIT** (D3.1), but with a THIRD term (`T_sym`) I had not registered |
| B-4a | mechanism = `(C3)`-attainment, P = 0.30 | **MISS, correctly priced low** |
| B-4b | mechanism = support-size/fibre counting, P = 0.60 | **HIT for `T_fib`** — and incomplete: `T_sym` is a second mechanism |
| B-5 | the upper-bound statement is stateable, P = 0.65 | **HIT — statement U** (D3.3) |
| B-5' | `B_ca^far < 2^128` moves, P = 0.03 | **HIT — it did not** |
| A-1 | `h_r = rho+d`, `dim K_0 = r+1-rho-d`, `d in {1,2}`, >= 2 shapes/fields | **HIT, far stronger: 210/210**, `d in {1,2,3}`, 5 shapes, 5 fields |
| A-2 | `p*(2) = 6,7,7,8,8` at `R = 10..14` | **HIT 5/5** for the injective-quadratic family (`f1_results.txt`, `d2-inj` rows) |
| A-3 | `dim K_0(d=2) = 5,6,7,6,7` at H1..H5 | **HIT 5/5** |
| A-4 | every `d=1` cell has `s=r+1`, `T_1 = r+1`, field-independent | **HIT** at 5 shapes x 5 fields, exactly verified |
| A-5 | no common-support cell has `T_1 > r+1` | **HIT as written, MISLEADING as intended** — see miss 2. `T_struct <= r+1` everywhere; total `T = 95 > 9` |
| A-6 | `d=2` non-injective gives `T_1` strictly between `ceil((r+1)/2)` and `r+1`; some cell attains `ceil((r+1)/2)` | **HALF MISS.** `T_1 = ceil((r+1)/2)` **exactly** at every 2-to-1 cell (5 shapes, 5 fields) — never strictly between. The attainment half HIT |
| A-7 | `s=r+2, d=2` gives `T_1 <= floor((r+2)/2) < r+1` | **HIT** — `floor((r+2)/2)` exactly (symmetric) or 0 (injective) |
| A-8 | `T = T_1` once `mu_1 << 1`; Poisson envelope at >= 8 rows (semi-blind) | **SPLIT: HIT for `d=1`** (`T = T_1 = 9`, 2 fields), **REFUTED for `d=2`** (`T = 95`, `T_1 = 9`). The envelope holds for `d=1` and `T_rand`, and fails by 750x for `T_sym` |
| A-9 | a search finds `h_r=rho+1` pencils outside the common-support family, P = 0.45 | **MISS — and better than a miss: the search was replaced by a THEOREM.** There are none with `>= 2` bad slopes; common support is forced (12/12 reconstructions) |
| MISS-2 guard | max-not-mean; emptiness never promoted; codim != emptiness; moment has zero power both ways | **HELD** — every `T`/`T_1` against a cap or floor is an exact count or an explicit max/min over a named set; no mean appears in any conclusion; the `f2`/`f3`/`f4` counts are exhaustive sweeps, not samples; and I exhibit the moment failing by 750x rather than trusting it |

---

## ZERO-POWER DECLARATIONS

1. **ZP-1 (registered, with a disclosed widening).** No razor-scale computation exists. All machine numbers at `q <= 999983` (registered `<= 65537`; widened to separate the excess from the moment, disclosed), `R <= 18`, `rho <= 4`, `r <= 14`. Every razor number is a closed-form evaluation.
2. **ZP-2 (registered).** Zero power from any `k=1` cell. None was used; every cell in this report prints `4rho<R`, `a>R+1`, `a-1>r` all True.
3. **ZP-3 (registered).** The first-moment model has zero power in both directions. It is wrong by `6.7e11` bits downward at the razor (LB1) and by a factor 750 downward at `q = 999983` (the symmetry excess). No `E[T]` supports any verdict here.
4. **ZP-4 (registered).** Uniqueness/classification claims are scoped to pencils with `>= 2` bad slopes, where common support is a theorem (12/12 verified). I claim **zero power** over pencils with exactly one bad slope, and over any non-generic `y_0, y_1` dependence.
5. **ZP-5 (registered).** Zero power over `char F_q`: every field here is an odd prime. The characteristic-2 remark (a degree-2 `L` is a bijection, so `T_1 = r+1` is automatic at `h_r = rho+2`) is **DECLARED UNMEASURED**, not asserted. The R7 char-2 stretch was not reached.
6. **ZP-6 (registered scope fence).** The type-2 ledger `(C2)` is vacuous by sign on the whole open bracket (anchor 1 D2.2) and was **not imported**. Every cap in this report is derived here from `C(w,r)` / fibre pigeonhole. `q_crit`, `theta_1 = 127.977457`, `theta_2 = 63.988728` are **razor-row constants** and were not used as row-level constants.
7. **ZP-7 (registered).** `T` and `T_1` are different functionals and I never equate them. The `d=2` rows are the proof that equating them is wrong.
8. **ZP-8 (registered).** `h_r = p*` is not used as an FG criterion anywhere; the two principality tests were computed separately (both False at every rate-half `d=1` row, as forced by `dim K_0 = r-rho != r-p*+1`).
9. **ZP-9 (registered).** Every structural claim carries `>= 2` shapes AND `>= 2` fields. Single-field claims: none. Single-shape claims: none, except the two flagged in ZP-10/ZP-11.
10. **ZP-10 (new).** **No exhaustive total-`T` census exists at `rho >= 3`.** `C(26,10) = 5,311,735` is out of reach in stdlib Python within the ramguard window. The `rho = 3,4` evidence is the even-locator scan (a complete sweep of that carrier, `C(13,5)`, `C(15,6)`, `C(17,7)`, `C(18,7)`) plus exact structural verification — **not** a total count. Any statement of the form "`T` is small at `rho = 3`" is NOT supported.
11. **ZP-11 (new, and the round's main gap).** The **symmetric-`T` symmetry mechanism is unmeasured at `rho >= 3`.** Measured only at `rho = 2` (`T = 336` vs moment 7.59, `f2_results_H2.txt:13`). My parity derivation predicts survival at `rho = 3`, death at `rho >= 4`; **the derivation is not a measurement** and I claim zero power on it.
12. **ZP-12 (new).** The D3.4 correspondence between `C(128,63)` and the banked `C(127,64) = 2^123.1714` plateau is **not computed and not claimed**. I flag it as a check, nothing more.
13. **ZP-13 (new).** `T_rand` (codeword-mediated bad slopes) is priced only by a model with zero power. Statement U's second failure mode is **completely unpriced** by this round. That is the honest residual.

---

## MEASURED FUNCTIONALS

Registered and measured: `h_r` (stacked rank) and `dim K_0` at 210 (cell, field, family) rows plus 120 rows of the general `(s, deg e_0, deg e_1)` grid; `p*` (as `min{i : P*F[x]_{<=r-i} subset K_0}`), `d* = min{deg P : 0 != P in Ann(V)_{p*}}`, `dim Ann(V)_{p*}`, and the two principality tests `K_0 == P*F[x]_{<=r-p*}` and `K_0 == P*F[x]_{<=r-deg P}` at all 210 rows; `deg L` and the fibre profile of `L` on `T`; the predicted structural slope set `{-1/L(t)}` and, for every member, an EXACT verification by exhibiting a locator and testing `(M_0+gamma M_1)sigma = 0` (210/210); column-farness by exhaustive sweep of all `C(n,r)` split locators (H1 at 5 fields, H2 at 2 fields); the exact total bad-slope count `T` by the same sweep, with the `T_struct`/`T_acc` split; `|S cap T|` histograms over all (locator, slope) incidences at 2 fields; the common-support reconstruction `syn(e_0) = y_0`, `syn(e_1) = y_1`, `|W|`, `f`, `T_struct`, and the cap `floor((r+f)/f)` at 12 random pencils, 2 fields; the complete even-locator carrier `C(m, r/2)` at 6 cells x 2 fields, with the `covering-the-off-T-orbits` refinement; `mu_1 = C(n,r)/q^rho`, `q*mu_1`, `1-e^{-mu_1}`; the razor closed forms `r+1`, `h_r(d)`, `p*(d)`, `p* - floor(R/2)`, `ceil((r+1)/d)`, `log2 C(n,r)`, `log2 mu_1` at `q = 2^128, 2^167, 2^256`, `log2 C(n/2, r/2)`, `ceil(rho/2)`, `(r+1)/2^39`.

Independent cross-checks against anchor 1 that came out **exact**: `mu_1` at H1's five fields (12.349, 1.0342, 0.12373, 0.0012579, 2.9329e-05); `mu_1 = 48.762` at H2 `q=101`; `p*(LB1) = 6,6,7,7,8` at `R = 10..14`; `r+1 = 1,082,331,758,593 = 2^39.977280`; `log2 C(n,r) = 2,198,635,969,270.39`; `log2 mu_1 = -6.704022e+11` at `q = 2^167`; `p*(razor) = 549,755,813,889 = floor(R/2)+1`.

Registered but **not** measured: the characteristic-2 cell (ZP-5), the `rho >= 3` total-`T` census (ZP-10), the `rho >= 3` symmetric-`T` mechanism (ZP-11), `C(128,63)` (ZP-12). All four were declared in advance or are declared here as gaps, none is silently absent.

---

## COMPLIANCE

`CONSTRAINTS.md` read first, `PREREG.md` second, then the **two named anchors only** (`notes/pilots_20260811/r35_fg_razor/REPORT.md`, `background/nodes/rate_half_ca_hankel_split_pencil_equivalence/statement.md`) — and **nothing else**: no grep, no `ls`, no interpreter invocation, no third read — before the `## Pilot registrations` block (R1 dictionary and faithfulness-checked cell table, R2a-R2g falsifiable derivation, R3's eleven blind priors B-1..B-5' and nine A-predictions, R4's six-clause MISS-2 guard, R5's nine zero-power pre-declarations, R6 deliverable registrations, R7 compute plan) was appended to `PREREG.md` with the **Edit tool**. **No registration was edited afterwards.** The block discloses that the R2 derivation, the cell table, and the A-2 numbers were computed in-head from the anchors before writing, and marks A-8 **semi-blind** (calibrated on anchor 1's published `T` rows).

**COMPUTE LAW: 6 interpreter invocations, 6 under `tools/ramguard`, ZERO breaches, zero bare `python3` for any purpose** — including patching, probing and no-ops (every file edit went through Edit/Write). All from the repo root, with a literal `--` and an explicit `RAMGUARD_TIMEOUT=290`, all `local`: (1) `f1_family.py`; (2) `f2_census.py H1`; (3) `f3_probe.py` — **SIGPIPE'd by a `head` pipe, wrote no results file** (miss 1); (4) `f3_probe.py` re-run, piped to `tail`, wrote; (5) `f4_close.py`; (6) `f2_census.py H2`. This is exactly the `<= 6` I registered. Stdlib only (`sys`, `math`, `itertools`). No Modal, no network, no git, no subagents.

**WRITE DISCIPLINE:** every file edit via the Edit/Write tools. No `sed -i`, no `awk -i`, no `perl -i`, no `tee`, no shell redirection of any kind onto any file. Each `f*_results*.txt` is written by its own script into my own directory. Read-only shell (`grep`, `sed -n`, `awk` print, `tail`, `head`) was used for inspection only. One disclosed convenience: a single `cd notes/pilots_20260811/r36_hrlow && for f in ...` for a read-only line-number sweep of my own results files.

**IMPORTED-SCRIPT RULE (the new round-35 rule): NOT TRIGGERED, and deliberately so.** I imported and executed **zero** banked scripts. All four scripts are fresh implementations against anchor 2's conventions (`v_x = 1/prod_{y!=x}(x-y)`, `y_m = sum_x e(x)v_x x^m`, `M_r(y) = (y_{i+j})`, low-to-high coefficient order, (HS1)/(HS2)/(HS3)), validated against anchor 1's published numbers where the cells overlap (seven exact matches, listed under MEASURED FUNCTIONALS). Anchor 1's Poisson-envelope code was **not** copied; had I copied it I would have grepped `open(`/`write`/results paths and repointed them with the Edit tool before the first import, as pre-committed in R7.

**RAM DISCIPLINE:** file-at-a-time; **`dag.json` never opened**; `critical/nodes/rate_half_band_crossing_location/statement.md` (3,600+ lines) read only through **two bounded windows** (48 lines at `:3096-3143`, 17 lines at `:650-666`) plus line-numbered greps; `background/nodes/rate_half_bivariate_locator_extension_kernel_reduction/statement.md` through one 17-line window; anchor 2 read in full (53 lines). Every locator sweep is a streaming DFS carrying `O(r)` retained state per node (the coefficient vector is updated incrementally, so `C(24,10)` never materialises); all results checkpointed to their own files; no run approached the memory ceiling.

**QUARANTINE:** `notes/pilots_20260802/CAMPAIGN_LEDGER.md` never opened at any line. **No round-36 sibling was read, opened, or listed** — `r36_lawcount_geom`, `r36_sat3_on_l2`, `r36_m4_nonsplit` were taken by NAME from `CONSTRAINTS.md:35-36` and excluded at search level on every recursive grep, together with `pilots_20260802`, `prize-codex-work`, `.git`, `__pycache__`, and `--exclude=dag.json`. **I never ran `ls`, `find`, or any listing on the parent directory** — CONSTRAINTS supplied the names, so unlike round 35 no listing deviation was needed. No path containing `prize-codex-` was touched. The `r35_*` directories are readable under CONSTRAINTS; I read exactly one file in them (anchor 1) and my greps surfaced lines from `r35_fg_razor/FABLE_AUDIT.md` and `e4_results.txt`, which are within the permitted set.

**WRITE SCOPE:** every write is inside `notes/pilots_20260811/r36_hrlow/` — `PREREG.md` (registrations appended), `f1_family.py`, `f1_results.txt`, `f2_census.py`, `f2_results_H1.txt`, `f2_results_H2.txt`, `f3_probe.py`, `f3_results.txt`, `f4_close.py`, `f4_results.txt`. **No `dag/`, `nodes/`, `critical/`, `background/` or `tools/` file was created, edited, or touched**; no git operation of any kind; no scratch file outside the pilot directory; nothing in `/tmp`. **AUDIT-AND-DRAFT respected: no node surgery applied** — the six corrections this round forces are flagged in D4.3 for the coordinator, not made.
