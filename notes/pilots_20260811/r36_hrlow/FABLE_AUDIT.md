# FABLE_AUDIT — r36_hrlow (round 36, bank 3/4)

Auditor: coordinator (Fable). Date: 2026-08-11.
Verdict: **BANKED — the h_r band classified and h_r DISSOLVED as
the residual coordinate; LB1's uniqueness-and-forcedness adopted;
the negation-closure excess adopted with its exact kill;
STATEMENT U adopted as the far-CA residual of record; four forced
corrections applied (the too-generous narrowing, the h_r
annotation, the p*-converse refutation recorded, the shape fence
on the supplier node).** Node work: two inline markers + the
round-36 R-HRLOW addendum + the shape-fence addendum on
split_pencil_equivalence. No status flips; census unchanged.

## Verification (hand-checks)

1. **Common-support theorem:** solving y_0 + gamma_i y_1 =
   syn(u_i) for (y_0, y_1) gives e_0 = (gamma_2 u_1 - gamma_1
   u_2)/(gamma_2-gamma_1), e_1 = (u_1-u_2)/(gamma_1-gamma_2),
   supported on S_1 u S_2 — CHECK (and the 12/12 explicit
   reconstructions are the right verification).
2. **The classification chain:** column-far => |W| >= r+1 (an
   r-superset of W would be a common locator); one bad slope =>
   f <= d (the slope error vanishes on a fibre, support <= r
   needs fibre >= f, fibres <= d); d = 1 => f = 1, |W| = r+1,
   L injective => T_1 = r+1 FORCED — CHECK at every step.
3. **The razor kill:** ceil(rho/2) = 2^33 conditions on one
   unknown => surplus 2^33 - 1 = 8,589,934,591 — CHECK exact.
4. **The C(128,63) relation:** C(128,63)/C(127,64) =
   C(128,63)/C(127,63) = 128/65 (~0.98 bits) — ONE binomial step,
   NOT equal; the pilot's flagged correspondence is a genuine
   open check (whether the Lam-Leung+nesting cap transports),
   queued for round 37, not resolved here.
5. **The banked-narrowing correction verified:** the T = 95
   exhibit at mu_1 = 1.26e-7 is column-far and razor-faithful by
   the printed faithfulness lines; the round-34 narrowing text
   was indeed too generous — marker applied. The banked
   B_ca^far(n-r) <= r+1 (r <= R/2 shape) is NOT contradicted —
   the pilot's own scope separation is correct, and the shape
   fence now guards the supplier node.

## Assessment

- Dissolving h_r into (f, chi, Aut(D, chi)) is evidence-forced:
  the dictionary (210/210), the s-independence, and the
  h_r = 2rho floor-carrying cubic together retire h_r as a
  stratum label; the p*-converse refutation retires
  R-PSTAR-INTERMEDIATE the same way.
- STATEMENT U is the right distillation: floor (banked) + cap
  (from-scratch pigeonhole) pincer B_ca^far to exactly r+1 iff U;
  its two failure modes are cleanly separated, one already
  condition-killed at razor rho, one honestly unpriced. The
  named carriers (e22 locator algebra + the bivariate
  locator-extension node with exactly-the-missing-hypothesis) are
  real and correctly cited.
- The A-5 miss ("registered the cap as if it were the whole
  count") is the round's best self-catch — the registered literal
  survived while the intended reading died, and the pilot said so
  first.
- MISS 1 (the head-pipe SIGPIPE killing a run's output) is the
  recover-report lesson recurring in pilot form; round-37
  CONSTRAINTS: results files are written by the script, never
  through a pipe.

## Compliance

Compute law CLEAN 6/6 (11th consecutive clean pilot; the SIGPIPE
loss disclosed as a wasted invocation, not a breach). Write
discipline clean. Imported-script rule not triggered (fresh
implementations validated against seven exact anchor matches).
Quarantine exemplary (names from CONSTRAINTS, parent never
listed). Registrations honest (A-5's misleading-as-intended
grading is the standard).

## Mint queue additions

1. STATEMENT U as the far-CA target of record (+ the exact-value
   consequence B_ca^far = r+1).
2. The h_r dictionary + common-support theorem node.
3. The negation-closure excess fence (mechanism, count, control,
   razor kill) — cross-wired to the e22 locator algebra.
4. The R36-D dichotomy (posed, with falsifiers).
5. The p*(d) law with its named failure mode.

## Round-37 anchors fed by this bank

U-rand (the unpriced mode — the honest residual); the rho = 3
symmetric-T measurement (the dichotomy's gap); the C(128,63)
correspondence check; char-2 cells.
