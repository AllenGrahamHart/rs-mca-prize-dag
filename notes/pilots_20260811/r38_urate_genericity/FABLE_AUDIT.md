# FABLE_AUDIT — r38_urate_genericity (round 38, bank 3/4)

Auditor: coordinator (Fable). Date: 2026-08-11.
Verdict: **BANKED — R-GENERICITY's rank half PROVED (with two of
four side-conditions); R-URATE REFUTED and replaced by the
exchange law (a banked residual-map target falls, with a
three-field exhaustive-census counterexample and a banked census
number corrected); R-USYM CLOSED (carrier completeness by degree
parity).** Node work: the ROUND-38 UPDATE marker on the round-37
residual map + the URATE/genericity addendum. No status flips;
census unchanged.

## Verification (hand-checks, all pass)

1. The line-pencil decoupling: the row structure is manifestly
   [delta_x | gamma_i delta_x | Z_{Y_i}(x) e_i] — the reading as
   point-on-line with blocks coupled only through p_x is exact;
   the per-point 2 x d(x) Vandermonde left-null argument gives
   the d(x) <= 2 corollary — CHECK.
2. The razor identity: 2r = 2(63*2^34)... r = R - rho =
   2^40 - 2^34; 2r/rho = (2^41 - 2^35)/2^34 = 128 - 2 = 126
   EXACTLY — CHECK (the exact-double-cover design is forced).
3. The exchange law: dT/d(delta) = 1/rho - 1 < 0 at rho >= 2 —
   the razor optimum at delta = 0; at rho = 3 the m=2 evaluation
   floor((2(r-1)+1)/(rho-1)) = floor(19/2) = 9 and T_max(2) =
   r + 9 = 19 — CHECK. The three-field full-census T = 19 with
   T_other = 0 is the right evidence standard for a cap
   refutation.
4. The carrier completeness: sigma = sigma^e(X^2) + X
   sigma^o(X^2); dependence => (X+c)-factor or pure-even; odd
   degree excludes the pure-even branch — CHECK (two lines,
   clean).
5. The pigeonhole-margin honesty (ZP-13, registered BEFORE the
   runs and then vindicated by its own m=2 counterexample which
   needs no pigeonhole) is the model of the campaign's
   epistemics.

## Assessment

- The far-CA picture after banks 2+3 of this round: the lower
  bound r+1+126 is now modulo TWO residues (in-block
  chi-injectivity, column-far Case B); the upper cap is
  normal-form-conditional with a named non-algebraic margin. The
  count is stable at the razor and PROVEN mobile off it — the
  correct final shape is likely "razor = r+1+126 exactly, small
  rho = strictly more", pending the two residues and the
  normal-form exhaustiveness.
- The self-refutation ledger (six of its own predictions broken,
  including its headline sharper cap killed by its first run) is
  the strongest single-round calibration display so far.
- The f_concurrency_equiv cross-reference is real and goes to
  the wiring queue.

## Compliance

CLEAN 5/5 under the checklist. Append-mode results. Anti-import.
Quarantine exemplary (no parent ls; names from CONSTRAINTS).
Registrations in three parts; A-8 honestly declared
registered-but-unmeasured.

## Mint queue additions

1. The rank formula + multiplicity-<=2 corollary node (PROVED).
2. The exchange law + the C3 counterexample fence + the
   corrected census numbers.
3. The carrier-completeness theorem (R-USYM closure).
4. The coset m <= 2 obstruction.
5. The f_concurrency_equiv cross-reference.

## Round-39 anchors fed by this bank

The two remaining +126 residues (in-block chi; column-far Case
B); the normal-form exhaustiveness question (deficiency
mechanisms cheaper than one fibre slope); C9's uncensused break.
