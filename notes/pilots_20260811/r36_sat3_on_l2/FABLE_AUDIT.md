# FABLE_AUDIT — r36_sat3_on_l2 (round 36, bank 2/4)

Auditor: coordinator (Fable). Date: 2026-08-11.
Verdict: **BANKED — (SAT3)-on-(L2) proved NON-VACUOUS (T = 2 over
mu_32 on certified e=m=2 objects, both fields); the (PAR)
parametrization adopted as the instrument of record after full
coordinator hand-verification; the realizability ledger's m=2
cell flip adopted as a forced correction with the coordinator's
stacking note.** Node work: the round-36 (SAT3)-on-(L2) addendum.
No status flips; census unchanged.

## Verification (hand, complete)

The entire (PAR) apparatus was verified by hand before banking:
(1) E1*f + E2*g gives Q_2(f^2-kg) = Q_0(g^2+hf); coprimality +
degree forces f^2-kg = Q_0*L, L linear; then Q_2*L = g^2+hf and
(dividing E1 by g) Q_1*L = fg+hk. (2) BOTH converse substitutions
vanish identically (expanded term by term — E1 and E2 are killed
with no residue). (3) det([[f,k],[g,f]] + z[[g,f],[-h,g]]) =
(f+zg)^2 - (k+zf)(g-zh) = (f^2-kg) + z(fg+hk) + z^2(g^2+hf) =
L*Q_z — expanded by hand, exact. (4) The third ell-condition
f(ell)g(ell) = -h(ell)k(ell) follows from the first two by
multiplying them (divide by f(ell)g(ell)), failing exactly at
f(ell) = g(ell) = 0 — the pilot's self-caught refinement is
correct and necessary. (5) Dimension 20+1-2-1 = 18 = the round-35
measured good-component dimension, by an independent route.
(6) (ERC2) read at the node: PROVED, closes 1 <= e <= m-1, so
(SAT3) forces e = m — the ledger correction's hinge is solid;
the (X6) subtraction target (saturation_rigidity:49-53) read and
verbatim as claimed. The +4-O arithmetic checks (23->18 shifts
excess by exactly 5).

## Coordinator reconciliation (the stacking note)

The bank's dimension correction (curve side, 5 units) and round
34's automorphism quotient (solution-orbit side, +4..+6) are
independent terms of the same ledger; stacked, the m=2 cell sits
at ~ +8..+10. The round-33 conjecture ("realizable iff m <= 2")
is now doubly re-posed to m <= 1 — consistent with the round-34
TCAP re-pose, from a second mechanism. Recorded in the addendum;
the banked pilot REPORT stays byte-original.

## Honesty audit

Among the campaign's best: the T-record scoped against the
banked-but-out-of-class T = 4 records BEFORE any number (MISS 1);
the packing derivation (X6) self-subtracted to (SAT4) verbatim
with the pre-registered P14 = 0.80 firing; the PGL_2 double-count
in its own registered ledger self-caught and corrected (13.20-bit
gap, reproducing anchor 2's numbers at five fields); the
f(ell)=g(ell)=0 refinement caught by its own data; Z1 (bespoke
vs mu_32) held rigorously throughout.

## Compliance

Compute law CLEAN 4/4 (zero bare python3 — 10th consecutive clean
pilot). Write discipline CLEAN. Imported-script rule engaged the
right way (zero imports; helpers duplicated per file SPECIFICALLY
so no import can write at import time — a pattern worth
codifying). Quarantine clean (sibling names from CONSTRAINTS; the
parent never listed — the pre-listing rule fully working).
Registrations honest; the three registration errors reported as
misses, not edited.

## Mint queue additions

1. The (PAR) parametrization node (four-line proof, witness-
   checkable, with (RES) and the determinantal form).
2. The T = 2 witness fence (first non-vacuous (SAT3)-on-(L2)
   objects; the exact-solve pattern square-root-mod-g +
   proportionality-mod-f).
3. The realizability-ledger correction record (e=m forces dim 18;
   stacked with the quotient; the double re-pose to m <= 1).
4. The eigenvalue-confinement obstruction shape (the 2x2 pencil
   reading; the occupancy/concentration functionals).

## Round-37 anchors fed by this bank

The THIRD EXACT SOLVE (the single missing instrument before
T = 4); the third-prescription algebra as its own question;
the s != 0 degeneracy criterion.
