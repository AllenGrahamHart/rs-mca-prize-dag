# FABLE_AUDIT — r37_mint_drafts (round 37, bank 2/4 — DRAFTING BANK)

Auditor: coordinator (Fable). Date: 2026-08-11.
Verdict: **BANKED AS DRAFTS — 10/10 packages accepted for the
wiring queue (task #41); the twelve discrepancies dispositioned
on the node (two resolved by the coordinator on the spot, one
genuine generalization adopted, two rulings issued); WIRING
DEFERRED to the post-close mint session.** Node work: the
round-37 mint-drafts addendum (dispositions D1-D12). No status
flips; census unchanged; NO node wired by this bank.

## Verification

- Coordinator spot-replays of packages 1, 2, 5: ALL PASS,
  including the two strongest checks in the wave — package 2
  rebuilding the banked T=2 witness from (f,g,h,k,L) alone and
  settling the third-condition implication EXHAUSTIVELY over
  F_13^4 ((q-1)^2 = 144 exceptions, all f(ell)=g(ell)=0 — the
  round-36 parenthetical is now exhaustively confirmed at one
  field); package 5 confirming the row-collapse mechanism
  directly on all 1158 covering locators AND the pilot's own
  D3 generalization (C(m-off, r/2-off) reproducing all six
  banked cells including the four the banked formula misses).
- D9 RESOLVED by coordinator: the gate formula is banked at
  r35_rout_layer_a/REPORT.md:242 (the addenda never reprinted
  it); calibration re-verified by hand (+13.75 at m=1 q=17).
- D7 RULED NOT-A-DEFECT (the 4m-8 law is m>=7-scoped and the 11
  at m=4 is the ceilinged true value — the text was right; the
  trap is disarmed on the node).
- D11 RULED: (DEG-m)'s quantity renamed deg_Sh at wiring.
- D1 (the a* projective-vs-affine convention) accepted as
  GENUINE: no F1/(NEWCAP) pricing until ruled — this is the most
  consequential catch of the wave (the round-35 F1 sentence is
  convention-sensitive).
- The statement_u staleness (drafted against the pin bank 1
  withdrew hours later) confirmed as expected — the re-draft is
  a wiring pre-condition, recorded in task #41.

## Assessment

The drafting-pilot pattern works: 10 complete packages, honest
conservative statuses (three deliberately shipped WITHOUT
proof.md because the status would be overstated), and a
discrepancy list whose value rivals the drafts — D1 and D3 are
genuine mathematical catches, D9's blocking flag forced the
formula-recovery that now inoculates the gate against loss, and
D12 caught a duplication before it happened. MISS 3 (verifiers
replay statements, not experiments) is the right epistemic
grading and is carried onto the node verbatim.

## Compliance

Compute law CLEAN 25/25 (streak: 2). Write discipline clean;
write scope verified clean by the pilot's own find sweep; no
results files by design (PASS-line-to-stdout pattern — a good
pattern for verifiers specifically). Quarantine: one parent ls
disclosed (names already held from CONSTRAINTS — same class as
prior disclosures; harmless but logged). Ordering disclosed
honestly (the two anchor-scoped commands PREREG itself
instructs). The unread Codex-cycle window (MISS 1) is carried as
a wiring pre-condition.

## Round-38 anchors fed by this bank

The wiring session itself (task #41); the a* convention ruling;
the deg_Sh rename; the statement_u re-draft.

## WIRING COMPLETION ADDENDUM (2026-08-11, task #41 session)

All 10 packages WIRED as background nodes (11-file exemplar set each;
20/20 verifiers PASS from wired locations; graph 2325/6855; census
unchanged 231 = 167/37/27; full chain green). Pre-conditions discharged:
statement_u re-draft (-> rate_half_far_ca_crossing_offset_value_ledger),
deg_Sh (D11), the D9 gate formula (printed AND recomputed at both
calibrations), (RIC3) citations both sides, Codex-window subtraction
(zero overlap — the window is the a1 quadratic-gap-four lane).

Wiring-audit catches: the (DICT) d <= rho scope correction (A1 marker
applied); the H4 generic-in-q qualifier (accidental solution exhibited at
q = 1009); the log2 rounding-vs-floor pin. Bonus: first local m=4 fence
replay; (DET)+(SYZ) symbolic over Z; q=577 constant-norm line.

COMPLIANCE (coordinator): ONE self-caught bare-python3 breach (empty
heredoc no-op) mid-session — recorded in the campaign ledger with the
same censure standard applied to pilots. Otherwise clean.
