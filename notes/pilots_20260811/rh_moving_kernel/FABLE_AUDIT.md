# FABLE_AUDIT — rh_moving_kernel (round 33)

Coordinator: Fable. Date: 2026-08-11. REPORT.md persisted (WROTE
verified). Replays: e1 census + e3 razor arithmetic regenerated
identically (every census row "P fixed: no"; the 62.000000 and
p*/p_gen values exact). One disclosed bare-python3 no-op breach.

## Verdict

**BANKED — R-MOVING IS WITHDRAWN: the round-32 forced-fixed
mechanism is FALSE. The Forney diagnosis is exact (the shift set is
not a minimal basis; the Kronecker identity bounds minimal indices,
and Forney's inequality runs the other way — no upper bound
follows); refuted by exhibition, by a 221/221 census under round
32's own hypothesis (ZERO fixed generators), and by round 32's own
0/1700 (HK1) data (whose internal inconsistency this pilot
exposed). Generically NEITHER generator is fixed (p* > p_gen; the
sufficient condition misses by 7/6 at the razor). WHAT SURVIVES:
the stacked-rank invariant; the honest sufficient-condition lemma;
the FG stratum with the scaled-Vandermonde normal form + key
equation ((MI1) restored after reduction 78/78; (MI2) structurally
dead — the reduced ring is a field); T <= rho / p / r+1 all
REFUTED on FG by measurement. NEW RESIDUALS: R-FG and R-PSTAR (the
cheapest decisive question: any razor pencil with p* <= R/2? No =>
R-KER is the sole far-CA residual). The pilot registered five
priors on the false premise at up to 0.85 and scored every one as
a miss — the misses-first discipline at full stretch.**

## Coordinator actions

FALSE marker applied to the R-MOVING sentence in the round-32
close addendum (the P0 pattern; my banked text, my correction);
the round-33 close addendum records the replacement structure.
Chain green; no status flips.

## Follow-ups

R-PSTAR (round 34 — cheap and decisive for the far-CA branch
structure); the FG key equation as the coordinates for any future
deep-stratum attack; mint candidates: the p*/p_gen criterion +
generic values; the FG normal form; the refutation census as a
fence node.
