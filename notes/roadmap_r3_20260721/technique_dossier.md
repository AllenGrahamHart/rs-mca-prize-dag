TASK B1 — THE TECHNIQUE DOSSIER (merged from A4 + A5; all node names verified against dag.json @ 1030 nodes, all cited anchor files verified to exist in both trees; instance counts are as reported by A4/A5, not extrapolated)

---

## 1. THE RECURRING WINNERS, ranked by instances × wins × breadth

**#1. Cyclotomic-norm / engineered-prime exclusions** — ~15 verified instances (A4 list of 15, KB #73–117 + WAVE7/8/9/14/17/18), ours-only but spanning THREE unrelated lanes (dli/WCL, F3/HGE4, C36).
- *Core:* any low-complexity relation (low weight, short window, narrow width) over an official row forces a nonzero algebraic integer u with an integer norm divisibility; official rows are engineered with huge 2-adic gates (v_2(q−1) ≥ 41), so 2^{f(w)} | Norm(u) while |Norm(u)| is polynomially capped — contradiction, or a finite engineered-prime census exhausts the survivors.
- *Why it keeps working:* the official rows' dyadic engineering is not incidental — it makes every finite/structured channel arithmetically rigid. The margins are absurd (v_2 gate 41 vs realized max 18–29; 23 doubling levels at slot (2,5)), which is why the same move closes wholesale.
- *Wins:* the ONLY family that outright CLOSES slots — (2,5) and (2,6) closed; Newton short-window closed 30 of 34 levels in one stroke; HGE4 quarter-width collapse killed the entire near-third strip; C36's 6^{n/4} closed the mobius large-field branch; c1r3's admissibility gate made the repaired pose survivable. No upstream counterpart (his arithmetic lives in rank ledgers and window compilers instead) — this is our comparative advantage.
- *Untried transfers (from A4, all target nodes verified open):* (i) `f3_h3_dsp8_correlation_bound` — pose the closing satellite "max P ≤ 24 at official aspect" via the audited collision-norm/coupled-ideal template, falsifier: any official row with P ≥ 21 (highest-leverage single pose on the board); (ii) `f2_growing_order_myerson` (summit) — "official-row extras of orbit weight ≤ 4 are ambient-excluded", a summit analogue of the WCL weight-3/4 censuses, never posed; (iii) `f3_hge4_norm_gate_count` — parity + norm combined pose on the odd-width primitive-swap free-union class (ERT4'''' residual); (iv) WCL slots (1,5)/(1,6) with the KB #98 Norm(u)-saturation gate fix, and (2,7)+ pending the canonicalization counting problem.

**#2. Pre-registered falsification / route-cuts** — both repos at scale: ours 12 survivals vs 7 kills with pre-registered kill lines (A4-e), upstream 17 `_no_go`/`_obstruction` Lean packages + 5 route-cut certificates + the moment-order floor + `CONJECTURAL_WITH_FALSIFIER` as an official node state backed by 1,342 verifier scripts (A5 T3/T9).
- *Core:* file impossibility as a first-class object; attack before proving; every conjecture ships its falsifier shape at birth.
- *Why it keeps working:* kills are net-POSITIVE for proof velocity — A4's clearest meta-result. The subjohnson kill (#124) seeded SUCCESSOR-A → Lemma COL → census gate PROVED within 48 hours; the G1 falsification exposed the wrong DEMAND and led to clauses (D)/(P) PROVED; the wave-9 fixed-crossing kill forced the re-pose that got DETERMINED below 2^167 one wave later. The ev-lane-until-survived rule made the C1'-r2 T-KILL cost zero downstream.
- *Complementarity is proved, not hypothesized:* our fleet run on HIS pre-registered falsifiers produced the null-suppression datum (twice at (41,20,10,2): N_w(z) can exceed N_w(0)), which adjudicated between the two candidate forms of his one binding red. Neither side alone gets that.
- *Untried transfers:* (i) import his **moment-order floor** (r ≥ 641,593 at M31 MCA / 680,397 at list) as a formal route-cut node in our DAG — free, and it pre-emptively deletes every planned fixed-moment attack on F2-finite/Q from our 22 reds; (ii) export our two honest refusals (u1_x4 h-band, dli F-round) in his no-go certificate format — his triage machine only ingests certificate-shaped negatives; (iii) owed constructive rounds at `f3_h3_dsp8` (round 2: BUILD rich primes from the norm set), `l1_mixed_petal_amplification` (attack.md measurement round never run), and per-chamber witness hunts at the 13 chambers; (iv) falsifier-at-birth vocabulary for our reds that lack one (B-WEAK has one; most others don't).

**#3. Exact-integer certificate replay / digit-exact cross-audit** — the highest raw instance count: ~10 named instances ours (A4-f), 536 manifested certificates upstream, and the one channel that is ALREADY FUSED in both directions (PR #1010 four-pair replay in his queue; his cor:prize-window-compiler consumed our n=2^41 determination; our mb_exact_margin reproduced his a0 = 1116047 / 22.011-bit margin exactly).
- *Core:* every status flip requires an execution re-pin of exact integers; hash-green ≠ execution-green (the #177 law).
- *Why it keeps working:* it is where catches actually fire (333+; #155's 2^-691 → 2^-690.2765, #71's exact-enumeration falsification repaired to land at exactly n³). In a two-repo program, digit-exact replay is the only trust protocol that composes.
- *Untried transfers:* the pruned-Q toy packet (his good-first-PR #1, the sole path onto his row-sharp-Q hard input) is exactly this genre and is the banked next submission; the {2^39, 2^39+1} sliver budgets at `rate_half_band_closure` likely fall to exact case analysis before any new theory; his checklist item 6 (one summed integer certificate per adjacent deployed row) is the natural joint deliverable since we hold the strongest per-row replays.

**#4. First-match atlases / cell ledgers** — ~6 instances ours (A4-g), 104 mentions + 5 Lean packages + 4 certificates upstream where it is input (i) of his final conditional theorem and item 2 of the Grande-Finale-v3 checklist (A5 T1).
- *Core:* exhaustively cover witnesses by an ordered cell family, charge each to its first matching cell (no double-charge), then demand a *paid* aggregate per cell — partition and payment are separate obligations (he explicitly polices this; we learned it twice the hard way, catches #119 and #102: ledgers must be posed at composition level, never per-cell).
- *Split strength:* his partition machinery (object-agnostic `firstocc_*` Lean kernels), our payments (W2 spend map with +75.15/+7.23/+40.82-bit margins; K4's Σ_χ(m_χ+1) ≤ (121/128)n⁶ chart sum). Our own `petal_g1_layer_maps/upstream_crosswalk.md` adjudicated the relation as "analogy, not a consumer bridge" — the bridge is the opportunity.
- *Untried transfers:* (i) his first-match Lean kernels formalize our G1 chart-sum ledger nearly for free → first Lean-backed floor on our side, feeding his checklist item 2; (ii) `ww_row_envelope_clause` — the allocation law R1' is imported, the per-cell envelope constant is the open pose (see #6 below); (iii) l1's "non-intrinsic first-match" residual is an unposed ledger; (iv) translating the K4 payment into his profile vocabulary gives his atlas a second proved cell family beyond quotient/planted/field-drop.

**#5. Exact descent (chart-carried quotient + scalar/base-field)** — ~9 instances ours (A4-c: K4, SUCCESSOR-A, Lemma COL, qa22 3392/3392, wave-1/3 collapses, WCL (2,6), PR #1013 prefix fibers), 26 M31 threshold notes + the full descent-organized M31 Lean lane upstream (A5 T2).
- *Core:* reduce at-q statements to quotient/prime-field statements exactly, prove there, lift back; the tail collapse Σ C(m,t) = m+1 is row-agnostic.
- *The hard-won scope law:* row-level/word-uniform descent is FALSE (#124: ×786 over cap at first open size); chart-carried descent is TRUE. Every future descent pose must carry its chart.
- *Asymmetry:* scalar descent is our largest documented audit debt (THREE_VS_SEVEN: our statements at q, toys prime-field) — he is decisively stronger; our one transported artifact (catch #11 β-normalization) came from his correction.
- *Untried transfers:* (i) `l1_mixed_petal_amplification` — chart-carried descent for MIXED profiles (descend mixed-petal support with its layer chart, reuse the m'+1 tail collapse per chart; full-petal machinery is proved, only chart-transport unposed); (ii) `rate_half_list_adjacent_crossing` — per-chamber quotient descent to the subgroup lattice (fiber-two path-fiber exclusions are the existing prototype); (iii) run his m31_scalar_descent_equivalence pattern across our seven floors for β-explicit restatements — prerequisite for resolving the CIRCLE-rows scope question (UPSTREAM_HARVEST D.4) against our conditional claim; (iv) `xr_highcore_collision_count` via component-union atlas descent.

**#6. Extended-window/ledger repricing + window compilation** — ~6 instances ours (A4-b), 2 compilers + Lean package + certificate upstream (A5 T7); the two halves are the same move at different maturities and are ALREADY FUSING (his compiler consumed our n=2^41 determination).
- *Core:* a refuted truncated allowance is not a dead lane — gate the accident class to official admissibility, extend the ledger window (L+5 → L+7), and the continuum claim reprices into finitely many integer slots. The cost is real and priceable: widening minted six new emptiness slots (4 → 10 dli leaves); the purchase was 2.85–3.9× margin on a lane that looked terminally killed.
- *Why it keeps working:* see Section 2 — this is the strongest hint about where the difficulty lives.
- *Untried transfers:* (i) compile the 10 WCL-ZONE-ext slots in his window-compiler format (integer check per slot + manifest + verifier) — converts the dli amber baseline (Decision 6) from prose obligations into certificates his triage machine ingests; natural third PR after #1010/#1013; (ii) `ww_row_envelope_clause` — the sup-form envelope is exactly a truncated-ledger shape; pose an extended per-cell ledger repricing the 1.263-bit mean/sup gap in the R1' currency; (iii) DSP8's accident-depth rectangle menu route; (iv) pre-planned repair playbook if a c2pp deep-depth kill fires (t=4 F-a, 40+-row F-c).

**#7. Pencil / normal-form reductions** — ~8 instances ours (A4-d), upstream `capg-split-pencil-refutation` certificate + the budget-3 split-pencil census in his newest local commit 7394eb47.
- *Core:* normal-form the witness matrices; the recurring bonus is termination in EXACT finite lists (13 chambers with the rank-deficient branch provably empty; 12 gcd triples), not mere dimension counts. Honest scoreboard: 0/13 chambers closed — this family reduces, it has never closed a red alone; pair it with #1 or #5 for the kill.
- *Untried transfers:* (i) split-pencil enumeration of the {2^39, 2^39+1} sliver witnesses at `rate_half_band_closure` (staircase equality already covers B ≤ B_Q; the sliver is pencil-sized); (ii) the former `xr_tangent_support_mismatch_bridge` pencil proposal is now P-A2, and the same normal form may attack its combined mismatch count; (iii) our Modal DP fleet replaying and extending his 7394eb47 split-pencil census (the joint mode with proved yield).

**#8–10, briefly.** **Rank/slack ledgers vs kernel-basis** (63 certs his vs our proved identities): recast the paused M5 A=384 pivot packet (PR #172) in his rank-ledger certificate format to give it a replay harness; offer him the F5 P9 W-collision identity as an exact replacement for what his rank-nine census approximates numerically. **Parity sharpenings** (~7 instances, cheap, keeps paying): the named next steps are the HGE4 odd-width class (combined with #1) and checking the wz2 odd-channel lemma against the WCL odd slots (1,5)/(1,7). **Sidon/Fourier payments** (45 mentions his, template-match with our F3/F5): see U3 below — his cell-ordering principle is a structural warning our dsp8 pose currently violates.

---

## 2. UNEXPECTED REPEAT PERFORMERS — and what the recurrence hints at

Three recurrences were flagged surprising, and one datum. Read together they point at one place.

**(a) Cyclotomic-norm exclusions winning in three unrelated lanes** says the official rows' 2-adic engineering propagates rigidity into every finite channel of the problem. Any obstruction of bounded description complexity is forced into an integer norm equation the engineering over-determines by a huge margin. **Hint: the finite part of both prize problems is not where the difficulty lives — it is systematically exhaustible.**

**(b) Extended-window repricing reviving a lane whose allowance was proved wrong** says the natural local ledgers are mis-truncations of a true global ledger: the failure was never "the bound is false," it was "the window at which the bound becomes true is a constant wider than the naive one, after gating a finite accident class." This happened at c1r3 (accident row 918552577 reprices from a killing 3.000058 to 1.333359), at WCL-ZONE (mass prices only at L ≥ 13, so L ≤ 12 is emptiness), and at the matching-truncation sharpening. **Hint: there is a finite-defect principle operating — every refuted truncated ledger seen so far differs from a true extended ledger by finitely many gateable accidents. Difficulty is not in any local allowance; it is in the cross-scale composition of allowances.**

**(c) Chart-carried descent true / row-uniform descent false — twice** says the objects are genuinely fibered: truth lives on charts, and any statement that forgets the fibration is not merely unproven but false (×786 violation at first open size). The same lesson appeared independently as the cell-ledger composition law (#119, #102) and as his partition-vs-payment policing. **Hint: the correct statements are exactly the ones that carry their bookkeeping structure; "uniform" is the recurring lie.**

**(d) The null-suppression datum + his Sidon insight** (a large Sidon-like fiber is NOT a hidden approximate group; mode-at-null fails at (41,20,10,2), forcing the exchange-compression form of his Q atom) say the extremal witnesses are quasi-random, not structured — inverse/classification tools point the wrong way at exactly the binding rows.

**The bold synthesis.** Every proved barrier on the board carries the same message from a different direction: single-poly Stepanov cannot fit the 4K allowance (nodal barrier, wave-16); fixed-moment routes need order ≥ 641,593 (his moment-order floor); row-uniform descent is false; per-cell ledgers do not compose; inverse arguments void against Sidon fibers. These are all statements that **bounded-complexity, structure-classifying certificates provably cannot reach the sharp constants.** Meanwhile everything structured closes: finite channels to norm gates, structured mass to exact staircases, accidents to gates. What remains — the 1.263-bit mean/sup gap at ww, the joint-endpoint budget at B-WEAK/c2pp, the DSP8 correlation constant, the summit envelope — is one problem wearing four costumes: **pricing the quasi-random core by an exact global ledger, i.e., the shared cancellation barrier.** The winning techniques recur because they are the three legs of the only argument shape that can work there: exhaust the structured part (norm exclusions), carry the fibration exactly (chart descent / first-match), and pay the remainder globally at the correct window (extended-ledger / Fourier payment). The program's true frontier is not any single red; it is proving the payment step once, in a form all four costumes instantiate.

---

## 3. CANDIDATE UNIFYING LEMMA SHAPES

**U1 — The Official-Row Low-Complexity Emptiness Lemma.** *Shape:* Let q be official (v_2(q−1) ≥ 41, or lane norm-gate N_L). For every lane object class of complexity ≤ w (orbit weight, window width, or swap width, lane-dependent), any witness yields u ≠ 0 in Z[ζ_m] with 2^{f(w)} | Norm(u) and |Norm(u)| ≤ g(w,m); there is a lane-uniform w_0 with 2^{f(w)} > g(w,m) for all w ≤ w_0, so the class is empty — and for w slightly above w_0 the survivors are exhausted by an engineered-prime (Pocklington/Proth) census. *Existing instances:* WCL slots (2,5),(2,6); weight-3/4 ambient; Newton short-window (sharp at 2ℓ+1); HGE4 quarter-width and swap-norm; C36 6^{n/4}; c1r3 gate. *Open reds that would become instances:* WCL (1,5),(1,6),(2,7)+; summit weight-≤4 extras; HGE4 odd-width swap; DSP8 max-P ≤ 24. *Attack:* the f,g pairs are already computed per-lane in the KB entries; the lemma is the assertion that one (f,g) template covers all three lanes with lane constants only. The Newton short-window theorem is the closest existing thing to the master statement and the natural base case.

**U2 — The Chart-Carried Descent Master Lemma (fibration principle).** *Shape:* Let X carry a chart atlas C compatible with the dyadic quotient tower, and let P be a chart-local, first-match-assigned payment. Then P descends exactly — charts factor onto quotient charts (SUCCESSOR-A form), the tail collapse Σ_t C(m,t) = m+1 holds per chart, and Σ_χ P_χ(X) ≤ Σ_{χ'} P_{χ'}(π(X)) uniformly over scales; no row-uniform strengthening holds (witnessed false by #124). *Existing instances:* K4, SUCCESSOR-A, Lemma COL, qa22 M ≤ t, wave-3 exact-scale injection, scalar_descent, WCL (2,6), PR #1013 prefix fibers — and his `first_match_partition`/`firstocc_*` Lean kernels are the object-agnostic combinatorial core, so the unification is directly formalizable. *Open reds as instances:* l1 mixed-petal (mixed chart transport), the 13 chambers (subgroup-lattice descent), xr_highcore (atlas descent). *Attack:* state P axiomatically (chart-locality + first-match + tail-collapse hypothesis), prove the master injection once, re-derive K4/COL/qa22 as corollaries; then l1-mixed is "verify the axioms for mixed charts" instead of a new proof.

**U3 — The Pay-Structured-First Lemma (Sidon-cell ordering + exchange compression).** *Shape:* For each binding row, every witness aggregate splits as (structured mass) + (quasi-random remainder), where the structured mass is paid by an exact ledger (staircase / first-match / extended window at W_ext = L + O(1) after gating a finite accident class), and the remainder lies in a Sidon-type cell — pairwise differences essentially non-repeating — on which inverse arguments are void but the exchange-compressed major/minor-arc payment is valid and sharp. *Existing evidence:* his def:effective-major-minor + the line-2210 ordering principle; our null-suppression datum forcing the exchange-compressed Q-atom form; the c1r3/WCL-ZONE finite-defect repricings (the "gate accidents, extend window" clause); the W3 sup/mean 1.263-bit gap as the price of skipping the strip. *Open reds as instances:* `f3_h3_dsp8_correlation_bound` (restructure to strip a Sidon cell FIRST — his lemma predicts the current direct correlation route stalls exactly because the strip is missing), `ww_row_envelope_clause`, the summit W3 envelope, c2pp's joint reserve. *Attack:* cheapest test is the dsp8 F-round harness with a Sidon-strip preprocessing step; if the residual correlation bound tightens toward ≤ 24, U3 is live and the same restructure is owed at ww and the summit.

---

## 4. ANTI-PATTERNS (fenced; do not re-spend)

Mathematical, ranked by blood spilled:
1. **Row-uniform / word-uniform descent without charts** — killed twice (#124 ×786; O(n'²) conjecture false). Always chart-carried (U2's negative clause).
2. **Truncated fixed-allowance ledgers** — killed C1'-r2 (the allowance itself was wrong). Any dli-shaped pose starts gated + extended (U3's positive clause is the only repair that has worked).
3. **Fixed-moment routes at binding rows** — his moment-order floor (r ≥ 641,593 / 680,397) closes the lane wholesale; import as a route-cut node before anyone spends on it.
4. **Single-poly Stepanov at sharp allowances** — proved nodal barrier (2^{5/3} can never fit G=4K); multi-poly or target/richness/signed-disjointness routes only.
5. **Inverse/additive-combinatorics arguments before a Sidon strip** — his line-2210 principle plus our null-suppression datum; the current dsp8 pose violates it.
6. **Per-cell ledger forms and mean-form envelopes where sup-form is load-bearing** — #119, #102, the 1.263-bit G1 catch; pose at composition level.
7. **Fixed-M / fixed-j / σ=1 uniformities that leak across scales** — SOL_TARGET_1/2, the Linnik-prime refutation; quantify over all dyadic scales or pin the level.
8. **n^6-capped atlas demands at fiber-rich cells** (#144 — wrong DEMAND; price column-relative), **pigeonhole "axis R" unification** (overlaps negative at all six candidates, zero proved instances ever), **jointness-by-aggregation** (falsified on three machine-checked grounds), **the dihedral route at rate-half** (killed and independently re-confirmed), **NG-ZERO "small-h is done" framings** (corridor-infinite, not consumable).

Process, with perfect recurrence: existence/label claims without artifact audit (ten-plus catches); status flips without Modal execution re-pin (hash-green ≠ execution-green); in-place statement rewrites; auto_discharge packaging sweeps; falsifiers not shown satisfiable at pose time (#101, #126); and importing conditional closures riding a refuted residual (the pma_wide guard — fired repeatedly in waves 8–10).

---

**Top five poses if only five are made** (intersection of Section 1 transfers with kill-or-close value): (1) DSP8 max-P ≤ 24 satellite via template norm machinery — with the U3 Sidon-strip restructure folded in; (2) moment-order floor imported as a DAG route-cut node (free deletion from the red surface); (3) the 10 WCL-ZONE-ext slots compiled in his window format (third PR after #1010/#1013, follows the proven n=2^41 fusion precedent); (4) chart-carried mixed-petal descent at l1 via U2; (5) summit weight-≤4 ambient-exclusion census via U1. Plus the cheapest live falsification against ourselves: the identity-prefix stress test of our B_ap columns with his exact deployed unsafe rows — the one audit with real kill potential against our own assembly.
