LENS conservative: verdict=Best-grounded draft I could have hoped to audit: the large majority of its quantitative claims verify exactly against the repos (Job A 46.44%/1,066,688 rows, 94,652,815 orbits, 14.53% of the 2^21 reserve, 1.401644/2.85x, 48,544-row DSP8 census, 13 chambers, 19/22 x4 reqs, 12/22 dli reds, catches #99–#101/#126, seven triaged band mechanisms, 4.73–4.83 bits, ~2^-5.2/row, the octave-31 4-row amendment, the KB #107 lightweight-cert fix, the Norm(u) weight-4/order-1024 gap, the (F1)–(F4)/six-input checklist, four_row_exact_completion_compiler_v1.json), and its four structural ideas (WW sweep first, unit-ideal pivot, dli cap, DSP8 satellite) are the strongest on the table. But it cannot ship as-is: one fatal evidence-pricing error at its Phase-0 keystone (the 27.0/60.6-bit WW headroom is flagged non-spendable within-column room; the binding aggregate margin is 0.9 bits), two misread math targets (the {2^39,2^39+1} seam sits at/above 2^167, not inside the already-unconditional 2^128–2^167 window; the 'pinned unique cubic candidate' is a completed exclusion, not an open pilot), a stale outward-facing block overtaken by the 07-21 queue walk (pruned-Q already filed as PR #1019; the μ8⊂F17 correction already repaired upstream and ruled dead; #1010/#1013 opened today, not past triage; a WCL-slot PR contradicts the banked different-lane verdict), a compute-custody miscast (Job A as 'Codex autonomous' against CR-004's no-authorization status and the Decision-5 time rule), and several garbled attributions (M31 route-cut contents, hge4 'highest fan-in', the F2 4.4 action list). Recommend: keep the skeleton and the four ideas, apply the corrections above, and re-sync every upstream-facing item to the KB #115–#118 state before this reaches the maintainer.; fatal/material issues: [fatal] Phase 0.1 / D0: ww_row_envelope_clause has "27.0/60.6+ bits of headroom" which "price survival as likely" for the WW cell sweep. -> State the true margins: aggregate line 0.9 bits (conservative convention); within-column room 27.0/60.6+ bits exists but requires a #493/QA.22 conventions ruling to spend. The sweep is still the right first move, but as a genuine coin-flip information purchase, not a likely-survival formality. | [material] ww_row_envelope_clause "is the only red that has never absorbed a falsification round." -> The precise true statement: the DIRECT re-posed form's pre-registered falsifier (the wider official-convention cell sweep, bg <= 1, generated-field normalization) is QUEUED and has never been executed — 'The existing F7 data support the qualitative q^-sigma model but do not certify an official sup bound' (attack.md). Say that instead. | [material] Phase 1.3: "the {2^39, 2^39+1} budget seam on 2^128 < q < 2^167"; timeline: "MCA safe side determined to 2^167 with the seam paid." -> Re-scope 1.3: the seam extends the determined window from 2^167 to ~(2^39+2)*2^128 and reduces the leaf to the beyond-2^167 bracket; the sub-2^167 window is closed and should be claimed as banked, not as Phase-1 work product. | [material] Phase 1.4: pilot 'the one chamber with a pinned unique cubic candidate (7 | 5·2^41+1 at the maximal row)' to validate a unified v2(q-1) >= 41 emptiness gate. -> Delete the cubic pilot. If a Phase-1 pilot is wanted here, pick one of the 13 real chambers (e.g. a pendant scroll chamber) under the e=1/e=2 field collapse, and validate the WCL-template port there. | [material] Phase 0.5 + 2.3: chase #1010/#1013 ('his triage pattern is ~24h; both are past it'); then file the WCL-slot compiler as PR #3, submit the pruned-Q toy packet, and surface the μ8 ⊂ F_17 near-rational counterexample. -> Rebuild 2.3 from the live state: three PRs in flight (#1010, #1013, #1019); no chasing before the 24–48h window; drop the μ8 item entirely; keep the WCL-ZONE-ext slot certificates as an INTERNAL Decision-6 hardening deliverable only (no upstream PR). | [material] Phase 1.1: 'Resume Job A… $45–60 to finish… Who: Codex autonomous runs.' -> Route Job A explicitly as a maintainer spend-approval line item (or contributor handoff), executed only after the two named repairs land; state the price as an unverified estimate; never assign launch authority to Codex. | [material] Phase 3.2: 'translate the M31 route-cut library (#999–#1008) — ADE wall, Chebyshev separator, Plücker defect window, rank-46 Forney frame, padding bridge… the one active duplication-risk zone; vendoring converts a race into a merge.' -> Keep the vendoring idea but name the objects correctly (ADE / shortened-flat / Chebyshev / whole-ball / parity-face / arbitrary-packet / rank-46 source-key), cite #984–#1008, drop padding-bridge from the 'library', and justify vendoring as reuse, not race-avoidance. | [material] Timeline: the partial-credit fallback 'is itself 42+ bits past the best upstream partial-credit position.' -> Delete the 42+ bits claim. The honest fallback statement: list grand closed + rate-1/2 MCA determination to 2^167 (≈0.5 bit past his all-rates window) + the strongest per-row replay/audit record — position on audit depth, not bit-lead. | [material] Phase 4.4 (F2): 'summit weight-≤4 ambient-exclusion census (U1 transfer, never posed)', 'signed-cancellation probes for his shallow-Fourier range', 'entropy demotion', 'our no-go #9'. -> Either ground each 4.4 item in a citable F2-campaign artifact (F2_FLIP_GOAL ladder L1–L5, the 12 no-gos by content) or present them as new proposals, not as established named leads.; strongest ideas: Phase 0.1 as written minus the headroom claim: run the queued, pre-registered WW official-convention cell sweep (bg <= 1, generated-field normalization) FIRST. Verified: ww_row_envelope_clause req-feeds both grands, its direct sup-form falsifier has genuinely never been executed ('The existing F7 data… do not certify an official sup bound'), and the 1.263-bit mean/sup gap + 2.417x layout-sup datum make this the cheapest decisive information on the board. The falsification-before-budgeting sequencing (know before committing months downstream) is exactly right — it just must be priced at the true 0.9-bit aggregate margin. ; Phase 2.1, the unit-ideal certificate pivot: attack the (1,5) three-variable ideal certificate to retire the census model for ALL remaining WCL slots. This matches CR-004's own preferred route verbatim ('compute and factor four compact integer certificates, beginning with the three-variable (1,5) ideal'; (2,7) census is an explicit NO-GO), and the draft's three gates on contributor compute — especially requiring the Norm(u)-saturation weight-4/order-1024 soundness gap fixed and audited before any $2k+ census — are verified against wclp_findings item 9 and are exactly the right conservative discipline. ; The D2 dli portfolio cap: hard-cap dli's share of any phase and freeze dli spend on ANY demotion event while drafting the B-WEAK-direct re-pose from surviving mechanism data. Verified structure: 12 of the 22 reds and the 5 pre-registered demotion criteria all sit behind the single dli_prime_weighted_large_block_support req edge into x4_exactlist_staircase_split (19/22 reqs proved) — concentration risk this plan is the only lens to hedge explicitly. ; Phase 1.5, the DSP8 max-P <= 24 satellite as the top cheap cascade: verified end-to-end — P=20 saturation over the exact 48,544-row F-round-1 census, dsp8r1_findings names 'a proved P <= 24 at official aspect satellite would close', and the DAG confirms the exact 3-amber single-req chain (mobius_excess_half -> three_to_one_c36 -> direct_floor_conditional_close) plus the feed into u1_x4_direct_column_budget (one of x4's three open reqs). The satisfiable-falsifier-at-pose-time guard correctly encodes the #101/#126 lessons.
LENS transfer: verdict=Keep this draft as a leading candidate: it is the most factually disciplined of the drafts I would expect at this density — of ~60 checkable claims (node ids/statuses, the 22-red surface, 10-slot WCL inventory, 19/22 x4 reqs, A2's +195-node/zero-close meta-datum, catch and KB attributions, the {2³⁹, 2³⁹+1} sliver, 7|5·2⁴¹+1, w8-C9's 0.5005%, F2 parity-theatre parameters, the six-input checklist items 2/6, firstocc kernels, moment-order floors) essentially all verified exactly against dag.json, the KB log, node packets, and upstream 18cfc199. It has one disqualifying-if-kept error — the near-rational μ₈⊂F₁₇ counterexample post in 0.4, which the survey of record explicitly retired (repaired upstream, on the DON'T list) and which would mislead the maintainer — plus two material staleness/discipline fixes (the pruned-Q packet is already submitted as PR #1019 with three PRs now in flight, so Phase 0/T4.2 sequencing must be rebuilt; the c2pp bulk≡1 pose must carry the honest 1.0662 worst case or it is born falsified). With those repairs and the six minor label/number corrections applied, the four structural ideas (RK-hedge parallel funding, the U1 broadside with D1 adjudication, the U3 joint-brief wiring onto grande_finale's own Sidon cell, and the WCL certificate conversion) are verified sound and should anchor the final roadmap.; fatal/material issues: [fatal] 0.4: 'post the near-rational μ₈ ⊂ F₁₇ support-wise counterexample against his v13.2 dichotomy corollary — obligatory under correspondence hygiene, and it buys the standing we spend in Phase 4' -> Delete the action entirely, or replace with: 'confirm the upstream near-rational repair covers our REFUTED node v13_2_near_rational_supportwise_payment and record the crosswalk in the correspondence ledger (internal only; F-C says dead as a PR).' | [material] T4.2: 'The pruned-Q toy packet (his good-first-PR #1)… banked and ready. Submit immediately after #1010/#1013 triage clears (don't stack three PRs in his queue at once; the ~24h pattern supports one-per-cycle)' -> Rewrite T4.2 as a triage-watch item for PR #1019 (and renumber T4.1 as the natural FOURTH PR). Update the board preamble to three PRs in flight. | [material] T2.3 pose: 'clause-(ii) bulk is exactly 1 (empirically < 10⁻³ deviation) — structured mass = the exact coset column plus orbit-quantized accidents, remainder empty' -> Pose the identity with the honest worst case: bulk → 1 along octaves (1.0662 worst, monotone-decreasing slope 0.133→0.069), the residual priced by the accident column; keep the 2²¹ reserve framing (14.53% round-2 usage is correct).; strongest ideas: The RK hedge as the Phase-5 pivot is EXACTLY right and I verified it computationally: marking rigidity_kernel's two verified alt targets (gap1_noneq_mass, xr_clean_residual_any_gate) as satisfied prunes precisely 18 of the 22 red leaves, leaving exactly {l1_mixed_petal_amplification, ww_row_envelope_clause, rate_half_list_adjacent_crossing, rate_half_band_closure} — the draft's claimed win set to the node. Funding the RK hedge and the direct-floor surgeries (B-WEAK→12 dli leaves; the counts check out) in parallel with the main line is the best portfolio structure any draft has proposed; the final roadmap must keep it. ; The U1 broadside (Phase 1) with the T1.1 vacuous-close pose as its flagship: every parameter checks out (richness saturates at maxP=20 across all 48,544 F-round-1 rows; the 10K₂₅⁰+17K₂₅ᴬ aggregate and five-rectangle menu are verbatim from the node; the max-P≤24 satellite is confirmed never-posed), and the cascade is real — closing this one red flips the three-amber chain mobius_excess_half → three_to_one_c36 → direct_floor_conditional_close, each with a single req in-edge. Firing the only outright-closing technique at all its untried targets simultaneously, with the D1 land-count adjudicating whether U1 is a lemma or folklore, is the draft's core method and it survives fact-check. ; The U3 adjudication wiring (T2.1/T2.2 → joint upstream brief): I confirmed grande_finale.tex line ~2210 literally states the Sidon/Fourier-cell-first ordering ('the first-match ledger must contain a Sidon/Fourier cell before the primitive inverse argument begins') and six-input #3 is 'Sidon/Fourier payment or equivalent effective-image MI+MA proof' (def:effective-major-minor exists as claimed) — so the claim that our Sidon-strip restructure and his input-3 atom are the same object, making a joint brief the only recorded route onto the F2 wall from either tree, is verified. The design of T1.1's pre-strip harness as the cheap designed experiment that adjudicates U3 before any summit spend is genuinely good experimental economics. ; T1.4's certificate conversion of the WCL family: all supporting facts verified exactly — CR-004's four fixed unit-ideal endpoints starting at the three-variable (1,5) ideal, the Norm(u)=1+x+y+z weight-4/order-1024 saturation soundness gap as the one unfixed blocker, Job A's remaining 53.56% at $45–60 with the two known repairs (re-shard aggregation + ECM tail), and the $1,900–5,300 post-GMP/FLINT-gcd-swap figure for the big censuses. Converting 10 of 22 red leaves from census-shaped to certificate-shaped in one template, with the compute kept strictly as contributor fallback, is the widest single collapse on the board and correctly costed.
LENS insight: verdict=KEEP THE ARCHITECTURE, REPAIR THE SPINE'S EVIDENCE AND REFRESH PHASE 0. This is the best-grounded of the drafts I would expect at this altitude: an unusual fraction of its fine-grained citations verify exactly against the repo (catches #99/#100/#101/#102/#119/#126; KB #97's DSP8 satellite and P>=21 falsifier; the {2^39, 2^39+1} seam; 19/22 proved reqs into x4_exactlist_staircase_split; the five 2026-07-07 floor re-poses and their exact DAG prune paths; the $45-60 / NO-GO WCL sizings; the theorem-cap and 2-adic gate constants), and its four strongest ideas (terminal-risk-first, FD, U1-with-ceiling, self-killing governance) are genuinely load-bearing. But it has one fatal defect: the same-object spine's lead evidence claims REGIME_MAP "proves" row-sharp Q == rate_half_band_closure, when the proved identity is Q == F2's zero-prefix instance and the Q<->band_closure bridge was explicitly adjudicated ANALOGY-ONLY with fatal side and quantifier mismatches (WP5_RATEHALF_VERDICT: "structurally incapable, not merely unproven"). PAY survives this correction — arguably strengthened, since the F2 costume then carries the proved identification — but the maintainer must not receive the draft as written. Second-order repairs: Phase 0's upstream pipeline is stale (pruned-Q already submitted as PR #1019; the mu_8 post ruled dead; the WCL-window PR contradicts the surveys' no-consumer verdict), the 12-leaf dli walk-through omits c1r3, the ww "27.0/60.6+ bit headroom" is within-column room against a 0.9-bit aggregate line pending the #493 conventions ruling, the "(f,g) pairs already computed" claim presents U1's first real work item as done, and the "Codex autonomous, ~2 days on Modal" sweep sizing violates Decision 5's <5-minute time rule as written. All are fixable without touching the phase structure, the decision gates, or the ranked bets.; fatal/material issues: [fatal] "REGIME_MAP already *proves* his row-sharp Q atom ≡ our `rate_half_band_closure` (same object, disjoint regimes)" — presented as the strongest evidence for the same-object spine (b). -> Re-target the spine's evidence to what is actually proved: Q-atom ≡ F2 zero-prefix instance (Myerson identification chain, each link proved/machine-verified), with the growing-order gap as F2's open content; cite band_closure's relation to upstream only via the ev-grade crossing-fidelity datum and K5 (razor-band witness kernel) as the honest local object, per WP5. | [material] Phase 0 pipeline: "Submit the pruned-Q toy packet" and "Post the near-rational μ₈ ⊂ F₁₇ counterexample to his v13.2 dichotomy corollary"; effort split lists both in the upstream 15%. -> Replace with the actual outstanding pipeline: chase triage on #1010/#1013/#1019 (all three in flight) and hold the HGE4→SP crosswalk as lead-grade per §F-E; drop the μ₈ post entirely. | [material] "Ship the third PR: the 10 WCL-ZONE-ext slots compiled in his window-compiler format ... converts Decision 6's baseline from prose to certificates his machine ingests, ~3 days us." -> Reframe as internal work: close slots as compute lands (contributor channel per CR-004), and only propose upstream packaging if a future survey shows a six-input consumer; do not present format-compilation of open conjectures as 'certificates his machine ingests'. | [material] "a proof flips `dli_c2pp_joint_reserve` and, with the WCL certificates from Phase 0's third PR, walks the whole 12-leaf dli lane through its single edge into `x4_exactlist_staircase_split`." -> State the walk correctly: FD-at-c2pp + WCL slot closures reduce the lane to c1r3 (or to a direct B-WEAK proof, which the draft elsewhere correctly identifies as the full 12-leaf prune); add c1r3's own close (round-3 census per D2, or a proof lead off the accident-row worked example) as an explicit remaining leg. | [material] "First proof attempt: ww ... 27.0/60.6+ bit headroom is the most room any costume offers." -> Quote both numbers: 0.9-bit aggregate margin pending the #493 conventions ruling, with 27.0/60.6+ bits of within-column room accessible only via a maintainer conventions decision; the ww-first argument should rest on the queued pre-registered falsifier being unrun (true) rather than on headroom. | [material] "The (f,g) pairs are already computed per lane in KB #73–117" (U1's per-lane divisibility-vs-norm-size data presented as banked). -> Rewrite as: 'the ingredient facts (gates, realized valuations, norm bounds) are banked across the WCL/HGE4/C36 packets; step one of U1 is to EXTRACT the per-lane (f,g) table from them' — and drop the KB #73–117 citation. | [material] "WW F-round 1 cell sweep ... Codex autonomous on Modal, time-rule sized, ~2 days." -> Either (a) decompose into <5-min route-deciding screens with banked per-shard timings, or (b) route the full official-convention cell sweep through the contributor-request/maintainer-authorization channel (as the draft correctly does for the non-poly census and CR-004). | [material] "His first-match atlas residual is converging on the same cells as `xr_lowcore_spread_heart`" and "His input 3 ≡ our `f2_growing_order_myerson`" (completing 'three of his six GF-v3 inputs'). -> Demote both to bridge conjectures with the written-bridge checklist attached; the honest count is ONE proved identification (Q-atom ↔ F2 zero-prefix), one strong lead (input 3 / Sidon-Fourier wall), one speculative cell-level correspondence (atlas ↔ XR).; strongest ideas: The falsification-asymmetry inversion — probe terminal risk FIRST. Promoting the non-polynomial-word hunt at the K5 bracket (the flank KB #39 itself names open) and the identity-prefix/B_ap audit-debt (UPSTREAM_HARVEST B.2, still undischarged) from 'someday' to Phase 0, with D0 as a full-stop tripwire, is the single best scheduling idea in the draft: both are cheap, both have genuine kill potential against our own assembly, and either outcome pays before weeks are invested downstream. The final roadmap must keep this inversion (with the census re-scoped as a one-sided kill probe). ; Naming the Finite-Defect Principle (FD) and grounding it in three independently verified revivals — every ingredient checks out against the repo: the c1r3 accident row 918552577 repricing 3.000058 → 1.333359 under its w6 orbit (c1r3b report), the WCL-ZONE emptiness-vs-counting threshold at exactly L ≥ 13 (wz_findings), and the matching-truncation majorant — with c2pp's round-2 'bulk ≈ 1 + exact coset column + orbit-quantized accidents, 14.53% of the 2^21 reserve' (c2r2 report) as the first testable instance. Posing the bulk-identity lemma from the banked round-2 mechanism data is a concrete, cheap first theorem-step, and FD is precisely the missing composition clause that catches #102/#119 (the two recorded ledger-composition failures) say any per-scale ledger needs. This is the draft's ranked bet 1 and deserves to be. ; U1 as a master 2-adic exclusion lemma WITH an honestly stated ceiling. The gate-vs-realized-valuation pattern is real (gate v₂(q−1)≥41 vs realized max v₂ = 18–29 across the WCL/HGE4/C36 exclusions; Newton short-window as proved base case), the theorem-cap numbers are quoted exactly (log₂ C(127,64) = 123.17 vs 127.90+, the 4.73–4.83-bit deficit), and the deliverable test (derive DSP8 max-P ≤ 24 from the master statement, falsifier 'any official row with P ≥ 21' pre-registered) matches KB #97's own named natural route. 'U1 is a broom, not a key' — one citable theorem replacing ~15 bespoke gate wins, explicitly barred from claiming the quasi-random core — is exactly the right shape and honesty level. ; Pre-registered self-kill governance for the insight lens itself: every bet carries a kill line and a fallback into the grind lane, RK is 'priced by attack, not assumed' (one dedicated refutation round; never build on it either way — the board simplifies under both outcomes), and D3 makes the whole insight bet falsifiable ('zero red closures by week 14 → the bet has failed its own falsifier; revert to grind ordering and record it in loop memory'). This turns the roadmap from a thesis into an experiment, which is the house's own doctrine applied to planning.
LENS convergence: verdict=Sound spine, stale outward edge. The draft's internal-board factology is remarkably accurate — I digit-checked and confirmed the six-input checklist verbatim (agents.md + thm:v3-package five clauses), 208/33/22 with Decision 6's exact structure (c1r3 + c2pp + 10 WCL slots = 12 lane leaves), the 4.73–4.83-bit deficit, ww's 1.263-bit sup/mean gap, the {2^39, 2^39+1} residual set, 13 chambers (9 lines + 4 scrolls), 7 | 5·2^41+1, catches #99/#100, the DSP8 max-P≤24/P≥21 satellite, the 7000n·max(1,B_h) adapter, WCL costs ($45–60 / $1.9k–5.3k optimized), c1r3's 4 octave-31 compute-law amendment, ε_mca > 2^−86 (42 bits), towards-prize §8 item 6 as the M5 ask, moment floors 641,593/680,397, and the five demotion criteria. But the upstream-facing half of Phases 0–1 was drafted against a board that moved on 2026-07-21 and against banked survey verdicts it does not cite: the pruned-Q packet is already PR #1019, the near-rational correction is dead (repaired in his tree — posting it is the one recommendation that would actively mislead the maintainer), the PR-4/5/6 pipeline contradicts the upr2 DEAD/different-lane rulings and rests on a 'fusion precedent' that never existed as a PR, and the M31 vendor list garbles 3 of its 5 named objects. Fix list for the final roadmap: rebuild Phase 0 against KB #115–#118 (three PRs in flight, queue empty, correction already posted), make every upstream-facing Phase-1 item either survey-consistent or explicitly a verdict-overturn proposal, correct the 2.4·10⁷ figure to the sourced ~1.66M/2.09M bits, fix the vendor inventory from the agents-log wave entry, and soften 'provably the same' to regime-complementarity. The lens itself — joint ledger, pay-the-wall-once, input-6 co-signature, vendor-before-census, RK as insurance — survives fact-checking and is worth keeping.; fatal/material issues: [fatal] P0.3: 'Post the near-rational μ₈ ⊂ F₁₇ counterexample — a live correction to his promoted cap authority (v13.2 dichotomy corollary)... the cheapest standing-purchase available.' -> Delete P0.3. If the roadmap wants a correction-economy item, the only live one on the books is the already-posted #750 note (KB #115, issuecomment-5032130941) — cite it as completed standing, not as pending work. | [material] P0.2: 'Submit the pruned-Q packet (us; 1–2 days)... This is PR-3' — and Phase 4 risk 1: '≤ 2 PRs in flight'; entry state 'PRs #1010, #1013 open'. -> Recast P0.2 as 'DONE (PR #1019, KB #118); watch triage', renumber the PR pipeline, and reconcile the ≤2-in-flight rule with the actual three-PR state (the survey's recorded pacing rule is 'one audit-genre PR in flight at a time'). | [material] Joint-ledger table row 1: 'we hold the below-balance approach (4.8-bit deficit vs his 2.4·10⁷-bit shortfall)'. -> Replace with the sourced figures: 'his packing-ceiling overhead is ~1.66M bits against ~22-bit margins (prop:proper-q-gap); his proved head-depth theorems sit Δ ≈ +2.09M bits above balance (REGIME_MAP)'. | [material] P1.1: 'Translate #999–#1008 (ADE wall, Chebyshev separator, Plücker defect window, rank-46 Forney frame, padding bridge) into our chart/atlas coordinates.' -> Vendor list should read: ADE cut, shortened-flat, Chebyshev separator, whole-ball, parity-face, arbitrary-packet, rank-46 source-key walls (PRs #984–#1008 wave); list padding bridge / common-core add-back / rank-two coloop / rooted-shell as his declared UNRESOLVED remainder — i.e., the seam where our XR work is additive. | [material] P1.3/P1.4/P1.5 PR pipeline: WCL-ZONE-ext slots 'compiled in his window-compiler format... following the proven n=2⁴¹ fusion precedent', M5 rank-ledger packet, f1 skeleton — presented as certificates 'his triage machine natively ingests'. -> Either drop P1.3–P1.5 as upstream PRs or state them honestly as NEW survey work that must first overturn the recorded DEAD/different-lane verdicts; delete the 'fusion precedent' justification; if WCL slots are exported, the honest channel is his no-go/certificate genre, not the window compiler. | [material] P0.1: '#1010 and #1013... His pattern is ~24h triage; both have sat. Escalation rule: if either is untriaged at 72h... PRs framed as checklist reductions get triaged, PRs framed in our node-ids do not.' -> Rewrite P0.1 as a watch item ('opened 07-21; watch triage against his ~24h integration cadence'), drop the sat/72h escalation premise or re-key it to actual elapsed time, and mark the framing rule as a hypothesis, not a fact. | [material] Phase 4: 'his ev-integration commits state no local replay is ever run; our replays are the only verification his structural theorems get' (basis for 'Neither side can produce this alone'). -> State it precisely: 'his integration waves are imported without local replay, and no independent adversarial replay lane exists in his tree; ours is the only such lane' — still a strong bilaterality argument, without the falsifiable absolute.; strongest ideas: The joint-ledger table with checklist input 6 as the structural merge point: 'one summed integer certificate per adjacent deployed row, emitted by his compiler, replayed digit-exact by us, co-signed' — plus the P0.5 grammar crosswalk (our packaging emissions vs his four_row_exact_completion_compiler_v1.json, which verifiably exists) and the double-keyed status-flip rule (hash-green ≠ execution-green). This is the one place bilaterality is real and verifiable, and it aligns with the standing KB #115 items (roadmap divergence, KB #107 harness fix). ; Vendor-before-census as the standing anti-duplication law for the XR zone (P1.1) and its generalization ('any upstream wave landing within one edit-distance of an open red of ours triggers vendor-before-census, never race') — the M31 wave overlap is real (#984–#1008 integrated, lanes now tree-owned per KB #116), and this rule is the cheapest way the joint framing pays. ; P2.1 DSP8 via the banked closing satellite — verified exactly against KB ('max P ≤ 24 at official aspect would make DSP8 vacuously TRUE and CLOSE THE C36 AMBER; falsifier = any official row with P ≥ 21; natural route = the audited collision-norm/coupled-ideal template'), with the verified 3-amber cascade (mobius_excess_half → three_to_one_c36 → direct_floor_conditional_close). The best-verified near-term move in the draft, correctly wired to the U3 Sidon-strip ordering probe (grande_finale's first-match order at ~line 2210 is real). ; The D2 RK-fork insurance posture: on a dli demotion, pivot to the recorded prune set rather than re-posing a fourth envelope — the wiring checks out (rigidity_kernel carries real alt edges into gap1_noneq_mass and xr_clean_residual_any_gate; a direct B-WEAK proof does prune all 12 dli lane leaves, which are exactly c1r3 + c2pp + 10 WCL slots per Decision 6), and 'RK is the insurance, not the plan' is the right claim-discipline framing for a schema-broad CONJECTURE.