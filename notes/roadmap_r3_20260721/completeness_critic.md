# D5 COMPLETENESS CRITIQUE — what all four drafts miss

Caveat: all four drafts arrived truncated mid-phase; items below are absent from every *visible* portion. The synthesis must contain each explicitly (or confirm a draft tail covers it).

## A. Open reds and residuals no draft concretely attacks

1. **`l1_mixed_petal_amplification` has no plan anywhere.** It is in the irreducible core under EVERY wiring (including the RK world), is a wired bottleneck for BOTH grands, and is the only core leaf with zero movement for three waves — yet no visible draft schedules even the cheap pre-registered balanced-strip growth-law measurement, the branch-(4) constructive first-match-supply lane, or the LS6 guarded atom. The synthesis must (a) schedule the growth-law round early (it is cheap and arms either a falsifier or a floor-band-emptiness license), (b) name l1 as the program's true critical path, and (c) give an escalation rule if waves 19+ also show zero movement (weight shift to clause-(P)/floor-band-emptiness, or formal pricing of the RK alternative).
2. **`xr_tangent_support_mismatch_bridge`** — graded *tractable*, depth fully paid, residual is pure bounded-depth branch width within 16n³ — and no draft names an attack. Likely one of the cheapest genuine red closures on the board; must appear.
3. **`xr_lowcore_spread_heart` (P-B)** — the one proof-shaped-hard XR leaf. Its only named surface (`f2_effective_energy_dichotomy` transport) is unscheduled in all four. Synthesis must either schedule the transport attempt or explicitly declare P-B reachable only via the `xr_smallcore_spread_count` floor / RK bypass and wire the plan accordingly.
4. **WCL slots (4,9)–(4,11) and the extended slots (1,7)(1,8)(2,8)(2,9)**: never sized, never scanned — "minted by the window widening" with zero evidence, hence the register's most exposed falsifier surface. Only (1,5)–(1,8) get any visible scheduling (C1). A sizing pass plus minimal exposure scan must be scheduled.
5. **CR-004 unit-ideal certificate pilot on the three-variable (1,5) ideal.** The matrix states a land there "retires Job A's $50 AND converts every slot from census to certificate," yet C1 spends the $45–60 finishing the census and nobody attempts the certificate. Synthesis should run the pilot in parallel with (or before) the Job A finish.
6. **dli amber maintenance is unscheduled.** The lane flipped amber four days ago (Decision 6); its life depends on c1r3 round 3 (gated-L=2 lever — which *requires a compute-law amendment* for the octave-31 4-row census; a governance step no draft names) and c2pp round 3 (F-a t=4 + 40-row F-c, pre-registered). C1 promises falsifiers "on schedule" generically; nobody dates them or writes the Decision-6-reversal contingency (dli RED re-stalls the entire 12-leaf strip subtree behind x4).
7. **f3_h3 survivor-reserve contingency**: if the max-P ≤ 24 route dies (any row with P ≥ 21), fallback is the CR-001 (36,1) survivor row (73,602 resultants) whose reserve-first close *forces a re-wire*. No draft plans that re-wire.
8. **The (2,7) soundness gap**: Norm(u)-saturation's weight-4 exclusion is unpaid at order 1024. This is a soundness hole in an already-cited route, independent of the $ cost debate. No draft fixes or fences it.

## B. Risks no draft names

9. **Upstream staleness and spine-supersession.** Our view of upstream is frozen at 18cfc199 under a no-fetch rule; C4's entire joint ledger is keyed to GF **v3** — which followed v1 and v2, and can be superseded by v4. No draft has a re-sync cadence, or a hedge if Przemek re-promotes a different spine (stranding the crosswalk and the six-input mapping). Also: **third parties**. scottdhughes (#999–#1008) and latifkasuli are independent actors inside his repo; the race is not two-player, and "first-match atlas completion on an M31 row" can be closed by neither of us.
10. **The prize spec is PRELIMINARY, and nobody audits DAG-vs-spec fidelity.** The whole roadmap assumes dag `prize` ≡ the actual challenge. The spec memory explicitly lists open accounting no draft mentions: the m-fold **interleaved** Λ(C^{≡m}) constant-m transfer, challenge-field-vs-generated-field accounting, the concrete poly-exponent B so n^B ≤ 2^−128·|F|, and tightening η toward the 2^−9 cap. Synthesis must include (i) a one-time spec-to-DAG fidelity audit and (ii) a monitor for spec finalization changes at proximityprize.org.
11. **Maintainer-ceremony throughput.** The meta-datum says every status flip this window was a maintainer ceremony. If Phase 1 produces the program's first red closures in five waves, they will queue behind ratification. A batching/cadence rule is needed; no draft has one.
12. **Codex-branch divergence.** Wave-1 Codex integration generated catches #76–#89 — integration finds errors. All four drafts hand Codex "autonomous" workstreams with no integration-audit cadence, no conflict rule, and no statement of the forced-corrections-authority boundary (pins/constants direct; status flips surfaced).
13. **Compute governance as a whole**: no draft names a total Modal/dollar budget or its owner, the CR-004 external-request pathway (required for any (2,7)+ census even post-FLINT), or the checkpoint-GC rescue hygiene (the Pilot-A 8,300× incident) as a recurring obligation.
14. **Falsifier power audit.** "0 deaths across 21 attack families" is only evidence if the falsifiers had power: C36's F-round 1 survived *vacuously*, ww has *never* run a round, several ambers' rounds saturate trivially. Before ambers enter the dossier, the synthesis needs a survivorship-quality pass classifying every floor as tested-with-power / tested-vacuously / untested. No draft questions this.

## C. Boring but load-bearing

15. **The packaging tier is reduced to the grammar crosswalk in every draft that mentions it.** Missing: the total certified f(C) compiler build, clean-checkout replay CI, provenance chain, nonclaim-ledger emptying, a **freeze date**, syncing the `critical/nodes/` folder census (254 dirs) to the 1030-node DAG (clean-checkout replay will fail while folders lag), and a **harness-coverage census** — `xr_highcore_collision_count` is the *named* md-only import, but nobody sweeps for others. w10-C1 has already refused three auto-discharges; leaving the dossier last is the register's own named failure mode.
16. **Lean scoping.** C3 caps Lean at 10%, C4 uses `firstocc_*` kernels — but no draft aligns us with upstream's explicit four-item Lean priority list (verified in `rs-mca/agents.md`: GF end-to-end first, then cs25_cap_v13_2, then thresholds, then deployed certificates), audits our own `prize/formal/lean/rs_mca_formalization` package status, or decides what (if anything) our dossier will claim at Lean level. Since his thresholds formalization target consumes our n=2⁴¹ determinations, our export channel acquires a Lean-statement obligation nobody has scoped.
17. **Submission strategy.** The spec says partial results are allowed and awards "may be split among multiple submissions." No draft addresses: joint vs separate submission with Przemek, credit split, timing against his Paper-D pay-per-bit positioning (ε_mca > 2^−86 — he is already positioned for partial credit 42 bits above target), or what **our** partial-credit deliverable is if the two walls stand (the statistically likely outcome given four waves of zero closures). Synthesis must define a concrete fallback dossier: "conditional resolution modulo two named atoms, everything else certified," with a decision date for switching postures.
18. **Standing-rule reconciliation**: the F2 flip campaign's "resume every session until T-WIN or T-FLOOR" conflicts with every draft's demotion of F2 to background — resolve explicitly. The artifact-refresh rule (Claude artifact + publish_site.sh after every DAG update) and the still-open DLI-CLOSE-6 Pro thread and paused M5 packet (PR #172) need owners in the plan.
19. **A correction to the task's own debt list**: the two queued v13 replay checks (prefix rigidity, Veronese) are NOT open debt — the deferred-verification queue was emptied 5-for-5 on 2026-07-10 (prefix rigidity PROVED, Veronese confirmed at char 0, plus BC-curve, capf-planted, L4-interior). The synthesis must not resurrect them; actual open verification debt = KB #107 (covered), xr verify.py (covered), **w8-C9 AZC re-run obligation** (0.5005% margin — uncovered by all drafts), the order-1024 Norm(u) gap (item 8), and the harness census (item 15).

## D. The maintainer's big-picture questions

20. **"What do the recurring unexpected techniques MEAN"** — only C3 answers, and only for the 2-adic gate. The synthesis needs the full inventory (cyclotomic-norm gates, orbit repricing, exact staircases, vacuous-round saturation, extension-window widening, exact-integer replay) and the two-level interpretation: (i) official rows are *engineered* (v₂(q−1) ≥ 41), so every bounded-complexity witness class is arithmetically overdetermined — the recurrences are one phenomenon, and they predict exactly where transfer fails: the two walls are the loci where engineering gives no purchase; (ii) the second-order meaning of zero red closures despite portable techniques: everything in either tree is an *exhaustion* tool; no technique has ever *paid* a sharp constant. That distinction (sweeping vs paying) should be stated as the program's central open methodological problem.
21. **The missing creative leap: reframe progress as bracket width, not leaf count.** The grand challenges ask to **determine** δ*, not to prove our conjectured value. Paper D's cap bounds δ* from above; our floors bound it from below. Therefore the register's "terminal-risk event... no fallback recorded" for the non-polynomial-word hunt is wrong as stated: a floor demotion *relocates* δ* rather than ending the program — provided we can certify at the relocated value. The synthesis should (a) record this fallback explicitly (a refutation branch is a first-class prize deliverable, matching C3's refute-ourselves-first instinct but going further), and (b) adopt "bits of remaining δ*-bracket, jointly over both trees" as the roadmap's progress metric — the only metric under which four waves of fencing were not zero progress.
22. **No draft states an end-state forecast**: a probability-weighted view over {full resolution, one grand, clean-rate milestone, partial-credit conditional dossier, relocated-δ* determination} with a calendar decision point. The maintainer's question about what the pattern of surprises *means* deserves an answer with consequences for scheduling, and that forecast is the honest form of it.