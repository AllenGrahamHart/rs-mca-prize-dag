# Roadmap r3 — toward full resolution (2026-07-21)

**Basis.** Our tree at master 550f176b (dag.json, 1030 nodes, single source of truth); upstream frozen at 18cfc199 under the no-fetch rule. Supersedes roadmap r2. Merged from four lens drafts (conservative / transfer / insight / convergence) after fact-check; every claim below reflects the corrected state. Voice contract: exact numbers, honest evidence grades, falsifiers before budgets, no over-claim.

---

## 0. Executive picture — did recent developments change the big picture?

**Short answer: the two walls did not move; everything else got compressed into a finite, priced, falsifiable surface. That changes the plan from exploration to sequenced purchase — with terminal risk probed first.** Four pivotal shifts:

1. **Decision 6 flipped the dli lane amber.** `dli_prime_weighted_large_block_support` is now CONDITIONAL on C2″ + baseline(C1′-r3 + WCL-ZONE-ext with 10 finite slots). Consequence: 12 of the 22 open red leaves (c1r3 + c2pp + the 10 WCL slots) sit behind one single req edge into `x4_exactlist_staircase_split` (19/22 reqs proved). This is simultaneously the board's widest opportunity and its worst concentration risk; three of the five pre-registered demotion triggers live in this one lane.

2. **Upstream promoted Grande Finale v3 as his final-resolution spine**, with a six-input checklist, and his thresholds paper (cor:prize-window-compiler) consumed our n=2⁴¹ rate-half determination at all four rates. The export channel is proven, the merge point is identified (checklist input 6: one summed integer certificate per adjacent deployed row, emitted by his compiler, replayed digit-exact by us, co-signed), and three PRs are in flight (#1010 four-pair exact audit, #1013 quotient-cell prefix-fiber calibration, #1019 pruned-Q toy packet — all opened 07-21, watch triage against his ~24h cadence, no chasing inside 24–48h).

3. **The meta-datum:** four waves, +195 nodes, +709 edges, **zero red closures**. Reduction is exhausted; every remaining leaf is being paid at full price. All four lenses independently concluded the same thing: the program must now either transfer its proven closers to untried targets, or pay one shared object once — not grind 22 sieges.

4. **The rate-half safe side is determined to 2¹⁶⁷ and banked.** Corrected scope (this was misread in two drafts): the {2³⁹, 2³⁹+1} budget seam sits **at/above** 2¹⁶⁷, not inside the closed window. Closing the seam extends the determined window from 2¹⁶⁷ to ~(2³⁹+2)·2¹²⁸ and reduces `rate_half_band_closure` to the beyond-2¹⁶⁷ bracket alone. Terminal risk is now localized to one flank (non-polynomial received words, uncensused) — and per §5, even a demotion there *relocates* δ\* rather than ending the program.

What did **not** change: the deep Sidon/Fourier payment (his input 3; our lead `f2_growing_order_myerson`) and the balance line remain walls by both trees' own quantified admissions. Identification discipline (correcting the strongest over-claim in the drafts): exactly **one** cross-tree identification is proved — his row-sharp Q atom ≡ our F2 zero-prefix instance, via the Myerson identification chain, each link machine-verified. The Q ↔ `rate_half_band_closure` bridge was adjudicated **analogy-only** with fatal side and quantifier mismatches (WP5_RATEHALF_VERDICT: "structurally incapable, not merely unproven"); input 3 ↔ the F2 summit is a strong lead; atlas ↔ XR cells is speculative. One proved, one lead, one conjecture — that is the honest count.

---

## 1. The recurring-technique thesis

**The inventory of unexpected repeat performers:** (i) cyclotomic-norm / engineered-prime exclusions — ~15 instances across three unrelated lanes (dli/WCL, F3/HGE4, C36), the only family that outright closes; (ii) extended-window / orbit repricing — c1r3's killer row 918552577 repriced 3.000058 → 1.333359 under its w6 orbit, WCL-ZONE mass pricing only at L ≥ 13, the matching-truncation majorant; (iii) chart-carried descent true / row-uniform descent false, twice (×786 violation, catch #124); (iv) exact staircases (a_RH(q) = n − ⌊q/2¹²⁸⌋ + 1 proved); (v) vacuous-round saturation (DSP8 richness saturating at P = 20 across all 48,544 census rows); (vi) exact-integer replay as the only trust protocol that composes across two repos.

**What the recurrence means, at two levels.**

*Level one — the recurrences are one phenomenon.* Official rows are engineered: v₂(q−1) ≥ 41 makes every bounded-complexity witness class arithmetically overdetermined (realized max v₂ = 18–29 against a 41 gate; 12–23 doubling levels of slack everywhere measured). Norm gates, staircases, repricings, and vacuous saturations are all the same fact — the finite/structured part of both problems is systematically exhaustible — and this predicts exactly where transfer fails: the two walls are the loci where engineering gives no purchase (the quasi-random core; the null-suppression datum says the extremal witnesses are quasi-random, so inverse tools point the wrong way at exactly the binding rows).

*Level two — sweeping vs paying.* Every technique in either tree, ours and his, is an **exhaustion** tool. No technique on the board has ever *paid* a sharp constant. Four waves of zero red closures despite portable weapons is the corollary. This distinction — sweeping the structured part vs paying the quasi-random remainder — is the program's central open methodological problem, and the three lemma candidates below are the three legs of the only argument shape that addresses it: exhaust structure, carry the fibration exactly, pay the remainder globally at the correct window.

**Candidate unifying lemmas, stated attackably.**

**U1 — Official-Row Low-Complexity Emptiness Lemma.** *Shape:* for official q (v₂(q−1) ≥ 41 or lane norm-gate), any lane-object witness of complexity ≤ w yields u ≠ 0 in Z[ζ_m] with 2^{f(w)} | Norm(u) and |Norm(u)| ≤ g(w,m); a lane-uniform w₀ exists with 2^{f(w)} > g for all w ≤ w₀, survivors slightly above w₀ exhausted by engineered-prime census. Base case: the Newton short-window theorem. *First work item (corrected):* the (f,g) pairs are **not** already tabulated — the ingredient facts (gates, realized valuations, norm bounds) are banked across the WCL/HGE4/C36 packets; step one is to extract the per-lane (f,g) table from them. *Deliverable test:* derive DSP8 max-P ≤ 24 from the master statement, falsifier pre-registered (any official row with P ≥ 21). *Ceiling, stated honestly:* U1 is a broom, not a key — the theorem cap (log₂ C(127,64) = 123.17 vs need 127.90+, a 4.73–4.83-bit deficit across all 2,978,146 band cells) proves the quasi-random core is not bounded-complexity; U1 never touches the bracket or the summit.

**U2 — Chart-Carried Descent Master Lemma.** *Shape:* for a chart atlas compatible with the dyadic quotient tower and a chart-local first-match payment P: charts factor onto quotient charts (SUCCESSOR-A form), tail collapse Σ_t C(m,t) = m+1 per chart, Σ_χ P_χ(X) ≤ Σ_{χ′} P_{χ′}(π(X)) over all dyadic scales; **no row-uniform strengthening** (witnessed false, #124). *Attack:* axiomatize P, prove the master injection once on his object-agnostic `firstocc_*` Lean kernels, re-derive K4 / Lemma COL / qa22 as corollaries; `l1_mixed_petal_amplification` branch (4) then becomes "verify the axioms for mixed charts" — the only route respecting the proved F₂₃ cross-quotient obstruction (which kills exactly the naive, chartless form).

**U3 — Pay-Structured-First (Sidon-cell ordering + exchange compression), with FD as its composition clause.** *Shape:* per binding row, witness aggregates split as structured mass (paid exactly by staircase/first-match/extended window W_ext = L+O(1) after gating a finite accident class) + remainder confined to a Sidon-type cell where inverse arguments are void but the exchange-compressed major/minor payment is valid and sharp. *Evidence:* grande_finale.tex ~line 2210 states the Sidon/Fourier-cell-first ordering literally; his def:effective-major-minor exists; our null-suppression datum forced the exchange-compressed Q-atom form. *The Finite-Defect clause (FD, named here):* every refuted truncated ledger seen so far differs from a true extended ledger by finitely many gateable, orbit-quantized accidents (the three revivals above) — FD is precisely the missing composition property that catches #102/#119 say per-scale ledgers need. *First testable instance, posed honestly:* the c2pp bulk identity — bulk → 1 along octaves (worst case 1.0662, monotone-decreasing slope 0.133 → 0.069), residual priced by the exact coset column plus orbit-quantized accidents, within X(R) ≤ 2²¹·A(R) (round 2 used 14.53% of the 2²¹ reserve). *Cheapest adjudication:* the DSP8 harness with a Sidon-strip preprocessing step — if the post-strip residual tightens toward ≤ 24, U3 is live and the same restructure is owed at ww, the summit, and (jointly) his input 3.

---

## 2. The board

**Counts (do not mix scopes).** Critical surface: 208 PROVED / 33 CONDITIONAL / 22 open red leaves. Req-closure of `prize`: 279 nodes = 221/35/23, the 23rd target being `submission_quality_paper_dossier` (RIPE-staged artifact). `critical/nodes/` folder census: 254 dirs — lags the DAG; the DAG is authority (sync scheduled, §3 endgame).

**Wired bottlenecks — no alt of any kind, no upstream substitute:** `l1_mixed_petal_amplification`, `ww_row_envelope_clause`, `rate_half_list_adjacent_crossing`, `rate_half_band_closure`, plus the dossier. There is no MCA-only resolution: the F1 pole pricing imports the base-row list threshold, so the list-side leaves are load-bearing for `mca_grand` too. **`l1_mixed_petal_amplification` is the program's true critical path**: in the irreducible core under every wiring including the RK world, wired into both grands, and the only core leaf with zero movement for three waves.

**Minimal win sets.** Unconditional: all 22 reds + dossier (the surface is pure AND). Sub-goals: `list_grand` = 3 leaves {l1, ww, `rate_half_list_adjacent_crossing`}, flips 8 ambers; the 20-leaf clean-rate milestone remains plausible-but-unaudited pending the Conflict-4 rate-scope pin on `ext_lift ← f1_pole_list_threshold_location ← list_adjacency_closing`. Route surgery: marking `rigidity_kernel`'s two verified alt targets (`gap1_noneq_mass`, `xr_clean_residual_any_gate`) satisfied prunes **exactly 18 of 22** leaves, leaving precisely the four wired bottlenecks — computationally verified. Direct floor proofs prune: B-WEAK → 12 (all dli lane leaves = c1r3 + c2pp + 10 WCL slots per Decision 6), `u1_x4_direct_column_budget` → 2, `xr_smallcore_spread_count` → 2, `u2c_giant_tnull_dichotomy` → 1, `worst_word_challenger_pricing` → 1.

**The ww evidence state, priced correctly (this correction gates Phase 0).** `ww_row_envelope_clause` is sup-form (mean forms dead: adversarial layout-sup 2.417× median = 1.263 bits over the line; q^σ normalization refuted by the 24/24 census). The **binding aggregate margin is 0.9 bits** under the conservative convention; the 27.0/60.6+ bits of within-column room exist but are spendable only via a #493/QA.22 conventions ruling (decision queue item). The precise falsification status: the direct re-posed form's pre-registered falsifier — the wider official-convention cell sweep, bg ≤ 1, generated-field normalization — is **queued and has never been executed**; per attack.md, "The existing F7 data support the qualitative q^-sigma model but do not certify an official sup bound." The sweep is therefore a genuine coin-flip information purchase, not a likely-survival formality — and still the right first move.

**Balance-line bilaterality, sourced.** His packing-ceiling overhead is ~1.66M bits against ~22-bit margins (prop:proper-q-gap); his proved head-depth theorems sit Δ ≈ +2.09M bits above balance (REGIME_MAP). Our theorem cap is 4.73–4.83 bits short from below. Regime-complementary, zero overlap, zero contradiction — but "same object" is proved only at the Q-atom ↔ F2 zero-prefix link (§0).

**Risk register (pre-registered triggers; all live).**

| Trigger | Fires when | Consequence |
|---|---|---|
| c1r3 amber-2 watch | Round-3 census (gated-L=2 lever; octave-31 needs the 4-row compute-law amendment) finds K′ ≥ 2 (watch) / ≥ 4 (kill); current worst 1.401644, 2.85× margin | dli back to RED; third envelope death in a row; strip subtree re-stalls behind x4 |
| c2pp round 3 | F-a t=4 + 40-row F-c breaks the 2²¹ reserve (round 2: 14.53%) | Decision 6 reversed; mechanism data survives as B-WEAK-direct re-pose seed |
| WCL slot falsifier | One admissible row + one vanishing polynomial; most exposed: (1,5)'s remaining 53.56%; the six widened slots (1,7)(1,8)(2,8)(2,9)(4,10)(4,11) never scanned — the register's most exposed falsifier surface | Baseline A(R) ≤ (41/8)³⁴ < 2¹⁰⁰ dies at that ℓ → dli RED |
| DSP8 satellite | Any official row with P ≥ 21 | Vacuous-close route dead (DSP8 itself threatened only at P ≥ 25); fallback = 5-rectangle menu / CR-001 (36,1) survivor row (73,602 resultants) — whose reserve-first close **forces a re-wire we pre-draft now** (§3, B2) |
| HGE4 catches #99/#100 | F-4 minimality challenged; strips undefined | `f3_hge4_norm_gate_count` false-as-posed; pin and define **in-statement** before any claim |
| w8-C9 AZC | Any (h,R,8n³) re-pin without AZC re-run (0.5005% margin) | Unsound close; standing re-run obligation |
| Non-polynomial word hunt | Supply witness outside fiber reduction | F6 floor demotes — **fallback now recorded**: δ\* relocates; certify at the relocated value (§5) |
| l1 stall | Waves 19+ zero movement | Escalation rule fires (§3, C4) |
| Packaging w10-C1 | Grammar drift; three auto-discharges already refused | Endgame scramble; mitigated by the rolling crosswalk |

**Portfolio note:** 12 of 22 leaves and 3 of 5 demotion triggers in one lane (dli), behind one edge. Hence the standing **D2 dli cap**: dli receives at most one-third of any phase's effort, and any demotion event freezes further dli spend for one full phase while the B-WEAK-direct re-pose is drafted from surviving mechanism data.

**Upstream-facing state (rebuilt to the 07-21 board).** Three PRs in flight (#1010, #1013, #1019); queue otherwise empty. The μ₈ ⊂ F₁₇ near-rational counterexample is **dead as an outward item** — repaired in his tree; the only action is an internal correspondence-ledger note confirming his repair covers our REFUTED node `v13_2_near_rational_supportwise_payment`. The completed #750 correction (KB #115, issuecomment-5032130941) is cited as banked standing, not pending work. The WCL-ZONE-ext slot certificates are an **internal Decision-6 hardening deliverable only** — the banked survey verdict is different-lane; no upstream PR unless a future survey shows a six-input consumer. Third parties: scottdhughes (#984–#1008 wall wave) and latifkasuli are independent actors in his repo — the race is not two-player.

---

## 3. The plan

Four tracks: a conservative backbone (finite closures, amber maintenance), a transfer offensive (U1 broadside + U3 test), a convergence track (joint ledger, vendoring, l1), and selected hedges. Owners: "us" (this branch), Codex (autonomous within the forced-corrections boundary — pins/constants direct, status flips surfaced), Modal (time-rule jobs only), maintainer (spend/ceremony), contributor compute (CR pathway only).

### Near-term (days 1–10) — terminal risk, cheapest information, hygiene

- **N1. WW F-round 1** — the queued official-convention cell sweep (bg ≤ 1, generated-field normalization) at `ww_row_envelope_clause`. Technique: pre-registered falsification. Priced as a coin flip at the 0.9-bit aggregate line. Compute lawfully: decompose into <5-minute route-deciding screens with banked per-shard timings, or route the full sweep through maintainer authorization — never "Codex autonomous, 2 days." Kill: a violating word demotes R1′ pre-amber; the recorded repair is the extended per-cell ledger repricing the 1.263-bit gap (the only repair pattern that has ever worked on a refuted allowance). **D0 gate: know before budgeting months downstream of ww.**
- **N2. Non-polynomial-word kill probe** at the K5 bracket (terminal-risk-first inversion; one-sided kill probe). Us design (2 days), Codex screens, CR pathway if scale demands. Kill: witness found → full stop, reconvene under the δ\*-relocation fallback (§5).
- **N3. Identity-prefix stress test** of our B_ap columns against his exact deployed unsafe rows (UPSTREAM_HARVEST B.2 — undischarged audit debt with real kill potential against our own assembly). Us, 1–2 days, exact replay.
- **N4. Route-cut imports:** his moment-order floor (r ≥ 641,593 / 680,397) + negative library (prop:proper-q-gap, prop:q-moment-order-floor, kb_rowsharp_route_d_*_no_go, prop:bc-not-q) as DAG nodes. Us, 1 day; new nodes surfaced to the maintainer.
- **N5. Conflict-4 rate-scope audit** (gates the 20-leaf milestone). Codex, 1 day.
- **N6. Harness debt + census:** `xr_highcore_collision_count` verify.py, KB #107 lightweight-cert fix, and a **harness-coverage sweep** for any other md-only imports. Codex, 2 days.
- **N7. Upstream watch:** #1010/#1013/#1019 triage watch (no chase inside 24–48h); internal μ₈ crosswalk note (N.B. internal only).
- **N8. Governance:** file the compute-law amendment for the octave-31 4-row census (legalizes c1r3 round 3); log the "framing rule" (checklist-reduction PRs triage faster) as a hypothesis to test, not a fact.
- **N9. WCL exposure pass:** Burnside-model sizing + minimal exposure scan of (4,9)–(4,11) and the four never-scanned extended slots. Codex, 2 days. These are the register's most exposed falsifier surface; they must not stay unsized.
- **N10. l1 growth-law round** (see C4) — scheduled here because it is cheap and the leaf is the critical path.

### Mid-term (weeks 1–8) — three tracks in parallel

**Track A — conservative backbone.**
- **A1. Unit-ideal certificate pilot** at the three-variable (1,5) ideal (CR-004's own preferred route; the (2,7) census is an explicit NO-GO). Us, 2–3 weeks, hard cap week 6. Runs **before/parallel to** the Job A finish: a land retires the census model for all 10 slots.
- **A2. Job A finish** (`dli_wcl_slot_1_5_emptiness`, 46.44% done, 1,066,688 rows banked): a **maintainer spend-approval line item** (est. $45–60, unverified), executed only after the two named repairs (re-shard aggregation + ECM tail) land, via contributor handoff or maintainer-authorized run. Kill: admissible row + vanishing polynomial → dli RED → D2 cap fires.
- **A3. Norm(u)-saturation soundness gap:** fix and audit the weight-4 exclusion at order 1024 — a soundness hole in an already-cited route, independent of cost debates. Precondition (with the GMP/FLINT gcd benchmark and A1's failure) for any (2,7)+ contributor request (~$1,900–5,300 post-swap).
- **A4. dli amber maintenance, dated:** c1r3 round 3 (gated-L=2 + octave-31, legal after N8) target week 3; c2pp round 3 (F-a t=4 + 40-row F-c) target week 4. The Decision-6-reversal contingency is written now (freeze + B-WEAK-direct re-pose draft + D2/RK fork consult).
- **A5. Local rate-half seam:** prove the uniform Hankel split-pencil bound at {2³⁹, 2³⁹+1}; exact binomial-ladder (#115) arithmetic is only a final replay tool, not the missing theorem. This is correctly scoped as extending the determination to ~(2³⁹+2)·2¹²⁸; the sub-2¹⁶⁷ window is banked. Small-analogue classification remains evidence under CR-003 until a complete official-scale certificate route exists. Us, theorem-led.
- **A6. `xr_tangent_support_mismatch_bridge` — CLOSED by exact clause merge.** P-A1/P-B retain their original generic `8+8` allocation; the former combined nongeneric `16n^3` target is merged verbatim as P-A2. The full-zero descent (caps 169/169/255/254/254/510) is the P-A2 attack.

**Track B — transfer offensive.**
- **B1. U1 step one:** extract the per-lane (f,g) table from the WCL/HGE4/C36 packets; then the master statement with Newton short-window as base case. Us, ~2 weeks.
- **B2. DSP8 max-P ≤ 24 satellite** at `f3_h3_dsp8_correlation_bound` — the flagship pose, via the audited collision-norm/coupled-ideal template with the U3 Sidon-strip preprocessing folded in. Falsifier (P ≥ 21) shown satisfiable at pose time (#101/#126 guard). Cascade verified: flips the 3-amber single-req chain `f3_h3_mobius_excess_half` → `f3_h3_three_to_one_c36` → `f3_h3_direct_floor_conditional_close`, and feeds `u1_x4_direct_column_budget`. Contingency pre-drafted **now**: if P ≥ 21 fires, the CR-001 (36,1) survivor-row fallback's reserve-first close forces a re-wire — we write the re-wire (which edges move, who ratifies) before the round runs, not after.
- **B3. HGE4 odd-width parity+norm pose** at `f3_hge4_norm_gate_count`, with the F-4 minimality pin (#99) and both strips defined (#100) in-statement. Context: fan-in went 0 → 22 in three waves — the highest derivative on the board, not the highest fan-in. Us, ~1 week.
- **B4. Summit weight-≤4 ambient-exclusion census** at `f2_growing_order_myerson` — a **new proposal** (U1 transfer, never posed), shrinks the honest 2^{o(n)} form, does not claim the wall. Any further F2 items route through the F2_FLIP_GOAL ladder L1–L5 and the 12 recorded no-gos, or are labeled new.
- **B5. Chamber pilot, corrected:** the "pinned unique cubic candidate" is a completed exclusion, not a pilot. Instead: one real chamber of the 13 (e.g., a pendant scroll) under the e=1/e=2 field collapse, validating the WCL-template port. Codex-capped priority (low strategic yield priced in).
- **B6. D1 adjudication (end week 3–4):** ≥3 of B2–B5 + A1 land → file U1 as a DAG lemma node and route remaining slots through it; ≤1 → U1 is lane-local folklore; keep instances, stop generalizing.
- **B7. U3 adjudication** via B2's pre-strip harness: residual tightens toward ≤ 24 → U3 live → propose the **joint brief** on input 3 (our Sidon-strip restructure and his input-3 atom are the same object by his own line-2210 ordering — the only recorded route onto the F2 wall from either tree). No tightening and ww survives unsplit → U3 demoted; RK weighting rises.
- **B8. c2pp bulk-identity lemma** (the FD instance), posed with the honest 1.0662 worst case (§1). Us, ~4 days. Kill = the pre-registered round-3 reserve break.

**Track C — convergence.**
- **C1. Vendor-before-census (standing law):** translate the M31 wall library — ADE cut, shortened-flat, Chebyshev separator, whole-ball, parity-face, arbitrary-packet, rank-46 source-key (PRs #984–#1008) — into our chart/atlas coordinates as **reuse**; his declared UNRESOLVED remainder (padding bridge, common-core add-back, rank-two coloop, rooted-shell) is the seam where our XR work is additive. Then the GRS4 census, with the w8-C9 AZC re-run on any re-pin. Generalized rule: any upstream wave within one edit-distance of an open red of ours triggers vendor-before-census, never race.
- **C2. Grammar crosswalk, rolling:** diff our packaging emissions against `four_row_exact_completion_compiler_v1.json` + Paper D's (F1)–(F4); build adapters as waves land. Starts now, not at the end.
- **C3. PR pipeline discipline:** pacing per the recorded rule (one audit-genre PR in flight at a time — reconcile with the current three by filing nothing new until one resolves). Fourth-PR candidates (M5 rank-ledger packet; f1 classification skeleton) are **new survey work** that must first show a consumer or overturn the recorded DEAD/different-lane verdicts — no "fusion precedent" claim. WCL slot certificates stay internal (§2).
- **C4. l1 standing lane (the critical path):** (a) the cheap pre-registered balanced-strip growth-law measurement runs in week 1–2 (N10) — it arms a branch-(2) falsifier or licenses the clause-(P) floor-band-emptiness argument; (b) branch (4), the constructive first-match-supply lane, via U2 chart-carried mixed-petal descent (the F₂₃ obstruction kills only the chartless form); (c) LS6 guarded atom as fallback. **Escalation rule:** if waves 19+ show zero movement, shift weight to clause-(P)/floor-band-emptiness and formally price the RK alternative; consider a joint session on his arbitrary-word list-interior atom (nearest neighbor, non-substituting) before further solo spend.
- **C5. P-B route declared:** one timeboxed (1 week, weeks 6–8) transport attempt of `f2_effective_energy_dichotomy` onto `xr_lowcore_spread_heart`; on failure, P-B is formally declared reachable only via the `xr_smallcore_spread_count` floor / RK bypass, and the plan re-wires.
- **C6. U2 formalization** on his `firstocc_*` kernels → first Lean-backed floor (G1 chart-sum ledger), feeding his checklist item 2 — within the Lean scope set in §5.

**Hedge track (funded in parallel, never built on).**
- **H1. RK priced by attack:** one dedicated refutation round on `rigidity_kernel` with the floor-campaign attack families. Survives → stays insurance; dies → the 18-leaf prune fantasy comes off the books. Either way the board simplifies.
- **H2. FD as schema:** posed only after B8 adjudicates the c2pp instance.
- **H3. D3 self-kill (week 14):** zero red closures despite Tracks A–C → the transfer/insight allocation has failed its own falsifier; revert to pure grind ordering and record it in loop memory. The roadmap is itself an experiment.

### Endgame (weeks 8+) — the wall paid once, then assembly

If U3 adjudicated live: transport the restructure ww → summit parity model (j: 0→1, n = 2²⁵, q = p²) → the joint exchange-compressed brief with upstream; the payment lemma, if it lands anywhere, lands on our side of the balance line (4.73–4.83 bits vs his ~1.66M-bit overhead) in his formulation. If the wall stands: the **partial-credit posture** — stated honestly as list grand (if closed) + rate-1/2 MCA determination to ~(2³⁹+2)·2¹²⁸ (≈0.5 bit past his all-rates window) + the strongest per-row replay/audit record in either tree. We position on **audit depth**, not bit-lead; no "42+ bits ahead" claim survives fact-check.

**Dossier (full packaging tier, not just the crosswalk):** total certified f(C) compiler; clean-checkout replay CI; provenance chain; nonclaim-ledger emptying; **sync the 254 `critical/nodes/` dirs to the 1030-node DAG** (clean-checkout replay fails while folders lag); the N6 harness census closed; a **freeze date** set at the posture-decision point (§5). Joint merge: input 6 co-signed certificates; every joint status flip double-keyed (his triage + our Modal execution re-pin; hash-green ≠ execution-green). Bilaterality, stated precisely: his integration waves are imported without local replay and no independent adversarial replay lane exists in his tree; ours is the only such lane.

---

## 4. Decision queue for the maintainer

**Spend.** (1) Modal envelope: authorize a fixed $400 for the near/mid phases, per-line burn tracked by Codex, overruns need named justification. (2) Job A finish: approve the ~$45–60 (unverified estimate) as a line item, or route to contributor handoff — after the two repairs land. (3) Contributor-compute gate for (2,7)+: confirm the three preconditions (A1 failed, A3 soundness gap fixed and audited, gcd swap benchmarked). (4) Name a total program compute budget and its owner — currently nobody owns the aggregate.

**Outward.** (5) The #493/QA.22 conventions ruling: is the 27.0/60.6-bit within-column ww room spendable? This reprices the entire ww lane and should precede any ww proof spend. (6) PR pacing: ratify the one-audit-genre-in-flight rule against the current three-PR state; approve the fourth-PR candidate order. (7) Joint-brief authorization on input 3 — only if B7 adjudicates U3 live. (8) Co-authorship/credit-split conversation: anchored to input 6's structural bilaterality, raised at the first declared milestone, not at submission. (9) Submission posture decision date (§5).

**Policy.** (10) Ratify the octave-31 compute-law amendment (N8). (11) Re-affirm the forced-corrections boundary for Codex workstreams and set an integration-audit cadence (wave-1 integration produced catches #76–#89 — integration finds errors; audits are not optional). (12) A ceremony-batching rule: if Tracks A–B produce the first red closures in five waves, they queue behind maintainer ratification — set a batch cadence now. (13) Reconcile the F2 flip campaign's "resume every session until T-WIN or T-FLOOR" standing rule with this roadmap's demotion of F2 to fence-exchange + B4 — the recommendation is to formally re-scope the standing rule to "B4 + ladder L1–L5 items only," but that is a maintainer call. (14) Owners for: the still-open DLI-CLOSE-6 Pro thread, the paused M5 packet (PR #172), and the artifact-refresh rule (Claude artifact + publish_site.sh after every DAG update — extended to cover the C2 crosswalk).

---

## 5. Completeness additions

Items absent from every draft, now explicit; each has an owner or a date.

**Spec fidelity (the roadmap's quiet assumption).** The prize spec is PRELIMINARY, and nothing audits dag `prize` ≡ the actual challenge. One-time spec-to-DAG fidelity audit (us, 2 days, week 1) covering the open accounting the spec memory lists: the m-fold interleaved Λ(C^{≡m}) constant-m transfer, challenge-field-vs-generated-field accounting, the concrete poly-exponent B with n^B ≤ 2^{−128}·|F|, and η toward the 2^{−9} cap. Plus a standing monitor on proximityprize.org for spec finalization.

**Upstream staleness and spine supersession.** Our view is frozen at 18cfc199; the joint ledger is keyed to GF **v3**, which followed v1 and v2 and can be superseded by v4 — stranding the crosswalk and six-input mapping. Mitigation: a re-sync cadence (maintainer-approved fetch windows), the crosswalk re-diffed on every spine move, and the explicit acknowledgment that scottdhughes and latifkasuli can close "first-match atlas completion on an M31 row" before either of us.

**Falsifier power audit.** "0 deaths across 21 attack families" is evidence only if the falsifiers had power: C36's F-round 1 survived vacuously, ww has never run a round, several ambers' rounds saturate trivially. Before any amber enters the dossier: classify every floor as tested-with-power / tested-vacuously / untested (us, 3 days, before the freeze date). Vacuously-tested floors get one powered round or an honest label.

**Verification-debt correction (to the task's own list).** The two queued v13 replay checks are NOT open debt — the deferred-verification queue was emptied 5-for-5 on 2026-07-10 (prefix rigidity PROVED; Veronese confirmed at char 0; plus BC-curve, capf-planted, L4-interior). Do not resurrect them. Actual open verification debt: KB #107 fix (N6), xr verify.py (N6), the **w8-C9 AZC re-run obligation** (0.5005% margin — covered in C1), the order-1024 Norm(u) gap (A3), and the harness census (N6).

**Lean scoping.** Align with upstream's explicit four-item priority list (rs-mca/agents.md: GF end-to-end first, then cs25_cap_v13_2, then thresholds, then deployed certificates); audit our own `prize/formal/lean/rs_mca_formalization` package status (Codex, 2 days); decide what the dossier claims at Lean level. Since his thresholds formalization consumes our n=2⁴¹ determinations, our export channel carries a Lean-statement obligation — scope it (likely: statement-level only, via C6's firstocc route). Cap: ≤10% of effort.

**Submission strategy.** The spec allows partial results and split awards. Decide: joint vs separate submission with Przemek (input 6 makes joint natural; the credit conversation is queue item 8); timing against his Paper-D pay-per-bit positioning (ε_mca > 2^{−86}, 42 bits above target — he is already positioned for partial credit); and our defined fallback dossier: **"conditional resolution modulo two named atoms (input 3/F2 summit; the beyond-2¹⁶⁷ bracket), everything else certified"** — with the posture decision date set at **week 14 (D3)**, the same gate as the self-kill.

**The progress metric, reframed.** The grand challenges ask to **determine** δ\*, not to prove our conjectured value. Paper D's cap bounds δ\* from above; our floors bound it from below. Therefore: (a) the non-polynomial hunt's "terminal-risk event, no fallback" is wrong as stated — a floor demotion *relocates* δ\*, and a refutation branch with certification at the relocated value is a first-class prize deliverable; that fallback is now recorded (N2). (b) The roadmap's progress metric is **bits of remaining δ\*-bracket, jointly over both trees** — the only metric under which four waves of fencing were not zero progress, and the honest answer to "what did all the fencing buy."

**End-state forecast (planning priors, not evidence; revisit at D3/week 14).** Full resolution of both grands within this roadmap's horizon: ~10%. `list_grand` alone: ~25%. Clean-rate milestone (conditional on the Conflict-4 audit): ~50%. Partial-credit conditional dossier at submission quality: ~85% (the statistically likely outcome given four waves of zero closures, and the reason the dossier work is front-loaded). Relocated-δ\* determination (a floor demotes and we certify at the new value): ~5–10%. The pattern of surprises — everything structured closes, nothing pays — is what these numbers encode; if D1/B7 adjudicate a unifying lemma live, the top lines move and the forecast is re-issued with the D3 review.
