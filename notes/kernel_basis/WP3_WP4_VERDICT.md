# WP3+WP4 — u1_x4 and dli vs K2 (Faces 1/2): both REDUCTION-PARTIAL
# (2026-07-10; fresh-context adversarial replay; anti-goal upheld;
#  eight verifiers green; one new catch)

## WP3 (u1_x4_direct_column_budget): REDUCTION-PARTIAL
DIRECTION FINDING: RK Face 1 does not supply x4 — it CONSUMES it
(Conjecture TR's per-leaf term ~ the quotient-row-transported
x4-split + u1-budget). The u1 red and Face 1's residue are THE SAME
open clause-(ii) instance seen from two consumers — a unification
datum, not a reduction. Chain: proved at the split/strips (incl.
U3 net-absorption replayed, certificate byte-identical; h=2 HBK and
h=3 char-0 = K2-box theorems per M2; the H3-ACT(4096) implication
replayed with exact budgets). Missing, named:
  X1 conic rank-deficit gate (incidence-shaped, absolute tolerance;
     K2-with-stronger-pinning at best, plausibly own finite-
     geometry kernel; WP0 gap 5 conversion unstated)
  X2 bridge accident census (K2-as-pinned given object+row pins)
  X3 h=5/h=8 finite certifications + coverage audit (non-kernel)
  X4 taxonomy extension: moment-trade + Luroth/net classes are NOT
     in imported K2's four-class clause (i) (M4 sibling)
  X5 = CATCH #26: U2's banked window '(t, polylog n]' is OVER-BROAD
     — the FM argument covers only b <= t log2(q)/log2(n) (exact:
     log2 C(2^41,10) = 388.2 > 384 fails from b = 10 at the s=41
     row; covered b* = 29.5 (s=13) down to 9.4 (s=41)); on the gap
     window K2 grants NOTHING (not FM-suppressed) — U2 there is a
     separate Weil-route claim or a charged column
  X6 FM-witnessing caveat (all F3 evidence at FM = Theta(1))
SIMPLIFICATION FOUND: at FM-suppressed strata x4's ABSOLUTE n^3
budget kills WP2's M6 problem (any B_RK gives remainder < 1); the
constant-exactness issue is confined to non-suppressed strata.

## WP4 (dli_prime_weighted_large_block_support): REDUCTION-PARTIAL
IDENTIFICATION EXACT (not analogy): dli's per-level counts B_j(M)
are literally hyperplane-concurrency objects (codim-L_j flat cap
D_j — Conjecture F's Lemma 4 object); clause-(i) strip replayed
18/18; low-dim clause (ii) proved (incidence-shaped, gap-5
conversion pending); dli's own coset-dressing observation is the
charged column in the wild. B-WEAK is AVERAGE-CASE-shaped (E_U) —
the quantifier type of RK's PROVED pair-ledger row; dli's hard
axes are J + D, not Q. Missing, named:
  Y1 per-level clause (ii) at growing codim (K2-as-pinned MODULO
     gap-5 conversion + generated-field pin)
  Y2 THE CRUX — the joint reserve at nested-conditional junctions
     (C2'', not yet precisely posed)
  Y3 ENDPOINT-EXC-RULE certification
  Y4 accident census in measure

## M2's jointness lead: FALSIFIED (three machine-checked grounds)
#212 does NOT cover B-WEAK's aggregation: (1) wrong quantifier type
(deterministic image-product over fixed direct-sum decomposition vs
U-weighted expectation of correlated ratios; per-level worst case
includes the 3^{N/2} antipodal kernel — the crude bound overshoots
its own corridors by +120..+952 bits, exact); (2) the aggregation
content is open even in RK's home lane (LL reduces jointness to a
leaf; TR is the open reserve; LL's direct-sum hypotheses fail at
dli's junctions — the F2-landmine proved nested-conditional
structure); (3) factorized transfer measured FALSE at dli's
junctions (f2b ratios replayed exactly: geometric mean 2.139 >
1.566/junction allowance, growing with q).
POSITIVE RESIDUE: TR (Face 1) and C2'' (dli) are SIBLING
joint-reserve statements with identical two-clause anatomy —
plausibly ONE kernel clause ('K2's joint reserve at
state-dependent junctions'). Unification lead U3-KB: would keep
k = 3 with K2 enlarged. Concrete next item: pose C2'' precisely.
