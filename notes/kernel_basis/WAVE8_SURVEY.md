# w8s Fresh-Context Survey — Codex v4 "PMA" campaign + v3 tail + upstream

Date: 2026-07-16. Scout: read-only reconnaissance; no repo writes anywhere.

## VERDICT LINE

**Bears on the mixed-petal gap: PARTIAL — no closure, but decisive.** The v4
campaign (a) REFUTES `pma_wide_residual` (the `#Post<=n^6` allocation), which is
one of the THREE predicates of master's live CONDITIONAL closure of
`petal_mixed_amplification` — so master's imgfib demotion today would otherwise
condition on a predicate now known FALSE; (b) proves ~30 PROVED `pma_*` support
nodes that pay every M<=3 source, M=4/t=4, all rate-quarter M=4/t=3 cells and
positive-denominator rate-half cells; (c) leaves open exactly: source-coupled
rate-half-tail certificates, M=4/t=2, larger-M full petals, and **partial
petals** (the diffuse partial-petal bucket) — the node is re-posed TARGET in v4;
(d) proves the refutations live OUTSIDE imgfib's `sigma >= C n/log n` reserve,
so imgfib itself survives as CONDITIONAL. Pointers:
`critical/nodes/pma_wide_residual/` (REFUTED),
`critical/nodes/pma_sigma_one_d4_generic_source_obstruction/` (b9beb6e8),
`critical/nodes/pma_sigma_one_d3_reciprocal_quadratic_obstruction/` (74225111),
`critical/nodes/petal_mixed_amplification/statement.md` (re-pose), all under
/home/u2470931/smooth-read-solomin/prize-codex-resolution-v4-20260713/.

---

# SOURCE 1 — Codex v4 worktree (PMA campaign)

Worktree: /home/u2470931/smooth-read-solomin/prize-codex-resolution-v4-20260713
Branch: codex/full-prize-resolution-v4-20260713.
Fork point = master HEAD **3892f81e** (merge-base confirmed). Campaign =
exactly **100 Codex commits** `3892f81e..ae2e5dd5` (07-13 22:22 → 07-15 07:56);
the other ~33 "since 07-13" commits are master's own history below the fork.
One mid-campaign merge of master: **8aeec4be** (07-14 14:19, "Repair
mixed-petal scope in universal image-fiber chain") — Codex synced master's
imgfib demotion and moved `petal_mixed_amplification` + `pma_*` triple from
background/ to critical/.

## (a) What PMA is

PMA = the **petal mixed amplification** program: the campaign to close
`petal_mixed_amplification`, the post-top-band / mixed-petal bucket of
`#ImgFib_U(k+sigma) <= n^B` (imgfib). Charter = `notes/PRIZE_RESOLUTION_ROADMAP.md`
(header dated 2026-07-15; synchronization checkpoint through integration note
r93; integration notes r54–r93 are the campaign log). Master node targeted:
**imgfib** (CONDITIONAL in both repos), through the corrected critical leaf
**petal_mixed_amplification**; secondary targets worked in the same campaign:
f3_h3_mobius_excess_half (C36), the two xr selectors, the dli WCL slots, and
the rate-half list band. v4 DAG: 691 nodes / 1291 edges / 526 PROVED; critical
orbit 245 nodes, 207 PROVED / 29 CONDITIONAL / 9 UNPROVED; its nine leaves are
the same nine reds master tracks.

## (b) Mixed-petal / diffuse partial-petal: what is actually proved

Statuses in v4 dag.json: `petal_mixed_amplification` = **TARGET** (not proved);
`imgfib` = CONDITIONAL; `pma_wide_residual` = **REFUTED**; ~30 `pma_*` nodes
PROVED. Net vs master: **70 new nodes, all PROVED**, plus 3 status changes
(`pma_wide_residual` TARGET→REFUTED, `petal_mixed_amplification`
CONDITIONAL→TARGET, `packaging` CONDITIONAL→PROVED — see audit flags).

Re-posed target statement (v4 `critical/nodes/petal_mixed_amplification/statement.md`,
condensed): after the proved global owners, the first-matched post-top-band
maximal-source class needs one printed row-uniform polynomial bound + explicit
natural-scale quotient/profile charges composing to imgfib. Already paid inside
it: saturated root pinning + petal-pattern ledger prove every fixed
`(t ell - h) + max(0, 2d+1-h)` region polynomial; band/root composition pays all
M<=3 sources and M=4,t=4; three-petal mu-basis + official-rate sieve +
projective Johnson bound pay every rate-quarter M=4,t=3 cell and all
positive-denominator rate-half cells; the rate-half tail is paid by 8n/source
absent a source-coupled positive 0/1/infinity-fiber certificate. **Open: those
source-coupled certificates, M=4,t=2, larger-M full petals, partial petals.**

Most load-bearing PROVED statements (read in full):

1. `pma_sigma_one_d4_generic_source_obstruction` — at valid official row
   (q,n,k)=(65537^2, 65536, 32768) an exact first moment builds a maximal
   source with >n^6 primitive post-owner defect-4 codewords evading all three
   proved owners (periodicity / dyadic / odd-lift). Refutes pma_wide_residual
   "without refuting a larger polynomial imgfib theorem".
2. `pma_sigma_one_d3_reciprocal_quadratic_obstruction` — explicit
   c(x)=x^{-1}+rx source with Omega(n^7) primitive diffuse (d,r)=(3,0) extras;
   kills any universal per-sunflower n^6 diffuse payment (uniform-in-sigma
   route dead).
3. `pma_sigma_one_variable_defect_exact_hit_floor` — exact-hit lower-bound
   formula for every sigma-one defect d; at d=n/8 with Linnik primes it is
   exponential ⇒ **no uniform sigma-one polynomial bound exists**; explicitly
   "outside the sigma=Omega(n/log n) imgfib reserve" (why imgfib survives).
4. `pma_saturated_mixed_support_kernel` — split defect locator lies in an exact
   weighted-Hankel kernel; canonical core-root pinning bounds split locators by
   binom(k-1, max(0, d-(|X|-d-1))); aggregation over first-match profiles is the
   open residue.
5. `pma_sigma_one_d3_diffuse_hyperplane_reduction` — the sigma=1 diffuse
   (3,0) cell reduces to five-point divided-difference hyperplanes; field-
   independent aggregate 16(k-1)^2·C((n-k)/2,5); remaining endpoint = one-power
   aggregate saving or a global owner. (Closest thing to a *diffuse* partial
   result: the diffuse cell is reduced-and-mostly-paid at defect 3, refuted at
   uniform sigma.)
6. `pma_full_petal_band_composition`, `pma_three_petal_projective_johnson_bound`,
   `pma_ratehalf_core_triple_excess_reduction`, `pma_source_paving_bridge`
   (bridges the PMA aux list to upstream PR #764's transverse weighted-RS chart,
   threshold exactly kappa+sigma).

Bottom line for the banking session: v4 does NOT hand master a mixed-petal
proof, but it hands (i) the refutation that master's current conditional
closure is predicated on a FALSE node, (ii) the corrected re-pose with a much
smaller open set, and (iii) the proof that imgfib's demotion should be to
CONDITIONAL-on-the-re-posed-target, not to REFUTED.

## (c) What it refuted, and whether master cares

- **b9beb6e8 "Refute finite PMA allocation at defect four"** → mints
  `pma_sigma_one_d4_generic_source_obstruction`, flips `pma_wide_residual` to
  REFUTED. Master cares MAXIMALLY: master's
  `background/nodes/petal_mixed_amplification/conditional.md` closes from
  predicates {pma_aux_list_reduction, pma_johnson_regime, **pma_wide_residual**}
  — that closure is now broken. Master's pma_wide_residual is TARGET today;
  it must become REFUTED on import, and petal_mixed_amplification must drop
  from CONDITIONAL to TARGET (v4 already did both).
- **74225111 "Refute uniform PMA diffuse payment route"** → mints
  `pma_sigma_one_d3_reciprocal_quadratic_obstruction`. Kills any route paying
  the diffuse bucket uniformly per-sunflower at n^6; forces either a global
  first-match owner or a larger audited allowance. Master cares: any plan to
  re-prove the old wide-residual style bound is pre-falsified; the new red must
  be posed reserve-scoped (sigma >= C n/log n) or with per-cell ledgers.
- Both refutations are constructive (explicit rows/families) and come with
  Modal census artifacts (`experiments/prize_resolution/pma_d3r0_census_*`).

## (d) Commit clustering (100 commits, pinned SHAs, dependency order)

Cluster G — master-echo duplicates, **skip on import** (5): d26cabdf, 912d9130,
e2713bba, da4387e1, aadf1c6d (same messages as master's ac7a705/8ba3cad/3892f81
with different SHAs; parallel copies of C1'-refutation-era commits).

Cluster A — C36 / f3_h3 chain (22 commits, 07-13 22:22 → 07-14 18:33):
a5c58e30 (quotient-block Stepanov tail), e2f411c3 (double-rich excess
compiler), 94c1758b, 2f5c2904 (rich blocks as rational surfaces), 9e920c85
(sparse resultants), 59519373 (global derivative ideal), 414f383e/2734658f/
9cf4ebbd (citation fences), fd707175→e374970f→6e0ce4a9 (uniform product-fiber
bound, constant tightened to 33), ecc13869 (Galois-orbit factorization),
08b59d06, 8c333b98 (prefix ideal orbits), 4e32f2b0 (unordered derivative
ideals), c40ee797 (level resultants), 94897216 (level common-factor
classification), de4dcdd0 (resultant easy factors), 93b3fc7b (sparse
subresultants), c5de88d5 (**local-cluster valuation** — the endpoint:
v_p(e^U_n)=V_n(p)>=Z_9, row-wise cap V_n(p)<=(150/17)n^2 implies C36', with an
exact boundary-row p^6 Hensel certificate), 1785fb67 (overvaluation ledger).
→ 17 new PROVED `f3_h3_*` nodes under `f3_h3_mobius_excess_half`. Internal
order: Stepanov tail → compiler → ideal chain → valuation endpoint.

Cluster B — DLI WCL slots + majorants (9 commits, 07-13 23:21 → 07-14 08:01):
ccb553c5 (ell-two weight-5 relations), 99e06891 (cyclotomic pair ideals),
1f38d814 (full-spectrum polymer majorant replaces truncated route), dc41eb7b
(raw-activity fence), 22119f50, f55f2be0 (matching-pressure majorants),
**9688c6ad (CLOSES WCL slot (ell,w)=(2,5) on every official row** — 1,514
odd-dilation pair orbits, 507 gcds over 168 Pocklington-certified primes, max
v_2(p-1)=18; 46k-line certificate JSON), 0087b00a, 760f4a68 (weight-6 slot
reduced to <=1,550,336 pair-product candidates with two explicit cyclotomic
norm obstructions each, via division-free doubling recurrences).
→ 6 new PROVED `dli_*` nodes. Bears directly on master's WCL-ZONE "six finite
zero-event slots": one slot fully closed, a second reduced to a finite
certified list.

Cluster C — XR selectors (17 commits, 07-14 02:30 → 09:40): 3b187648
(**corrects an XR rank-four coverage claim** — read first), 6470eb3a, f3762832,
3a9e17d7, 10c335b1, 0a928482, 4977ad05, c85cf2e5, d400016b, ffb1cb81 (rank-five
split-pencil census globalized), dd144beb (collision direction rigidity),
74895e7a, e2f3d73a (one-loop low-core defect), 3204875d, 665f3671, f7a63192,
be7be3cc (dual trades). → 15 new PROVED `xr_*` nodes incl.
`xr_lowcore_u0_v1_grs_defect_reduction` (P-B side) and the rank-five family
(P-A side). Caution: wave-5/6 audits found earlier XR imports needed #158
scoping; audit these against the same fence.

Cluster D — rate-half list floors (2 commits): 2af18dfd (fixed-tail prefix
floor: full-band ordinary-list failure through log2(q)=255.92…), 5bf2cbb0
(**cyclic rotated prefix floor: whole residual band list-unsafe at sigma* for
ordinary AND every constant common-support interleaving arity, all q<2^256,
75.0796 bits of margin**). → 2 new PROVED nodes; v4 re-scopes
`rate_half_band_closure` to the support-wise MCA/CA crossing only (list object
removed from the red). Small audit surface, large narrowing.

Cluster E — the PMA campaign proper (30 commits, 07-14 14:19 → 07-15 07:56),
in dependency order (cluster totals: 22+9+17+2+30+15+5 = 100):
- E1 scope repair + bridges: 8aeec4be (merge; mixed_petal_scope_audit note +
  verify_scope_repair.py), cdaccfec (source + B11 routing bridges), 73e5095c
  (global exact-periodic owner), e132cadc, 9f673cd0 (route cuts).
- E2 sigma-one cell payments: 3a0fc232 (low-defect), 019b54ab (d3 background),
  5d35fdc9 (d3 full-petal), 938ec17f (d3 diffuse → hyperplanes), 6930ba25
  (LineRay absorption note).
- E3 refutation 1: **74225111** (uniform diffuse route dead).
- E4 owners: 461d4d93, 835442c6 (dyadic near-coset owner to growing defect),
  0c65de06 (odd-lift boundary owner + petal scope repair).
- E5 chart composition: 6f799109 (post-top allowance ledger), 1f429973,
  91e8c6fc (paired-core normalization), aab9a46c (core census), 88423f87
  (first-layout collapse/domination).
- E6 refutation 2: **b9beb6e8** (pma_wide_residual REFUTED), 0b6f5ea7
  (growth-vs-reserve separation = the imgfib-survives argument).
- E7 saturated kernel + endgame: 6696f9e9, da20fbe5 (saturated interpolation
  kernel), b5bbdaf6 (petal-pattern root-pinning ledger), 5d45a126 (full-petal
  band composition), fbc2d0d5 (constant-shift strip exclusion), 6812bd42
  (arbitrary-petal source realizability), 440e54c5 (three-petal mu-basis),
  0ad5260d (official-rate sieve), ae2e5dd5 (projective Johnson bound).

Cluster F — roadmap/upstream-intake bookkeeping (~15 commits): 22f8565f,
77770bb3, 55b18438, 45d9d924, 565c20ab (live upstream adoption gates table for
PRs #755–#775), 7a5d04fc, d923d7f3, bb571ac2, 82dda3b9, 52892f56, 84eb283e,
e1f50a1c, c3eb0d4a, 05adf08d, 952ca04a. Integration notes r54–r93 in
`notes/PRIZE_RESOLUTION_ROADMAP.md`. FYI-import (merge the roadmap deltas; the
PR-adoption-gate table is genuinely useful).

### Audit flags found during survey

1. **Unjustified status flip riding an unrelated commit**: `packaging`
   CONDITIONAL→PROVED happened inside 9688c6ad ("Close DLI ell-two weight-five
   slot by norm gcd") with no packaging files touched. Verify whether this is a
   legitimate gate="all" recompute (a dependency completing) or an accident,
   BEFORE accepting the dag.json wholesale.
2. Cluster C (XR) must be re-checked against the #158 scoping fence from
   wave-5/6.
3. v4's `dli_wcl_ell2_weight6_triple_cubic_router` ASSUMES "the paid official
   order-1024 weight-3 exclusion" — confirm that assumption maps to master's
   `dli_wcl_weight3_ambient_exclusion` exactly.

### Recommended import priority

1. **E3+E6 refutation core first** (74225111, b9beb6e8 + the pma_wide_residual
   node folder): it changes a status decision master is making TODAY (imgfib
   demotion must not condition on pma_wide_residual).
2. Rest of Cluster E (28 new PROVED pma_* nodes + petal_reserve_rich_fiber_reduction
   + re-posed petal_mixed_amplification) — the new mixed-petal red should enter
   master in its v4 narrowed form, not the broad form.
3. Cluster D (2 nodes, small audit, retires the list half of
   rate_half_band_closure).
4. Cluster B (DLI WCL: one slot closed + one reduced; heavy but certificate-
   backed — replay the Pocklington/norm-gcd verifiers).
5. Cluster A (C36 chain to the local-cluster valuation endpoint; long but
   linear).
6. Cluster C (XR; largest scoping risk, audit last with #158 fence in hand).
7. Cluster F roadmap merge; skip Cluster G.

## (e) v3 tail (worktree /home/u2470931/smooth-read-solomin/prize-codex-resolution-v3-20260712)

- **77485270 "Fence the DLI boundary divisor elimination route"** — a
  diagnostic/fence, not a theorem: reduces a residual (2,5) WCL relation to
  f(T)=T^5+aT^3+bT-c | T^1024-1 and an exact 3-variable elimination system;
  Groebner resource ladder shows Singular times out from order 128 up, so the
  route is fenced as computationally saturated. Files:
  `critical/nodes/dli_wcl_zone_coverage/boundary_divisor_route.md` + 2 Modal
  scripts + results. **Import: LOW/FYI** — v4's 9688c6ad closes the (2,5) slot
  outright by the norm-gcd route, superseding this fence's purpose; import only
  as negative-route documentation for the zone-coverage node.
- **020d1a71 "Prove the C36 quotient-block Stepanov tail"** — mints
  `f3_h3_quotient_block_stepanov_tail` (T blocks of size>=tau have total size
  <6(nT)^{2/3}; marginal tail only, no product-fiber control). **Superseded:**
  v4's a5c58e30 re-lands the same node (different patch-id, same node id,
  content refreshed); import from v4, not v3.
- **f477a948 "Reduce C36 to double-rich block excess"** — mints
  `f3_h3_double_rich_excess_compiler` (X_18<=bS_18+Y_b compiler; b=14..17
  restricts to simultaneously product-rich blocks of size>=16..19; a compiler,
  not the estimate). **Superseded** by v4's e2f411c3 the same way. Note both
  bear on master's C36 red `f3_h3_mobius_excess_half` and are the first two
  links of v4's Cluster A chain — auditing Cluster A covers them.

Verdict on v3 tail: nothing to import separately except (optionally) the
boundary-divisor fence note as background; the two C36 commits are subsumed by
v4 Cluster A.

---

# SOURCE 2 — upstream rs-mca (remote upstream/main, 5 commits since 9262f63c)

Commits: c35a6da3 (L1+L2 PR wave, 60 files, +23,098), 2633895a (frontier PR
wave, 222 files, +98,153 — mostly L1 two/three-sector census verifiers +
rank16 Hahn-LP), 764f1c02 (L1/BC/L2/Lean wave, 330 files, +70,486 — the
mixed-petal/B9-frontier wave), 02728b20 (L1/M1/rank/site wave, 80 files,
+31,758), 9c4ca98c (RS_MCA_Paving_v8 ePrint package).

## (f) Relevance to master's 9 critical reds

Only ONE red has real new upstream substance: the **L1 mixed-petal/imgfib
lane**. Confirmed ABSENT from all 5 commits: anything on
xr_highcore_collision_count / xr_lowcore_spread_heart, f2_growing_order_myerson,
f3_hge4_norm_gate_count, ww_row_envelope_clause, C36/f3_h3_mobius_excess_half,
and the dli/W_cl lane (apparent WCL hits were substring false-positives).
agents-log 2026-07-15 wave entry: "the blanket L1 imgfib discharge remains
blocked by a mixed-petal gap" — upstream now RECORDS the gap master is demoting
on.

IMPORT-CANDIDATE (mixed-petal / petal_mixed_amplification):
- `experimental/notes/l1/l1_imgfib_crosswalk_audit.md` — upstream's adversarial
  audit of the imgfib chain vs `ass:locator`; Clause 6 = mixed-petal /
  diffuse-partial-petal amplification recorded OPEN GAP / CONJECTURAL; names
  the retracted `petal_excess_induction` / `petal_mixed_amplification_step`.
  Machine-checked: `experimental/data/certificates/l1-imgfib-crosswalk-audit/certificate.json`
  + `experimental/scripts/verify_l1_imgfib_crosswalk_audit.py` (PASS 41/41).
- `experimental/notes/l1/l1_pma_auxiliary_johnson.md` — PROVED-COMPILER for the
  few-petal part; explicitly "does not prove mixed-petal sunflower
  amplification" (upstream's own scoping of the red's boundary; mirrors
  master's pma_johnson_regime).
- `experimental/notes/l1/l1_mixed_petal_residual_frontier_ledger.md` (+ cert +
  verifier) — finite B7–B11 residual-frontier ledger; explicitly not a proof of
  amplification.
- `experimental/scripts/analyze_l1_mixed_petal_template_211.sage` / `_221.sage`
  (+ CAS verifiers + certs) — finite mixed-petal templates; "cannot certify
  asymptotic mixed-petal amplification".
- `experimental/notes/l1/l1_official_rows_crosswalk_20260713.md` — our PR #750
  content, integrated byte-identical; roadmap-promotion text withheld.

IMPORT-CANDIDATE (rate_half_band_closure machinery; closure left OPEN):
- `experimental/notes/thresholds/band_uniform_cube_reduction.md` (+ cert +
  verifier + `experimental/lean/band_uniform_reduction/BandUniformReduction.lean`)
  — "band-uniform signed death" flat-cube certificate; kills the ell^2/signed
  side of the dichotomy on every failing band.
- `experimental/lean/razorband_witness/RazorbandWitness.lean` (+
  NearSidonRazor.lean + AXIOM_AUDIT.md) — K5 razor-band witness kernel; "band
  closure open".

FYI:
- `experimental/lean/grande_finale/GrandeFinale/PrefixRigidityPacking.lean` (+
  correspondence note) — prefix-rigidity distance + Johnson packing cap;
  touches the rigidity census, not obviously master's rigidity_kernel object.
- Keyword-only non-matches (checked and excluded): IdentityPrefixCollisionFloor
  (pole-line collisions, not xr P-A), rank15 hyperplane slicing + weighted GRS
  coset packing ("Mobius" = Gaussian-binomial, not C36), profile/incidence
  envelopes (not ww_row_envelope_clause), Hankel/Sidon/selector/locator hits in
  the charge-dichotomy program (band lane, not xr selectors).
- Nothing upstream bears on a repaired C1' or the WCL six slots.

## (g) RS_MCA_Paving_v8.tex — "MDS Paving Bounds for Reed-Solomon MCA" (Chojecki, Ulam.ai, dated 2026-07-16)

Claims: an integrated support-count + paving-information-set upper bound for
affine support-wise MCA of every linear MDS code; finite smooth-multiplicative
and circle RS certificates with eps_mca <= 2^-128 beyond Johnson at n=512;
challenge-restricted endpoint-exact linear-budget thresholds to ~half minimum
distance via exact CA-MCA decomposition + BCHKS; all-radius saturation once the
integer slope budget dominates C(n,k+1). Explicit that the unrestricted
constant-rate smooth/circle frontier stays OPEN. Bears directly on prize
problem 1 (support-wise MCA, smooth rows); only tangentially on problem 2
(one pair-interleaved Johnson lemma, line ~1009).

Consumes/supplies vs master:
- CONSUMES (missing, named in its Conclusion as the exact open inputs): an
  **image-normalized Sidon payment at accessible moment order** (sec 8, line
  ~4164), a **residual ray compiler**, **first-match profile exhaustion**, and
  **profile add-back with a safe target reserve**. Also a conditional
  Assumption `ass:retained-factor-lift` (RF1-RF3, function-field Hensel lift).
  These are things master's lanes could in principle supply — sec 8's
  image-fiber-moment machinery is the paper-side counterpart of master's
  #ImgFib_U / mixed-petal bucket, and "first-match profile exhaustion" is
  exactly the aggregation residue left open by v4's
  pma_saturated_mixed_support_kernel.
- SUPPLIES: the primitive-inverse theorem (image-normalized Sidon payment =>
  primitive max-fiber flatness, |F| <= e^{C0 sigma N}), augmented-word paving
  count, tangent floor, all-radius saturation. Does not close any of the 9
  reds.

## (h) Upstream open PRs

- Open queue (top 20 at 07-16): dominated by holmbuar's C0 f28/q64
  locator-block formalization series (#824-#842) + rank-16/M1/fixed-core items.
  **No open PR targets L1/mixed-petal/imgfib or the dli/WCL/xr-selector reds.**
- **#827** (holmbuar, "shell-mass spectral law and the first single-shell
  failing band") — topically adjacent to rate_half_band_closure; worth a look.
- #836 ("canonical C0 f28 locator blocks") — locator keyword only; finite
  C0/rank-16 lane, not the L1 imgfib lane.
- Watched PRs resolved: **#744** CLOSED 07-14 not merged → integrated as
  `selector_free_exact_weight_all_pair` (c35a6da3); **#745** CLOSED 07-14 not
  merged → integrated as the `selected_owner_unit_layer_boundary` packet
  (c35a6da3); **#750** (ours, imgfib official-row discharge) CLOSED 07-14 not
  merged → crosswalk integrated byte-identical + upstream wrote the adversarial
  audit; Clause 6 mixed-petal recorded upstream as OPEN GAP.

---

# PRIORITIZED AUDIT/IMPORT PLAN (banking session)

1. **v4 Cluster E3+E6 (74225111, b9beb6e8 + pma_wide_residual folder)** —
   FIRST, and before finalizing today's imgfib demotion: master's
   petal_mixed_amplification conditional closure is predicated on
   pma_wide_residual, which v4 refuted constructively. Demote pma_wide_residual
   to REFUTED, drop petal_mixed_amplification to TARGET in the v4 narrowed
   form. Audit = replay the two obstruction constructions (explicit rows;
   Modal census artifacts in experiments/prize_resolution/pma_d3r0_census_*).
2. **Rest of v4 Cluster E** (28 pma_* + petal_reserve_rich_fiber_reduction +
   re-posed target statement). This is the mixed-petal bank: owners,
   cell payments, saturated kernel, three-petal Johnson chain.
3. **v4 Cluster D** (2 rate-half list floors) — small audit, retires the list
   half of rate_half_band_closure; pairs with upstream
   band_uniform_cube_reduction (which kills the signed side of the MCA
   dichotomy) — together they materially shrink that red.
4. **v4 Cluster B** (DLI WCL: slot (2,5) closed, (2,6) reduced to a finite
   certified list) — replay norm-gcd/Pocklington verifiers; check the
   weight-3-exclusion assumption mapping; also resolve audit flag on the
   packaging status flip that rode in 9688c6ad.
5. **v4 Cluster A** (C36 chain to local-cluster valuation endpoint) — linear
   22-commit chain under f3_h3_mobius_excess_half; supersedes v3 tail
   020d1a71/f477a948.
6. **v4 Cluster C** (XR, 15 nodes) — last of the heavy clusters; re-audit
   against the #158 scoping fence; starts with a self-correction (3b187648).
7. **Upstream pulls**: l1_imgfib_crosswalk_audit (+cert) into the
   petal_mixed_amplification node notes (node-local notes rule);
   band_uniform_cube_reduction + razorband into the rate-half lane;
   Paving_v8 consumed/supplied ledger into the upstream-interfaces note; watch
   PR #827.
8. **v3 tail**: only the boundary-divisor fence note (77485270) as optional
   background documentation; the rest superseded.

