# Wave-5 fresh-context replay audit — Codex v3 clusters not on master

Auditor session 2026-07-13. Pin: e3c08000 (worktree /home/u2470931/smooth-read-solomin/prize-codex-resolution-v3-20260712, HEAD == pin at audit start).
Master baseline: af11a7b (orbit rebuild after census-gate promotion). Catch ledger at #157 (notes/kernel_basis/KB_LOG.md); new catches #158+.
READ-ONLY discipline: no writes to either repo; all replays under tools/ramguard.

Master baselines pinned:
- P-A xr_highcore_collision_count (dag.json:5039, TARGET, key): slope COUNT <= 8n^3 per generic-branch pair, post-strip, six candidates (RowC n=1024, prize n=2^41) x rates 1/4,1/8,1/16; dihedral+extension inside; catch #42 slope-count-NOT-moment pin; per-pair sup form; quality cert in xr_smallcore_spread_count/notes/. NO node folder on master (dag-only statement).
- P-B xr_lowcore_spread_heart (dag.json:5045, TARGET, key): pairwise cores <= k-1, <= 8n^3, same pins.
- f3_h3_mobius_excess_half (dag.json:5130, TARGET, key): cutoff-18 weakened form 17*X_18 <= 300n^2, full corridor s=13..41, p = 1 mod n, p >= n^2; non-swap compiler S_ns <= 1200n^2 sufficiency; banked cutoff-35/M35 strictly stronger background routes; source-boundary audit REFUSED KSV/MSS citation close.

## Findings

### CLUSTER 1 (ray bounds)

**734c664b xr_generic_mds_kernel_ray_bound (GRK)** — PROOF SOUND (hand-traced): MDS kernel bijective on d-sets; persistent-coordinate cap h-1 via genericity (E = U minus T minus persistents has size exactly r, carries both lifts); charging map (gamma,T)->(T,x) injective since e_T(gamma)_x is a nonzero affine polynomial in gamma. |Z|C(d+h,d) <= C(R+d,d)R. Wording nit: statement's "genericity is exactly the exclusion of joint A-agreement" is one-directional (global tangent-pay exclusion IMPLIES chart genericity; chart-local E subset U is weaker) — sound in the consumed direction. verify.py REPLAYED PASS (ramguard tiny): finite exhaustion (11,2,3,1) tight at bound 3, (7,3,5,2) max 2 vs bound 5; genericity-removal mutation fires; six-candidate thresholds d<=3/3/3 (RowC), 11/10/9 (prize) re-derived — I checked the h=n/scale+1, budget-8n^3 arithmetic against master's P-A row pins: exact match. Wiring: new PROVED background node + ev edge -> P-A; P-A dag statement byte-identical; new P-A node folder (statement/contract/frontier/attack/subdag) faithful to master's dag statement, catch #42 pinned as "nonclaim", scope honestly fixed-chart ("cannot be multiplied by C(n,R+d)"). Consumer = xr_highcore_collision_count (ev only, no status change). Provenance to vendored asymptotic_rs_mca_frontiers.tex checked (thm labels exist). VERDICT: IMPORT-CLEAN.

**e3751d68 xr_direction_distance_ray_bound (DDR)** — PROOF SOUND (hand-traced): c_gamma = -gamma b_1 - z_gamma in K+<b1>, pairwise distance >= d_U(y1) (scalar multiples of y1-lifts), all in radius-t ball around b_0; s-set agreement second moment + Cauchy-Schwarz gives M <= N(d-t)/((N-t)^2-N mu) when (DD) (N-t)^2 > N(N-d); DD failure <=> d <= 2t-t^2/N = explicit sparse-direction lift (exact dichotomy). floor(...) <= N^2 since denominator >= 1, d-t <= N. verify.py REPLAYED PASS (tiny): two exhaustive charts (q=5 R=2 N=3 r=1: 12 paid directions, max 3; q=5 R=3 N=5 r=1: 104 paid, max 2); low-direction mutation is documented-arithmetic only (weak but honest); candidate table = max excess e with DD for MDS-direction charts: h^2 > e(R-2h) -> 0/0/0 RowC, 45210182/38693399/8985287 prize (I re-derived rate-1/4 prize ~ h^2/(R-2h) = 4.52e7 ✓); whenever DD holds the ray count <= N^2 <= n^2 < 8n^3 at all candidates. Wiring: PROVED background node, ev edges -> P-A AND P-B (legitimate: slope bound is core-size-agnostic). Honest per-chart scope; provenance to vendored direction_distance_large_kernel_ray_compiler.md verified (same open boundaries retained). VERDICT: IMPORT-CLEAN.

**a183775e collinearity fence** — 720-permutation symbolic fence REPLAYED PASS (ramguard local, sympy): no direct assignment of the six subgroup variables (x,x',y,y',z,w) to the classical collinear-triple determinant vanishes identically; so the published T(H) << n^4 log n bound is NOT citation-close for (LS2) without a new rational bridge. Consistent with (and strengthens) master's banked source-boundary REFUSED verdict on KSV/MSS. Scope honestly "direct coordinate permutations only". DEPENDENCY: annotates background/nodes/f3_h3_line_star_moment_identity — a node NOT on master, minted off-cluster in 15ff12ab (07-13 10:52, post-wave-4 scope). I audited the dependency: LS1 bijection (m=b'/a=b/a'; equal<=>a=a', swap<=>m=1; inverse construction) hand-verified SOUND; LS2 follows; geometric line-star reading checks (points (1-a,1-ma) on Y=1+m(X-1)); its verify.py REPLAYED PASS (tiny). VERDICT: IMPORT-CLEAN **conditional on co-importing 15ff12ab's line-star node** (statement+proof+verify+symbolic_search_result) — the fence cannot land on master without it.

Consumers & parameters (mission question): both ray bounds wire as ev into P-A (DDR also P-B) at the pin; neither claims P-A. Claimed bounds hold at the consumed parameters — GRK single-chart fits 8n^3 through excess d<=3 (RowC) / 11,10,9 (prize); DDR pays <= N^2 < 8n^3 whenever DD holds. f3_h3_mobius_excess_half is NOT a consumer of cluster 1 (the fence commit touches it only as prose in attack/frontier).

### CLUSTER 2 (C36 lane)

**43c91d91 narrowing** — Core compiler math re-derived by hand and exact: 68(m-18) <= m(m-2)+d for m>=19 ((m-34)(m-36)+d >= 0; m=35 parity d>=1; 68 sharp at 1088=34*32, 1224=36*34 = master's banked pins); truncation to P(t)>=19 keeps 68X_18 <= S_ns^rich (LHS vanishes on m<=18) and S_ns^rich <= S_ns (Q(t)>=0 as a count); 68->17 / 1200->300 divide-by-4 exact. Critical target 17X_18<=300n^2, falsifier, and IMPORT PINS r2 block preserved VERBATIM in dag statement; one sentence rewritten in place (S_ns -> S_ns^rich preferred target) — see custody note. NEW THEOREM f3_h3_shifted_product_sidon: PROOF SOUND (hand-traced): v(1-zeta^e)=2^{v_2(e)} via totally-ramified dyadic tower; two-powers-of-two sum matching; r<q case splits (2) over Q(eta)=K0+eta*K0 giving y=v, x=u; r=q case gives xy=uv, x+y=u+v (same monic quadratic); quotient uniqueness corollary; finite transfer alpha != 0 (reduction mod P bijective on mu_n since p coprime n), p | Norm(alpha), |Norm| <= 6^phi(n); (AD) split exact WITH the positive part (R-1)_+. verify.py REPLAYED PASS (local, 0.9s): exhaustive char-0 SID for n=4..32 in the exact Z[zeta] basis (folding mod X^{n/2}+1 — this is exact arithmetic, not numerics), finite collision rows (97,32),(257,16),(4289,64) nonzero (mutation control), plus aggregate identity sum Q = sum R(R-1) (validates LS1 aggregate) and the (AD) split. #96-style guard discipline RESPECTED: boundary-row S_ns^rich=720 collapse explicitly marked "route-selection evidence only... not a corridor-wide estimate" at every site; nothing consumes the empirical smallness. DRIFT NIT: attack.md renders (AD) without the (R-1)_+ subscript that statement.md and the code correctly carry — repair on import. MODAL REPLAY BY ME (fresh run ap-0mMWZNuzejB0jmgXOgfCYN + full rerun, pre-registered expectations): ALL banked constants exact — single=41778976, double=24031452 (sum 65810428=S_ns), rich_nonswap=720, rich_targets=2, X_18=4, product_max=20, X_35=0. VERDICT: IMPORT-WITH-REPAIRS (custody-level only, see surgery spec).

**bca6fc8c ledger pin** — pure hash-cell fix dafd14ab->43c91d91. IMPORT-CLEAN (fold into cluster ledger row).

**848c2977 fifth-orbit compiler** — PROOF SOUND and exact (hand-verified): U=(P+D)/2 orbit count; D<=2, D==P mod 2; P-18 <= 2(u-9) for u=ceil(P/2)>=10; C(u,5) >= 231(u-9) by induction (step (u+1)(u-9)-(u-4)(u-8)=4u-41>0); sharp at (P,D,U)=(22,0,11) both sides 4; consumer arithmetic 17*(2/231)*(34650/17)=300 exact. verify.py REPLAYED PASS (tiny; exhaustive P<=2000 with parity-valid D, uniqueness of equality case, sharp-coefficient mutation 2/232, consumer arithmetic). Boundary M_5^rich=504 = 2*C(10,5) internally consistent (two P=20,D=0,R=1 targets) and CONFIRMED EXACTLY by my Modal replay (rich_fifth_orbit_moment=504, fifth_orbit_moment=17914). New PROVED background node + ev edge; no status changes; consistent with banked cutoff-18 pins (adds an alternative sufficient route, does not displace the critical target). VERDICT: IMPORT-CLEAN.

### CLUSTER 3 (XR high-core campaign) — ANSWER: REDUCES P-A, does not close it; P-A stays TARGET/red

Object discipline verified at every step: all five commits count SLOPES (catch #42 respected — contract lists moments as "nonclaim"; atlas counts nonisolated slopes; SEL/ACG run on one-per-slope selectors); per-pair sup form (fixed received pair throughout); six candidates re-pinned identically in every verifier (A=k+n/scale+1, scales 256/256/512, budget 8n^3); generic branch = the consumer's tangent-paid dichotomy = exactly the GRK/AC/ACG transversality-genericity hypotheses (verified equivalence); dihedral+extension inside (theorems count all live slopes in the stratum); post-strip = rung cores <= k, so high-core = core exactly k <=> |E union E'| = R (complement identity verified). P-A/P-B dag statements MERGED insert-only (sentence-level diff vs master: additions only) — #104 custody law respected. All four new nodes PROVED background, ev edges only, no status changes anywhere in the clusters.

**7d3e8a01 component atlas** — PROOF SOUND (hand-traced): lex-BFS one-ray-per-slope selection (first edge keeps both endpoint rays so |U_C| >= R); genericity inherited by restriction; shortened-RS MDS; GRK per component; |Z_high| = Sigma|Z_C| exact partition. Z_high = exactly P-A's cross-slope high-core slope set (pin P1: same-slope excluded, matches "join two DISTINCT slopes"). verify.py REPLAYED PASS (local): 10,648 exhaustive one-ray systems + multiray fixture + edge-atlas-not-a-partition mutation. Honest residual: component count and excesses d_C unbounded. VERDICT: IMPORT-CLEAN.

**4c085ee0 + 66182724 all-LineRay affine-core + minimum-selector (audit as squashed pair)** — PROOF SOUND (hand-traced): A_P->F^{T_e} injectivity via kernel-distance + transversality (the beta != 0 case constructs both lifts on supp(e)); zero-minor recovery; distinct pairs have distinct errors; symmetric nonuniform Bollobas set-pair on (I_e,S_e) gives Sigma 1/C(s+wt,s) <= 1; (SEL) one-per-slope corollary; RS instantiation exact (kernel distance R+1 > r <=> A > k-1 true). Thresholds: C(d+r,d) <= 8n^3 iff d<=3, d=4 fails — re-derived at all six rows. verify.py REPLAYED PASS (local): 702 exhaustive ternary syndrome lines, all 990 selectors enumerated, transversality mutation fires. Provenance honestly declares upstream PR #737's Lean file is statement-only; proof here is self-contained (and I verified it). P9 toy diagnostics (component_profile_20260713.md): instrument's selector search traced complete (RREF-canonical memoized backtracking, anchor WLOG); explicitly evidence-only, "return 4 means >3". VERDICT: IMPORT-CLEAN (paid branch: exists a one-per-slope selector of rank <= 3 => both predicates hold; residual honestly open).

**60791913 collision-line basis ledger** — CLB1-CLB4 PROOFS SOUND (hand-traced): |W_ell| = k via affine coordinate functions on lines; restriction kernel exactly <q> (rank s-1); (s-1)-subset J determines its line (rank-(s-1) system's solution set = ell) => basis injection Sigma b_ell <= C(n,s-1); matroid exchange m_ell <= b_ell+s-2; loop rows force common-support chart R+m_ell (CLB1b); disjoint petals of size >= h give line cap L = floor(R/h) (CLB2); CLB4 self-localization (J's must be nonloop => land in the min-basis line's chart). Row classifications independently re-computed and CONFIRMED: rank-4 residual b_*=2 (RowC 1/8), b_* in {2..5} (RowC 1/16), b_*=8 (prize 1/16); RowC 1/4 and prize 1/4,1/8 closed. verify.py (pin content) REPLAYED PASS (local): Vandermonde 7-plane fixture attains Sigma b_ell = C(7,2); pair-vs-line overcount mutation; sharp loop fixture; exact row table matches. BUT: see CATCH #158 — the P_hi covering semantics. VERDICT: IMPORT-WITH-REPAIRS (#158 caveat sentences).

**e3c08000 affine-core cogirth ray bound** — PROOF SOUND (hand-traced): cogirth basis lemma (deletion/contraction + Pascal; base cases a<=1, c=1 checked); a = s-1 via syndrome-line surjectivity (affine subspace of a line containing 2 points = the line); persistent cap |U|-r-a-1 via genericity; K->F^{Z_e} injective (Delta > r); restricted-code cogirth >= Delta-r (cocircuits = minimal codeword supports); charging (T,x) receives at most one (gamma,e) — including same-slope multiplicity (same T + same gamma forces same e). (ACG): |P| C(h+a,a) <= C(N,a)(N-a) — COVERING-FREE (needs no collision structure). Full-domain paid selector ranks 4,4,3,11,11,10 independently re-derived (exact at RowC: a=3 gives 3.253e9 <= 8.59e9 at 1/4,1/8; 9.109e9 > 8.59e9 at 1/16; prize n^{a+1}/h^a arithmetic gives 11,11,10). verify.py (PIN content extracted via git show — HEAD advanced mid-audit) REPLAYED PASS: 32,280 matroids (3,960 equalities), 624 exhaustive affine families, tangent mutation; localized-chart arithmetic exact. P-B addendum + QUALITY cert addendum are the honest covering-free form (open ranks 5,5,4,12,12,11 — note the 4 at RowC 1/16, correctly weaker than P-A's claim). VERDICT: IMPORT-WITH-REPAIRS (#158 on the P-A-side "all six rows" sentences; P-B side clean).

**NET P-A STATE (the mission question):** REDUCED, two named residuals, no close:
(R1) component-ledger route: bound Sigma_C floor(C(R+d_C,d_C)R/C(d_C+h,d_C)) <= 8n^3 — component count and union excesses d_C open (toy components saturate d_C = k).
(R2) selector-rank route: paid = sigma<=3 (all rows) + sigma=4 (five rows unconditionally via full-domain ACG; RowC 1/16 only via the line handoff carrying #158's caveat) + prize sigma<=11,11,10; honest residual = every-selector rank >= 5,5,{4*},12,12,11 where {4*} at RowC 1/16 means rank-4 families whose min-rank selector hull is line-free or has all lines at b_* >= 48 with a line-uncovered member are NOT actually paid (see #158).
P-B inherits (covering-free, clean): counterexample needs min selector rank >= 5,5,4,12,12,11; QUALITY_xr_lowcore_spread_heart.md addenda import verbatim.

## CATCHES

**CATCH #158 (moderate; the only mathematical repair in the wave): RowC 1/16 rank-4 "close" has an unproven covering step.** CLB3/CLB4 bound only members ADMITTING a witness collision line inside the hull of the family they are applied to. Applied to a min-rank selector P', a selected member's exact-k partner ray is generally NOT selected, so the member may lie on no collision line of aff(P') — coverage is not automatic (the selector's own pairwise cores can all be < k while the slopes are high-core via unselected rays). Applied instead to the full family, coverage holds but the localization excess m_ell <= b_ell + s_full - 2 scales with the FULL rank (toys: s_full = k+1), destroying the arithmetic. Exact unpaid sub-case at RowC 1/16, sigma=4: min-rank selector hulls with (i) no collision line at all, or (ii) all lines having b_* >= 48 with some member line-uncovered (my sharper computation, exact: localized ACG pays covering-free up to chart size 1009 = R+49, i.e. b_* <= 47 — C(1009,3)*1006 = 171,722,775,504 <= 8n^3*C(6,3) = 171,798,691,840, while N=1010 fails; CLB3 pays covered members for b_* >= 7; so only line-free hulls or uncovered members at b_* >= 48 escape) — a structural condition not excluded by anything proved. The five other rows are UNAFFECTED (full-domain ACG is covering-free). Asymmetric internal evidence that this is a quantifier drafting slip: the P-B texts written in the same commits correctly do NOT claim the rank-4 close at RowC 1/16 (open ranks "...,4,..."). REPAIR SITES (all textual; no status changes since P-A stays red): (1) P-A dag addendum sentence "Thus selector rank four is closed on all six rows" -> "closed unconditionally on five rows (full-domain cogirth); at RowC 1/16 closed for families whose min-rank selector hull admits a collision line with core-nonloop count m <= 49 (equivalently b <= 47; localized cogirth on the R+m chart, covering-free) or whose high-core members are all line-covered (CLB3, b_* >= 7); the line-free/line-uncovered rank-4 sub-case at RowC 1/16 remains open"; (2) claim_contract.md "rank-four close: complete on all six rows" -> same scoping; (3) frontier.md "Every selector of the open family must have affine error rank at least five" -> "at least five at RowC 1/4,1/8 and 12,12,11 at prize; at RowC 1/16 at least five OR rank-four with line-uncovered hull"; (4) basis-ledger statement.md final paragraph + cogirth statement.md final sentence: add "for line-covered members" to every CLB3/CLB4-based close; (5) campaign ledger rows for 60791913/e3c08000: annotate (never rewrite — #155 precedent: annotate the record).

**CATCH #159 (minor, notation): (AD) split missing positive part in attack.md.** 43c91d91's attack.md renders S_ns = Sigma_{R>0} Q + Sigma_{t!=1} Q(R-1) without the (R-1)_+ that statement.md and both verifiers correctly carry; as written the second sum would be wrong if Q(t)>0 with R(t)=0 (not excluded by anything proved). Repair: add the subscript in attack.md at import.

**CATCH #160 (minor, custody): two in-place statement rewrites in 43c91d91.** (a) f3_h3_mobius_excess_half dag statement: one sentence rewritten (S_ns -> S_ns^rich preferred sufficient target) + title changed; critical target, falsifier, IMPORT PINS r2 all preserved verbatim; content sound (68X_18 <= S_ns^rich <= S_ns verified). (b) PROVED node f3_h3_cutoff18_nonswap_compiler statement strengthened in place (68X_18 <= S_ns; S_ns<=1200n^2 sufficient -> 68X_18 <= S_ns^rich <= S_ns; S_ns^rich<=1200n^2 sufficient); proof.md updated and covers the stronger form (verified exact). The #104 merge law prefers appended dated addenda over in-place rewrites. Repair: import both as "[NARROWING r3 (2026-07-13, wave-5 audited): ...]" appended blocks preserving the r2 text, rather than the rewritten sentences.

**CATCH #161 (import ordering): two cluster commits depend on off-cluster commit 15ff12ab (line-star node, 07-13 10:52, not on master, not covered by wave-4).** a183775e annotates it; 43c91d91's attack.md/dag delta builds on it. I audited the dependency: LS1/LS2 bijection proof SOUND (hand-traced both directions + swap/equal exclusions), verify.py REPLAYED PASS, and the Sidon verifier independently validates the LS1 aggregate (Sigma Q(t) = Sigma_{m!=1} R(m)(R(m)-1) checked on three finite rows). Import order: 15ff12ab node content -> a183775e -> 43c91d91 (+bca6fc8c folded) -> 848c2977.

PROCEDURAL NOTE (not a catch): HEAD advanced during the audit (9d058055 route_limit fence, 261bef6d mca quadratic rows — both POST-PIN, out of wave-5 scope; the route_limit.md paragraph in the cogirth result.md is post-pin content). All contaminated files re-read via git show at e3c08000; both affected verifiers replayed at pin content. Post-pin commits carry a "localized envelope route limit" claim (paid ranks 4,4,4,11,11,10 — note it upgrades RowC 1/16 to 4 wholesale) that inherits #158 and must be re-audited in wave-6 with the covering caveat in hand.

## DEFERRED VERIFICATIONS (pre-registered expectations)

1. P9 wcollision Modal diagnostics (ap-EC3OVO0F8gifH3co7fMcj9 class, n=16/k=8 toys): expectation = far profiles (50,8),(52,8),(33,8),(34,8); full/canonical-selector rank 9=k+1; exact selector search returns >3 on all four far families; the two raw near-pencil controls have rank-1 selectors pre-deletion only. Falsifier: any far family with a rank<=3 selector post-deletion (would VOID the "residual is populated" honesty datum but STRENGTHEN the paid branch).
2. Repo-wide 128/128 Modal manifest + five gates at the pin (branch infra; per-node verifiers independently replayed here, so not import-blocking).

## IMPORT SURGERY SPEC (exact, per master conventions; statements merged never replaced)

Order: [15ff12ab dep] -> a183775e -> 43c91d91+bca6fc8c -> 848c2977 -> 734c664b -> e3751d68 -> 7d3e8a01 -> 4c085ee0+66182724 -> 60791913 -> e3c08000.

1. NEW BACKGROUND NODES (folders copied verbatim from pin; dag.json node entries + PROVED status + refs as at pin): f3_h3_line_star_moment_identity (dep), f3_h3_shifted_product_sidon, f3_h3_fifth_orbit_moment_compiler, xr_generic_mds_kernel_ray_bound, xr_direction_distance_ray_bound, xr_highcore_component_union_atlas, xr_all_lineray_affine_core_bound (pin content = post-66182724), xr_highcore_collision_line_basis_ledger (statement.md WITH #158 repair 4), xr_affine_core_cogirth_ray_bound (statement.md WITH #158 repair 4; result.md WITHOUT the post-pin route_limit paragraph). Placement: background/nodes/* (matches branch; none are critical).
2. NEW EDGES (all ev): line_star->f3_h3_mobius_excess_half; sidon->f3_h3_mobius_excess_half; fifth_orbit->f3_h3_mobius_excess_half; grk->xr_highcore_collision_count; ddr->{P-A,P-B}; atlas->P-A; all_lineray->{P-A,P-B}; basis_ledger->P-A; cogirth->{P-A,P-B}.
3. P-A NODE FOLDER creation at critical/nodes/xr_highcore_collision_count/ (statement/attack/frontier/claim_contract/dependency_subdag/component_profile_20260713.md at pin content, WITH #158 repairs 2-3) + refs added to the dag node (including the existing QUALITY cert path).
4. P-A dag statement: append the 8-sentence addendum block AS AT PIN but with #158 repair 1 applied to sentence 5 and the frontier-matching caveat; master text untouched (verified insert-only).
5. P-B dag statement: append the 2-sentence addendum verbatim (audited clean); QUALITY_xr_lowcore_spread_heart.md: append the two PROVED BRANCH blocks verbatim.
6. f3_h3_mobius_excess_half: per #160(a) append "[NARROWING r3 ...: preferred sufficient target S_ns^rich = sum_{t!=1,P(t)>=19}[P(t)(P(t)-2)+D(t)]R(t) <= 1200n^2, strictly weaker; former full-S_ns target retained as strictly stronger envelope; Sidon narrowing: every non-swap record is a p-specific norm-gate accident, 0<|Norm(alpha)|<=6^phi(n); boundary row (8192, 67657729) exact: S_ns=65810428 vs S_ns^rich=720, X_18=4, M_5^rich=504 — route-selection evidence only, wave-5 re-replayed exactly]"; title change optional (old title was already stale; if titles are mutable labels, accept). Node files attack/frontier/reduction/claim_contract/source_scope_audit/verify.py: apply pin content (baselines == master verified) with #159 fix. u1_x4 QUALITY cert: apply pin edit (append-style, acceptable).
7. f3_h3_cutoff18_nonswap_compiler: per #160(b) statement.md/proof.md get the strengthened form as a dated addendum preserving the original claim text.
8. background/nodes/f3_h3_mobius_overlap_cap35/: rich_fiber_modal.py + rich_fiber_result.md pin deltas verbatim (all constants independently re-replayed on Modal by this audit).
9. background/nodes/f3_h3_line_star_moment_identity/: include classical_collinearity_no_go.md + h3_collinearity_embedding_search.py (experiments/prize_resolution/) from a183775e; f3_h3_line_star dag statement as at pin (includes both fences + S_ns^rich sentence).
10. verifier_manifest: register the 9 new verify.py + statement/proof hashes (recompute hashes on the repaired files — the #158/#159 text repairs change statement.md hashes).
11. Campaign ledger: import the pinned rows for all 11 commits with #158 annotations on the 60791913/e3c08000 rows; catches #158-#161 to notes/kernel_basis/KB_LOG.md.
12. NOT IMPORTED (out of scope / other lanes): branch status divergences packaging/petal_g1_k4_scale_reserve/petal_chart_carrying_descent (pre-existing branch divergence, wave-4+ territory); quotient_row_random_word_lower_bound, rate_half_endpoint_reduction (other waves); post-pin 9d058055/261bef6d (wave-6, re-audit with #158).
