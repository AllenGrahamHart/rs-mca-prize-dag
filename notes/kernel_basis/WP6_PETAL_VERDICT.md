# WP6 — petal_growth vs K4 (+ E22 import): REDUCTION-PARTIAL
# (2026-07-10; fresh-context adversarial replay; anti-goal upheld)

E22 IMPORT: all FOUR packets PROVED-REPLAYED (10/10 verifiers exit
0, line-by-line proof reads, byte-identical across vendor branches
and origin/main): (1) agreement cofactor equations; (2) cofactor
petal divisibility (full petal => full petal locator divides H_i);
(3) tail-coset locator algebra (L_B(X) G(X^M), top-t coefficient
invariance) — conditional on its antecedent; (4) the quotient
staircase COMPOSITE (7 lemma packets: saturation, injectivity,
cross-scale recovery, canonical form, nested-fiber residual,
generating function vs n=8 brute force, inclusion-exclusion).
FLAGS: a FIFTH branch exists (vendor-e22-minimal-scale-accounting)
— NOT replayed, must not be treated as imported; the inclusion-
exclusion weight W is a declared placeholder; the referenced node
e22_cross_scale_pricing_multiplicity EXISTS NOWHERE (reference
only).

CHAIN (petal_growth <- K4), 12 steps: PROVED/replayed at 2,4,5,7,8;
ARITHMETIC checked at 10 (staircase column 5005 = C(15,6) matches
our v13_capf_planted_lower_count binomial sublist EXACTLY — the
separate charge is proved NECESSARY; exponent <= 5.785, ~9.5 bits
slack), 12 (modulo opens); OPEN:
  G1 layer-map supply for ARBITRARY U (step 1) — upstream
     explicitly withholds ("theorems ... only when the
     decomposition supplies the stated layer maps");
  G2 SUPPORT-FORCING DICHOTOMY (step 6) — THE central bridge:
     top-band divisibility system forces staircase-or-primitive;
     packets 1-2 self-describe as its 'entry point'; F4-A2b
     empirics are evidence, not proof;
  G3 PRICING MULTIPLICITY (step 9) — codewords-per-canonical-
     support poly(n) per U; the named node is absent everywhere;
  G4 E22 <-> auxiliary-layer identification (one-page note;
     E22 allows Z cap C nonempty, layers forbid D0-agreements —
     E22 is the more general normal form);
  G5 K4 band-restriction compatibility (rides on G2).
  K4 itself = step 11 (the kernel, by design).

VERDICT: REDUCTION-PARTIAL. Not analogy — the replayed steps are
load-bearing and the planted node proves the ledger necessary;
coverage fails exactly at G2 with per-U uniformity biting twice
(G1, G3). petal_growth's kernel dependency: K4 + {G1, G2, G3}
(G2 the natural candidate to POOL with K4 into a single
'petal kernel' if upstream's L1 lane doesn't supply it).
Scope: floor = top band only; mixed-petal/below-top are separate
obligations untouched here.
