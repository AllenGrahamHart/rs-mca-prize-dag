# T-BASIS ASSEMBLY — the kernel basis of the Proximity Prize DAG
# (2026-07-10; kernel-basis program, KB log #1-#9; every claim below
#  is banked on the named node or verdict note)

## The per-red verdict table (all seven critical reds)

| red | verdict | kernel dependency (banked on the node) |
|---|---|---|
| u2c_giant_tnull_dichotomy | CONDITIONAL-CLOSE VERIFIED (the F2 campaign; f2_conditional_close) | K1 (parity summit) — and statement-level dual cover by K2 (U1: F2 IS an RK instance; RK => F2 at official rows) |
| xr_smallcore_spread_count | REDUCTION-PARTIAL (WP2) | K2 constant-exact (M6) + K2-as-pinned at 5c/5e + items M1, M3-M5, M7 (M2 resolved into K2) |
| u1_x4_direct_column_budget | REDUCTION-PARTIAL (WP3) | K2 (Face 1 CONSUMES this node — TR ~ quotient-row x4, one instance two consumers) + X1-X6 (catch #26 on U2's window) |
| dli_prime_weighted_large_block_support | REDUCTION-PARTIAL (WP4) | K2 Face 2 (identification EXACT) + Y1-Y4; crux Y2 = the joint reserve (C2''), plausibly ONE clause with Face 1's TR |
| rate_half_band_closure | ANALOGY-ONLY on K3 (WP5) | OWN KERNEL K5 (razor-band witness kernel) — no upper-bound kernel can serve (side mismatch, proved) |
| petal_growth | REDUCTION-PARTIAL (WP6) | K4 + G2 (support-forcing dichotomy, pooled as THE PETAL KERNEL) + G1, G3 |
| worst_word_challenger_pricing | ANALOGY-ONLY on Face 4, map mis-attributed (WP7, catch #27) | K2 via the SCK census door, CONSTANT-EXACT clause (ii) (pins P1/P2) + W1-W3 |

## THE MINIMAL KERNEL BASIS (k = 3)

K2* — THE ENLARGED RIGIDITY KERNEL (rk_rigidity_kernel + the
     program's pins): the census sub-schema SCK as its core (M2),
     the extended clause-(i) taxonomy (+ Luroth/net + moment-trade
     classes, X4; extension-type = toral/norm-kernel, gap-4 pin),
     the CONSTANT-EXACT clause-(ii) strengthening where slack is
     sub-bit (M6/P2 — F5 at 0.86 bits, worst_word at 0.9), and the
     JOINT-RESERVE clause at state-dependent junctions (TR = C2'',
     the U3-KB unification, posing pending). Serves: u2c (via U1),
     xr_smallcore, u1_x4, dli, worst_word.
K4* — THE PETAL KERNEL (K4 + G2): stabilizer-primitive image-fiber
     bound POOLED with the support-forcing dichotomy. Serves:
     petal_growth (with G1/G3 as named work).
K5  — THE RAZOR-BAND WITNESS KERNEL: priced deficit-side witness
     families over (R(lq), sigma*] at every razor row, uniform in
     q. Serves: rate_half_band_closure.

(K1 = the parity summit remains the ATTACK MECHANISM for K2*'s
hardest probed instance — u2c — with its complete chart, satellites
16-30. K3 = row-sharp Q sits on the EVIDENCE SHELF: an upstream
kernel whose yield here is evidence upgrades, not closures.)

## Minimality (no member conditionally closes over the others)

- K5 not from K2*/K4*: WP5's side-mismatch argument is structural —
  K2*/K4* are upper-bound/classification kernels; K5 demands LOWER
  witnesses over a band that is (if the floor is true) genuinely
  unsafe; no upper-bound theorem can supply them.
- K4* not from K2*: the planted lower count (v13_capf_planted_lower_
  count, PROVED) shows generic image-fiber bounds are FALSE without
  the separate charge; K2*'s clause (ii) at the petal band would
  itself require the G2 dichotomy to name its primitive branch —
  G2 is not an RK clause.
- K2* not from K4*/K5: different objects entirely (census/agreement
  rigidity vs per-U image fibers vs band witnesses).

## Delegation briefs (one per kernel)

- K2*: notes/kernel_basis/BRIEF_K2_ENLARGED.md (+ the WP0 import
  package, the four face verdicts, and — for the u2c instance —
  PRO_FLOOR_2_F2_SUMMIT_V4.md with the complete K1 chart).
- K4*: notes/kernel_basis/BRIEF_K4_PETAL.md.
- K5:  notes/kernel_basis/BRIEF_K5_RAZORBAND.md.

## The cross-kernel frontier (what any attacker hits first)

Two axes recur across every K2* face: (1) CONSTANT-EXACT clause
(ii) where consumers have sub-bit slack; (2) the axis-Q small-core
classification heart (5e/R_spread). Plus the joint reserve (C2''
posing) as the dli/gap1 convergence. These three items are the
sharpest formulation of "what the prize still needs" that either
program has produced.

## T-BASIS STATUS: SATISFIED (2026-07-10)

Every critical red carries a machine-verified conditional-close
(u2c) or a banked verdict with its missing kernel(s) named (the
other six — banked on the nodes themselves); the basis {K2*, K4*,
K5} is explicit and minimal per the arguments above; each kernel
carries a delegation brief. T-COLLAPSE (k <= 2) was NOT reached:
the unification checks resolved M2 and U1 INTO K2* but K4*/K5
resisted absorption on structural grounds. The program's laws held
throughout: five fresh-context replays + four self-audits, zero
false ambers, four catches (#24-#27) found and corrected.
