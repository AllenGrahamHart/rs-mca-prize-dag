# Wave-7 fresh-context replay audit — Codex v3 post-wave-6 WCL-ZONE commits

Auditor session 2026-07-13. Pin: 554144a3 (worktree /home/u2470931/smooth-read-solomin/prize-codex-resolution-v3-20260712, HEAD == pin throughout).
Master baseline: e0e3e08 (orbit rebuild after WCL-ZONE round 2); wcl F-round state 2/2 (catches #190-#204); decision 4 (two-ledger split) OWED; baseline node BLOCKED-AS-WRITTEN. Catch numbering LOCAL (w7-C1...); the banking session maps to global numbers.
READ-ONLY discipline: no writes to either repo; all pin content read via `git show 554144a3:<path>`; replays from a scratchpad mini-tree (w7_tree/) under `tools/ramguard tiny` locally and `tools/modal_run_script.py` on Modal. Replay/check driver: w7_checks.py + w7_weight5_validate.py + w7_weight5_chunk.py + w7_modal_weight4_wrap.py + w7_w4spot_modal.py (same directory).

PROCEDURAL NOTE: HEAD advanced mid-audit to 77485270 ("Fence the DLI boundary divisor elimination route") — POST-PIN, out of wave-7 scope, queued for wave-8; every file and diff in this audit was taken from `git show 554144a3:<path>` / commit-range diffs, so no post-pin contamination.

Eight commits, three clusters (all WCL-ZONE-focused, oldest first):
- CLUSTER B (terminal exclusion ladder): 52785f27 scope+fence, 26728703 weight-3, 8c21b434 weight-4, fdde5f0e weight-5 probe
- CLUSTER A (audited FIRST per brief): d35a5196 raw-ledger interface guardrail
- CLUSTER C (ell-2 ladder + Newton cutoff): 141e9246, ef6b39aa, 554144a3

Master baselines pinned:
- dli_wcl_zone_coverage (TARGET, background/nodes/ on master): dag statement carries F-ROUND 1 (catches #190-#197) + F-ROUND 2 (catches #198-#204) blocks; #190 = at official aspect N_L=256L the min window mass 16L/2^L > 1/32 iff L <= 12, so WCL-ZONE at L <= 12 is EQUIVALENT to window emptiness; #198/wz2-C1 = under the canonical dedup spec, C1' FAILS at the banked accident row (L=1, q=7937): W_cl 236 -> 8, K'_dedup = 6.5019 > 4, while the banked RAW K' = 0.246909432 < 4 survives (M2_C1PRIME_RESULT_AUDIT.md).
- C1' pose of record (C1PRIME_LEVEL_SCALED_POSE.md): W_cl = sum over primitive orbits, mass 2N*2^-w, window L+1 <= w <= L+5 — NO dedup phrase; M2 survival (12/12 rows) computed on RAW ledgers.
- dli_dyadic_k_core dag statement (r2 restatement): "W_cl the primitive signed-shift orbit ledger over L+1<=w<=L+5" — raw by definition; no node folder on master (dag-only).
- M3 CORRECTED POSE (M3_ENDPOINT_EXCEPTION_COVERAGE.md lines 81-135, imported wave-6): "W_cl(R,L) = sum_O 2 N_L 2^(-w(O)), N_L = 256L, ... after ... first-owner de-duplication" — the dedup phrase and the linear schedule BOTH live here.
- dli_marginal_baseline100_coverage (CONDITIONAL, background/nodes/): INTERFACE CAVEAT "#198/wz2-C1 ... BLOCKED AS WRITTEN until the symbols are separated"; parked pending_amber_verify.py (v1 schema, N_L=256L pins).
- MAINTAINER_DECISIONS_20260713.md decision 4: (a) which ledger each of the zone bound and the C1' allowance reads; (b) RATIFY-1..8 (load-bearing: RATIFY-4 singular-source scope, RATIFY-6 lift admissibility).
- Production-tower source of truth (BANKED, pre-dating everything here): archive/compressed_dli_lane_20260705/b2b_primitive_core/notes/pro_skew_tower_packet.md points 2/4 — "level-j skew is <= L_j = ceil(T_j/2)", "sum_j L_j = t exactly (each m <= t is uniquely 2^j * odd)", "prize t = 2^33", "level 0: 2^32+1 of the 2^40 cells"; echoed by S6_LEVEL_FACTORIZATION.md and C2PP_POSED_20260710.md ("official schedule 34 levels, Σ_j L_j = t, 33 junctions").

## Findings

### CLUSTER A (d35a5196 — raw-ledger interface guardrail) — VERDICT: IMPORT-WITH-REPAIRS (the guardrail node + attack/ledger notes); the WCL statement/dag rewrite itself is the EXECUTION of decision 4(a) and goes to the maintainer queue with a RATIFY recommendation

Content: new PROVED background node dli_wcl_raw_ledger_interface_guardrail (statement/proof/verify.py, closure "deduction") + ev edge -> wcl zone; in-place rewrites of the branch's wcl statement.md/attack.md/dag statement dropping the dedup phrase ("without multiplier-shadow or level-lift de-duplication ... raw primitive ledger ... Multiplier shadows, additive clusters, and lifts remain charged at their own level and weight"); official_terminal_attack.md raw paragraph.

THE DEDUCTION IS SOUND (hand-traced against master's own record):
1. The C1' OF RECORD reads the raw ledger — by definition, not by choice. The pose file (C1PRIME_LEVEL_SCALED_POSE.md lines 20-29) sums over primitive orbits with NO dedup; master's dyadic_k_core dag statement (both the original C1' calibration "W_cl = the row's full primitive-orbit ledger (clusters at their weights)" and the r2 restatement) says the same; PRO_DLI_CLOSE_6.md's lattice reframe is explicit: "no independence convention anywhere; multiplier shadows, additive clusters, lifts are all just lattice points, priced at their own weights." All three citations verified verbatim on MASTER (not only on the branch).
2. The banked M2 survival record is a RAW record: K' = 0.246909432 at (L=1, q=7937) with the raw ledger (W=236); wz2 confirmed "M2's banked numbers are raw by construction" (wz-C2).
3. Substituting a dedup zone bound into raw-C1' is INVALID absent a mass re-charge theorem: W_owner <= W_raw is the only free inequality (wrong direction). The guardrail's interface countermodel is exact: nine weight-21 orbits in one ownership class at ell=16 give W_owner = 1/256 <= 1/32 < W_raw = 9/256 (arithmetic replayed).
4. CONSISTENCY WITH wz2-C1 (the decisive cross-check the brief demanded): the two records are consistent PRECISELY under the raw reading. Raw C1' at q=7937 survives (0.2469 < 4); dedup "C1'" fails (6.5019 > 4). If C1''s object had been the dedup ledger, M2's own survival record would be about the wrong object. Under the raw reading, wz2-C1 is exactly what it pre-declared itself to be (NK-5): a pose-interface finding — the dedup phrase in the wave-6 M3 CORRECTED POSE was never C1''s object, and the banked raw K' stands untouched.
5. ELL-EQUIVALENCE ARITHMETIC vs #190: the guardrail's minimum window charge 2*(256 ell)*2^-(ell+5) = 16 ell/2^ell is IDENTICAL to #190's L-form; > 1/32 iff ell <= 12 (replayed exactly, w7_checks schedule). The guardrail states the equivalence at "ell in {1,2,4,8}" — this is #190 RESTRICTED to the dimensions that actually occur in the corrected production schedule (see cluster B): the schedule's dims <= 12 are exactly {1,1,2,4,8}. Consistent; and the guardrail adds the correct new observation that the raw-vs-owner distinction is VACUOUS at emptiness levels (an ownership class is nonempty iff its raw ledger is) and first bites at ell=16 (min mass 1/256).

DECISION-4(a) SOUNDNESS VERDICT (for the maintainer; the guardrail does NOT settle the decision, the maintainer does): the deduction is SOUND and decision 4(a) can be treated as SETTLED BY THEOREM **within the recorded interface**: given C1' as posed and banked (the only C1' with any F-round record), the baseline assembly's WCL-ZONE premise must be W_raw <= 1/32; the zone bound and the allowance read the SAME raw ledger, and the wz2-C1 "failure" is thereby resolved as a misapplied ledger, not a live inconsistency. What remains genuinely discretionary: (i) the formal ratification act itself; (ii) the alternative route wz2_report round-3 option (b) — re-posing C1' against the canonical dedup ledger — remains a formal option but is NOT live as written (its literal K' <= 4 form is already refuted at q=7937), so choosing (b) means designing a new allowance shape and discarding the banked M2/F-round record; (iii) a future hybrid (dedup zone bound + a separate theorem re-charging deleted mass to owners) is explicitly left open by the guardrail's own nonclaim. RECOMMENDATION BLOCK at the end of this file.

VERIFY.PY REPLAYED PASS (ramguard tiny): DLI_WCL_RAW_LEDGER_INTERFACE_PASS low_levels=1,2,4,8 owner_countermodel=1/256 raw_countermodel=9/256 negative_controls=5/5. Fail-closed: nonempty orbit list required, primitivity flags must be literal True, weight window enforced, "owner" consumer rejected. CAVEAT w7-C6: its structural pins hard-code the BRANCH file layout (critical/nodes/dli_wcl_zone_coverage/statement.md with the new raw phrasing; critical/nodes/dli_dyadic_k_core/statement.md — no such folder on master; master's wcl node lives at background/nodes/ and still carries the dedup phrase). On master as-is it FAILS. Repair below.

### CLUSTER B (52785f27 + 26728703 + 8c21b434 + fdde5f0e — tower scope repair + terminal exclusion ladder) — VERDICT: schedule correction CONFIRMED-AND-MAJOR (maintainer ratification required, custody-queued); the two scope nodes and the weight-3/weight-4 exclusions IMPORT-CLEAN; the weight-5 probe IMPORT-WITH-REPAIRS (one-sentence completeness scope, w7-C4)

**(52785f27 part 1: THE SCHEDULE REPAIR — w7-C1, the wave's central finding.)** The commit REPLACES the production schedule in the wcl statement, the baseline node, and the M3 corrected pose: master's "levels L = 1..34, N_L = 256L" becomes the "actual odd-index tower schedule" t = 2^33, j = 0..33, ell_j = ceil(floor(t/2^j)/2), N_j = 256*ell_j, dims (ell_0..ell_33) = (2^32, 2^31, ..., 2, 1, 1), sum_j ell_j = t, top generated subgroup n'_0 = 2^41.

I VERIFIED THE CORRECTION AGAINST THE BANKED SOURCE OF TRUTH: the archived skew-tower packet (verified, banked 2026-07-05) defines the levels as the dyadic-valuation partition of the moment indices m <= t (each m uniquely 2^j*odd), pins L_j = ceil(T_j/2) per level, sum_j L_j = t EXACTLY, prize t = 2^33, and the level-0 support threshold 2^32+1 — i.e., level dimension = ceil(floor(t/2^j)/2), EXACTLY Codex's formula. S6_LEVEL_FACTORIZATION.md and C2PP_POSED_20260710.md (both banked on master) carry the same pins ("official schedule 34 levels, Σ_j L_j = t, 33 junctions" — note sum(1..34) = 595 != 2^33, so the linear reading contradicts the banked sum pin outright). The identity sum_j ceil(floor(t/2^j)/2) = t and the exact dims replayed (w7_checks schedule). CONCLUSION: master's current WCL-ZONE/baseline instantiation "N_L = 256L for L = 1..34" (imported in wave-6 from the branch's own M3 corrected pose) CONFLATES the level INDEX with the level DIMENSION; the C1'-depth at tower level j is the level's dimension ell_j (its count of moment constraints), and the balanced width is N_j = 256*ell_j (the C1' pose's own aspect sentence "the production tower has N = 256L" read at depth ell_j). The dyadic schedule is the correct object. The same conflation appears in older master files (DLI_CLOSE_PINNED.md "for the 34-level production schedule, S(M) = SUM_{L=1..34} log2(1+M*256L*2^-L)"; A1_PROD_NORM_SIEVE.md "exact production instantiation Q = 2^255, N = 256L" with L-indexed windows; PRO_DLI_CLOSE_3/5; SELF_TENNIS_GOAL) — those need maintainer re-scoping, NOT auto-repair (their per-level arithmetic as a function of DEPTH is correct; only the identification of the 34 production depths with 1..34 is wrong; see w7-C1 repair notes).

CONSEQUENCES OF THE CORRECTED SCHEDULE (all verified):
- The 34 levels' dimensions are powers of two, so N_j = 2^(40-j) (j <= 32) and 256 (j = 33) and n'_j = 2N_j are ALL 2-POWERS: on official rows the ring z^N+1 is irreducible, every nonzero source is invertible, and the wz2 dedup spec's singular-source path (RATIFY-4) STRUCTURALLY CANNOT FIRE on official rows — its "OFFICIAL n'_L = 512L for every L with odd part > 1" premise inherits the schedule conflation. RATIFY-4 remains meaningful for analogue censuses only. (This defuses one of decision 4(b)'s two load-bearing items; and under the raw ruling of cluster A the dedup spec exits the official predicate entirely, remaining a census/bookkeeping tool.)
- #190's emptiness set instantiates to the five terminal level-slots ell in {1,1,2,4,8} (16*ell/2^ell > 1/32 iff ell <= 12; schedule dims <= 12 are exactly those); the first mass-counting dimension is ell = 16 (min charge 1/256). The official_terminal_attack.md table (8, 8, 4, 1/2, 1/256) replayed exactly.
- The official windows shift from max weight 39 (old reading) to w up to ell_j+5 at ell_j up to 2^32 — the corrected object is MUCH stronger at deep levels; this is an honest hardening, not a convenience (and is then closed by the Newton cutoff, cluster C).
- The baseline arithmetic is schedule-robust: 34 factors, r_j = q^{ell_j}/2^{256 ell_j} < 1 iff q < 2^256, (41/8)^34 < 2^100 via 41^34 < 2^202 (~20 bits slack; replayed exactly). The corrected baseline text/verify.py adds the dims pins (schema v2, 13 mutations incl. tower_t and dimension-list tampers).
- The exclusion gate "v_2(q-1) >= 41": necessary for ALL FOUR official rates (n = 2^41..2^44 all have 2^41 | n | q-1), i.e., the WEAKEST admissibility split — the correct conservative gate for exclusion theorems; and it implies q = 1 mod n'_j for every level (n'_j <= 2^41), so order-n'_j roots exist. The q < 2^256 cap is the strict prize field cap (also what makes r_j < 1 strict). Both directions verified.

**(52785f27 part 2: the two scope/fence nodes.)** Both IMPORT-CLEAN:
- dli_wcl_scope_guardrail (PROVED): the isolated-row strengthening of WCL-ZONE is FALSE — at (q, n', N, ell) = (65537, 512, 256, 1), omega = 3^128 = 15028 has exact order 512 and 1 + omega^95 - omega^146 = 0; primitive (weight-2 would force omega^d = +-1, d < 256, contradicting exact order); full signed-shift orbit size 512 = 2N (odd support size cannot be partitioned by nontrivial 2-power translation orbits — hand-checked); charge 2N*2^-3 = 64 > 1/32; the row satisfies all isolated C1' aspect pins but v_2(q-1) = 16 < 41. PROOF HAND-TRACED SOUND (Pocklington with F = 2^16, witness 3; all steps elementary). verify.py REPLAYED PASS (tiny; full orbit reconstructed, subsums nonzero, aspect pins, ambient split failure). Genuine and useful: it proves the production-row scope (the ambient 2^41 split) is LOAD-BEARING — any WCL proof must use q's splitting structure, and any claimed falsifier must check the ambient split.
- dli_wcl_engineered_terminal_scope (PROVED, computation): the strongest banked engineered weight-6 witness P = 1-z^33+z^40-z^136-z^143+z^145 at order 512 has EXACT norm 2q with q the displayed 256-bit prime, v_2(q-1) = 9 — it reaches the terminal split but misses the ambient split by 32 powers of two. verify.py REPLAYED PASS (tiny; recursive Pocklington for q; norm reconstruction; exact order-512 vanishing dilation found). MY INDEPENDENT CHECK (w7_checks engineered): recomputed the resultant exactly with MY OWN CRT prime set (ascending from 2^30, disjoint from the verifier's descending-from-2^31 set), Miller-Rabin on q, v_2(q-1) = 9 — all match. Honest framing preserved at every site: "not evidence that official WCL is true; they prove only that both known attacks were terminal-level constructions."

**(26728703 — weight-3 ambient exclusion at ell=1)** — IMPORT-CLEAN. THEOREM: among all C(256,3)*2^2 = 11,054,080 reduced signed weight-3 polynomials on {0..255} (counting replayed), NO cyclotomic norm Res(X^256+1, P) has a prime divisor q < 2^256 with v_2(q-1) >= 41; max v_2 below the cap = 18. Hence no official row (any of the four rates) has a weight-3 vanisher at either terminal ell=1 level: an official relation P(omega) = 0 at an order-512 root would give q | Res (omega is a root of X^256+1 mod q); primitivity is irrelevant (the exclusion covers all weight-3). SOUNDNESS ARCHITECTURE VERIFIED — the local verifier trusts NOTHING from Modal/FLINT/PARI:
- Orbit partition: affine action e -> ae+b (a odd) on full exponents in Z/512 (negative coefficient = e+256); Res is affine-invariant (Galois permutes roots; shift multiplies by (prod zeta)^b = Phi_512(0)^b = 1; global sign = (-1)^256) — hand-verified; the verifier rebuilds EVERY orbit (256 dilations x 256 shifts + sign normalization), checks canonical-key disjointness, and checks the covered sum equals the full universe 11,054,080 — an airtight partition proof.
- Exact resultants WITHOUT trust: deterministic 31-bit primes p = 1 mod 512 with product > 2*3^256, exact order-512 root per prime, product of P at all 256 odd powers; |P(zeta)| <= 3 and conjugate-pairing give 0 <= Res <= 3^256, so CRT equality is exact integer equality; a claimed-nonzero norm cannot mask a true zero (0 < norm enforced; and weight-3 norms are provably nonzero: three unit vectors cannot sum to zero at 512th roots — 3 does not divide 512).
- Factorizations WITHOUT trust: product of claimed prime powers must equal the certified norm EXACTLY; every distinct factor proved prime by a shared recursive Pocklington tree (full factorization of p-1, per-factor Fermat + gcd witnesses, trial division below 10^4, orphan-node check). Candidate scan: zero factors q < 2^256 with v_2 >= 41 enforced, max_v2 == 18 pinned.
REPLAYED FULL AND LOCAL (ramguard tiny, 28s): DLI_WCL_WEIGHT3_AMBIENT_EXCLUSION_PASS classes=254 covered=11054080 crt_moduli=14 factor_roots=439 prime_nodes=1498 max_v2=18 negative_controls=5/5. MY INDEPENDENT CHECKS: (i) 4 random classes' resultants recomputed EXACTLY with my own disjoint CRT prime set + factors re-verified (Miller-Rabin, product, v_2) — all match (w7_checks w3spot); (ii) 25 random raw signed supports each land in EXACTLY ONE listed orbit (independent coverage sample, w7_checks cover).
- WHERE "TERMINAL" SITS (brief question iii): the corrected schedule's two ell=1 levels are j=32, 33 (N=256, n'=512, window w in 2..6, both evaluated at one order-512 root each); exponent range {0..255} = the reduced ring Z[z]/(z^256+1) — exactly the right object; per #190-instantiated emptiness, closing w=3 closes one of the five weight slots that WCL-ZONE at ell=1 needs empty. Exclusion strength: covers ALL q < 2^256 with v_2 >= 41 — much stronger than the two official rows.

**(8c21b434 — weight-4 ambient exclusion at ell=1)** — IMPORT-CLEAN. Same architecture at C(256,4)*2^3 = 1,398,341,120 supports via a normalized-section compiler: every affine orbit has a representative containing {0, 2^v} (0 <= v <= 7) — normalization hand-verified (y-x = 2^v*u, u odd mod 2^{9-v}, a = u^{-1}; v = 8 impossible for non-antipodal pairs); the verifier rebuilds the 1,014,080-key section from scratch, generates each representative's complete normalized-transform set (all dilations sending some ordered pair to (0,2^v) — verified to be exactly orbit-cap-section), and proves the 24,979 orbits partition the section exactly, which with the normalization lemma covers the raw universe without enumerating it. Norm bound 4^256 (weight 4); norms provably nonzero (four distinct non-antipodal 512th roots cannot sum to zero — zero sum of 4 unit vectors forces two antipodal pairs; also = the Newton even-case in char 0). CRT over 17 moduli, numpy-vectorized with no int64 overflow (all operands < 2^31). REPLAYED ON MODAL (both verifiers, 16GiB): DLI_WCL_WEIGHT4_LEDGER_PASS classes=24979 section=1014080 max_orbit=516 crt_moduli=17 factor_records=88086 factor_roots=44599 max_v2=29 negative_controls=5/5; DLI_WCL_WEIGHT4_PRIMES_PASS roots=44599 nodes=154086 negative_controls=4/4. MY INDEPENDENT CHECK (Modal, w7_w4spot): 2 random classes' resultants recomputed exactly with my own CRT prime set; factors Miller-Rabin + product + v_2 < 41 — match.

**(fdde5f0e — weight-5 first-64 split-prime probe)** — IMPORT-WITH-REPAIRS (w7-C4). An honest FINITE theorem (statement + nonclaims are explicit: "a finite falsification-survival theorem, not an exhaustive ambient weight-5 exclusion"): the first 64 primes q = k*2^41+1 (3 <= k <= 996; the 932 other k certified composite by explicit divisors; partition of 1..996 complete; Pocklington + primitive generator per prime row) admit NO reduced signed weight-5 relation at an order-512 root. MITM completeness hand-verified: 130,560 = C(512,2)-256 legal pairs, 22,108,160 = C(512,3)-256*510 legal triples (a triple holds at most one antipodal pair), every legal five-set has a legal 2/3 split, collision legality re-checked. REPLAYED IN FULL: validate() + 5 mutation controls PASS (tiny); the pinned C++ (SHA-verified) recompiled and ALL 64 searches re-run locally in three tiny-budget chunks — every row returns pair_count=130560, triples_checked=22108160, found=false. w7-C4: the C++ filters candidate hits through a PRIMITIVITY test (proper-subsum masks 1..30), so the binary reports only PRIMITIVE relations; "no reduced weight-5 relation" is nonetheless correct on these rows because any imprimitive weight-5 relation needs a vanishing proper sub-relation of weight 1-4, and weights 2 (order/Newton), 3, 4 (ambient censuses — these 64 rows sit squarely in their scope: v_2(q-1) >= 41, q < 2^51 < 2^256) are all excluded; but proof.md's sentence "a zero relation exists if and only if the compiler reports one" silently uses that composition. One-sentence repair below.

### CLUSTER C (141e9246 + ef6b39aa + 554144a3 — ell-2 ladder + Newton short-window cutoff) — VERDICT: IMPORT-CLEAN (all three; the cutoff is the wave's headline theorem)

**(141e9246 — ell=2 weight-3 ambient exclusion)** — IMPORT-CLEAN. Order-1024 analogue of the weight-3 census: C(512,3)*2^2 = 88,954,880 supports, zero-containing normalized section (521,220 keys), 510 affine-Galois classes, CRT product > 2*3^512 (27 moduli), 1,141 factor roots via a 5,608-node Pocklington graph, zero ambient candidates, max v_2 = 21. Uses only the FIRST odd-index equation P(omega) = 0 (omega order 1024) — conservative and correct (an ell=2 vanisher satisfies it a fortiori). REPLAYED FULL AND LOCAL (tiny): LEDGER_PASS section=521220 classes=510 crt_moduli=27 ... max_v2=21 5/5; PRIMES_PASS roots=1141 nodes=5608 3/3. MY INDEPENDENT CHECK: 2 random classes, my own CRT set, exact match (w7_checks ell2spot). (Logically subsumed for the WCL purpose by the Newton cutoff one commit later — but the norm census is STRICTLY STRONGER in a different direction: it excludes weight-3 relations at the first equation alone, for every split prime under the cap, and banks the factor ledger. Keep both; the redundancy is load-bearing cross-validation, and indeed the two agree.)

**(ef6b39aa — ell=2 weight-4 Newton exclusion)** — IMPORT-CLEAN. A genuine field-uniform PROOF (no computation): p_1 = p_3 = 0 (the two odd-index equations; p_3 = sum s_i omega^{3e_i} since s^3 = s) + Newton at m=1,3 give a_1 = a_3 = 0 (char not in {2,3}: q = 1 mod 2^41 is odd and > 3); the quartic root polynomial T^4 + a_2 T^2 + a_4 is EVEN, so the four distinct nonzero roots split into antipodal pairs; antipodal full exponents differ by 512 and reduce to EQUAL exponents mod 512 — contradicting distinct reduced support. HAND-TRACED SOUND end to end (root distinctness from exact order 1024 + support range; evenness-pairing; encode step). verify.py REPLAYED PASS (tiny): fields=9 four_sets=60796 paired_hits=344 5/5 — exhaustive over 9 small fields: every 4-set with p_1 = p_3 = 0 IS antipodally paired. The retired resource note (notes/resource_route.md) is exemplary honesty: the attempted weight-4 norm-census route (22.6B supports, 100,926 classes, SAMPLE_PARTIAL with 53 timeouts) is retired as evidence-free and superseded by the proof.

**(554144a3 — the Newton short-window cutoff)** — IMPORT-CLEAN; THE HEADLINE. THEOREM (field-uniform): char 0 or char > w, omega exact order 2N, reduced signed P of weight w with distinct exponents in {0..N-1}; if P vanishes at the first ell odd powers omega^{2j-1} (j = 1..ell) and w <= 2ell, then no such P exists. PROOF HAND-TRACED SOUND: the odd-power hypotheses are exactly p_1 = p_3 = ... = p_{2ell-1} = 0 for the signed roots r_i = s_i omega^{e_i} (s^{odd} = s); induction on odd m <= min(w, 2ell-1) through Newton's identity m*a_m = sum_i (-1)^{i-1} a_{m-i} p_i (odd i: p_i = 0; even i: a_{m-i} = 0 by induction; m invertible since char > w) kills every odd elementary coefficient; odd w <= 2ell forces w <= 2ell-1 and a_w = +-prod r_i = 0 against nonzero roots; even w makes the root polynomial EVEN, forcing antipodal pairs (roots distinct/nonzero, char != 2), which reduced support forbids (antipodal full exponents reduce to equal base exponents). Root distinctness from exact order + exponent range hand-checked. OFFICIAL INSTANTIATION EXACT: q > 2^41 > ell_j + 5 >= w at every level (ell_j <= 2^32), and n'_j | 2^41 | q-1 supplies the exact-order root; ell + 5 <= 2ell iff ell >= 5, so EVERY window at ell_j >= 8 is EMPTY — thirty of the 34 levels are closed OUTRIGHT (W_cl = 0); at ell=4 only w=9 survives; at ell=2 only w in {5,6,7}; at ell=1 the cutoff also formalizes the previously "elementary" w=2 case. SHARPNESS HONESTLY FENCED: quadratic-residue subgroups of F_7/F_11/F_19 satisfy all available odd moments at w = 2ell+1 with NO antipodal pair (I verified all three by hand and exhaustively) — Newton alone stops exactly at w = 2ell. verify.py REPLAYED PASS (tiny): finite_sets=208373 paired_hits=969 residual_slots=6 boundary_controls=3 negative_controls=6/6 — exhaustive finite validation over 6 primes x 4 levels + the three sharp boundary rows must be rejected for exactly the "weight cutoff" reason + an independent residual-slot recomputation from the schedule. MY INDEPENDENT CHECK (w7_checks newton): re-implemented the whole check from scratch on FRESH primes {37, 41, 43} (disjoint from the verifier's): 13,505,889 root sets, 4,569 hypothesis-satisfying hits, ALL antipodally paired; QR sharpness 3/3.

### THE NET WCL-ZONE STATE (the brief's cluster-C question — what is now closed, what remains)

Given the corrected schedule (w7-C1), the raw ledger ruling (cluster A), and the five proved exclusions, WCL-ZONE-COVERAGE is now EQUIVALENT to six finite emptiness slots. Precisely, unconditional on every official DLI production row (q prime < 2^256, generated field, ambient split 2^41 | q-1):

- Levels with ell_j >= 8 (j = 0..29, thirty levels): window w in [ell_j+1, ell_j+5] entirely EMPTY by the Newton cutoff (w <= 2ell); W_cl(R,j) = 0. CLOSED.
- ell = 4 (j = 30): w = 5..8 empty (Newton). Residual: w = 9.
- ell = 2 (j = 31): w = 3, 4 empty (Newton; the w=3 norm census independently confirms w=3 for the first equation alone). Residual: w = 5, 6, 7.
- ell = 1 (j = 32, 33): w = 2 empty (Newton), w = 3, 4 empty (ambient norm censuses, all q < 2^256 with v_2(q-1) >= 41). Residual: w = 5, 6.

RESIDUAL (master's next target), exactly six slots, each an EMPTINESS obligation (single-orbit masses 16, 8, 32, 16, 8, 4 respectively — all > 1/32):
1. (ell=1, w=5): no reduced signed weight-5 P with P(omega) = 0, omega exact order 512. [Evidence: none on the first 64 ambient split primes — exact MITM, replayed.]
2. (ell=1, w=6): same at weight 6. [Anti-evidence shape exists: the engineered weight-6 witness vanishes at order 512 for a NON-ambient prime (v_2 = 9) — the slot is genuinely q-dependent; wz2-C3's fixed-vector exclusion says no single vector works at all official q.]
3-5. (ell=2, w=5/6/7): no reduced signed weight-w P with P(omega) = P(omega^3) = 0, omega exact order 1024. [Both moment equations should be used together — the Newton method contributes a_1 = a_3 = 0 for free at any weight; attack note prefers (2,5) first.]
6. (ell=4, w=9): no reduced signed weight-9 P with P(omega^{2j-1}) = 0, j = 1..4, omega exact order 2048. [The QR-subgroup of F_19 shows w = 9 = 2*4+1 is exactly where Newton fails — a census or a new algebraic input (the 2^41 splitting structure) is required.]

Equivalence direction verified: if all six slots are empty then W_cl <= 1/32 at every level (0 at ell >= 8 and at ell in {1,2,4} windows); conversely any nonempty slot puts a single orbit of mass >= 4 > 1/32 in a window, refuting WCL-ZONE. (At the residual slots, primitive-vs-all is immaterial: lower-weight relations are all excluded there, so any residual-slot vanisher is automatically primitive — e.g. an imprimitive (1,6) vanisher needs a vanishing proper subvector of weight 2..5; 2/3/4 are proved empty and 5 is slot 1.) The remaining difficulty is exactly the A1-PROD density-vs-row gap: the six slots are q-dependent statements at 2^255.9-scale fields that no analogue census can decide; the live tools are (a) norm-census exclusions like w3/w4 if the class counts stay feasible (the retired resource probe measured (1,5)-scale as ~100k classes AT ell=2 weight 4 — the (1,5) section is bigger; Codex's attack note honestly flags storage), (b) two-moment algebra at ell=2, (c) a genuinely new obstruction from the 2^41 splitting requirement.

## CATCHES

**w7-C1 (MAJOR, genuine — credit Codex; maintainer ratification required): master's production-schedule instantiation of WCL-ZONE/baseline is a level-index/level-dimension conflation.** The banked skew-tower packet, S6, and C2PP pin levels = dyadic valuation classes of moment indices m <= t = 2^33 with dimensions L_j = ceil(floor(t/2^j)/2) = (2^32,...,2,1,1), sum = t; master's "N_L = 256L, L = 1..34" (wave-6 import, branch-authored) plugs the INDEX into the C1' depth. The corrected object changes: which windows exist (dims are 2-powers; official rings all 2-power — RATIFY-4's singular path never fires officially), where #190's emptiness levels sit (the five slots ell in {1,1,2,4,8}), and the deep-level obligations (now weight ~2^j windows — subsequently closed by the Newton cutoff). REPAIR: this is a POSE REVISION of a TARGET node's object — master text stands, the correction lands as a dated "SCHEDULE CORRECTION r2" addendum on the wcl dag statement + node statement + M3 corrected pose + baseline node, with the maintainer's one-line ratification; annotate (never rewrite) the #190 F-round sentence with the ell-form instantiation; queue re-scoping of the older linear-schedule instantiation sections (DLI_CLOSE_PINNED S(M) sum, A1_PROD_NORM_SIEVE production-instantiation section, PRO_DLI_CLOSE_3/5 mentions) as a maintainer follow-up — their per-depth arithmetic is right, the depth-set identification is not.

**w7-C2 (MAJOR, genuine — credit Codex; resolves the wz2-C1 blocker): the raw-ledger interface deduction (cluster A) is sound.** C1'-as-recorded consumes W_raw by definition (pose file, dyadic_k_core statement, PRO_DLI_CLOSE_6 lattice frame — all verified on master); the banked M2 record is raw; substituting a dedup zone bound is invalid without a mass-transfer theorem (exact countermodel at ell=16); wz2-C1's dedup failure at q=7937 is thereby a misapplied-ledger artifact, and the banked RAW K' = 0.246909432 < 4 stands. Decision 4(a) is settled by theorem WITHIN the recorded interface; the ratification act, and the (refuted-as-literal) option of re-posing C1' against the dedup ledger, remain the maintainer's. See the recommendation block.

**w7-C3 (blocking custody, all statement-carrying commits): in-place rewrites of master-owned statements.** The branch rewrites (rather than appends to) the wcl node statement.md/attack.md + dag statement/title (twice: schedule then raw), the baseline statement/conditional/attack/verify.py (+ dag statement) and its schema (v1 -> v2), the M3 CORRECTED POSE section, and verify_m3_endpoint_contract.py (whose master version was already the w6-C7 divergence point). Master's copies have meanwhile evolved (F-ROUND 1/2 blocks in the wcl dag statement; the #198 BLOCKED-AS-WRITTEN caveat on baseline; the M3 note's catch-#181 header). #104 resolution: master text stands; the schedule + raw corrections land as dated addenda (texts in the surgery spec); the F-ROUND blocks and the BLOCKED caveat are master's records — Codex did NOT touch them (they postdate the merge base), but a naive file-level import WOULD clobber them; the caveat itself is RESOLVED (not deleted) by the decision-4 ruling if the maintainer ratifies per w7-C2.

**w7-C4 (minor, documentation; fdde5f0e): the weight-5 MITM completeness sentence hides a composition.** The pinned C++ reports only PRIMITIVE relations (proper-subsum filter, masks 1..30); proof.md's "a zero relation exists if and only if the compiler reports one" is true on the 64 rows only because weight 1-4 sub-relations are excluded there by the banked order/Newton/census theorems (the rows are inside those theorems' scope: v_2(q-1) >= 41, q < 2^256). REPAIR: append to proof.md: "[w7 audit: the search's primitivity filter discards candidates with vanishing proper subsums; on these rows that loses nothing because weight 1-4 relations are excluded by the exact-order argument and the weight-3/4 ambient censuses, whose scope (v_2(q-1) >= 41, q < 2^256) covers all 64 rows.]"

**w7-C5 (minor, mechanical): the raw guardrail's verify.py hard-codes branch paths/phrasings.** It pins critical/nodes/dli_wcl_zone_coverage/statement.md containing "without multiplier-shadow or level-lift de-duplication", critical/nodes/dli_dyadic_k_core/statement.md (no such folder on master; master's C1' text lives in dag.json), and critical/nodes/.../PRO_DLI_CLOSE_6.md (exists on master — OK). On master as-is it fails. REPAIR: at import, point the wcl pin at master's background/nodes path AFTER the decision-4 addendum lands (the addendum supplies the raw phrase), and replace the dyadic_k_core file pin with a dag.json statement check ("full primitive-orbit ledger" / "primitive signed-shift orbit ledger"); or park as verify_pending_ratification.py until decision 4 executes. Same class as w6-C10.

**w7-C6 (nit, latent): newton verify.py's ACTUAL_LEVELS is a module-level generator expression** consumed once inside residual_slots(); correct as called, but a second consumer would silently see an empty sequence. One-line repair (tuple(...)) at import; no behavioral effect today.

**w7-C7 (nit, ledger): duplicate roadmap note numbers on the branch.** The pin's PRIZE_RESOLUTION_ROADMAP.md carries two "r5" and two "r6" integration notes (52785f27 inserted "r6 DLI schedule and scope repair" out of sequence against the existing "r6 exact quadratic MCA rows"; an "r5" pair likewise). Renumber into master's roadmap sequence at import; content unaffected.

OBSERVATIONS (not catches):
- The wcl statement pins t = 2^33 / top split 2^41 — the banked prize-row tower (skew packet "prize t = 2^33"). Whether the other three official rates need their own towers (t = n/256 = 2^34..2^36?) is a PRE-EXISTING lane question not introduced here; the exclusion theorems are rate-robust regardless (they gate on v_2 >= 41, the weakest row's split, and the Newton cutoff is field-uniform), and (41/8)^k < 2^100 keeps slack through k = 42 levels.
- The ell2-w3 census and the Newton cutoff overlap at (2,3) and agree — kept as deliberate cross-validation; the census is stronger in scope (first-equation-only, all split primes), the cutoff stronger in uniformity.
- The branch's integration note r17 correctly reads master's e0e3e08 round-2 record ("Fable's round-two exact q=7937 audit makes deduplicated C1' fail, so the existing C1' route requires the raw WCL ledger") and honestly disclaims analogue-status transport. Cross-repo hygiene is good in this wave: no F-round text touched, no status flips anywhere (verified across all eight dag deltas: nine ADDED PROVED background nodes + ev edges into the wcl TARGET; wcl stays TARGET; baseline stays CONDITIONAL).

## REPLAY LEDGER (all green)

| artifact | where | result |
|---|---|---|
| scope_guardrail verify.py | tiny | PASS q=65537 orbit=512 charge=64 |
| raw_ledger_interface_guardrail verify.py | tiny | PASS owner=1/256 raw=9/256 5/5 |
| engineered_terminal_scope verify.py | tiny | PASS norm_bits=257 v2=9 5/5 |
| weight3 verify.py (FULL, incl. orbits+CRT) | tiny 28s | PASS 254/11,054,080/14/439/18 5/5 |
| weight4 verify_ledger.py | Modal | PASS 24,979/1,014,080/17/88,086/44,599/29 5/5 |
| weight4 verify_primes.py | Modal | PASS 44,599 roots / 154,086 nodes 4/4 |
| weight5 validate + 5 mutations | tiny | PASS 64 rows k=3..996 5/5 |
| weight5 C++ replay, all 64 rows | tiny x3 chunks | PASS found=0, exact pair/triple counts |
| ell2w3 verify_ledger.py (FULL) | tiny 9s | PASS 521,220/510/27/1,329/21 5/5 |
| ell2w3 verify_primes.py | tiny | PASS 1,141/5,608 3/3 |
| ell2w4 newton verify.py | tiny | PASS 60,796 sets / 344 hits 5/5 |
| newton_short_window verify.py | tiny | PASS 208,373/969/6 slots/3 boundary 6/6 |
| w7 independent: schedule identities | tiny | PASS dims/sum/#190-ell-form/41^34 pin/6 slots |
| w7 independent: Newton on fresh primes 37/41/43 | tiny | PASS 13.5M sets, 4,569 hits all paired, QR 3/3 |
| w7 independent: w3 resultants (my CRT, 4 classes) | tiny | PASS exact match |
| w7 independent: w3 coverage sample (25 random supports) | tiny | PASS each in exactly one orbit |
| w7 independent: ell2w3 resultants (my CRT, 2 classes) | tiny | PASS exact match |
| w7 independent: w4 resultants (my CRT, 2 classes) | Modal | PASS exact match |
| w7 independent: engineered norm (my CRT + MR) | tiny | PASS 2q, v2=9 |

Fail-open sweep (#137/#165 patterns): every verifier's mutation controls replayed and trip (5/5, 4/4, 3/3, 6/6 as listed); nonemptiness asserts present where emptiness would be vacuous (newton paired_hits>0; raw guardrail rejects empty orbit lists; weight censuses pin exact universe counts so an empty certificate cannot pass); no acceptance path found that cannot fail; the certificates are NOT trusted (resultants CRT-recomputed, factorizations product+primality-reproved, orbits rebuilt). The only trust surface left is the Modal generation of the CERTIFICATE CONTENTS, which the verifiers make irrelevant by design — and my disjoint-prime-set spot-checks double-cover it.

## DEFERRED VERIFICATIONS

None blocking. Optional depth: (1) full re-factorization of all 25,489 census norms from scratch (the verifiers re-PROVE the shipped factorizations; regenerating them independently would only re-derive identical data); (2) the branch's own full-suite Modal replays (138-144/144 PASS ledger entries) — branch infra, every per-node verifier independently replayed here.

## IMPORT SURGERY SPEC (exact; statements merged never replaced; per master conventions)

Order: [decision-4 ruling FIRST (it gates the statement texts)] -> B(52785f27 scope nodes + schedule addendum) -> B(w3) -> B(w4) -> B(w5, with w7-C4 sentence) -> A(guardrail node + raw addendum) -> C(ell2w3) -> C(ell2w4) -> C(newton cutoff) -> bookkeeping.

1. MAINTAINER PRE-STEP (decision 4 + schedule ratification; exact texts in the recommendation block below). Without these, import items 3-4's statement addenda stay in the maintainer queue and everything else can still land (the nine background nodes reference the wcl node only via ev edges).
2. NEW BACKGROUND NODES (verbatim from pin, PROVED): background/nodes/{dli_wcl_scope_guardrail, dli_wcl_engineered_terminal_scope, dli_wcl_weight3_ambient_exclusion, dli_wcl_weight4_ambient_exclusion, dli_wcl_weight5_first64_mitm_exclusion, dli_wcl_raw_ledger_interface_guardrail, dli_wcl_ell2_weight3_ambient_exclusion, dli_wcl_ell2_weight4_newton_exclusion, dli_wcl_newton_short_window_exclusion}/ (all files incl. notes/verify_*_remote.py launchers) + experiments/prize_resolution/{dli_wcl_engineered_norm_modal.py, dli_wcl_engineered_norm_result.json, dli_wcl_weight3_classes_modal.py, dli_wcl_weight3_classes_result.json, dli_wcl_weight4_section_modal.py, dli_wcl_weight4_section_result.json.gz, dli_wcl_weight4_prime_cert_modal.py, dli_wcl_weight4_prime_cert_result.json.gz, dli_wcl_terminal_weight5_mitm.cpp, dli_wcl_terminal_weight5_mitm_modal.py, dli_wcl_terminal_weight5_mitm_result.json, dli_wcl_ell2_weight3_classes_modal.py, dli_wcl_ell2_weight3_classes_result.json, dli_wcl_ell2_weight3_prime_cert_modal.py, dli_wcl_ell2_weight3_prime_cert_result.json.gz, dli_wcl_ell2_weight4_resource_modal.py, dli_wcl_ell2_weight4_resource_result.json.gz}. Dag: nine node entries verbatim + nine ev edges -> dli_wcl_zone_coverage. REPAIRS APPLIED AT IMPORT: (a) w7-C4 sentence appended to weight5 proof.md; (b) w7-C6 tuple() one-liner in newton verify.py; (c) w7-C5 raw-guardrail verify.py path/pin retarget (wcl statement path background/nodes/..., dyadic_k_core pin -> dag.json check) OR park that verify.py as notes/verify_pending_ratification.py with a 2-line header until the decision-4 addendum lands (banker's choice; parking preserves the file byte-identical).
3. WCL NODE (background/nodes/dli_wcl_zone_coverage/ + dag entry) — MERGE, never replace:
   a. dag statement: KEEP master's full text incl. both F-ROUND blocks; APPEND: "|| SCHEDULE CORRECTION r2 + RAW-LEDGER RULING (2026-07-13, wave-7 audited, w7-C1/w7-C2, maintainer-ratified [date]): the production schedule of record is the dyadic odd-index tower t=2^33, j=0..33, ell_j=ceil(floor(t/2^j)/2), N_j=256*ell_j, n'_j=2N_j, dims (2^32,...,2,1,1) summing to t (skew-tower packet points 2/4; S6; C2PP Σ_j L_j = t) — NOT N_L=256L at L=1..34, which conflated level index with level dimension; and W_cl is the RAW primitive signed-shift ledger (no shadow/lift/cluster de-duplication; every primitive orbit at its own level and weight, per the C1' pose of record and dli_wcl_raw_ledger_interface_guardrail). Instantiation of #190: the emptiness-equivalent levels are exactly the five terminal slots ell in {1,1,2,4,8}; first counting dimension ell=16 (min mass 1/256). All official rings are 2-power, so the wz2 dedup spec's singular-source path (RATIFY-4) cannot fire on official rows — analogue-census-only. STATE AFTER THE WAVE-7 EXCLUSIONS: Newton cutoff (w<=2ell) closes every window at ell>=8 outright (30 levels, W_cl=0) and w=2 at ell=1, w=3,4 at ell=2, w=5..8 at ell=4; the ambient censuses close w=3,4 at ell=1 for every q<2^256 with v_2(q-1)>=41. WCL-ZONE is now EQUIVALENT to six emptiness slots: (1,5),(1,6),(2,5),(2,6),(2,7),(4,9)." Title: keep master's; the branch's retitle folds into the addendum.
   b. node statement.md: keep master's; append the same addendum (branch text 52785f27+d35a5196 merged, dated).
   c. attack.md: append the branch's three additions (positive-control paragraph, engineered-scope paragraph, raw-ledger paragraph) as a dated block; update the falsifier sentence via addendum ("raw" qualifier).
   d. official_terminal_attack.md: NEW file — import verbatim at pin (554144a3 version; it is the residual bookkeeping of record).
   e. attack_surface (dag): append the residual-slot sentence (the branch's rewrite is content-correct; merge as append to master's existing sentence).
4. BASELINE NODE (background/nodes/dli_marginal_baseline100_coverage/ + dag): keep master's texts INCLUDING the #198 INTERFACE CAVEAT; append to the dag statement and statement.md: "|| SCHEDULE r2 + LEDGER RESOLUTION (2026-07-13, w7): dimensions ell_j=(2^32,...,2,1,1) (not 1..34; proof uses only the 34-factor count and r_j=q^{ell_j}/2^{256 ell_j}<1); under the maintainer's decision-4 ruling both premises read the RAW ledger W_raw and the #198 caveat is RESOLVED: the dedup ledger was never C1''s object (w7-C2; the banked raw K'=0.2469 stands), so the two-symbol collision disappears — the assembly is re-armed as written with W_cl := W_raw." conditional.md: append the branch's corrected derivation as a dated section. pending_amber_verify.py: replace with the branch's v2 (tower-pinned, 13 mutations) KEEPING the pending_amber_ name and unregistered status (it replayed conceptually in wave-6; its v2 deltas re-audited here: dims list + tower_t pins and 2 new mutations — sound), OR keep v1 and note the v2 swap for the amber ceremony. Default: swap now (the v1 pins the WRONG schedule — leaving it would enshrine w7-C1).
5. M3_ENDPOINT_EXCEPTION_COVERAGE.md (master's copy): keep all text; inside the "M3 CORRECTED POSE" section append a dated corrigendum sub-block: the corrected schedule display (t=2^33, ell_j, N_j) replacing N_L=256L, the raw-ledger sentence replacing the first-owner phrase, and a pointer to the decision-4 ruling + w7-C1/C2. Do NOT delete the original lines — strike-annotate per #155 ("[superseded 2026-07-13 per w7-C1/C2 + decision 4: schedule = dyadic dims; ledger = raw]").
6. verify_m3_endpoint_contract.py (master's copy): master's version stands (w6-C7 precedent); AFTER items 3-5 land, update ONLY its contract-string pins to match the appended texts if it greps for the superseded displays (check at banking time; master's version greps master's own M3 text, which item 5 preserves — likely no-op). REFUSE the branch's version (it asserts the branch's replaced texts).
7. Ledger/manifest/orbit: import the branch's six campaign-ledger rows (8c21b434, fdde5f0e, d35a5196, 141e9246, ef6b39aa, 554144a3) verbatim (their claims verified here) with a w7 annotation on fdde5f0e ("w7-C4 scope sentence added"); import the roadmap integration notes for all eight commits (52785f27's "r6", 26728703's "r11", and r12-r17) RENUMBERED into master's sequence per w7-C7, annotating the 52785f27 note ("schedule correction ratified [date], w7-C1"); register the nine nodes' statement/proof hashes + remote launchers in tools/verifier_manifest.json (recompute hashes AFTER the w7-C4/C6 repairs); catches w7-C1..C6 to notes/kernel_basis/KB_LOG.md under their assigned global numbers; orbit rebuild; refresh the Claude artifact + public site per the standing artifact-refresh rule.
8. MAINTAINER QUEUE (beyond decision 4): (a) re-scope the linear-schedule instantiation sections in DLI_CLOSE_PINNED.md / A1_PROD_NORM_SIEVE.md / PRO_DLI_CLOSE_3.md / PRO_DLI_CLOSE_5.md / SELF_TENNIS_GOAL.md (annotate, since these are historical Pro-thread records: "[w7-C1: the 34 production depths are (2^32,...,2,1,1), not 1..34; the per-depth arithmetic here is unaffected as a function of L]"); (b) note that wz_fround1's #190 official-instantiation sentence is annotated per item 3a; (c) optionally re-pin round-3 recommendations for the wcl node: the six residual slots replace the previous certificate-design agenda.

## DECISION-4 RECOMMENDATION BLOCK (for the maintainer — this audit's finding, not its ruling)

State after this audit: WCL-ZONE survived F-rounds 2/2; the #198 blocker is RESOLVABLE BY DEDUCTION; the node's object needs one ratified correction (schedule) and one ratified disambiguation (ledger), after which 30 of 34 levels are CLOSED by a replayed field-uniform theorem and the node equals six finite emptiness slots.

(a) TWO-LEDGER SPLIT — recommend: RULE RAW FOR BOTH. Both the zone bound and the C1' allowance read the RAW primitive ledger W_raw. Grounds (verified): C1''s pose text, master's dag statement, and the lattice reframe all define W_cl raw; the banked M2 survival is a raw record (K' = 0.2469 at the accident row); the dedup "failure" K'_dedup = 6.5019 was C1' applied to an object it never claimed; deleting shadow/lift mass without a transfer theorem is unsound (exact countermodel, 1/256 vs 9/256 at ell=16). Suggested ruling line (append to MAINTAINER_DECISIONS decision 4): "MAINTAINER RULING (4a): W_cl in WCL-ZONE and in the C1' allowance is the RAW primitive signed-shift ledger (no first-owner deletion); the dedup phrase in the M3 corrected pose is struck per w7-C2; wz2-C1 is recorded as a misapplied-ledger finding; the baseline assembly is re-armed as written." Pre-registered reversal: a future theorem re-charging deleted mass to owners may re-introduce a dedup ledger as a COMPUTATIONAL DEVICE with an explicit W_raw <= f(W_dedup) bridge; absent that theorem the raw reading stands.
(b) RATIFY-1..8 — under ruling (a) + the schedule correction these become ANALOGUE-CENSUS conventions only (no dedup enters the official predicate; official rings are 2-power so RATIFY-4's singular path is unreachable officially). Recommend: ratify the defaults as census bookkeeping (they are what wz2's module implements, 12/12 conformance), explicitly annotating RATIFY-4 and RATIFY-6 as non-load-bearing for official certificates after (a).
(c) SCHEDULE — ratify w7-C1's correction as pose revision r2 of dli_wcl_zone_coverage and dli_marginal_baseline100_coverage (one line: "MAINTAINER RATIFICATION (w7-C1): the production tower of record is t=2^33, ell_j=ceil(floor(t/2^j)/2), dims (2^32,...,2,1,1); the N_L=256L displays are superseded."). The F-round records stand as analogue-scale evidence unaffected by the official-schedule correction (rounds 1-2 probed window machinery at analogue rows, not the official schedule); #190's arithmetic is schedule-free and its instantiation is annotated, not changed.

## BOTTOM LINE

- A (raw-ledger guardrail): deduction SOUND; decision 4(a) settled-by-theorem within the recorded interface (formal ratification still the maintainer's); guardrail node IMPORT-WITH-REPAIRS (path pins); consistent with wz2-C1 exactly as the brief hoped — the dedup ledger was never C1''s object and the banked raw K' stands.
- B (terminal ladder): the schedule repair is CORRECT and MAJOR (w7-C1, confirmed against the banked skew-tower packet; maintainer ratification + custody-merge required); scope nodes and the weight-3/weight-4 ambient exclusions IMPORT-CLEAN (fully replayed, incl. my own disjoint-CRT spot-checks); weight-5 probe IMPORT-WITH-REPAIRS (w7-C4 one-liner).
- C (ell-2 + Newton): IMPORT-CLEAN; the Newton short-window cutoff is a genuine field-uniform theorem (hand-traced, exhaustively replayed, independently re-implemented) that closes 30 of 34 production levels outright.
- NET: given the two maintainer ratifications, WCL-ZONE = six finite emptiness slots (1,5),(1,6),(2,5),(2,6),(2,7),(4,9) — the sharpest the node has ever been; the six slots are q-dependent official-scale statements (the A1-PROD density-vs-row gap) and are master's next target.
