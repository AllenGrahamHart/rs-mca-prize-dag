# U1 — F2 as an RK instance (comparison note, 2026-07-10)

VERDICT: F2's dichotomy IS an RK instance in shape, and RK (with its
quantifiers pinned) IMPLIES the F2 statement outright at official
rows. K1 (the parity summit) is a MECHANISM sufficient for that
instance, not equivalent to it.

## The mapping, clause by clause
- Row/domain: mu_n subset F_q^x, n a 2-power — RK's setting exactly.
- Agreement object: t-null blocks = the z = 0 depth-t PREFIX FIBER of
  locator space (the full-ladder dictionary, banked PROVED) — the
  "deeply-agreeing codeword / aligned support" object type. F2 needs
  only the distinguished fiber z = 0; RK's "uniformly in the fixed
  word" covers all z.
- Clause (i) at F2's fiber: mu_M-coset unions ARE the multiplicative
  pullback class (ell of a coset union = g(X^M), one line). The OTHER
  taxonomy classes are PROVABLY ABSENT at j <= 3 over Z (the
  Lam-Leung descent note: M_1 = M_2 = 0 iff mu_4-coset union) and
  empirically absent in the teeth band (q = 24001 recount: all 256
  extras unstructured — no dihedral/shift/inversion members).
- Clause (ii) at official rows: FM ~ 2^{-1.05e12}, so n^B FM << 1 for
  any polynomial B => RK predicts a primitive remainder < 1 = ZERO
  extras => the n^3 budget is met with the entire tolerance unspent.
  (Deep-regime exactness, satellite 29, PROVES this in the q >> 2^n
  corner unconditionally; official rows are the opposite corner and
  need RK/K1.)

## Caveats (RK's import-time gaps apply)
RK's (B, allowance) pair and quantifier order are unpinned except in
the Face-1/TR form; the implication above uses the TR-style reading.
The unstructured teeth-band extras confirm clause (ii) is genuinely
needed (the taxonomy alone does not exhaust nulls off the deep
regime).

## Basis consequence
u2c is DUAL-COVERED: by K1 (mechanism, complete attack chart,
satellites 16-30) and by K2's F2-instance (statement-level). The
effective kernel basis is {K2, K3, K4} with K1 retained as the
attack surface for K2's hardest probed instance. T-COLLAPSE
progress: k = 3 candidates, pending WP2/5/6/7 replays.

## Data generated
q = 24001, j = 2 recount: N = 512 (W4's join VINDICATED), struct =
256 (mu_4-coset unions, all verified null), extras = 256, all
unstructured, sizes 10/14 observed. Catch #25 filed (regime
misattribution in the original W4 reading; corrected on satellite
25's statement).
