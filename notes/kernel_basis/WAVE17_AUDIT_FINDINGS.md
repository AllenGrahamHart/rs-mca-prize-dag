# Wave-17 fresh-context replay audit + master integration — v7 full-prize-resolution (HGE4 width-collapse + C36 quotient compilers + c1/c2 fiber-two + DSP8 fence + 3 Modal no-hit screens)

Auditor session 2026-07-20 (agent aab33dcaf33f786bf, 20 consolidated checks all
PASS). Catch numbering LOCAL (w17-C#). Master integration performed same session
under the forced-corrections authority; genuine choices SURFACED, not adopted.

## PINS
- **v7 pin**: prize-codex-resolution-v7-20260719, branch
  codex/full-prize-resolution-v7-20260719, HEAD pinned at audit start =
  **173cd994** ("Exclude widths above two sevenths"). (Worktree HEAD later
  advanced to ae169a11 = post-pin WIP; NOT imported.)
- **base** = wave-16 pin **cd1f6407**. 948 nodes, 2045 edges.
- **master** at integration start: **c49991ce** (wave-16 close). 948 nodes,
  **2044 edges** (1 short of base — see provenance). Range cd1f6407..173cd994 =
  **44 commits**. Master NOT an ancestor of pin (linear re-expression pattern).

### PROVENANCE (wave-16 import gaps found + fixed this wave)
Master c49991ce was behind base cd1f6407 on THREE non-wave-17 items — a
wave-16 import that dropped content:
1. **1 ev edge** `f3_h3_excess_multistar_degree_ladder -> f3_h3_dsp8_correlation_bound`
   (present at base, absent on master). RESTORED this wave (naturally, as a
   pair-absent-on-master new edge).
2. **3 UPSTREAM_IMPORT_LEDGER rows** (discrete subfield census, near-rational
   pair proximity, nondeep tangent) present at base, absent on master.
   BACKFILLED by adopting the pin ledger (master was a strict subset).
Wave-17-specific deltas are therefore measured **base->pin** (true 44-commit
delta), consistent with the audit.

## VERDICT LINE (headline)
**NOTHING CLOSES. NO CRITICAL-SURFACE STATUS EVENT.** 0 status flips (net AND
per-node). 46 new nodes, ALL PROVED, wired **ev-only** into their
TARGET/CONDITIONAL consumers. The four clusters REDUCE / route / fence; none
proves a residual bound. **The wave's one genuine new class of event is
GOVERNANCE (w17-C1): three in-tree Modal no-hit screens, each load-bearing for
a PROVED node's verify.py, under the still-maintainer-unratified sub-$1
self-authorization.**

## CLUSTER VERDICTS

### HGE4 (13 nodes -> f3_hge4_norm_gate_count) — REDUCES HARD, NO CLOSURE.
`f3_hge4_norm_gate_count` TARGET->TARGET, in-deg 3->15 (+12 ev). The push
COLLAPSES the exact-level width window:
- `nonfull_complement_third_gate`: E_h^prim(m,p)=0 when 3h>=m (sum stops at
  floor((m-1)/3)).
- `near_third_dual_gap_exclusion`: E_h^prim(m,p)=0 when 7h>=2m+1 => **live
  widths h <= floor(2m/7)** (the binding bound, 2/7 < 1/3).
- Boundary strata e=1,2 now INDIVIDUALLY PAID (linear E<=m-2; quadratic
  E<=2(m^2-1)/3; necklace debits -> 2 and (m+4)/3 via the new
  `tame_central_star_belyi_necklace_bound` helper).
- Swap branch reduced to a signed odd-Vandermonde kernel / integer-
  Nullstellensatz certificate (odd h only; empty at even h).
- **EXACT RESIDUAL** (every node disclaims it): the surviving near-third band
  **h in [4, floor(2m/7)]** for each level m|n — prove the levelwise aggregate
  `sum_h E_h^prim(m,p) <= (21/2)m^2` (sums geometrically to <14n^2 => HGE4;
  "the levelwise estimate itself is not proved") — PLUS the odd-width
  primitive-swap free-union class.

### C36 (8 nodes -> correlation_bound + official_order_template_survivor) — REDUCES.
Both consumers stay TARGET (correlation_bound in-deg 13->22 incl. the DSP8
fence; official_order_template_survivor 4->12). **The C36 amber
`f3_h3_mobius_excess_half` stays CONDITIONAL and is UNTOUCHED** (in-deg 42==42,
statement byte-identical) — a routing change from wave-16. Extends the C36'
Pareto diagonal to the twelfth endpoint (36,1), compresses candidate support
(24,534 Galois-orbit blocks, Taylor-resultant content, three-resultant screen,
cutoff ladder, n=8 oracle). Does NOT close the max-P<=24 / accident-depth
satellite (rectangles (19,18),(21,16),(25,12),(29,8),(33,4) all remain open).

### c1/c2 (23 nodes) + scalar_descent (1) -> rate_half_list_adjacent_crossing — REDUCES, NO CHAMBER CLOSED.
`rate_half_list_adjacent_crossing` TARGET->TARGET, in-deg 71->95 (+24 ev).
Nonclaim intact ("Budgets one and two are exact; the adjacent crossing remains
open for B*>=3"). Reduces the fiber-two denominator-MISMATCH branch to
constant-degree resultant/norm decision interfaces; excludes two sub-geometries
(`c2_gap_only_parity_counterexample` = a PROVED refutation of a heuristic
shortcut, correctly NOT added to REFUTED; `c2_pure_fourth_root_primary_exclusion`).
NONE of the 13 budget-3 chambers closed; the B*=3 bracket [k+2^34,3n/4] unmoved.
`list_support_distance_scalar_descent` is a VENDORED upstream import
(przchojecki/rs-mca m31_scalar_descent_equivalence.md @ 57e39fba, independently
reconstructed, ledger-attributed; M31 quartic uses 2^-100, not local 2^-128).

### DSP8 (1 node -> correlation_bound) — PROOF-METHOD FENCE.
`f3_h3_dsp8_nodal_decoration_class_discount_no_go` PROVED: an F_769 order-256
signed-disjoint witness shows no row-independent 4/9,5/9,2/3 antipodal class
discount follows from nodality/decoration/signed-disjointness/richness/
quotient-support alone. Route fence, not a DSP8 counterexample. No closure.

## GOVERNANCE (w17-C1) — THREE MODAL NO-HIT SCREENS (SURFACED, not auto-ratified)
First wave to actually exercise the maintainer-UNRATIFIED sub-$1 self-auth
(waves 13-16 launched zero). Each result.json (app_id + coverage_exact + hits=[]
+ per-shard sha256) is load-bearing for a PROVED node's verify.py, with a local
coverage/hash certificate checker (`*_check.py`) but NO local re-execution of
the 2-4M-modulus search:

| app_id | screen | ceiling | candidates/shards | hits | consuming PROVED node |
|---|---|---|---|---|---|
| ap-6KQ2mJjoE3Qkq7VaKqnxlZ | c1-parity-antiinvariant | $0.25 | 2,247,721 / 16 | 0 | ...c1_parity_frobenius_router |
| ap-Js6Im9DeoBlc0di05YG2WE | c1-parity-harmonic | $0.50 | 4,495,441 / 32 | 0 | ...c1_parity_harmonic_exclusion |
| ap-PVTrzkKlh4j1B6qDmGU1Wf | harmonic-top (2^41) | (none) | 2,247,720 / 16 | 0 | ...matched_post_field_compiler |

Wired ev-only; no red flipped on their account. Cert checkers trip under
fault-injection (audit control M4). Annotated maintainer-unratified in
notes/PRIZE_COMPUTE_REQUESTS.md; open decision = accept remote no-hit screens
under a local coverage certificate as PROVED evidence + ratify the sub-$1
policy. Two additional LOCAL sweeps (c2_normalized_small_order_census,
primary_gap_rigidity_sweep) are honest evidence-only, NOT verifier-load-bearing.

## MASTER INTEGRATION RECORD (this session)
- **+46 node folders** (all background) copied from pin 173cd994.
- **+142 edges** appended (141 wave-17 + 1 restored wave-16 ev edge).
  **3 kind-change pairs HELD as master's req** (m_sweep->list_safe,
  m_handling->list_safe, list_adjacency_closing->list_grand; w15-C1
  maintainer-ratify — NOT adopted as ev).
- **Pose-contract re-takes from pin** (reverse-consumer-pins): critical
  norm_gate_count/{verify.py,statement.md} (+markers grepped by 7 HGE4
  verify_audit.py) and rate_half_list_adjacent_crossing/{verify.py,statement.md}
  (71->95 brackets). Body files (attack/frontier/claim_contract) NOT clobbered.
- **1 tool ref** tools/c36_taylor_cutoff_reference.py copied (dangling-ref fix).
- **5 experiment packets** (3 load-bearing certs + 2 evidence sweeps) copied.
- **Ledger** notes/correspondence/UPSTREAM_IMPORT_LEDGER.md adopted from pin
  (clean superset: +scalar_descent attribution +3 wave-16-gap backfills).
- **Governance annotation** added to notes/PRIZE_COMPUTE_REQUESTS.md
  (maintainer-unratified, three-launch table). PRIZE_RESOLUTION_ROADMAP +
  compute-request RESTRUCTURE deferred (master has unique content the pin lacks;
  Codex-owned planning reconciliation, not load-bearing).
- **dag.json**: 948->994 nodes, 2044->2186 edges. Round-trip-guarded atomic write.

## LOCAL VERIFICATION (in place, on master)
- Structural validator PASS: structure, refs, acyclicity, reachability, status
  propagation. Status counts match pin exactly (PROVED 810, TARGET 68,
  CONDITIONAL 41, CONJECTURE 15, PROVABLE 27, REFUTED 26, WALL 7).
- **46 new-node battery: 92/92 verify*.py PASS** (matches audit's pin-tree
  92/92). Both reverse-consumer-pins PASS in place.
- **Blast-radius sweep of existing consumers all PASS**: adjacent_crossing 67/67,
  norm_gate_count 3/3, dsp8_correlation_bound 13/13 (incl. restored edge),
  official_order_template_survivor 4/4, mobius verify.py PASS. No existing
  verifier pins a changed in-edge set beyond the 2 re-taken poses.
- Experiment certs replay PASS (harmonic candidates=4495441 shards=32 hits=0).
- Manifest refreshed: scripts 612->704 (+92), proof_assets 863->955,
  remote_launchers 10; VERIFIER_MANIFEST_PASS. (704 vs audit's 705 = the audit
  counted the untracked post-pin WIP node; correctly excluded here.)
- **No Modal re-pin run**: the local battery + validator + full consumer sweep +
  the audit's independent 92/92 & 5/5 mutation controls give comprehensive
  in-place confirmation; wave-17 verifiers are all lightweight cert-readers (no
  heavy-MITM), so the wave-16 Modal 1-CPU timeout artifact does not recur.

## OPEN / DEFERRED
- w17-C1 governance decision (three Modal screens + sub-$1 policy) — SURFACED.
- w15-C1 kind-change req->ev downgrades — still HELD as req (maintainer-ratify).
- Roadmap / compute-request restructure divergence — Codex-owned, deferred.
- Standing harness fix (KB #107): lightweight cert-check verify.py + full MITM in
  remote_launcher for dli_wcl_weight5_first64_mitm_exclusion — still open.
