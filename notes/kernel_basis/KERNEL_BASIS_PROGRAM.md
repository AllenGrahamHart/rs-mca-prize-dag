# THE KERNEL-BASIS PROGRAM — map (draft for commitment as a goal)
# (2026-07-10; joint over OUR results and CODEX's; not yet a charter)

## Objective

Reduce the critical surface's 7 reds to a MINIMAL KERNEL BASIS: for
every red, either a VERIFIED conditional-close onto named kernel
statements (the red's companion node goes amber at full rigor), or a
documented irreducibility verdict (the red needs its own kernel —
which then joins the basis). Deliverable: "7 statements, all
conditionally closed over a kernel basis of size k" with k expected
in 3..4, plus a recycling ledger of which shared infrastructure
closed what.

SEMANTICS (the discipline that makes this honest): amber =
CONDITIONAL = a complete machine-verified proof chain with ONLY the
named kernel(s) open — the house conditional-close pattern
(f2_conditional_close is the template). Claimed consumption maps are
NOT reductions until independently replayed (catch #23: a plausible
reduction was wrong by sqrt(q^j); 8 of 9 rows passed on slack).
Method: fresh-context delegation replays (5-for-5 record), one
writer banks, falsification-first, exact arithmetic, catches logged.

## Candidate kernel basis

K1  PARITY SUMMIT (ours; node f2_growing_order_myerson): 2^{o(n)}
    alignment of eps = (-1)^{K+U} vs exp(S) — generalized
    Gauss-lemma parity cancellation. Chart complete (satellites
    16-30), brief v4 armed.
K2  RIGIDITY KERNEL RK (Codex's; NOT YET a local node — import
    task): agreement objects at FM-suppressed operating points =
    paid taxonomy + bounded primitive remainder, uniform + joint.
    Source: rigidity_kernel.md @ codex/f3-net-absorption, with a
    proved-instance table and four consumption faces.
K3  ROW-SHARP Q at depth w+1 (upstream prob:row-sharp-q; local
    support: v13_bc_l4_interior_chart_to_q's sole open input).
K4  STABILIZER-PRIMITIVE IMAGE-FIBER BOUND (upstream
    prob:capf-primitive-image-fiber; local support:
    v13_capf_planted_lower_count fixed the target's correct form).

UNIFICATION CHECKS (could shrink k):
U1  F2-as-RK-instance: t-null blocks = agreement objects; coset
    unions = the multiplicative pullback class; extras = primitive
    remainder. If exact, K1 becomes an RK instance (the deepest-
    probed one) and k drops by one.
U2  Row-sharp-Q-as-RK-instance: max-fiber control as clause (ii)
    at quotient rows (RK Face 1 territory). Plausible, unchecked.

## Work packages (one replay campaign per red)

WP1 u2c_giant_tnull_dichotomy -> K1. DONE (the F2 campaign;
    f2_conditional_close banked). Residual: U1 comparison note.
WP2 xr_smallcore_spread_count -> K2 Face 3 (spread classification:
    below-cap exceptions = taxonomy trace; AG/net, v-degenerate,
    syzygy circuits). Replay Codex's Face-3 consumption map +
    verify the observed-class census claims. RECYCLE: our F5_P9
    W-collision identity + orbit-normalization re-read of F5's
    banked constants (the F2 lesson: constants may be orbit
    multiplicities).
WP3 u1_x4_direct_column_budget -> K2 (via the h=3 conditional close,
    the closing-set conjectures, and the gap1/dli interfaces).
    Replay the closing set's three conjectures' consumption wiring;
    check which are RK instances vs independent. NOTE: dli feeds
    x4's interface (1 of 19 wired inputs) — WP3 and WP4 share
    structure.
WP4 dli_prime_weighted_large_block_support -> K2 Face 2 (evaluation
    flats / Conjecture-F residue). Replay the Face-2 map against
    dli's B-WEAK floor form (joint endpoint budget). RECYCLE: our
    census anatomy + a free deep-regime exactness theorem for DLI's
    block censuses (cyclotomic rigidity transfers).
WP5 rate_half_band_closure -> K3. The L4-interior-chart reduction
    (replayed sound, cycle 110) is the template; task = formalize
    the band determination's dependence on row-sharp Q as a
    conditional-close node + first-moment tracking replay in the
    razor slice. RECYCLE: MTM censuses for band scans.
WP6 petal_growth -> K4. The corrected L1 target is petal_growth's
    natural kernel (imgfib consumes it). Import Codex's E22 packets
    (cofactor equations, petal divisibility, tail-coset locator,
    quotient staircase) via replay — they are the petal lane's
    proved structure. Then bank the conditional-close if the chain
    reaches the K4 statement.
WP7 worst_word_challenger_pricing -> K2 Face 4 (clean-rate
    exclusion: no pair carries s+1 unpaid deep bad slopes;
    pigeonhole -> two-slope -> clause (i)). Replay the Face-4 map
    against the certified-envelope floor form. If it does not
    reach, document the gap: worst_word may need its own kernel.

WP0 (prerequisite): import RK as a local node (statement + the
    proved-instance table + the four faces, each face's map as a
    separate checkable edge), via fresh-context read of Codex's
    note + instance verifiers. RK enters as CONJECTURE; its faces
    enter as claimed edges pending WP2/3/4/7 replays.

## Recycling ledger (standing; grow as WPs run)

From F2: census-Fourier anatomy, sign quantization, ladder identity,
tower transfer (all proof-portable); cyclotomic rigidity /
deep-regime exactness (portable to any moment-condition census: DLI,
F5); orbit normalization (re-read every banked constant); MTM
censuses (n <= 32, any q); no-go fences (annealed constant, two-basis
spectral flatness — pre-close absolute routes everywhere).
From Codex: RK schema + instance table; E22 petal packets; the
closing-set conjectures; its verifier fleet conventions.
From the ledger replays: prefix rigidity (second moments at general
m), curve second moment, Veronese char-0 emptiness, planted lower
count, L4 chart (the row-sharp-Q discharge template).

## Sequencing (cheap + decisive first)

1. WP0 (RK import) — everything downstream needs it.
2. U1 (F2-as-RK comparison) — one note; may shrink the basis.
3. WP2 (F5 -> Face 3) — first true consumption-map replay; sets the
   pattern and the pass/fail bar for WP3/4/7.
4. WP5 (rate-half -> K3) — the reduction template already exists.
5. WP6 (petal -> K4 + E22 import).
6. WP3 + WP4 together (shared x4/dli interface).
7. WP7 (worst_word -> Face 4) — likely hardest; may mint kernel #5.

## Draft termination conditions (for when this becomes a goal)

T-BASIS: every critical red carries either a machine-verified
conditional-close onto the kernel basis, or a banked irreducibility
verdict with the missing kernel named; the basis is explicit,
minimal (no member conditionally closes over the others), and each
kernel has a delegation brief.
T-COLLAPSE (early exit, better): unification checks reduce the
basis to <= 2 and the DAG re-renders with effective-open = basis
size.
Anti-goal: no red is ambered on an unreplayed consumption map.
