# TARGET 3C — extraction audit (2026-07-10, fresh-context mechanical
# extraction; deliverable banked verbatim from the agent report)

See SOL_TARGET_3C_PRIMITIVE_CENSUS.md for the frozen statement.
This note preserves: (1) the per-clause citation trail (consumer
interface = u2c -> x4 req edge + b2_modp_giant_extras count;
counted face = catch-#30-corrected b2b_primitive_core; conditions =
f2_full_ladder_dictionary char-p precision; strips = x4 staircase
form + boundary-scale column L7; budget split = near-tails 2^121
+ census 2^122 + boundary <= 2^6*33 ~ 2^122.59 < 2^123, matching
f2_conditional_close); (2) the 32/32 adversarial checklist with
exact outputs (char-7 family: 32 seed blocks by MITM, guard table
sub-budget iff aspect-valid; char-3: p_3 = p_1^3 on all 11440
subsets, G1+G2 both fail; lift identity exact at scales 8/4/2;
near-tail replay 2^121.006 at t = 2^33+1 and 2^120.998 at rate-1/2
t*; F_193 primitive witness + dressing + closure checks; (97,32,2)
census = 160 exactly, complements verified); (3) the P0-P6 paid
ledger with citations; (4) the internal-closure resolution of
trade multiplicity (disjoint unions of primitive members are
members — no external charging lemma needed for the count).

## THE INCONSISTENCY LEDGER (I1-I7, flagged not silently resolved)

I1 prize-max N: prose says n ~ 2^40 in several F2 notes; the
   consumer budget n^3 = 2^123 pins N = 2^41. FIX: annotate.
I2 official t: 't ~ 7e10' (base-field reading, log2 p ~ 31) vs
   t* = 8.59e9 (generated-field reading, log2 q ~ 256) — same
   window bits; THE OFFICIAL FACTORIZATION q = p^k IS PINNED
   NOWHERE. Admissible prize-max sliver: log2 Q in
   [255.9113, 256) — WIDTH 0.089 BITS. This is upstream's own r2
   'prize band underdetermined' made quantitative. MAINTAINER-
   LEVEL: candidate upstream flag.
I3 the '~2%' sub-balance prose is wrong in BOTH directions; the
   truth is the 0.089-bit sliver. FIX: annotate u2c.
I4 'q-free' vs 'p-free': the dictionary's extension-field-verified
   addendum says p-free (p = char). 3C freezes p-free. FIX:
   wording on the catch-#30 face.
I5 b2b_near_tail_bound says 'at official parameters' but the proof
   is at t = 2^33+1 and FAILS at lower-rate t* (exact: 2^125.4 /
   2^133.9 / 2^144.2) — harmless only because those rows fail G1
   under |F| < 2^256. FIX: scope caveat on the node.
I6 L7's (M_0, R) rate labels inconsistent with xr's t* table;
   (2^33, 256) is correct at rate 1/2. FLAG only (archive doc).
I7 x4 statement.md still prints the stale n^2 target (known).
