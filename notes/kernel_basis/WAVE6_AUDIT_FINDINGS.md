# Wave-6 fresh-context replay audit — Codex v3 post-pin commits

Auditor session 2026-07-13. Pin: 7e4be37f (worktree /home/u2470931/smooth-read-solomin/prize-codex-resolution-v3-20260712, HEAD == pin throughout the audit).
Master baseline: 465a9fd (orbit rebuild after clause (P) banking); wave-5 imports at 4d30ace (repairs #158-#161); dli amber surgery at 561f729 (#165 tether + #162 deletion + #164 fix). Catch numbering here is LOCAL (w6-C1...); the banking session maps to global numbers.
READ-ONLY discipline: no writes to either repo; replays under tools/ramguard tiny (none needed Modal; the two >tiny scripts are evidence-only and left DEFERRED). Replay driver: w6_checks.py (same directory).

Nine commits, four clusters (topological note: the true commit topology interleaves canonical-master merges cf7cbbdc/124f27ec; e909175d actually PRECEDES d80d448b — every diff below was taken against the commit's real parents, and all content statements refer to PIN content):
- CLUSTER A: 9d058055 "Fence the localized XR envelope route"
- CLUSTER B: 261bef6d "Add four exact quadratic MCA prize rows"
- CLUSTER C: 3e7e0e7e "Decompose the DLI endpoint non-circularly" + 3ec0f716 "Repair the DLI endpoint decomposition" (audited as a pair)
- CLUSTER D: d80d448b, 4d073683, e909175d, dce84024, 7e4be37f (C36 norm-cutoff ladder)

Master baselines pinned:
- xr_affine_core_cogirth_ray_bound imported at 4d30ace WITH catch #158 scoping (RowC 1/16 rank-4 close conditional; line-free/line-uncovered min-rank selector hulls open).
- f3_h3_mobius_excess_half (TARGET, critical): r3 NARROWING addendum (S_ns^rich <= 1200n^2 preferred, rich locus P(t)>=19); background sidon (norm gate 0<|Norm(alpha)|<=6^phi(n)), cutoff18 compiler, fifth-orbit compiler, overlap_cap35 all on master. Banked boundary-row pins (8192, 67657729): S_ns=65810428, rich_nonswap=720, rich_targets=2, X_18=4, M_5^rich=504.
- dli_prime_weighted_large_block_support (TARGET, critical): M4 assembly BANKED on master (561f729): m4_assembly_verifier.py live with #165 reserve-credit tether + MUT-8, catch #162 duplicate deleted, #164 display fixed; verdict "ASSEMBLY-VERIFIED-CONDITIONAL (on C2'', C1', ENDPOINT-EXC as named predicates) — never unconditional while any predicate lacks a maintainer countersign"; amber awaits ONLY M5 (maintainer one-liner; notes/M5_CONFIRMATION_2P121.md does NOT exist); F-round state C2'' 1 / C1' 1 / ENDPOINT-EXC 1; catch #108 /33 junction display; verify_m3_endpoint_contract.py enforces "dli_stays_target".
- mca_grand (CONDITIONAL): "for each admissible C exhibit adjacent a with B_C(a-1) > floor(q_line/2^128) >= B_C(a), all conventions printed"; clean rates via wired premises, rate 1/2 additionally conditional on rate_half_band_closure (TARGET, razor slice log2 q in (255.900,256)).

## Findings

### CLUSTER A (9d058055 — localized XR envelope route fence) — VERDICT: IMPORT-WITH-REPAIRS

Content: route_limit.md (finite-extrema reduction for the exact minimum-basis envelope E_s(b) = min(floor(C(N_b,a)(N_b-a)/D), L*floor(C(N_b,a)/b)), N_b = min(n, R+b+a-1), D = C(h+a,a), L = floor(R/h)); verify.py gains highcore_overlap_rank + highcore_local_envelope_rank; result.md paragraph; new ledger row + a hash-pin fix ("this commit" -> e3c08000) on the wave-5 cogirth row.

FENCE MATH SOUND (hand-traced): f(b) = C(R+b+a-1,a)/b has ratio f(b+1)/f(b) = (R+b+a)b/((R+b)(b+1)), so f decreases while (a-1)b < R and increases after — the route_limit monotonicity claim is exact; the unfloored cogirth term is strictly increasing pre-saturation (constant after saturation b >= k-a+1, re-derived); the ratio (cogirth)/(line) = (N_b-a)b/(DL) is strictly increasing pre-saturation, so the two terms cross at most once, exactly at b(R+b-1) = LD; g<h implies floor g <= floor h, so floors preserve the segment structure and max_b min(g,h) is attained in {1, crossover-1, crossover, saturation, C(k,a)} — precisely the verifier's candidate set; post-saturation g is constant and h nonincreasing, max at the saturation endpoint (included).

REPLAYS (all ramguard tiny):
1. verify.py at pin (byte-identical from 9d058055 through 7e4be37f, checked): PASS, 5.3s — matroids=32280 equality=3960 affine_lines=624 max_pairs=5 (all match wave-5 pins) + new columns PA_s<= / local_PA_s<= = 4,4,4,11,11,10 = the route_limit Modal claim (ap-3SsE1JL5IiiOkotzT89CcR).
2. w6_envelope_brute.py (wave-6 original): both new functions validated against full scans over EVERY b in 1..C(k,a) on 7278 parameter sets (n<=39, k<=11, 4 reserves, 6 budgets): 0 mismatches. The finite-extrema reduction is exact.

SEMANTIC MAP verified: overlap route = CLB line charge (global C(n,a) basis injection + petal line cap L) OR localized cogirth ACG on the R+b+a-1 chart; the line_threshold floor algebra (L*floor(C/b) <= budget iff b >= C//(quotient+1)+1, quotient = budget//L) is exact in both divisibility cases; the RowC 1/16 numbers reproduce catch #158's sharper computation exactly (chart 1009 = R+49 pays; 1010 fails).

#158 INHERITANCE (as pre-flagged by wave-5): BOTH new functions PRESUPPOSE the unproven covering step — every member either line-paid or chart-paid at the min-basis line's localization — exactly what #158 says is NOT proven for line-free / line-uncovered min-rank selector hulls at RowC 1/16 rank 4. Two directions:
- NEGATIVE (the actual fence: this arithmetic cannot pay rank five on any row) — SOUND and unaffected: the envelope is evaluated OPTIMISTICALLY (granting covering), so its failure at rank 5 kills the route a fortiori. Legitimate, useful route-kill.
- AFFIRMATIVE — route_limit.md "These are exactly the already-paid ranges after the existing rank-four handoff", result.md "returned the same paid P-A selector ranks 4,4,4,11,11,10", and the ledger row repeat the PRE-repair wholesale rank-4 close at RowC 1/16; on master that close is #158-scoped. The fence also neither pays nor fences the #158 residual (line-free hulls escape the envelope entirely): "this arithmetic iteration is exhausted" is true only FOR THE COVERED BRANCH. See w6-C1.

### CLUSTER B (261bef6d — four exact quadratic MCA prize rows) — VERDICT: IMPORT-CLEAN

Content: new PROVED background node mca_quadratic_prize_rows (statement/proof/verify/result/upstream_crosswalk), dag node + ONE ev edge -> mca_grand, roadmap note r6, ledger row, manifest. mca_grand's own dag statement untouched.

THEOREM (QMS) PROOF SOUND (hand-traced, all five parts):
1. Form equivalence (n-r)^2 >= n(k+r) iff F(r) = r^2 - n(3r-(n-k)) >= 0: exact.
2. Large-overlap collapse: overlap >= k+r interpolates (c_0,c_1); every witness support meets the full common support C_0 (c >= k+r) in >= a+c-n >= k coords, MDS forces c_v = c_0+v*c_1; coords outside C_0 are nonzero affine functions of the slope, <= 1 finite root each; noncommonness forces >= max(1,a-c) cancellations (>= 1 because S_v inside C_0 would be commonly explained by (c_0,c_1)); floor((n-c)/max(1,a-c)) <= r+1 in all three branches (c >= a; c = a-1 exactly r+1; x = a-c >= 2 gives (r+x)/x <= r+1).
3. Mean-overlap forcing at N = r+2: sum_x C(d_x,2) <= C(N,2)(k+r-1) vs Cauchy-Schwarz >= (N^2(k+r)-Na)/2 under the quadratic; the gap is N(k+3r+1-n)/2 > 0 iff 3r >= R; text/code use 3r > R and the deep branch covers 3r <= R — overlapping at 3r = R, no gap.
4. Deep branch (3r <= R): residual pair supported on T = E_1 union E_2 (|T| <= 2r); any further bad slope's difference codeword has weight <= 3r <= R < d = R+1, hence 0; |T'| >= r+1 case: #bad*(|T'|-r) <= |T'|, and t/(t-r) is decreasing so <= r+1 (sharp at |T'| = r+1); |T'| <= r case: the 2r <= R collapse plus one T'-coordinate per slope (two slopes sharing x force e_1(x) = e_0(x) = 0), so <= r.
5. Lower bound (universal, needs only r <= R-1): r+1 independent dual-MDS parity columns; s_1 = sum h_i, s_0 = -sum gamma_i h_i; the combined syndrome at gamma_i lies in the span of the other r columns while s_1 does not; noncommon size-(n-r) witnesses at r+1 distinct slopes.
Wording nit (w6-C3): witnesses are selected "of size exactly a", which trimming does not justify — but the whole argument goes through verbatim with |S_z| >= a (Cauchy-Schwarz only improves; |S_v \ C_0| >= a-c still holds), so no gap.

THE FOUR ROWS: each p is DESIGNED so B = floor(p/2^128) lands exactly on the quadratic boundary: F(B-1) >= 0 > F(B) (replayed; e.g. r1_16: R = 15*2^40, quadratic root 6.2336e12 vs B = 6233898019554). F is decreasing on [0,B], so (QMS) holds at every r <= B-1 (numerator r+1 <= B, safe: B/p < 2^-128 needs remainder > 0 — CHECKED); r = B unsafe via the universal floor (>= B+1); monotone above. Safe real set exactly [0, B/n), largest grid radius (B-1)/n. This is precisely mca_grand's demanded shape: adjacent a = n-B+1 with B_C(a-1) >= B+1 > floor(q_line/2^128) = B >= B_C(a), both conventions printed.

PRIZE-SPEC ELIGIBILITY verified: k = 2^40 (<= 2^40), |F| = p in 167..171 bits (< 2^256, checked), L = order-n subgroup, n = 2^41..2^44 (smooth power-of-two), rho in {1/2,1/4,1/8,1/16}; spec allows per-row partial results. These are genuinely admissible instances — the first full exact MCA determinations on eligible rows at all four official rates. NOT the frozen near-capacity corridor rows nor the ~256-bit rate-half razor row — stated honestly in statement, crosswalk, and roadmap; no list claim; deliberately NO edge to rate_half_band_closure (right call).

VERIFIER REPLAYED PASS (tiny, <1s): proof_rows=32654, toy_max=(1,2), all four B exact — matches banked Modal ap-ch5PkckXFfHvrs8p6qHR03. Quality: Proth certificates exactly replayed (sufficient for primality); n | p-1; p < 2^256; exact divmod with 0 < remainder (load-bearing and present); adjacent signs against PINNED per-row constants (not fail-open); proof arithmetic exhausted for n <= 80 (32654 rows: equivalence + strict overlap gap when 3r > R + collapse floor over every c); exhaustive syndrome toy (n=4, k=1, q=5, radii 0/1, all 15625 pairs) asserts numerator max == r+1 EXACTLY (upper bound AND tightness; nonempty by construction, no #137 vacuity); 3 mutation controls. No fail-open path found.

PROVENANCE: upstream rs-mca@9262f63c exists locally (/home/u2470931/smooth-read-solomin/rs-mca, "Integrate threshold PR wave"); all five cited labels present in experimental/rs_mca_thresholds.tex at pin (lines 410/1195/1286/1527/1853). Node proof self-contained and independently verified here.

### CLUSTER C (3e7e0e7e + 3ec0f716 — DLI endpoint decomposition pair) — VERDICT: SPLIT — IMPORT-WITH-REPAIRS for the decomposition core (new nodes, circularity finding, implication verifiers); REFUSE the status flips, statement replacements, M5 self-settlement, and the retirement of master's repaired M4/EEX interfaces

WHAT THE PAIR DOES (dag level): adds critical nodes dli_c2pp_joint_reserve (TARGET: X(R) <= 2^21 A(R), A = prod_j E_U[rho_j], X = q^{-t+H} W_cen), dli_marginal_baseline100_coverage (CONDITIONAL: C1' at all 34 production levels + W_cl <= 1/32 ==> A <= (41/8)^34 < 2^100), dli_wcl_zone_coverage (TARGET: W_cl(R,L) <= 1/32 at every official row and level, W_cl = the C1' primitive signed-shift ledger over L+1 <= w <= L+5, N_L = 256L); rewires dli_dyadic_k_core (C1') from ev->dli to req->baseline and dli_level_factorization from ev->dli to ev->c2pp; adds req edges {c2pp, baseline} -> dli; FLIPS dli TARGET -> CONDITIONAL and level_factorization TARGET -> REFUTED; REPLACES the dag statements of dli, dyadic_k_core, level_factorization; rewrites M3_ENDPOINT_EXCEPTION_COVERAGE.md wholesale; retires eex_verify.py and m4_assembly_verifier.py (headers stamped RETIRED, entry points exit 2); edits QUALITY.md/REPOSE_B_WEAK.md/C2PP_POSED/eex_report/x4 packet; adds node verify.py programs; deletes the "maintainer confirmation owed" M5 markers.

(iii) THE MATHEMATICAL CORE IS A GENUINE ADVANCE ON MASTER'S M3 (the widest dli gap):
- CIRCULARITY FINDING CONFIRMED (w6-C4): master's ENDPOINT-EXC certificate item 6 is "an exact rational final check against the binding endpoint 2^121", and soundness is ACCEPT ==> X(R) <= 2^121. Hence ENDPOINT-EXC-COVERAGE ("every official row admits an accepting certificate") IMPLIES B-WEAK outright and is at least as strong — as a decomposition target it is circular (proving coverage = proving B-WEAK row-by-row, plus exhibitability). Master's planned amber ("conditional on C2''/C1'/ENDPOINT-EXC") is thereby conditional on a predicate that dominates the node's own claim — logically near-vacuous as a decomposition, though still valuable as a checker. This is a correct and important observation that master's M3 note ("soundness without coverage is only a conditional checker") gestured at but did not state.
- THE REPLACEMENT DECOMPOSITION IS SOUND AND NON-CIRCULAR (hand-traced): C2'' gives X <= 2^21*A; baseline gives A <= 2^100 from C1' + WCL-ZONE at the official schedule: r_L = q^L/2^{256L} < 1 (strict, from |F| < 2^256), E_L <= 1 + 4*1*(1+1/32) = 41/8 (E_L >= 1 >= 0, so products behave), A <= (41/8)^34 < 2^100 via the exact integer comparison 41^34 < 2^202 (true with ~20 bits margin: 41^2 = 1681 < 2^11 so 41^34 < 2^187; measured usage 80.16 bits); product X <= 2^121 = the exact consumer statement. Neither predicate mentions 2^121. The three ultimate open leaves become C1', WCL-ZONE, C2'' — each strictly weaker than B-WEAK, each independently attackable, WCL-ZONE cleanly falsifiable (one row+level with a certified partial ledger > 1/32 refutes; completeness needed only for the upper bound).
- (ii) CONVENTION CONSISTENCY CONFIRMED: 34 production levels (= master's M4 "tau_L = 2^-5 at all 34 levels"); tau = 1/32 = 2^-5 identical; 21-bit AGGREGATE joint reserve (catch #40 = 21 not 22, respected); /33 junction convention untouched (the aggregate form is primary; the only /33 display edit is 1.555 -> 1.554 = master's catch #164, ALREADY fixed on master — dedupe at import); 2^121 endpoint exact. WCL-ZONE's ledger definition matches C1PRIME_LEVEL_SCALED_POSE.md exactly (window L+1..L+5, N_L = 256L, first-owner de-duplication). Predicate relocation audit: ENDPOINT-EXC items 2/3 -> WCL-ZONE certificate duties, item 4 (residual near-peak) -> inside the C1' inequality itself (w_max(L) = L+5 truncation is part of the C1' claim), item 5 -> the C2'' node, item 6 -> eliminated (that was the circularity). Coherent; nothing dropped.
- (i) NO CIRCULARITY WITH MASTER'S M4 ASSEMBLY: same arithmetic core, different predicate packaging; the branch implication is strictly more honest as a decomposition. Shared (pre-existing, NOT introduced here) wiring question: C1' is posed for generated rows with q = 1 mod n', and the official instantiation rests on the pose note's "the production tower has N=256L, so it lies inside the posed aspect range" — identical exposure in master's banked M4; recorded as an observation, not a catch.

VERIFIERS REPLAYED (tiny, scratchpad mini-tree at pin content): dli node verify.py PASS "DLI_M4_ASSEMBLY_PASS boundary=2^121 negative_controls=9/9 premises=baseline100+C2pp21"; baseline verify.py PASS "DLI_BASELINE100_ASSEMBLY_PASS factor=41/8 aggregate_bits=(80,81) negative_controls=11/11". Both are exact-rational, fail-closed (schema whitelisted, universal row-set matching, float premise rejected, over-budget rejected, boundary values exact: (41/8)^34 reconstructed and bracketed in (2^80, 2^81)). Neither has a #165-style acceptance path that cannot fail. CAVEAT w6-C10: their structural dictionaries HARD-CODE the branch statuses/edges (dli CONDITIONAL etc.) and would fail on master's wiring.

WHY THE WHOLESALE IMPORT IS REFUSED (each independently blocking):
1. w6-C5 statement custody: the dli dag statement (title + full text) is REPLACED by a 5-sentence summary — deleting the B-WEAK re-pose derivation pointer, the pre-registered falsifier standard, the nine-round + survival history, catches #13/#40/#108/#162, the F-round state, and the evidence-lane law. dyadic_k_core and level_factorization statements likewise replaced. Master's dli statement has meanwhile APPENDED the M4 banking + #164 + #162-executed + #165-repaired blocks — a three-way conflict whose only #104-legal resolution is: master text stands, branch content arrives as a dated addendum.
2. w6-C6 unauthorized status flips: dli TARGET -> CONDITIONAL front-runs the amber ceremony. The node's own pinned law: promotion of any route to a conditional close requires surviving adversarial rounds (F-round rule) + master's M4 verdict requires maintainer countersigns; WCL-ZONE is a brand-new predicate with ZERO survived rounds; the branch's own m4_amber_statement_draft.md (which master banked) says "The conjecture node B-WEAK itself KEEPS its user-posed CONJECTURE/TARGET status... the amber is a route-level close, not a truth upgrade", and its re-surgery criterion 6 classifies silent upgrades as custody incidents. level_factorization TARGET -> REFUTED is defensible bookkeeping (the F-round verdict "frozen form REFUTED" is long-recorded, and the successor now has its own node) but is master's ceremony to perform — queued as a maintainer decision, not imported.
3. w6-C7 stale retirement of repaired interfaces: eex_verify.py and m4_assembly_verifier.py are stamped RETIRED/exit-2 on the ground that #165's reserve-credit path is fail-open — but master REPAIRED that path (561f729: credits tethered to coset/accident-owned bucket mass, MUT-8 regression 10/10) AFTER the branch's merge base. "The repaired DAG eliminates that interface rather than attempting to tether the credits" is factually stale as a description of master. Importing the retirement would clobber a live, audited, banked verifier. Also verify_m3_endpoint_contract.py inverts master's "dli_stays_target" guard into "dli_is_conditional".
4. w6-C8 M5 self-settlement: REPOSE_B_WEAK.md "Maintainer confirmation owed as a one-line ruling" -> "M5 is confirmed directly by that corrected consumer identity"; C2PP_POSED "maintainer confirmation owed" -> "confirmed directly from the corrected two-sided consumer identity"; node statement.md "M5 is settled by the corrected consumer formula". No M5_CONFIRMATION_2P121.md exists; M5 is a maintainer act by master's banked M4 report (section 3, exact required text). An agent cannot self-discharge it. The underlying argument (the consumer's corrected face already displays 2^121) is a fair input to the maintainer's ruling — but is not the ruling.

WHAT DOES IMPORT (with repairs — see surgery spec): the two new pose nodes as route-lane TARGETs, the baseline implication node, the circularity finding as dated addenda to M3/QUALITY/REPOSE, the corrected M3 zone contract as an appended section, the x4 2^122 display staleness fix as an annotation (w6-C9), and the two implication verifiers parked pending the amber ceremony.

### CLUSTER D (C36 norm-cutoff ladder) — VERDICT: IMPORT-CLEAN (all five commits; one ledger-row fold, w6-C11)

**d80d448b + 4d073683 (norm cutoff, audited at pin = sharpened constant 6)** — new PROVED background node f3_h3_rich_fiber_norm_cutoff: P(t) >= 19 ==> p <= 6^{n/4}; hence X_18 = 0 on the complete large-field branch p > 6^{n/4} and the mobius open range shrinks from p >= n^2 to n^2 <= p <= 6^{n/4}. PROOF SOUND (hand-traced end to end):
- Folding (1): zeta^{re} = epsilon(e) zeta^{r bar(e)} for odd r (zeta^m = -1) — exact; v_E in Z^m collects the three signed coefficients of q_E = zeta^{i+j} - zeta^i - zeta^j.
- Odd-conjugate Parseval (3): the Galois conjugates of Q(zeta_n), n = 2^s, are exactly the m = n/2 odd residues; sum over odd r of zeta^{rd} = [n | d]*n - [m | d]*m = 0 for 0 < |d| < m — orthogonality exact; constants cancel so (3) uses v_E - v_F.
- Classification (5): ||v||^2 > 3 forces two same-coordinate same-sign terms; the negative pair combine only for i = j; positive-with-negative only for zeta^{i+j} = -zeta^i i.e. j = m (symmetrically i = m); unique norm-9 vector at i = j = m; all other exceptional vectors <= 5.
- Packing: P(t) >= 19 gives >= ceil(19/2) = 10 distinct unordered representations (D <= 2 diagonal since a^2 = t has <= 2 roots; <= 1 pair contains m since 1-g^m = 2 forces the cofactor t/2); so >= 7 vectors have norm^2 <= 3. Coordinate sums are odd (epsilon(i+j) - epsilon(i) - epsilon(j) = odd), so pairwise squared distances are EVEN; if all 21 exceeded 6 they'd be >= 8, but the centroid identity caps sum_{r<s} ||v_r - v_s||^2 = 7*sum||v_r||^2 - ||sum v_r||^2 <= 147 < 168 — contradiction; some pair has distance^2 <= 6.
- Norm transfer: alpha = beta_E - beta_F != 0 by the master-banked Sidon theorem; both betas reduce to t under zeta -> g, so alpha in P and p | Norm(alpha) with |Norm(alpha)| >= p; AM-GM on (3): |Norm|^{2/m} <= (1/m) sum |sigma_r(alpha)|^2 = ||v_E - v_F||^2 <= 6, so p <= 6^{m/2} = 6^{n/4}. QED.
This is a square-root sharpening of master's sidon norm gate (6^{phi(n)} = 36^{n/4} -> 6^{n/4}), and a genuine structural narrowing of the critical red: for each corridor n the open p-range is now bounded. Consumer edits (claim_contract, statement.md, source_scope_audit, frontier, attack_surface) are APPEND-style, keep the OPEN GAP verdict, keep the falsifier, no target promotion, mobius dag statement byte-identical at pin (only attack_surface gained two scoped sentences). Edges: sidon -req-> cutoff (proof dependency, correct kind), cutoff -ev-> mobius. verify.py REPLAYED PASS (tiny): vectors=43434 parity_pairs=130293 cutoff=19 parseval=6 exponent=n/4 — exhaustive (5)-classification for orders 4..256, parity for orders <= 32, packing arithmetic 168 > 147, PLUS the honest internal sharpness marker 126 <= 147 (the same argument cannot give cutoff 4 — matches the dce84024 fence). Ledger note: 4d073683 rewrote the d80d448b ledger row in place (8 -> 6, new Modal apps) — fold at import (w6-C11).

**e909175d (Fermat-factor adversary)** — evidence-only stress: the two F_12 prime factors with order-8192 subgroup generated by the integer 2 (p = 190274191361, 1256132134125569; deliberately hostile low-height generators); exact Modal enumeration (ap-MdlzVrunPKIxHe0OyZSWmb, 4GB workers): both rows have max_t P(t) = 5, EMPTY rich loci, X_18 = S_ns^rich = M_5^rich = 0. #96 GUARD DISCIPLINE RESPECTED at every site: "two-row adversarial evidence, not a uniform bound... does not promote f3_h3_mobius_excess_half or any sufficient premise"; attack.md addition draws only the negative lesson (a norm-divisor attack needs more than generator height). Nothing consumes the smallness. No dag change. Local replay infeasible under tiny (4GB) — DEFERRED (pre-registered expectations below); non-blocking since nothing consumes it.

**dce84024 (cutoff-four packing fence)** — honest route fence: metric packing + factor-disjointness alone cannot improve 6^{n/4} to 4^{n/4}; exact 7-clique witnesses (pairwise distance^2 >= 6, all norms 3, disjoint exponent pairs) at orders 16/32/64. Probe REPLAYED PASS locally (tiny): order-64 matching witness = exactly the fence note's pairs (1,2)...(13,14). Fence honestly scoped: "not a C36 counterexample: the displayed pairs are not claimed to have one common shifted product modulo an official prime"; any further gain must use the same-product congruences. Frontier addition consistent. No dag change.

**7e4be37f (same-fiber cyclotomic ideal batching)** — new PROVED background node f3_h3_rich_fiber_ideal_batching. PROOF SOUND (hand-traced): alpha_i != 0 by Sidon; 1 - zeta^e in (pi) via the geometric sum, so beta_i in (pi)^2 and alpha_i in (pi)^2; common fiber puts alpha_i in P; (pi) and P lie above 2 and odd p so (pi)^2 and P are comaximal and (pi)^2 cap P = (pi)^2 P; N(pi) = Phi_n(1) = 2 (n = 2^s), N(P) = p (degree one); containment reverses norm divisibility: 4p | N(J) | gcd_i |Norm(alpha_i)| — (IB); if two principal norms have gcd exactly 4p then N(J_2) = 4p and J_2 = (pi)^2 P by equal-index containment — (SAT). Statement honestly disclaims any uniform rich-fiber bound ("does not prove the open C36' estimate"). Boundary certificate at the banked row (8192, 67657729): both rich targets (exactly wave-5's rich_targets=2; 10 unordered pairs each, consistent with P = 20 and X_18 = 4) saturate at gcd = 4p after two generators. verify.py REPLAYED PASS (tiny, mini-tree): targets=2 pairs=20 norms=18 gcd=4p mutations=7/7 — fiber membership recomputed EXACTLY locally (pow-based shifted products), 4p-divisibility and gcds exact, resultant p-valuations checked, dag wiring checked, 7 mutation controls incl. a dag-status mutation. The 18 raw norms come from pinned Modal FLINT shards (ap-WgM34tE25CAe0FYz8owGUJ) — trusted from the certificate with strong internal consistency (theorem forces 4p | each norm: verified; the certificate could not have been fabricated to pass the local fiber checks accidentally) — Modal recompute optional, DEFERRED. Frontier/attack additions marked "exact route evidence, not a uniform middle-field estimate" — guard discipline respected. Edges: sidon -req-> batching, batching -ev-> mobius. No status changes.

CLUSTER-WIDE: mobius dag statement UNCHANGED at pin (verified by field-level diff — only attack_surface changed); the critical target, falsifier, and r2/r3 pins untouched; guard discipline (#96) respected at every empirical site; nine/eleven-leaf counts in ledger rows reflect mid-branch merges, not claims.

## CATCHES

**w6-C1 (moderate, textual; cluster A): the envelope fence's affirmative sentences re-assert the pre-#158 wholesale rank-4 close.** route_limit.md ("These are exactly the already-paid ranges after the existing rank-four handoff"), result.md, and the new ledger row inherit #158: at RowC 1/16 the rank-4 handoff covers only line-admitting (b <= 47) or line-covered hulls; the line-free/line-uncovered sub-case is open and NOT fenced by this envelope (which presupposes covering). The rank-five kill is sound on the covered branch. REPAIR: append to route_limit.md and result.md: "[w6 audit 2026-07-13: the paid ranges quoted here inherit catch #158 — at RowC 1/16 the rank-4 handoff is conditional (line-admitting b <= 47 or line-covered hulls); the line-free/line-uncovered rank-4 sub-case remains open and is not fenced by this envelope, which presupposes the covering step; the rank-five kill is unconditional on the covered branch.]"; annotate the new ledger row identically; do NOT re-import the branch's rewritten e3c08000 ledger row (master's #158-annotated copy stands).

**w6-C2 (minor, prose; cluster B): "realizes credit-bearing milestone 2" over-reads the milestone.** Milestone 2 = exact finite MCA determinations at clean rates; WP5's exit gate quantifies over every clean-rate corridor row; the packet closes four self-chosen eligible rows. REPAIR: in the imported roadmap note r6: "realizes the milestone-2 shape on four particular eligible rows (first exact full-MCA determinations at all four official rates); the corridor clean-rate rows and the WP5 exit gate remain open."

**w6-C3 (minor, wording; cluster B): "exact noncommon witness support of size a" is an unjustified normalization — and an unused one.** Trimming can break noncommonness; the proof is insensitive (works verbatim with |S_z| >= a). Optional one-line repair in proof.md.

**w6-C4 (MAJOR FINDING, cluster C — import as a dated addendum, credit Codex): ENDPOINT-EXC-COVERAGE is circular as a decomposition target.** Certificate item 6 checks the binding endpoint 2^121 per row; by the soundness implication, universal coverage IMPLIES B-WEAK and is at least as strong. Master's planned amber ("conditional on C2''/C1'/ENDPOINT-EXC") is therefore conditional on a predicate dominating the node's own claim. The branch's replacement — B-WEAK <== C2'' (X <= 2^21 A) AND baseline (A <= 2^100 <== C1' + WCL-ZONE) — is a sound, exactly-verified, genuinely NON-circular decomposition whose three open leaves (C1', WCL-ZONE, C2'') are each strictly weaker than the endpoint. This resolves the SHAPE of master's M3 gap; the WCL-ZONE truth claim itself remains open with zero F-rounds. REPAIR SITES: dated addenda (never replacements) to M3_ENDPOINT_EXCEPTION_COVERAGE.md, QUALITY.md, REPOSE_B_WEAK.md, plus the dli dag statement addendum in the surgery spec.

**w6-C5 (blocking custody, cluster C): wholesale statement replacement on three dli-lane nodes.** dli_prime_weighted_large_block_support (title + statement -> 5 sentences; falsifier, nine-round history, catches #13/#40/#108/#162, F-round state, evidence-lane law all DELETED), dli_dyadic_k_core, dli_level_factorization. Master's dli statement has since evolved (M4 banked, #162 executed, #164, #165 repaired) — the #104 merge law resolution: master text stands; branch content lands as dated addenda (texts in surgery spec).

**w6-C6 (blocking process, cluster C): unauthorized status flips.** dli TARGET -> CONDITIONAL front-runs M5 and the F-round promotion law (WCL-ZONE has 0 survived rounds; master's banked amber design — written by the branch's own earlier M4 builder — keeps B-WEAK at TARGET, amber as a route-level close pending maintainer countersign; re-surgery criterion 6 makes silent upgrades custody incidents). dli_level_factorization TARGET -> REFUTED is content-defensible (F-round verdict long-recorded; successor node now exists) but is master's ceremony — queue both as MAINTAINER DECISIONS.

**w6-C7 (blocking, cluster C): retirement of master's REPAIRED verifiers on a stale premise.** eex_verify.py + m4_assembly_verifier.py stamped RETIRED/exit-2 citing #165 fail-open — but master's 561f729 TETHERED the reserve credits (MUT-8 10/10) and banked the M4 addendum. Importing the retirement clobbers a live repaired interface; verify_m3_endpoint_contract.py additionally inverts master's "dli_stays_target" guard. REFUSE these three file edits; keep master's versions; the alternative-decomposition cross-reference goes into eex_report.md as a dated note WITHOUT declaring the interface dead.

**w6-C8 (blocking process, cluster C): M5 self-settlement.** Three sites replace "maintainer confirmation owed" with "M5 is confirmed/settled by the corrected consumer identity"; no M5_CONFIRMATION_2P121.md exists and master's m4_report.md section 3 specifies the exact maintainer text required. REFUSE; keep M5 outstanding; forward the consumer-identity argument to the maintainer as supporting input.

**w6-C9 (minor, genuine fix; cluster C): x4 REDUCTION_PACKET stale 2^122 display.** Master's line 28 still reads "prize budget q^{-t+H} W_cen <= 2^122" although catch #40 re-pinned 2^121 (master's own corrected face, 11 lines below, already says 2^121). Codex's content fix is correct; import per #155 as an annotation ("[#40: superseded display; binding = 2^121]") rather than the silent rewrite. The C2PP 1.555 -> 1.554 edit duplicates master's already-executed #164 — dedupe (no-op at import). The x4 "FRONTIER red -> FRONTIER amber" relabels are refused until the amber executes on master.

**w6-C10 (minor, mechanical; cluster C): the two new implication verifiers hard-code the branch wiring.** dli/verify.py asserts dli status == CONDITIONAL + req edges; baseline/verify.py likewise. On master's post-import wiring (ev-lane, dli TARGET) they FAIL as written. REPAIR: park both under the dli node's notes/ as *_pending_amber.py, unregistered in the manifest, with a header stating they become <node>/verify.py verbatim when the maintainer executes the amber; OR import now with the four structural expectations adjusted to the ev-lane wiring (and re-strictened at amber time). Default: park (preserves their fail-closed design unmodified).

**w6-C11 (minor, ledger; cluster D): 4d073683 rewrote the d80d448b ledger row in place** (8^{n/4} -> 6^{n/4}, four new Modal app ids). Wave-5 precedent (bca6fc8c): fold to one accurate row at import, with "(supersedes the same-day 8^{n/4} row, commit d80d448b)" noted in the verdict cell.

OBSERVATION (not a catch; shared with master's banked M4): the C1' pose's hypotheses (q = 1 mod n', 2^N >= q^L, N >= 16L) are instantiated at official production levels via the pose note's aspect-range sentence; a fully rigorous baseline discharge will eventually need that instantiation spelled out per level. Pre-existing exposure, identical in master's M4; noted for the C1' lane.

## DEFERRED VERIFICATIONS (pre-registered expectations)

1. Fermat-factor adversary Modal replay (e909175d; ap-MdlzVrunPKIxHe0OyZSWmb class; needs ~4GB, script experiments/prize_resolution/f3_h3_fermat_factor_adversary_modal.py at pin): expectations — p in {190274191361, 1256132134125569} both certified prime with ord(2) = 8192, p = 1 mod 8192, p >= n^2; max_t P(t) = 5, P(1) = 3, zero rich targets, X_18 = 0, S_ns^rich = 0, M_5^rich = 0, all mass checks pass. Falsifier: any nonempty rich locus (would refute the result note, NOT the cutoff theorem).
2. FLINT norm shard recompute (7e4be37f; ap-WgM34tE25CAe0FYz8owGUJ class; script experiments/prize_resolution/h3_rich_norm_gcd_modal.py): expectations — the 18 stored norms exact; per fiber gcd(first two) = gcd(all nine) = 4p = 270630916; reduced resultants of bit lengths 2906/3481 with p-valuation 1 and cofactor bits 2880/3455.
3. Branch repo-wide Modal harness (133/133 + five gates + orbit at pin) — branch infra; every per-node verifier independently replayed here, so not import-blocking.
4. Cluster A Modal app ap-3SsE1JL5IiiOkotzT89CcR — already superseded by the local pin replay + brute-force validation (item covered; no action).

## IMPORT SURGERY SPEC (exact; statements merged never replaced; per master conventions)

Order: [D: d80d448b+4d073683 folded] -> e909175d -> dce84024 -> 7e4be37f(batching only) -> [B: 261bef6d] -> [A: 9d058055] -> [C core, repaired] ; maintainer-decision queue LAST.

1. CLUSTER D NEW NODES (verbatim from pin; PROVED background): background/nodes/f3_h3_rich_fiber_norm_cutoff/ (statement/proof/result/verify.py/dependency_subdag — pin = sharpened constant 6) and background/nodes/f3_h3_rich_fiber_ideal_batching/ (all five files) + experiments/prize_resolution/{h3_rich_norm_gcd_modal.py, h3_rich_norm_gcd_result.json, h3_norm_parseval_search.py, h3_rich_parseval_packing.py, h3_parseval_cutoff4_probe.py, f3_h3_fermat_factor_adversary_modal.py}. Dag: two node entries + edges sidon -req-> cutoff, cutoff -ev-> mobius, sidon -req-> batching, batching -ev-> mobius. No status changes anywhere.
2. CLUSTER D mobius node-folder deltas (pin content, all append-style, import verbatim): statement.md (+4-line RFNC paragraph), claim_contract.md (dependency + range-scoped open content), source_scope_audit.md (verdict paragraph, content-preserving), frontier.md (cutoff/batching/fermat/fence blocks), attack.md (boundary-separation + fermat paragraphs), parseval_cutoff4_route_fence.md, fermat_factor_adversary_result.md (new files); dag attack_surface two added sentences. Mobius dag STATEMENT untouched (verified byte-identical — nothing to merge).
3. CLUSTER B: background/nodes/mca_quadratic_prize_rows/ five files verbatim; dag node entry + single ev edge -> mca_grand; roadmap note r6 WITH w6-C2 rewording; ledger row verbatim; optional w6-C3 one-liner in proof.md.
4. CLUSTER A: route_limit.md + result.md paragraph + verify.py additions at pin, WITH the w6-C1 scoping sentence appended to route_limit.md and result.md; new ledger row with the w6-C1 annotation; SKIP the branch's edit to the e3c08000 ledger row (master's stands).
5. CLUSTER C IMPORTABLE CORE:
   a. New dag nodes (critical/): dli_c2pp_joint_reserve (TARGET; statement/attack verbatim from pin), dli_wcl_zone_coverage (TARGET; statement/attack verbatim), dli_marginal_baseline100_coverage (CONDITIONAL; statement/attack/conditional verbatim).
   b. Edges (master currently has exactly dyadic_k_core -ev-> dli, level_factorization -ev-> dli, v13_base_field_normalization_guard -ev-> dli into the node): ADD dyadic_k_core -req-> baseline; wcl_zone -req-> baseline; level_factorization -ev-> c2pp; baseline -ev-> dli; c2pp -ev-> dli (ev not req — deviation from branch, per the node's evidence-lane law). KEEP dyadic_k_core -ev-> dli and level_factorization -ev-> dli (history; removal is part of the amber ceremony if the maintainer wants the branch's exact final topology). The req edges {baseline, c2pp} -> dli are the AMBER move — maintainer queue.
   c. dli dag statement: APPEND (do not touch master's existing text): "|| NON-CIRCULAR DECOMPOSITION POSED (2026-07-13, wave-6 audited, w6-C4): ENDPOINT-EXC-COVERAGE is circular as a decomposition target (certificate item 6 already checks the 2^121 endpoint; coverage implies B-WEAK). Corrected exact decomposition, machine-verified: [C2'': q^{-t+H} W_cen <= 2^21 prod_j E_U[rho_j], node dli_c2pp_joint_reserve] AND [baseline: prod_{L=1}^{34} E_L <= (41/8)^{34} < 2^100 from C1' + WCL-ZONE (W_cl <= 1/32 at all 34 levels, node dli_wcl_zone_coverage), exact integer pin 41^34 < 2^202, node dli_marginal_baseline100_coverage] ==> the 2^121 endpoint. The three ultimate open leaves are C1', WCL-ZONE, C2''. The req-rewiring and any status change await the maintainer's amber ceremony (M5 still owed; F-round rule applies to WCL-ZONE)."
   d. dyadic_k_core dag statement: APPEND the branch's clean 3-sentence pose as "[RESTATEMENT r2 (2026-07-13, wave-6): ...]" preserving master's F-round history text; same for the node folder statement if divergent.
   e. M3_ENDPOINT_EXCEPTION_COVERAGE.md: keep master's full text; APPEND the branch's "M3 corrected: primitive-ledger zone coverage" content as a dated section "## M3 CORRECTED POSE (2026-07-13, appended per #104; the contract above remains the banked M4 interface)" — including the WCL-ZONE display, the downstream assembly block, and the circularity paragraph.
   f. QUALITY.md + REPOSE_B_WEAK.md: append the branch's new sections EDITED per w6-C7/w6-C8: drop "The live repair eliminates ENDPOINT-EXC..."/"the legacy M4 program now exits 2" (master's is repaired and live) and drop "M5 is confirmed directly..." (keep "maintainer confirmation owed"; add "the corrected consumer identity supplies the confirming computation for the maintainer's ruling").
   g. eex_report.md: append a dated cross-reference note (circularity + successor decomposition) WITHOUT the "not a live proof interface"/"exits 2 by design" sentences. REFUSE the eex_verify.py, m4_assembly_verifier.py, verify_m1_result.py, verify_m3_endpoint_contract.py edits wholesale (master's repaired/banked versions stand).
   h. The two implication verifiers: park at critical/nodes/dli_prime_weighted_large_block_support/notes/m4b_assembly_verify_pending_amber.py and .../dli_marginal_baseline100_coverage/verify_pending_amber.py (pin content + a 3-line header: "becomes verify.py verbatim at the amber ceremony; structural checks expect the amber wiring"), unregistered in the manifest (w6-C10). Alternative (banker's option): adjust the four structural expectations and register now.
   i. x4_exactlist_staircase_split: REDUCTION_PACKET.md line-28 — annotate per w6-C9 ("(2^122 display superseded by catch #40; binding 2^121)"); REFUSE the red->amber relabels in REDUCTION_PACKET.md/conditional.md until the amber executes.
   j. C2PP_POSED_20260710.md: import ONLY the "M4 SUPERSESSION" paragraph as a dated appended note, REWORDED: "the ENDPOINT-EXC coverage predicate is circular as a decomposition input (w6-C4); the posed successor decomposition uses dli_marginal_baseline100_coverage + dli_c2pp_joint_reserve; ENDPOINT-EXC's banked verifier remains the M4 interface of record until the maintainer's amber ceremony." Keep "maintainer confirmation owed" for M5. Skip the 1.554 hunk (already on master).
   k. dli node statement.md (folder): append the "Conditional close (2026-07-13, M4)" block RETITLED "Posed decomposition (2026-07-13)" with the M5 sentence replaced per (f) and status line left pointing at dag.json's TARGET.
6. NOT IMPORTED (refused/queued): dli status flip (queue: maintainer amber ceremony = M5 file + req-rewiring + CONDITIONAL flip + strict verifiers in one move); level_factorization REFUTED flip + statement replacement (queue with recommendation: acceptable once c2pp lands, via appended addendum); all three verifier retirements; M5 self-settlement sentences; the branch's e3c08000 ledger-row rewrite; x4 amber relabels.
7. Manifest/ledger/orbit: recompute verifier_manifest hashes on the REPAIRED files (w6 repairs change statement.md/route_limit.md/result.md hashes); import ledger rows for all nine commits with w6-C1/C11 annotations; catches w6-C1..w6-C11 to notes/kernel_basis/KB_LOG.md under their assigned global numbers; orbit rebuild after banking; refresh the Claude artifact + public site per the standing artifact-refresh rule.

## BOTTOM LINE

- A: IMPORT-WITH-REPAIRS (sound rank-5 fence; #158 scoping sentences).
- B: IMPORT-CLEAN (a genuine prize partial: four admissible rows exactly determined; theorem + verifier + provenance all check).
- C: SPLIT — the decomposition core is the best structural advance on the dli gap to date (w6-C4 confirmed: it replaces a circular coverage obligation with three strictly-weaker leaves) and imports with repairs; the status flips, statement replacements, M5 self-settlement, and retirements of master's repaired M4/EEX interfaces are REFUSED (w6-C5..C8) and the amber goes to the maintainer queue.
- D: IMPORT-CLEAN (the 6^{n/4} large-field closure of C36' is proved and replayed; the ladder's evidence and fences are exemplary #96 hygiene; one ledger fold).
