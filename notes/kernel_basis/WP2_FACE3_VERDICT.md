# WP2 — Face 3 vs xr_smallcore_spread_count: REDUCTION-PARTIAL
# (2026-07-10; fresh-context adversarial replay; anti-goal upheld —
#  the red does NOT amber)

VERDICT: genuine consumption, incomplete reduction. Face 3 is not
analogy — the quotient-coset staircase falsification (2^82.97
spread supports paid as multiplicative pullback) and the dihedral
column were both absorbed as RK clause (i) at this node, and the
two live opens (W-collision second moment 5c; far-from-pencil
heart 5e) have exactly clause-(ii)/axis-Q shape. But the
conditional-close chain has SEVEN named non-RK items:

M1 same-slope PrimitiveExactList <= n^2 (list lane, outside Face 3)
M2 shared_census_kernel for the near-pencil stratum (SCK-as-RK-
   instance = the U1-sibling unification, HIGH LEVERAGE)
M3 circuit locus-density counts (extra-taxonomic by e32's own
   dictionary; PROVABLE-staged, not proved)
M4 AG/net <-> multiplicative-pullback identification (or mint a
   fifth paid class — a taxonomy event)
M5 full-rank dimension-cap lemma (under the narrow object pinning)
M6 CONSTANT-EXACT RK: the slack scan leaves 0.86 bits (29n^3 vs
   16n^3), so Face 3 consumes B_RK = 3 WITH absolute constant <= 16
   net of proved strata — materially stronger than the schema's
   unpinned n^{B_RK}; no other face needs this
M7 pin extension-type at the six clean-rate candidates (gap 4;
   possibly vacuous at prime rows — must be pinned, not guessed)

Gap pinnings made for this face (recorded): object = per-slope
deeply-agreeing codewords + rank-loss events (broad); (B, allowance)
= (3, 16n^3-net); FM-witnessing caveat: ALL face evidence sits at
FM = Theta(1)..107 — never FM-suppressed; prize behavior is
extrapolation.

Machine checks: rungs 2a/2b verifier 39/39; e32 census 17/17;
independent exhaustive forcing replay at two fresh rows (84,375 +
184,877 pairs, 0 violations, pre-registered criteria) — upgrades
the shipped single-example check to exhaustive at depths 1-3.

Chain step statuses: 1 PROVED, 2 OPEN(M1), 3 PROVED/definitional,
4 PROVED-as-charging-map (population bound is another node's debt —
must not double-count as handled), 5a/5b PROVED, 5c OPEN->RK
(closes iff M6 granted), 5d CONDITIONAL on SCK (M2), 5e OPEN->RK
(axis Q, the heart), 6 ARITHMETIC+M7, 7 OPEN (M3/M4), 8 ARITHMETIC.

Highest-leverage next (agent's recommendation, endorsed): M2 (SCK
subset-of RK identification — would collapse 5d into K2) and 5c
(adjacent to PROVED v13_second_moment_shift_pair_identity).
