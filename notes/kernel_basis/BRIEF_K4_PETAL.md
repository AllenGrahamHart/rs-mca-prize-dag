# DELEGATION BRIEF — K4* (the petal kernel = K4 + G2)

STATEMENT: (K4) a polynomial bound for the stabilizer-primitive
part of ImgFib_U^{<k}(k+sigma), once quotient-periodic codewords
are charged separately; POOLED WITH (G2) the support-forcing
dichotomy: every full-petal top-defect-band contributor's support
is tail-plus-full-quotient-fibers (staircase) OR the codeword is
stabilizer-primitive.

WHAT IS ALREADY PROVED (use, do not redo): the E22 ledger —
cofactor equations, petal divisibility, tail-coset algebra,
seven-lemma quotient staircase (all replayed green, WP6); the
planted lower count (separate charge NECESSARY — the staircase
column C(n/M-1, k/M) is a lower bound, exactly matching);
budget arithmetic at official rows (exponent <= 5.785, ~9.5 bits
slack). WHAT IS OPEN: G2 itself (the packets are its 'entry
point'; F4-A2b's two-regime empirics are evidence); G1 (per-U
layer maps — upstream withholds); G3 (pricing multiplicity —
the ledger's own named missing node, absent everywhere); K4's
primitive bound. START AT: G2 via the divisibility system's
rigidity (packet 2's divisor form is the hypothesis side);
G3 via the e22_cross_scale canonicalization machinery.
SCOPE: top band only (d >= M(t-2)); mixed-petal and below-top
are separate obligations.
