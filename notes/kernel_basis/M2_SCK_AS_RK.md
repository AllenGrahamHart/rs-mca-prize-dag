# M2 — shared_census_kernel is RK's census sub-schema (2026-07-10)

IDENTIFICATION (schema-level, definitional given two pins):
every SCK instance is an RK instance with
  object          = census fiber (prefix/quotient/split-pencil/
                    t-null counts) — the 'deeply-agreeing codeword'
                    entry of RK's object menu, census-aggregated;
  operating point = generated-field sub-balance (|B0|^t >= 2^n,
                    beta-normalized) <=> FM <= 1 <=> RK's
                    FM-suppressed point at allowance 1  [PIN 1];
  clause (ii)     = 'max-fiber <= mean * poly(n)' == 'remainder <=
                    n^B * FM' — identical form, same unpinned
                    exponent;
  clause (i)      = the instance's paid families: quotient
                    pullbacks -> multiplicative pullback class
                    (exact); toral/norm-kernel families ->
                    EXTENSION-TYPE  [PIN 2 — and this PINS WP0's
                    GAP 4: extension-type = cosets of norm-kernel
                    (unit-circle/toral) subgroups in the tower,
                    per our f3_h3_char0_classification].
The converse FAILS: RK's design-space objects (rank-loss
configurations, concurrency points — WP2's step 5e) exceed SCK.
So SCK = the census core of K2, independently converged on by both
programs; SCK's welds are sharper (machine-checked to F2/F5/F3
instances) and SCK owns the first PROVED instances OUTSIDE RK's
box: the h=2 shallow collision census (Heath-Brown--Konyagin,
f3_h2_stratum_theorem) and the h=3 char-0 classification — both
now COUNT AS RK-INSTANCE THEOREMS (the box grows from our side).

CONSEQUENCES FOR THE PROGRAM:
1. WP2's M2 item resolves: chain step 5d (near-pencil, conditions
   on SCK) conditions on K2 THROUGH this identification — one of
   the seven Face-3 blockers becomes internal to K2.
2. RK's jointness axis (#212 tower product) is machinery SCK
   lacks; it is the natural candidate for dli's missing joint/
   tower aggregation (SCK's own F1 caveat) — a WP4 lead.
3. Basis: k = 3 holds ({K2 (SCK = census core), K3, K4}); K2 is
   no longer purely conjectural — its census core carries two
   theorems and a classified-instance ladder to n = 512.
UNTOUCHED: M6 (constant-exact 16n^3 — poly(n) unpinned in BOTH
schemas); RK-broad objects (5e); the identification is
definitional-given-pins, not a replayed theorem — the pins are
recorded on both nodes.
