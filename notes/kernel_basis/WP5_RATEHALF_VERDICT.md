# WP5 — rate_half_band_closure vs K3: ANALOGY-ONLY; K5 minted
# (2026-07-10; fresh-context adversarial replay; anti-goal upheld)

VERDICT: NO conditional close on K3. Two independent fatal points:
(F1) SIDE MISMATCH: K3 (row-sharp Q) is a safe-side MAX-FIBER UPPER
bound feeding thm:finite; the node's open content is the DEFICIT
side — witness/lower-bound families over the band (R(lq), sigma*],
~2.98M radii per row. If the v2 floor is TRUE, band radii are
genuinely unsafe and NO upper-bound theorem can cover them: K3 is
structurally incapable, not merely unproven.
(F2) QUANTIFIER MISMATCH: K3 quantifies over the four deployed
n = 2^21 rows (q ~ 2^186 extension fields, 31-bit prefix charges);
razor rows are n = 2^41, 256-bit PRIME q (no subfield, ~256-bit
witness charges — exactly why the prize band exists and upstream's
rows have none). No transport lemma exists; the ledger itself
grades the bridge NOTE-LEVEL.

POSITIVE (ev-grade only): K3 + prob:saturated-bc conditionally
complete upstream's four-row determination at n = 2^21, upgrading
this node's crossing-fidelity EVIDENCE from half-theorem to full
theorem — an ev-edge upgrade, not an amber.

K5 MINTED — RAZOR-BAND WITNESS KERNEL: a priced witness family
covering (R(lq), sigma*] at every razor row, uniformly in q. Live
routes (from the node's own docs): averaged conversion at giant M;
B2b balance; safe-side push (which would close the consumers but
falsify the floor's location).

MACHINE CHECKS (all exact, local, small-RAM): four-pair replay
11/11 (margins to 4 decimals); sigma* = 8,592,912,738 = t*-1
replayed exactly; cap reach 2^33 exact; band width 2,978,146;
razor threshold 255.899990; f6a2 crossings diff +0. PINNED FACT
(new): near the cap the row-local random-word FM crossing sits
BELOW the proved unsafe reach — the floor's 'first-moment model'
must be the worst-word/pincer object, whose per-row crossing is
NOT yet formalized: an open item OF THE FLOOR, recorded.

FLAGS: the safe-side-above-sigma* pincer machinery was consumed
from banked docs, not re-audited (future audit item if this chain
formalizes); M31 rows extra-official (inherited caveat).
