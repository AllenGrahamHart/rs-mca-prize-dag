# DELEGATION BRIEF — K5 (the razor-band witness kernel)

STATEMENT: at every admissible rate-1/2 razor row (log2 q in
(255.900, 256), n = 2^41, q prime = 1 mod n), a PRICED WITNESS
FAMILY certifying the deficit side through the band
(R(lq), sigma*], sigma* = 8,592,912,738 = t*-1 (replayed exact),
band width up to 2,978,146 radii, UNIFORM in q.

WHY NO UPPER-BOUND KERNEL CAN SERVE (do not re-derive): WP5's
side-mismatch argument — if the floor is true the band is
genuinely unsafe; witnesses are the only certification.
STRUCTURAL FACTS (exact, replayed): unsafe reach caps at 2^33 =
n/256 exactly at the cap (plateau scales 2^22..2^33); the
row-local random-word FM crossing sits BELOW the proved unsafe
reach near the cap — the operative model is the worst-word/
pincer object, WHOSE PER-ROW CROSSING IS NOT YET FORMALIZED
(open item OF THE FLOOR — formalize first).
LIVE ROUTES (from the node's docs): averaged conversion at giant
M; B2b balance; safe-side push (NOTE: closes the consumers but
falsifies the floor's location — an acceptable outcome, changes
the node's statement not its consumers). START AT: formalize the
worst-word per-row crossing; then extend the quotient-remainder
witness family beyond the 2^33 plateau.
