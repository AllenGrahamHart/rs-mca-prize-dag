# FABLE_AUDIT — l1_pma_diag (round 21, agent 4 of 4 — round complete)

**Auditor:** Fable, 2026-08-07. **Verdict: BANKED, MAINTAINER-LEVEL —
mystery 6's first diagnosis lands the same one-two the mystery-5
diagnosis did: the comfort evidence is dissolved (the N10 census
could never have fired its own falsifier — its domain is an exactly
degree-6 polynomial, closed form matching all three banked counts to
the digit) AND the honest replacement is sharper than what it
replaces (the bucket is PROVED empty/capped by counting at fixed
petal size; the entire open content moves to growing `ell`, with
`ell = Omega(n/log n)` at the official rows).**

Replay: all six scripts re-run clean under ramguard —
- d2_growth_law.py: P1 closed form MATCHES 5,096/386,640/27,152,032
  exactly; P3 random-word law 0.4-2% at n=32,64 (26.6% at the small
  (16,8,97) cell, a +1.55 sigma Poisson fluctuation); P4 doubling
  factor predicted 38.58 vs banked 38.00/38.01; P5 sigma_min 2/5/8
  with sigma=1 short by 6.88/22.48/53.03 bits.
- d2_danger_map.py: Lambda = 2*ell+b-2 law verified across the grid;
  the rich-fiber fence row (ell = Omega(n/log^2 n)) is exactly where
  BOX goes super-polynomial.
- d3_ell_sweep.py: fixed n=24, ell=2,3,4 -> 475/8,135/20,942
  (17.1x/44.1x) — one increment of ell beats a doubling of n.
- a1_a3_adversary.py + a3_exhaustive_exact.py: replication gate
  (43/33 at n=16 via the census path) + EXHAUSTIVE max 66 vs mean
  32.145 over all 830,490 legal words. Note the filter-guided search
  (a1_a3) found only 36 — the exhaustive max 66 is NOT filter-
  extremal, which independently confirms the mechanism (the filter
  is anti-correlated with exact retention at the degenerate end).
- a5_scale32.py: gate 2879/2857 PASS at n=32; minimal-degree word
  3,273 (+11 sigma); RANSAC spike 268,026 -> exact 122; both
  pre-registered escape tests silent.

ADOPTED (forced corrections applied this bank):
- **The C-V4-1 reconciliation** (attack.md RESOLVED bracket): the
  field-corrected 6.14-6.21 exponent is the exact box polynomial's
  pre-asymptotic slope, approached from ABOVE while the count
  approaches n^6/2304 from BELOW. "Above the disproved n^6 line"
  was a finite-size artifact. The queued decisive (64,32,97)
  control is SUPERSEDED — the exponent question is settled by
  machine-checked derivation, not another census point. (This
  retires a control I queued this morning; the derivation forces
  it.)
- **The re-pose of record L1-MPA-w** (statement addendum): clause
  (a) discharged by counting unconditionally (0.31 columns of a
  720-column allowance at ell=2); clause (b) — growing ell —
  carries the entire content. Falsifiers F-w1/F-w2 registered.
- **The compute swap**: L1-N10-128 DROPPED (a fourth point on a
  curve with a known closed form, ~$4.1); L1-N10-ELL is the
  replacement (the sweep that moves the parameter the object
  depends on). Roadmap section 07 updated; recompiled.
- **The cross-lane matrix**: zero band-lane instruments transfer
  (all fail at hypothesis level); the one ambient match is a proved
  route CUT. No unification language — consistent with the
  round-19 gate discipline.

HONEST SELF-CORRECTIONS accepted: P5 falsified as written at one
cell (sigma_min = 2, not >=3, at (16,8,97) eps=0 — the conclusion
survives at all rows for both eps); P3 misses its 10% bar at 1 of 6
cells; MAX32 labelled a search lower bound, not a max; one subagent
read the ledger only to line 2303 (the quarantine marker is 2304 —
held, barely; noted for future briefs: put the marker line number
IN the brief).

BOARD EFFECT (mystery 6): from "a census that resisted the
falsifier with a worrying flat exponent" to "a PROVED counting cap
at fixed ell + one honest obligation: polynomial control at
ell = Omega(n/log n), where the box is n^{Theta(n/log n)} — plus a
reachable falsifier (the ell-sweep) and a measured danger rate
(44x per two ell-increments)". The mystery is now WELL-POSED, and
its hard core is genuinely hard: the counting comfort at ell=2 was
real but irrelevant to the consumer regime.

ROUND 21 COMPLETE (4/4 banked). The diagnosis round did exactly
what it was launched to do: both promoted mysteries (5, 6) are now
well-posed with re-poses of record and registered falsifiers; both
closability hopes among the mystery-linked reds are dead with
structural blockers; the 25-red accounting stands at 14 mystery-
linked / 9 WCL-grind / 2 straddling with no cheap wins hiding.
