# REPORT — ge_floor_falsifier (persisted verbatim by the coordinator from the session transcript, task aa5edfd7cba37294d, 2026-08-07)

All computations complete and replay clean. Report follows.

---

# MYSTERY 5 round 22 — the FLOOR-GE falsifier search, executed

**Headline: FLOOR-GE survives as literally stated, but its route-blocking corollary DIES — and the replacement is stronger. ESCAPE-GE's escape class is real (proved: 2 odd prime ideals buy 2N'+1 &gt; N'+1 centers at N'=8, exhaustively) but it is PRICED OUT at the prize cell itself: at N'=128 the best certified family costs ~1 odd prime ideal per center and the cost grows quadratically. Separately, the GE-WEAK (b) obligation is far cheaper than banked — a complete per-row certificate at N'=128 is a 2^27-node lattice enumeration, not 2^188.**

---

## CATCH 0 — prior-art subtraction, stated before any claim

My D4 line arrived at "the folded kernel lattice has dimension 64 and its λ₁ exceeds the box radius 16, so enumeration decides it". **That observation is already in the repo.** `background/nodes/e1_folded_no_vector_certificate_256_payload/PRO_W3_e1_density.md:49`, verbatim:

&gt; `(The N'=128 zero cert already HELD, lambda_1 = 31.67 &gt; 16, as a bonus.)`

and `:47`:

&gt; `of grand challenge 1). Do NOT attempt the dim-128 exact lattice enumeration --`

I claim **no novelty** for λ₁ &gt; 16. The repo also already rules the evidence short — `status_ruling.md:12`: *"The prior BKZ observation was explicitly inconclusive."* (BKZ returns an **upper** bound on λ₁; certifying emptiness needs the **complete enumeration**.) What is new below is only the **price of that complete enumeration**, in a model validated against exhaustive brute force at two cells.

Note also: `PRO_W3`'s "do not attempt" is about **N' = 256** (dimension **128**), where I confirm it is right. The generator_economy prize cell is **N' = 128** (dimension **64**), a different and vastly smaller problem.

---

## Method — the model, and its direction of safety

`critical/nodes/far_pair_separation/conditional.md:27` fixes the certification form verbatim:

&gt; `(height-budget unit) * (multiplicative semigroup generated by g &lt;= poly bases).`

I replaced "semigroup generated by g bases" by its ideal-theoretic relaxation (PREREG P1): F is **S-certified** iff every nonzero difference generates an ideal supported on a set S of prime ideals. Any G-certified family is S(G)-certified, so **caps proved here are caps there**; an escape found here must be converted before it counts. Registered before running.

`L_k(N')` = max |F ⊆ C(N')| with all differences supported on {λ} ∪ (≤ k odd prime ideals). `L_0` is round-21's FLOOR-GE / MAXPOW2.

All arithmetic exact, stdlib only. Norms via the 2-adic tower recursion `v(x)v(−x) = a(y)² − y·b(y)²`; ideal support via `σ_p(d) = gcd(d mod p, x^h+1)` in F_p[x] — no polynomial factorization is ever needed, because unions of ideal sets are lcms of squarefree divisors. `selftest.py` checks `tower_norm` against an independent Bareiss determinant on the **full box at h=2,4** plus 300 random h=8 vectors: **0 mismatches**, `SELFTEST PASS`.

---

## D1 — THE FALSIFIER SEARCH, EXECUTED

**Exhaustive box sweep** (`sweep.py`): every nonzero vector of `{−2..2}^h`, exact norm, exact ideal support.

| h | N' | box | ODDCOST 0 | 1 | 2 | &gt;2 | PRIMEPOOL | MAXNORM |
|---|---|---|---|---|---|---|---|---|
| 4 | 8 | 624 | 160 | 464 | 0 | 0 | 7 | 144 = 2^7.170 |
| 8 | 16 | 390624 | 4224 | 215520 | 155840 | 15040 | 554 | 614656 = 2^19.229 |

**The ESCAPE-RATE curve at N' = 8 — PROVED-exhaustive in centers AND in ideal subsets** (all 41 centers; all C(22,k) subsets of the 22 occurring ideals):

| k | L_k(8) | GAIN | registered bound (k+1)(N'+1) |
|---|---|---|---|
| 0 | **9** | 0 | 9 |
| 1 | **9** | 0 | 18 |
| 2 | **17** | 8 | 27 |
| 3 | **17** | 8 | 36 |
| 4 | **17** | 8 | 45 |
| 5 | **17** | 8 | 54 |

- `L_0(8) = 9 = N'+1` — reproduces round-21's MAXPOW2(8) from an independent stdlib implementation.
- `L_1(8) = 9`: **one odd prime ideal buys nothing.**
- `L_2(8) = 17 = 2N'+1`, achieved by `T = {both primes above 3}`, witness `{0} ∪ {±1}^4` (16 all-signs vectors plus 0). **This is a genuine ESCAPE-GE witness: a family strictly larger than N'+1, using odd-prime-norm bases.**
- k = 3, 4, 5 buy **nothing further**. |C(8)| = 41, so this is a real cap, not saturation.

**At N' = 16** (`d1_h8.py`, 3281 centers, unit-orbit-representative reduction):
- `L_0(16) = 17 = N'+1` — **EXHAUSTIVE**, 7.6 s. H1 confirmed at the second cell.
- `T` = each of the three largest single ideals (|Δ| = 1952 each, all above p = 17): `L = 17` each, **exhaustive per T**.
- `T` = all three carried ideals above p = 17 (5856 differences): `L = 17`, **exhaustive**. Rational-prime escape at N'=16 buys nothing with 3 ideals.
- **Not completed:** the 6- and 8-ideal rational-prime runs and k=2 both hit the `ramguard local` 5-minute wall with no verdict. Stated plainly, not glossed.

**THE COST LAW, measured at the prize cell** (`cost.py`). Exact unit reduction: for `F_m = {0} ∪ orbit(c_1) ∪ … ∪ orbit(c_m)` (|F_m| = 1 + m·N'), every difference is a unit times one of `c_i`, `c_i(x^t−1)`, `x^t c_i ± c_j` — so `m + C(m,2)·2h` ring elements carry the whole ideal support. Cost = exact small-prime ideal count + gcd-components of the large cofactors (a rigorous lower bound; **exact** wherever `comps_lg = 0`).

| N' | m=2 | m=3 | m=4 | centers/ideal |
|---|---|---|---|---|
| 8 | 17 / **2** | 25 / **6** | 33 / **9** | 8.0 → 4.0 → 3.56 |
| 16 | 33 / **10** | 49 / **14** | 65 / **24** | 3.20 → 3.43 → 2.67 |
| 32 | 65 / **26** | 97 / **62** | 129 / **106** | 2.46 → 1.55 → 1.21 |
| 64 | 129 / **80** | 193 / **212** | — | 1.60 → 0.91 |
| **128 (prize)** | **257 / 140** | **385 / 457** | — | **1.83 → 0.84** |

Cross-validation: at N'=8 the greedy construction costs exactly **2** ideals for 17 centers, and the exhaustive clique search independently returns `L_2(8) = 17`. The construction is optimal at the only cell where "optimal" is checkable.

The cost fits `C(m,2)·c(N')` with `c(128) = 140` (predicts 420 at m=3, measured 457).

**Consequence at the prize cell.** The consumer needs `B*/2^33 = 2^89` centers. Linear reading (rate ≤ 1.83, falling): **≥ 2^88 odd prime ideals**. Quadratic reading: `m = 2^82` orbits ⟹ `70·m² ≈ 2^170` ideals. Either way, against `poly(128)`: **the gap is 82 bits (linear) to 163 bits (quadratic).**

---

## D2 — THE VERDICT

**Two verdicts, kept separate as registered (PREREG P5).**

1. **FLOOR-GE (k = 0) SURVIVES.** `L_2adic(8) = 9`, `L_2adic(16) = 17` — both `= N'+1`, both PROVED-exhaustive, by an implementation independent of round 21's. The registered falsifier — *"Exhibit, at any 2-power N' ≤ 64, a set of N'+2 elements of C(N') whose pairwise differences all have 2-power norm"* (`REPOSE_DRAFT.md:38`) — is **exhausted empty at N' = 8 and 16**. I did **not** search N' = 32 or 64 (the 5^16 / 5^32 boxes); those cells remain exactly as round 21 left them.

2. **THE ROUTE-BLOCKING COROLLARY DIES.** `L_2(8) = 17 &gt; 9 = N'+1`, PROVED-exhaustive, with an explicit witness. So `N'+1` is **not** a cap on certified families once odd-prime bases are allowed — FLOOR-GE must never be quoted as one. ESCAPE-GE is confirmed as a *description* of the escape and refuted as a *closure*.

3. **THE REPLACEMENT (proposed floor, falsifier attached).** **PRICE-GE**: the odd-prime-ideal cost of a certified family grows at least linearly and empirically quadratically in its size, with an exchange rate ≤ 1.83 centers per ideal at N' = 128 and falling. Hence poly(N') bases certify poly(N') centers. *Status:* PROVED-exhaustive at N'=8 (the L_k table); construction-side measured exactly at N' = 16, 32, 64, 128; the upper-bound half is conjectural above N'=8. *Pre-registered falsifier:* exhibit, at any 2-power N' ≥ 32, a certified family of size &gt; 4N' using ≤ N' odd prime ideals. Nothing I found comes close (at N'=128 the best is 2N'+1 = 257 centers for 140 ideals).

**Board effect (mystery 5):** the (a)-route is now dead for a reason that no longer depends on the 2-power-norm restriction. Round 21 killed it *inside one norm class*; this closes the one door round 21 left open, at the prize cell, measured rather than extrapolated.

---

## D3 — GE-WEAK FIRST POSITIVE

**The ell-condition system, exactly.** Since `Φ_{N'}(X) = X^h + 1`, the only cyclotomic relations are antipodal. With `FOLD(v)_j = v_j − v_{j+h}`:

&gt; `K_p` has a non-cyclotomic ternary vector of support ≤ 2ℓ' **⟺** some `w ≠ 0` in `{−2..2}^h` with `‖w‖₁ ≤ 2ℓ'` has `w(ζ) ≡ 0 mod p` **⟺** `p | Norm(w)` for such a `w` (p ≡ 1 mod N' has residue degree 1).

(The minimum unfolded support realising a folded `w` is `Σ_j |w_j|` — that is the whole content of the `‖·‖₁` budget.)

**This makes the certification threshold EXACT and UNIVERSAL, not per-row:**

`TIGHTEMPTY(N', L)` := largest prime p ≡ 1 mod N' dividing some box norm at that radius. Computed exhaustively:

| N' | L = 2ℓ' | TIGHTEMPTY | MAXNORM | AM-GM (4h)^{h/2} |
|---|---|---|---|---|
| 8 | 8 (full) | **137** | 144 = 2^7.170 | 2^8 |
| 16 | 6 | **4049** | 20736 | 2^14.34 |
| 16 | 16 (full) | **463249** = 2^18.822 | 614656 = 2^19.229 | 2^20 |

&gt; **THEOREM (toy, PROVED-exhaustive).** For every prime p ≡ 1 mod 16 with p &gt; 463249, `K_p` contains no non-cyclotomic ternary vector of any support. For N' = 8, likewise above 137. Both thresholds are **attained** (witnesses printed).

**C-4 replay + generalisation.** `lattice_cone_certificate/statement.md:21` banks *"288 antipodal cyclotomic relations, zero non-cyclotomic at N'=16, p=12289, w&lt;=6"*. Reproduced exactly: **576 cyclotomic kernel vectors = 288 up to global sign; 0 non-cyclotomic.** And since `TIGHTEMPTY(16, 6) = 4049 &lt; 12289`, that verdict now holds **for every p ≡ 1 mod 16 above 4049**, not only for the pinned prime.

**Scope catch on the C-4 anchor.** At the *same* prime p = 12289 but **full radius** (2ℓ' = 16), there are **6** non-cyclotomic kernel witnesses (e.g. `w = (−1,−1,−2,−1,2,2,1,−1)`, `Norm = 12289` exactly). No contradiction — C-4 declared `w ≤ 6` — but the anchor does **not** extend to full radius, and any consumer treating it as a full-radius exemplar is wrong.

**Bad-prime census, N'=16, exhaustive:** 536 bad primes, largest 463249. Density by dyadic window: 1.00 (≤2^12), 0.964 (2^12), 0.920 (2^13), 0.672 (2^14), 0.281 (2^15), 0.069 (2^16), 0.013 (2^17), 0.003 (2^18), **0.000 (2^19+)**.

**CATCH 1 — the banked expected-hit figure.** `kernel_lattice_reframing/statement.md:9` banks *"~2^-50 expected hits at N'=128"*; round-21 reproduced 2^-47.1 = 3^128/2^250. That counts the **unfolded** ternary cube, i.e. it counts the multiplicity of one folded class as independent trials. The number of distinct folded classes is `5^64 = 2^148.6`, so the **existence** probability is `2^-101.4`, not `2^-47.1` — **54.3 bits apart**. This is the *same defect class* as round-21's own COLLISION CATCH (multiplicity counted as distinctness), now on the other side of the node. It makes the claim **safer**, not weaker. My exhaustive census independently shows even the class heuristic **over**-predicts badness by 2–3 orders of magnitude at h=8 (measured 0.069 vs heuristic 0.981 at 2^16).

**The smallest new THEOREM that removes per-row work — and why it is not the norm bound.** A threshold theorem `p &gt; MAXNORM` is what the norm instrument gives. Measured exhaustively: `MAXNORM(h=4) = 144 = 12²`, `MAXNORM(h=8) = 614656 = 28⁴`, i.e. base `4(h−1)` at both. If that continued, `h=64` gives `252^32 = 2^255.273` — sharpening the banked `253^32 = 2^255.456` by 0.18 bits, and still above the prize rows at 2^250 (closing it needs base **224.6**, not 252). And **TIGHTEMPTY sits within 0.07 / 0.41 bits of MAXNORM** at h = 4 / 8, so no "which primes actually occur" refinement rescues it either. **Conclusion: no threshold theorem of the norm family reaches the prize rows.** The smallest new theorem is therefore *not* a sharper norm bound — it is a **certified λ₁ lower bound on the folded kernel lattice**, which is D4.

---

## D4 — THE CONE GEOMETRY

`Λ_p = {w ∈ Z^h : w(ζ) ≡ 0 mod p}`, dimension **h** (not N'), determinant **p**. Certification target: no nonzero `w` with `‖w‖_∞ ≤ 2`, i.e. no lattice vector of Euclidean norm ≤ `R = 2√h`.

Stdlib exact-rational LLL + Fincke-Pohst (`d4_cone.py`), every verdict cross-checked against the exhaustive box sweep:

| h | p | 2ℓ' | witnesses | FPCOST | BFCOST = 5^h | speedup |
|---|---|---|---|---|---|---|
| 4 | 137 (= TIGHTEMPTY) | 8 | 2 | 13 | 625 | 48× |
| 4 | 401 | 8 | **0** | 5 | 625 | 125× |
| 8 | 12289 | 6 | **0** | 19 | 390625 | **20559×** |
| 8 | 12289 | 16 | 6 | 373 | 390625 | 1047× |
| 8 | 463249 (= TIGHTEMPTY) | 16 | 2 | 25 | 390625 | 15625× |
| 8 | 463457 | 16 | **0** | 23 | 390625 | 16984× |

Every witness has `Norm(w)` exactly equal to `p`. The tool decides **both** directions and agrees with the exhaustive sweep at every cell. H6 confirmed by 10³–10⁴×.

**Pricing the prize cell** (`d4_price.py`; GSA + standard FP node model, validated against the exact counts above — it **over**-predicts by 2–3×, the safe direction):

| cell | n = h | log₂p | R = 2√h | GH λ₁ | **R/λ₁** |
|---|---|---|---|---|---|
| N'=128 | 64 | 250 | 16.00 | 29.02 | **0.551** |
| N'=256 | 128 | 250 | 22.63 | 10.60 | **2.135** |

At N'=128 the box sits **strictly inside** the shortest vector — emptiness is the generic truth and enumeration certifies it:

| basis quality | log₂ FPCOST | peak depth |
|---|---|---|
| LLL only (δ=1.0219) | **27.37** | 20 |
| BKZ-20 | 16.92 | 16 |
| BKZ-45 | 13.47 | 13 |
| BKZ-90 | 10.39 | 11 |

**CATCH 2 — the D4 pricing of record is off by ~20 orders of magnitude at N'=128.** `REPOSE_DRAFT.md:107` banks *"the full radius `w = 2l' = 128` costs `2^188.2`"* and `:111` *"Memory, not time, binds"*. Three corrections, in increasing order of size:
- the folded **coordinate**-split MITM costs `2^74.3`, not `2^188.2` (the banked weight-split model `C(N',w/2)2^{w/2}` is right for small w and wrong from **w = 28** upward);
- the folded **lattice enumeration** costs `2^27.4` nodes with LLL alone;
- so GE-WEAK (b) at a pinned N'=128 row is a **laptop-seconds** computation, not "Modal-scale, 95 core-hours, memory-bound".

**Spec (no install, per the brief).** `fplll`: build `Λ_p` from `(p,0,…,0)` and `(−ζ^j, …, 1 at j, …)`; BKZ-30; `enumerate` with radius 16 and per-coordinate box pruning `|w_j| ≤ 2`; the certificate is the empty enumeration transcript plus the reduced basis. Dimension 64 — routine. **At N'=256 (dim 128) the banked "do NOT attempt" is correct**: R/λ₁ = 2.135, witnesses are *expected* (matching `PRO_W3_e1_density.md:26`'s "~2^48 expected"), and the enumeration costs 2^152–2^235.

**What is left after this.** Per-row certification becomes cheap; **universal** closure does not. `integer_code_distance_cert/statement.md:16-20` (*"There is no hidden finite registry of official row primes"*) still binds, and my exhaustive census shows bad primes exist right up to TIGHTEMPTY with no gap. The honest residue is: *cheap per row, still unbounded in rows.*

---

## Registered predictions vs outcomes

| | prediction | outcome |
|---|---|---|
| **H1** | L_0(8)=9, L_0(16)=17 | **CONFIRMED**, both exhaustive, independent implementation |
| **H2** | L_k ≤ (k+1)(N'+1) | **CONFIRMED** at all 8 measured cells |
| **H3** | GAIN(1) ≥ 1 at N'=8 | **FALSIFIED.** L_1(8) = L_0(8) = 9 — one odd ideal buys nothing; a *stronger* floor than FLOOR-GE |
| **H4** | CBASE ≤ 3h at h=4,8 | h=4: 12 = 3h (boundary pass). h=8: **28 &gt; 24 — FALSIFIED** |
| **H5** | TIGHTEMPTY many bits below MAXNORM | **FALSIFIED.** 0.072 bits (h=4), 0.407 bits (h=8) |
| **H6** | FPCOST ≤ BFCOST/100 at h=8 | **CONFIRMED** by 1047×–20559× |

---

## Self-corrections and honest residuals

1. **My first cost functional was 5× too loose and I published a wrong rate from it.** `rate.py`'s gcd-components-on-full-norms bound is capped by the number of odd *rational* primes in the box (7 at h=4), giving `ORBITADD(8) = 1` where the exact answer — confirmed by the exhaustive clique search — is **2**, and a headline "RATE = 16 centers/ideal" where the truth is 0.84–3.2. `cost.py` fixes it (exact small-prime ideal counting + components only on large cofactors). `rate.py` and `rate2.py` carry SUPERSEDED headers so no replayer cites them.
2. `rate2.py` printed `|F_m| = 1 + 2N'm`; an orbit `{±x^r c}` has exactly `N'` elements, so `|F_m| = 1 + N'm`. Corrected in `cost.py`.
3. **The `MAXNORM = (4(h−1))^{h/2}` pattern is weakly supported.** Exhaustively true at h = 4, 8; **false at h = 2** (8 vs 4); at h = 16 my structured search over the h=4/8 maximiser shape returns `2^46.587`, *below* the pattern's `2^47.255` — so the maximiser shape changes and the pattern is unconfirmed there. The "252 at h=64" figure is conjectural on two points with a known small-cell failure. The rigorous AM-GM ceiling `(4h)^{h/2} = 2^256` is unaffected, and the D3 conclusion does not depend on the sharpening.
4. **The λ₁ &gt; 16 idea is a rediscovery**, already banked (CATCH 0). I found it by grepping only after arriving at it independently; I claim only the pricing.
5. **Two runs died on the compute law**, with no verdict: the 6- and 8-ideal rational-prime modes and k=2 at N'=16 both hit the `ramguard local` 5-minute wall. So `L_1(16) = 17` is exhaustive only over the **top-3** ideals (SEARCHED, not PROVED), and `L_2(16)` is unmeasured.
6. **Rule brush, disclosed:** before writing my PREREG registrations I ran two environment probes through ramguard (`python3 -c "import sys"` and a numpy-availability check that hit the tiny wall). No measurement was taken; it is recorded in PREREG.md's disclosure line rather than omitted.
7. **Quarantine held absolutely.** I never opened `notes/pilots_20260802/CAMPAIGN_LEDGER.md` at all, nor `l1_ell_sweep`, `bb_nu_transport`, `f2_rlocality`. **I dispatched no subagents.**
8. No status flips, no closure claims. Every write is confined to `notes/pilots_20260807/ge_floor_falsifier/` plus the mandated PREREG append. No git, no Modal, stdlib only, every `python3` via `tools/ramguard`.

**Files** (all in `/home/u2470931/smooth-read-solomin/prize/notes/pilots_20260807/ge_floor_falsifier/`): `PREREG.md` (registrations appended), `gelib.py`, `selftest.py`, `sweep.py`, `sweep_h4.json`, `sweep_h8.json`, `d1_small.py`, `d1_h8.py`, `stats.py`, `cost.py`, `d3_kernel.py`, `d4_cone.py`, `d4_price.py`, and the superseded `rate.py`, `rate2.py`.

**result:** FLOOR-GE SURVIVES as stated (L_2adic = N'+1 reproduced exhaustively at N'=8,16 by an independent implementation; its registered falsifier exhausted empty there) but its route-blocking corollary DIES — 2 odd prime ideals provably certify 2N'+1 = 17 &gt; 9 centers at N'=8 (exhaustive in centers and in all C(22,k) ideal subsets, witness {0} ∪ {±1}^4) — and the replacement floor PRICE-GE is stronger and measured AT the prize cell: at N'=128 the cheapest certified families buy 257 centers for 140 odd prime ideals and 385 for 457, a rate of 1.83 falling to 0.84 centers per ideal with cost quadratic in orbit count, so 2^89 centers costs 2^88–2^170 ideals against poly(128), an 82-to-163-bit ceiling that no longer depends on the 2-power-norm restriction; on the GE-WEAK (b) side the exact fold reduction gives PROVED-exhaustive universal emptiness thresholds (no non-cyclotomic ternary kernel vector for any p ≡ 1 mod 16 above 463249, or above 4049 at radius 6, generalising the banked C-4 anchor from one pinned prime to a whole congruence class, while catching that the same anchor's prime p=12289 has 6 witnesses at full radius), the banked ~2^-50 expected-hit figure is 54.3 bits multiplicity-inflated (existence probability is 2^-101.4, the same defect class as round-21's own collision catch, and it makes the node SAFER), the norm instrument is dead at the prize row for a structural reason rather than 5.456 bits, and the D4 pricing of record is wrong by ~20 orders — a complete per-row certificate at N'=128 is a dimension-64 radius-16 Fincke-Pohst enumeration costing 2^27.4 nodes with LLL alone (model validated against exhaustive brute force at h=4,8 where it decides both directions at 10^3–10^4x speedup), not 2^188.2 Modal-scale, though λ_1 &gt; 16 itself is a rediscovery of banked PRO_W3 prior art and universal closure over an unbounded row set remains the genuine residue.
