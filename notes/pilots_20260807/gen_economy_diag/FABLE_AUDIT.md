# FABLE_AUDIT — gen_economy_diag (round 21, agent 2 of 4 to report)

**Auditor:** Fable, 2026-08-07. **Verdict: BANKED, MAINTAINER-LEVEL —
mystery 5's first diagnosis lands three decisive results: (1) THE
COLLISION CATCH: the banked Pro-Brief-F family collapses BY ITS OWN
FORMULA (e_1(B) = (1+z)z^s is T-independent, so the C(N/2-2, N/4-1)
padding factor is exact e_1-collision multiplicity — the same defect
class as the already-REFUTED signed-8-core); the family has exactly
N = 128 centers (the orbit of (1+x), verified directly at the prize
cell), the banked "SHORT 23.3 bits" is really an 82.00-BIT DEFICIT,
the claimed N' = 256 PASS is FALSE, and conditional.md's obligation
is unsatisfiable as written. (2) THE STRUCTURAL CEILING: FLOOR-GE —
any base set of 2-power norm certifies at most N' + 1 centers,
INDEPENDENT of base count (exhaustive at N = 8, 16; constructive
lower bound everywhere; 240k-sample null extension at N = 32;
conjectural above — falsifier pre-registered). Template compression
cannot work in principle (the ceiling is base-count-independent);
difference-set imports are a category error (spread vs confine),
independently corroborated by the banked 3/3 REFUTED hypothesis.
(3) THE RE-POSE: the construction route decides ZERO prize rows
(129 certified centers decide q < 2^168 vs rows at 2^250); GE-WEAK
makes the KERNEL-EMPTINESS form (b) the node's obligation of
record — which is exactly integer_code_distance_cert's territory.**

Replay: all three scripts re-run clean (the MAXPOW2 witness clique,
the MITM memory arithmetic, the flat-profile probe). The collision
identity verified against the quoted pro_construction.md formula —
e_1 is manifestly T-independent by the antipodal cancellation the
construction itself advertises; the auditor confirms the catch is
arithmetic, not interpretive.

ADOPTED:
- THE COLLISION CATCH + the corrected deficit (82.00 bits) + the
  refutation of the N' = 256 pass + the death of Pro brief H's
  enlargement (same defect). FORCED CORRECTIONS applied this bank
  to generator_economy (statement addendum; conditional.md
  addendum marking the (a)-route obligation unsound as written and
  adopting GE-WEAK's (b)-form as the route of record — node stays
  TARGET).
- **FLOOR-GE** (the structural ceiling, with its honest epistemic
  ladder: proved / constructive / sampled-null / conjectural) and
  **ESCAPE-GE** (any >N'+1 family needs an odd-prime-norm base;
  the integer-factored FREE class confined to m in {1,2}).
- THE CONSUMER CONTRACT (D1) with the derived weakest form: a
  2^m-family decides exactly q < 2^{m+161} — the sharpest
  contract statement the lane has ever had, and the proof the
  (a)-route is dead (not short — DEAD: zero prize rows decided).
- **THE BRIDGE VERDICT** (the round-19 gates working exactly as
  designed): OBJECT partial (coefficient class fails the shape-pun
  test — 5-ary folded, not ternary), REGIME pass (the same
  crossing-razor rows), CRITICALITY pass-in-side/fail-in-interval
  (tau = 1.9531 vs the instrument's 1.9957 — a 0.0426 gap),
  METHOD partial-asymmetric. NO unification language — but a
  GENUINE instance-family relationship on the exclusion side, with
  the anti-numerology check passing (the banked ~2^-50 expected
  hits reproduced at 2^-47.1 unfitted). COORDINATOR NOTE: mystery
  5's re-posed obligation now sits in the same subcritical
  emptiness family as I2/I3 AND shares its object with
  integer_code_distance_cert — the probes pilot's verdict will
  compose directly.
- THE D4 PRICING (MITM cost model reproducing the banked numbers
  exactly; Modal-scale for a pinned row at w <= 16 ONCE the memory
  is re-engineered — the banked 2^48.4 is time-only, ~2^18x over
  the modal memory ceiling; NOT a universal-closure strategy — no
  finite row registry).

HONEST SELF-CORRECTIONS accepted, and they are the round's best:
R4's registered structural bar FAILED (pure counting leaves
thousands of bits of headroom) and the pilot says so — the cap it
found rests on the norm-support argument its registration did not
anticipate; R3 superseded (reported as such, not as completed); the
PREREG named the wrong ring (flagged, not silently patched); the
N' = 128 ceiling labelled an extrapolation with its exact epistemic
status; the read-only git status brush disclosed. Quarantine held
including through three subagents.

BOARD EFFECT (mystery 5): from "an open construction with a
23-bit gap" to "a REFUTED construction route + a structural ceiling
(FLOOR-GE, falsifier registered) + one honest obligation: kernel
ternary/short-support emptiness at the prize rows (= the
lattice-cone line, Modal-priceable per pinned row, universal form
open)". The mystery is now WELL-POSED.
