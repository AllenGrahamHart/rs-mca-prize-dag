# FABLE_AUDIT — red_closability_probes (round 21, agent 3 of 4 to report)

**Auditor:** Fable, 2026-08-07. **Verdict: BANKED — both probes return
NO with PERMANENT, named structural blockers. This is the successful
outcome of a cheap decisive probe (per the pilot's own P8): the two
reds that recent theorems might have quietly made closable are NOT
closable, and the reasons are already-banked refutations, not missing
effort. The "2 possibly closable" column of the 14/9/2 accounting is
zeroed.**

Replay: probe1_verify.py 126/0, probe2_verify.py 11/0, both re-run
clean under ramguard, fail-closed controls exit non-zero as required.
REPORT.md persisted verbatim by the coordinator from the session
transcript (task-id recovery script).

## PROBE 1 (integer_code_distance_cert vs Z-1/Z-2): NOT CLOSABLE — permanently

- The registered prediction P3 CONFIRMED exactly: the node's system
  supplies ell = 1 odd-power condition; the certificate needs
  ell = l' = 65. Z-2 at ell = 1 certifies only min l1 weight >= 3,
  and 3 is ATTAINED (so the floor is sharp, not slack).
- The blocker is PERMANENT, not a gap in effort: manufacturing
  ell >= 2 independent conditions is exactly what
  multi_multiplier_reduction attempted, and that node is REFUTED
  (the rank-1 outer-product collapse, re-verified by the pilot at
  six cells). No banked supplier provides the missing 64 conditions.
- MY BRIEF WAS WRONG and the pilot corrected it: the brief said
  "THE SCOPE CHECK IS THE WHOLE PROBE" (the shift-0 restriction);
  in fact the shift-0 scope check PASSES (the node's single
  condition IS the first odd power) — the kill is the ell-count
  (H4), not the shift. Accepted; recorded here as the round's
  brief-defect entry.
- Bonus banked: the PROVED high-field branch covers 5.02% of the
  prime-row window — quantified, labelled, not oversold.
- COMPOSITION with mystery 5 (gen_economy_diag, banked same round):
  GE-WEAK re-poses generator_economy's obligation as kernel
  emptiness = this node's territory. The probe confirms this node
  is the GENUINE open content of the kernel-lattice line: the
  ell-condition system is the real problem, and no known reduction
  manufactures it.

## PROBE 2 (unsafe_crossing_family_instantiation vs THEOREM BB): CLOSES NO PART

- The registered prediction P6 CONFIRMED, and strengthened: (C1)
  fails FIRST — BB bounds L_1 (list-side maximal codeword count)
  while the node counts B_C (ambient-MCA bad slopes), and the
  pilot produced an EXACT FINITE COUNTERMODEL to the tempting
  inference: RS[F_5, |D| = 4, k = 2] has L_1 = 6 > B* = 5 >= B_C
  = 5. No banked PROVED L_1 -> B_C transfer exists (searched).
- Independently fatal: (C2) BB's break-region rows are DISJOINT
  from the node's pair-feasible residual (which forces e = 1, and
  BB's tower rows have e >= 3); (C4) the endpoint mismatches by
  ~1e6. Three of four registered conditions fail independently.
- LEAD banked (not a closure): BB's METHOD shape — accident
  concentration into shells — matches the M-route's need for an
  upper bound on nu(A). Filed as a round-22 candidate, priced as
  method-transfer work, not a certificate.

## CATCHES (all three applied by the coordinator this bank)

- **CATCH-P1:** folded_certificate.md's "min support 5" is the
  FOLDED Hamming statistic; the node-relevant unfolded ternary
  support is 7. Wrong statistic in a certificate note — addendum
  applied to integer_code_distance_cert/notes/folded_certificate.md.
- **CATCH-P2:** weight_graded_mitm/proof.md justified its factor-N'
  speedup by a Frobenius map that is the IDENTITY when p = 1 mod N';
  the saving is real but the mechanism is the cyclic shift —
  corrected in place.
- **CATCH-P3:** unsafe_crossing_family_instantiation was filed
  under notes/roadmap/LIST.md despite being an MCA-lane node (sole
  out-edge to unsafe_at_crossing -> mca_unsafe -> mca_grand).
  Filing corrected on both sides (LIST.md annotation + MCA.md
  entry) and in the node's statement addendum.

## Board effect

The 25-red accounting sharpens: 14 mystery-linked (no cheap wins
hiding among them — both candidate closures now REFUTED with
structural blockers), 9 WCL-slot certificate grind, 2 straddling.
Every closable-by-known-methods hypothesis for the mystery-linked
reds has now been tested and killed; what remains is mystery work.

## Process notes

Quarantine held. Registrations appended before computation; both
decision rules (P2, P5's C1-C4) were binding and were followed to
the letter — the pilot refused the "nearly closes" framing its own
P8 anticipated. Verifiers stdlib-only under ramguard. Exemplary
round.
