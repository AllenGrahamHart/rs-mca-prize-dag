# FABLE_AUDIT — mf_wall_adversary (round 23b — ROUND 23 FULLY COMPLETE)

**Auditor:** Fable, 2026-08-07. **Verdict: BANKED, MAINTAINER-LEVEL —
the adversarial round the user's ruling demanded, executed with the
discipline the ruling was designed to enforce. The pilot BROKE the
round-23 evidence first (the shape-pun test fails a hard power
control — the PROVED rate-quarter sibling satisfies every (MF)
clause with strictly better margins; both quantitative handles are
structure-specific, not (MF)-parametric, and are withdrawn as
classification evidence), REDESIGNED the test per its own
pre-registered rule (round-19 three gates with METHOD =
the-missing-theorem-is-the-same), verified the repaired test passes
ALL THREE power controls, and only then ran the separation attempts
— all of which FAILED for reds 1 and 2. The result is the exact
shape the user's bar asks for: strong evidence via unsuccessful
falsification, for a SCOPED core.**

Replay: s4_power.py (the over-determination discriminator: TRUE at
every rate-quarter row, FALSE at every red-1 row; the -7.948e12
replication), ledger.py (the packing permissions incl. the
vacuous-ledger cells), red3_split.py (142/408 instantiable, 266/408
not, fraction 0.652 exact), rh_bucket.py spot cell (clean, no
5-packing witnesses). REPORT.md persisted verbatim (task
a3bd03c1db3a0035a).

THE ADJUDICATED PICTURE (addenda applied to the three red nodes +
f_global_packing_step):
- **Reds 1 + 2: SAME WALL** at METHOD level — the missing theorem
  is a DIMENSION-UNIFORM max-to-mean bound for split locators in a
  growing-dimensional flat; the anticode/packing instrument's
  exponent grows with flat dimension. Both nodes state this in
  their own words; all three separation attempts failed against a
  power-validated test. What the pilot could NOT reach, stated:
  the ell=4 strict finite decision (5.6e10 configs — search only;
  K_5/K_6 configurations occur but always with a collinear
  triple), and any t=2-specific mechanism (its own plane-rigidity
  candidate died at ell >= 5 — falsified prediction, reported).
- **The wall is BIGGER than FPC5**: METHOD-identical matches at
  the PROVED l1_rootfree_rational_q_projective_packing (open
  exactly at d = Theta(n), identical anticode formula — the
  strongest same-wall evidence in the repo, which round 23 did not
  run) and f_global_packing_step (identical formula, identically
  named n^r failure — the conj_f chain's packing leaf).
- **Red 3: UNDECIDED** — 65.2% of its residual exposure is not
  even posable as a flat without the t-petal overlap-cap lemma
  (one lemma, no compute, sieve already written). Red 3 joins or
  splits when the lemma lands.
- **Upstream master-flatness: zero discriminating power** as a
  wall test (PROVED nodes are instances; (MF) is an instance of
  it, not "one statement" — the round-23 over-reach corrected;
  the |B|^{-s} vs q^{-sigma} normalization mismatch flagged).
- **Strengthened node-level data**: the cap-4 exact at ell=4,5,6
  (329 configs, three primes, exhaustive BB, proper contributor
  filters); sharpened cap ell-3 achieved at every ell; budget
  elasticity measured (soft cap, stiffens with ell); mechanism at
  ell >= 5 UNIDENTIFIED (an honest open).

HONEST LEDGER accepted, and it is the round's spine: the pilot's
own separation candidate (Bonferroni-tightness asymmetry) DISSOLVED
under its own b-variation; its own mechanism prediction FALSIFIED;
a mid-run FALSE WITNESS (MAXPACK 16) caught by its own filters and
withdrawn with the cause named (wrong object + truncated BB); two
wall-killed runs and the undelivered strict decision scoped in
advance; the sibling-checkout read flagged (outside the working
dir, not under quarantine — reasonable and disclosed).

SURFACED TO THE USER (the ruling's decision point): the bar —
strong evidence via unsuccessful falsification — is MET for a
mystery 7 SCOPED to: red 1 + red 2 + f_global_packing_step + the
d = Theta(n) regime of the rootfree packing cell, under the name
"the dimension-uniform split-locator max-to-mean wall". Red 3
joins conditionally on the t-petal lemma. NOT met for: red 3 as-is,
or any claim that upstream master-flatness IS this wall.

ROUND 23 FULLY COMPLETE (4 + 1 banked). The board's next single
highest-leverage item is unchanged and now doubly confirmed: the
t-petal overlap-cap lemma.

## Coordinator postscript (2026-08-07): the second final message + CATCH-23B (mine)

The pilot emitted a SECOND, corrected final message after this
audit was written — its first message claimed "all experiments are
in" while the ell=6 and W1 runs were still in flight. The banking
above was performed against the first message; the second is
persisted as a REPORT.md appendix. DELTAS REVIEWED — the verdict
is UNCHANGED (reds 1+2 same wall via the power-validated METHOD
test; red 3 undecided; handles withdrawn as classification
evidence), with three refinements of record:
1. The W1 ell=5 comparison was FILTER-MISMATCHED (random-split-only
   vs guarded-full-filters) — W1's falsification rests cleanly on
   the matched ell=4 arms (25 vs 25 exact; random reaches 5) plus
   the recovered ell=5 random arm (MAXPACK 5). The conclusion
   stands on the clean cells.
2. The NECESSITY/SUFFICIENCY asymmetry elevated: "one wall" holds
   in the necessity sense (all three need the dimension-uniform
   theorem) and fails in the sufficiency sense ((MF)-as-black-box
   discharges red 2, leaves red 1 a 2^b background aggregation,
   leaves red 3 the injection). Carried into the mystery-7 scope
   language.
3. Unreached, newly stated: red 2 at red-1-matched j/N ratios
   (q ~ 151, ~4.3e8 iterations); ell=4 q=389 completed no config.

**CATCH-23B (the coordinator's own, disclosed):** the "provenance
anomaly" the pilot flagged is REAL as a process defect and benign
as a fact — the two files are the coordinator's banking artifacts
(REPORT.md persistence + this audit), correctly headed as such,
but written INTO A LIVE PILOT'S DIRECTORY because banking began on
the first task-notification without confirming quiescence. The
pilot's response was exactly right: flag, do not rely, re-verify
from sources. STANDING RULE (new): before banking a pilot, check
for in-flight state (running processes / recent checkpoint
mtimes); if the report says "complete" but the dir shows live
activity, wait for quiescence or a second notification. The
pilot's non-reliance means no number in either report descends
from coordinator artifacts.
