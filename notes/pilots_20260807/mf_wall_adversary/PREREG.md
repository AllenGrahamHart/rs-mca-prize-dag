# PRE-REGISTRATION — THE (MF) ONE-WALL CLASSIFICATION, ATTACKED (round 23b)

Round 23b, 2026-08-07. Coordinator brief; the pilot appends its own
registrations BEFORE any computation. MANDATE (user ruling of
record): mystery 7 is created ONLY on proof or strong evidence via
UNSUCCESSFUL FALSIFICATION that the three FPC5 reds share one wall.
The round-23 fpc5_diag shape-pun test is STATEMENT-LEVEL — the
single statement (MF) specializes to all three reds — and does NOT
meet that bar: a statement can be general enough to cover different
walls. Your job is to BREAK the classification. If your registered
separation attempts fail AND your power control passes, the
promotion evidence exists; if any succeeds, name which red diverges
and why. Either outcome is a win.

## 0. Sources (quote verbatim first)
- notes/pilots_20260807/fpc5_diag/ — the classification under
  attack: REPORT.md (the (MF) statement, the codim = sigma
  identity, the cap-4 finding, the trivial-owner concentration,
  the 408-row exposure), PREREG.md, and the scripts (REUSE the
  machinery — rh_pack_adversary.py, ls6_bucket.py, fpc5_exact.py
  are coordinator-replayed).
- The three red nodes' round-23 addenda (statements of the
  quantitative handles + named probes).
- notes/CONJECTURE_F_FALSE_GREEN_AUDIT_20260807.md — the (MF)
  derivation, the upstream face (prob:capfr1-master-flatness,
  quoted at :51-54), and the claim that the same wall is "visible
  in L1's root-free rational-Q/Pade section" (a FOURTH face to
  test, via l1_rootfree_rational_q_projective_packing and its
  neighbours in the l1_mixed_petal_amplification attack sections).
- The round-19 unification-gate discipline (OBJECT/REGIME/METHOD)
  and the floor campaign's falsifier-POWER discipline (a test that
  cannot separate known-different walls has no evidential value).

## 1. Deliverables
- (D1) THE CLAIM MADE FALSIFIABLE. "Same wall" must predict
  something. State the predictions the one-wall hypothesis makes
  at the MECHANISM level (not the statement level): (a) the
  mechanisms behind the three quantitative handles (why the cap is
  4; why the mass concentrates at the trivial owner; why the
  residual resists) have a common cause expressible in (MF) terms
  alone; (b) an instrument that moves one red's hard core moves
  the others'; (c) the minimal (MF) progress (any nontrivial
  max-to-mean bound on a growing-dimension split-locator flat)
  yields progress on all three. Register these as testable.
- (D2) SEPARATION ATTEMPTS, registered each with its own escape:
  - **S1 (the cap-4 mechanism).** Red 2's measured cap EQUALS its
    Bonferroni number (the pair-determinant instrument is tight).
    Compute red 1's Bonferroni number at its cell (m*j -
    C(m,2)*overlap <= N with the SHARPENED overlap cap ell-3): if
    Bonferroni ALLOWS 5 but the measured cap is 4, red 1's cap has
    a DEEPER, non-packing mechanism that red 2's does not — a
    concrete separation candidate. Adjudicate it: run the ell = 4
    finite decision (the named probe — enumerate
    (L_0,L_1,L_2,c_1,c_2) up to the small affine equivalence
    reduction; exhibit a 5-packing or prove <= 4). If <= 4 is
    proved and the proof mechanism is t=2-SPECIFIC (uses the
    guarded congruence structure absent at t=3), that is
    separation evidence; if the mechanism re-derives as packing
    arithmetic after the correct sharpening, unification evidence.
  - **S2 (instrument cross-response).** Apply red 2's tight
    pair-determinant/Bonferroni instrument at red 1's cell and red
    1's core-choosable adversary at red 2's off-tail cell (t = 3):
    does each red's cap phenomenon reproduce under the OTHER's
    instrument with the parameters transformed by (MF) alone? A
    cap that fails to transfer under the (MF) dictionary separates
    the walls.
  - **S3 (the upstream + fourth faces).** Hypothesis-match (MF)
    verbatim against prob:capfr1-master-flatness AND against the
    L1 root-free rational-Q/Pade cell. Exact matching per the
    round-19 gates — quantifiers, coefficient class, flat type,
    what "count" means. A face that matches only after weakening
    (MF) to vacuity is NOT the same wall; say so.
  - **S4 (POWER CONTROL — mandatory, run FIRST).** Apply your
    same-wall test to a pair KNOWN to be different walls (e.g.
    red 1 vs mystery 6's clause (b) growing-ell control, or red 1
    vs a WCL slot certificate cell). The test MUST separate them.
    If it cannot, your test has no power and NO conclusion below
    may be drawn from it — redesign before proceeding.
- (D3) THE VERDICT: SAME WALL (all separation attempts failed +
  power control passed — state exactly what was tried and what
  each attempt could and could not reach) / SEPARATED (which red,
  which mechanism, what the board split should be) / UNDECIDED
  (the named missing experiment and its price). No promotion
  language either way — the coordinator surfaces, the user
  ratifies.

## 2. Falsifiers / honesty
- The user's bar is the mandate: statement-level fit is NOT
  evidence. Every unification claim must be mechanism-level and
  survive your own registered attack.
- Census evidence is evidence, never proof; label throughout.
  Searches give lower bounds on true maxima — label them.

## 3. Rules
- DRAFT ONLY in notes/pilots_20260807/mf_wall_adversary/. Never
  edit dag.json/nodes/tools; no git; no Modal. COMPUTE LAW: every
  python3 invocation via tools/ramguard tiny|local -- python3 ...
  (literal --), from repo root, INCLUDING file patching and JSON
  peeking. 2-power toy grids where yours to choose (CATCH-Z6);
  official-arithmetic-shaped cells where the object demands them;
  name every measured functional (CATCH-19C). Verbatim quotes with
  file:line. No REPORT.md — your final message IS the report.
  QUARANTINE: do not read notes/pilots_20260802/CAMPAIGN_LEDGER.md
  at or past line 2929 (the "ROUND 23B LAUNCHED" marker); the LIVE
  pilot dir notes/pilots_20260807/ge_lattice_cert is OFF-LIMITS;
  the banked round-23 dirs (fpc5_diag, cw_shared_target,
  c2pp_diag) are readable sources. PASS THIS QUARANTINE CLAUSE
  VERBATIM to any subagent you dispatch.

---

# PILOT REGISTRATIONS (appended 2026-08-07, BEFORE any computation)

Author: Opus pilot, round 23b, `mf_wall_adversary`. Everything below
is registered before a single line of code is run. My mandate is to
BREAK the round-23 one-wall classification, not to confirm it.

## R0. Sources READ at registration time (before registering)

`notes/pilots_20260807/fpc5_diag/{PREREG,REPORT,FABLE_AUDIT}.md` and the
scripts `rh_m4t2_census.py`, `rh_pack_adversary.py`, `ls6_bucket.py`,
`fpc5_exact.py`; `notes/CONJECTURE_F_FALSE_GREEN_AUDIT_20260807.md`
(all 234 lines); the three red statements
(`critical/nodes/l1_fpc5_ratehalf_m4_t2_payment/statement.md`,
`.../l1_fpc5_ratehalf_m4_t3_split_slice_payment/statement.md`,
`.../l1_fpc5_large_source_payment/statement.md`) incl. their round-23
addenda; `background/nodes/l1_fpc5_ratehalf_m4_t2_joint_support_distance/
statement.md`; `.../l1_fpc5_ratehalf_m4_t2_sharp_projective_flat_descriptor/
statement.md`. Three read-only subagents (each carrying the quarantine
clause verbatim) harvested: (i) `prob:capfr1-master-flatness`, (ii)
`l1_rootfree_rational_q_projective_packing` + the mixed-petal sections,
(iii) mystery 6's re-pose of record + a WCL slot certificate.

## R1. What "SAME WALL" must mean here (registered definition)

Per the round-19 unification gates, I register that two problems share
a WALL iff all three hold:

- **OBJECT.** The counted set is the same object class under the (MF)
  dictionary (same ambient space, same defining condition class, same
  "count" semantics).
- **REGIME.** The parameter regime agrees on the features that decide
  difficulty (first moment position relative to 1; whether the
  dimension grows; whether the system is over- or under-determined).
- **METHOD.** *The missing theorem is the same*: one theorem statement,
  instantiated with parameters, discharges both. This is the
  mechanism-level bar and is the only one that meets the user's
  mandate.

The round-23 test checks OBJECT only (a statement specializes). I
register in advance that OBJECT alone is NOT sufficient, and that the
power control must be run against a pair that passes OBJECT.

## R2. D1 — THE CLAIM MADE FALSIFIABLE (mechanism-level predictions)

### D1(a) — the three quantitative handles have a common cause in (MF)

Round 23 offers three handles: red 1's q-invariant packing cap 4;
red 2's 52.4% trivial-owner concentration; red 3's 408-row residual
exposure. The one-wall hypothesis at mechanism level predicts:

- **W1.** Red 1's cap is a function of the (MF) parameters
  `(N, j, r, sigma, q)` ALONE, not of the guarded congruence structure.
  **TEST (registered):** run the identical core-choosable packing
  adversary on a UNIFORMLY RANDOM linear flat of the same dimension
  `ell-1` inside `F_q[X]_{<=d}` (same `d`, same allowed point set,
  same core budget `5ell-5`, same primitivity filter, untouched-petal
  filter dropped in BOTH arms so the arms are matched).
  **W1 CONFIRMED** if the random-flat cap distribution matches the
  guarded-flat cap distribution (same max, means within 1).
  **W1 FALSIFIED** if they differ by >= 1 in the max at ell = 4 or 5.
  If W1 is FALSIFIED, the cap is structure-specific and is NOT an
  (MF)-level phenomenon; the handle then supports neither unification
  nor separation and must be withdrawn as evidence.
- **W2.** The cap is invariant in `ell` (the round-23 headline).
  **TEST:** exact (not sampled) chart enumeration at `ell = 4, 5, 6`
  by last-coordinate bucketing (the `ls6_bucket.py` technique ported
  to the m4_t2 chart), >= 100 configurations per cell.
  **W2 FALSIFIED** if the max packing at `ell = 5` exceeds 4 or at
  `ell = 6` exceeds the `ell = 5` value. NOTE: round 23's `ell = 5`
  number came from a SAMPLE of 3e5 of 2,048,383 chart points, so its
  "flat in ell" claim is a lower bound only; I register that I expect
  W2 to be FALSIFIED, on the derivation in R3 below.
- **W3.** Red 2's trivial-owner concentration is (MF)-predicted, i.e.
  it is what a generic flat of those parameters gives.
  **TEST:** compare the measured `g = deg gcd(D_0,D_H)` histogram to
  the hypergeometric prediction `P(g) = C(j,g)C(N-j,j-g)/C(N,j)` for
  two independent uniform `j`-subsets of `[N]`. **W3 CONFIRMED** if
  the measured histogram matches within a factor 1.5 per bin;
  FALSIFIED otherwise.

### D1(b) — an instrument that moves one red's hard core moves the others'

**Registered as ALREADY AT RISK from round 23's own cross-lane
matrices**, which I quote in the report: `fpc5_diag/REPORT.md:129`
(LS6 chart FAILS on red 1), `:131` (prefix/shell machinery FAILS on
red 1, APPLIES to red 2), `:231` (all `M=4` constructions FAIL on
red 3). **TEST:** for each of the three named round-23 gates (the
`t`-petal overlap-cap lemma; the base-cover number of `G=1`; the
`ell=4` finite decision), state which of the three reds' residuals it
strictly reduces. **D1(b) CONFIRMED** if some gate moves >= 2 reds;
**FALSIFIED** if every gate moves exactly one.

### D1(c) — minimal (MF) progress yields progress on all three

Define **minimal (MF) progress** = a black-box theorem: "for every
affine subspace `A` of the monic degree-`j` space of codimension `s`,
`|A cap Dloc_j(D)| <= P(n)(C(n,j)|B|^{-s} + 1)` for an explicit
polynomial `P`" (this is upstream `prob:capfr1-master-flatness`
verbatim). **TEST:** for each red, decide from its OWN printed
contract whether that black box discharges the payment, and name what
survives. **D1(c)-STRONG CONFIRMED** if it discharges all three;
**FALSIFIED** if it discharges fewer than three, with the surviving
obligation named per red. I register the weak form too: **D1(c)-WEAK**
= it strictly reduces each red's residual.

## R3. What I derived BY HAND before computing (H-claims)

- **H1 (the incidence reformulation of red 1's chart).** In the monic
  guarded chart `A_F = {F_0 + sum_i c_i F_i}` of affine dimension
  `r = ell-2`, evaluation at a point `x` is AFFINE in `(c_1..c_r)`, so
  each candidate root `x` defines a HYPERPLANE `H_x` in the chart, and
  a chart point `p` is split iff exactly `d = 2ell-3` of the
  hyperplanes `{H_x : x in allowed}` pass through `p`. A packing of
  `m` split members with `|union of roots| <= N = 5ell-5` therefore
  requires, writing `mult(x)` for the number of chosen members having
  `x` as a root,
  ```text
  sum_x (mult(x) - 1) >= m(2ell-3) - (5ell-5).
  ```
  Combined with the proved pairwise overlap cap `lambda`
  (`sum_x C(mult(x),2) = sum_{i<j} |Z_i cap Z_j| <= C(m,2) lambda`)
  this gives the necessary condition
  ```text
  C(m,2) * lambda >= m(2ell-3) - (5ell-5).            (H1-BOUND)
  ```
- **H2 (H1-BOUND never forbids m=5 at red 1's cells, so cap 4 is NOT
  packing arithmetic).** With the round-23 SHARPENED cap
  `lambda = ell-3`, (H1-BOUND) at `m=5` reads
  `10(ell-3) >= 5ell-10`, i.e. `5ell >= 20`, i.e. `ell >= 4`: TRUE at
  every admissible `ell`, and TIGHT (equality) exactly at `ell = 4`.
  At `m=6`: `15(ell-3) >= 7ell-13` i.e. `8ell >= 32` i.e. `ell >= 4`:
  also true, also tight at `ell=4`. **So the proved+sharpened overlap
  cap forbids NOTHING at red 1's cells.** Contrast red 2's off-tail
  cell `(ell,b,a)=(4,1,1)`: `j=7`, `N=15`, `h=2`, and the plain
  Bonferroni union bound `m*j - C(m,2)*h <= N` FAILS at `m=4`
  (`28-12=16>15`), matching the round-23 measured cap 3 EXACTLY.
  **This asymmetry is my S1 separation candidate, registered.**
- **H3 (the `ell=4` mechanism is projective-plane rigidity).** At
  `ell = 4` the chart is a 2-PLANE and `lambda = ell-3 = 1`. A
  5-packing then forces: all `C(5,2)=10` pairs share EXACTLY one root,
  all 10 shared roots distinct, no root in 3 members, total distinct
  roots exactly `15 = N`. Because two distinct points of a plane lie
  on a UNIQUE line, each of the 10 shared roots is DETERMINED by its
  pair. So a 5-packing exists iff the "connection graph" on split
  chart points (edge `p~p'` iff the line `pp'` is a root-line) has a
  K_5 with the 5 points in general position and one extra root-line
  per point. **A 4-packing needs only 5 of the 6 connecting lines**
  (`4*5-15 = 5 <= C(4,2)=6`), i.e. K_4-minus-an-edge. The jump from
  5 edges to 10 edges is the cap-4 mechanism at `ell=4`.
- **H4 (H3 is dimension-2 specific — the PREDICTION that breaks W2).**
  At `ell >= 5` the chart has dimension `r = ell-2 >= 3` and the
  hyperplanes through a fixed PAIR of points form a family of
  dimension `r-2 >= 1` (q+1 of them at `r=3`), so the "shared root is
  determined by the pair" rigidity DISAPPEARS. **PREDICTION P-W2:
  the max packing at `ell = 5` (exact enumeration) is >= 5, and the
  round-23 "ell-invariant cap 4" is an artifact of `ell=5` having
  been SAMPLED.** FALSIFIED if exact enumeration at `ell=5` over
  >= 100 configurations still caps at 4.
- **H5 (the rate-quarter PROVED sibling instantiates (MF) with a
  growing flat).** At rate quarter `M=4`: `4ell+b=3k+1`, `b<ell`,
  hence `2ell>k-1` (`CONJECTURE_F_FALSE_GREEN_AUDIT_20260807.md:
  177-183`). The FPC5 cap is `d < ell(M-2) = 2ell` and `d <= N = k-1`,
  so `e = 2d+1-2ell` can reach `~k/2 = Theta(n)`. **PREDICTION P-PC:
  the rate-quarter cell satisfies codim = sigma, has `e` growing
  linearly in `n`, and has first moment far below 1 — i.e. it passes
  every clause of the round-23 shape-pun test and of the obvious
  mechanism battery — YET IT IS PROVED CLOSED with the absolute bound
  10.** This is my hardest power control (S4-P1). If P-PC holds, the
  round-23 test FAILS the power control: it groups a two-line-proved
  problem with an open one.
- **H6 (the upstream face is universally quantified, so matching it
  is nearly free).** Upstream `prob:capfr1-master-flatness`
  (`rs-mca/tex/cs25_cap_v13_2.tex:9253-9259`) quantifies over EVERY
  affine subspace of the monic degree-`j` space of codimension `s`,
  with NO growth clause. **PREDICTION P-UP: every congruence-cut
  split-locator cell in the repo — red 1, red 2, red 3, the L1
  root-free rational-Q cell, `f_global_packing_step`, and the PROVED
  rate-quarter sibling — is an instance of it.** A test that all six
  pass is a test with no discriminating power. Registered as an
  S3 outcome to be reported either way.

## R4. THE SEPARATION ATTEMPTS, each with its escape (registered)

### S4 — POWER CONTROL (RUN FIRST; nothing below is reported unless S4 is settled)

- **S4-P0 (the mandated coarse controls).** Apply the round-23
  shape-pun test to (red 1 vs mystery 6 clause (b)) and (red 1 vs the
  WCL slot certificate). PASS = the test refuses to instantiate.
- **S4-P1 (the HARD control — my addition).** Apply the round-23
  shape-pun test, and my mechanism battery, to (red 1 vs the PROVED
  rate-quarter `M=4,t=2` sibling `l1_fpc5_ratequarter_m4_t2_payment`).
  This pair is KNOWN-DIFFERENT in the only operational sense: one is
  PROVED with absolute bound 10, the other is open. **The test MUST
  separate them.** Computed by exact integer arithmetic at
  `k = 2^40` and at small cells: codim vs sigma, `e` growth, first
  moment, and the over-determination predicate `t*ell > N`.
  **VERDICT RULE:** if the shape-pun test does not separate this pair,
  the round-23 evidence has NO POWER and I must redesign before
  drawing ANY conclusion from S1/S2/S3.
- **S4-P2 (power control on the CAP-4 INSTRUMENT specifically).** The
  random-flat control W1 of R2. If a random flat of the same
  parameters reproduces cap 4, the instrument cannot distinguish the
  guarded structure from a generic flat and the handle carries no
  mechanism information.

### S1 — the cap-4 mechanism (red 1) vs red 2's Bonferroni-tight cap

Registered predictions: (i) red 1's Bonferroni/packing number at its
cell EXCEEDS its measured cap (H2 says it is unbounded, so this is
already derived — I will confirm numerically); (ii) red 2's equals its
measured cap. **ESCAPE-S1 (what would REFUTE separation):** if, after
the correct sharpening, red 1's measured cap is reproduced by a
packing/incidence bound expressible in `(N, j, r, lambda)` alone and
the SAME bound reproduces red 2's cap, the mechanism is common and
this is unification evidence. **ESCAPE-S1' (what would ESTABLISH
separation):** the ell=4 decision proves `<= 4` by an argument using
the plane rigidity of H3 (a `t=2`, `r=2` fact absent at `t=3`).
**ADJUDICATOR:** the `ell=4` finite decision, scoped honestly (see R5).

### S2 — instrument cross-response

Apply red 1's core-choosable adversary to red 2's LS6 chart at cells
whose `(j/N, h)` are matched to red 1's `(d/N, lambda)`, and compare
the measured functional
```text
CAPDEF(cell) := (Bonferroni-first-failure m) - (measured max packing)
```
**ESCAPE-S2:** if `CAPDEF` differs between the two reds at MATCHED
parameters, the cap phenomenon does not transfer under the (MF)
dictionary and the walls separate. If `CAPDEF` agrees at matched
parameters (i.e. the round-23 difference is fully explained by the
cells' `(d/N, lambda)` arithmetic), that is unification evidence.

### S3 — the upstream + fourth faces

Exact hypothesis matching of (MF) against (i) `prob:capfr1-master-
flatness` and (ii) the L1 root-free rational-Q cell
`l1_rootfree_rational_q_projective_packing`, on the round-19 gates:
quantifiers, coefficient class (`|B|^{-s}` vs `q^{-s}`), flat type
(congruence modulus with roots IN the domain vs root-free modulus),
and what "count" means. **ESCAPE-S3:** a face that matches only after
weakening (MF) to vacuity is NOT the same wall and will be reported
as such.

## R5. Scope declarations, honesty pins, CATCH-Z6 / CATCH-19C

- **The `ell=4` finite decision is SCOPED, and I say so in advance.**
  A literally exhaustive enumeration of `(L_0,L_1,L_2,c_1,c_2)` is
  infeasible even at the smallest admissible `q`: after normalizing
  by the two available symmetries (domain scaling `x -> ax`, fixing
  the single background point to 1; and `W -> alpha W`, fixing
  `c_1 = 1`), the residual count at `q = 37` is
  `C(35,4)*C(31,4)*(q-2) ~ 5.6e10` configurations. I will therefore
  deliver (a) the H3 STRUCTURE THEOREM (a 5-packing at `ell=4` exists
  iff a general-position K_5 exists in the connection graph), (b) a
  large randomized + STRUCTURED + hill-climbed search under that
  criterion, and (c) an explicit UNDECIDED label with the price if no
  5-packing is found and none is excluded. I will NOT call a search
  a proof.
- **Toy cells (CATCH-Z6).** Red 1's object exists only on the line
  `5ell = k+4`, which meets the 2-power grid only at `ell in
  {4,52,820,...}` (round-23 P1). `ell = 4` (`n = 32`) IS 2-power and
  is my control; `ell = 5, 6` (`n = 42, 52`) are declared NON-2-POWER
  and used because the object demands the line. Red 2's off-tail
  ladder is declared OFF-TAIL throughout. Cells used purely as
  MECHANISM probes with non-official `(ell,b,a)` are labelled
  NON-OFFICIAL.
- **Named measured functionals (CATCH-19C).**
  `MAXPACK(cell,config)` := max `m` with `m` distinct split chart
  members whose root sets union to `<= N` points;
  `NSPLIT(config)` := number of distinct split chart members;
  `BONF(N,j,h)` := `min{m : m*j - C(m,2)*h > N} - 1` (`+inf` if no
  such `m`);
  `CAPDEF` := `BONF - MAXPACK`;
  `RANDMAXPACK` := `MAXPACK` on a uniformly random flat of the same
  `(dim, d, q, allowed, N)`;
  `KDEG(config)` := the connection-graph edge count / clique number
  at `ell = 4`.
- **Honesty.** Census/search evidence is EVIDENCE, never proof, and
  gives LOWER bounds on true maxima — labelled at every use. Every
  self-correction stated plainly. No status flips, no promotion
  language. If S4-P1 shows the round-23 test is powerless, I say so
  and redesign rather than proceed.
