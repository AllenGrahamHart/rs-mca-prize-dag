# FABLE_AUDIT — f2_rlocality (round 22, agent 3 of 4 to report)

**Auditor:** Fable, 2026-08-07. **Verdict: BANKED, MAINTAINER-LEVEL —
the diagnosis the brief asked for, delivered with proofs: the 8.60
deficit is REAL ARITHMETIC ON THE WRONG LAYER (it is DEF_INSTR(1);
the binding-layer number of record is 6.3130), the deficit IS
STRUCTURAL (a formalized k-LOCAL class with exact LP floors: toy
floor 1.5889 proved at G1; lifted official-row floors 6.2063 (k=R) /
3.4848 (k=2R) as asymptotic evidence), and the banked instrument is
within +1.7% OF OPTIMAL for what R-wise independence allows. The
remaining 1.81x headroom provably collapses back onto V_1 at the
licensed radius. Mystery 2's requirement is now NAMED: a non-local,
essentially-EXACT smoothness count for the GRS value code — and its
nearest banked object is the crossing side's constant-weight
Z-FLOOR cell, making this the FOURTH lane to converge on the
constant-weight population instrument.**

Replay: verify_d1 22/0 (COROLLARY 8's p <= 8.30 reproduced
independently), verify_d2 13/0 (no licensed attempt beats 6.3130),
verify_d3 12/0 (the +1.7% optimality finding; P13 UNRESOLVED as
reported), verify_d3b dies on the ramguard wall at the k = 2R LP
EXACTLY as the pilot disclosed (never estimated, honestly
NOT-COMPUTED). 47 PASS / 0 FAIL total. REPORT.md persisted
verbatim (task a147942949e1e2b9a).

FORCED CORRECTIONS APPLIED (both flagged by the pilot, node
untouched by it; f2_z1_mass_knife_edge/statement.md byte-identical
master-vs-v12, merge-safe):
- **CATCH-RL1 (layer error):** the node now carries the corrected
  ledger — 8.5990 = DEF_INSTR(1) where R-locality in fact costs
  NOTHING (OPT_k(1) = p^{-k} exact, THE sharpest finding: the
  c = 1 requirement is met on the nose by pure R-locality with the
  knife-edge constant as the entire margin); binding-layer deficit
  6.3130; the internally inconsistent 0.443/0.116 sentence
  dissected (four different numbers were riding one label).
- **CATCH-RL2 (position-entropy artifact):** THEOREM 10's "dies at
  every p / H(1/L) > 1/L" diagnosis withdrawn — the exact R-local
  binomial moment cancels the position entropy; the route dies
  NUMERICALLY (deficit 258.9), so the ledger is unchanged but the
  wall is locality, not position entropy. Coordinator note filed
  in the tail_count dir.

ADOPTED: the four-factor decomposition (the identity telescopes;
at c* the lossiest step is the LOCALITY CAP 2.654, and the cap is
SHARP — N_{R+1} fails at all three toy rows with the banked G2
value reproduced); the k-LOCAL class formalization + LIFTING LEMMA
(the pilot's registered direction-of-safety discipline held: exact
toy floors labelled EXACT, official-row floors labelled ASYMPTOTIC
EVIDENCE, the un-licensed toy comparison NOT run); the D4
obligation with the 4.77-bit-window sharpening (the finite target
leaves room for NO multiplicative constant — the non-local input
must be essentially exact).

HONEST LEDGER accepted: 14 registered predictions HIT with the
registered error bands; P13 UNRESOLVED (LP infeasible past
p ~ 41 — reported, not extrapolated); seven self-corrections
including a sign error caught by a textbook smoke test, a missing
factor L caught by a negative exponent, unreliable low-probability
LP rows discarded BECAUSE they broke monotonicity, and a
direction-of-inequality fix on RL-6 (dual feasibility bounds from
above) — each caught by the pilot's own controls. scipy failing to
import inside the ramguard wall forced a from-scratch simplex:
the compute law held even against tooling friction. Quarantine
absolute; no subagents.

BOARD EFFECT (mystery 2): the R-local lane is CLOSED as a route
class — not by fiat but by an LP floor with the banked instrument
sitting on it. The mystery's open content is now a single named
non-local obligation, convergent with the crossing crux's
constant-weight instrument. The number 8.60 is retired from the
board; 6.3130 and the floors take its place. Round-23 brief
material: the constant-weight Z-FLOOR cell now serves TWO
mysteries — price it as the shared target.
