# FABLE_AUDIT — c2pp_diag (round 23, agent 1 of 4 to report)

**Auditor:** Fable, 2026-08-07. **Verdict: BANKED, MAINTAINER-LEVEL —
mystery 3's diagnosis is the round-21 pattern executed at its
sharpest: the pilot corrected MY brief's false premise BEFORE
computing (C2'' was not "posed but never attacked" — it survived TWO
F-rounds; the event my brief remembered was the 2026-07-07 kill of
the PREDECESSORS), designed F-round 3 as a genuinely new falsifier
family instead of replaying a twice-survived battery, and returned
the only kind of kill that needs no new transport judgement: the
pose's own theta-insensitivity claim REFUTED on its own 8 rows by
its own kill rule (fires at every theta in {2.5, 3, 4}; the 85%
margin was produced by a classification threshold two accident
classes cleared by 0.24 and 0.14). And then — the round's honest
peak — the pilot RETIRED ITS OWN 482% overflow reading together
with the survival it attacked, under the symmetric not-evidence
clause: both use the same uniform 33x stacking, and neither is
evidence. C2'' is now correctly recorded as UNMEASURED AT ITS OWN
QUANTIFIER DEPTH.**

Replay: all four scripts re-run clean (F-d FIRED with the scope
limits printed; theta fragility REFUTED reproduced with the exact
bulk jumps 0.0000 -> 2.2387 at (2,8353); accident multiplicity
non-conservative at 2/8 rows; the coset mechanism mu values and
33x transports exact). Positive controls inside the scripts: 8/8
banked raw ratios bit-exact, F-b's banked path reproduced
(1.066159 -> 14.523% vs banked 14.53%). REPORT.md persisted
verbatim (task a72521ce78de75db3).

COORDINATOR ACTIONS APPLIED:
- The round-23 adjudication addendum on
  critical/nodes/dli_c2pp_joint_reserve/statement.md: the binding
  symmetric not-evidence clause (retires round-2's margin and
  round-3's overflow TOGETHER; Pro's 2026-08-01 demotion now
  recorded on the node, closing C-7); the theta kill C-3 with the
  mandatory theta-pinning rule for any future three-part form; the
  selection-bias record C-2/C-4 (all three original falsifiers
  excluded the high-loss cells by three different mechanisms); the
  statement gap C-1 (the one-line `_reduced` qualifier; clause (i)
  conflates internal-correlation-freedom [true, iota = 1.0000]
  with contribution-freedom [false, 21.8x weight shift]; clause
  (iii) non-conservative at 2/8 rows).
- **C2''-r3 ADOPTED as the re-pose of record** (the junction-sum
  unreduced form; falsifiers G-a/G-b requiring >= 8 CONSECUTIVE
  junctions of a SINGLE tower; uniform stacking excluded on both
  sides). No status flip — the node was already TARGET; what
  changed is the POSE and the EVIDENCE LEDGER.
- node.json mechanical fixes (C-5 packet path critical/ not
  background/; C-6 rounds_survived; C-7 stale margin note replaced
  with the adjudication note; prior note preserved as historical).
- C-8: the roadmap FD-schema gate adjudicated — the c2pp first
  instance is UNSUPPORTED (selection-biased + theta-fragile);
  sections 03 + 07 updated; the FD schema currently has no live
  instance. Recompiled; verify chain green; census unchanged
  (TARGET stays TARGET).

ADOPTED FINDINGS (beyond the catches): the consumer contract from
CURRENT edges (Decision 6's req-wiring stands; the consumer needs
the UNREDUCED X(R) <= 2^21 A(R), exactly the form C2''-r3 states);
the cross-lane matrix (only the dli norm-gate family has
all-junction reach; U2/Z-2/WCL/ternary all fail with named
reasons — U2 is 42.6 bits lossy against a 21-bit reserve and
esg_lane_rescope formally unwires it); the convergence with the
independently-run nullity-structure route (the joint excess is
carried by the delta = 0 NONCOSET stratum at exactly the row where
the pilot's coset_leakage < 1 — the mechanism is row-dependent,
which the pilot also self-corrected into the record).

HONEST LEDGER accepted: PR2 refuted, PR4 untestable-as-posed (NaN
branch registered in advance so the escape still resolved), the
working coset-mechanism hypothesis wrong and said so, the
F-d-rests-on-the-same-transport disclosure that motivates the
symmetric clause. The pilot proposed no status flip and edited
nothing.

BOARD EFFECT (mystery 3): from "twice-survived conjecture with an
85% margin" to "well-posed junction-sum conjecture, UNMEASURED at
its own quantifier depth, with a named decisive instrument: a
>= 8-consecutive-junction single-tower measurement (Modal-scale,
M1-census-shaped), the bar set by the 32-wise trap." ROUND-24
CANDIDATE: spec + price that instrument (the M1 census harness is
the base; the compute request would be the first C2'' measurement
that is evidence under the new clause).
