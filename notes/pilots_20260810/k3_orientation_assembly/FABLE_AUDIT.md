# FABLE_AUDIT — k3_orientation_assembly (round 30)

Coordinator: Fable. Date: 2026-08-10. Pilot: Opus (~23 min, 100 tool
uses, 2 interpreter invocations). REPORT.md persisted verbatim by the
coordinator (harness blocked the pilot's write). Quarantine: one
disclosed grep leak (~15 matched lines from the sibling allocation
PREREG printed by an early recursive grep; file never opened; priors
already registered; no conclusion touches it) — accepted.

## Verdict

**BANKED. The routing theorem is NOT buildable today, and the report
proves why with the right citations: the node's domain (Z_BC bad
slopes) and codomain (workboard cells) are connected by NO proved
map — the bridge is explicitly disclaimed by the geometry's own
supplier ("The endpoint parameter line is not the evaluation
carrier"). Eight named obstructions (O1 bridge / O2 the live (2,8,1)
fourth class / O3 source-line image inexact / O4 source-cover object
ABSENT / O5 label-vs-slope unit gap / O6 preservation unstated /
O7 transpose not owner-safe / O8 chronology undefined at row level),
each with a pre-registered falsifier. Offsetting: the orientation
TRICHOTOMY is already a theorem (delta=|S| forces the three order-two
subgroups; the source-subfield dichotomy is exclusive), so a
conditional draft theorem with one named hypothesis (H-bridge) is
writable now — drafted in D3.4, coordinator-gated.**

## Coordinator verifications (mine)

| what | result |
|---|---|
| replay_orientation_images.py full replay | FAILURES: none (17 checks; B1-B10 positive workboard, C1-C3 negative, D1-b skeleton comparison, block A five-row census 1350/2700/3600/1800/720+225=10395) |
| O1 disclaimer | verbatim at decomposition_source_pencil_compiler/statement.md:48 |
| O2 (2,8,1) live | verbatim at attack_sections/06:47-48 |
| KBDM-3 five rows | verbatim at facet_mixing_obstruction/statement.md:42-46 |
| source-cover "Neither branch is yet excluded" | verbatim at source_subfield_dichotomy/result.md:6-7 |
| node.json:7 image-exactness clause | confirmed from my own wave-55 read |

## Audit judgements

- **M3/D1-c is a real node-text defect in Codex's lane**: "the
  source-line image is exactly the declared c2(1,1,2) workboard" is
  false as written unless rows (1,0,4)/(0,1,4)/(0,0,6) and orbit
  (KBDM-10) are separately deleted — no such theorem exists. The
  repair is a genuine choice (theorem vs scope restriction) in
  Codex's write lane: SURFACED, not applied.
- **O2 is the sharpest structural catch**: the live m=2 frontier is
  TWO types and the node covers one. Repair (b) (restrict + fourth
  ledger term) changes the ledger identity U_K3 = U_positive +
  U_sourcecover — coordinator/Codex-gated, not a pin.
- **FINDING D1-b (the two coordinate workboards are different
  five-skeleton lists, sharing three)** is new to the repo and
  directly relevant to any future counting assembly.
- The three novelty claims are properly subtracted (CATCH-24A greps
  quoted); four zero-power declarations (Z1-Z4) properly scoped.
- Blind priors honest: obstruction count landed at the upper edge
  (8 vs registered 5, interval 3-8); the trichotomy-not-priority
  surprise is exactly what priors are for.

## Corrections applied

None to nodes (K3 lane = Codex's write lane; all findings surfaced
via ledger + the wave-57 brief). Pilot dir banked as-is.

## Follow-ups filed (not executed)

- The conditional draft theorem (D3.4) is mint-ready once (H-bridge)
  is named as a node-level hypothesis — candidate for the next mint
  wave or a Codex handoff.
- The missing certificate is named exactly (D4.3: the biquadratic
  source-cover workboard, four rows x two passports) — Codex brief
  material.
- O2 repair decision (prove (2,8,1) empty vs restrict+fourth term) —
  needs Codex coordination; goes in the wave-57 brief.
