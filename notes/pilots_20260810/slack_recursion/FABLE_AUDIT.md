# FABLE_AUDIT — slack_recursion (round 29)

Coordinator: Fable. Date: 2026-08-10. Pilot: Opus (task ac3db0cd2feba501d,
~42 min, 44 tool uses). Quarantine marker: ledger line 4474, observed.

## Verdict

**BANKED. The supply side does not close — it closes the other way,
and the pilot followed its own pre-registered fallback rule when its
own measurement refuted its own registered direction. The
arbitrary-word maximum is now a THEOREM (the product word realizing
the classical Graham-Sloane construction; matching upper bound
within one bit), it GROWS past the razor-need line between n=16 and
n=32 — and the honest reading, which the pilot states rather than
buries, is a MODEL CRITIQUE: the t=1 model is provably unfaithful
to the razor's coset-scale supply question (a naive transport would
over-satisfy by ~115 bits, which nobody believes). The recursion is
killed with explicit counterexamples. Two of my banked lines are
corrected (the round-28 "same fate likely"; the round-27
"undetermined number" — now determined, opposite direction), and
the round-27 sampling frame's 6.4x under-measurement is quantified.
THE NAMED NEXT OBJECT: the (t,M) transport dictionary — C(127,64)
matches neither coset-level formula, so nothing transports until
the banked plateau derivation is reconciled.**

## Replays and verifications (mine)

| what | result |
|---|---|
| sr_trend.py (the theorem bracket table) | reproduced; the A/B bracket rows match the report |
| THEOREM A's structure | the Lagrange interpolant of 1/x on A is (L_A(0)-L_A(X))/(X L_A(0)) — checked; the criterion and flatness verified by the pilot in-situ at 4 scales + 3-field exhaustion at n=8 on TWO independent instruments |
| the ms_exact escape | the pilot's replay identical to the banked file I had myself replayed identically at the round-28 bank |
| self-corrections | 6 disclosed incl. its own headline prediction refuted and one no-op compute-law breach (a piped `python3 -c "pass"` that produced nothing) |

## Audit judgements

- **The R6 fallback firing is the discipline working**: the pilot
  registered "supply closes with margin" at high confidence, measured
  the opposite, and wrote the reversal as the headline. The growing
  curve is theorem-backed, not sampled — the strongest kind of
  reversal.
- **The model critique is the correct frame and changes the
  program**: the t=1 flank family (rounds 27-29's entire supply-side
  measurement campaign) is now known to be structurally different
  from the razor's t = 2^34 question. All t=1 verdicts stand AS
  MODEL FACTS; none transports. The (t,M) dictionary is the gate on
  everything.
- **Graham-Sloane entering the campaign is notable**: a classical
  construction, outside THEOREM CAP's scoped universe (consistent —
  CAP was honest about its universe), realized by a single explicit
  word. The novelty subtraction was run and honest (the code is
  classical; the word and the matching bound are new here).
- **Corrections cascade handled**: round-27 frame gap, round-28
  addendum line, the 5-vs-6 reconciliation — all applied via the
  dated-addendum idiom.

## Corrections applied

- critical/nodes/rate_half_band_crossing_location/statement.md —
  the round-29 slack addendum (the reversal, the theorems, the
  model critique, the named object, the frame-gap and banked-line
  corrections). No status flip.

## Follow-ups filed (not executed)

- THE (t,M) TRANSPORT DICTIONARY — the gate on all supply-side
  razor claims; needs the banked C(127,64) plateau derivation
  reconciled with the two coset-level formulas. Natural round-30
  anchor (and a Codex-assist candidate: the witness-hunt derivation
  is banked in its territory).
- MINT_PACKAGE.md (Theorems A-D with proofs + harness) — queue.
- The product-word object at the coset-faithful scale — after the
  dictionary, not before.
