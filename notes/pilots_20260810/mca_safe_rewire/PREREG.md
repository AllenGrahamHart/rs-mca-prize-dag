# PREREG — mca_safe_rewire (round 28)

Coordinator brief, written before dispatch. The pilot appends its own
registrations BELOW the brief BEFORE any computation. THIS IS AN
AUDIT-AND-DRAFT PILOT: the actual surgery stays coordinator-gated;
your deliverable is the verified draft + the seam map.

## Mandate

THE NAMED FOLLOW-UP SURGERY from the band decomposition: weaken
mca_safe's rate-1/2 premise from the strong rate_half_band_closure
node onto the PROVED HD1 (rate_half_half_distance_safe_bracket:
B_mca(3n/4) <= n <= floor(q/2^128) for q >= 2^169). The resolved
lead of record: mca_safe's own claim is B_C(a_safe) <= B* at "the
certified safe agreement" — a_safe is textually FREE at the mca_safe
level; the adjacency burden lives downstream (mca_grand consumes
mca_safe's certified index AND mca_unsafe's index AND
adjacency_closing's assertion that they are ADJACENT). If the
rewire is sound, mca_safe's rate-half dependence collapses onto a
PROVED node and the full location burden concentrates in
adjacency_closing — a real simplification of the consumer chain.
YOUR JOB: verify every link of that reasoning at audit grade, map
the seams exactly, and draft the surgery.

## Deliverables

**D1 — THE QUANTIFIER AUDIT (the load-bearing read).** Round 27
resolved the lead from two blind directions, but at
reading-and-verdict depth. Do it at audit depth: (i) mca_safe's
statement, conditional, and node wiring — is a_safe genuinely free,
or does any downstream consumer (mca_grand's assembly, the
stratified-sum convention in counting_frame, the dedup/first-match
conventions) pin a_safe to a specific index or range? (ii) If
mca_grand's adjacency composition requires mca_safe's certified
index to BE a_RH (the same index adjacency_closing certifies
adjacent to the unsafe one), then the rewire changes mca_grand's
logical surface — map exactly what mca_grand would then consume
from where. State the verdict: REWIRE SOUND AS-IS / SOUND WITH A
STATED ADJUSTMENT TO mca_grand's conditional / UNSOUND (with the
pinning clause quoted).

**D2 — THE ROW-FAMILY SEAM MAP (the part nobody has done).**
HD1 holds for q >= 2^169. The wave-10 staircase determines the
crossing (hence the safe half) unconditionally for q < 2^167. THE
GAP: q in (2^167, 2^169) — 2 bits of q-axis. Map exactly: (i)
which admissible rows of mca_safe's quantifier fall in the gap
(admissibility: q prime, q = 1 mod n, the row conventions of
s0_zero_open/mixed_radix_frontier — read them); (ii) what covers
the gap TODAY (candidate: the wave-10 bracket a_RH <= n gives a
safe point at a = n trivially? Check whether B_mca(n) <= B* is
free — full agreement; be precise about what "safe half" needs
there); (iii) confirm the round-27 D4 cross-link from the OTHER
side: if the apolar theorem closes residual budget 2^39+1, HD1's
domain extends to all q > 2^167 and the gap closes — verify that
implication from the primary texts, not the round-27 report.
Also map the razor slice explicitly (q in (2^255.9, 2^256) subset
of q >= 2^169 — trivially, but SAY it) and the deployed extension
rows (n = 2^21 — does mca_safe's quantifier even include them, or
are they K3's rows only? The tiling seam of the decomposition —
this is the FIRST rigorous pass at it).

**D3 — THE SURGERY DRAFT.** The exact edits, file by file:
mca_safe's conditional.md premise list and proof paragraph (the
rate-1/2 clause re-pointed at HD1 with the gap's coverage stated);
mca_safe's node.json requires (does rate_half_band_closure leave
the req list? Does HD1 enter? What happens to the ev edges?); any
text in mca_grand or adjacency_closing that names the old wiring.
Every edit as an exact old-string/new-string pair, ready for the
coordinator to apply and verify. Include the census impact
prediction (does anything leave/enter the critical orbit?).

**D4 — THE CONSEQUENCE AUDIT.** After the draft: what breaks?
Run the checkable parts — grep every consumer of mca_safe and of
rate_half_band_closure for text that assumes the old wiring; check
the verify chain's partition/status-propagation rules against the
drafted shape (a CONDITIONAL premise leaving a req list has
validator implications); list every doc that cites "the strong
rate_half_band_closure premise" (the compiled roadmap, QUALITY
certs, board entries) needing a conforming edit.

## Escape tests (before the main work)

- Reproduce the round-27 reads: mca_safe/conditional.md:96-99 and
  mca_grand/conditional.md:34-40 say what the lead resolution
  claims they say (quote them).
- Verify HD1's verifier passes (SCRATCH COPY if it writes).

## Rules (binding)

- QUARANTINE: do not read notes/pilots_20260802/CAMPAIGN_LEDGER.md
  at or below line 4302; do not read the other round-28 pilot dirs
  (apolar_origin, ssparse_endpoints, maxscan_algorithm). Pass this
  clause to any subagent verbatim.
- COMPUTE LAW: every interpreter run via
  `tools/ramguard tiny|local -- python3 ...` from the repo root —
  including file patching and JSON peeking.
- BANKED SCRIPTS FROM SCRATCH COPIES ONLY.
- RAM DISCIPLINE: file-at-a-time reads; never open dag.json (use
  node.json shards + grep); no bulk loads.
- DRAFT-ONLY: writes only in notes/pilots_20260810/mca_safe_rewire/;
  no dag/nodes/tools writes; no git; no Modal; stdlib only. THE
  SURGERY IS A DRAFT — the coordinator applies it after replay.
- Register predictions BEFORE reading beyond the named anchors;
  misses first. Every quantifier claim quoted with file:line
  (CATCH-24C discipline). Own-repo grep gates every "nothing covers
  the gap" claim (CATCH-24A).
- Your final message IS the report. End with a compliance
  paragraph.

## Pilot registrations (appended by the pilot before any computation)
