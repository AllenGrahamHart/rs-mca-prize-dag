# PREREG — mca_safe_rewire (round 28)

Coordinator brief, written before dispatch. The pilot appends its own
registrations BELOW the brief BEFORE any computation. THIS IS AN
AUDIT-AND-DRAFT PILOT: the actual surgery stays coordinator-gated;
your deliverable is the verified draft + the seam map.

## Mandate

THE NAMED FOLLOW-UP SURGERY from the band decomposition: weaken
mca_safe's rate-1/2 premise from the strong rate_half_band_closure
node onto the PROVED HD1 (rate_half_half_distance_safe_bracket:
B_mca(3n/4) <= n <= floor(q/2^128) for q >= 2^169). The resolved
lead of record: mca_safe's own claim is B_C(a_safe) <= B* at "the
certified safe agreement" — a_safe is textually FREE at the mca_safe
level; the adjacency burden lives downstream (mca_grand consumes
mca_safe's certified index AND mca_unsafe's index AND
adjacency_closing's assertion that they are ADJACENT). If the
rewire is sound, mca_safe's rate-half dependence collapses onto a
PROVED node and the full location burden concentrates in
adjacency_closing — a real simplification of the consumer chain.
YOUR JOB: verify every link of that reasoning at audit grade, map
the seams exactly, and draft the surgery.

## Deliverables

**D1 — THE QUANTIFIER AUDIT (the load-bearing read).** Round 27
resolved the lead from two blind directions, but at
reading-and-verdict depth. Do it at audit depth: (i) mca_safe's
statement, conditional, and node wiring — is a_safe genuinely free,
or does any downstream consumer (mca_grand's assembly, the
stratified-sum convention in counting_frame, the dedup/first-match
conventions) pin a_safe to a specific index or range? (ii) If
mca_grand's adjacency composition requires mca_safe's certified
index to BE a_RH (the same index adjacency_closing certifies
adjacent to the unsafe one), then the rewire changes mca_grand's
logical surface — map exactly what mca_grand would then consume
from where. State the verdict: REWIRE SOUND AS-IS / SOUND WITH A
STATED ADJUSTMENT TO mca_grand's conditional / UNSOUND (with the
pinning clause quoted).

**D2 — THE ROW-FAMILY SEAM MAP (the part nobody has done).**
HD1 holds for q >= 2^169. The wave-10 staircase determines the
crossing (hence the safe half) unconditionally for q < 2^167. THE
GAP: q in (2^167, 2^169) — 2 bits of q-axis. Map exactly: (i)
which admissible rows of mca_safe's quantifier fall in the gap
(admissibility: q prime, q = 1 mod n, the row conventions of
s0_zero_open/mixed_radix_frontier — read them); (ii) what covers
the gap TODAY (candidate: the wave-10 bracket a_RH <= n gives a
safe point at a = n trivially? Check whether B_mca(n) <= B* is
free — full agreement; be precise about what "safe half" needs
there); (iii) confirm the round-27 D4 cross-link from the OTHER
side: if the apolar theorem closes residual budget 2^39+1, HD1's
domain extends to all q > 2^167 and the gap closes — verify that
implication from the primary texts, not the round-27 report.
Also map the razor slice explicitly (q in (2^255.9, 2^256) subset
of q >= 2^169 — trivially, but SAY it) and the deployed extension
rows (n = 2^21 — does mca_safe's quantifier even include them, or
are they K3's rows only? The tiling seam of the decomposition —
this is the FIRST rigorous pass at it).

**D3 — THE SURGERY DRAFT.** The exact edits, file by file:
mca_safe's conditional.md premise list and proof paragraph (the
rate-1/2 clause re-pointed at HD1 with the gap's coverage stated);
mca_safe's node.json requires (does rate_half_band_closure leave
the req list? Does HD1 enter? What happens to the ev edges?); any
text in mca_grand or adjacency_closing that names the old wiring.
Every edit as an exact old-string/new-string pair, ready for the
coordinator to apply and verify. Include the census impact
prediction (does anything leave/enter the critical orbit?).

**D4 — THE CONSEQUENCE AUDIT.** After the draft: what breaks?
Run the checkable parts — grep every consumer of mca_safe and of
rate_half_band_closure for text that assumes the old wiring; check
the verify chain's partition/status-propagation rules against the
drafted shape (a CONDITIONAL premise leaving a req list has
validator implications); list every doc that cites "the strong
rate_half_band_closure premise" (the compiled roadmap, QUALITY
certs, board entries) needing a conforming edit.

## Escape tests (before the main work)

- Reproduce the round-27 reads: mca_safe/conditional.md:96-99 and
  mca_grand/conditional.md:34-40 say what the lead resolution
  claims they say (quote them).
- Verify HD1's verifier passes (SCRATCH COPY if it writes).

## Rules (binding)

- QUARANTINE: do not read notes/pilots_20260802/CAMPAIGN_LEDGER.md
  at or below line 4302; do not read the other round-28 pilot dirs
  (apolar_origin, ssparse_endpoints, maxscan_algorithm). Pass this
  clause to any subagent verbatim.
- COMPUTE LAW: every interpreter run via
  `tools/ramguard tiny|local -- python3 ...` from the repo root —
  including file patching and JSON peeking.
- BANKED SCRIPTS FROM SCRATCH COPIES ONLY.
- RAM DISCIPLINE: file-at-a-time reads; never open dag.json (use
  node.json shards + grep); no bulk loads.
- DRAFT-ONLY: writes only in notes/pilots_20260810/mca_safe_rewire/;
  no dag/nodes/tools writes; no git; no Modal; stdlib only. THE
  SURGERY IS A DRAFT — the coordinator applies it after replay.
- Register predictions BEFORE reading beyond the named anchors;
  misses first. Every quantifier claim quoted with file:line
  (CATCH-24C discipline). Own-repo grep gates every "nothing covers
  the gap" claim (CATCH-24A).
- Your final message IS the report. End with a compliance
  paragraph.

## Pilot registrations (appended by the pilot before any computation)

### mca_safe_rewire (Opus pilot, round 28) — registered before any read beyond this PREREG

Registered before ANY computation, ANY grep, and ANY read other than this
PREREG file itself. I have not yet opened mca_safe/conditional.md,
mca_grand/conditional.md, or any node.json. These are blind predictions
from the coordinator brief alone. Scoring: I will report each as HIT /
MISS / PARTIAL, misses first.

**P1 (D1 verdict).** SOUND WITH A STATED ADJUSTMENT to mca_grand's
conditional — 55%. SOUND AS-IS (no mca_grand text change needed) — 30%.
UNSOUND (a clause pins a_safe) — 15%. Reasoning: the brief's own lead
says mca_grand consumes mca_safe's certified index AND adjacency_closing's
adjacency assertion; an assembly that composes two indices almost always
names them, and naming is where a pin hides. But a pin at mca_safe's own
level (which is what UNSOUND would require) is unlikely given round 27
read it twice blind.

**P2 (where the pin, if any, lives).** If anything pins a_safe it is in
mca_grand's conditional/proof text, NOT in mca_safe's statement and NOT
in counting_frame's stratified-sum convention — 70%. I predict
counting_frame's convention is a *counting* convention (each n counted
once, sum over strata) that is indifferent to WHICH a is certified, and
therefore does not pin — 65%.

**P3 (dedup / first-match).** I predict there IS a first-match or
minimal-index convention somewhere in the counting/row conventions
(s0_zero_open or mixed_radix_frontier or counting_frame) — 45% — but
that it applies to the ROW index (n, or the radix row), not to the
agreement index a — 75% conditional on it existing.

**P4 (D2, what covers the (2^167, 2^169) gap today).** My primary
prediction: NOTHING in the repo covers the gap with a single node; the
gap is covered only by the trivial full-agreement point (a = n, or
a = a_max) if B_mca at full agreement is <= B* — 50%. Secondary: the
wave-10 staircase's own domain actually extends past 2^167 and the "gap"
is an artifact of a conservatively stated bound, so the true gap is
narrower or empty — 30%. Tertiary: some third node (a bridging bracket)
already covers it — 20%.

**P5 (the trivial full-agreement point).** I predict B_mca at full
agreement is NOT free — i.e. the a = n / full-agreement point does not
automatically satisfy B_mca(a) <= B*, because B_mca is (I expect) an
increasing function of the agreement parameter over the relevant range,
so the LARGEST agreement is the WORST case, not the free one — 60%.
If so the gap is NOT covered by triviality and D2(ii) must report a
genuine hole. I flag now that if this fires, the honest D2 answer is
"the gap is open" and the rewire is conditional on the apolar cross-link
(P6) or must retain a fallback premise.

**P6 (D2(iii), the apolar cross-link).** I predict the round-27 D4
cross-link VERIFIES from primary texts — i.e. closing residual budget
2^39+1 does extend HD1's domain to all q > 2^167 — 60%; but I predict
the implication is stated in the primary texts only up to an arithmetic
step I will have to redo myself (the 2^39+1 -> 2 bits of q conversion),
and I register in advance that 2^169 / 2^167 = 4, so a "2 bits" claim
needs a factor-4 budget improvement, and 2^39+1 is a budget in a
DIFFERENT unit — I predict I will have to do a unit-reconciliation and
that this is where a round-27 error, if any, will be found — 40% that I
find a unit or off-by-a-power discrepancy.

**P7 (the razor slice).** q in (2^255.9, 2^256) is trivially inside
q >= 2^169 — 95% HIT (this is arithmetic; I register it only because
the brief asks me to SAY it).

**P8 (deployed extension rows, n = 2^21).** I predict mca_safe's
quantifier does NOT include the n = 2^21 deployed rows — they are K3's
rows — 55%; i.e. the decomposition tiles by row family and mca_safe owns
a different family. Alternative: mca_safe's quantifier is stated over
all admissible (n, q) and the deployed rows ARE inside it, with K3
merely giving a sharper statement there — 45%.

**P9 (D3, node.json shape).** rate_half_band_closure LEAVES mca_safe's
requires list and HD1 (rate_half_half_distance_safe_bracket) ENTERS —
75%. That the ev edges need a change too (not just requires) — 60%.
Number of distinct FILES needing an edit in the surgery draft: I predict
4 (mca_safe conditional.md, mca_safe node.json, one of
mca_grand/adjacency_closing text, one compiled/doc file) — point
estimate 4, 80% interval [3, 8].

**P10 (census impact).** I predict rate_half_band_closure does NOT leave
the critical orbit as a result of this rewire, because it has other
critical consumers besides mca_safe — 60%. I predict HD1 is ALREADY in
the critical orbit (so nothing enters) — 65%. Net census delta on the
critical count: 0 — 55%.

**P11 (D4, consumers).** Number of distinct files (excluding the pilot
dir and the quarantined ledger) whose TEXT names the old wiring
("rate_half_band_closure" in connection with mca_safe, or "the strong
rate-1/2 premise"): point estimate 6, 80% interval [2, 15].

**P12 (validator).** I predict the drafted shape does NOT break the
verify chain's status propagation — a CONDITIONAL node may drop a premise
and stay CONDITIONAL — 70%; but I predict there IS a rule somewhere that
ties a node's status to its requires-edge statuses which I must check by
reading the validator source, and 25% that the drafted shape trips it
(most likely failure mode: an orphaned edge left in dag/edges or an
ev-edge referencing a now-absent requirement).

**P13 (overall).** I predict I will report at least one genuine
self-correction (a claim I make mid-pilot and then retract) — 70%. I
predict at least one CATCH-grade finding (something the coordinator did
not know and that changes the surgery) — 65%.

**Falsifier for the pilot as a whole.** If mca_safe's conditional.md
contains, in its statement or premise list, any clause of the form
"where a is the agreement index certified by adjacency_closing" (or any
text binding a_safe to the adjacency pair), the lead of record is WRONG
and I report UNSOUND with that clause quoted, regardless of how much
downstream work that costs.

