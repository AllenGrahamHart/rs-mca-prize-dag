# REPORT.md — mca_safe_rewire (round 28) — persisted verbatim by the coordinator (Fable) from the pilot's final message, task a67a757b8516dc8c5, 2026-08-10. Pilot: Opus. No edits.

All work verified. Here is the report of record.

---

# mca_safe_rewire — round-28 audit-and-draft pilot, report of record

## VERDICT (first)

**D1 — THE QUANTIFIER AUDIT: UNSOUND.** The named premise-weakening surgery must **not** be applied. `a_safe` is unbound in `mca_safe`'s own prose (the round-27 lead is textually correct) but it is **bound by its consumers**, and the naive swap makes a consumer's claim false-or-open.

The pinning clause, quoted, is in a node the brief did not name — **`critical/nodes/mca_unsafe/statement.md:9`** (identically `critical/nodes/mca_unsafe/node.json:12`):

&gt; `B_C(a_safe - 1) &gt; B*: cap above 1-rho-2^-9 (proved) + witnesses at the adjacent grid point (unsafe_at_crossing).`

`mca_unsafe` states its claim **at `a_safe - 1`, reusing `mca_safe`'s own symbol**. Two further clauses complete the binding: `critical/nodes/mca_grand/statement.md:9` / `node.json:13` — "For each admissible C: **exhibit** adjacent `a` with `B_C(a-1) &gt; floor(q_line/2^128) &gt;= B_C(a)`" (the verb is *exhibit*, so `a` is a located index, not a mere existential) — and `critical/nodes/mca_grand/conditional.md:34-37` (= `94-97`): "`mca_safe` supplies the safe-side inequality **at the certified agreement index `a`** … `adjacency_closing` supplies the final missing assertion that **these two certified indices are adjacent**."

**The decisive argument is a reductio, and it does not depend on reading any of those clauses charitably.** `B_C(a)` is **nonincreasing** in `a` — `critical/nodes/crossing_localization/statement.md:9` (PROVED): "B_C(a) is nonincreasing and integer-valued in a, so a unique a\* with B_C(a\*-1) &gt; B\* &gt;= B_C(a\*) exists UNCONDITIONALLY for every row." Therefore a safe point at `3n/4` bounds **nothing** at any smaller index; HD1 is an upper **bracket end**, never the safe half **at** the crossing. And if `a_safe` really were free, `mca_safe`'s rate-1/2 instance would be discharged **with no field restriction at all** by the PROVED `mca_full_agreement_endpoint` — `background/nodes/mca_full_agreement_endpoint/statement.md:12` (FA1) `B_mca(n)=1`, `:23` "is a certified safe endpoint at and above it" (i.e. every `q &gt;= 2^128`), `:29` (FA3) `k+8,594,128,896 &lt;= a_RH(q) &lt;= n`. That route is **strictly stronger than HD1** (no `q &gt;= 2^169` floor, and it has been in-repo since wave 6). A claim dischargeable by a wave-6 triviality does not need eight premises. The eight premises are the proof that `a_safe` is pinned.

Concretely: the swap would re-point `mca_safe`'s rate-1/2 claim to `3n/4`, whereupon `mca_unsafe`'s claim becomes `B_C(3n/4 - 1) &gt; B*` — which holds **iff `a_RH = 3n/4` exactly**, i.e. iff candidate **(RH-AC-hi)** is tight. That is one of the two named endpoint candidates with, in the repo's own words, "**NO discriminating evidence held**" (`critical/nodes/rate_half_band_crossing_location/statement.md:32-34`). **The surgery would not discharge the open content; it would silently move it from `mca_safe`'s premise list into `mca_unsafe`'s claim, where it is currently justified by a clean-rate mechanism.** That is a hidden burden-shuffle, not a simplification.

## WHAT COVERS THE GAP

The gap `(2^167, 2^169)` is covered today by the **trivial full-agreement bracket top**, and by nothing else. Primary text, `critical/nodes/rate_half_band_closure/statement.md:164`:

&gt; "beyond 2^167, brackets only: a_RH in [k+2^34, 3n/4] for q &gt;= 2^169, **[k+2^34, n] otherwise**."

The top `n` is FA1/FA3 above (PROVED, all admissible `q`). **My registered prediction that full agreement would *not* be free was wrong** — it is free, `B_mca(n) = 1 &lt;= B*(q)` for every `q &gt; 2^128`, and correcting that error is what produced the reductio.

**But the framing in the brief needs a correction, and this is the load-bearing D2 finding: `(2^167, 2^169)` is a gap in the BRACKET TOP, not in the safe half.** HD1 never supplied "the safe half"; it supplied a bracket end. Above `2^167` there is **no safe-half-at-the-crossing input anywhere** — including the razor slice, including all of `q &gt;= 2^169`. So closing the gap restores the `3n/4` bracket top on 2 bits of `q`-axis; it gives no consumer its safe half. Under the *free*-`a_safe` reading the gap does not exist at all (FA1 covers every `q`); under the *pinned* reading — the correct one — the uncovered range is `[2^167, 2^256)`, ~89 bits, not 2.

**CATCH-24A gate.** I ran own-repo greps for `2^167`/`2^169`, for determination/safe-bound language across every `statement.md` in `critical/` and `background/`, and for node ids matching rate-half safe/bracket/crossing patterns. Nothing else covers it. The repo's own exhaustive sweep corroborates — `statement.md:183-184`: "No safe-side theorem at or near it exists (exhaustive own-repo sweep: the only rate-half MCA safe upper bounds are RQ1/RQ2 (q &lt; 2^166.5), HD1 (a = 3n/4), and the lossless sparse-layer identity)" (that inventory omits FA1, correctly, since FA1 is not "at or near sigma\*").

**The apolar cross-link (D2 iii) VERIFIES from primary text, with one precision fix.** `statement.md:441-447` claims closing residual budget `2^39+1` "extends the proved bracket top to ALL q &gt; 2^167 — a 2-bit window". Verified independently: `(RH-SPLIT)` is a **max**, not a sum (`statement.md:124`: `B_mca(a)=max(B_ca^far(a), S_sparse(a))`), and HD1's own proof (`background/.../proof.md:9-22`) bounds the sparse side by `r = 2^39` already, so the far-CA term alone imposes the floor. The Hankel layer's form is `B_ca^far(n-r) &lt;= r+1` for `r &lt;= 2^39-2` (`statement.md:162`); at `r = 2^39` that reads `&lt;= 2^39+1`. **Units reconcile exactly** — the budget is a slope count, the same unit as `B*` — so the threshold is `2^39+1 &lt;= floor(q/2^128)`, i.e. `q &gt;= (2^39+1)·2^128 = 2^167 + 2^128`, `log2 = 167.0000000000026`. **Fix: "ALL q &gt; 2^167" overstates by the sliver `[2^167, 2^167+2^128)`**, which is precisely residual budget `2^39`'s own interval — so the "all q &gt; 2^167" claim holds for the **pair** of budgets, not for `2^39+1` alone. I exhibited the point concretely: the **smallest admissible row above `2^167`** is `q = 187072209578355573530071658587684226537949598056449` (prime to 64 MR rounds, `q ≡ 1 mod 2^41`, `q &lt; 2^256`), and it has `B* = 2^39` **exactly**, sitting inside that sliver. The staircase would put its crossing at `3n/4 + 1` — one step *above* HD1's safe point, confirming HD1's `2^169` field floor is **sharp, not slack**.

**Razor slice:** `(2^255.9, 2^256) ⊂ [2^169, 2^256)` trivially, `255.9 &gt; 169`. Saying it as asked: every razor row is inside HD1's field domain, so HD1's bracket top `3n/4` holds at every razor row — and, per the above, still supplies no safe half there.

## IS THE TILING SOUND? NO — a second, independent catch

**The 2026-08-09 band decomposition's children do not tile their parent's quantifier.**

- Parent's own row scope: `n=2^41, k=2^40, 2^128&lt;q&lt;2^256` (`statement.md:66-68`).
- Child 2 (`rate_half_band_crossing_location`) poses **razor rows only**: "At every admissible razor row (n = 2^41, k = 2^40, **q prime in (2^255.9, 2^256)**, q = 1 mod n)" (`statement.md:11-12`).
- Child 1 (`rate_half_band_structural_surplus`) is scoped to **`n = 2^21` extension workboard rows** and says so in terms (`statement.md:35-38`): "transport to the n = 2^41 prime razor rows is NOTE-LEVEL (the WP5 quantifier mismatch stands) … **not as razor-row coverage**."
- Parent's own text: "beyond 2^167, **brackets only**" (`statement.md:164`).

So rate-1/2 rows at the official `(n,k)` with **`q ∈ [2^167, 2^255.9]`** are inside the consumers' quantifier and located by nothing. That directly contradicts `critical/nodes/adjacency_closing/conditional.md:97-98`: "if the re-posed rate-half premise holds, the for-each-admissible-row quantifier has **no remaining exception**." It does have one. This is drafted as **E7**, ratification-gated, because it narrows a consumer's claimed scope.

**Deployed-row membership (asked explicitly):** the `n = 2^21` rows are **K3's rows, not `mca_safe`'s** — the whole rate-half node family is scoped to `n=2^41,k=2^40`, and child 1 disclaims razor-row coverage itself. *Adjacent flag, outside my mandate:* `rules_freeze/statement.md:9` states the caps as `k &lt;= 2^40` (an upper bound), and `official_row_primes_pinning` records ABF26 quantifying "for every choice of F, L, and k". I grepped and found **no node** establishing that `k = 2^40` is the extremal rate-1/2 row or reducing smaller-`k` rate-1/2 rows to it. Not asserting a hole — flagging an unexamined quantifier.

## DELIVERABLES

All in `/home/u2470931/smooth-read-solomin/prize/notes/pilots_20260810/mca_safe_rewire/`:

- **`edits.json`** — the surgery draft: 8 exact old-string/new-string pairs (E1, E2, E3, E4, E5, E5b, E6, E7) plus 4 doc-conformance items left unpaired. **Draft A (the naive swap) is recorded as REJECTED** with the pinning clauses; **Draft B (applied draft)** is the retirement + conforming edits. E1 is the load-bearing one (`mca_safe/conditional.md`, dated correction section in the `rate_half_band_closure` precedent style, history preserved). E5/E5b **must be applied together** (`statement.md` + `node.json`, since the shard is the DAG source). E7 is **ratification-gated**.
- **`check_edits.py`** — verifies every `old_string` occurs **exactly once** (Edit-tool precondition) and simulates `verify_conditional_propagation.py`'s check-3 against the **post-edit** text. Result: **8/8 OK, all five touched CONDITIONALs CLEAN.**
- **`census_impact.py`** — rebuilds the req/ev/alt graph from **node.json shards only** (dag.json never opened) and reproduces the validator's pins exactly: `orbit=239 TARGET=32 CONDITIONAL=40 rounds=8`, matching `tools/verify_conditional_propagation.py`'s `EXPECTED_TARGETS=32 / EXPECTED_CONDITIONALS=40 / EXPECTED_ROUNDS=8` and the live run `CONDITIONAL_PROPAGATION_PASS targets=32 conditionals=40 … rounds=8`. That agreement validates the reconstruction.
- **`gap_witness.py`** — the explicit admissible gap rows and the sliver arithmetic.

**Census impact of Draft B: ZERO** (no edge changes). **Census impact had Draft A been applied** (measured, not estimated): `orbit 239 → 242`; entering are exactly `rate_half_half_distance_safe_bracket`, `mca_from_ca_reduction`, `half_johnson_ca` — **all three PROVED**, so `TARGET 32`, `CONDITIONAL 40`, `rounds 8` are all unchanged and the three numeric pins survive. `rate_half_band_closure` would **not** leave the critical orbit: it has exactly two requires-consumers, `mca_safe` and `adjacency_closing` (verified by grep over all shards), and `adjacency_closing` keeps it.

## D4 — CONSEQUENCE AUDIT

- **Validator implication of Draft A (the real one, and it is not the one I predicted).** Removing the edge while leaving the name in `mca_safe`'s prose **fires check 3** of `tools/verify_conditional_propagation.py`: `rate_half_band_closure` would no longer be an ancestor, ev-parent, or descendant of `mca_safe`, so the mention becomes a "NEW unrelated open mention" — the tool's own words: "this is how an unwired hypothesis appears". Measured, both directions (`ancestor=True → benign` before, `ancestor=False → FIRES` after). Note the scan covers `node.json`'s `statement`/`notes` too, and `mca_safe/node.json:13` names the node. Draft B avoids this entirely.
- **Validator implication of Draft B: none.** Check-3 CLEAN on all five touched CONDITIONALs; three numeric pins untouched.
- **Baselines established green before drafting:** `verify_prize_dag.py` → `PASS: structure, refs, acyclicity, reachability, status propagation`; `verify_conditional_propagation.py` → PASS; `verify_unsafe_crossing_status_regression.py` → `UNSAFE_CROSSING_STATUS_REGRESSION_VERIFIED`. That last one pins `mca_unsafe` req-parents `== {cap_theorem, unsafe_at_crossing}` and its status `CONDITIONAL`; E5/E5b touch neither, and it enforces no token check on `mca_unsafe`'s statement text. All three verifiers confirmed **write-free** by grep before I ran them.
- **Docs naming the old wiring** (grep-gated, archive and quarantine excluded): `mca_safe/conditional.md:43` **and** `:96-97` — **note these are two different bodies saying different things**, the head body ("included only through the strong `rate_half_band_closure` premise") and the WAVE-9 pin body ("it is the safe half of the rowwise adjacent certificate"); `mca_grand/conditional.md:38-39` and `:98-99`; `adjacency_closing/conditional.md:41` and `:92-98`; `rate_half_band_closure/conditional.md:34-37`; `rate_half_band_crossing_location/statement.md:67-71`; `rate_half_band_closure/statement.md:240-243`; `notes/PRIZE_RESOLUTION_ROADMAP.md:22921-22922` and `:22939` ("next analytic audit: test whether mca_safe's rate-half consumer bar is already met" — now audited and refuted); `notes/floor_campaign/SURVEY_OTHER_LANES.md:49` (quote stays true under Draft B, no edit needed); `notes/amber_audit_20260706/ledger.md:172` (dated history, leave).
- **Escape tests both pass.** `mca_safe/conditional.md:96-99` and `mca_grand/conditional.md:34-40` say what the lead resolution claims — quoted above. HD1's verifier: `RATE_HALF_HALF_DISTANCE_SAFE_BRACKET_PASS agreement=1649267441664 radius=549755813888 budget=2199023255552`; I confirmed by reading it that it performs no writes, so no scratch copy was needed.

## PREDICTIONS vs OUTCOMES — misses first

**MISSES.** **P5** — I predicted `B_mca` was *increasing* in the agreement, so full agreement would be the worst case and not free (60%). Flatly wrong: it is nonincreasing and `B_mca(n)=1` is free at every admissible `q`. This was the most consequential error I made and correcting it *produced* the verdict. **P1** — I put 55% on "sound with a stated adjustment", 30% on "sound as-is", only 15% on UNSOUND; the answer is UNSOUND. **P2** — I predicted the pin, if any, would live in `mca_grand`'s conditional; the decisive pin is in `mca_unsafe/statement.md:9`, a node I did not name (the `counting_frame` half was right: it is a per-`A` counting convention and pins nothing). **P9** — I predicted `rate_half_band_closure` would leave `mca_safe`'s requires and HD1 enter (75%); no edge changes at all. **P10, second clause** — I predicted HD1 was already in the critical orbit (65%); it is `background/`, ev-edge only, hence outside, and would have *entered*. **P12, mechanism** — I named "orphaned edge / ev-edge referencing an absent requirement" as the likely validator trip; the actual trip is the prose-mention audit (check 3).

**Also a miss, and a structural one: my own pre-registered pilot falsifier was mis-scoped.** I wrote that only a binding clause *inside `mca_safe`* would make me report UNSOUND. No such clause exists — so my falsifier, taken literally, would have licensed a SOUND verdict. I report UNSOUND anyway, on the basis of the consumer-side binding plus the reductio, and I flag the falsifier as wrongly localised rather than quietly satisfied.

**HITS.** **P4** (primary branch — the gap is covered only by the trivial full-agreement point, exactly `statement.md:164`); **P3** (a first-match/dedup convention exists, and applies to support/ray selection, not to `a`); **P6** (cross-link verifies; units reconcile; and I did find the discrepancy I forecast — the `2^128` sliver, not a unit error); **P7**; **P8**; **P10, first and third clauses** (`rate_half_band_closure` stays critical; net TARGET/CONDITIONAL delta 0); **P9's file-count interval** (8 drafted edits over 7 files, inside `[3,8]` on files); **P11** (≈8 files, inside `[2,15]`); **P12, conclusion** (Draft B does not break status propagation); **P13** (both clauses — ≥1 self-correction and ≥1 catch-grade finding).

## SELF-CORRECTIONS (all disclosed)

1. **Monotonicity direction** (P5 above) — registered backwards, corrected against `crossing_localization/statement.md:9` before it entered any conclusion.
2. **Mid-pilot verdict reversal** — after reading `adjacency_closing`'s bracket language ("the bracket between the two sides collapses to one grid step. Current per-rate corridor widths are 1.12-2.17 grid steps", `statement.md:9`) I was heading for SOUND-WITH-ADJUSTMENT. Reading `mca_unsafe/statement.md:9` flipped it to UNSOUND. Disclosed rather than presented as a straight-line derivation.
3. **`edits.json` `_meta.validator_impact` was wrong when first written** — I asserted two `AUDITED_UNRELATED` entries were *required*. After finalising E1's wording (which says "the unsafe-side node" instead of spelling the ids) I measured that they are not. The field is rewritten **in-file** with the correction stated, and the conditional case is preserved because it constrains any rewording.
4. **I introduced a JSON syntax error** in `edits.json` inserting E7 (missing comma); my own checker caught it on the next run and it is fixed.

## COMPLIANCE

Registrations were appended to the PREREG with the Edit tool **before any computation and before any read other than the PREREG itself** — I had not opened `mca_safe/conditional.md`, `mca_grand/conditional.md`, or any `node.json` at registration time. **QUARANTINE fully respected:** I never opened `notes/pilots_20260802/CAMPAIGN_LEDGER.md` at any line, and never read `pilots_20260810/apolar_origin`, `ssparse_endpoints`, or `maxscan_algorithm`; every recursive grep explicitly excluded all four paths. **No subagents were used**, so the clause needed no onward transmission. **COMPUTE LAW: every interpreter invocation went through `tools/ramguard tiny|local -- python3` from the repo root — 12 of 12, including one-liners and JSON peeks; zero bare `python3`.** Non-interpreter shell (`ls`/`grep`/`find`/`cat`) was used only for search and small reads. **BANKED SCRIPTS:** I ran four banked verifiers (HD1's `verify.py`, `verify_prize_dag.py`, `verify_conditional_propagation.py`, `verify_unsafe_crossing_status_regression.py`) only after confirming by inspection/grep that **none of them writes**; no scratch copies were therefore required, and none were skipped for convenience. **RAM DISCIPLINE:** I never opened `dag.json` — the census was rebuilt from `node.json` shards and cross-validated against the live validator's own output; reads were file-at-a-time; two oversized grep outputs were immediately re-run with `dag.json` excluded and truncated. **DRAFT-ONLY:** every write landed inside `notes/pilots_20260810/mca_safe_rewire/` (`PREREG.md` registrations, `edits.json`, `census_impact.py`, `gap_witness.py`, `check_edits.py`); no `dag/`, `nodes/`, or `tools/` file was modified; no git, no Modal, stdlib only. **The surgery is a draft — nothing has been applied.** Every quantifier claim above is quoted with `file:line` (CATCH-24C), and every "nothing covers it" claim is gated by own-repo grep (CATCH-24A) with the gate described.
