# FABLE_AUDIT — mca_safe_rewire (round 28)

Coordinator: Fable. Date: 2026-08-10. Pilot: Opus (task a67a757b8516dc8c5,
~23 min, 91 tool uses, 12/12 compliant interpreter runs). Quarantine
marker: ledger line 4302, observed.

## Verdict

**BANKED — and the audit-and-draft gate earned its existence twice in
one report. D1 = UNSOUND: the HD1 premise-weakening I resolved as a
lead on 2026-08-09 and recorded on three surfaces is WRONG, by a
reductio I verified from primary text: a_safe is unbound in mca_safe's
prose but pinned by its consumers (mca_unsafe states its claim at
a_safe - 1 with the SAME symbol; mca_grand must EXHIBIT the adjacent
pair), B_mca is nonincreasing so HD1's 3n/4 bracket end bounds
nothing at the crossing, and — the decisive stroke — a genuinely free
a_safe would be discharged unconditionally by the wave-6 PROVED
full-agreement endpoint (FA1: B_mca(n) = 1) with no field floor,
which is absurd for a claim carrying eight premises. The naive swap
would have silently moved the open content into mca_unsafe's claim,
where it would hold iff (RH-AC-hi) is tight — a hidden burden-shuffle
onto an undiscriminated endpoint. SECOND CATCH: the decomposition's
children did not tile the parent's quantifier — rows q in
[2^167, 2^255.9] were located by nothing. Both catches corrected this
bank.**

## Coordinator verifications (all mine, from primary text)

| what | result |
|---|---|
| the pinning clause | mca_unsafe/statement.md:9 verbatim: "B_C(a_safe - 1) > B*" — the shared symbol confirmed |
| the monotonicity engine | crossing_localization/statement.md:9 verbatim: B_C nonincreasing, unique a* unconditionally |
| the reductio's triviality | FA1 "B_mca(n)=1" + "certified safe endpoint at and above it" + FA3, verbatim from mca_full_agreement_endpoint |
| check_edits.py replay | ALL PRECONDITIONS OK; all touched CONDITIONALs CLEAN |
| census_impact.py replay | BEFORE 239/32/40/8 matches the live validator; Draft-A counterfactual 242 with three PROVED entrants, pins unchanged — measured, not estimated |
| gap_witness.py replay | the sliver arithmetic exact: budget 2^39+1 needs q >= 2^167 + 2^128; the witness prime sits inside the sliver |

## Corrections applied (this bank)

1. **Draft B, all eight pairs (E1-E7 + E5b), applied verbatim** after
   precondition replay: the retirement section on mca_safe's
   conditional; the lead-withdrawal corrections on
   rate_half_band_closure's conditional + statement and the
   crossing-location child's consumer bars; the SYMBOL BINDING
   annotation on mca_unsafe (statement + shard together); the
   same-index clarification on mca_grand; the SCOPE SEAM flag on
   adjacency_closing.
2. **The tiling repair (coordinator, beyond the draft — the exit the
   ratified design intent demands):** rate_half_band_crossing_location's
   quantifier WIDENED from razor-only to all admissible
   2^167 < q < 2^256 (statement + shard + title + notes; the refuted
   lead text in the shard's notes replaced; the E7 seam flag given a
   same-day RESOLUTION note on adjacency_closing). The widening
   EXPANDS the child's obligation — the honest direction — and the
   [2^167, 2^167 + 2^129) budget interval of the widened range is
   exactly the apolar target's payoff territory.
3. Verify chain green after all edits, including
   verify_conditional_propagation (the pilot's predicted check-3
   exposure avoided by Draft B's design); census unchanged
   239 = 167/40/32 (pose changes, no status changes).

## Audit judgements

- **This is the round-27 D0 pattern repeating one level up**: a lead
  resolved at reading depth (by me, twice-reinforced by blind
  convergence) fell at audit depth to a consumer-side binding in a
  node nobody had named. The lesson compounds: quantifier freedom
  claims need the CONSUMERS' consumers read, not just the node and
  its direct consumer. The pilot's own registered falsifier was
  mis-scoped the same way (binding-inside-mca_safe only) — it
  reported UNSOUND anyway and flagged its falsifier as wrongly
  localised rather than quietly satisfied. Exemplary.
- **The prediction record is a model of informative failure**: P1 hit
  the 15% branch; P5 (monotonicity) was registered backwards and its
  correction PRODUCED the verdict; P2's pin location was wrong
  (mca_unsafe, unnamed in the brief). Misses first, all disclosed.
- **The D2 reframe matters strategically**: the (2^167, 2^169) "gap"
  was never a safe-half gap — above 2^167 there is NO
  safe-half-at-the-crossing input anywhere; the uncovered range under
  the correct reading is ~89 bits of q-axis, all of it now owned by
  the widened RH-AC child. The apolar cross-link verified with the
  2^128-sliver precision fix (the "all q > 2^167" payoff holds for
  the budget PAIR, not 2^39+1 alone) — and HD1's 2^169 floor is
  SHARP, witnessed.
- **Out-of-mandate flag noted, not chased**: no node establishes
  k = 2^40 as the extremal rate-1/2 row (the k <= 2^40 quantifier
  unexamined). Filed below.
- **Compliance clean**: registrations before ANY read beyond the
  PREREG (blind priors, scored honestly); quarantine total; 12/12
  ramguard; write-free verifiers confirmed by inspection before
  in-place runs; draft-only held.

## Follow-ups filed (not executed)

- THE k-QUANTIFIER FLAG: does anything reduce rate-1/2 rows at
  k < 2^40 to the k = 2^40 row, or is k = 2^40 extremal by a proved
  argument? Unexamined; a small dedicated read (the round-29
  candidate list).
- The apolar pilot's payoff statement should absorb the 2^128-sliver
  precision fix at its bank (its brief predates it).
- mca_grand's E6 same-index clarification may deserve promotion into
  the statement field at the next mca_grand-touching wave.
