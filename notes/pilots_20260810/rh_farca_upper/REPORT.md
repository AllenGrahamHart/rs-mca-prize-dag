I must disclose one accidental bare-interpreter no-op in that last command. Report follows.

The harness forbids me writing `REPORT.md` (subagent report-file restriction), so per the brief's fallback clause the full report text is returned verbatim below.

---

# REPORT — rh_farca_upper (round 32)

## MISSES FIRST

1. **THE BRIEF'S BAR IS MISSTATED, AND I REGISTERED THE MISSTATEMENT AS TRUE.** The brief says "`2^X` for `X < 216` (the current trivial payload bound)". There is no `2^216` upper bound in the repo. `2^216` is a **lower** bound one index *below* the razor: "*the PROVED simple-pole floor's received pair is COLUMN-FAR … B_ca^far(k+2^34-1) >= 2^216.0000 vs B* = 2^128, 88 bits inside unsafe*" (`critical/nodes/rate_half_band_crossing_location/statement.md:280-284`). So the true bar is **any finite upper bound at all**, and the honest arithmetic of the target is far worse than the brief implies: the safe half needs `B_ca^far` to fall from `>= 2^216` to `<= 2^128` across **one unit of agreement** — an 88-bit drop per unit (`d1_prices.py`). My PR-I registered `2^216` as an upper bound to beat; that reading was wrong and I found it wrong myself.
2. **My route-(c) mechanism prediction (PR-B) was right in outcome and wrong in mechanism.** I predicted the wide regime forces `e=0` through `(MI1)` and therefore collides with LB1. The collision is real and the clash factor is exactly the registered `63.0` — but the break is *upstream* of `(MI1)`: the object `Q_Z` **does not exist** in the wide regime, so `e` is undefined and `(MI1)` has no instance. I measured this; I did not predict it.
3. **UB-FIXED, my strongest conditional, covers a class I measured at ~0 density.** `(HK1)` held in **0 of 1,700** random column-far planes across four cells (`d5_minindex_results.txt`), and `dim K_0 = 0` at every census maximiser. The class is nonempty (I construct it), but it is thin. I lead with this rather than with the 94-bit margin.
4. **PR-G(i) is a partial miss.** I registered "`rho_cell` at the razor is `< 2^-6e11`". True at `q >= 2^167` (`-6.704e11`), **false at `q = 2^128`**, where it is only `2^-387,286,153` (`d1_prices.py`). Still `<< 1`, but not what I wrote.
5. **PR-E was proved and NOT MEASURED.** The two-maximal-core lemma is a two-line proof and its arithmetic is checked (`d1_prices.py`), but I dropped the planned small-cell verification for wall budget. It is stated as an unverified lemma.
6. **The `(9,2,a=5)` cell is effectively unsampled and I quote no maximum from it.** 40,000 samples out of `[7,2]_11 ~ 10^10` planes is a `4e-6` fraction; its "max T = 6" is a lower bound with negligible power.
7. **No exhaustive census exists at any cell with `rho_cell < 1`** — i.e. at no cell in the razor's own regime. Every exhaustive number I report comes from cells where a *random* pair already has several bad slopes; the razor's expected count is `2^-6.7e11`. This is the round's dominant zero-power fact and it was pre-registered.
8. **One bare-interpreter breach**, disclosed in COMPLIANCE: a no-op `python3 -c "print()"` probe. No computational content, no bearing on any result, but it is a literal violation of the compute law.
9. **I did not move the object.** `B_ca^far(k+2^34)` has no unconditional upper bound after this round either. What I hand back is one unconditional bound on a **stratum**, one conditional bound on a **thin class**, a structural diagnosis of why every route in the brief fails, and a sharp reformulation.

---

## VERDICT

**The open bracket is bounded by two classical radii, and the razor sits below both.**

- The Hankel/GNF layer's "`a > 3n/4`" scope **is the unique-decoding radius**: `r <= (d-1)/2 = R/2` ⟺ `a >= n - R/2 = 3n/4` exactly (`d1_prices.py`: `n-R/2 = 1,649,267,441,664 = 3n/4`). Below it the pencil is **wide** — `(R-r) x (r+1) = 17,179,869,184 x 1,082,331,758,593` at the razor — so full column rank is impossible, every slope has a nonzero kernel, and the layer's counting mechanism is **void, not weakened**.
- Every incidence/counting instrument (first-level slope Fisher, second-level core Fisher, k-subset counting, anchored list bounds) has its threshold at `a = sqrt(nk) = 0.70710678 n`. The razor is at `a/n = 0.5078125` exactly. Both walls are above it.

**D1(c) answer: the obstruction is STRUCTURAL, and I have located the exact step and refuted it by measurement.** `rate_half_ca_hankel_minimal_index_budget/statement.md:22-23` asserts, with no radius hypothesis, "*The generic right and left kernels are generated by shifts of one homogeneous degree-`rho` apolar form `Q_Z`*". In the wide regime that is **false**: at an explicit column-far `RS[F_11,{0..6},2]` pencil with `r=3`, `R=5`, `rho=2` (so `1<=rho<=r`, the node's own quantifier), the generic kernel has **gcd degree 0** at 10 of 11 slopes instead of the required `rho=2` (`d6_kernel_results.txt`). The single-generator step is what carries `(3) delta = rho-Ae`, hence `(MI1)` and `(MI2)`; without it, `e` is undefined and the budget has no instance. Any naive instantiation is violated by measured data: `(MI2)` at `s=0,e=1` gives `<= 3` against a measured `T = 8`.

**D2 answer: two first bounds, both honest, neither sufficient.**

> **UB-NEAR (unconditional).** Every column-far pair at the razor row is *a fortiori* column-far at every smaller radius, so the banked corollary applies at `r' = R/2-2`: **`#{CA-bad slopes with agreement >= 3n/4+2} <= 2^39-1 = 549,755,813,887`**, `89.00` bits under `2^128`. This is the **first upper bound of any kind on a stratum of `B_ca^far` inside the open bracket**, and it makes the residual exact: the entire exposure is the **deep stratum**, error weight beyond the unique-decoding radius.

> **UB-FIXED (conditional on `(HK1)`).** `rate_half_ca_hankel_fixed_kernel_branch` is PROVED with **no radius hypothesis** (`statement.md:13-26`, `proof.md:1-45`, `node.json` quantifier: "*an RS syndrome Hankel pencil*"). In the wide regime `rho <= R-r = a-k`, so for every column-far pair whose generic kernel is fixed, **`B_ca^far(a) <= a-k`** — `2^34 = 17,179,869,184` at the razor (**94.00 bits of margin**) and `< 2^39` across the whole open bracket (**> 89 bits**). The residual class is exactly the *moving* generic kernel — which is precisely round-29's flagged-absent branch, and precisely where LB1 lives.

**D3 answer: the floor is NOT tight in the interior.** The working conjecture `B_ca^far = n-a+1` is **false**. Two independent refutations: (i) measured — max `T` is `2.000 x (r+1)` at `(7,2,a=4)` and `2.500 x (r+1)` at `(8,3,a=5)`, stable across `q in {11,13,17,23,31}`; (ii) banked — `B_ca^far(k+2^34-1) >= 2^216` while `n-a+1 = 2^39.98` there (`crossing_location:283`). **A genuinely new measured law:** the extremal count is **`q`-independent** — identical maxima `8` and `10` while `q` rises `11 -> 31` and the bad fraction of `P(W)` falls `0.667 -> 0.250`.

**Neither half of RH-AC closes. Status unchanged.**

---

## D1 — THE ROUTE MAP, PRICED

### (a) R-LINEDEGREE — **DEAD by vacuity, quantitatively**

The anchored line degree is `t = |L(f_2, a-s)|`, worst case at agreement `2a-n = 2^35 = 34,359,738,368`. Every list instrument in the repository needs one of two thresholds, and `2a-n` fails both by exact powers of two (`d1_prices.py`):

| instrument | needs | has | short by |
|---|---|---|---|
| `k`-subset ratio `|L| <= C(n,k)/C(A,k)` | `A >= k = 2^40` | `A = 2^35` | `2^5 = 32` **exactly** |
| Johnson (`rate_half_list_integer_johnson_safe_anchor`) | `A > sqrt(nk) = 1,554,944,255,987` | `A = 2^35` | `2^5.5 = 45.2548` |
| Johnson at the **best** case `A = a` | `a > sqrt(nk)` | `a = 1,116,691,496,960` | **1.3924564** |

The third row is the decisive one: **the entire open bracket lies below the Johnson radius**, so no list instrument reaches *any* agreement in it, at any anchor. The `k`-subset bound is non-void only for anchors of error weight `s <= a-k = 2^34`, i.e. `2^-6` of the admissible `s`-range, and even then `C(n,k)/C(a,k) = 2^{2.07e12}`. **Route (a) cannot be repaired by a better anchor; it needs a sub-Johnson list instrument, which is exactly what the campaign does not have.**

### (b) THE MOVING-KERNEL BRANCH — **it IS the residual, and it is now named**

Round 29 flagged it absent. It is not absent, it is *the complement of everything proved*:

- `fullrank_branch` (`statement.md:9,22,25`): hypothesis `r<R/2` and `(HF1) rank = r+1`; conclusion `T <= r+1`. Full column rank needs `R-r >= r+1`, i.e. `r <= (R-1)/2` — **the tall pencil, i.e. unique decoding**. The node itself records the boundary: "*At the last CA-only block `B*(q)=2^39+1`, one has `r=R/2`; the matrix has only `r` rows and `r+1` columns, so generic rank deficiency is automatic*" (`:41-42`).
- `fixed_kernel_branch` (`(HK1)/(HK2)`): **no radius hypothesis** — this is UB-FIXED, D2 below.
- `minimal_index_budget` `(MI1)/(MI2)`: the moving case, and **it does not survive below `3n/4`** (next paragraph).

So below `3n/4` the trichotomy collapses to a dichotomy: *fixed kernel* (bounded, `T <= rho <= a-k`) versus *moving kernel* (**nothing**). LB1 is a moving-kernel object: `T = r+1 > rho`, so by the PROVED `(HK2)` its pencil must violate `(HK1)`. Round-31's LB1 is therefore best read as **the first certificate that the moving-kernel branch is non-vacuous inside the open bracket**.

### (c) THE GNF/CATALECTICANT ROUTE — **structural break, located to one sentence**

The equivalence itself is unrestricted: "*For `1<=r<=R`, form the `(R-r) x (r+1)` Hankel matrix*" (`split_pencil_equivalence/statement.md:14`), and `(HS3)` identifies the finite CA-bad slope set for a column-far pair (`:36-42`). The scope sentence "*`r=B*(q)-1<=R/2`*" (`:44-45`) is a parameter remark, not a hypothesis. So the transport is legal; what dies is the budget.

Chain of failure, each link file:line:

1. `minimal_index_budget/statement.md:22-23` asserts the generic kernels are **shifts of one degree-`rho` apolar form `Q_Z`**.
2. `proof.md:61-72` upgrades this to "*Hence all `nu_R=n_c-rho`, `nu_L=m-rho` right and left minimal indices equal `e`*".
3. `proof.md:79-85` derives the Kronecker identity `(nu_R+nu_L)e + delta = rho`, i.e. `delta = rho - Ae` with `A = R+1-2rho`; `proof.md:88-90` bounds the rank-drop slopes by `delta`.
4. `(MI1)`, `(MI2)` follow; `statement.md:39-40` disposes of `e=0` via the fixed-kernel theorem.

**Link 1 is false in the wide regime, and I refute it by exhibition.** For the RS`[F_11, D={0,…,6}, k=2]` pencil `y_0 = (5,8,7,5,4)`, `y_1 = (4,10,7,4,3)` (`r=3`, `R=5`, column-far, `rho=2`, so `1<=rho<=r`): `ker M(gamma)` has dimension `2 = r+1-rho` at **every** slope, rank `= rho` at every slope, and the **gcd of the kernel is a constant** (degree `0`) at 10 of 11 slopes (`d6_kernel_results.txt`). A shift family `{Q_gamma, x Q_gamma}` has gcd `Q_gamma` of degree `rho = 2`. Two further cells reproduce it: `(8,3,a=5,q=11)` gcd degrees `{0}`, `(7,2,a=4,q=7)` gcd degrees `{0,1}`.

**Why, structurally.** The kernel of `M_r(y)` is `Ann(Phi_y)_r`, and for a binary form the apolar ideal is a complete intersection with generator degrees `p` and `R+1-p`. The degree-`r` piece is the shift family of the low generator **iff `r < R+1-p`**; when `r >= R+1-p` the **second generator enters**, and the kernel is a 2-generated truncation, not a shift family. Since `rho = min(r+1, p, R-r)`, the generic wide pencil has `rho = R-r` and `p >= R+1-r`, i.e. **the second generator always enters in the wide regime**. This is the same wall as `r > R/2`, seen apolarly.

**Is the obstruction quantitative or structural? STRUCTURAL.** There is no "worse constant" version: without a single generator there is no `Q_gamma` to divide the split locator (`proof.md:146-147`), so the divisor-counting term `floor((N-s)e/d)` has nothing to count. Numerically, instantiating `(MI2)` anyway (`s=0`, `e=1`, which is what the measured minimal indices `[1,1]` would suggest) gives `<= 3` against measured `T = 8`, and `<= 4` against measured `T = 9`.

**CATCH — SCOPE DEFECT, FLAGGED NOT APPLIED (AUDIT-AND-DRAFT).** `minimal_index_budget/claim_contract.md:7-9` declares the quantifier "*every field, every evaluation domain, and every syndrome Hankel pencil of generic rank `1<=rho<=r` in the split-pencil equivalence*", and `claim_contract.md:22` names as falsifier "*a Hankel pencil violating `(MI1)` or `(MI2)`*". The `r<=R/2` hypothesis appears only inside the proof's corollary section (`proof.md:181`, `:253-255`) and in the statement's "Official rate-half corollary" (`statement.md:55-61`). **The deployed corollary is untouched and remains correct — the pencil is tall there and link 1 holds.** What needs narrowing is the general statement's quantifier and the claim contract: they should read `r < R/2` (equivalently: "the generic kernel is a shift family"). I apply no surgery; this is for the coordinator.

---

## D2 — THE STRONGEST PARTIALS

### UB-NEAR (unconditional; the first upper bound inside the open bracket)

> **UB-NEAR.** Let `(f_1,f_2)` be column-far at radius `r`, with `R/2 < r <= R-1` (any `a` in the open bracket, in particular the razor `a = k+2^34`). Then
> ```
> #{CA-bad slopes gamma with dist(f_1+gamma f_2, C) <= R/2-2}  <=  R/2-1 = 2^39-1.
> ```
> Equivalently `#{bad slopes of agreement >= 3n/4+2} <= 549,755,813,887`, i.e. **89.00 bits below `2^128`**.

*Proof.* Column-farness is a strict inequality on one integer — the column distance to `C^2` (`split_pencil_equivalence/statement.md:28-34`) — so column-far at radius `r` implies column-far at every `r' < r`. Apply the banked corollary at `r' = R/2-2`: "*For `N=2^41, R=2^40, r<=R/2-2`, every column-far syndrome pencil has at most `r+1` supported finite slopes*" (`minimal_index_budget/statement.md:55-61`). Its supported slopes are exactly the slopes of distance `<= r'` by `(HS1)/(HS3)`. ∎

*Scope, stated plainly:* this bounds a **stratum**, not `B_ca^far`. The complementary deep stratum (`error weight in (R/2-2, r]`, agreement in `[a, 3n/4+2)`) carries the whole difficulty — and carries all of LB1, whose slopes have error weight exactly `r`.

*Correction it forces (flagged, not applied).* `crossing_location/statement.md:275-280` says the Hankel layer "*never touches the bracket interior*". Strictly, it does: it discharges the near stratum of every bracket-interior pair. The operative P0 conclusion — the far-CA term is not discharged on the open bracket, and is binding — is **unaffected and remains true**.

### UB-FIXED (conditional on `(HK1)`, unconditional in `r`)

> **UB-FIXED.** Let the pair be column-far at radius `r > R/2` and suppose the generic right kernel of `M(Z)` is defined over `F` (`(HK1)`). Then
> ```
> B_ca^far(n-r)  <=  rho  <=  min(R-r, r+1)  =  R-r  =  a-k .
> ```
> At the razor: `<= 2^34 = 17,179,869,184`, **94.00 bits below `2^128`**. Across the whole open bracket `a < 3n/4`: `<= a-k < 2^39`, **> 89 bits**.

*Proof.* `rate_half_ca_hankel_fixed_kernel_branch` `(HK2)` verbatim (`statement.md:21-26`), whose proof (`proof.md:1-45`) uses only: `(HK1)`; `M_0v=M_1v=0` for `v in K_0`; column-farness plus `(HS2)` to give `K_0 cap D_r(D) = empty`; and `deg Delta <= rho` for one nonzero `rho x rho` minor. **No step uses `r < R/2`.** The wide-regime input is only `rho <= R-r`, which is forced by the shape. ∎

*Class size, measured and unflattering.* `(HK1)` held in **0/500, 0/500, 0/400, 0/300** random column-far planes at `(7,2,a=4,q=7)`, `(7,2,a=4,q=11)`, `(8,3,a=5,q=11)`, `(9,2,a=5,q=11)` (`d5_minindex_results.txt`). The class is **nonempty**: the syndromes annihilated by a fixed irreducible `P` of degree `p` form a `p`-dimensional space, every member has `rho <= p`, and irreducibility forbids a `D`-split multiple, hence column-farness. Constructed and verified at two cells — `(9,2,a=5,r=4,q=11)` and `(8,1,a=4,r=4,q=11)`, all 55 monic irreducible quadratics over `F_11`: **55/55 column-far, `rho = 2` in every case, `dim K_0 = 3 = nu_R` so `(HK1)` holds, and `T = 0 <= rho`; 0 violations** (`d4_deficient_results.txt`, `d5_minindex_results.txt`). Control: `P` split over `D` gives **0/20 column-far** — all column-close, exactly as the mechanism predicts.

### Two exact by-products

> **INJECTIVITY (and the honest trivial bound).** For a column-far pair, `sigma in ker M(gamma) cap ker M(gamma') cap D_r(D)` with `gamma != gamma'` would put `sigma` in both endpoint kernels, hence column-close by `(HS2)`. So the map (bad slope) → (its split locator) is injective and **`B_ca^far(a) <= C(n,r) = C(n,a) = 2^{2,198,635,969,270}`**. Verified in vivo: every bad slope in `d6_kernel_results.txt` carries exactly one split locator and all are distinct. This is the only *unconditional* bound on the full object that exists, and it is `2^{2.2e12}` — ten orders of magnitude in the exponent worse than `2^216`. **No counting-over-`a`-sets argument can do better** (PR-I, HIT).

> **THE COMMON KERNEL IS HUGE (the reformulation to hand forward).** Kronecker: `sum(eps) + sum(eta) + delta = rho`, and `dim K_0 = #{i : eps_i = 0} >= nu_R - rho = r+1-2rho`. In the wide regime `rho <= R-r = a-k`, so at the razor
> ```
> dim (ker M_r(y_0) cap ker M_r(y_1))  >=  r+1-2(a-k)  =  1,047,972,020,225,
> ```
> and column-farness says this `~2^40`-dimensional space contains **no** squarefree `D`-split degree-`r` polynomial. **RH-KER (reformulation).** `B_ca^far(k+2^34)` counts the slopes at which the `<= 2^34`-dimensional increment `ker M(gamma) / K_0` acquires a member of `D_r(D)`, given that `K_0` itself has none, and distinct slopes acquire distinct members. *Zero power declared:* this inequality is vacuous (`>= 0`) at every reachable cell — measured `dim K_0 = 0` there, consistent and tight.

---

## D3 — SCALED MEASUREMENTS

Regime control, registered before running: `rho_cell = C(n,n-a)/q^{a-k-1}` is the expected bad-slope count of a random 2-plane. Razor: `2^-6.7e11` at `q = 2^167`. Every reachable cell has `rho_cell >= 1`. **Cells with `a-k = 1` are degenerate** (`rho_cell = C(n,a)`, independent of `q`) — pre-registered as zero-power, and confirmed: at `(6,3,a=4)` and `(7,3,a=4)` and `(6,2,a=3)` the measured max is `q+1` or `q` for every `q in {7,…,31}`, i.e. *every* slope is bad (`d3_wide_results.txt:5-92`). Nothing about the razor can be read off those.

Informative cells (`a-k >= 2`), exhaustive where marked:

| cell `(n,k,a,r)` | `q` | mode | column-far planes | **max T** | `r+1` | ratio | `C(n,r)` | `q+1` |
|---|---|---|---|---|---|---|---|---|
| (7,2,4,3) | 7 | **exhaustive** (140,050) | 138,139 | **7** | 4 | 1.75 | 35 | 8 |
| (7,2,4,3) | 11 | sample 160k | — | **8** | 4 | **2.000** | 35 | 12 |
| (7,2,4,3) | 13 | sample 120k | — | **8** | 4 | 2.000 | 35 | 14 |
| (7,2,4,3) | 17 | sample 40k | — | **8** | 4 | 2.000 | 35 | 18 |
| (7,2,4,3) | 23 | sample 40k | — | **8** | 4 | 2.000 | 35 | 24 |
| (7,2,4,3) | 31 | sample 40k | — | **8** | 4 | 2.000 | 35 | 32 |
| (8,3,5,3) | 11 | sample 90k | — | **10** | 4 | **2.500** | 56 | 12 |
| (8,3,5,3) | 13 | sample 30k | — | **10** | 4 | 2.500 | 56 | 14 |
| (8,3,5,3) | 17 | sample 30k | — | **10** | 4 | 2.500 | 56 | 18 |
| (9,2,5,4) | 11 | sample 40k (`4e-6` of the space) | — | 6 | 5 | — | 126 | 12 |

**Finding 1 — the floor is NOT tight in the interior.** Max `T` exceeds `n-a+1` at every informative cell, exhaustively at `(7,2,4,q=7)`. The working conjecture `B_ca^far(a) = n-a+1` is **refuted at small scale**, and independently at full scale by the banked `B_ca^far(k+2^34-1) >= 2^216` versus `n-a+1 = 2^39.98` there. I do **not** pose it.

**Finding 2 (new, and the one I would carry forward) — the extremal count is `q`-INDEPENDENT.** Across `q in {11,13,17,23,31}` the max is *exactly* `8` at `(7,2,4)` and *exactly* `10` at `(8,3,5)`, while the bad fraction of `P(W)` falls monotonically `0.667 -> 0.250` and the whole `T`-distribution shifts left. Two cells, five and three fields; **no law is fitted from two cells** — but the qualitative reading matters for the razor, where `q ~ 2^167` and `n = 2^41`: if the extremum is governed by `(n,k,a)` rather than `q`, the `2^128` budget is not the binding scale.

**Finding 3 — LB1 replays exactly in the wide regime.** At `(7,2,a=4,r=3,q=11)` with core `E = {0,1,2}` and `T = {3,4,5,6}`, LB1 gives `T = 4 = r+1` finite bad slopes whose split locators are precisely the four 3-subsets of `{3,4,5,6}` — the petal structure, verbatim (`d6_kernel_results.txt`). Column-far in `195/200` random `lam`-assignments there, `200/200` at `(8,3,5)` and `(9,2,5)` (`d3_wide_results.txt`). LB1 is real, it is not extremal, and its pencil is moving-kernel (`dim K_0 = 0`).

**Finding 4 — how much of the space UB-FIXED can ever cover.** Since `(HK2)` is proved, `T > rho` certifies `(HK1)` fails. At `(7,2,4,q=7)`, exhaustively, **80.80%** of column-far planes have `T > rho` (`111,622 / 138,139`); at `(8,3,5,q=11)`, `85.5%`; at `(9,2,5,q=11)`, only `1.0%`. The fraction tracks `rho_cell`, so it does **not** transport to the razor (where a random pair has no bad slopes at all and is trivially inside UB-FIXED). What does transport is the direct `(HK1)` measurement: `0/1700`.

**Finding 5 — the deficient stratum is empty at `R-r = 2`.** Exhaustively at `(7,2,4,q=7)`: `0` of `140,050` planes have `rho < R-r`. Reason: `rho = 1` would put the whole 2-plane in the cone over the rational normal curve. Hence `R-r >= 3` is needed to probe the fixed-kernel class at all — which is why the probe is by construction, not by search.

---

## D4 — VERDICT AND THE HONEST FRONTIER

The open bracket `[k+2^34, 3n/4)` is the gap between two classical radii, and the campaign's frontier sits exactly where the instruments do:

```
a/n :   0.5078125        0.70710678       0.7109        0.75
        razor            Johnson          wave-57       unique decoding
        |----------------|----------------|-------------|
        combinatorics VOID here           |   algebra VOID below here
```

- Above `0.75n` (`r < R/2`): tall pencil, full column rank, `T <= r+1`. PROVED.
- Between `sqrt(nk)` and `0.75n`: Johnson-type core counting bites (at `a = 0.7109n`, cores of size `~a` against threshold `sqrt(n(k-1))` give line counts `O(10^2)`), which is the shape of the wave-57 reach.
- **Below `sqrt(nk) = 0.70710678 n`, and in particular at the razor `0.5078125 n`: every counting instrument is void by the exact factors in D1(a), and the algebra is void by D1(c).**

**Named residuals handed forward.**

- **R-DEEP (the whole object).** Bound `#{CA-bad slopes of error weight in (R/2-2, r]}` for a column-far pair at the razor. UB-NEAR discharges the complement. Any finite bound here is the first ever.
- **R-MOVING (the algebraic form of R-DEEP).** Supply a budget for pencils whose generic kernel is a **2-generated apolar truncation** (`p >= R+1-r`), the wide-regime replacement for `(MI1)/(MI2)`. The Kronecker identity survives; the shift-family step does not. Concretely: the kernel splits as `(r+1-p)` shifts of `P` and `(r+p-R)` shifts of `Q'`; since the two multiplicities sum to `2r+1-R ~ 2^40` while their weighted sum is `<= rho <= 2^34`, **at least one generator is forced fixed** — so column-farness forbids *that* generator from being `D`-split-squarefree. Bounding the slopes contributed by the other one is the open problem.
- **R-KER (the reformulation).** Bound the number of slopes at which a `<= 2^34`-dimensional increment over a split-locator-free subspace `K_0` of dimension `>= 1,047,972,020,225` acquires a member of `D_r(D)`. Injectivity gives `<= C(n,a)`; the target is `2^128`.
- **R-LINEDEGREE stays dead** unless a sub-Johnson list instrument appears; the deficit is `1.3924564` in agreement at the *best* anchor, `45.25` at the worst.

**Two flags for the coordinator (no surgery applied).** (i) `minimal_index_budget`'s general statement and claim contract are quantified over all `1<=rho<=r` but their structural premise fails for `r > R/2` — narrow to `r < R/2`; the deployed corollary is correct and untouched. (ii) `crossing_location:275-280`'s "the layer never touches the bracket interior" is too strong by UB-NEAR; its conclusion stands.

---

## PREDICTIONS vs OUTCOMES

| | registered | outcome |
|---|---|---|
| R1 | route (b/c)-by-stratification P=0.55 top-ranked | **HIT** — UB-NEAR landed by exactly the registered mechanism; UB-FIXED was a bonus I did not foresee |
| R1 | P(first movement on full object) = 0.12 | **HIT as registered miss** — none |
| R2 | P(floor tight) = 0.20; P(measured max > r+1) = 0.80 | **HIT** — floor refuted twice over |
| R3 | `X_near = 39.0` | **HIT exactly** (`2^39-1`, 89.00 bits) |
| PR-A | obstruction structural, P=0.75 | **HIT** |
| PR-B | clash factor `63.0`, window [62.9,63.1] | **HIT** `63.00000000005821`; mechanism mispredicted (miss 2); resolution went against the node's premise, not LB1, as the 0.85 branch said |
| PR-C(i) | `k/(2a-n) = 32` exact | **HIT** exact |
| PR-C(ii) | `sqrt(nk)/(2a-n) = 2^5.5` | **HIT** `45.25483399591758` |
| PR-C(iii) | best-case ratio in [1.3924,1.3925] | **HIT** `1.3924564306436178` |
| PR-D | core threshold ratio in [1.3924,1.3925] | **HIT** `1.3924564306448648`; dead even at the maximal core |
| PR-E | two-maximal-core threshold `a <= 3n/4+1/2` | **HIT (arithmetic), NOT MEASURED** (miss 5) |
| PR-F | 89.0 bits, window [88.9,89.1]; P(banked)=0.35 | **HIT** `89.00000000000261`; grep says **not banked** |
| PR-G(i) | razor `rho_cell < 2^-6e11` | **HIT at `q>=2^167`, MISS at `q=2^128`** (miss 4) |
| PR-G(ii) | every reachable cell has `rho_cell >= 1` | **HIT** |
| PR-G(iii) | max at `(7,2,4,q=11)` in [6,12] | **HIT** — `8` |
| PR-G(iv) | `a-k=1` cells degenerate, zero power | **HIT** — measured `T = q+1` throughout |
| PR-H | will not claim the crossing moves | **HELD** |
| PR-I | `T <= C(n,a)`, no counting route reaches `X<216` | **HIT** — and the `2^216` premise itself was wrong (miss 1) |

---

## CATCH-24A SUBTRACTIONS (own-repo greps run before every novelty claim)

1. **`(HK2)` is BANKED and PROVED** — `background/nodes/rate_half_ca_hankel_fixed_kernel_branch/`. My additive content is only that its quantifier carries no radius hypothesis, so it applies in the open bracket where `rho <= a-k`. `grep -rln "fixed_kernel_branch|fixed-kernel" critical/nodes background/nodes` returns its own folder, `minimal_index_budget` (which cites it only for `e=0`), and `rate_half_band_closure/node.json`. `grep -rn "B_ca\^far *<= *a-k|<= *a-k\b" critical/nodes/*/statement.md background/nodes/*/statement.md` returns **nothing**: no node states `B_ca^far <= a-k` anywhere.
2. **The `r+1` corollary is BANKED** (`minimal_index_budget/statement.md:55-61`). UB-NEAR is its scope-monotone re-use; grep for `"column-far at radius|near stratum|shallow stratum|monoton.*radius"` over both node trees finds no far-CA stratification.
3. **The Johnson threshold `sqrt(nk)` is BANKED** and was measured by round 31 (`rh_overlap_cap/REPORT.md:60-62`, `GAP_ALG` vanishing at `1,554,944,255,988`). My contribution is only to identify it as the *common* threshold of all four instruments and to price the third row (`1.3924564`).
4. **LB1 is round 31's**, not mine (`rh_overlap_cap/REPORT.md:79-85`). I replay it in the wide regime, verify its petal structure exactly, and reclassify it as a moving-kernel certificate.
5. **The `2^216` unsafe floor is BANKED** (`crossing_location:280-284`); I only correct the brief's reading of it.
6. **The `A=3`/`A=1` residuals are explicitly non-claimed by the node** (`claim_contract.md:19-21`) — my scope flag does not touch them.
7. **Genuinely additive this round:** the refutation-by-exhibition of the single-apolar-generator step in the wide regime; the identification of `3n/4` as the unique-decoding radius and of `sqrt(nk)` as the common instrument wall; UB-NEAR; the open-bracket application of `(HK2)`; the injectivity bound and its verification; the `dim K_0 >= r+1-2rho` reformulation; and the measured `q`-independence of the extremal count.

---

## ZERO-POWER DECLARATIONS

1. **No exhaustive census exists in the razor's regime** (`rho_cell << 1`). Every exhaustive cell has `rho_cell >= 1`; the razor has `2^-6.7e11`. Pre-registered.
2. **Sampled maxima are lower bounds.** `8` and `10` are sampled at `q >= 11`; only `(7,2,4,q=7)`'s `7` is exhaustive. `(9,2,5)`'s `6` samples `4e-6` of its plane space and supports nothing.
3. **`(HK1)` density `0/1700` is a sampled negative**, not a proof that `(HK1)` is measure-zero.
4. **The `q`-independence law rests on two cells.** No functional form is claimed for `max T(n,k,a)`.
5. **`dim K_0 >= r+1-2rho` is vacuous at every reachable cell** (`>= 0`); its razor content is unmeasured.
6. **UB-FIXED and UB-NEAR bound sub-objects.** Neither is an upper bound on `B_ca^far(k+2^34)`, and I do not report either as one.
7. **The refutation in D1(c) is of a structural step at small scale.** It refutes the *general* claim (whose quantifier includes those cells) and blocks the extension; it does **not** refute the deployed `r <= R/2-2` corollary, and I do not claim it does.
8. **`a-k = 1` cells carry no information about the razor** (pre-registered, then confirmed).
9. **Two-field or better confirmation** was used for every structural claim (`q in {7,11,13,17,23,31}`); the `d6` refutation is reproduced at three independent cells.

---

## COMPLIANCE

Brief read first; the two named anchors (`notes/pilots_20260810/rh_overlap_cap/REPORT.md`, `background/nodes/rate_half_ca_hankel_minimal_index_budget/statement.md`) read next and **nothing else** before the `## Pilot registrations` block — R0-R3, PR-A…PR-I with numeric windows, four zero-power declarations, and the compute plan — was appended to `notes/pilots_20260810/rh_farca_upper/PREREG.md` with the Edit tool, **before any grep, any `ls`, and any interpreter invocation**. No registration was edited afterwards.

**COMPUTE LAW: 7 interpreter invocations, 6 under `tools/ramguard`, 1 BREACH (disclosed).** Under ramguard, from the repo root, with a literal `--` and an explicit `RAMGUARD_TIMEOUT`: (1) `d1_prices.py` *tiny*/55s; (2) `d3_wide.py` *local*/290s, completed 22.9s; (3) `d4_deficient.py` v1 *local*/290s — **MemoryError** at the `(11,2,a=6,r=5,q=11)` cell (syndrome table `~4.6e7` entries), cell dropped, disclosed; (4) `d4_deficient.py` v2 *local*/290s, completed 20.0s; (5) `d5_minindex.py` *local*/290s, completed 0.5s; (6) `d6_kernel_structure.py` *tiny*/55s. **The breach:** a `python3 -c "print()"` no-op appended to a `ls` command as a shell guard — a bare interpreter invocation with no arguments, no computation, no output used anywhere, and no bearing on any number in this report. Disclosed rather than omitted. Stdlib only throughout (`itertools`, `random`, `time`, `math`); no Modal, no network, no git, no subagents.

**BANKED SCRIPTS: none were run, and none were copied.** Every primitive (`build`, `rank`, `syn_table`, `planes`, `analyse`, `min_indices`, `polygcd`) was written from scratch in my own directory against the node's own conventions (`v_x = 1/prod_{y!=x}(x-y)`, `y_m = sum_x e(x)v_x x^m`, `M_r(y)=(y_{i+j})`, `(HS2)` column-farness as a plane invariant). Cross-checks: `(HS1)`-membership agrees with the `X`-subset projection test at every cell; LB1's petal structure reproduces exactly; the Kronecker identity `sum(eps)+sum(eta)+delta = rho` holds at every measured pencil.

**RAM DISCIPLINE:** file-at-a-time reads with bounded windows (`offset`/`limit`) on the two long statements; **`dag.json` never opened**; node facts taken from `critical/nodes/*/` and `background/nodes/*/` shards plus grep; results checkpointed to `d3_wide_results.txt`, `d4_deficient_results.txt`, `d5_minindex_results.txt`, `d6_kernel_results.txt` after every emit; one run re-scoped after a MemoryError rather than retried at scale.

**QUARANTINE HELD TOTALLY:** `notes/pilots_20260802/CAMPAIGN_LEDGER.md` never opened at any line; the three sibling round-32 directories (`rh_fr_algebraic`, `rh_haboeck_seam`, `rh_residuals_close`) never read and never listed — `notes/pilots_20260810/` itself was never listed, only my own directory and the one anchor path I am entitled to; no path containing `prize-codex-` was touched, read, or globbed.

**WRITE SCOPE:** every write is inside `notes/pilots_20260810/rh_farca_upper/` — `PREREG.md` (registrations appended), `d1_prices.py`, `d3_wide.py`, `d3_wide_results.txt`, `d4_deficient.py`, `d4_deficient_results.txt`, `d5_minindex.py`, `d5_minindex_results.txt`, `d6_kernel_structure.py`, `d6_kernel_results.txt`. **`REPORT.md` was not written: the harness forbids subagent report files**, so this report is returned verbatim as the final message per the brief's fallback clause. **No `dag/`, `critical/`, `background/`, `nodes/` or `tools/` file was edited**; no git operation of any kind; no file created outside the pilot directory. AUDIT-AND-DRAFT respected: **no node surgery is applied.** The two corrections that touch node text — `minimal_index_budget`'s unrestricted quantifier, and `crossing_location:275-280`'s "never touches the bracket interior" — are **flagged for the coordinator, not applied.**
