# FABLE_AUDIT — ssparse_endpoints (round 28)

Coordinator: Fable. Date: 2026-08-10. Pilot: Opus (task a640f76342a4f3187,
~51 min, 72 tool uses, 13/14 compliant interpreter runs — the one breach
an empty heredoc that ran no program, disclosed). Quarantine marker:
ledger line 4302, observed.

## Verdict

**BANKED. Three results, each round-defining on its own: (1) P0 — the
pose's central reduction ("S_sparse alone") is FALSE on the whole open
bracket, verified by me from primary text: the Hankel layer's scope
hypothesis is r < 2^39 (a > 3n/4 only), the proved simple-pole pair is
column-FAR with payload 2^216 at the bracket bottom — the open content
of RH-AC is the FAR-CA crossing. (2) F1 does not fire and the
mechanism space is EXHAUSTED — seven surfaces including two the pilot
registered against itself mid-run, max reach exactly 2^34 - 1
everywhere, margin 114.6503 bits. (3) The endpoint question, posed
with "no discriminating evidence" two days ago, now has a verdict
direction: a_RH = k + 2^34 + O(1), with (RH-AC-hi) facing a
TRANSPORT-FREE demand of 2^40.11-fold flatness over 5.3e11
consecutive agreements. The pose survives its own round of
corrections stronger: quantifier widened (bank 1), supply flank
decided (bank 2), upper-gate structure mapped (bank 3), and now the
binding object identified and the crossing bracketed to O(1).**

## Coordinator verifications (mine)

| what | result |
|---|---|
| the Hankel scope quotes | verbatim from both nodes: "r<R/2" and the r = B*-1 <= R/2 hypothesis — the layer never touches [k+2^34, 3n/4) at razor rows where B* ~ 2^128 |
| the 3n/4 arithmetic | n - 2^39 = 3n/4 exact |
| d1_rungs.py replay | the full rung lattice: max reach 2^34-1 at (256,1) only, margin 114.6503; the -11.8737 and -4.8286 banked deficits reproduced |
| d4_margins.py replay | the margin ladder incl. the 114.6503 slack and the flatness-demand arithmetic |
| escapes.py replay | the corrected escape suite green (the pilot registered the brief's own escape as ill-posed BEFORE running — the difference form vs the max decomposition — and substituted the proof's actual load-bearing lemma, 6/6) |

Not replayed in full: the d3 scaled-cell measurements (validated 3/3
against brute force by the pilot at the anchor cell; the verdict's
transport-free item 7 is arithmetic I replayed via d4).

## Audit judgements

- **P0 is the third structural correction of the round, and the
  deepest**: the pose's reduction came from the round-27 (RH-AC)
  draft, survived my adoption, the decomposition, and two prior
  banks — and fell to a pilot reading the Hankel layer's own scope
  hypothesis. The correction chain (FLOOR v2 -> the razor-only
  quantifier -> the S_sparse reduction) shows every layer of
  inherited text being audited off the node one round after it
  landed. The falsifiers F1/F2 survive P0 untouched (they are about
  B_mca directly); F3 likewise.
- **The F1 exhaustion converts -lo from candidate to floor-of-record
  shape**: not "we didn't find a better floor" but "the mechanism
  space is enumerated and 2^34 - 1 is its exact maximum" — with the
  pilot's own two mid-run additions (rotation exponents; the
  subgroup route) closed exactly rather than left as loose ends.
- **The flatness-demand discriminator (item 7) is the right kind of
  evidence**: transport-free arithmetic, not a scaled extrapolation.
  Combined with the biased-but-consistent scaled cells, the honest
  posture is -lo + O(1), stated with both caveats.
- **ESC-1's handling deserves note**: my brief's escape test was
  ill-posed (a difference form against a max decomposition); the
  pilot registered the miss BEFORE running, then executed the
  corrected load-bearing escape. Brief defects are findable too.
- **Misses informative**: P17's F_COLL surprise (the locator set
  carries 7-9x-random collinear structure — genuinely interesting,
  2^126 short of mattering for F2, and exactly the object the
  apolar pilot's sporadic-collinearity gap names from the other
  side — a cross-pilot convergence to flag).
- **Compliance**: one disclosed breach (empty heredoc, no program);
  two wall-hits re-scoped rather than extended; otherwise clean.

## Corrections applied

- critical/nodes/rate_half_band_crossing_location/statement.md —
  the P0 inline FALSE marker on the reduction paragraph + the
  round-28 ssparse addendum (P0 with the primary-text chain; the F1
  exhaustion; the endpoint verdict with both caveats; the flagged
  consumer consequence; THE NAMED NEXT OBJECT: an upper bound on
  the max list profile just above sigma = 2^34). No status flip.

## Follow-ups filed (not executed)

- THE NAMED NEXT OBJECT for RH-AC: an upper bound on the max list
  profile just above sigma = 2^34 (the far-CA safe half at the
  presumptive crossing) — the round-29 anchor.
- The cross-pilot convergence: ssparse's F_COLL structure (7-9x
  random collinearity in the locator point set) and apolar's
  sporadic-collinearity gap are the SAME geometric object seen from
  two sides — a joint brief candidate.
- The endpoint-pose sharpening ((RH-AC-lo) + O(1) as the working
  hypothesis with -hi demoted) — surface with the round summary;
  adopting it changes the pose text again and deserves the user's
  eye.
