# FABLE_AUDIT — apolar_origin (round 28)

Coordinator: Fable. Date: 2026-08-10. Pilot: Opus (task a408f7a74c40910fa,
~46 min, 53 tool uses). Quarantine marker: ledger line 4302, observed.

## Verdict

**BANKED. The named theorem did not land — at neither budget — and
the report says so in its first sentence. What it banked instead is
worth the round: the mandate's premise corrected (CATCH-24A's fifth
firing: the apolar origin was never missing, and the m=1 fence's own
text says imposing it is insufficient — the mechanism is a PORT of
the proved QMU/QMP species to the full A=3 pencil), one genuine
official-scale theorem (the cyclotomic exclusion: round-27's sole
field-independent threat to budget 2^39+1 is now dead at the
official parameters BY PROOF, margin ~5.5e11), the w* window, a
per-stratum closure covering a third of the sharp face, the
reciprocal-locator normal form that mechanically LOCATES the q=17
counterexample as one sporadic collinearity, and a one-line fence
refuting both banked certificates. The remaining gap is sharper
than it has ever been: control sporadic collinearities of the
reciprocal-locator set at large w*.**

## Replays (mine)

| what | result |
|---|---|
| d2_scan.py (the (AO1) bound, P4-P7, R3/R4 arithmetic) | **IDENTICAL** to banked |
| d6_stratum.py (uniqueness legality both profiles; the w* window; the 1/3 closure band) | **IDENTICAL** to banked |
| d5_sporadic.py (the flat-840 structured count; sporadic 0.167 at q=17, 0.000 at q >= 97; the fence's triple) | **IDENTICAL** to banked |
| the escape replays | the pilot's e1/e2 scratch-copy replays are byte-identical to the round-27 banked results — which I had myself replayed byte-identical at their bank |
| the sliver arithmetic | 2^128*(2^39+1) = 2^167 + 2^128 — verified at the rewire bank this morning; THIRD independent derivation |

## Audit judgements

- **The honest-miss discipline at its best**: P10 registered the
  uniform theorem as miss-likely and it missed; A2's registered
  heuristic was falsified at every field (840 flat vs the predicted
  q^-4 decay) and the pilot corrected the model in a marked addendum
  BEFORE re-running — and that falsification produced the
  structured/sporadic dichotomy, the report's best finding.
- **The cyclotomic exclusion (R3) is a real theorem with real
  payoff**: the official-scale threat kill upgrades round-27's
  accessible-analogue law to a proof at the deployment parameters.
  It also re-derives the round-27 law's sign on 7/7 rows from pure
  support combinatorics — no Hankel system evaluation.
- **The q=17 story is now complete**: forced onto m=1 by
  divisibility, excluded from the closure by explicit hypothesis,
  and mechanically explained as one sporadic collinearity that
  small fields supply and q >= 97 already does not. The official-
  scale sporadic heuristic (q^{-(3m-2)4m}) is flagged as heuristic
  — correctly, since converting exactly that heuristic into a proof
  IS the remaining gap.
- **Three-way blind convergence on the sliver**: mca_safe_rewire,
  apolar_origin (both quarantined), and my replay all landed on
  2^167 + 2^128. The precision fix is applied to the child's Known
  Structure with both pilots credited.
- **The self-caught vacuous first theorem** (the w* >= 4m+2 bound
  emptying its own clean hook) is the falsification machinery
  applied to one's own draft — the right reflex.
- **Compliance clean**: registrations + marked post-registration
  addenda each preceding their experiment; quarantine total;
  ramguard total; scratch copies byte-verified; draft-only held.

## Corrections applied

- critical/nodes/rate_half_band_crossing_location/statement.md —
  the round-28 apolar addendum (honest headline first; the mandate
  correction; the six landed items; the sharpened remaining gap)
  + the sliver PRECISION FIX on the Known Structure line (both
  pilots credited). No status flip; both budgets stay open.

## Follow-ups filed (not executed)

- THE SHARPENED TARGET for any future budget attempt: bound
  sporadic collinearities of the reciprocal-locator set
  {P_S} at large w* over prime q > 2^167 — the exact shape of the
  remaining proof obligation, now with a normal form to attack.
- Mint candidates: R3 (the official-scale cyclotomic exclusion);
  the w* window; R4 (the disjoint-support fence).
- The per-stratum closure's complement (large w*) is where any
  round-29 attempt should start — the average configuration sits
  there.
