# FABLE_AUDIT — k3_splitbc_transport (round 30)

Coordinator: Fable. Date: 2026-08-10. Pilot: Opus (~26 min, 67 tool
uses, exactly 1 interpreter invocation, ramguard). REPORT.md
persisted verbatim by the coordinator (harness blocked the pilot's
write). Compliance clean; one sibling filename printed by grep -l,
never opened.

## Verdict

**BANKED. The transport audit Codex asked for, delivered better than
asked: the common/source layer of the split block is LITERALLY the
same 60 compiled algebra rows as the closed O0a route (zero
recomputation — every per-cell common certificate is valid
verbatim), and four exact transports cut the outside layer from
37,800 raw labels to 11,304 representative systems (3.34x) BEFORE
any algebra, with a named missing certificate (the 433-1b root-sign
quotient, M6) taking it to ~3,414 (11.1x). The structural reason
O0b's quotient tops out at 57/60 instead of O0a's 36 is exact: the
colored incidences move from {F,F} to {E,F}, killing the D/E
transport with no repair. No guard-only closure anywhere — fully
consistent with the PR #1155 fence. THE CATCH OF THE BANK IS M1: a
PROVED node titled "universal 433-1b outside-label quotient" whose
statement DROPPED the "-> O0a" route qualifier its parent carries;
BOTH its generators fail on O0b, and applying its 105->36 census to
an O0b lane would falsely delete 65.7% of the labels — a live
booby trap sitting directly on Codex's active path.**

## Coordinator verifications (mine)

| what | result |
|---|---|
| label_orbits.py replay | both validations reproduce banked censuses EXACTLY (57 @ 1:9,2:48; 36 @ 1:3,2:15,4:18); new counts 60 (SDE/SDF), 56 (S0 role map), 32 (S0 Klein four-group) |
| M1 scope drop | verbatim: child statement.md:5 "In every positive deployed 433-1b common role cell" (no route); parent statement.md:5 "...433-1b -> O0a common role cell" |
| M3 under-scope | verbatim: cell3 quotient scoped "repeated-BC cell-3" while its proof is d->-d on records only, requires only the O0b atlas |
| M8 ledger arithmetic | 63,000 - 16,800 - 3,360 = 42,840 exact; block splits 12,600x3 + role-cell orbit structure consistent |
| C6 kill mechanism | the be -> bd non-record argument is airtight given the gauge lemma (a,b,c,e,f share status; only d flips) |

## Audit judgements

- **M1 is the highest-priority finding of the round so far and is
  TIME-SENSITIVE**: Codex is actively working the eleven routes and
  the mis-scoped "universal" node is exactly the tool it would
  reach for on the split block. The fix (restore "-> O0a" to the
  statement + a one-sentence O0b non-transport note) is Codex-lane;
  it goes at the TOP of the wave-57 brief. Until it lands, the
  ledger records the hazard.
- **M3 is the cheapest big win**: a PROVED 105->57 quotient already
  covers 12,600 of the 37,800 labels, excluded only by its own
  scope wording. Widened-scope reissue with the falsifiable digest
  prediction (must equal banked 70c074ad...) is exactly the right
  shape.
- **The lane-transport structure (SDE -> SDF free, S0 self-map
  15->9 cells) is verified by my replay** and turns "six lanes" into
  effectively "2.5 lanes" of real work.
- **Honest self-downgrades throughout**: M5 (the split saturation's
  expected yield LOW, with the bc=bc coincidence reason), the
  sizing caveat (11,304 representatives sit over positive-dimensional
  curves, unlike the clean 10,080 point-run — hence the
  function-field atlas shape, not a census), and the root-sign port
  caveat (cells 0,1,2 need their own argument). Five zero-power
  declarations properly scoped.
- Prior scoring honest: the R2 primary mechanism prediction scored
  WRONG (structural {E,F} vs guard factorization), the vacuous-
  transport trap it pre-registered actually fired (P on S0).

## Corrections applied

None to nodes (Codex's write lane). DRAFT_CODEX_BRIEF.md stays in
the pilot dir; the coordinator ships a curated version to
notes/codex_briefs/ with the round-close handoff.

## Follow-ups filed (not executed)

- Wave-57 brief items, in order: (1) M1 scope repair (booby trap);
  (2) M3/T1 widened-scope S0 quotient reissue (digest-pinned);
  (3) T2 SDE/SDF identical-pair quotient (60 orbits); (4) T3
  SDE->SDF lane transport; (5) T4 S0 role transport (56/32 orbit
  option); (6) M6 the 433-1b root-sign quotient (cells 3..14 clean,
  0/1/2 separate); (7) M4 parent-scope addendum (route-independence
  fact into the rank-drop classifier, node-local notes rule).
- M2/M5/M7 as ledger notes for the eventual split campaign contract.
