# FABLE_AUDIT — k3_chain_seams (round 30, adversarial)

Coordinator: Fable. Date: 2026-08-10. Pilot: Opus (~30 min, 101 tool
uses, 11 interpreter invocations, all ramguard; two disclosed
wall-hits, both the pilot's own float bug — M3 — honestly recovered
by switching to exact integer comparison). REPORT.md persisted
verbatim by the coordinator. Two disclosed prior-round reads
(k_extremal DRAFT_SCOPE_FLAGS, mca_safe_rewire PREREG) — both
permitted and both used for proper novelty subtraction.

## Verdict

**BANKED — the round's decisive pilot. 12 findings, 3 HIGH, all
verified: the chain's arithmetic foot is clean (link 4 and the
KBPRW reproduction survived attacks with real power) but its
quantifiers and scopes were not. F1: the parent's LIVE conditional
proof still asserted the S_sparse reduction its own child banked as
FALSE at round 28 — a same-day propagation failure. F2/F3: the K3
arm is an exhibit-scoped n=2^21 certificate under the standing WP5
no-transport verdict, serves NO consumer bar, yet blocks the spine
under gate all. F4 (NEW AXIS): the only location-owning child poses
at q PRIME while the admissible family is q = p^e — with an
exhibited razor-slice extension row (q = p^2 > floor(2^255.9),
v_2(q-1) = 42) covered by neither child. Plus F5-F12 (stale
razor-only claim, retired HD1 clause live in the shard, the orphaned
razor-surplus falsifier direction, the unexecuted 418-edge
migration, a double-wire, review-scope wording, the balanced-core
qualifier).**

## Coordinator verifications (mine)

| what | result |
|---|---|
| all four scripts replayed | exhibit_extension_rows (both exhibits + KB-row arithmetic controls exact), dup_edge_scan (418/555 ev-in, leaf wiring), route_partition_check (11+2=13, consistent), kbprw_reproduce (NO KILL, 5 orbits / 7 skeletons consistent) |
| F1 both sides | parent conditional.md:29-31 vs the child's banked P0 FALSE marker — read directly, contradiction confirmed |
| F7 | the HD1-dischargeable clause present in the shard AND retired in the same node's conditional.md — confirmed |
| F8 | the shard falsifier field was verbatim the retired FLOOR v2 text — confirmed |
| F4 exhibit | replayed; the q = p^2 razor-slice row checks out to the unit |
| WP5 verdict text | in the shard's statement field, as quoted |

## Coordinator actions (forced-corrections authority)

APPLIED (propagations of banked corrections, no status flips):
F1 (conditional.md + shard: the far-CA correction of record),
F6 (the widened pose reflected in premises/claim), F7 (retired HD1
clause removed from the shard), F8 (shard falsifier synced to the
per-child split + the unowned-direction flag), F5 (plural narrowed
to the singular KoalaBear exhibit row). F4 FLAGGED on both nodes
(E7 flag-not-resolve). Round-30 addendum appended to
band_closure/statement.md as the correction of record. Full chain
green after; census unchanged 239 = 167/40/32.

SURFACED (user decisions, NOT applied): F2/F3 — demote child 1's
edge req -> ev (matches WP5 "ev-edge upgrade, not an amber") vs
keep gate all with the rider status now stated; F4 resolution —
widen the pose to q = p^e vs prime-only family restriction + an
extension-row child; F9 — the 418-edge evidence migration.

TO CODEX (wave-57 brief): F10 (ledger double-wire), F11
(review-scope wording), F12 (balanced-core qualifier carry).

## Audit judgements

- **The audit cadence claim held exactly**: every major text landing
  has now grown its catch within one round — and this round's catch
  is the propagation DISCIPLINE itself (two same-day corrections
  reached child and consumer and skipped the node in between). The
  lesson to bank: a forced correction must be applied by grep over
  ALL texts carrying the corrected claim, not by editing the node
  where it was found. (That is how F1/F6/F7 escaped: the round-28
  banks edited the child and the consumer, never re-grepping the
  parent.)
- **F4 is the honest new mathematics of the round**: the e-axis was
  invisible to the s-axis work of round 29, and the exhibit is
  constructive, in-range, and unit-exact. The pilot's own caveat is
  right — the restriction may well be intended, but under the
  PROVED official_row_primes_pinning rule an unwritten restriction
  is exactly the forbidden state.
- **The pilot's misses-first section is a model**: the M3 float
  confession (log2 rounding at 2^-86 relative) in a campaign whose
  own node text bans floating point is exactly the disclosure
  discipline we want; the fix (integer comparison vs
  iroot(2^2559,10)) is correct.
- **Clean results carry weight**: the located-index quantifier
  surviving all six consumer rungs (A3, with power) is the
  round-28 a_safe hardening confirmed end-to-end; link 4 and the
  KBPRW reproduction retire those attack surfaces.
- Priors honestly scored: headline prior wrong in DIRECTION
  (anti-bridge, not absent bridge), rank-1 candidate wrong (the
  worst seam was staleness, not row scope) — both scored as misses.

## Follow-ups filed (not executed)

- BANK THE LESSON as a process rule candidate: forced corrections
  propagate by claim-grep, not by node-edit (goes to the ledger
  close; candidate for the standing-rules memory).
- The razor-row surplus falsifier direction needs an owner once the
  F2/F3 ruling lands (if child 1 demotes to ev, the direction
  belongs to the parent's own re-posed falsifier).
- M4 zero-power (the ten-orbit parent reduction above KBPRW) is a
  cheap targeted replay candidate for a future round.
