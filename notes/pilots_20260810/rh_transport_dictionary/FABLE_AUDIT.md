# FABLE_AUDIT — rh_transport_dictionary (round 31)

Coordinator: Fable. Date: 2026-08-10. Pilot: Opus (~47 min, 56 tool
uses, 45 interpreter invocations, ALL ramguard, documented
RAMGUARD_TIMEOUTs; one RAM-ceiling death disclosed and relaunched;
one cell still running at write time, excluded from all
conclusions). REPORT.md persisted verbatim by the coordinator.

## Verdict

**BANKED — THE (t,M) DICTIONARY EXISTS, HAS FIVE ENTRIES, AND
INVERTS ROUND 29'S DIRECTION. The C(127,64) puzzle is RESOLVED by
an exact integer identity: PLATEAU(n) = C(n/2-1, n/4) = the qcore
formula C(n/M-1, k/M) at (M,sigma) = (2,1) — the round-29 model
and the razor sit at two different points of ONE family, and only
the plateau coincided. The round-29 "t = M" identification is off
by exactly one (the banked qcore requires sigma < M, extremal at
sigma = M-1) and that one step is load-bearing: the razor's
crossing index sigma = 2^34 is EXACTLY a coset scale, where the
qcore cliff is 63.503 bits and the CPW (full-coset product-word)
family cuts the drop to 6.02 bits — CPW stands 57.480 bits above
qcore at the crossing index yet 10.75-10.85 bits BELOW the need.
THE DIRECTION INVERTS: round 29's "+115 bits over-satisfaction"
was a scale artifact (the same law at the razor's own n gives
1.1e12 bits — a quantity that moves ten orders under a scale
change was never transportable); the faithful transport UNDERSHOOTS.
AND THE LANE LANDS LIST-SIDE: F_LIST is L_1, the audited guard
forbids the list-threshold-as-MCA-surrogate transport, so the
missing dictionary entry is the CA/MCA CONVERSION, not more (t,M)
entries. Positive confirmation: the char-0 minimal-slack cap is
EXACTLY tight at 2-power n (the razor's regime) and fails off
2-powers precisely at the Lam-Leung boundary — an independent
verification of the banked THEOREM CAP in its own stratum.**

## Coordinator verifications (mine)

| what | result |
|---|---|
| the razor ladder | HAND-CHECKED exact: C(127,64) = 2^123.1714, C(63,32) = 2^59.6686, C(128,65)/128 integer (gcd(65,128)=1) = 2^117.1491; cliff 63.503; CPW-over-qcore 57.480 — all match |
| td_razor.py replay | green; structure domination at the razor by 2^2.2e12 confirmed |
| the sigma < M quote | verbatim at ww_lower_witnesses/proof.md:18 (M = n/N, sigma = M-1, "1 <= sigma < M") |
| the qcore formula | verbatim at qcore/node.json:12 ("whenever M | k and sigma < M") |
| the LIST-side guard | verbatim at the literature map's target_mappings.json:838 (the audited GUARD against the MCA surrogate) |
| escape tests | both green by different algorithms (L_1(8,1) = 7 three fields; the n=16 Theorem-A word at 715 flat) |

## Audit judgements

- **Miss 1 is model honesty**: the pilot's headline number was
  in-repo (the literature map's razor reading) and it says so
  first, scoping its own contribution to the exact value at the
  exact index + the mechanism verification. Four of its five new
  claims are exact integer identities or exact measurements.
- **The two unregistered axes (Q6 slack, Q7 object) are the real
  dictionary**: the entire surplus lives on the slack axis
  (measured stratum-by-stratum), and the object axis (L_1 vs
  B_mca) is where round 29's model critique lands for good. The
  supply question is now CLOSED as posed: no coset-family supply
  reaches the need at the crossing index, and nothing on the list
  side transports to the open content without the CA/MCA
  conversion.
- **The decisive cell (n=12, sigma=2, q=37, L_1 = 5 vs the naive
  transport's 41.25)** kills the C(n,a)/n law at sigma >= 2 by a
  factor 8 — the concrete refutation round 29's critique lacked.
- **Zero-power honesty is complete**: no exact global maximum at
  2-power n with sigma >= 2 exists in the report (priced, not run);
  nothing measures B_mca/B_ca^far/S_sparse; the q gap to 2^256 is
  declared with the one cleanly-transporting row (regime
  membership) argued exactly.
- **Cross-bank coherence**: bank 2 (LB1) moved the far-CA side;
  this bank moves the supply side; both point the same way — the
  open content of RH-AC is now THE FAR-CA UPPER BOUND
  (R-UPPERBOUND) plus the CA/MCA conversion, with every
  overlap-statistics and coset-supply door closed.

## Corrections applied

- crossing_location supply addendum (round-31): the dictionary
  entries, the t = M off-by-one correction, the +115-bits scale
  artifact retirement, the direction inversion, the LIST-side
  landing + the CA/MCA conversion as the named missing entry, the
  char-0 cap confirmation, F-CAP/F-CPW/F-SIGMA1/F-OBJECT armed.
  No status flips.

## Follow-ups filed (not executed)

- THE CA/MCA CONVERSION is now the supply lane's single named gate
  — a round-32 anchor (and the ABF26 Lemma 4.6 unique-decoding
  equivalence eps_mca = eps_ca may be the door: it is field-general
  and δ < δ_min/2 covers the far-CA regime — flag for the next
  brief, unverified).
- F-CAP at n = 32 (565M subsets) is a Codex-scale compute candidate.
- The still-running n=12/q=61 cell: harvest when done or re-run;
  no conclusion depends on it.
- Mint: the PLATEAU=QCORE identity + the two-family (QCORE/CPW)
  ladder as a note on the qcore node.
