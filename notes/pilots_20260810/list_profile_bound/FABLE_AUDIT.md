# FABLE_AUDIT — list_profile_bound (round 29)

Coordinator: Fable. Date: 2026-08-10. Pilot: Opus (task a76b2cf8b476e498a,
~37 min, 43 tool uses, 5/5 compliant interpreter runs). Quarantine
marker: ledger line 4474, observed.

## Verdict

**BANKED. No UB theorem at small c — and the failure is now an exact
number instead of a shrug: the open bracket is 99.975% exactly the
region where the MDS pairwise-overlap cap exceeds the Fisher
threshold (T5), so the theorem target collapses to ONE inequality (a
pairwise-overlap cap below a^2/n at sigma = 2^34, worth 89 bits via
T3 the moment it lands). Three corrections ride along, each
first-class: the consumer bar is c = 0 ONLY (every c >= 1 re-opens
the unsafe half against an exhausted mechanism space — my round-28
flagged consequence was over-broad); the round-28 ratio transport is
REFUTED by a three-field q-ladder (F_LMAX is an absolute constant,
not a log2 q fraction — and my banked "0.65-3.09 units" line
inherited the error); and the seven far-CA instruments are ONE
unique-decoding threshold seen seven ways, which explains
structurally why nothing reaches the bracket.**

## Replays (mine)

| what | result |
|---|---|
| d1_core.py | GAP_FISHER = 532,441,726,975, ratio 0.999748 — exact |
| d2_sunflower.py | 0 violations / 21,832 exhaustive configs (T1/M_LINE/T3); T4 tight (forced 2, observed 2) |
| d3_ladder.py | the exact F_LMAX section IDENTICAL (7/1/1 at q = 17/41/97, decay 2.8074 absolute — the q-independence refutation); the sampled B_ca^far section differs only by candidate-pair count (900 mine vs 300 banked — sampled lower bounds, benign, noted) |

## Audit judgements

- **T5 is the round's best number**: the bracket and the MDS-Fisher
  gap ending together (0.999748) is the kind of coincidence that is
  not a coincidence — the open problem IS the overlap-cap problem.
  And it CONVERGES with the collinearity pilot's object: pairwise
  overlaps of root sets and collinear structure of the
  reciprocal-locator set are adjacent facets of the same extremal
  configuration question. Cross-flagged for that pilot's bank.
- **The q-ladder refutation is the max-vs-mean lesson's little
  sibling**: a ratio read at the smallest admissible field
  transported as if scale-free. The correction (absolute constant,
  c ~ 32 under the honest transport) also demotes my banked
  "0.65-3.09 units" line — corrected in the addendum with the
  do-not-quote flag on the 91x-discrepant 217 figure.
- **The consumer-bar correction closes a loop**: round-28 ssparse
  flagged the consequence; this pilot showed it holds only at
  c = 0. The working hypothesis stands but its payoff is
  all-or-nothing at c = 0 — which honestly raises the stakes on
  the overlap-cap object.
- **The validator-bug episode is exemplary**: 1.68M spurious
  violations from a leaked-degree functional, diagnosed as a CODE
  bug (a - k functionals needed, not one) rather than used to
  weaken the theorem; 0 violations post-fix.
- **The structure theorem de-mystifies the instrument landscape**:
  seven instruments, one threshold — and the one genuine
  threshold-crosser (Johnson, 0.707n) bounds the wrong object,
  which is itself a lead (a Johnson-for-B_ca^far analogue is now a
  posable question).
- **Compliance clean**: 5/5 ramguard with explicit walls; the one
  banked script from a scratch copy; F3 honored throughout.

## Corrections applied

- critical/nodes/rate_half_band_crossing_location/statement.md —
  the round-29 list_profile_bound addendum: the c = 0 consumer bar
  + the c-bracket; the transport refutation + the do-not-quote
  flag; the structure theorem; T1-T5 banked; THE NAMED NEXT OBJECT
  SUPERSEDED (the pairwise-overlap cap below a^2/n); the
  scaled-cell program decommissioned for c. No status flip.

## Follow-ups filed (not executed)

- THE OBJECT OF RECORD for RH-AC's safe half: prove pairwise
  overlaps of column-far bad-slope root sets < a^2/n =
  2^39 + 2^34 + 2^27 at sigma = 2^34 — one inequality, 89 bits of
  margin behind it. Feeds directly from/into the collinearity
  pilot's structured-family census.
- The Johnson-for-B_ca^far analogue (the one instrument that
  crosses the threshold bounds L_1; is there a slope-count
  version?) — a posable question, unposed.
- Mint candidates: T1 (sunflower rigidity), T3 (the Fisher
  sub-stratum bound), T5 (the obstruction identity).
- The Hankel moving-kernel branch is ABSENT (the above-3n/4
  discharge itself has a hole) — filed for the next wave audit's
  attention since Codex owns the Hankel suite.
