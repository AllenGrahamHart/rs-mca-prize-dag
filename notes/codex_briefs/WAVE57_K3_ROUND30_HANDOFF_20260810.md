# Wave-57 handoff — the round-30 K3 development round (coordinator, 2026-08-10)

Four Opus pilots ran the K3 arm end to end (reports + audits in
notes/pilots_20260810/k3_{orientation_assembly,allocation_inequality,
splitbc_transport,chain_seams}/). Zero edits were made in your lane.
This brief is the complete handoff, ordered by priority. The urgent
M1 scope hazard was shipped separately
(URGENT_M1_SCOPE_HAZARD_20260810.md) — it is item 1 here for
completeness.

## 1. URGENT — the mis-scoped "universal" quotient (booby trap)

`..._433_1b_universal_generic_outside_label_orbit_quotient` states
universality over every 433-1b cell but is O0a-only (its parent
carries the "-> O0a" qualifier; the child dropped it). Both
generators fail on O0b; misapplication falsely deletes 65.7% of
labels. Fix: restore the qualifier + a one-sentence O0b
non-transport note. Details in the urgent brief.

## 2. The split-block campaign plan (bank 3 — saves you ~2/3 of the work)

Your own pre-registered precondition (the transport audit vs closed
O0a machinery) is done: notes/pilots_20260810/k3_splitbc_transport/
{REPORT.md,DRAFT_CODEX_BRIEF.md,label_orbits.py}. Highlights:

- The common layer is LITERALLY your closed O0a 60 compiled algebra
  rows — every per-cell common certificate is valid verbatim for all
  six split lanes. Zero recomputation.
- Four exact transports cut 37,800 raw labels -> 11,304
  representative systems before algebra: (a) the PROVED cell-3
  d->-d 105->57 quotient applies verbatim to S0 — its proof never
  uses cell 3; a widened-scope reissue (digest-pinned: the orbit
  ledger must equal your banked 70c074ad...) covers 12,600 labels;
  (b) SDE/SDF identical-pair quotient, 105->60 (replayed, profile
  1:15,2:45); (c) SDE -> SDF lane transport (B<->C, E<->F) makes
  both SDF lanes FREE; (d) the S0 role self-map folds 15 -> 9 cells
  (Klein-four option: 32 orbits on the three rho-fixed cells).
- The D/E transport DIES structurally on O0b (colored incidences
  {F,F} -> {E,F}; be -> bd is not a record; no gauge repair) — that
  is the exact reason O0b tops at 57/60 vs O0a's 36. Do not look
  for a repair.
- MISSING CERTIFICATE worth minting first: the 433-1b root-sign
  quotient (your 433-1a analogue exists; cells 3..14 port cleanly,
  cells 0/1/2 need their own argument — the AB+ identical-role step
  is unavailable). With it: ~3,414 representatives (11.1x).
- Shape: function-field generic-rank atlas + finite-boundary replay
  (the monolithic ideal is already fenced by your 300s cap datum).
  The rank-five block sits over positive-dimensional curves — the
  10,080-point run is not a sizing precedent.
- Also: cells-11/14 pairing does NOT extend to split lanes (no
  BC1<->BC2 gauge there) — consistent with your O0a route paying
  cell 11 and cell 14 separately.

## 3. The ledger's honest price (banks 1+2 — read before more census)

Two blind pilots converged: no further census pays into
k3_distinct_slope_budget_ledger until these land, in dependency
order:

1. **Labels-to-slopes conversion with exact multiplicity.** Every
   banked workboard is in raw labels/systems; the ledger unit is
   DISTINCT_BAD_FINITE_SLOPES_PER_RECEIVED_LINE. Nothing prices one
   in the other (your own claim_contracts say so). This is the
   binding blocker and has no owner.
2. **A definition of U_K3_allocation.** It is defined NOWHERE
   (4 in-repo occurrences, all in the two K3 nodes demanding it).
   Either prove U_Q = U_new = 0 (promoting the joint reserve
   274980728110413983 exactly), or mint an explicit three-way split
   with its own certificate. Note the DERIVED floor
   U_Q + U_BC + U_new >= 57197049262 (identity-prefix lower attack
   minus the U_paid cap; one owner ruling outstanding): "record
   U_K3 = 0" is not a free fallback.
3. **The orientation-assembly gaps** (bank 1, eight named
   obstructions with falsifiers): the parameter-to-carrier BRIDGE
   (disclaimed by your own compiler: "The endpoint parameter line
   is not the evaluation carrier"); the LIVE (2,8,1) fourth class
   outside the three orientations (either prove it empty or add a
   fourth ledger term — the latter changes U_K3's identity);
   the SOURCE-COVER OBJECT does not exist (named mint:
   `..._diagonal_biquadratic_source_cover_workboard`, four live
   KBDM-3 rows x two KBDS-7 passports, per the dichotomy's own
   frontier); the source-line image-exactness clause is FALSE as
   written (rows (1,0,4)/(0,1,4)/(0,0,6) + orbit (KBDM-10) have no
   declared image — either delete them or restrict the clause).
   MEANWHILE: the orientation TRICHOTOMY is already a theorem
   (delta=|S| + the exclusive source-subfield dichotomy) — the
   conditional draft under (H-bridge) in bank 1's D3.4 is
   mint-ready and free.
4. **Wiring + hygiene:** the allocation node has requires=[] (its
   demanded binding is unrepresentable — proposed edges in bank 2's
   binding_schema_draft.json, refuse-to-substitute design); row
   binding must key on (n,k,p,K), never B_star (kb_mca and kb_list
   share B_star = 274980728111395087); "allocation" is a homonym in
   the c2(1,1,2) lane (same/swap/mixed) — avoid bare grep.

## 4. Small text fixes in your lane (bank 4: F10/F11/F12 + bank 2: M10)

- F10: ledger -> structural_surplus is double-wired (req in the
  child + evidence_for in the ledger shard) — drop the ev edge; it
  is the only such pair crossing into the band chain.
- F11: independent_review's shard promises four named certificate
  families while structural_surplus's conditional asks for "the
  complete load-bearing sub-DAG" — align them (and note the
  negative-coordinate subtree + the raw O0a workboard are currently
  unnamed); the eventual reviewer must be outside both producing
  implementations.
- F12: the balanced-core qualifier appears in
  orientation_assembly's premise and vanishes in the ledger's
  conclusion — one clarifying clause in the ledger closes it.
- M10 (bank 2): the ledger's "source-line contributes zero" leg
  cites a certificate scoped to SATURATED packets; carry the word
  across or cite the exceptional-orbit disposition.

## 5. Context you should know (no action)

- The chain above you was audited (bank 4): five stale-text
  propagations were applied on the band side (coordinator's lane);
  the K3-arm gate status (req vs ev under the WP5 no-transport
  verdict) and the new e-axis flag (q prime vs q = p^e) are
  surfaced user decisions. None of this changes your campaign.
- Round-30 replays confirmed your (KBPRW) tables and the 11+2=13
  route partition exactly — those attack surfaces are retired.

— Fable (coordinator), round 30 complete, canonical 87d033cc1
