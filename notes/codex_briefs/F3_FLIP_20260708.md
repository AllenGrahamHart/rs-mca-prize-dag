# GOAL (2026-07-08, for Codex): the F3 FLIP PACKAGE — prove the h=3
# per-row accident bound and assemble everything needed to turn
# u1_x4_direct_column_budget GREEN

## 0. Context (10 minutes; you did most of this groundwork last night)

F3 (u1_x4_direct_column_budget) is one of the SEVEN floor conjectures the
two Proximity Prize problems are conditional on. Its content decomposes
over active-core widths h; current per-stratum status after your
overnight run (all integrated to master — start from CURRENT master):

  h=2: CLOSED per-row (T_2 <= E/8; CP import (2/3)n^{5/2}; PLUS your
       in-house explicit chain E(H) <= 83851 h^{5/2}).
  h=3: interior mechanism = THEOREM (char-0 classification + norm-gate
       activation bounds — yours); the ONE wall = the PER-ROW accident
       bound, which you POSED last night
       (notes/u1_x4_direct_column_budget/notes/F3_H3_PERROW_ACCIDENT_*).
  h=4..8: boundary-row certificates + shell exclusions (yours); no
       general-n theorem yet.

If the h=3 wall falls with constants covering the official range, and
h=4-8 get general-n arguments, F3 flips TARGET -> PROVED: the first of
the seven floors to go GREEN, unconditionally — reds 7 -> 6.

READ FIRST: your own F3_H3_PERROW pose; F3_H2_RICH_COSET_STEPANOV.md
(your lemma — the template); F3_SHALLOW_LADDER.md sections 7-9 (the
hyperbola normal form + norm-gate structure); the Terminal C census
files (the exceptional-family structure at n = 96).

## 1. THE THEOREM CHAIN (the night's spine)

### T1 (the new lemma): RICH-CURVE STEPANOV

Prove the multi-condition generalization of your rich-coset lemma:

>  Let H = mu_h <= F_p^*. Let r_1, r_2, r_3 be rational functions of
>  degree <= 2 (the signature-curve parametrizations; via the hyperbola
>  normal form each (e1,e2)-fiber of ordered triples is parametrized by
>  one variable X with three H-membership conditions r_i(X) in H). For
>  a family Z of pairwise-inequivalent signature curves, bound
>      sum_{z in Z} T(z)   (T(z) = #curve points with all three
>                           coordinates in H)
>  by an explicit C * (h |Z|)^{alpha}, alpha < 1, under an explicit
>  h-vs-p hypothesis (target: h <= p^{2/3-eps} or the natural analogue).

Route: your Lemma-5 template with auxiliary polynomial
Phi(X, r_1(X)^h, r_2(X)^h, r_3(X)^h) — 4 blocks of variables; the
normalization step (your D(u) = u^{-1}C(u) trick) becomes choosing curve
representatives so the fiber conditions evaluate to constants; the
sparse-monomial nonvanishing lemma generalizes (watch: rational r_i
need denominator-clearing before sparsity counting — do the bookkeeping
explicitly). MACHINE-CHECK every lemma at multiple rows (exact) before
composing. Degenerate curve families (the toral (0,0) signature, the
3|q-1 line degenerations) must be EXCLUDED by explicit hypotheses and
charged separately — enumerate them; your hyperbola note has the list.

### T2 (the wall theorem): PER-ROW h=3 ACCIDENT BOUND

Dyadic-compile T1 exactly as your energy compiler did:

>  M := #{(x,y) in H^6 : p1(x)=p1(y), p2(x)=p2(y)}
>     <= (explicit trivial term ~ 6n^3 + repeat-entry term you computed)
>        + C_M * n^{beta},  beta < 3, C_M explicit.

Then T_3 <= toral + M-arithmetic (your exact bookkeeping identity from
the h=3 program: M = trivial + 72 T_3 + repeat residue — replay it) gives
the per-row bound. VERIFY against every banked census row (your ladder
data: truth ~ n^2, so a proof at n^{8/3} or n^{5/2+eps} has slack in the
exponent but the CONSTANT is the battleground — see T3).

### T3 (the constant campaign): make the numbers cover the official range

For BOTH chains (h=2 in-house; the new h=3 chain):
- optimize parameters (your X/4, Y/2, X/64 floors were deliberately
  lazy; grid-search the parameter space EXACTLY — this is pure
  arithmetic, perfect for Modal sweeps);
- produce the CROSSOVER TABLE: for each chain, the explicit n_0(C) above
  which the theorem beats n^3, vs the official row range n in
  [2^13, 2^41];
- for official rows BELOW n_0: quantify the midrange honestly — per-row
  exact-census certificate cost (your census machinery: cost ~ C(n,3)
  per row, shardable; estimate shard counts and wall time; FEASIBLE
  means < 2000 Modal shards of < 60s each) and produce certificates for
  every feasible official row. Rows neither covered by theorem nor
  feasible = the residual list, stated exactly.

### T4 (h = 4..8 general-n): the remaining strata

Two routes; attack in this order:
(a) RIGIDITY route: at h >= 4 your data shows the primitive residue
    EMPTY past pullbacks. Try to prove it: the h=4 analogue of the
    char-0 classification (the unit-circle identity generalizes: for
    4-subsets, e2 and e3 relate to conj-symmetric functions — work out
    the identity family; if char-0 forces structure, the norm-gate
    argument transports verbatim and gives per-shape bounds; then the
    activation count is the same wall — but the budget n^3 vs the much
    smaller h>=4 populations may let CRUDE bounds through: fiber <=
    deg*n by the one-condition curve cut, plus your radius-shell
    exclusions);
(b) MOMENT route: the same T1/T2 machinery at depth 3 (four
    H-conditions on a curve) — constants worsen; only if (a) stalls.

### T5 (assembly): THE FLIP DOSSIER

If T1-T4 land: write F3_FLIP_DOSSIER.md — the full per-stratum theorem
list with constants, the official-row coverage table (theorem / census
certificate / residual), every replay command, and the exact statement
of what (if anything) remains between the dossier and F3 = PROVED.
DO NOT edit dag.json — the maintainer flips nodes after replay.

## 2. HONEST EXPECTATIONS (read this; it shapes your choices)

- T1+T2 alone = the wall broken in principle: F3-green becomes
  constants-engineering. That is already a big night.
- The known risks: (W1) constants — if your T2 constant leaves n_0 above
  2^41, the theorem is still banked (asymptotic form) and T3's census
  certificates carry the load for small rows; state the gap exactly.
  (W2) T4(a) may reveal genuine h=4 char-0 families — that is a
  DISCOVERY, not a failure; classify and bank them.
- If T1 itself stalls > 3 hours: bank the precise obstruction (which
  step of the template fails for rational r_i), then pivot to T3 (the
  constant campaign on the h=2 chain is independent and pure
  arithmetic) and T4(a).

## 3. RULES (same as OVERNIGHT_20260708.md sections 2-4; summary)

- Workflow: pre-register every stage in your notes BEFORE computing;
  falsification-first; machine-check every identity at multiple rows
  exactly; honest labels (PROVED / MACHINE-VERIFIED / VERIFIED-AT-ROWS /
  HEURISTIC / REFUTED / OPEN); bank catches; replay commands with
  everything.
- Compute: Modal (~/.venvs/modal/bin/modal), < 60s/task preferred,
  cap 280s when justified; shard; int64 guards + checksums; local
  processes < 1.5 GB; sympy on Modal for exact cyclotomics.
- Repo: fresh worktree
    cd ~/smooth-read-solomin/prize
    git worktree add ../prize-codex-f3flip-20260708 -b codex/f3-flip-20260708
  ALL work there; commit per stage; NO dag.json edits, NO master
  touches, NO remotes/pushes/PRs, NO rs-mca* directories.
- NEVER STOP EARLY: completed stage -> next stage; T5 done -> optimize
  constants further (T3 is bottomless) and extend T4.
- Log every ~2 hours in FLIP_LOG.md; final FLIP_REPORT.md with
  confidence-ranked claims + replays + the residual-gap statement.

The prize: the first unconditional floor. Even the failure modes of this
brief leave the flip distance measured in explicitly-stated certificates
rather than open mathematics.
