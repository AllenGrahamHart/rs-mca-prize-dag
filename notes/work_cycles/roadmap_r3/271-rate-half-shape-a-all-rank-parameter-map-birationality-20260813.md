# Cycle 271: rate-half Shape-A all-rank parameter-map birationality (2026-08-13)

The normalization argument from the rank-three router is independent of
rank. For every nontrivial minimal tensor presentation, the exact column
deficit permits at most one empty slope. Active slopes occur in complete
reduced fibers of the parameter coefficient map, so its degree `d` divides
one of `3e,3e-1`, as well as `m=e-2`. Both official gcds equal one.

```text
tensor rank scope:          every r>=2
empty-column maximum:       1
gcd(m,3e):                  1
gcd(m,3e-1):                1
parameter-map degree:       1
critical status effect:     none
hostile mutations:          7/7
```

This makes the parameter line the normalization of its coefficient image
in every surviving rank. The rank-three singularity node should consume
this theorem; ranks at least four now inherit birationality but still need
an image-geometry or source/Hankel obstruction.
