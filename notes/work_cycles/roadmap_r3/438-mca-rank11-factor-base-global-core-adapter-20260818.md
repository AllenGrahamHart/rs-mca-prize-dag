# Cycle 438: rank-eleven factor base to global core

## Starting pins

```text
our branch: codex/full-prize-resolution-v12-20260807
our SHA: dc29ef241
canonical prize SHA: 0dd5b3244
upstream main SHA: 93fba1be3
relevant open PRs: #1173 (2788d5ec3), #1172, #1171, #1170
critical mandatory-open count: 28 TARGET nodes
```

Canonical, upstream main, and the head of `#1173` are unchanged. No source
tree was modified.

## Action

Compare the common-pencil-base output of the exact `2 x 5` factor router
against the proved complete-family 31-anchor globalization interface.

## Result: ROUTED

`rate_half_mca_rank11_factor_flag_common_base_global_core_adapter` is PROVED.
At an anchor-good common zero of `P`, every product in `PB` vanishes and hence
every correction in `C'=span(PB)` vanishes. Both components of every selected
pair equal the anchor pair there, and the anchor equals the received pair.
The coordinate therefore belongs to every selected maximal agreement support.

The common-base horn is exactly the existing nonempty line-global-core route
`(C)`. It is not a new unpaid terminal. Payment of `(C)` is not claimed.

## Burn-down

```text
node/workboard item: KoalaBear rank-eleven factor-router common-base horn
result: ROUTED to existing (C)
DAG delta: +1 PROVED background node; critical open count remains 28
upstream delta: successor-packet candidate recorded; no competing PR opened
delta-star movement: none
new assumptions: none beyond the scoped exact factor presentation
live compute requests: none
Modal spend: zero
next action: attack factor-presentation existence or the deeper 18166-coordinate
             residual flag
```

## Replay

```text
tools/ramguard tiny -- python3 background/nodes/rate_half_mca_rank11_factor_flag_common_base_global_core_adapter/verify.py
tools/ramguard tiny -- python3 background/nodes/rate_half_mca_rank11_factor_flag_common_base_global_core_adapter/verify_audit.py
tools/ramguard tiny -- python3 background/nodes/rate_half_mca_rank11_factor_flag_common_base_global_core_adapter/verify.py --tamper-selftest
tools/ramguard local -- python3 tools/verify_prize_dag.py
```

## Upstream posture

This adapter uses the established `(C)` name and can travel with a later
factor-flag successor packet. It should be presented as exact interface
alignment, not as a payment or as existence of a split-pencil model.
