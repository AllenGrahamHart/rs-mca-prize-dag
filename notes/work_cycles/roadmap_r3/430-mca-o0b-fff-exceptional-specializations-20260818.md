## MCA O0b `FFF` exceptional specializations (2026-08-18)

### Closed exceptional fibers

The denominator-free replay of the original `q5`, `q7`, and `q6` necessary
system completed at every root of the exact generic exceptional ledger. Five
of the fourteen fibers are empty:

```text
465887767, 666570304, 676802667, 1036595577, 1141382033.
```

Together with the exact nonzero generic determinant, this proves emptiness
away from nine explicit base-field fibers.

### Survivor split

The two roots `47655010` and `1893783428` leave zero-dimensional ideals.
Their full seven-element bases express `E,s,x,r,c` linearly in `b` and leave
one quadratic equation for `b`. They are finite exact candidates, not a
symbolic-computation bottleneck.

The other seven roots `0,1,16711679,451278922,1629292471,2113994754,-1`
leave dimension-two ideals. On these special components the restored
finite-pair equations do not remove the free `E,s` directions. The next
route decision is whether each component is killed by an original
admissibility guard or by the omitted `q4` equation; only a component that
survives both deserves a new structural decomposition.

### Proof boundary

`FFF` and O0b remain open. No exceptional survivor is discarded merely
because it is a denominator root. Any closure must use a globally valid
guard, the original omitted equation, or a direct contradiction in the
unsaturated source system.

Modal app: `ap-qUwke475x3bODff3kZHe9N`. Result SHA-256:
`c066bb4f5813be4915e40a51225287cfde11284b3b3df4cabdae889778a97b88`.
