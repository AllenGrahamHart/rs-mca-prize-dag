## MCA O0b `FFF` complete exclusion (2026-08-18)

### Theorem

For canonical case

```text
(cell=3, S0, sigma_o=-1, epsilon=(-1,-1), xi=2, pairing=0),
```

the all-finite projective matching-root chart `FFF` is empty on the exact
admissible locus over `F_2130706433`.

### Fiber proof

The exact cleared `R76` determinant is nonzero away from fourteen explicitly
enumerated values of `t`, proving generic emptiness. A denominator-free
specialization of the original `q5,q7,q6` necessary system closes five of
those fibers and emits exact bases for nine survivors. Reintroducing
`E=e^2`, all route and chart guards, the rank-cofactor ideal, and original
`q4` closes the remaining nine: seven at route-guard saturations and two at
`q4`.

No exceptional point is discarded through a rational specialization. The
root ledger contains every denominator and determinant root, and every
exceptional replay begins from globally valid polynomial generators.

### DAG consequence

The projective-chart theorem partitions the canonical system into eight
charts. The other seven are already proved empty (`III`, `FII`, `IFI`,
`IIF`, `FFI`, `FIF`, `IFF`). Therefore the canonical `xi=2,pairing=0`
representative is now completely excluded. Symmetry transports this to its
four raw cases.

This does **not** close the full cells-3/6 workboard or 433-1b/O0b route. The
proved quotient had 1,416 representatives; one earlier basis-fed orbit and
this representative are closed, leaving 1,414 representatives.

Modal apps: `ap-JRNgbYBHv9QaOl3g3LaTBx`,
`ap-5hqyVNmIQiOaC6j0YkZe3N`, `ap-qUwke475x3bODff3kZHe9N`, and
`ap-gjp7cX9I4U5M6OGDjM7sZI`. Final admissibility result SHA-256:
`71bb63b164620fb408c08377e33224db69b6dac929ab375ead370ebd658e45ee`.
