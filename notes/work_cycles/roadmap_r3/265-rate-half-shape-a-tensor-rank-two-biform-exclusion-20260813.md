# Cycle 265: rate-half Shape-A tensor-rank-two biform exclusion (2026-08-13)

The exact `e=7` degree-ledger kernel is generated by a separation-rank-two
biform. That mechanism is now excluded in the official regime.

For a primitive rank-two parameter pencil, distinct projective row types
have disjoint `m=e-2` root sets in the `3e` slopes. Since
`4(e-2)>3e` for `e>=9`, at most three types occur. Each type is a fiber of
one degree-at-most-`n` projective coefficient map and therefore owns at most
`n` domain rows. Shape A has

```text
R=(9e-7)/2=3n+7,
```

which is impossible. Common parameter factors and globally constant
coefficient ratios instead create identically zero parameter fibers, also
forbidden.

```text
result:                  PROVED tensor rank >=3 for every official survivor
official row surplus:    7 beyond the rank-two capacity
sharp onset:             e=9; the e=7 fence is correctly retained
hostile mutations:       2/2 rejected
critical status effect:  none
```

The next route action is to classify or exclude tensor-rank-at-least-three
`K_all` kernels using the official spread, collision, or source/Hankel
constraints.
