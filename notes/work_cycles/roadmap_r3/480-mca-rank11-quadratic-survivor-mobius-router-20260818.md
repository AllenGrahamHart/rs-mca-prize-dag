# Cycle 480: quadratic survivor Mobius router

## Result: PROVED base-field degree-two classification

Compose the owner-safe affine mass router with the proved pair-locator
Mobius dichotomy. Either a packet already has `chi>=2299571`, the forced
4,370-fiber pencil has degree different from two, or its quadratic fibers
are the two-cycles of one base-field involution

```text
phi(x)=(c-a*x)/(a+b*x),       a^2+b*c!=0.
```

The nonzero affine class has already been removed. The remaining quadratic
classes are exactly:

```text
antipodal:         phi(x)=-x,
constant product: phi(x)=kappa/x, kappa in H,
shifted inversion: phi(x)+tau=kappa/(x+tau), tau,kappa!=0.
```

The shifted class must contain at least `2*4370=8740` distinct nonfixed
official-domain graph points. This removes the degree-two extension-field
normalization ambiguity but pays no class.

## Falsification probe

The preregistered Modal app `ap-CiORfCwgZcjfdftPZfgEg2` completed 96/96
shift shards and 6144/6144 exact parameter rows. Every shard independently
recounted its maximum with exponent membership.

```text
maximum=2336, q50=2066, q90=2150, q99=2218,
threshold gap=8740-2336=6404.
```

The candidate shifted-inversion cap survived with wide sampled margin. This
is heuristic evidence only; 96 of 1016 shift cosets were sampled.

## Burn-down

```text
critical target attacked: rate_half_band_crossing_location
DAG delta: +1 background PROVED node, +3 edges
critical status delta: none
route delta: quadratic residual -> exact antipodal/product/shifted trichotomy
new assumptions: none
next action: prove a uniform shifted-inversion cap or price quotient classes
```

## Nonclaims

- no payment of the shifted-inversion, antipodal, or constant-product class;
- no classification of degrees `1,3,...,11`;
- no high-complexity payment, rank-eleven closure, or MCA closure.
