## Work cycle 404: rank-eleven rich-flat residual mass compiler

### Pins

- starting Codex pin: `9add2d131`
- canonical Fable prize pin: `c31605f55`
- upstream main pin: `93fba1be3f3299b0ba4708d88715377bbb656e45`
- upstream rich-flat tip: PR `#1173` at `2788d5ec3`

### Result: PROVED

PR `#1173`'s complete transverse envelope is below the least unsafe integer
cardinality by

```text
274980728111395088-274978720888758363=2007222636725.
```

Thus every unsafe line has at least that many selected slopes in
nontransverse rank-one/rank-two row-space groups. The maximum original group
cap is `R_2=247628052`, forcing at least `8106` distinct represented row
spaces.

For each such group, extend a selected rich proper flat to a hyperplane of
`U^perp`; its orthogonal is a dimension-two or dimension-three promoted
space `W` vanishing on the same `42453` actual anchor-good coordinates.
Merge all groups assigned to identical `W` before charging. The interleaved
affine-container caps are

```text
R_2=247628052,  R_3=3953204973.
```

Hence the distinct promoted-container counts satisfy

```text
R_2 B_2+R_3 B_3>=2007222636725,
B_2+B_3>=508.
```

Each bucket is an exact input to the common-core shortening adapter; the
shortening remains group-local. Primary and independent arithmetic replay
pass. Modal app `ap-vICnkUlgtQNsXJtC9zXZQH` used one 256 MiB worker.

### Burn-down

- critical node attacked: `rate_half_band_crossing_location`
- DAG delta: one background `PROVED` mass/compiler node; no new premise
- critical status delta: none
- route delta: singleton rich-flat terminal strengthened to a population of
  508 distinct typed containers and 8106 original rich row spaces
- upstream delta: compact strengthening suitable for PR `#1173`
- next route action: prove a collision/synchronization dichotomy for 508
  dimension-two/three subspaces of the ten-dimensional polynomial direction
  space, retaining their actual zero sets and disjoint owner buckets
