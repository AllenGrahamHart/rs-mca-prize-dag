# WAVE-20 fresh-context replay audit — v8 pin 61f3900a vs master 74eea5b8 (branch point 16604899)

Status: IN PROGRESS. Incremental findings.

## 0. Setup / lineage verification
- Pin repo /home/u2470931/smooth-read-solomin/prize-codex-resolution-v8-20260721, branch codex/full-prize-resolution-v8-20260721, HEAD == 61f3900a ("Record concurrent Modal spend and contributor handoff policy") — EXACT match, no drift.
- dag.json sha256 4d97e45d1322889de68f73948961cb1790016cb4cf3a0dec7462054f18ca6207 — identical in working tree, git object at 61f3900a, and archived w20_tree/.
- merge-base(v8, master) = 16604899 CONFIRMED. 166 commits in range 16604899..61f3900a. First commit ba4c4cd9 "Exclude reciprocal C2 affine shard" = re-expression of v7 tail as predicted.
- Counts: pin 1151 nodes / 2657 edges; master@74eea5b8 1067 / 2427; base@16604899 1030 / 2294; master@8eb59e7c (w19 import) 1067 / 2427.

## 1. Headline dag-delta (exhaustive, computed not assumed)
- pin-new vs base: 121 nodes. Of these, 7 are wave-19 re-expressions (overlap with master's w19 import), 114 are v8-NEW.
- **REMOVALS: ZERO.** pin removes NO node present at its branch point. The briefing's "30 removed nodes" are in fact the 30 wave-19-imported master nodes that v8 (branched pre-import) simply never re-expressed: all 19 xr_* wave-19 campaign nodes + f3_hge4_ambient_order_norm_level_cap + 10 more xr_ (full list in w20_sets.json "mas_only"). These are SURVIVAL items, not removals. => Priority-2(d) "custody of removals" reframes to: do v8's new nodes conflict with/duplicate these 30.
- Status flips base->pin: exactly 10 (verified per-node):
  - DEMOTIONS (8): knife_edge_census, certified_valueset_lower, far_pair_separation, lattice_cone_certificate, certifier_uniformity PROVED->CONDITIONAL; generator_economy, integer_code_distance_cert, u2_per_row_certifier PROVED->TARGET
  - REFUTATION (1): generator_size_budget_check PROVED->REFUTED
  - CLOSURE (1): xr_tangent_support_mismatch_bridge TARGET->PROVED
  - (briefing said "9 demotions"; actual is 8 demotions + 1 refutation)
- Edge delta: pin +370/-7 vs base; of the +370, 19 re-express w19 edges, 351 v8-NEW. Master-only edges vs pin: 121 = 114 unre-expressed w19 edges + the 7 edges v8 removed.
- **7 genuine edge REMOVALS (both endpoints survive)** — each needs adjudication:
  1. cluster_certificates -> generator_economy (req) [demotion sweep]
  2. generator_size_budget_check -> generator_economy (req) [refuted parent unhooked]
  3. list_planted_arithmetic -> list_adjacency_closing (req) [CORRIDOR-ADJACENT — investigate]
  4. xr_strip_classification_rungs -> xr_tangent_support_mismatch_bridge (ev)
  5. xr_tangent_support_mismatch_bridge -> xr_clean_residual_any_gate (req) [bridge's OWN downstream req removed in same wave it is PROVED — investigate]
  6. xr_target_budget_audit -> worst_word_challenger_pricing (req)
  7. xr_true_tangent_coordinate_injection -> xr_tangent_support_mismatch_bridge (ev)
- master vs pin shared-node status diffs = exactly the same 10 (no other drift).

## 2. PRIORITY 1 — xr_tangent_support_mismatch_bridge TARGET->PROVED
**VERDICT: CLOSES-A-RESTATEMENT (sound as such), NOT master's node. Self-declared, machine-coupled, obligation-preserving.**

### 2.1 What v8 proved (exact)
Pin re-poses the node: title "XR official-row tangent/support-mismatch bridge" -> "XR support-wise tangent/mismatch scope bridge". Proved content = a routing/scope adapter over the six (RowC,prize)x(1/4,1/8,1/16) candidates:
1. Exhaustive dichotomy: every received pair is globally generic (no joint codeword-pair explanation on an A-support) or globally nongeneric.
2. Generic branch: strip rung gives pairwise support intersections <= K; Gamma_hi (some intersection exactly K) = P-A1, Gamma_lo (all <= K-1) = P-B; disjoint exhaustive.
3. Nongeneric branch: only q_z=0 recovered-line tangents removed (paid by xr_true_tangent_coordinate_injection, <= n-A coords); every retained q_z!=0 mismatch proved support-locally transverse (syndrome-lift equivalence: lifts of both endpoint syndromes on E_z <=> codeword pair agreeing on S_z); whole retained population routes to P-A2.
4. Explicit nonclaim (claim_contract): "this node proves neither generic 8n^3 clause nor the nongeneric combined 16n^3 clause".
Proof = short case split + transversality equivalence; req parents ONLY xr_strip_classification_rungs [PROVED in base] + xr_true_tangent_coordinate_injection [PROVED in base]. New ev feeders (not load-bearing): xr_supportwise_transverse_lineray_rank_charge (PROVED; selector charge extended to P-A2: sigma<=3 everywhere from 8n^3 half-reserve, sigma=4 at RowC 1/4 from P-A2's 16n^3; P-A2 counterexample min selector rank >=5 at RowC-1/4, >=4 elsewhere; its own req xr_all_lineray_affine_core_bound PROVED-in-base, verifier PASS), upstream_finite_q_shortcut_route_cuts (a route-cut/negative result from the N3 identity-prefix audit lane). **The closure does NOT rely on any of the 30 master-only wave-19 nodes** — import-independent.

### 2.2 Pose crosswalk (the #750-class question)
- MASTER pose: node itself carries the open 16n^3 bounded-depth branch-width obligation (statement/attack_surface/falsifier all quantify "generic-chart plus low-core slope width exceeding 16n^3"); req-feeds xr_clean_residual_any_gate directly.
- PIN pose: obligation RELOCATED, not discharged. Own audit.md: "This is an obligation-preserving contraction, not a proof of an XR count." notes: "OBLIGATION-PRESERVING SCOPE CONTRACTION... The former standalone nongeneric 16n^3 branch-width target is merged verbatim as P-A2".
- Where the obligation landed (verified textually in dag records):
  - xr_highcore_collision_count (stays TARGET) re-posed as a TWO-CLAUSE target: "P-A1 (generic high core): ... <= 8n^3. P-A2 (nongeneric mismatch total): ... total retained support-mismatch population <= 16n^3 combined, with no separate 8n^3 high/low demand."
  - xr_lowcore_spread_heart (stays TARGET): scope tightened "generic-branch" -> "globally generic-branch" (terminology pin, same obligation).
  - xr_smallcore_spread_count (stays CONDITIONAL): composition clause now "IF highcore supplies P-A1<=8n^3 generic AND P-A2<=16n^3 combined nongeneric, AND lowcore supplies P-B<=8n^3 generic, THEN R_post(u,v;A) <= 16n^3 for EVERY received pair" (was: generic-branch pairs only). Gains req edge from the bridge.
  - xr_clean_residual_any_gate: direct req on bridge REMOVED; still reqs smallcore, so it transitively reqs bridge + highcore + lowcore. Statement UNCHANGED (field-identical to base). No laundering: 16n^3 reaches clean_residual only when highcore(P-A1+P-A2)+lowcore(P-B) resolve.
- Total obligation before = after: generic 8+8, nongeneric 16, per pair 16n^3 either way. Depth caps 169/169/255/254/254/510 + terminal breadth + low ranks retained as evidence/"principal alternative attack" on P-A2 (attack.md).

### 2.3 Verifier replays + machine coupling
- bridge verify.py PASS: replays high/low partition exhaustively over all subfamilies of C(5,3) supports at caps 1,2 (1048 checks) + the F_17 n=8 K=2 A=3 eight-support regression fixture + asserts the FULL scope-transfer wiring: bridge PROVED, "P-A1"/"P-A2" tokens in highcore stmt, "globally generic" in lowcore stmt, all 6 new edges present, AND the old bridge->clean_residual req ABSENT (anti-double-counting guard, line 65).
- smallcore verify_scope_assembly.py PASS: statuses (bridge PROVED, highcore TARGET, lowcore TARGET, smallcore CONDITIONAL) + tokens + wiring + absent-old-edge + 8+8=16 identity at n=1024, 2^41.
- strip rungs PASS, rank_charge PASS (real arithmetic: comb(r+3,3)<=8n^3, comb(r+4,4)<=16n^3 iff rank_four_paid + transversality model), route_cuts PASS + audit 6/6, all_lineray_affine_core_bound PASS, highcore verify+audit PASS.
- **CATCH-A (integrity defect in shipped pin): 3 verifiers FAIL in the pin tree** — critical/nodes/xr_true_tangent_coordinate_injection/verify.py:38, background/nodes/xr_tangent_mismatch_external_zero_factor_reduction/verify.py:101, background/nodes/xr_nondeep_tangent_supportwise_payment/verify.py:80 — each asserts bridge status == "TARGET" (staleness pins that correctly fired on the flip; v8 never updated them). All three PASS on master (bridge TARGET there). Import must patch these three assertions in the SAME commit as the flip.
- Per-commit history: 3 commits touch the node; flip is ATOMIC at d248438d (re-pose files + highcore/lowcore/smallcore absorption + rewiring in one commit); e3cae980 refinement ("Preserve exact XR generic and mismatch allocations"); no transient PROVED-with-dangling-obligation state.

### 2.4 Mutation battery (7 mutations, 6 kills + 1 qualified)
M1 bridge->TARGET: KILL. M2 delete req strip_rungs->bridge: KILL. M3 strip "P-A2" token from highcore stmt (obligation-drop probe): KILL (bridge+assembly verifiers). M4 re-add forbidden bridge->clean_residual req: KILL. M5 parent strip_rungs->TARGET: KILL by pin validator ("declared PROVED but requirements are not all PROVED"). M7 smallcore->PROVED laundering flip: KILL (assembly + validator). M6 corrupt rank_charge statement constants (sigma<=3 -> sigma<=5): SURVIVE — its verifier machine-checks the underlying arithmetic but does not pin the statement TEXT constants; minor gap, ev-only node, not load-bearing.

### 2.5 Cascade on master if imported
- Status flips: EXACTLY ONE (bridge TARGET->PROVED at the NEW pose). smallcore stays CONDITIONAL (still gated by TARGET highcore+lowcore), clean_residual stays CONDITIONAL, r2_clean_rates untouched. NO packaging-adjacent flip. The clean-rate lane's effective frontier is unchanged: everything numerical still hangs on P-A1/P-A2/P-B.
- Consumers: on master the bridge's only OUT is req->clean_residual (rewire to req->smallcore + ev->highcore/lowcore per pin). Master's 21 in-edges include 9 wave-19 ev feeders (xr_quotient_*, xr_mismatch_accumulated/terminal/global_explanation, threshold_quotient_image_lcm) absent from pin: KEEP as ev on the proved adapter (pin's own dependency_subdag: descent/chart nodes "remain evidence for attacks on the two target predicates") + add node-local crosswalk note that wave-19-era "P-A" = new "P-A1" and the quotient/mismatch evidence now attacks P-A2 (now housed in highcore). Wave-19 xr_prize_* floor statements referencing "uniform P-A branch" refer to the generic branch = P-A1 after the split — needs the same one-line crosswalk note on xr_highcore_collision_count (node-local notes rule).
- VERDICT restated: adopt ONLY as the atomic package {node re-pose (8 files) + highcore 2-clause re-pose + lowcore scope pin + smallcore composition edit + 7-edge rewiring + 3 stale-verifier patches + crosswalk notes}. A naked status flip onto master's pose would be UNSOUND (master's statement still claims the 16n^3 content) — REFUSE that form.

## 3. PRIORITY 2 — demotion sweep + refutation (VERDICT: ADOPT, whole class)
All 9 flips land in ONE commit 4a83c64b "Correct the N11 false-green frontier" (+ the companion harness census in notes/CRITICAL_HARNESS_COVERAGE_20260722.md, "AUDIT AND N11 TRUTH REMEDIATION COMPLETE").
(a/b) Nature: this is the ROADMAP'S N11 audit executed against the legacy catch-#69-retrofit family. Root cause: the 2026-07-10 header retrofit made dag.json the source of truth and stamped these nodes PROVED while their folder headers read "was: TARGET"/"was: CONDITIONAL"; tools/auto_discharge.py then minted modus-ponens proof.md stubs ("every predicate is now green... By modus ponens the statement is PROVED") on top of false-green leaves. Three of the demoted (integer_code_distance_cert, u2_per_row_certifier, generator_size_budget_check) are EXACTLY master's validator-flagged "status PROVED but no proof.md" entries (long-flagged status-artifact-gap class). The demotions are RESTORATIONS of pre-retrofit truth with modern per-node status_ruling.md files:
  - far_pair_separation -> CONDITIONAL: "The implication in conditional.md remains proved... The construction premise is open, so the unconditional statement is not proved." (implication proof retained)
  - u2_per_row_certifier -> TARGET: "Procedure intent is not a per-row certificate" — no two-prime result/pinned certificate/independent checker banked.
  - integer_code_distance_cert -> TARGET: banked execution report "contains no official or adopted exhibit-row matrix... or replayable full-row certificate"; names the exact return-to-PROVED path.
  - knife_edge_census / certified_valueset_lower / lattice_cone_certificate / certifier_uniformity -> CONDITIONAL: upstream chain collapse (their conditional implication proofs stay banked; auto-discharged unconditional proof.md stubs deleted — machine-generated, regenerable, no content loss).
  - generator_economy -> TARGET: re-posed as open construction ("lift to half-size subset sums WITHOUT counting exact e_1 collisions as distinct centers"); status_ruling names both dead routes.
(c) REFUTATION VERIFIED INDEPENDENTLY: generator_size_budget_check -> REFUTED. The signed-8-core N'=128 proposal counts 19*C(64,8)*C(56,28) = 2^89.055 subsets, but each antipodal padding pair {x,-x} adds ZERO to e_1, so all C(56,28) paddings of a fixed core share one e_1 value; distinct centers <= 19*C(64,8) = 2^36.291, and even with all 2^8 sign patterns <= 2^40.043 — ~49 bits short of 2^89. I recomputed all four numbers independently (binom(64,8)=4426165368, binom(56,28)=7648690600760440, raw=643233019505026505856396480, deficit=48.9566 bits) — all match; verify.py replay PASS. The zero-sum identity holds in every characteristic. Audit correctly notes it kills ONLY the signed-8-core padding route, not the broader generator_economy target. NOTE: the refuted claim is the LATER signed-8-core proposal, not the base statement's Brief-F numeric check (Brief-F content survives in generator_economy/notes/pro_brief_gap128.md); the node was re-posed + moved critical/->background/ with full artifact set (statement/refutation/result/audit/verify.py).
(d) Removals: N/A — zero node removals (sec 1). The 7 "removed" edges resolve to: 6 req<->ev KIND FLIPS (cluster_certificates->generator_economy req->ev; generator_size_budget_check->generator_economy req->ev [refuted node correctly unhooked as requirement, kept as evidence]; list_planted_arithmetic->list_adjacency_closing req->ev; xr_target_budget_audit->worst_word_challenger_pricing req->ev; strip_rungs->bridge and true_tangent->bridge ev->req) + 1 TRUE REMOVAL (bridge->clean_residual req — justified + machine-guarded, sec 2). Nothing for master to KEEP-vs-drop; all content preserved.
(e) Cascade: the 9 sit in the PRIZE req-closure (chain generator_economy -> far_pair_separation -> certified_valueset_lower -> knife_edge_census -> adjacency_closing[CONDITIONAL] -> mca_grand -> packaging -> prize; u2_per_row_certifier -> x4_exactlist_staircase_split[CONDITIONAL] -> strip/mca_safe/gap1). ZERO downstream status flips in pin (and none would occur on master — all consumers already CONDITIONAL/TARGET in both trees; verified in all three dags). Honest effect = the prize req-closure's open frontier WIDENS (critical surface goes 210/31/20 -> "201 PROVED / 36 CONDITIONAL / 23 open mathematical leaves" per the census note).
(f) Coherence: fully coherent falsifier-power sweep: one commit, four adjudicated contradictions, statuses propagated through the requirement graph, tools/auto_discharge.py made partition-aware + NEW guard tools/verify_auto_discharge_paths.py, harness census reproducible + manifest-registered; also repairs xr_strip_classification_rungs (formerly artifact-free; now conventional proof + 69/88-check verifier — this is the bridge's req parent, so the P1 closure benefits) and gives xr_highcore_collision_count a fail-closed 6-row verifier w/ 10 mutations. Pin validator: PASS, status-artifact gaps 31 -> 25.
IMPORT: adopt the whole commit's content on master (status flips + status_ruling/conditional edits + proof.md stub deletions + refutation node move critical->background + auto_discharge fix + verify_auto_discharge_paths.py + census note + manifest entries). Same-commit couplings: the generator_economy re-pose and the refutation must land together (the ruling cites it); worst_word_challenger_pricing/proof.md -10 lines edit is part of it.

## G. PRIORITY 4 — governance
- **Lineage refinement (major)**: v8 re-expressed ALL FIVE of master's post-branch commits with identical messages (pin 1c9989fc=master 570f0067 WCL hardening; 3b2259a1=0ae71ef1 sizing/weight-5; 1ee2799c=a222b5f5 screens; 985f38cf=b959fe8f CR-004/KB#124; 27173279=74eea5b8 PR#1050/KB#125), interleaved with its own work. So the survival list collapses to BYTE-DIFF checks, and v8 demonstrably acknowledges PR #1050 + the register + the screens + CR-004 refresh. The only master content NOT carried: the 30 wave-19 XR/HGE4 nodes (v8 re-expressed only the 7 DSP8/c1c2 wave-19 nodes from v7's tail; the XR ones are superseded-in-spirit by its own bridge re-pose, and the HGE4 ambient_order cap is mathematically dominated by v8's new ambient_norm_level_contraction — same c=2ceil(mr/(8s)) formula + sharper (ALC-exact) per-width test, 55,050,457,488 cells vs wave-19's 27.8B — but master's audited copies stay valid; KEEP all 30, no conflict).
- **HEAD commit 61f3900a**: +11 lines PRIZE_COMPUTE_REQUESTS.md — records ~$3 Modal credit remaining, imposes concurrent-spend cap: five-minute rule AND sub-$1 conservative cost must BOTH pass; larger runs become recorded contributor handoffs. +8 lines roadmap. Governance-only, sound, consistent with the wave-19 "$1 overlay" precedent.
- **Modal launches in range** (tabulated from added app-ids + ledger): (i) WCL-15 pilot fleet — ap-i3qn (msolve 0.10.1, 10.4s regen + 240s timeout), ap-k2FB (msolve 0.7.5, 240.086s), ap-Oklj + ap-wcQe (Singular micromamba, 240s algebra timeouts), ap-CbRA (native quotient, 240s timeout at exponent-256), ap-o5Jx (cancelled during image build, no compute), ap-tqtB (same family) — ALL hard-capped at 240s < 5min, route-deciding (produce standing route FENCES: "do not retry"; work moved to external subrequest CR-004-MSOLVE-LONG), hash-pinned certificates banked (canonical input sha256 c7b87cdf...), logged. COMPLIANT. (ii) l1_balanced_mixed_growth census — ap-8dNe (pilot + complete n<=32), ap-zu0w (n=64 first run), ap-jOUf (3-shard retry); 998.04 aggregate worker-seconds sharded at 1 CPU/1GiB, $0.016; n=128 explicitly REFUSED ("Do not launch n=128 on the remaining credit", $4.1 estimate) and converted to contributor request L1-N10-128. COMPLIANT with both ceilings; minor registration nit: the record banks aggregate worker-seconds + shard ids rather than an explicit wall-time figure (clause-2 audit language). (iii) ap-YVuP/ap-Nskx/ap-CRn9 in rate_half_cyclic_rotated_prefix_floor/result.md = registrations of historical wave-16 screens, not new launches.
- **PRIZE_COMPUTE_REQUESTS.md three-way**: base 4897 lines; master 5508 (its own +639: CR-004 refresh, r3 pointer @line ~1, register); pin 6854 (master-tail re-expressed + Codex overlay: WCL-15 fences, CR-004-MSOLVE-LONG, L1-N10-128, the $1 cap section). The r3 PLAN-OF-RECORD POINTER is present in BOTH (pin kept it; branch point included it). Import = three-way merge of the request queue; master's copy is baseline, pin's additions are well-scoped new sections/entries.
- **r3 roadmap**: pin's file IS r3 (identical header incl. KB#120 supersession text) + ~1460-line Codex overlay expanding sections 3-7. NOT pure append: 90 base lines updated in place — checked the sensitive ones: identification-discipline paragraph PRESERVED verbatim with count updated ONE->TWO proved (new claim: "L1 scalar-cofactor top shell == one locator-Q fiber (l1_exact_shell_fixed_cofactor_prefix_transport)" — a named machine-verified chain, discipline-compliant pending that node's verifier PASS in the battery); U1 rewritten by a first-work-item audit (notes/U1_OFFICIAL_ROW_NORM_GATE_TABLE.md) that CORRECTS r3's schematic "2^f(w) | Norm(u)" to "p^r | |Norm(u)| <= G(w,m)" and honestly demotes the C36 star packet ("r=1... cannot by itself derive DSP8 max-P<=24"); HGE4 m=64 close recorded with "one small level is not yet the D1 uniform HGE4 purchase". Roadmap-uptake: STRONG (N3, N5, N10, N11 roadmap items all executed and named; falsifiers pre-registered; honest ceilings). Import rule: master's r3 remains of record; adopt pin's overlay SELECTIVELY as a gate-event refresh (the U1 correction + identification-count update are factual and should land; keep KB#120 authority).
- **MAINTAINER_DECISIONS_20260713.md: byte-identical pin vs master.** No decision-file tampering.
- **WCL survival items**: verify_slot_decomposition.py present + identical both sides; slot-node files identical EXCEPT 3 pin-side ADDITIVE amendments (slot_1_7/slot_2_8 statements + zone_coverage official_terminal_attack.md) from new node dli_wcl_extended_six_slot_sparse_divisor_endpoints: exact divisor forms G=YA^2-B^2 | Y^256-1 etc., pruned straight-line systems (76,78)...(142,147) for the six C1'-r3 extended-window leaves, unit ideals over Q, "No certificate is computed and no leaf closes", blind affine scan formally rejected (class floors 6.4e9..1.5e22). Slots stay TARGET. Master text fully preserved underneath — import additive.

## 3b. Folder-surface re-scoping (commit ba5f1806 "Retire mis-scoped W3 consumer")
46 renames critical/->background/ demote the ww/worst-word lane's FOLDERS off the critical surface: list_planted_arithmetic, v13_base_field_normalization_guard, worst_word_challenger_pricing (+notes), worst_word_planted, ww_lower_witnesses, ww_row_envelope_clause; qcore promoted background/->critical/. No dag ids removed. Same commit rewires list_adjacency_closing: list_planted_arithmetic req->ev, list_safe + list_unsafe ADDED as req (pre-existing nodes; list_safe CONDITIONAL, list_unsafe PROVED). Corridor note: the Decision-7 corridor edges are UNTOUCHED; this adds requirements to the corridor's consumer — a strengthening (more explicit conditions), not a bypass. ww semantics in sec 4b.

## 4. PRIORITY 3 — campaign verdicts (sub-agent audits + my verification)
### 4a. L1 (39 nodes, consumer in-deg 36->76) — VERDICT: GENUINE residual shrink at sub-piece level; NO close; NO smuggle; NO U2 content
All 39 l1_* nodes PROVED; 3 req-chained subclusters (first-match/source-layout/pullback ledger; the deep Toeplitz/Pade/tangency chain rooted at l1_exact_shell_prefix_hankel_bridge with the row-sharp-Q bridge nodes; standalone M31/route-cut pins) + 4 standalones; every cluster root's upstream req is pre-existing PROVED (11 suppliers, all PROVED). l1_mixed_petal_amplification: stays TARGET, in-edges all ev (0 req) before and after; statement REWRITTEN (master==base, so pin's rewrite is the only delta) attributing named sub-piece discharges to specific new PROVED nodes (source-layout multiplicity, whole-petal-anchored quotient supply, tame refinement census to <= M tau(ell) <= n classes, periodic-owner obstruction, anchored triple-polarity closure under fixed caps, cofactor-depth q^e cancellation for e<k, raw-ledger exponential fence). EXACT residual (quoted): "small-scale general-pullback or collective aperiodic-support payment on known tame refinements, wild or unanchored quotient maps, growing signed layout/core-defect/petal polarity, the balanced sub-Johnson strip, and arbitrary petal locators outside one common pencil" — closure needs "the global smooth-section/tangent payment or aggregate payment of this narrower internal residual"; the global route's open core is explicitly fenced (Pade section: "does not prove independence, codimension w, or a row-sharp intersection bound when e>=k"; "global split-point transversality remains open"). Upstream identification consumed SOUNDLY (Hankel map = pma_saturated_mixed_support_kernel PROVED; unitriangular isomorphism proved in-node; Lean/PR references re-proved in-node, only numerics replayed). U2-shaped chart-carried descent axioms: ZERO hits (opposite found: "no fixed affine normalization is assumed"). No quantifier slips in 7 nodes read fully. One cross-folder read: l1_tangent_confluent_packet_packing loads sibling l1_split_pencil.../verify.py as helper (unpinned).
### 4b. ww (5 nodes) — VERDICT: D0 RESOLVED-BY-SCOPE-CORRECTION; F-round RE-POSED (retired), margin number unchanged; counterexample arithmetic INDEPENDENTLY REPLAYED
All 5 PROVED. D0 (roadmap gate) resolved: "The consumer polarity was wrong: the lower side needs one witness, not an upper bound on every planted receiver there. Clean-rate adjacency now consumes list_unsafe, list_safe, and list_corridor_ledger directly. W3 remains an open background TARGET and is removed from the critical requirement path." Key payments: parametric_row_scope_router (W3's row registry is a parametric family — killed the queued finite-registry sweep); paid_residual_partition_adapter (first-match guards partition, |X(U)| <= sum u_i + sum b_c); two_class_minimal_ledger_reduction (D0 = ONE theorem: N_nonplant(U) <= B*-p(U)); qa22_currency_separation (QA.22's 16n^3 is per-ordered-pair slope currency, W3 is per-word list-codeword currency; NO proved conversion => xr_target_budget_audit->worst_word req DEMOTED to ev with written justification); spending_cell_fiber_layout_counterexample (REF-kind edge; at q=1705*2^120+1 [Proth-verified prime], n=8192, k=2048, B*=6: constructs a non-planted codeword giving |List(U)| >= 7 > 6 — witnesses an admissible UNSAFE spending cell; "does not refute W3's literal safe-side quantifier"). F-round: the 0.9-bit QA.22 aggregate-vs-B* line NUMBER is unchanged, but currency separation makes it TYPE-INAPPLICABLE to the W3 envelope; the pre-registered regression test is retired with the envelope ("neither easier nor harder — no longer required"). "ref" edge kind: pre-existing convention (8 prior uses, all dead-route/refutation references) — not novel. prize req-closure effect (I verified directly): base 278 members/23 open leaves -> pin 274/24; OUT: ww_row_envelope_clause, worst_word_challenger_pricing, list_planted_arithmetic; IN: list_safe, list_unsafe; +generator_economy/integer_code_distance_cert as open members (demotions). NET: open leaves 23->24 — rigor over optics. IMPORT CAUTION: this is the wave's second-largest semantic decision (a TARGET leaves the prize path). The counterexample + polarity argument is machine-verified and coherent, but the maintainer should re-derive the "one witness suffices on the lower side" polarity claim before accepting the corridor rewire — recommend ADOPT-WITH-MAINTAINER-RATIFICATION.
### 4c. rate_half_ca (58 nodes +58 ev into band_closure) — VERDICT: clean evidence-only case-split/routing tree; NO smuggle; seam and bracket DO NOT MOVE
All 58 PROVED (+rate_half_kb_v4_tangent_source_atom, +rate_half_arbitrary_line_syndrome_router = 60). Internal: 68 req edges all target new PROVED nodes; 44 feeding reqs from pre-existing PROVED; ALL 58 edges into rate_half_band_closure are ev (in-deg 80->138, all ev). band_closure node: title/statement/notes/refs BYTE-IDENTICAL base vs pin — TARGET unchanged; K5 seam (sigma* = t*-1 = 8,592,912,738) and beyond-2^167 bracket (budgets {2^39, 2^39+1}, a_RH in [k+2^34, 3n/4 or n]) VERBATIM unchanged, and neither number migrated into any new node. Sub-families: 38 distance-three dihedral/Pade/quartic-support routers+fences (e.g. external_split_design_exclusion: "the distance-three chart is empty" on the official A=1,s=1,e=2m-1 face), 11 Hankel (Forney MDS-Schur router etc.), 8 quotient clean-fiber jets, syndrome router (SL1 iff: bad slopes <=> syndrome pairs mod C^2; toy q=7 witness = falsification lead, no lift claimed). 4 sampled proofs scope-clean. kb atom: KoalaBear row first-cell cap |Z_paid| <= 981104 + 4-cell partition, from upstream rs-mca PR #1049 @c15927c9; "proves neither the KoalaBear row... nor any adjacent Prize threshold". Verifier hygiene notes: 1 hash-pinned executed experiments script (five_slope_route_fence), 1 text-only read of a cross-node Modal launcher (syndrome router), 4 unpinned runpy imports of sibling verify.py.
### 4d. Other new-node clusters, one-line verdicts
- f3_hge4_ambient_norm_level_contraction + f3_hge4_multiscale_haar_m64_level_close (PROVED): supersede/dominate w19's ambient_order cap (same c=2ceil(mr/(8s)) frontier + sharper ALC-exact; 55,050,457,488 cells) and close the COMPLETE m=64 exact level ("Surviving HGE4 levels now start at m=128"); norm_gate_count stays TARGET; consumer edits additive + one justified 1-line supersession of "No exact-ratio level estimate (ERT4) is proved"; ERT4'''' aggregate untouched.
- dli_wcl_extended_six_slot_sparse_divisor_endpoints (PROVED): exact sparse divisor forms + pruned straight-line unit ideals for the 6 extended-window slot leaves; slots stay TARGET; "No certificate is computed and no leaf closes"; blind affine scan formally rejected; contributor handoff CR-004-MSOLVE-LONG.
- f1_pole_same_rate_scope_router (PROVED) = roadmap N5/Conflict-4: "F1 pole/tower descent preserves the rate" — rate-half adjacency NOT a hidden clean-rate premise; f1_pole_list_threshold_location statement byte-identical across all three trees (CONDITIONAL, +1 ev only); corrects the stale 20-of-22 leaf count to 19 clean-rate mathematical leaves; explicitly "not a proof of any list crossing".
- deployed_identity_prefix_owner_scope_audit (PROVED) = roadmap N3: verdict "PROVED; NO ISSUE" — the four deployed upstream identity-prefix attacks straddle printed budgets but are boundary/higher-shell Q or simple-pole owners, stripped before local B_ap; "The below-budget attack at the next row is not a safety certificate". Companion upstream_finite_q_shortcut_route_cuts (PROVED): moment orders 94196/94991/641593/680397 not reusable without tau; Q packing ceiling 1.66M bits too weak.
- xr_lowcore_near/oriented/shift + xr_supportwise_transverse (4 nodes): P-B/bridge evidence (rank_charge covered in sec 2).

## 5. Standard battery — integrity results
- **Pin validator (tools/verify_prize_dag.py in pin tree): PASS** — 1151 nodes / 2657 edges; CONDITIONAL:48, CONJECTURE:15, PROVABLE:27, PROVED:959, REFUTED:27, TARGET:68, WALL:7. Status-artifact gaps 31 (master) -> 25 (pin) — net repair. RIPE m4_table/window_m5_charts + artifact-kind list + 1 stale header: unchanged informational.
- **Replay battery: 198 PASS / 2 FAIL of 200** runnable new/changed verify*.py + tools (22 excluded by design: moved notes launchers + experiments *_modal.py/contributor scripts). Both FAILs are v8 self-inflicted staleness SHIPPED RED at HEAD:
  - critical/nodes/rate_half_band_closure/verify_attack_contract.py (added 9daf3f8c) requires marker "PRE-REQUEST WITH ONE EXACT FACE CONTRACT" in PRIZE_COMPUTE_REQUESTS.md; commit 5011d0c4 ("Close the official distance-three split design") removed the marker without updating the verifier.
  - tools/verify_critical_harness_coverage.py pins critical orbit == 261; the ba5f1806 folder moves changed the orbit after the census ("pinned classes force an explicit refresh" — the refresh never happened).
  - PLUS the 3 stale bridge-status pins from sec 2.3 (not in the changed-file battery; they are pre-existing unchanged files that fail in the pin tree) => total 5 red verifiers shipped in the pin tree.
- **Mutation controls: 10 total, 8 genuine kills, 2 qualified survivals** — sec 2.4's M1-M7 (6 kills + M6 qualified) + M8 REFUTED->PROVED resurrection of generator_size_budget_check: SURVIVE — its verify.py replays the arithmetic but NEVER reads dag.json (no status pin; and with no req consumers the validator wouldn't catch a resurrection either) — CATCH-B, import should add a status pin line; M9 delete counterexample ref edge: KILL; M10 kb-atom 981104 dag-text corrupt: SURVIVE (verifier recomputes internally + pins own status but not the statement text constant) — same minor genus as M6.
- **Manifest delta**: tools/verifier_manifest.json +466/-16, updated in step at each commit (final state consistent; battery + validator green over it).
- **Ceremony/descriptor/dli-lane survival: CLEAN.** Zero files touched in range matching descriptor/dli_* EXCEPT dli_wcl_* (WCL slots, additive consumer amendments + new node + tested_with_power/subfamily notes — legitimate); MAINTAINER_DECISIONS byte-identical; no verifier asserts dli/descriptor statuses (checked via the changed-verifier scan); the dli one-third cap untouched (WCL work is within the lane's standing scope).
- **FLIP_LOG.md/FLIP_REPORT.md: NOT updated in range** — the 10 flips are recorded in commit messages, status_ruling.md files, and the census note instead; minor convention drift (legacy files).

## 6. The two pin lists
**List 1 — new/changed verifiers reading OUTSIDE own folder + root dag.json** (import couplings; all verified during battery):
1. rate_half_arbitrary_line_syndrome_router/verify.py -> text-read of critical/nodes/rate_half_band_closure/notes/witness_hunt_20260712/rh_band_witness_census_modal.py (no exec).
2. rate_half_ca_hankel_strict_m1_corefree_five_slope_route_fence/verify.py -> sha256-pinned runpy EXEC of experiments/prize_resolution/rate_half_strict_m1_locator_lines.py.
3. 4x rate_half_ca quartic verifiers + l1_tangent_confluent_packet_packing -> runpy/spec-import of SIBLING new-node verify.py (unpinned — hygiene nit).
4. dli_wcl_weight5_first64_mitm_exclusion/verify.py -> experiments mitm .cpp + _modal.py + result.json, hash-pinned. NOTE: pin's version DIVERGES from master's (master compiles+replays the search via g++ subprocess; pin is certificate-check-only but adds a LAUNCHER_SHA256 pin). Recommend: keep master's stronger replay + adopt pin's launcher hash pin.
5. ww_qa22_currency_separation/verify.py -> reads worst_word conditional.md, ww_row_envelope statement/spec_frontier, xr_clean_residual statement, xr_smallcore conditional, PRIZE_COMPUTE_REQUESTS.md (wide pin-token audit — most import-fragile verifier of the wave; must land with ALL its referenced file edits).
6. ww_parametric_row_scope_router + ww_two_class + ww_paid_residual -> ww_row_envelope files + official_row_primes_pinning + notes files.
7. f1_pole_same_rate_scope_router/verify.py -> proof.md of ext_import, f1_minimal_field_descent, interleaved_floor_import; deployed_identity...audit/verify.py -> statement.md of its 3 req parents.
8. verify_dli_wcl_15_msolve_modular_pilot_result.py (experiments) -> notes/PRIZE_COMPUTE_REQUESTS.md + roadmap; rate_half_band_closure/verify_attack_contract.py -> notes files (currently RED).
9. reciprocal_affine_collapse -> experiments result.json (w19-known, unchanged).
**List 2 — existing MASTER verifiers that break on import**: EXACTLY THREE, all by the same stale pin `nodes["xr_tangent_support_mismatch_bridge"]["status"] == "TARGET"`: critical/nodes/xr_true_tangent_coordinate_injection/verify.py:38, background/nodes/xr_tangent_mismatch_external_zero_factor_reduction/verify.py:101, background/nodes/xr_nondeep_tangent_supportwise_payment/verify.py:80. Patch to "PROVED" in the flip commit. NOTHING else: zero master verifiers assert the demoted nodes' PROVED statuses; true_tangent's own clean_residual req edge assertion is untouched by the rewiring; the 9 wave-19 xr verifiers only assert their own ev-edge membership into the bridge (edges kept); master's norm_gate/adjacent_crossing verifiers unaffected (files identical or additive).
**Reverse-consumer pins**: nothing on master pins the bridge's STATEMENT text; l1_mixed_petal/petal_mixed/l1_mixed_residual/band_closure in-edge sets are NOT exact-asserted by any master verifier (the l1/pma/xr verifier hits are edge-membership assertions of their OWN edges, all preserved). adjacent_crossing bracket list: pin==master (110 brackets, no new bracket this wave).

## 7. Refusal screening (house law)
- Packaging flips: NONE (packaging/prize/mca_grand statuses identical base/pin; no RIPE change).
- Req-smuggle into reds/ambers: NONE from new nodes (both agents + my sweep). The 3 new req edges into pre-existing CONDITIONAL nodes (list_safe->list_adjacency_closing, list_unsafe->list_adjacency_closing, bridge->xr_smallcore_spread_count) all point PROVED-or-stronger content INTO conditionals (adding requirements/support, not discharging them) — legitimate, each with written justification.
- Silent discharges: NONE — every status change has a ruling artifact; the one edge REMOVAL is machine-guarded.
- Rewrites of master-held statements: FOUR, all adjudicated: bridge re-pose (sec 2 — adopt as package), l1_mixed_petal residual restructure (justified, residual-preserving), ww_row_envelope RETIRED rewrite (sec 4b — maintainer ratification), norm_gate 50-line additive + 1-line supersession (justified by m64 close). Plus highcore/lowcore/smallcore absorptions (part of bridge package).
- Ceremony + descriptor + dli lane: SURVIVES (sec 5).

## 8. Import-surgery spec (ordered, with same-commit couplings)
Baseline: master 74eea5b8. All steps node-scoped; never copy pin's dag.json/roadmap/notes wholesale.
1. **Wave-19 survival (no-op guard)**: keep master's 30 xr_/hge4 wave-19 nodes + 114 edges + the 7 re-expressed nodes' master copies (byte-identical — nothing to do). Keep master's roadmap r3 + decisions file.
2. **N11 demotion sweep (one commit, mirrors 4a83c64b)**: 8 demotions + REFUTED flip in dag.json + status_ruling/conditional/statement edits + DELETE the auto-discharge proof.md stubs + move generator_size_budget_check critical/->background/ with its new artifact set + worst_word_challenger_pricing/proof.md edit + tools/auto_discharge.py fix + NEW tools/verify_auto_discharge_paths.py + census note + manifest entries. ADD (audit repair, CATCH-B): status-pin line in generator_size_budget_check/verify.py. Coupled edge-kind flips: cluster_certificates->generator_economy req->ev, generator_size_budget_check->generator_economy req->ev.
3. **Bridge closure package (one commit)**: bridge re-pose (all 8 files) + TARGET->PROVED + in-edge kind flips (strip_rungs, true_tangent ev->req) + new ev in (rank_charge, route_cuts — requires step 5's nodes, so order after or merge with 5; alternatively land nodes first) + OUT rewiring (-req clean_residual, +req smallcore, +ev highcore/lowcore) + highcore two-clause re-pose + lowcore "globally generic" pin + smallcore composition edit + verify.py/verify_scope_assembly.py + PATCH the 3 stale ==TARGET pins (List 2) + node-local crosswalk notes on highcore & the 9 wave-19 ev feeders ("w19 'P-A' = P-A1; quotient/mismatch evidence attacks P-A2"). Refresh xr_strip_classification_rungs' repaired proof+verifier (from N11 commit content) BEFORE this lands.
4. **New-node payloads (bulk, order-free within)**: 114 v8-NEW nodes + their edges: L1 (39), rate_half_ca+kb+arbitrary (60), ww (5), f3_hge4 (2, WITH norm_gate_count statement/attack/frontier edits + keep master's ambient_order ev edge), dli_wcl (1 + slot statement amendments + tested_with_power notes), f1_pole + deployed_identity + route_cuts + xr_lowcore trio + rank_charge. Couplings: ww nodes must land WITH the ww_row_envelope rewrite + worst_word frontier/conditional edits + the req->ev downgrade (qa22's verifier reads them all); the mitm verifier: keep master's replay version + add pin's launcher-hash pin.
5. **D0 rewire (separate commit, maintainer ratification)**: ba5f1806 mirror — folder moves (6 lanes critical->background, qcore in) + list_adjacency_closing rewiring (planted req->ev, +list_safe/+list_unsafe req) + ww_row_envelope RETIRED statement + counterexample ref edge. Pre-registered falsifier retirement recorded.
6. **Staleness repairs before merge**: refresh verify_attack_contract.py marker (restore a historical-record marker line or update the verifier to the promoted request name) + re-run/refresh verify_critical_harness_coverage.py pinned classes to the post-move orbit.
7. **Governance merges**: three-way PRIZE_COMPUTE_REQUESTS.md (master baseline + pin's new sections: $1 cap, CR-004-MSOLVE-LONG, L1-N10-128, WCL fences, launch registrations); roadmap r3: adopt pin's overlay SELECTIVELY as gate-event refresh (U1 correction + identification TWO-count + D0 resolution + N3/N5/N11 results); manifest merge; regenerate orbit artifacts; refresh the Claude artifact + public site per standing rule (rs-mca artifact refresh).
8. **Post-import battery**: full validator + the 200-file battery + the 3 patched verifiers + mutation spot-checks on master.

## 9. INFO / minor
1. Briefing calibration: "30 removed nodes" = lineage gap, not removals; "9 demotions" = 8 demotions + 1 refutation; xr_tangent in-deg on pin is 14 (12 base - 2 kind-flips + 2 new + 2 kind-flips back = 14), master 21.
2. Codex does not run its own full verifier battery before finalizing a branch (5 red verifiers at HEAD across two independent causes) — recommend adding a pre-handoff battery requirement to the contributor policy.
3. Statement-text constants not machine-pinned in 3 new verifiers (M6/M10 genus): rank_charge, kb atom (+the refuted node's missing status pin = CATCH-B). Consider a lint.
4. agent-B noted pin edge count 2456 in its own recount vs my 2657 (raw); reconciled: 2657 raw includes the known pre-existing duplicate petal edge + counting method; immaterial to any conclusion.
5. l1_balanced census Modal record banks aggregate worker-seconds (998.04s) + shard ids but no explicit wall-time figure — minor clause-2 registration nit.
