# Pro strategy — Haar-relative inverse flatness (the shared black hole) — 2026-08-01

> **Provenance:** GPT Pro strategy document responding to the planner's
> six-brief meta-diagnosis ("inverse flatness is one mystery wearing four
> masks"), relayed by the maintainer. Checker:
> `verify_inverse_flatness_swif_l1_strategy.py` (this directory; replayed
> under ramguard, full PASS). **Fable audit:**
> `BLACKHOLE_STRATEGY_AUDIT.md` — read first.
> **Minted from this document:** `dli_c1_l1_block_owner_ledger` (PROVED).

## The thesis

The shared black hole is: *a deterministic bounded-alphabet convolution
generated by an algebraic orbit is Haar-flat at consumer scale unless a
bounded-complexity exact algebraic resonance is present.* Cleanest
laboratory: C1 at L=1 (coefficients 1, omega, ..., omega^255, order 512)
— no matrix, no selectors, no descent; the mystery fully present.

## Key exact content (all verified)

1. **Four-block owner ledger** (now a node): C1-ZERO at L=1 is EXACTLY
   `sum_(j<64) [A_j - 15*2^(4j)/q] <= 3 + 1/q` — the 64 structured
   four-bit targets attract at most ~3 units of excess weighted relation
   mass over Haar.
2. **Weight-9 fence:** min-weight-9 gadget (superincreasing + sum) has
   ideal-contraction ratio 257/256 > 1 — literal SWIF-4 may fail while
   C1-ZERO holds; the threshold 4 is an aggregate resonance budget.
   Central theorem softened to **CERP-512**: excess ==> bounded canonical
   resonance packet (bounded blocks/rank/height, q-free certificate),
   containing a weight-<=8 relation OR in a finite exceptional list.
3. **Analogue resonance chain** (q=50177, N=16): min weight exactly 9,
   spectrum 320/32/96/64 at 9/10/12/14, |Res(P, X^16+1)| = q,
   v_2(q-1) = 10, excess < 1 — the full causal chain
   (resonance -> resultant -> gate-visible prime) in exact arithmetic.
   Contrast q=353: weight 3, resultant = q, strong local spike.
4. **Lambda-rigidity programme** (the new idea): diffuse excess ->
   Freiman model -> repeated lambda=omega^4 invariance -> exact integer
   endomorphism -> Cayley-Hamilton -> bounded q-free cyclotomic relation.
   **The v_2 >= 41 gate becomes a resonance-prime sieve** on packet
   resultants — the first candidate mechanism for the gate, using the
   same norm/resultant machinery as the completed WCL exclusions.

## Architecture

Layer A: Haar-relative defect localisation (always subtract 2^(4j)/q;
spike-vs-diffuse dichotomy; exact owner-trajectory compiler).
Layer B: relative inverse Littlewood-Offord / weighted BSG (near-Haar
branch paid first; progression is intermediate, NEVER a terminal count).
Layer C: lambda-rigidity (the genuinely new theorem; five kill points
pre-registered).
Layer D: Booleanisation (atom provenance retained; coefficient 2 is not
two coordinates) + the 2-adic packet sieve (gate-free statement
preferred, gate consumed only at the terminal sieve).
Work packages IF0-IF8; falsification-first phases 0-5; kill lines K1-K4;
precedents (Hochman, Breuillard-Varju, Tao-Vu, Habegger, GGMT) as models
not imports. Spillover: C2'' (Bellman tilt-invariance), P-B (syndrome
transport), xr (owner compression — may benefit earliest), crossing
(locator recurrence).

## Full text

Preserved at the maintainer's thread and session record; this summary +
audit + checker + the minted node are the load-bearing extract.
