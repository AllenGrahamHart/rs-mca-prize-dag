# Fable audit — Pro's inverse-flatness / SWIF-L1 black-hole strategy — 2026-08-01

**Verdict: SOUND, and the most mathematically productive single document of
the exchange.** The four-block owner ledger is now MINTED
(`dli_c1_l1_block_owner_ledger`, PROVED background, ev into the C1 head —
the third Pro-derived node). One important correction to OUR previously
adopted posture is accepted. No status changes.

## Replay and verification record

- Checker: full PASS under `ramguard local` (~3 min). Verified: the
  complete L=1 identity chain at a full official-shape row (q=7681,
  256 coordinates, order 512) including the order-512 subgroup zero-subset
  identity (a 500-bit integer equality) and the 64-term block-owner
  telescoping in exact rationals; the q=50177 analogue (min signed weight
  exactly 9 by MITM, spectrum 1/320/32/96/64 at weights 0/9/10/12/14,
  |Res(P, X^16+1)| = q, v_2(q-1) = 10, total excess < 1, all four block
  contractions <= ideal); the q=353 contrast (min weight 3, resultant =
  q, a >2x local spike); the weight-9 gadget (superincreasing 3^i, ninth
  = sum: min weight exactly 9, scaled contraction 257/256 > 1).
- Hand checks: the ledger derivation (convolution step, kappa mass
  (1+1/2+1/2)^4 = 16, kappa(0) = 1 via the banked weight-<=4 exclusions,
  Haar increment 15·2^(4j)/q, telescoping from X_0 = 1 - 1/q);
  v_2(50176) = 10; the equivalence (BO-4).
- Independent node verifier written and PASS: per-block kappa mass and
  kappa(0)=1 checked directly (64 ternary DPs), recursion and telescoping
  replayed in exact rationals.

## Correction to our adopted posture (accepted)

The Brief-1 audit adopted "SWIF-4 falsification campaign before any
theorem fleet." **The weight-9 fence sharpens this: literal SWIF-4 may be
false while C1-ZERO is true** — a single weight-9 relation already
perturbs the ideal block contraction by 257/256, so several weight-9/10
orbits could conceivably create excess with nothing in the [L+1, L+7]
window. Adopted revisions:

1. The falsification campaign's FIRST target is engineered weight-9/10
   resonance primes (sparse polynomial norms, deep-v_2 divisors) — an
   in-gate kill of literal SWIF-4 would be a discovery, not a disaster.
2. The central theorem is **CERP-512** (excess ==> bounded canonical
   resonance packet: bounded block support, Freiman rank, coefficient
   height, q-free certificate; weight-<=8 relation OR finite exceptional
   list), with SWIF-4 as the ideal corollary. No fleet whose only
   terminal output is a weight-<=8 relation.
3. The threshold 4 is an aggregate resonance budget, not a zero-defect
   claim — activity ledger accounting, exactly the C2''/E1 pattern.

## What is genuinely new here

- **The minted ledger** localizes the inverse-flatness mystery into one
  exact sentence: 64 structured four-bit targets, at most 3 + 1/q excess
  attraction. The cleanest formulation of the black hole to date.
- **The lambda-rigidity mechanism** (diffuse excess -> Freiman model ->
  repeated lambda-invariance -> exact integer endomorphism ->
  Cayley-Hamilton -> bounded q-free cyclotomic relation) is the first
  candidate CAUSAL account connecting probabilistic non-flattening to
  algebraic resonance. Layer C (IF4) is the genuinely new theorem; its
  kill points (approximate-not-exact invariance, coefficient height
  explosion) are honestly pre-registered.
- **The v_2 gate finally has a candidate mechanism**: a resonance-prime
  SIEVE — packets survive only at primes dividing bounded cyclotomic
  resultants AND lying in the deep splitting progression. The q=50177
  chain (resonance -> resultant = q -> v_2 visible in the prime)
  demonstrates it end-to-end in exact arithmetic, and it is the same
  machinery as the completed WCL norm-gate exclusions — existing
  infrastructure, not speculation.
- **The spillover map** (section 13) is credible precisely because it
  transfers technology, not statements: fibre -> Bellman state (C2''),
  lambda-rigidity -> syndrome-line transport (P-B), -> locator recurrence
  (crossing). The high-core lane may benefit earliest (its packets are
  already compiled; only ownership/reconstruction is missing).

## Points of caution

- Layers B-D are a research programme with five honest kill points; only
  Layer A's interfaces (the ledger, minted; the compiler; the primitive
  normalisation) are near-term bankable.
- The precedents (Hochman, Breuillard-Varju, Tao-Vu, GGMT) are correctly
  labelled as models, not imports — the finite, Haar-relative, weighted,
  q-uniform setting needs its own theorem.
- The analogue evidence is order-32; the checker's L=1 row (q=7681) shows
  near-zero excess, consistent but small-field.

## Adopted sequencing (Codex-shaped items marked)

1. Ledger node — MINTED today.
2. [Codex] Adversarial weight-9/10 resultant campaign: engineered sparse
   polynomials, factor cyclotomic norms, select deep-v_2 prime divisors,
   build exact rows, measure the ledger. THE decisive early experiment.
3. [Codex] Exact owner-trajectory compiler (block excesses, min owner
   weight, tilted spectrum, candidate approximate periods) at N=16/32/64.
4. Packet-complexity growth test (kill line K2).
5. Lambda-rigidity theorem attempt — only after 2-4.
No official-field computation before steps 1-4; no engine selection
(weighted BSG / entropy / switching) before the pilots.
