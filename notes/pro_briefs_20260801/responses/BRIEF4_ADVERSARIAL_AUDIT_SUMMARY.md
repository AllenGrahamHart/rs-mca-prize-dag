# Pro's adversarial self-audit of the Brief-4 (P-B) dossier — 2026-08-02

> **Provenance:** Pro's adversarial audit of its own P-B producer dossier
> (same thread), relayed by the maintainer. Checker:
> `verify_adversarial_audit_brief4_lowcore.py` (replayed under ramguard,
> all seven fixtures PASS — including a LIVE polynomial fixture over
> F_12289). **Fable verification:** the split-fibre identity, degree
> gaps, intersection caps, char-2 Sidon argument, producer cardinality
> arithmetic, and pilot-vacuity identity re-derived by hand.
> **Addendum recorded on `BRIEF4_DOSSIER_AUDIT.md`.** No DAG changes.

## The construction (the deepest fence of the four audits)

**The split-fibre pencil at exact RowC 1/4 parameters** (N=1024, K=256,
A=261, the official mu_1024 domain): U = G X^208, V = -G X^204,
p_J = -G R_J(X^4) gives U + z_J V - p_J = G Q_J(X^4) EXACTLY — a
globally generic received pair whose candidate witnesses at
M = 129,948,699,009 > 15 x 8N^3 distinct slopes (via a weight-52 binary
Sidon family {(x,x^3)} pushed through the four-point fibres, slopes
separated at infinitely many split primes by the deg-103 < deg Phi_256
cyclotomic argument) have: exact A-supports, pairwise intersections
<= K-1, and EVERY oriented difference distinct (D_* = E_* = M(M-1)).

Consequence: **RS realization, the syndrome line, global genericity, the
dyadic domain, distinct slopes, and the low-core cap are JOINTLY
insufficient to produce additive energy.** The producer
D_* <= N^2(M-1) fails on this family by a factor > 123,928; the
rectangle and weighted variants fail identically; the (slope,alpha,beta)
owner key fails by pure cardinality (M(M-1) > MN^2).

**Not yet a P-B counterexample:** the exhibited witnesses need not be
the normative first-match selections. Hence the central finding:
**first-match minimality is not one hypothesis among many — it is the
ONLY remaining possible source of P-B.**

## Also established

- **The RecPB closure trilemma:** a local inherited class admits the
  mutation family (producer false); child reselection breaks recursive
  composition; ancestor certificates are unbounded. The instruction
  "weak enough for puncture closure, strong enough for the producer" is
  structurally self-contradictory, not merely hard.
- **Pilot vacuity:** D_* <= M(M-1) <= N^2(M-1) automatically whenever
  M <= N^2 — every small-row producer pilot was incapable of testing the
  conjecture. Our adopted PP4.2 gate was toothless as designed.
- **Track B is not finite above rank two:** the cyclic two-triple
  hypergraph (L blocks, 2L coordinates, degree 3, block size 6, pairwise
  intersection <= 1, no private coordinates, extensible to every L)
  satisfies all combinatorial support constraints — the GRS coefficient
  equations must supply boundedness, and no such theorem exists yet.
- An order-independent producer theorem is FALSE (an adversarial witness
  order selects the whole family), so any P-B proof must exploit the
  exact canonical order's algebra.

## The replacement: root-level first-match exchange

For W_z = all exact-A witnesses at slope z and the normative
(p_z, S_z) = min_prec W_z, the target theorem becomes: a super-budget,
low-energy selected family forces construction of an earlier witness
(p', T) prec (p_z, S_z) at some slope — contradicting first-match.
A lex-shadow/exchange theorem at the ROOT: puncturing is only an
algebraic subroutine, no child is ever reselected, every contradiction
owned once by (slope, earliest T, first construction). The split-fibre
family becomes the required mutation: the theorem must show why its
intended witnesses are not normative first matches.

## THE decisive experiment (priority one across the whole program)

FM2: run the exact normative selector on scaled split-fibre pencils and
compare the candidate family (super-budget Sidon) with the SELECTED
family. **Kill line K1: if the selector itself chooses a super-budget
Sidon subfamily, P-B — the target, not the route — may be false, and the
program stops immediately.** Either outcome is decisive: the mechanism
is identified, or a critical red is endangered. Codex-shaped (FM0
selector manifest + FM1 complete candidate compiler + FM2 fixture).

## Posture

Conditional exploratory GO for FM0-FM3 only (selector manifest, complete
candidate compiler, split-fibre mutation, first exchange lemma). NO
recursive middle-width fleet, no producer pilots below M > N^2, no
O(MN^2) owner keys, no high-rank "finite atlas" claims. Track B remains
valid in rank two only.

## The four-audit pattern, completed

Every lane is now stripped to its irreducible core mechanism:
C1 -> long doubling-orbit exponential moments; C2'' -> the weighted
nullity tail; F2 -> the b-resolved slice coefficient theorem;
P-B -> root first-match exchange. In each case the abstract version is
provably uncontrollable and exactly one structure-specific mechanism
remains. Brief 6 is the only dossier not yet adversarially audited.
