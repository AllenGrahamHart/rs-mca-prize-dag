# Pro's adversarial self-audit of the Brief-6 (crossing) dossier — 2026-08-02

> **Provenance:** Pro's adversarial audit of its own Brief-6 dossier (same
> thread; the FINAL audit of the five-audit set). Checker:
> `verify_adversarial_audit_brief6_crossing.py` (replayed under ramguard,
> all six fixtures PASS — including live exact-shell enumerations by full
> Lagrange interpolation). **Fable verification:** the rank-one Toeplitz
> construction (constant-coefficient argument, gcd(e,n)=1
> equidistribution, exactness guard), the n/8 = 2^38 gap, and the
> multinomial chamber explosion re-derived by hand.
> **Addendum recorded on `BRIEF6_DOSSIER_AUDIT.md`.** No DAG changes.

## SURVIVES COMPLETELY (the good news)

- **`rate_half_list_cyclic_budget_staircase` (our minted node) — every
  tier intact.** The lower frontier remains productive and exact.
- The exact-integer Johnson anchor; the exact-shell Toeplitz normal form
  WITH its gcd guard (correct interface, retained); cumulative-shell and
  first-owner disciplines; REALIZE/EXCLUDE/UNKNOWN semantics; the
  fieldwise optimizer concept.

## Retired / demoted (with exact fixtures)

1. **The certificate contract needs a SUCCINCTNESS clause.** "Finite
   row-computable total oracle" is brute-force-trivial (q^n received
   words: log2 count > 2^47 on the B*=3 branch alone). Required:
   certificate + verifier complexity polynomial in (n, log q, log B*),
   or a q-independent symbolic grammar. Our pending contract amendment
   gains this clause.
2. **The 13 chambers are incidence TYPES, not finite cases.** One exact
   defect-two cell has (4d-4)!/((d-1)!)^4 labeled placements — >2^41
   BITS of orbits at d = 2^39 even after codeword and domain symmetry
   quotients (the evaluation domain is not permutation-symmetric). The
   repo's own chamber algebra is already official-degree (2^35, 2^37
   objects). PP6-SMALL is demoted from closure route to
   semantics/certificate calibration — still worth one complete run
   (chamber completeness, certificate replay, a possible outright B*=3
   REALIZE), but NOT a scalable plan: all-excluded moves the safe
   frontier by ONE coordinate against a 2^38-coordinate gap.
3. **Rank/profile primitive envelopes are FALSE.** The rank-one fence:
   U_c = X^(n-1) + c X^k at shell k+1 gives the SAME homogeneous
   rank-one Toeplitz map for every c, yet the exact fibre is 0 at c = 0
   (no divisor of X^n - 1 has zero constant coefficient) and
   C(n, k-1)/n at c in mu_n (subset products equidistribute since
   gcd(e,n) = 1; exactness guard verified). The affine position relative
   to the split-divisor variety is the ENTIRE problem. Max-to-average
   gap = q/n (up to ~2^215): no average/second-moment route converts.
4. **Per-shell caps do not compose** (the 35+7 = 42 toy, live-enumerated)
   — now a required-to-trip mutation.
5. **RHL-STRUCT as posed is tautology-prone**: without a pre-registered
   template list, q-independent reconstruction theorems, exact
   population bounds, and a CONCRETE primitive property ("not on the
   owner list" is not a property), the owner grammar is a relabeling of
   the list problem. PE1 survives as the statement of the black hole,
   retired as an executable route.

## The replacement: split-section inverse classification

Large gcd-guarded split-divisor fibre in a realized affine Toeplitz
section ==> one bounded q-independent same-word PACKET (fixed template,
exact cardinality, injectivity, compatibility with all other packets),
with the primitive residue defined by a concrete stronger property
(monodromy, no low-degree quotient factors, split-divisor expansion, or a
q-free circuit exclusion). Mandatory mutation library: the rank-one
family, every rotated-prefix scale, fixed tails, dihedral packets, packet
unions, cross-shell examples. Exploratory GO for: the succinct contract,
the mutation suite, and ONE complete packet theorem end to end. Kill
lines K1-K7 (notably: owner taxonomy growing with q/n, or descriptor
collisions with super-polynomially different fibres).

## THE FIVE-AUDIT SET IS COMPLETE — the final frontier map

Every mystery lane now stands on one irreducible, named mechanism, with
the abstract version provably uncontrollable in each case:

- C1: exponential moments of the cyclotomic potential on LONG DOUBLING
  ORBITS (small orbits -> finite resultant sieve).
- C2'': the weighted CROSS-JUNCTION NULLITY tail (R = E q^delta).
- F2: the B-RESOLVED SLICE COEFFICIENT theorem for the carry-DFT mode
  product.
- P-B: ROOT FIRST-MATCH EXCHANGE (the split-fibre selector experiment =
  program priority one).
- Crossing: the SPLIT-SECTION INVERSE CLASSIFICATION (this audit).

Common shape: local/bounded statistics are blind; each lane survives
only through one structure-specific global feature; every route that
ignored it has been retired with an exact fixture. This map — five
named theorems, each with its falsification compiler and kill lines —
is the program's frontier as of 2026-08-02.
