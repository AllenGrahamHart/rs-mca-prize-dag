# Fable audit of the Brief-5 Pro dossier — 2026-08-01

**Verdict: SOUND — and it contains the cycle's single largest strategic
finding: the consumer may need only 2^(n/3) per rung, not square-root
cancellation.** One interface correction accepted (applies to the node
statement AND our brief); the 1/3 budget observation verified exactly; the
new-literature sweep our brief requested is delivered with a
method-only/no-import verdict. No DAG status changes; the proposed
alignment sibling is endorsed but waits on the seam theorem.

## Replay and verification record

- Companion script: full PASS under `ramguard local`. Verified: the
  sixteen-rung geometric sum 2^41 - 2^25; the 1/3-fits (317G slack) /
  1/2-fails (49.5G excess) budget dichotomy with theta* in (1/3, 1/2);
  the 3/1 weighted-alignment counterexample (exact integers: numerator N,
  L2 mass 5N, squared comparison against 2^30); the M_j structural table
  with the j=4 (n/8, not n/4) guard; orbit multiplicity sqrt-scaling
  (2^28 at the 2^56 proxy); the ineffective-asymptotics counterexample
  (10^15 + sqrt(n) is o(n) but busts the allowance); the annealed
  constant log2(4/pi); the fixed-k literature error at both regimes
  (vacuous ~2^50.8 at k~2^24, effective at k=4); the 43-vs-44
  block-calibration dichotomy; the banked j<=3 b-resolved structural
  formula (C(16,8) = 12,870, complement symmetry).
- Hand checks: 2^25(2^16 - 1) = 2^41 - 2^25;
  (2^41 - 2^25)/3 = 732,996,567,040; counterexample ratio bits
  (40 - log2 5)/2 = 18.839; theta* = 0.47749.
- Source-hierarchy audit: all cited files exist (FINDINGS.md, SUMMIT_V4
  with parity/carry content, the QUALITY certificate, the campaign log).
  One trivial erratum: `f2_conditional_close` is in critical/, not
  background/ as Appendix C lists. The drift discipline (later sources
  override earlier; V4 supersedes V3; our brief was written from the dag
  statement and inherited the coarse wording) is exactly right.

## Corrections accepted

1. **The executable target is the signed weighted parity alignment, not
   magnitude/max-to-mean control.** Our brief inherited the node's coarse
   wording; the 3/1 fence proves a prover could close "max-to-mean" and
   leave the theorem open. The failure mode is real and now fenced. The
   node keeps its historical wording as provenance; the executable
   contract moves to the alignment formulation (sibling node
   `f2_weighted_parity_alignment` endorsed — minting deferred until
   PP5.0's seam theorem exists to wire it correctly, per the same pattern
   as Brief 1's C1-ZERO).
2. **Our sharpest question is answered.** We asked for "the weakest
   hypothesis under which ANY nontrivial bound is provable — even
   2^(2^40)-type constants beat the tolerance." Pro's answer is sharper
   than we hoped: 2^(n_r/3) per rung suffices under the pessimistic
   composition, and the block calibration shows one contraction bit per
   43 coordinates clears the proxy. The mystery's "absurdly weak target"
   signature is now a number.
3. **Our literature re-sweep request is fulfilled**: the June 2026
   Cornelissen-Hokken-Ringeling revision post-dates our 2026-07-27 sweep
   and is the genuine new item — method lead (Wasserstein, random walks),
   no theorem import (fixed-k error vacuous at tower shape; Mahler
   magnitude, not parity). Recorded per the literature-map discipline.

## What I accept beyond the corrections

- PP5.0 (the consumer seam) as the absolute gate: six equivalent-sounding
  currencies exist in the records, and no local theorem should be
  optimized before the composition law (add/Cauchy/multiply) is frozen.
  This is the same Gate-0 pattern as Brief 2's packet seam — ours to
  drive.
- The carry-compression audit (PP5.4) as the first scientific question:
  K couples all pairs mod 2p; whether the 2-adic/Frobenius pair grammar
  compresses it decides between the Bellman route and the circulant
  spectral route. A negative answer is bankable.
- The twelve inherited death-ledger classes and the five new fences
  (3/1, orbit-lift, ineffective-o(n), M_j drift, raw/pruned typing) as
  binding on all future F2 work.
- The convergence observation: this dossier's architecture (canonical
  owners + Bellman transfer + descriptor collision audits + exact seam
  first) is the same technology stack as Briefs 2, 3, 4 — the six
  mysteries have collapsed onto ~two proof technologies.

## Points of caution

- The 1/3 budget is PROVISIONAL until PP5.0 proves the composition law;
  Pro is explicit and so is this audit. If losses compose
  multiplicatively across descents, the picture changes.
- The 43-coordinate calibration uses the campaign's frequency-population
  proxy, not the official manifest.
- The first-descent theorem must be functorial from birth (finding I):
  an accidental order-two feature at F_(p^2) that dies at rung two would
  waste the campaign.

## Adopted posture

CONDITIONAL GO. Sequencing: (1) PP5.0 seam + notation manifest — ours to
draft, the cycle's last Gate-0; (2) the four-path exact toy compiler
(worker-shaped); (3) carry-compression audit; (4) then ONE route chosen
(Bellman contraction / circulant spectral / sign-reversing pairing /
lattice case compiler), not four fleets. The first contributor brief
(F2-FIRST-DESCENT-ALIGNMENT-1/3, §23) is adopted verbatim as the eventual
ask — issued only after PP5.0 certifies the 1/3 exponent closes the seam.

> **[SUPERSEDED IN PART 2026-08-01 — Pro's adversarial self-audit,
> verified on our side, RETIRES the bounded-carry/block-contraction spine
> adopted above.]** Exact fixtures: hidden modulation w = 1 + 2^(-m/6)eps
> saturates 2^(m/3) with all proper marginals flat (the 1/3 target
> demands excluding 2^(-n/6)-scale joint bias — invisible to every local
> test); full-cube parity cancels exactly yet is CONSTANT on each Hamming
> slice (sqrt(C(60,30)) ~ 2^28 — full-window theorems are useless at
> fixed b); the exact carry automaton needs ALL 2p ~ 2^32 states
> (Myhill-Nerode, no translation stabilizer) so PP5.4 becomes a
> reachable-sumset theorem request; pairings need 2^(-m/6) weight
> accuracy; chambers hit order-p complexity; order-two sector flatness
> does not transport; coherent sectors cost sqrt(s); unit drift costs
> 2^20. The 43-coordinate calibration survives only as consumer
> arithmetic, not as evidence of a local witness. SALVAGE (not minted —
> the F2 bijection is the unproven seam): diagonalize the carry exactly
> (dense odd-mode DFT, logarithmic L1), state-free product of local mode
> multipliers, balance x phase-spread contraction per mode; the new
> central black hole is the B-RESOLVED SLICE COEFFICIENT THEOREM.
> Exploratory GO for F2A.0-F2A.4 only. See
> `BRIEF5_ADVERSARIAL_AUDIT_SUMMARY.md`.
