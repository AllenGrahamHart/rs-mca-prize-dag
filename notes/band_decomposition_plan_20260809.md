# BAND DECOMPOSITION PLAN (2026-08-09, user-directed; execution at the round-27 bank)

The user directed the structural decomposition of the critical red
`rate_half_band_closure` into its two halves, matching the mystery-1
board structure. Design of record:

## Shape (three new objects, parent retained)

1. **rate_half_band_fm_model** (background supplier, initially
   TARGET): the formalized worst-word/pincer per-row first-moment
   crossing sigma_FM — the shared model both halves reference.
   Statement = the audited D1 deliverable of the round-27
   pincer_formalization pilot. Feeds BOTH children via req edges.
   Rationale: the FLOOR v2's two directions quantify over the SAME
   model; splitting without naming it would let the halves diverge.

2. **rate_half_band_structural_surplus** (critical child, TARGET):
   no structural surplus above sigma_FM at the band radii — the K3
   arm. Consumes the kb_m2_r4 workboard (existing ev edges migrate
   here as the req-side once K3's labels-to-slopes conversion is
   audited). Flips PROVED at K3-complete + the certificates' own
   required independent review. Inherits the surplus-direction
   falsifier.

3. **rate_half_band_anticoncentration** (critical child, TARGET):
   no anti-concentration failure below sigma_FM — BAND-AC.
   Statement = the audited D3 deliverable of pincer_formalization,
   with the consumer bars named per CATCH-24C. Inherits the
   collapse-direction falsifier. The round-27 evidence base
   (window law, crossing fidelity, upstream datum, the flank
   census, any Z-FLOOR transport result) attaches here.

4. **rate_half_band_closure** (parent, becomes CONDITIONAL,
   gate:all on 2+3): stays the consumer-facing node —
   adjacency_closing, list_adjacency_closing, mca_safe consume the
   CONJUNCTION (the located determination). Statement gains the
   decomposition record; all history preserved in place (sectioned
   addenda convention).

## Execution gates (why not now)

- The analytic child's statement must BE the audited BAND-AC pose
  (round-27 D3), not a paraphrase preceding it.
- pincer_formalization D0 (the audit of the consumed
  safe-side-above-sigma* machinery) can change the model node's
  content; execute after its verdict.
- If D0 returns BROKEN: STOP, re-surface to the user before any
  surgery.

## Mechanical checklist (bank-time)

- New node.json shards (write-path v12); edges as above; the
  parent's gate flipped to all-of-children; falsifier text split.
- Census repin with provenance: the counted red structure changes
  (parent TARGET -> CONDITIONAL; +2 critical TARGETs, net red
  accounting to be recomputed and recorded in board r6).
- verify chain (prize_dag + orbit_census + sectioned) green before
  commit; site + artifact refresh (orbit view WILL change);
  board r6 note in roadmap section 12; ledger entry.
- Surfaced items at execution: the exact statements of both
  children (user sees them before the flip lands, since this is a
  status-structure change).
