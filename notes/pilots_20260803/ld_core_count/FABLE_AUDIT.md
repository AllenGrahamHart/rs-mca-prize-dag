# Coordinator audit — L-D core-count pilot

**Auditor:** Fable, 2026-08-03. **Verdict: BANKED — and the pilot's
principal finding is a CORRECTION OF THE COORDINATOR'S OWN
CONSOLIDATION. Accepted in full.**

## Replay

ramguard local -> ALL CHECKS PASS (coordinator). Banked record
confirmed present: notes/pilots_20260802/xr_band_occupancy/REPORT.md
THEOREM 2 (per-slope multiplicity = punctured MDS list size; M = 1 at
2d >= h) and PROPOSITION 5 (slope-side no-go: needs |Gamma_band| <=
1.32n^2). The fiber identity RAW_d = SUM MAX_e C(k+e, k+d) and the
fixture arithmetic (334 > 272; L-filter monotonicity) hand-checked.

## Coordinator error acknowledged (recorded, not softened)

The consolidation note (ad0336d3) (a) mis-worded L-D into a FALSE
statement (raw subsets instead of maximal joint agreement sets), (b)
misapplied its own core-disjointness lemma to the raw objects, and
(c) recommended as "route of record" a chain that was executed and
TERMINATED the previous day in xr_band_occupancy — a subtraction
failure against our own notes (the fifth-surface rule exists for
exactly this). The negative finding of the consolidation (the
ray/pair lens cannot close; quantified V* boundary) SURVIVES and is
sharpened by the pilot (V* = 1.166n, factor 1.072 gap, PROP 5 no-go).
Corrections APPLIED to CONSOLIDATION.md (dated correction block).

## What the heart's open surface ACTUALLY is (post-correction)

The binding obligation lives at the PRIZE rows only (RowC vacuous for
0.68n^2) and equals the banked xr_band_occupancy terminus: an RS
list-size bound at agreement tau = k + ceil(h/2) (~1.01k, 25-50% of
Johnson) for SOME pencil member — the species of positive target #1 —
with L-A and L-B still valuable but PROVABLY insufficient (PROP 5).
The occupancy lemma cannot be closed by counting slopes or by the
dimension count; the codeword-pair side is the only route left
standing, exactly as the banked record said on 2026-08-02.
