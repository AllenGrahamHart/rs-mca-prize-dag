# Coordinator audit — unified pencil bound pilot (retry)

**Auditor:** Fable, 2026-08-03. **Verdict: BANKED — PARTIAL with exact
boundary; the anchor becomes {four proved lemmas} + banked flag F9.**

Replay: both halves, 0 falsifiers fired (coordinator). Hand-checked:
LEMMA 1's gate arithmetic (2s+1 <= 3s-1 forces s >= 2 — kills the
s=1 escape that would force C >= 1); THEOREM UPB (e=1: m+1 = 3t makes
every live triple's union exactly 3t, hence pairwise disjoint —
L <= n/t <= n/2, UNCONDITIONAL and stronger than asked: all live
slopes, no pencil hypothesis); LEMMA 3 (three shared fibres identify
the pencils); the Ann-monotonicity pinning (subfamilies keep U, so
rank only drops — realisable family => every 3+3 subfamily realisable
=> C' forces one pencil at e=1 => M <= 1). The in-run UPB-F6
retraction (loose proxy vs registered wording) is the discipline
working; the corrected PART E (0/720 realised, positive control
200/200) is the honest form. New tightness witness: the local
FULL-GATE toy pencil at q=11 (L/n = 0.500 exactly).

ADOPTED: C = 1/2 PROVED at e = 1 (all live slopes) and per pencil at
every e; across-pencils at e >= 2 reduced EXACTLY to v5_occupancy
flag F9 (does 0 < dim Ann < e occur off-pencil?) — the third
dependency reduction of the unification (after L-A => L-B and
|K|-residual = OV). The unified mystery's open kernel is now:
{SL-2, OV, F9} — three named, toy-attackable conjectures, each
paying multiple lanes. Flags honest (toy-scale confirmations;
sampled G2 direction-correct; Q1 fixture-verified).
