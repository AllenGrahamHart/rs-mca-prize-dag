# Coordinator audit — escape-1 realizability pilot

**Auditor:** Fable, 2026-08-03. **Verdict: BANKED — channel (ii)
resolved (realizable; protected by the 3-drop floor and the
admissibility filter); ONE MAJOR cross-channel catch accepted.**

## Replay + reconciliation (the audit's core)

Replay: 113 checks, 0 FAIL, 0/12 falsifiers fired (coordinator).
PREREG timestamped before first run. Reconciliations hand-verified:

- **Zfib11 vs the v5 trichotomy: CONSISTENT.** Zfib11 IS the pencil
  family at V=11 (t=2, t_0=0, h=3: k = (V-1)t - h = 17, m = t+h = 5,
  e = 2t-h = 1) with slopes = fibre parameters — Mobius-equivalent by
  the IDENTITY map — so rank = 2m - e = 9, exactly the v5
  classification. Its charge failure (0.818) sits in the trichotomy's
  2V > 2m regime. NEW content: it passes the FULL band gate and
  realises with exact-A agreements — the admissibility filter does
  NOT kill the zero-escape pencil family.
- **Zfib11 vs the U5 re-pose: NO contradiction.** Zero-escape rays
  fail U5's hypothesis, so the proved implication is untouched; what
  dies is the HOPE that full-gate admissibility forces the hypothesis
  — the heart's charge route is refuted as a universal statement.
- **THEOREM D hand-verified**: the 3-subset argument (relation
  supported in X forces three supports into a <= k-1 triple
  intersection; MDS weight kill) is exact; LEMMA B's |K| >= 4 makes
  the 3-subset available; tightness on the U-mechanism (predicts the
  recorded dim Rel = 1). LEMMA A (h >= 3) and LEMMA R (realisers
  dimension 2m - rank; rank = 2m kills exact-A liveness) — both
  hand-checked; LEMMA R is the banked T3-type consequence made an
  admissibility test, correctly credited.
- **E1 arithmetic** (A = 23 = |A_0| + 11s + 1 at s=2, pairwise k+1/
  k+2, triples k-1, all escapes 1) — checked.

## Flag adjudication

FLAG 1 (the catch) ACCEPTED and APPLIED as dated addenda (TARGET note
+ definitions addendum 5): the heart's charge ROUTE is refuted for
full-gate admissible systems in general; the column bound and the
occupancy lemma's conclusion are untouched (Zfib11's own ledger is
comfortable). The proved protections that DO survive: at prize-row
parameters every counterexample class found needs 2V > 3h (v5) or
n_1 >= 3h-2 (D1) — both astronomically false there. FLAGS 2-9 honest;
F4's evidence-not-proof and the two sharp open questions (E1-family
deficit; the full-gate oracle at m >= 9) recorded as next anchors.

## What this changes

- Channel (ii) RESOLVED as posed: escape-1 core rays exist gate-clean
  (flag 5 affirmative), but ONE such ray never defeats charge 2
  (3-drop floor), a defeat needs n_1 >= 3h-2 low-escape rays, and
  every constructed counterexample is band-INADMISSIBLE (rank = 2m).
- New floors of record: THEOREM D (3-drop; dominates the kernel floor,
  tight on U-mechanism), LEMMA R (rank <= 2m-1 necessary for
  admissibility), LEMMA A (h >= 3), LEMMA B (|K| = 0 or >= 4).
- The heart's OPEN statement must now be routed through
  ADMISSIBILITY + ROW ARITHMETIC, not the gates alone: charge >= 2
  needs V <= m, and the known defeat classes all violate prize-row
  arithmetic (2V <= 3h holds by ~1e8 there). SURFACED DECISION: the
  next coordinator analysis should consolidate exactly what remains
  open for the heart at prize-row parameters under the four new
  floors (candidate: nothing in the block class; the general-V
  admissible case via LEMMA R + THEOREM D needs one more argument).
