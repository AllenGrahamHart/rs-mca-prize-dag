# Coordinator audit — SL-1 windowed projection pilot (retry)

**Auditor:** Fable, 2026-08-03. **Verdict: BANKED — the codeword-pair
route to the occupancy lemma REOPENS. The round's decisive result.**

Replay: all four verifiers PASS (34 fixtures + 26 + 103 + 168 checks,
0 failures); the killed run's PREREG honored, post-hoc items fenced.
Hand-checked: THEOREM A's exactness (a depth-d pair's core IS its full
joint agreement set, so every non-core y has (e_y, e'_y) != 0 and
joins exactly one member — the T-CELL bijection F_q^2\{0} ->
P^1 x F_q^*); THEOREM B (q >= n makes clean members unavoidable);
THEOREM E's averaging algebra (N_raw <= avg W/(1 - beta/(q+1)),
beta < n <= q); THEOREM F's 2-adic gap (h = 2^m + 1 at all six rows,
band proper [2^{m-1}+1, 2^m - 1] contains no power of two — the
missed power IS the cascade depth); THEOREM U's identification (the
pair functional = the L-B ray functional pulled back along pi_z;
11,994 machine evaluations).

ADOPTED: SL-1 as posed is PARTIAL — "every member" FALSE (forced at
the single depth d = h-2, unconditional), "some member" TRUE
(gate-free, >= q+1-(n-k-d) clean members) — and the WINDOWED
THEOREM-2 upgrade survives on the true horn with 127.7 bits of
good-member margin at all three prize rows. MC is excluded from the
window by the three-case spectral exclusion. SL-1 and L-B are ONE
dichotomy read on opposite horns — the unification's selection leg is
now a theorem. Flags honest: parity (h odd) fragility inherited;
THEOREM F scoped to coset/structured families — the residual risk is
EXACTLY SL-2 (a non-coset family at band-proper depth inside the
window), now THE single remaining question for the occupancy lemma;
the replication subtraction (mcdepth M1/M2) was self-declared.
