# Coordinator audit — gamma j >= 2 close pilot (retry)

**Auditor:** Fable, 2026-08-03. **Verdict: BANKED — PARTIAL accepted;
heart 7 does NOT retire (per the no-premature rule); its residual
folds into the unified mystery as one named object.**

Replay: e2 (126) + e3 (33) OK; pilot total 9,415 checks, 0 failures,
0/9 falsifiers fired, PREREG pre-dated. Hand-checked: THEOREM D's
reduction (prod(T) in x0^{r'} mu_n for every r'-subset of H — so the
slope identity z = (-1)^j gamma / (prod(T) e_{j-1}(T^{-1})) confines
Gamma_j to n cosets per class of e_{j-1}(T^{-1}); THEOREM Y = the
E_1 = 1 case exactly); the scope theorem (MC-3's own constraints give
w = M at the prize rows, so j <= M-1 = w-1, while all 18 banked
counterexamples have w = 2 < M and j >= w — machine-verified 126);
the one-parameter averaging gap (only c is free; each T admits O(1)
values of c, so averaging buys ONE factor of q against the w needed —
the named obstruction to any X < 1 conditional proof).

ADOPTED: (1) THEOREM D as mintable (supersedes the j=1-only wording;
Y becomes a corollary); (2) the CORRECTION to the banked record — the
j >= 2 "inheritance from X < 1" is a CALIBRATION; X >= 1 is necessary
(min 1.045 over all 18) but NOT sufficient (prize-shape rows reach
X = 4.44 with E_j = 1; a w=2 row at X = 1.79 has none); every
gate-intact excess ever observed has w = 2; (3) the lemma re-wording
of record (band_adjudication amendment applied); (4) the advlib
docstring fix (m_1 = c/z^2, not c/z — not load-bearing, corrected);
(5) the RETARGET: the consumer gap (E_j <= 29.6n vs trivial 2^209) is
now the mu_n-coset spread of ONE symmetric function e_{j-1}(T^{-1})
over admissible T — same species as the unified structured-liveness
kernel; ATTACHED to the unified mystery as kernel item E_j (the
species identification is conjectured, flagged, not assumed). Caveats
honest (THEOREM F nearly untested — range empty at reachable j; the
j < w boundary empirical; toy scale).
