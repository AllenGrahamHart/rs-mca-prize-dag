# Coordinator audit — list-size program pilot

**Auditor:** Fable, 2026-08-03. **Verdict: BANKED — the adjudication
catch is CORRECT and is the round's most important output.**

Verified: the retirement WAS banked (KEY LEMMA node non-claim names
the refutation verbatim; list_bound_transfer R1/R2 margins 0.55-0.82h
/ 0.41h / 2^(10^11) match the pilot's independent re-derivation
exactly); all three verifiers replayed PASS. COORDINATOR ERROR
(second of its kind, recorded): CONSOLIDATION UPDATE 3 and my L-D
audit propagated a terminus refuted the previous day — the
fifth-surface subtraction failed twice on the same lane. Hand-checked:
the SHADOW LEMMA (KEY-LEMMA rank-0 branch + injectivity at d,d' >=
ceil(h/2) via k-packing d+d' <= h-1 — one inference step, correct);
the one-depth localisation (MC at h-1 only; BP parity; h odd).

ADOPTED: corrected primary surface = un-reduced two-slope band
occupancy at band-proper high depths; SL-1 (windowed projection,
unifies with L-B) = next anchor; SL-2 = the Pro-brief candidate;
"positive target #1 species" label struck. CONSOLIDATION UPDATE 4
applied. Standing rule sharpened: every next-anchor statement in a
consolidation MUST carry a same-day grep against notes/pilots_*/ for
prior refutation before being handed to a pilot.
