# Coordinator audit — overlap sliver pilot

**Auditor:** Fable, 2026-08-03. **Verdict: BANKED — the sliver is
CLOSED at the rows; ROUND 10 COMPLETE.**

Replay 33/33. Hand-checked: O1's one-line triple corollary (exact:
the (T) block form minus w_bc); O2's constant-lam forcing under the
band hypothesis; O4's determinant ((t-lam)^{V-1}(t-lam+lam V) > 0
since t-lam = h-d >= 2 — a two-line Fisher argument, self-contained);
the row table (|U| = clique_u from the recorded constants — matches
stage5_escape.json). The refutation witness's gates verified by the
pilot's machine audit; PG(2,3) sharpness accepted (classical
incidence geometry + machine check). Deza used only for the optional
O5 — flagged as external, correctly.

ADOPTED: the sliver is closed FOR THE RECORDED ROWS (V <= |U| <=
0.26n) including the L-B residual; the literal universal n/2 phrasing
of UPDATE 3 item (2) is struck (my phrasing — third wording defect
caught by a pilot this campaign; the anchor-grep rule now extends to
CONSTANT phrasings: state bounds at the rows, not universally).
CONJECTURE OV recorded as open with exhaustive toy support. The
compute-law slip (file surgery, no computation of record) recorded.

ROUND-10 NET: the ray side of the band heart is FINISHED — every
configuration class is either proved bounded at the rows, refuted-
with-fixture, or reduced to the two named survivors: SL-1 (windowed
projection, = L-B over-agreement species) and SL-2 (unstructured
high-window exclusion, Pro-brief scale) on the codeword-pair side,
plus CONJECTURE OV as a clean-up target. Decision stack for the user:
SOL_TARGET_4 reprice (task #36); SL-2 Pro brief; SL-1 pilot; PF2/PF3
+ N=256 q=769 compute requests (Modal).

## Dated addendum (2026-08-03, OV pilot): CONJECTURE OV restated
slope-free — Jperp := intersection of (W_a + W_b) over pairs = 0
(proved: Jperp = 0 => Ann = 0 for every slope tuple). This file's
3.3e12-slope-tuple evidence was gathered in the WRONG search space
(slopes; the obstruction is support-level); the right-space sweep
(8,400 point-set configs) found 0. The r = d branch is PROVED (incl.
PG(2,3)); residual = r > d. V <= |U|/2 upgrade STILL BLOCKED. See
ov_conjecture/.
