# CONJECTURE OV — pilot report (2026-08-03, Opus 5)

(Coordinator-persisted from the pilot's final message; REPORT.md write
was harness-blocked. Replay: ramguard local -> 22 checks, 0 FAIL;
OV1-OV8 + P1-P6 pre-registered, none fired.)

VERDICT: PARTIAL — OV reduced to a slope-free statement, proved in
the minimal-degree branch; no counterexample; consumers stay blocked.

THEOREM 1 (gate <-> MDS dictionary): pairwise >= k+1 <=> pair-unions
independent (|A_a u A_b| = m-d <= m-1); (T) <=> triple-unions
dependent (>= m+1); zero escape <=> the pairwise-union intersection
is empty. Explains the sibling wall: no vanishing-set argument can
close OV.

THEOREM 2 (slope-free reduction — the result): with Jperp :=
intersection over pairs of (W_a + W_b): Jperp = 0 => Ann = 0 for
EVERY slope tuple (nondegenerate annihilator's 2-plane lies in every
W_a + W_b; degenerate branch dies on zero escape). The obstruction is
support-level; the sliver's 3.3e12 slope-tuple evidence was the wrong
search space; the right space (point sets) swept 8,400 configs +
7,920 exhaustive-mod-affine tuples on MINWIT: 0 hits.

THEOREM 3 (flat form: Jperp = pairwise-k-flat functions mod RS_k;
union I_ab = U under zero escape). THEOREM 4 (degree filtration:
class invariant r = deg v - k >= d; r = d <=> e_1..e_d(I_ab) constant
across pairs). THEOREM 5 (r = d branch EMPTY for lam = 1 + uniform
multiplicity + char not dividing V-1-mu — the (V-1-mu) sum forcing;
COVERS PG(2,3), V=13, mu=4). Disjoint/overlap dichotomy: PG(2,3) and
MINWIT solution spaces = constants only (dead); the X1 disjoint
control alive (dim 5) — overlap kills the only known non-collapsing
mechanism (pencil fibres are disjoint).

RESIDUAL: the r > d dependency branch (the m vectors (e_j(B_p))_p
dependent for j >= 2); the s=1 telescoping cocycle (f_ab - f_ac =
alpha Z_abc, cocycle relations) is the named next attack. Flags: OV
stays OPEN; no status flip; consumers (sliver V <= |U|/2, cash-out
|K| close) must NOT cite this as a close; lam >= 2 sample thin (2
systems); toy scale n_U <= 15, V <= 13; no band-gate (u,v) built;
novelty subtraction done; compute law kept.
