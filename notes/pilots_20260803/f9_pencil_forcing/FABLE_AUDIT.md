# Coordinator audit — F9 pencil forcing pilot

**Auditor:** Fable, 2026-08-03. **Verdict: BANKED — T0 PROVED (the
anchor's exact statement); T2/T1 refuted constructively; C = 1/2
INTACT. The adjudication discipline worked as designed.**

Replay 39/39. Adjudication ACCEPTED on all three counts: (D1) literal
F9 = CLOSED-YES at V = 4 via W1/W2 (as the brief anticipated); (D2)
the anchor consumes T0 (no two distinct pencils each carrying >= 3
blocks), strictly weaker than the T2 the brief proposed — and T2/T1
are FALSE (18 fixtures: split-member construction; 1-rogue and
2-rogue shapes) while T0 holds; (D3) P-SHARE — distinct pencils share
<= 1 fibre, sharpened from unified's <= 2 — is NEW, PROVED, and
load-bearing (at 2 shared the pinning subfamily could be V = 4, the
no-content regime, voiding the reduction). Hand-checked: LEMMA 5
(no third block in the pencil intersection — the unconditional case
(a) kill), the case (b) cross-multiplication (gcd(zeta_i, zeta_j) = 1
forcing s_1 ~ zeta_i), and the T0 => M <= 1 => C = 1/2 chain
(a lone rogue cannot carry a second family: any 3-set through it has
2 P-blocks spanning P).

ADOPTED corrections: (1) v5 F9 closed-yes, replaced by T0; (2)
unified Q5 sharpened to <= 1; (3) la's V >= 5 non-existence evidence
VOID (random partitions structurally blind to a codim-4 condition;
its exhaustive route ran at fields too small to fit a 5th disjoint
block) — its falsifier FB fires at Delta = 0, PREDICTION P refuted;
its proved theorems replay clean on all 18 new fixtures — the
conjecture died, the theorems held. RESIDUAL of record: T0's case (b)
needs t >= e + max|Z| (unconditional for e <= 3 and t >= 2e-2); the
band t <= 2e-3 includes prize shapes (h >= 3d+3) — empirical zero
there (54 + 12 COMPLETE two-pencil sweeps incl. inside the band,
positive controls alive; dim G = 1 in all 134 non-collapsing systems
seen). P-DISJ narrowed not closed. The honest in-run E2 note (check
corrected, not the lemma) accepted.

KERNEL STATE after this pilot: F9 -> T0 PROVED-with-named-residual;
the anchor C = 1/2 stands on {UPB e=1 + T0 + P-SHARE}; kernel =
{SL-2-RES, OV, T0-residual (t <= 2e-3 band), E_j/Fourier}.
