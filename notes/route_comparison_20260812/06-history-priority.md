# Prior rulings: K3/SEM-QBC (Route 1) vs direct S/A/E (Route 2)

All paths relative to `/home/u2470931/smooth-read-solomin/prize` unless noted. SOURCE-STATED unless marked **[INFERENCE]**.

## 1. Explicit route rulings already on record

**R-1 (cycle 19, the governing ruling).** After the source-witness compiler was discharged, the recorded decision is: "the next route decision is to compare that direct route with K3 endpoint realization and pursue the shorter bankable theorem. More raw K3 workboard deletions do not address this active proof gap" (`notes/work_cycles/roadmap_r3/19-active-bc-semantic-gap-20260810.md:56-59`). Stated reason (lines 10-21): the active-v4 BC stage stores only the predicate `BAD_SLOPE_NOT_EARLIER_AND_HAS_ACTIVE_V4_BALANCED_CORE_CERTIFICATE`; an exhaustive 256-assignment two-slope audit found 31 assignments with nonempty `Z_BC` and empty endpoint set, so "the current partition contract cannot imply the K3 bridge" — a logical-independence result, not a counterexample (lines 21-24). The one Route-1 obligation that survives this ruling: "derive, rather than assume, the `Q=6,s=6,u=2` endpoint hypotheses" (lines 51-52).

**R-2 (cycle 18, refactor rationale).** The old K3 orientation leaf "conflated three different domains: active first-match bad slopes, endpoint components, and raw workboard systems" (`18-k3-bridge-and-allocation-refactor-20260810.md:9-10`). The refactor "removes two unsound shortcuts: component classification is no longer treated as a slope bridge, and the joint reserve is no longer treated as a K3-only budget" (lines 58-61). Ruling on the budget: `274980728110413983 = B*−981104` is jointly owned by `U_Q`, `U_BC`, `U_new` and "is not `U_K3_allocation`"; a red leaf must certify the three-way allocation before any K3 comparison can run (lines 45-55).

**R-3 (cycle 16, sibling-first attack ruling).** `433-1b→O0b` chosen "because it is the closest sibling of the newly closed `O0a` route and may admit an owner-preserving transport or a sharply isolated obstruction" (`16-k3-aggregate-20260810.md:46-48`). Also: raw workboard closures are "a raw-system theorem; it is not a distinct-affine-slope payment" (18-19); ledger movement none (55).

**R-4 (cycle 17, transport-over-census ruling).** "compile the exact residual partition… first… preferring an exact transport or quotient over a fresh per-system census" (`17-positive-433-repeat-bc-20260810.md:103-106`); after cell 11, "certify the duplicate-BC role transport to cell 14 rather than repeating all 1,680 labels" (210-211).

**R-5 (cycle 12, export-strategy rulings).** For the #1106 K3 export: "do not optimize another fixed-union constant. The binding red wall is the aggregation of retained unions, with the sparse branch kept separate" (`12-joint-harvest-log.md:41-43`). PR #1122 verdict: abstract incidence (pairwise/ternary/exchange/rank/circuit) "do[es] not imply cap 68"; the load-bearing successor must use GRS-span distance, actual locators/cocycle, Hilbert-Burch, "or a same-record active owner" (2754-2760).

**R-6 (cycle 119, Route-2 charge discipline).** "Do not globalize a common support or charge the complementary far stratum. The next MCA ledger using this leaf must print its first-owner order and reserve with the literal `2w` term" (`119-near-rational-supportwise-two-anchor-repair-20260811.md:44-47`).

## 2. Tried and terminated, with terminating artifacts

**Route 1 (K3/SEM-QBC):**
- Endpoint-count-only bridge assumption — terminated by the 256-assignment logical-independence audit (cycle 19:16-21).
- Component classification as a slope bridge; joint reserve as K3 budget — terminated by the cycle-18 refactor (R-2).
- Fixed-union per-chart constant optimization — terminated by #1106 supersession audit (cycle 12:36-43).
- Abstract-incidence closure of the 405-case equality wall — terminated by upstream #1122 (cycle 12:2738-2760).
- Direct six-variable saturation and monolithic outside ideals — terminated as compute fences, "not mathematical evidence" (cycle 16:163-169; cycle 17:205-209, 289-297 — 309s symbolic pilot replaced by `x=2` specialization).
- Guard-only cell-11 closure — fenced by upstream #1155's surviving guarded signed-pair point: "the residual quadratic cover must be counted or routed to an owner" (cycle 17:306-311).
- Maximal source-bound K3 attack — terminated upstream without payment (#1157 route cut; cycle 48:35: "zero prize-ledger movement"); direct carrier identification refuted (#1158); dimension shift `d1=67473` isolates SEM-QBC as the first missing bridge before `Rec_2_4` (`background/nodes/rate_half_kb_m2_r4_k3_independent_review/statement.md:29-37`).

**Route 2 (direct S/A/E):**
- The `+1` near-rational payment — falsified at deployed scale and REPAIRED by the proved `2w` theorem (#1160; cycle 119:13-31; `v13_2_near_rational_supportwise_two_anchor_payment/statement.md`).
- Zero-cost core deletion ("divide the core and declare it paid") — false by converse embedding; `thm:partial-relative` constants non-uniform past `c=4131`; fixed-core cells die at `s=3`; Jo transfer blocked by the 3765-bit multiplier (`rate_half_kb_common_core_shortening_adapter_staircase_import/statement.md`, walls 1-4; note its header: the adapter theorem is PROVED but "the chronology route-cut framing is a RECORD, not a theorem", lines 3-5).

**Not terminated** (open per R-1): Route-1 endpoint realization from the proved source-witness certificate; Route-2 chronology-correct whole-line selector for varying common cores.

## 3. Division of labor and bankability

SOURCE-STATED: `#1152` is "the living K3 export" / "living aggregate" (cycle 18:5-6; cycle 16:90-91; cycle 19:5). Scott (scottdhughes) has independently replayed two of our banked packets upstream with exact agreement — #1153 (cell-5 `xi=3` pairings) and #1157 (raw `433-1b→O0a`, 25,200 systems, zero survivors) — while his own K3 output is "route cuts only, zero ledger movement upstream" (`k3_independent_review/statement.md:19-31`). Cycles 48/65/66 pin the same: #1157/#1159 are K3 route cuts, non-overlapping, zero ledger movement (48:35; 65:9; 66:9). Cycle 12's promotion test shows upstream K3 stacks are harvested as watch-evidence only until a first-match transport exists (2728-2733).

**[INFERENCE]:** Route-1 residual algebra (workboards, cell closures) is the demonstrated upstream-bankable channel — it flows through #1152 and earns external replays that accumulate toward the `independent_review` TARGET. But R-1 caps its value: more deletions don't touch the proof gap. Route 2's interface is upstream-owned (`grande_finale.tex`, thm:partial-relative), and Scott is actively instrumenting it (#1160, #1163); a Route-2 selector theorem would be immediately upstream-bankable there, whereas we currently have no Route-2 export vehicle of our own. The fleet should treat Route-1 exports as review-credit maintenance and put new theorem effort on the R-1 comparison.

## 4. Pre-registered falsifiers / success conditions

- **Route-1 success condition (cycle 12 promotion test):** after merge, "independently replay the exact stack and prove a source-bound transport into one frozen first-match cell; then either derive its exact distinct-slope integer or an exhaustive zero residual" (12:2728-2732).
- **K3 node falsifiers (declared):** allocation — over-budget total, mismatched manifest/row/partition, unpinned input, or owner/unit/multiplicity change between payment and allocation (`k3_allocation_inequality/statement.md:24-28`); ledger — missing/overlapping owner, non-genuine pencil, uncounted line, unpaid higher-dimensional component, total above allocation (`k3_distinct_slope_budget_ledger/statement.md:33-37`); orientation — active slope outside the bridge, omitted trivial-stabilizer/source row/passport, transport changing line/support/slope/multiplicity (`k3_orientation_assembly/statement.md:29-34`).
- **Review gate:** `independent_review` stays TARGET until a record names reviewer, commits, commands, verdicts, resolved objections; the two Scott replays are explicitly PARTIAL (`statement.md:13-15,39-41`).
- **Route-2 constraints (declared):** ledgers must use the literal `2w` charge with printed first-owner order; no support globalization, no far-stratum charge (cycle 119:44-47); shortened objects must not be identified with original line/carrier/support, and reverse transport needs `Q(x)≠0` on deleted points (adapter `statement.md`, adapter clause).
- **[INFERENCE]** No pre-registered falsifier exists yet for the Route-2 whole-line selector itself; #1163's residual labels (`DIRECTION_LIST_SHORTENED_s`, `COMMON_CORE_SHORTENED_s_GE_14`) function as its open obligation set, not as a falsifier.