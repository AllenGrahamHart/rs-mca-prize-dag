# Skeptic's assessment: the direct S/A/E advocate plan

## Steelman

The plan's core claims are real: the 2w charge is PROVED and drops into any first-match subset (`v13_2_near_rational_supportwise_two_anchor_payment/statement.md:10-32`); the adapter, four walls, and paid perimeter are PROVED and replayed (`rate_half_kb_common_core_shortening_adapter_staircase_import/audit.md:20-28`); N1163 §5 SOURCE-STATES the exact selector spec and sanctions a bypass; cycle 19 gives both routes one witness substrate. R-1 legitimately orders this comparison. The falsifier ordering P3→P2→P1 is the cheapest honest kill sequence either route possesses. That is the strongest version. Now the cuts.

## Cut 1: L1 is priced as "2 packets"; it is source surgery on a zero-slack document we do not own

Verified directly: eq:owner-target is `B_owner(g) ≤ B*−31−(n−g)` with the reserve hardcoded (GF, "The exact large-owner atom target" section), and thm:conditional-final's proof removes "the exceptional set E supplied by exception routing," uses `|E|≤31` in **every** case branch, and closes "adding the reserved 31 gives exactly at most B*" (GF:7089-7110, SOURCE-STATED). MY-INFERENCE, load-bearing: 134944 > 31, so the near-rational stratum **cannot be hosted by the exception mechanism at all** — the plan's word "absorbed" is a category error. Either (i) the stratum is proved contained in an already-counted owner (a new containment theorem the plan never lists), or (ii) eq:owner-target must be re-proved as `B*−134975−(n−g)`, which means re-deriving thm:owner-localization's delivery at the tightened target for every g, plus re-checking every case of thm:conditional-final. That is a rewrite of the active source's endgame arithmetic, not a "position ruling + arithmetic replay." L1's honest depth is unknown and possibly unbounded if (ii) fails numerically — which is exactly what P3 will report, at which point 100% of the plan is stranded, not just L1.

## Cut 2: the selector is SEM-QBC in different clothes, minus K3's finished compute

Line up the obligations. WLCS needs: totality, exactly-once first-match termination, chronology-correct earlier-owner adjudication (which is **slope-global** — the selector must certify no earlier owner exists for the identical slope, precisely SEM-QBC condition (3), the "Boolean tag on one chosen BC witness is insufficient" trap, #1159 §6); a K-adjudicated domain (obstruction map §4: the d1 shift "binds BOTH"); fibers in distinct-slope units (= (FIB)); and a T3/T4 fence (= the all-Z_BC fence). MY-INFERENCE: WLCS ⊇ SEM-QBC's hard kernel; the only obligation it sheds is End_{2,4} endpoint realization — and in exchange it acquires L5, disjointness over **varying** cores, which Route 1 never faces (fixed (2,4) type). Meanwhile Route 1's banked compute (25,200 systems, cell-5/11, external replays #1153/#1157) converts on bridge completion; Route 2's banked assets are mostly *walls* — constraints on the selector, not payments. The claimed asymmetry "one theorem vs a chain" double-counts: after WLCS, T3/T4 are unbounded bins (L7 concedes this), so WLCS delivers ledger movement **0** — the same epistemic class as a proved SEM-QBC. The plan's own §1 prints `#(T3∪T4)` with no bound, while quietly citing bypass clause (d)'s "literal row-sharp slope bound" it does not produce.

## Cut 3: Scott collision, on his seam, from his unmerged branch

Verified: pr1163 carries **40 commits not on main** — the entire #1160/#1163 substrate (repairs, walls, selector spec, hostile controls) is unmerged by Przemek. The plan's packet-1 regenerates grande_finale interfaces using #1160 semantics that exist only on Scott's branch; if Przemek merges a revised form (his prerogative — the repairs touch his theorem statements), our stacked PR rebases against moving source. Worse: N1163 §7 is Scott **pre-registering the forest compiler and its falsifier as his own next move**; the plan's deliverable (i) is a note in his lineage and house style. We have the 2026-08-01 m2 export-collision precedent (memory), and history §3 concedes "we currently have no Route-2 export vehicle of our own." His track record on this seam is five consecutive route *cuts* with zero ledger movement — the base rate says the selector attempt meets a sixth cut, possibly published mid-flight under our feet. MY-INFERENCE: the expected value of 9-11 packets here must be discounted both by duplication risk and by pre-emptive refutation risk; Route 1's residue, however long, is at least uncontested ground.

## The two steps most likely to die, and the cost

1. **L1 (chronology regeneration), at probe P3.** The 31-reserve is structurally incapable of hosting a 134944-slope stratum; survival requires either a new containment theorem or re-proving owner-localization at a target tightened by ~135k — on unmerged upstream source. Cost if it dies: small sunk (≤2 packets) but **total plan stranding**, since every later lemma presumes the regenerated ledger. Cost if it "survives": the true depth balloons from 2 packets to open-ended source surgery.
2. **L5 (κ canonical-core disjointness), at probe P1.** The converse embedding makes cores non-intrinsic (N1163 §2); the falsifier (smallest selector collision) is pre-registered by its own author, which signals he expects a collision to exist. Cost: 3-5 packets sunk, pivot to WLCS-B whose depth the plan admits is unknown — i.e., the route's advertised "single missing theorem" becomes two, the second unscoped.