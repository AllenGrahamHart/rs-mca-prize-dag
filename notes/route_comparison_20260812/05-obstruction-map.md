I have everything needed. Composing the obstruction map now.

# Obstruction map: what both balanced-core routes must survive

Units throughout: distinct finite affine bad slopes per received line; only banked atom U_paid = 981104; every packet below records ledger movement = 0.

## 1. Carrier-fold cut (#1158)

**Kills:** the direct endpoint-parameter-coordinate → evaluation-carrier identification. Exact countermodel on the deployed carrier: with all 36 selected degree-two fibers split completely on D, the printed source-pencil/rational-deck identities still do not force τ(T)=1−T to preserve D — τ(D)≠D since the X^(n−1) coefficients of X^n−1 and (1−X)^n−1 differ and p∤n (SOURCE-STATED: `kb_mca_v4_k3_active_slice_semantic_bridge_route_cut_v1.md` §3, via `git show pr1163:experimental/notes/thresholds/`).
**Binds:** ROUTE 1 only — it severs endpoint-record geometry from the carrier where slopes live. Route 2 never touches endpoint coordinates.
**Leaves open:** conjugated folds — gτg⁻¹(T)=−T preserves D since −1∈D — but only with a new record-level conjugacy theorem transporting received line, support, owner, chronology, guards (§3 "Conjugacy disclosure"); and "every possible same-record adapter" is expressly not refuted (§7).

## 2. Route cut B (#1157)

**Kills:** the maximal source-bound K3 attack as composed: no source-bound membership predicate for Z_BC^(2,4), no total canonical selector from active slopes into supplied actual endpoint records, no projection-fiber bound, no all-Z_BC reduction — so raw zeros, 32,099 partitions/record, 1,575 labels, 25,200 signed systems, and the 11,304 orbit workload can never be read as slopes ("success condition B"; §Verdict, §Exact ledger consequence of `kb_mca_v4_k3_source_bound_compiler_route_cut_v1.md`). Secondarily kills 433-1b→O0b transport inside the frozen natural map class (R_BC = −1 vs +1; incidence signatures Ξ non-isomorphic under all six typed permutations).
**Binds:** ROUTE 1. (Its fail-closed unit discipline — labels ≠ slopes — constrains any route, but the missing bridge named is Route 1's.)
**Leaves open:** the selector theorem itself ("not a claim that the missing source-bound theorem is false"), stated exactly as the "maximal next theorem" with its falsifier; orientation-changing algebraic transports outside the declared map class; native O0b closure.

## 3. Guard non-transplant (#1155)

**Kills:** transplanting the 433-1a signed-pair guard product into the 433-1b tower — the cross-remainder does not vanish (SOURCE-STATED via #1157's reconciliation: `VALID_GUARD_TRANSPLANT_ROUTE_CUT_REGRESSION_DEPENDENCY`, sixteen mutation tests pass; also our `background/nodes/rate_half_kb_m2_r4_k3_independent_review/statement.md`, 2026-08-12 addendum).
**Binds:** ROUTE 1 (route-table closure machinery).
**Leaves open:** the residual **Q2 quadratic cover** is named as "the correct next obligation" (independent_review addendum); the preserved cover-bound/normalizer artifacts survive for regression; per our `roadmap_r3/16` audit, it does not conflict with our stronger independent 105-label cell-11 closure.

## 4. d1 dimension shift (#1159)

**Kills:** silent K=k ↔ K=k+1 substitution as owner semantics. An actual deployed record has d1^(k)=d1^(k+1)=67473, which is the boundary numerical profile under K=k but first-interior under K=k+1; no cited theorem identifies either profile with the frozen Q or BC predicate, so SEM-QBC is the first missing bridge before Rec_2_4 (`kb_mca_v4_k3_actual_record_source_dimension_route_cut_v1.md` §§0,4,6). It also voids our DAG's `bcCertified` shortcut: the PROVED label of `rate_half_kb_active_balanced_core_witness_compiler` defines BC by its own relation (substitution, not equivalence) and substitutes K=k+1 (§5, four reasons; matches our `roadmap_r3/19` logical-independence audit — 31 of 256 Boolean assignments give nonempty Z_BC with empty endpoint set).
**Binds:** BOTH. Route 1 needs SEM-QBC by definition; Route 2's whole-line selector must still decide which slopes are post-Q balanced cores, i.e. needs the same executable, K-adjudicated predicate. YOUR-INFERENCE on the Route-2 half: #1159 states SEM-QBC "must be uniform over every received line", which any chronology-correct selector consumes.
**Leaves open:** "a future semantics-preserving owner adapter" — sensitivity shows it needs proof, not that it fails (§4); the upstream order-32 theorem itself is untouched (§5).

## 5. +1 near-rational falsifier (#1160)

**Kills:** the printed one-slope near-rational reduction and every global-common-support deletion shortcut: an actual deployed line has ≥ 67,472 near-rational support-wise bad slopes with empty balanced set (`kb_mca_supportwise_near_rational_two_anchor_repair_v1.md` §1; our node `v13_2_near_rational_supportwise_two_anchor_payment`, `roadmap_r3/119`).
**Binds:** BOTH — explicitly "a mandatory regression for any SEM-QBC adapter" (Route 1) and, for Route 2, the S/A/E "reserve and interfaces must be updated rather than pretending this stratum costs one slope" (§§3,5).
**Leaves open:** the PROVED replacement 2w = 134,944 charge in genuine slope units, uniform over guarded RS domains — an instrument, not just a cut; and thm:partial-relative remains applicable once its common-factor branch is routed inside frontier (S) (§5).

## 6. Converse embedding (#1163)

**Kills:** zero-cost core deletion: inverse-lifting shows uniform common-core routing contains the complete compatible staircase of shortened RS-MCA problems — "divide the core and declare it paid is false" (`kb_mca_v4_common_core_shortening_staircase_route_cut_v1.md` §2 converse; our import node `rate_half_kb_common_core_shortening_adapter_staircase_import/result.md`).
**Binds:** ROUTE 2 (self-similarity hardness of its own staircase).
**Leaves open:** the typed reversible adapter itself is PROVED; fixed-family terminals genuinely pay when globally affine, s≤2, or direction-separated 3≤s≤13 (walls: c=4131 degree-interface drop; J_13 < B_* < J_14); residual labels DIRECTION_LIST_SHORTENED_s / COMMON_CORE_SHORTENED_s_GE_14; and "an alternative same-owner maximum-type theorem could bypass the fixed-core forest entirely" (§5).

## 7. 3765-bit Jo wall (#1163 §4)

**Kills:** paying uncovered cores through Jo's slope-preserving shortening transfer: at the first degree-drop core c=4131, binom(n,c)/binom(m,c) has a 3765-bit ceiling (> B_* before any numerator); staging telescopes, so splitting cannot help.
**Binds:** ROUTE 2.
**Leaves open:** explicitly "a budget obstruction to that theorem, not a proof that every possible common-core compiler must fail."

## 8. The eleven unresolved routes (#1157 table)

**Kills:** any claim that the positive-coordinate K3 composition is done. Of thirteen routes, only 433-1a→O0b and 433-1b→O0a are raw-zero (still null slope payment); 442-0a→{O0b,O1b,O1d}, 442-1b→{O0a,O0b}, 433-0→{O0a,O0b,O1b,O1c,O1d}, 433-1b→O0b carry no literal integer, and "the generic one-pencil cap does not pay a union of undeclared pencils."
**Binds:** ROUTE 1 exclusively — a workload that survives even after SEM-QBC and Rec_2_4, alongside orientation assembly's four live diagonal rows × two passports = eight open combinations and undefined U_sourcecover.
**Leaves open:** native O0b algebra "may still close or pay it"; the two raw zeros transport the moment the selector exists.

## Asymmetric obstructions — what decides the comparison

- **Route-1-only:** carrier-fold cut, route cut B's selector/fiber/fence gap, guard non-transplant (Q2 pending), the eleven routes + eight orientation combos + allocation inequality + independent review (our K3 critical sub-DAG, `roadmap_r3/16`). Route 1's residue is a long conjunctive chain of unproved bridges *plus* an unfinished census.
- **Route-2-only:** converse embedding and the Jo wall. Route 2's residue is exactly one missing theorem (the chronology-correct whole-line selector) plus two honest residual labels — and its instruments (2w charge, reversible adapter, exact walls) are already PROVED.
- **Symmetric:** the d1 shift/SEM-QBC semantic gap and the +1 falsifier regression bind both, so neither discriminates.

YOUR-INFERENCE: the comparison therefore turns on whether Route 2's single selector can survive the converse-embedding self-similarity (its staircase contains the whole shortened problem family) better than Route 1 can survive four independent cuts and a census; #1163 §5's sanctioned bypass — a new maximum-type theorem avoiding the fixed-core forest — is the one freedom that would let Route 2 dodge its only exclusive obstructions, while Route 1 has no analogous single-theorem escape.