# K3-route skeptic report

**Steelman.** The plan is the honest maximum: it concedes SEM-QBC is a definition+theorem package, correctly names T5 hardest, pre-registers cheap falsifiers, and its downstream chain really is wired so the 25,200-system/cell-5/cell-11 zeros convert with no re-derivation. If everything closes, it delivers the first nonzero-information ledger movement (#1157's success condition B). Now the cuts.

**Cut 1 — the reposed (COV) deletes the slice and dumps the cost into an unpriced L6/allocation coupling.** #1157's "maximal next theorem" demands the membership predicate for `Z_BC^(m=2,r=4)` **"and its claimed scope within Z_BC"** (route-cut file, pr1163, lines 405–406, SOURCE-STATED). The advocate's repose proves π_γ{P_BC} equals the whole-BC complement — it never defines the (2,4) slice at all, so the scope obligation has **no packet**. Everything not in the slice must exit through the fence "to an earlier owner, a separately paid component, or explicit `U_new`" (#1158 line 172). Priced at ≈1 packet, but that fence is slope-domain routing over ALL of Z_BC — exactly what R-2 forbids inferring from the order-two trichotomy — i.e. it secretly contains orientation assembly's open surface (eight row×passport combos, undefined U_sourcecover). Worse: any nonzero U_new mass the fence emits flows into the allocation-definition TARGET, which needs **exact sibling U_Q and U_new atoms**; the joint floor 57197049262 cannot isolate U_BC (cycle 18:54–55), and no U_Q census instrument exists anywhere. "Allocation ≈2–3 packets" assumes atoms nobody has started.

**Cut 2 — the claimed T6 reuse is a construction the source explicitly declares unsafe.** The plan reuses cycle-19's "unit fibers by lex minimization" (19-…:47–48). #1158 line 180 (SOURCE-STATED): "witness-first lexicographic selector is unsafe because another witness for the same slope may trigger Q before BC." So the one proved selector on hand is precisely the class L3 must defeat; T6 cannot inherit it until L3 is proved with a **different** selector taken post-realization. Also L2→L3→D2 is a dependency chain, not siblings: P_Q lives at K=k, P_BC at K=k+1, and #1159's boundary record (d1=67473) collides the frames on one actual record. If L2 dies (probe 1), D2 — the plan's flagship reuse, defined at K=k+1 — must be rebuilt at K=k, and #1159's "uniform over every received line" quantifier (line 10) must be re-established for the rebuilt relation. The compiler's PROVED label does not survive an L2 death.

**Cut 3 — the strongest single reason the route is longer than it looks: T5 re-imports the whole-line chronology problem Route 1 was supposed to avoid.** Direct coordinate identification is refuted (#1158 §3); the only surviving path is the conjugated fold, which needs a new theorem transporting "received line, support, explaining data, **owner, chronology**, and all record guards." Owner and chronology are whole-line, first-match properties — so T5's transport theorem carries the same chronology-correctness burden as Route 2's selector, plus endpoint scaffolding on top. #1130 doesn't help: it runs "per **supplied** actual endpoint record" — it consumes realizations, it cannot produce them. And critically: **the three cheap probes target L2, L3, D2 — none touches T5.** The hardest step has no pre-registered falsifier and dies late, after L1–L6 are sunk.

**Cut 4 — "downstream conversions" are not risk-free.** Ten of the eleven routes carry no literal integer; the closest sibling (433-1b→O0b) already failed transport within the natural map class (R_BC sign, Ξ non-isomorphic) and its residual alone is 408 rows/42,840 labels that cycle 17 explicitly "does not promote." Batching ten routes into 4–6 packets extrapolates from the one route that took multiple cycles at 25,200-system scale. Independent review is priced at 1–2 packets but must cover the **whole** load-bearing sub-DAG (statement.md:1–15), which every underestimate above inflates.

**Honest depth re-estimate [MY-INFERENCE]:** 20–26 becomes ~30–40 once the slice-scope theorem, U_Q/U_new atoms, T5's chronology transport, and realistic route censuses are priced — with the novel-math risk no longer "L2, L3, T5" but "L2, L3, T5, L6-scope, allocation-atoms."

**Two steps most likely to die:**

1. **T5 (endpoint realization).** Logical independence is proved (31/256 assignments), the direct path is refuted, and the sole escape needs an unwritten chronology-transport theorem with no cheap probe. Death arrives **late**: ~9–12 bridge packets sunk, plus any eleven-route/orientation packets started in parallel — realistically **12–18 wasted packets**, with only the witness substrate salvageable for Route 2.
2. **L6-scope + allocation coupling.** If the fence cannot route non-(2,4) BC mass without a nonzero, unboundable U_new — and no U_Q instrument exists — the ledger comparison is unposeable even with a proved bridge. Death strands every converted payment packet (eleven-route 4–6, orientation 3, assembly 1): **8–10 wasted packets**, discovered only at the final allocation step because the plan schedules no early U_Q/U_new probe.

Run probes 1–3, then demand a T5 transport-theorem sketch and a U_Q-atom feasibility note **before** funding any downstream conversion.