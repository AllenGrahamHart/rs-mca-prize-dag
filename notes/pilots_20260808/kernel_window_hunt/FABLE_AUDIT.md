# FABLE_AUDIT — kernel_window_hunt (round 24, agent 3 of 4)

**Verdict: BANKED, MAINTAINER-LEVEL — THE FLEET'S DECISIVE RESULT:
the family-uniform emptiness conjecture is FALSE, by an exhibited
witness at N' = 128 with a BLS-PROVEN 247-bit prime (REPRO PASS on
coordinator replay: Norm(w) = P exactly, ||w||_1 = 127 <= 2l',
non-cyclotomic ternary lift in K_P), 20,636 further W_TOP hits —
AND the repo already held the N' = 256 falsifiers since July,
mis-filed behind a prize-interval filter (CATCH-24C applied). The
pilot's calibration discipline held (C1-C4 against round-22 ground
truth before any h = 64 claim), its witness protocol held, and its
novelty subtraction SELF-AMENDED when its own prior-art sweep
returned: two claims retracted (the N' = 256 exhibits and the
forward-direction method are prior art; NORMLAW-128 is routine),
the headline confirmed free (the N' = 128 exhibit + the stated
conclusion + the v_2 refinement). The deployed rows are PROTECTED:
v_2(p-1) = 7 generic vs 92-200 on every pinned row — the narrowing
option this makes defensible is real.**

Applied: the board event on integer_code_distance_cert (the
uniform branch closed; three narrowing options SURFACED to the
user); the GE-WEAK universal-reading refutation on
generator_economy; CATCH-24C on the e1_n256 audit node.
REPORT.md persisted (3 parts incl. the amendment).

**CATCH-24B (the coordinator's own, disclosed):** the agents-1+2
bank commit used a broad `git add notes/pilots_20260808`, sweeping
THIS live pilot's in-progress state into git history mid-run (no
modification, no harm — but the explicit-path rule existed and was
violated). RULE RESTATED: explicit paths only while pilots run.

NEXT (user decision pending): choose (a) exhibit-scoped closure +
consumer narrowing, (b) o(1)-sparsity re-pose, or (c) the
large-v_2 restriction. The v_2 dichotomy makes (c) the natural
candidate; (b) is compatible with all measurements.
