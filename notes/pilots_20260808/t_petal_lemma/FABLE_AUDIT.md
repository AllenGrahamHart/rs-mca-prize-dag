# FABLE_AUDIT — t_petal_lemma (round 24, agent 1 of 4)

**Verdict: BANKED, MAINTAINER-LEVEL — the board's highest-leverage
lemma was ALREADY PROVED in our own repo (clause (JB3) of
l1_fixed_support_defect_johnson_bound at arbitrary support size),
hidden under six names while three round-23 artifacts recorded it
as missing. The pilot registered the reframing BEFORE computing,
wrote the self-contained general-t proof (5 elementary steps —
degree counting, no syzygy budget; coordinator-read and verified),
ran the refutation search to completion anyway (0 violations,
MIN_SLACK = 0 — the cap is ATTAINED so the search has resolution;
power control fired 2/3 broken arms), and executed the payoff: the
J-sieve is legal at every t, the 156 illegally-paid rows are now
legal, residual 408 row-for-row identical. TWO NEW RESULTS beyond
mandate, both machine-checked and drafted for minting: the FREE
disjointness hypothesis (primitivity forbids petal roots) and THE
SLICE-DIMENSION THEOREM dim V = e+1 at every t — which makes red
3's t >= 4 rows POSABLE AS FLATS: mystery-7 membership is now
DECIDABLE.**

Replays: the JB node verifier (banked output reproduced exactly);
tpetal_dim (C_KER/C_DIM ok); tpetal_payoff (674/408/156
reproduced, the verbatim-sieve assertion held); tpetal_refute main
seed (0 violations, MIN_SLACK 0). REPORT.md persisted (task
a4fb31b26cd8c4da6). Quiescence checked before banking (CATCH-23B
rule).

Applied: the FORCED CORRECTION on l1_fpc5_large_source_payment
(round-23 claim (i) false in both clauses); the identification +
two extension drafts recorded on the JB node (mint candidates).
Honest ledger accepted: registered danger C7 became a theorem;
BRK-DISJ prediction failed FOR A PROVED REASON; the CJ3 probe's
first run wrong and corrected (67.4% -> 1.97%); the mandate
reframing registered pre-compute. STANDING RULE (CATCH-24A): hard
law 5's own-repo grep GATES every "named gate"/"missing lemma"
before it is priced — naming variance hid a proved theorem from
three consecutive rounds. NEXT: audit CJ2/CJ3's chart hypotheses
at M >= 5 (would rescue 71 rows); re-run the 23b membership test
on red 3's newly posable rows; mint the slice-dimension theorem.
