# FABLE_AUDIT — z_ceiling_assault (round 24, agent 2 of 4)

**Verdict: BANKED, MAINTAINER-LEVEL — Z-CEILING SURVIVES its worst
directions (59,203 exact cells, registered falsifier untripped) and
comes out repriced, scoped, and structurally understood: the
constant of record rises to >= 1.7681 (record cell TMASS = 159/64
exact, 3-way verified; headroom 4.44 -> 3.95 bits); the sigma ->
-inf direction is PROVABLY safe (the identity re-aim registered
before computing); THEOREM RC (proved, 20/0 replayed) shows each
N-line's sup is a finite max and kills every admissible p >
N^{N/2}; the load-bearing hypotheses are now sharp ("admissible"
pinned — the general-subspace form is FALSE at CRATIO 25.23; the
2-power gate = Res(Phi_2N, f) != 0, with the composite blowup
(5/4)^{L/3} in closed form and a composite cell crossing 2). THE
HONEST PEAK (S2): Z-CEILING is proved EQUIVALENT to the node's
named non-local smoothness-mass input — a faithful restatement,
not a weaker stepping stone; the ensemble form is proved and
inert. Mystery 2's candidate closure stands, better understood
and slightly more expensive.**

Replays: rc.py 20/0; addendum.py (the CZ-M count refutation at
n = 30 reproduced FAIL-exactly; one-odd-prime cells PASS; the
composite exhibit + record arithmetic in the suite). REPORT.md
persisted (task a0bc32d723fc69262).

Applied: the round-24 update on f2_z1_mass_knife_edge (repriced
constant, RC, the scope pin, the equivalence finding, the named
N = 32 sigma~0 next computation); CATCH-Z24-A forced correction on
tern_master_threshold (the count formula false at two-odd-prime n;
the spine re-confirmed). Honest ledger accepted: P4b/P4c growth-law
tests FAILED as registered and reported (the model is an upper
bound, not a description); the all-ones "unbounded in p"
mis-registration self-corrected (saturates in p, unbounded in N);
CATCH-Z24-B (its own lower-bound-vs-count error) self-caught and
recomputed exhaustively. NEXT: the N = 32 sigma in [-2,2] band
(new algorithm or Modal); the Lambda-relaxation band (1.5784 at
non-consecutive Lambda) as the constant's next pressure point.
