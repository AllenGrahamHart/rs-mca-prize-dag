# FLOOR CAMPAIGN (2026-07-07, user strategy): every critical red becomes its weakest-form conjecture, then survives or dies

## The strategy (user-set)

For each of the 7 critical reds: (1) derive the WEAKEST FORM — the exact
statement its consumer(s) actually consume, pinned from the consumer's
packet arithmetic, no route structure; (2) re-pose the node as that floor
conjecture, standing until refuted; (3) PRE-REGISTER the falsifier before
collecting data (what counts, what does not — proxy/route failures never
count); (4) attack aggressively: numerics (Modal) + Pro briefs; (5) a node
that DIES forces early downstream re-thinking (better now than after
months of corridor work on a false region); (6) survivors — hard
conjectures resisting both proof and falsification — are THE TRUE
FRONTIER.

Template (established at dli, REPOSE_B_WEAK.md): pin the consumption from
the consumer's packet verbatim → weakest joint/exceptional form → evidence
ledger opens with the node's existing survival record → pre-registered
refutation standard with scaled-toy transport rules → routes to the
evidence lane; promotion back to conditional only after surviving
adversarial rounds (F-round rule).

## The 7 reds and their consumers

| # | node | consumer(s) | floor status |
|---|---|---|---|
| 1 | dli_prime_weighted_large_block_support | x4_exactlist_staircase_split | **DONE — B-WEAK; survival +1** (joint scaling study; quotient-staircase dressing classified) |
| 2 | u2c_giant_tnull_dichotomy | x4 | survey in progress |
| 3 | u1_x4_direct_column_budget | x4 | survey in progress |
| 4 | petal_growth | imgfib | survey in progress |
| 5 | xr_smallcore_spread_count | xr_clean_residual_any_gate | survey in progress |
| 6 | rate_half_band_closure | adjacency_closing + list_adjacency_closing + mca_safe | survey in progress |
| 7 | worst_word_challenger_pricing | worst_word_planted + list_planted_arithmetic | survey in progress |

Note: nodes 2, 3 share x4 with dli — their floors compose with B-WEAK
into x4's full wired-hypothesis set; deriving all three floors from ONE
packet read keeps the interfaces consistent. Node 6 has THREE consumers —
its floor is the WEAKEST statement satisfying all three simultaneously
(or, if the three consume genuinely different projections, possibly a
SPLIT into per-consumer floors, which would itself be a discovery).

## Program discipline

- One node at a time; each re-pose banked with its own REPOSE note +
  pre-registration + dag statement + ritual before the next begins.
- Attack phase per node: at least one direct numeric experiment (Modal)
  aimed at the floor's most refutable projection + a Pro brief when the
  node's object suits Pro's strengths.
- Survival ledger per node in the dag statement (+1 per absorbed attack).
- A death is a CAMPAIGN EVENT: stop, write the downstream-consequence
  analysis (which ambers/chains above are invalidated, what re-routes),
  and report to the user before continuing.

## Log

- 2026-07-07: campaign opened. dli already treated (template). Two survey
  agents dispatched for the remaining six floors.

## THE SIX DERIVED FLOORS (2026-07-07, from the banked surveys — verbatim consumption quotes in notes/floor_campaign/)

**F2. u2c_giant_tnull_dichotomy → EXTRAS-BUDGET floor.** Consumed (b2_modp_giant_extras): "at official
prize-max rows: #{non-coset-union t-null blocks + trade families} <= n^3 = 2^123". The repaired dichotomy
(ZERO accidents at sub-balance rows) is a strengthening — the consumer tolerates 2^123 extras. FLOOR: the
extras COUNT bound at official rows. Pre-reg falsifier: sub-balance scaled rows with extras exceeding the
transported budget, sustained across scales; above-balance window accidents (the known witness family) do
NOT count. Opening evidence: prize rows sub-balance by ~2% (window empty at every b); boundary scan 192->64->0.

**F3. u1_x4_direct_column_budget → already the floor.** Conjecture framing + pre-registration only.
Consumed: <= n^3 fully-stripped primitive residue at official corridor rows (consumer sufficiency proved at
16n^3). CORRECTION to bank: x4's statement.md prose still prints the stale n^2 target and the old
u1_pullback premise — flag, don't silently rely. Opening evidence: probe PASS at 1.5% of budget, positive
control detected.

**F4. petal_growth → PETAL-ESCAPE-BUDGET floor.** Consumed (imgfib): "For ALL received words U:
#ImgFib_U(k+sigma) <= n^B once sigma log2(q_D) >= (1+eps) log2 C(n,k+sigma)" — petal_growth is the only
open branch (the petal escape route). FLOOR: at the official rows, for every U, the full-petal (top-defect
band d >= M(t-2)) contribution to #ImgFib_U fits the n^B budget. NO uniformity-in-c and NO per-c polynomial
claim (route). Pre-reg falsifier: scaled official-like rows with a super-budget top-band petal family,
sustained; below-top behavior is already Lemma-13-clean (76-row scan).

**F5. xr_smallcore_spread_count → already sharpened to the floor (2026-07-06).** Consumed: per-pair
R_post(u,v;A) <= 16 n^3 post-strips at the official candidates (sufficiency = exact integer arithmetic at
all six). Conjecture framing + pre-registration: E27-pencil search for a super-16n^3 pair at scaled rows;
FM-band-consistent remainder counts do NOT refute (inside budget with ~2^100 slack). Opening evidence: E27
remainder at FM-normalized rates, all classifiers at chance, no fifth mechanism.

**F6. rate_half_band_closure → BAND-SAFETY floor (the most consequential re-pose).** The node currently
DEMANDS a mechanism ("cover the band by a genuinely new priced mechanism") — that demand is route, not
obligation. Consumed (identically by adjacency_closing, list_adjacency_closing, mca_safe): the rate-1/2
determination has NO exception in the band, i.e. every radius sigma in (2^33, 8,592,912,738] at every
admissible rate-1/2 row is safe-side. FLOOR: that truth claim, mechanism-free. The RAZOR narrows it: the
band is nonempty only for log2 q in (255.900, 256) — the floor family covers everything below. Pre-reg
falsifier: exact row arithmetic exhibiting one band radius at one admissible top-slice row that lands
unsafe/bracket-grade (the corridor machinery computes this; a genuine death here re-routes mca_safe — a
campaign event of the first order). Opening evidence: rates 1/4, 1/8, 1/16 clean by < -121; both refuted
routes (AQB, dihedral sibling) failed as MECHANISMS, not as safety counterexamples.

**F7. worst_word_challenger_pricing → ROWWISE-ENVELOPE floor.** Consumed (list_planted_arithmetic, weakened
2026-07-06): "row by row, a certified upper envelope for every non-extremal/non-planted contribution and
explicit lower witnesses for unsafe rows" — NOT global two-class exhaustion. Exhaustion at official cells is
already PROVED (Lemma A covers bg <= 1 = the official max-fill convention). FLOOR: at every official row,
the non-planted low-slack contribution obeys the certified envelope consistent with the verified count law
~ K_cell/q^sigma. Pre-reg falsifier: an official-convention (bg <= 1) row with challenger count exceeding
the envelope, sustained across the row family; open-window (small-q) counts do NOT refute (window law:
sigma=2 window closes by q=97). Opening evidence: two censuses UNCLASSIFIED=0 (77 + 219 cells).

## Attack-phase priority (threat-ordered)

1. F6 rate_half BAND-SAFETY — three consumers incl. mca_safe; the razor makes the numeric attack concrete
   (top-0.1-bit rows, band radii, exact corridor arithmetic). A death here matters most.
2. F2 u2c EXTRAS-BUDGET — the entropic-suppression question is dli-shaped; the boundary-scan machinery
   extends directly (Modal sweeps toward the sub-balance boundary).
3. F7 worst_word ROWWISE-ENVELOPE — e22 census machinery ready (pre-registered Modal script exists).
4. F4 petal PETAL-ESCAPE-BUDGET — extend petal_excess_local_scan to top-band scaled rows.
5. F5 xr smallcore — E27 pencil search at scaled pairs.
6. F3 u1_x4 — probe already PASS at 1.5% of budget; lowest immediate threat; Pro brief candidate.

## CORRECTION (2026-07-07, attack phase opening): F6 floor v1 was MIS-SIGNED — caught before any attack ran

F6 v1 said "every band radius is safe-side". The primary docs (P6 brief) show the band is the UNDETERMINED
stretch between the proved deficit side (reach 2^33) and the proved safe side (above sigma*); conjecturing
it all-safe guessed a side without evidence. F6 v2 (banked): the band determination EQUALS the first-moment
prediction (window law at the band radii); falsifier = scaled band-analogue counts deviating from the model,
either direction, >= 3 scales. LESSON (campaign discipline instance #2, after the C1 freeze): floors derived
from survey compressions must be checked against primary docs before attack — now a standing protocol step.
