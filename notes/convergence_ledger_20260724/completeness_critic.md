# TASK C3 — COMPLETENESS CRITIQUE OF B1 + B2

Verification basis: ours @ prize master 8a820bdb (`notes/PRIZE_RESOLUTION_ROADMAP.md` §§2, 5, 8–10), upstream @ b13de811 (`agents.md` §§6–7, `grande_finale.tex` thm:audited-status at :7530, `experimental/lean/` tree). Both plan texts arrived truncated (B1 cut mid-E-6, B2 cut mid-H5); findings below marked (T) could be cured by the missing tails, all others are structural and cannot be.

## VERDICT IN ONE LINE

B1 and B2 are excellent *flow* plans — a ranked export queue and a ranked harvest queue — but the maintainer's frame asks for a *convergence* plan, and neither contains the four things convergence requires: a joint finish-line ledger, a conditional-discharge story, a scoped Lean start, and collaboration governance. As written, executing both plans perfectly yields a better-fed exchange pipeline and an essentially unchanged red/conditional count: piecemeal exchange, not "both finish together."

## 1. "FINISH TOGETHER, ALL-GREEN" — NOT DELIVERED

**1a. No definition of DONE, no burn-down accounting.** All-green means: 24 TARGETs closed AND 38 CONDITIONALs *discharged* AND his four terminal open inputs (thm:audited-status names exactly `prob:mca-spread-routing`, `prob:large-owner`, `prob:mca-exception-routing`, `prob:list-completion`) closed. Neither plan states this, and neither computes its own expected DAG delta. Doing so is damning: B1's exports close **zero** of our reds by construction (they are outbound); B2's harvests are almost all **ev**-wired ("req only after chamber coordinates are bound" — stated for H1 only). Expected end-state after both plans execute fully: reds 24 → ~24, conditionals 38 → 38. **Addition C3-1:** append to each plan a burn-down table (item → which red/conditional it closes, routes, or merely evidences), and adopt the roadmap §9 metric — "bits of remaining delta*-bracket, jointly over both trees" — as the per-wave progress report. Any wave that moves zero joint bits is exchange, not convergence, by our own standing metric.

**1b. His four terminal inputs are not used as a checklist.** B2 names (S)/(A)/(E)/list-completion in its baseline, then never returns to them. Visible coverage: list-completion ↔ H1 (real, the best item in either plan); (S) spread-routing ↔ H3→`xr_lowcore_spread_heart` (ev only); **(A) large-owner and (E) exception-routing ↔ nothing** (T). **Addition C3-2:** a 4-row coverage table for his inputs and a matching 24-row table for our TARGETs; every uncovered row is either explicitly assigned "no route this cycle — reason" or generates a new item. Silence is what the current plans do instead.

**1c. Conditional discharge is entirely absent.** 38 CONDITIONAL nodes are the second-largest non-green mass, and "all-green" fails on them exactly as on reds. The obvious convergence move — dedupe our conditionals' assumption set against his six grande_finale inputs so shared hypotheses are counted once and attacked once — appears nowhere. **Addition C3-3:** a one-session audit producing `assumption_dedup.md`: every distinct assumption behind the 38, mapped to his (F1)–(F4)/six-inputs where identical, each with owner + route + falsifier. This is also the *only* way the final DAG can be "the dependency graph of the joint proof": conditional edges must terminate in jointly-recognized hypotheses, not two private hypothesis vocabularies.

**1d. The DAG itself is never shown to him.** The frame says the critical DAG *IS* the joint dependency graph — but it lives in our repo; both plans exchange theorems while the map stays unilateral. Nothing exports the critical-orbit projection (with the crosswalk's his-label ↔ our-node dictionary) as an upstream artifact, and nothing even confirms he has the public prize-dag site. Two people cannot finish together on a map one of them cannot see. **Addition C3-4:** a small standing export — `critical_orbit_snapshot.md` (nodes, status, req edges, his-label crosswalk column), regenerated per wave, offered either as an upstream `experimental/notes/` file or a pinned link. Caution: his stop-rules list "structural JSON/schema acceptance" and "theorem-label maps and clean rewrites" as non-progress — so frame it as *the shared status board accompanying real packets*, never as a standalone PR.

## 2. LEAN — NOT CONCRETE ENOUGH TO START, AND NEITHER PLAN MENTIONS IT AT ALL

This is the cleanest miss: the frame's stated endpoint is "ready for Lean formalization (DAG nodes → Lean modules)" and the word "Lean" does not occur in either visible plan (T, but B1's structure gives no slot for it).

Constraints already on the record: upstream has a large working Lean estate (dozens of packages under `experimental/lean/` — toolchain exists, conventions exist) and an explicit Lean policy (agents.md §7): high-value = "proved local theorems used by live atoms, first-match/add-back kernels, endpoint/integer conversion, final row certificates, counterexample correspondence"; explicitly *banned*: Lean stubs, axiomatized global conjectures, end-to-end GF-v4 formalization while inputs are open. So the Lean story must be scoped-pilot, not grand.

**Addition C3-5 (pilot):** attach a Lean 4 formalization to B1's E-1/E-2 — Pocklington/Proth certificate verification + exact budget `floor(q·2^-128)` endpoint conversion. This is squarely two of his five named high-value genres ("final row certificates", "endpoint/integer conversion"), it rides packets already at the head of the export queue, it reuses his in-repo toolchain, and it is the cheapest end-to-end test of the nodes→modules pipeline on content *both* sides have blessed.
**Addition C3-6 (readiness audit):** a `lean_ready` classification pass over our 213 PROVED nodes: statement frozen in finitary/exact-integer form (formalizable) vs prose/sketch-level (not). The proof-sketch-loop heritage means a nontrivial fraction of the orbit is prose; knowing the ratio is prerequisite to any nodes→modules promise. Output: one field in dag.json plus a count in the roadmap.
**Addition C3-7 (conventions):** a half-page nodes→modules convention note agreed with Przemek *before* volume: namespace scheme (node id → module name), statement-freezing rule (the DAG statement text is the spec; Lean statement diffs are catches), and the double-key rule from roadmap §8 (his triage + our execution re-pin) extended to "builds-green ≠ statement-matched" per his own certification rule.

## 3. THE TWO WALLS — ONE TOUCHED IN PASSING, ONE SILENTLY SKIPPED

**Wall 1 (his input 3 == our F2 summit `f2_growing_order_myerson`).** B2 does touch it: R4 imports the Q-moment order floor as a route-cut, and H5 imports the GF Part III Sidon/Fourier chain as ev to the summit. Both are inbound garnish. What's missing is the *outbound and joint* half: (i) our two PROVED identifications (Q-atom == F2 zero-prefix Myerson chain; L1 scalar-cofactor top shell == locator-Q fiber, roadmap §2) are not in the export queue — until they land upstream, *he does not know the walls are shared*, and the two trees can close/attack one object twice in incompatible formulations; (ii) the roadmap's own mechanism — U3 adjudication via the DSP8 Sidon-strip harness → "propose the JOINT BRIEF on input 3," flagged at §10 as awaiting maintainer authorization — appears in neither plan, so the only recorded route onto the wall from either tree (roadmap :593) has no plan item. **Addition C3-8:** an export packet "cross-tree identification certificates" (the two machine-verified chains, stated with his labels, fenced "identifies objects; strengthens no bound"); **Addition C3-9:** a named plan slot "run the DSP8 Sidon-strip harness → if residual ≤ 24, trigger the joint-brief authorization ask" with its precondition and owner.

**Wall 2 (the balance line: our 123.17 bits from below vs 127.90+ needed, all 2,978,146 band cells; his ~1.66M-bit packing overhead from above).** **Silently skipped — confirmed.** The nearest item is B2-H4's thm:f17 as an ev calibration point for `rate_half_band_closure`'s falsifier family; nothing in either plan attacks, mines, or even *ledgers* the wall. Yet the regime-complementarity ("zero overlap, zero contradiction") is itself the highest-value exportable observation on this front, and there is an obvious unexplored mining question: does his packing-ceiling machinery, *restricted to our 2,978,146 band cells*, lose fewer than 1.66M bits — i.e., can his upper-side technology be transported through the pincer to shave our 4.73–4.83-bit deficit? Nobody on either side has recorded trying. **Addition C3-10:** (a) an export "balance-line pincer ledger" (exactly which cells each side's bound covers, the joint residual stated as a co-owned open problem — this is also partial-credit posture insurance per roadmap §8); (b) a mining item "restrict his overhead bound to the band" with a pre-registered falsifier; (c) an explicit statement in the plan if the wall is being *deliberately* deferred — deferral is legitimate, silence is not, because under the frame an unaddressed wall means "finish together" is impossible at rate-1/2 regardless of how well everything else goes.

## 4. GOVERNANCE — FOUR MISSING PIECES, THREE OF THEM ALREADY ON OUR OWN DECISION QUEUE

Roadmap §10 already lists "PR pacing ratification," "joint-brief authorization on input 3," and "the co-authorship/credit conversation (anchored to input 6, raised at the first declared milestone)" as pending outward decisions. B1/B2 carry none of them forward.

**4a. Pacing vs "very proactive" — unresolved contradiction.** B1's pacing rule (≤2 open PRs, never same lane, serialize behind triage) is distilled from *observed old mechanics*; the maintainer's *new* frame says very proactive. B1 silently resolves the tension in favor of the old rule. That may be right — but it's a decision the maintainer flagged for ratification, not for us to default. **Addition C3-11:** make the first outbound communication a short protocol proposal: keep the 2-PR cap OR move to batched waves with a standing status board (C3-4) between waves; let him choose. This costs one note and de-risks the entire B1 schedule.

**4b. Triage latency — no SLA, no stall policy.** B1 serializes E-3+ behind E-1/E-2 triage, and the Lane-L freeze (holmbuar #1097/#1099/#1101) has no timeout. PR #174 has already demonstrated multi-week triage latency. A single stall freezes the whole export pipeline while our "very proactive" mandate goes unmet. **Addition C3-12:** pre-registered stall rule — if a wave sits untriaged N days (propose N=10), consolidate the next wave into one omnibus packet, keep building upstream-ready packets in-repo under `notes/outbound_ready/`, and route what fits through pre-authorized channels (the WCL contributor-request register is already a negotiated no-triage-cost lane — B1-E-3 should be *promoted* in the queue for exactly this reason, not sequenced behind E-1/E-2 triage).

**4c. Attribution.** B1's house header covers per-PR provenance; nothing covers the endgame split. The prize spec supports split awards; the roadmap anchors the credit conversation to input 6 / first declared milestone. If B1-E-1 lands, that *is* plausibly the first declared milestone-grade joint artifact (his headline table completed by our prime). **Addition C3-13:** a bilateral attribution ledger (who proved what, per node/theorem, both trees, appended per wave, co-signed) proposed *at* the E-1 landing — retroactive credit reconstruction at submission time is the classic multi-party failure mode, and our import ledger currently records provenance in one direction only.

**4d. Correction/rollback custody.** No plan item says what happens when a post-integration defect is found in an exported packet (who fixes, in which repo, how the DAG and his register are jointly rolled back). The forced-corrections authority rule covers our repo only. **Addition C3-14:** one paragraph of agreed custody for cross-repo defects; cite the double-key rule (hash-green ≠ execution-green).

## 5. RISK REGISTER FOR THE STRATEGY

Roadmap §5's register covers *math* triggers only. The collaboration itself has none. Additions, each with trigger → consequence → mitigation:

- **C3-R1 Triage stall** (wave untriaged > N days) → export pipeline freeze, "proactive" mandate unmet → C3-12 omnibus rule + pre-authorized channels + in-repo staging.
- **C3-R2 Contributor scoop.** The corridor literal prime is a *printed public ask in his headline TeX* — any contributor can claim it any day; WCL slots CQ-1..4 are open requests; holmbuar is active and fast. B1 front-loads E-1 but never names scoop-exposure as the ranking force. Mitigation: rank exports by (his-need × readiness × **scoop-exposure**); for mineable areas we can't ship within a cycle, post an intent note in the negotiated channel; upgrade the A5 overlap audit from submission-day-only to a per-session poll of his open PR queue against our in-flight items.
- **C3-R3 Correspondence drift.** Upstream renumbers and rewrites (v3→v4 already moved labels; waves 1047–1086 landed since our welds were verified). The rolling crosswalk mitigates packaging drift but nothing *detects* semantic drift in cited environments. Mitigation: a correspondence CI check — script that resolves every cited upstream label at the pinned SHA and fails on movement; a standing rule that every new harvest first replays the relevant identification/weld chain at the current pin ("a stale source pin is a failed provenance gate" — his own rule, adopt it inbound).
- **C3-R4 Concurrent-close race.** Both trees attack a shared object (now guaranteed by the wall identifications) and close it independently in incompatible formulations → wasted work and a dossier seam. Mitigation: extend the lnl TAKEN/RACED/DEAD discipline to a standing two-column "who's-on-it" board inside C3-4's snapshot.
- **C3-R5 Frame churn / v5 rewrite.** A v5 of the results synthesis invalidates E-4's subtraction table and H4's inventory mid-flight. Mitigation: pin every export to the upstream SHA it subtracts against and state the subtraction is pin-relative.
- **C3-R6 Single-maintainer bus factor.** Everything gates on one person's static-only review. Mitigation: keep verifiers stdlib-deterministic (B1 has this) *and* offer attested execution transcripts he can spot-check — reduce per-packet triage cost so the bottleneck widens instead of us throttling.
- **C3-R7 The D3 self-kill applies here too.** If two full export+harvest cycles produce zero joint-bracket-bit movement (metric C3-1), the exchange strategy has failed its own falsifier → revert effort to direct red-closure grind and take the submission-posture decision. Neither plan has any self-termination condition; every other program we run does.

## 6. INTERNAL DEFECTS BETWEEN THE TWO PLANS

- **Double-claimed item:** B1-E-1 and B2 Part 3 both claim the corridor literal prime ("CLEAR outbound item (Part 3)"). One owner, one packet — dedupe before anything ships, ironically the exact discipline the A5 registry imposes upstream.
- **Baseline discrepancy:** B2 re-measures 275 = 213/38/24 and calls the brief's 201/36/23 stale; B1 builds on the A1/A2 inventories without acknowledging the re-measure. Reconcile to one baseline of record in the roadmap header before either plan executes, else every burn-down number is ambiguous.
- **Mining absent from B1 (T):** B1 is titled EXPORT + MINING; every visible item is export. If the truncated tail lacks a mining section, the harvest-side "be very proactive" half of the frame rests entirely on B2's one-time list — with no *recurring* mining cadence (his repo lands multi-wave weeks; a one-shot harvest list is stale in ten days). Addition: a standing per-session upstream-delta sweep (new commits at pin-advance → triage into R/H/ledger lanes) rather than episodic harvest planning.
- **ev→req promotion criteria missing:** B2 states the promotion condition for H1 only. Every ev-wired harvest needs its named promotion test (what verification converts evidence into a requirement edge), else the harvests permanently decorate reds without ever greening them — the mechanism by which "piecemeal" persists.

## SUMMARY OF CONCRETE ADDITIONS

C3-1 burn-down table + joint-bracket-bits metric per wave; C3-2 coverage checklists over his 4 inputs and our 24 targets (explicit "no route" entries allowed, silence not); C3-3 conditional-assumption dedup ledger vs his six inputs; C3-4 exported critical-orbit snapshot + crosswalk (the shared map); C3-5 Lean pilot riding E-1/E-2 (certificates + endpoint conversion, his named genres); C3-6 `lean_ready` audit of the 213 PROVED nodes; C3-7 nodes→modules convention note; C3-8 export the two proved wall identifications; C3-9 named U3-harness→joint-brief slot; C3-10 balance-line pincer ledger + restricted-overhead mining item + explicit deferral statement; C3-11 pacing ratification ask as the first outbound note; C3-12 triage-stall rule + channel promotion of E-3; C3-13 bilateral attribution ledger at first milestone; C3-14 cross-repo defect custody; C3-R1..R7 strategy risk register incl. the D3-style self-kill; plus the four inter-plan defects (corridor dedupe, baseline reconcile, recurring mining cadence, ev→req promotion criteria).

Files load-bearing for this critique: `/home/u2470931/smooth-read-solomin/prize/notes/PRIZE_RESOLUTION_ROADMAP.md` (§2 walls, §5 register, §8 endgame, §9 metric, §10 decision queue), upstream `agents.md` §§6–7 (stop rules, Lean policy), `experimental/grande_finale.tex` :7530 (thm:audited-status terminal inputs), upstream `experimental/lean/` (existing toolchain estate).