# TASK B1 — EXPORT + MINING PLAN (2026-07-26)

Basis: A1 greens inventory, A2 ambers+mining, A5 registry — ours @ prize master 8a820bdb, upstream @ origin/main b13de811. **Operative pacing rule (distilled from A5 observed mechanics):** the maintainer triages in consolidated waves under static-only review ("no Python, Sage, Lean, or TeX build was run"), so (i) at most two of our PRs open at once, never two in the same lane; (ii) verifiers must be airtight-deterministic before submission, stdlib-only, fail-closed, with mutation controls; (iii) every PR carries the house header ("Claude Fable 5 acting for AllenGrahamHart"), mirror-SHA pin + upstream target commit, an explicit non-claims section, and a self-run open-PR overlap audit clause citing the A5 registry; (iv) anything Lane-L-adjacent is frozen until holmbuar #1097/#1099/#1101 triage.

---

## 1. IMMEDIATE EXPORT PIPELINE — CLEAR items, ranked by (his-need × readiness)

**E-1. Literal corridor prime (ships this week, day 1–2; effort S).** His own printed gap in the headline safe-side table: v4 `thm:corridor` remark (~line 293) states the prize-scale corridor packet "does not yet pin one literal prime … A final row release should add a literal admissible field and rerun the same exact comparisons." Submission: mint q = c·2^41+1 with 2^255.9 < q < 2^256, Pocklington certificate attached, and replay all six GKL24/Hab25 corridor comparisons with exact budget floor(q·2^-128), reproducing the three printed safe radii 1092724518963 / 1415997755216 / 1644686143216 digit-exactly, plus adjacent-failure checks. Consumer citation: `experimental/proximity_prize_results_v4.tex` corridor remark, rates 1/4, 1/8, 1/16 at n=2^41. Source machinery: the E1 Pocklington exhibit precedent (already integrated upstream) + `rate_half_band_closure` row conventions. Fence: one line — "pins the denominator; strengthens no bound." Pacing: pure gap-fill on his headline section, zero over-claim surface — the ideal wave-opener.

**E-2. Proth-row independent replay audit (ships this week, alongside E-1; effort S).** Direct fill of v4 submission-package item (3): "a reproducibility dossier containing literal row manifests, primality certificates, source pins, exact replays" for `thm:proth`. Submission: pure AUDIT packet — second-codebase replay of all four Proth rows' primality, budgets B, adjacent quadratic signs, endpoint conversions; PASS verdicts only. Source: `/home/u2470931/smooth-read-solomin/prize/background/nodes/mca_quadratic_prize_rows/` (PROVED 2026-07-13; 32,654 proof-arithmetic instances; in-node `upstream_crosswalk.md` already pins his labels). Fence: the theorem is HIS (ChoShort26/ChoThresholds26) — we confirm, close nothing. Pacing: AUDIT status, different lane from E-1 (audits vs corridor row release) — safe to co-open; same shape as the accepted #1010 four-pair precedent.

**E-3. WCL first-installment theorems into the integrated register (week 2, after E-1/E-2 triage; effort S–M).** Consumer: his own integrated `experimental/notes/wcl_slot_contributor_requests.md`, which cites the (2,5)/(2,6) closures by SHA-pin only — landing the theorems makes the register self-contained while CQ-1..CQ-4 are open asks. Submission: per-cell packets in his certificate-dir format for `dli_wcl_ell2_weight5_norm_gcd_exclusion`, `dli_wcl_ell2_weight6_recursive_norm_exclusion` (+ split16 counterfixture), `dli_wcl_weight3_ambient_exclusion` (254 classes / 439 primes / max v_2=18), `dli_wcl_weight4_ambient_exclusion` (24,979 / 44,599 / max v_2=29), `dli_wcl_newton_short_window_exclusion` — each stated as the register's printed cell predicate verbatim (zero translation risk). Precondition (from A2): fold in the register maintenance first — Norm(u) order-1024 fix, gcd benchmark, (1,5) stage-0 repairs. Fences: serves the dli lane by the register's own printed lane declaration; slots (1,5),(1,6),(2,7),(4,9) open; survival sampling is not discharge. Pacing: fulfillment of an already-negotiated channel, not a new pitch — low triage cost.

**E-4. Rate-half exact-threshold remnant packet (week 2–3; effort M).** Consumer: agents.md §1 family-bracket criterion + v4 `tab:threshold-map` / §10 threshold-pinned rows. Submission = re-cut of the existing draft `/home/u2470931/smooth-read-solomin/prize/notes/upstream_pr_proposals_20260720/upr_draft_2.md`, subtracting what his `cor:target`/`thm:official` now cover (his compiler already yields B ≤ B_Q — state our determined-family half as **concordance, not novelty**). Four claims: (i) seam reduction — on B_Q < B ≤ 2^39+1, safety ⇔ the single far-CA bound B_ca^far(n−B+1) ≤ B; (ii) q ≥ 2^169 bracket k+8,594,128,896 ≤ a_RH(q) ≤ 3n/4; (iii) the MDS counterexample fence `rate_half_postquadratic_mds_extension_fence` (explicit F_5 instance; route-cut of the agents.md item-5 genre); (iv) the mandatory subtraction table vs his rows (the v10-roadmap obligation). Sources: `rate_half_quadratic_exact_range` (wave-10 audited, 7 verifiers, 25/25), `rate_half_half_distance_safe_bracket`, anchor-pencil normal form + rider reduction. Fences: NOT §0.4 near-capacity (the quadruple-mismatch fence printed verbatim — the #750 trap); 3n/4 endpoint imports BCIKS (not self-contained); {2^39, 2^39+1} residual open; no list claim. The ~40-node far-CA Hankel supporting layer is "available on request" only — its terminal is TARGET, and exporting it would trip his stop-rule. Pacing: opens only after E-1/E-2 close; A5 confirms no open PR touches far-CA/quadratic-staircase (the holmbuar trio sits at the cyclic-floor agreement, disjoint object) — but re-verify the open queue on submission day.

**E-5. L1 m=4,h=3 official emptiness (week 3–4; effort M).** Consumer: v4 Chebyshev/analytic section (thm:d128) + workboard M1 ("Existing one-shell and rooted-shell packets are local reductions, not exhaustive row bounds") — ours is an exhaustive stratum closure on all four official n=4(p+1) Mersenne rows. Source: `/home/u2470931/smooth-read-solomin/prize/background/nodes/l1_m4_h3_official_emptiness/` (wave-22 audited, +28 nodes PROVED, triple-replayed; in-node `upstream_crosswalk.md` already written as the outbound vendoring translation). Required additions: the row-dictionary section (print the n=4(p+1) family definition; claim nothing about his n=2^21 deployed rows — the one real packaging risk) + one verifier port. Fences: primitive shift-pair ledger, two-value classification, m=8/16 strata, t>p widths all open. Pacing: separate lane from E-4 — can overlap in flight.

**E-6. Petal clause-(P) full-petal closure (week 4+; effort M–L; heaviest fences on the list).** Consumer: workboard K4 ("Pay or eliminate quotient, extension, … planted … and every other named cell"); his own `l1_imgfib_crosswalk_audit.md` already carries clause-(P)/census-gate vocabulary citing us, and the 7-cert petal substrate is already integrated — this is an increment on an installed base. Submission: three-node chain `petal_growth` + `petal_chart_carrying_descent` + `petal_small_scale_staircase_census` (all PROVED KEY) with consumer arithmetic. Mandatory fence paragraph UP FRONT, citing #750 by number: closes the full-petal top-band clause ONLY; mixed-petal gap open (catch #212; `petal_mixed_amplification` TARGET, `pma_wide_residual` REFUTED); the `imgfib` parent stays CONDITIONAL; shipped as bankable-cell progress, never as an imgfib/L1 closure. Pacing: last in the queue deliberately — it is the item where over-claim risk is highest and where four prior clean integrations buy the credibility to land it.

**Deferred-CLEAR (registry-listed, not queued):** the interleaved-transport + band-propagation addendum to the integrated cyclic floor (lnl RANK 1) is bundle-only and frozen until holmbuar triage; the 13-chamber split-pencil census is lead-grade only (0/13 closed); the Lane-L unconditional post-Johnson upper bound is flagged as new-mathematics territory our machinery cannot reach — name it honestly if asked, do not attempt.

---

## 2. CONDITIONAL-ARCHITECTURE OFFERS — what to present and how

Upstream discipline (A5 Part 3, binding): conditional material is an accepted first-class genre IF it carries named hypotheses PLUS positive content (payment, route cut, or counterexample), a workboard binding, and a deterministic verifier; it lives in `experimental/`, never moves endpoints; rejections #1084/#1081 show conjectural skeletons without payment content die on arrival. Every offer below is shaped to that contract.

**O-1. C36 conditional proposition (`f3_h3_mobius_excess_half` ⇐ DSP8) — the best conditional export we own; draft after E-1..E-3 land.** Present in results-v4 dense-shell/MASTER style: one displayed inequality (17·X_18 ≤ 300n² on prime rows n=2^s, 13 ≤ s ≤ 41, p ≡ 1 mod n, p ≥ n²), hypothesis = the named DSP8 leaves with the P24/P25 iff stated as the satellite reduction, evidence = the exact sweeps (7,090 rows, two n=8192 exact replays, rational margin 48/425), falsifier printed in the packet ("one exact official row with 17·X_18 > 300n²"). Route into HIS spine honestly: offered as *input toward* the grande_finale Sidon/Q ⇒ SP ledger ("primitive Q after an explicit Sidon payment"), never as paying it. First-paragraph fence: his deployed KoalaBear/M31 rows are extension fields (F_{p^6}, F_{p'^4}); our corridor is prime fields — the mismatch stated before the result (the #750 lesson institutionalized). Second fence: DSP8 is a reduction layer; 0 sharp constants paid.

**O-2. Dossier co-ownership (packaging tier → his submission-package item 3) — raise at the next milestone message; this is a proposal, not a PR.** His item (3) is verbatim our packaging tier (total certified f(C) compiler, clean-checkout replay CI, provenance chain, nonclaim-ledger emptying), and ours is the only independent adversarial replay lane between the two trees. Offer: we supply the replay/audit half, with co-signed summed integer certificates and double-keyed status flips (his triage + our Modal re-pin; hash-green ≠ execution-green); conditional-close ambers ride inside with falsifiers attached. Hard fence: freeze-date/posture is the maintainer's call (gate D3) — raise, don't decide. E-2 (the Proth audit) is deliberately the down-payment demonstrating this lane.

**O-3. P-A1/P-A2 obligation-preserving scope contraction — short methods note, low priority, gated on Track C.** Consumer: grande_finale's E-input (`def:exception-routing`, chronology-correct exception routing) and the interplay table's "Koala tangent and localization packets." Content: the pattern (nongeneric 16n³ obligation routed verbatim into P-A2, allocation ledger conserved, full-zero descent becomes the P-A2 attack rather than a third red) with the worked instance. Fences: reduction only, no constant paid; do not imply the P-A2 attack is complete; **blocked until the rolling grammar crosswalk (Track C) maps our XR chart conventions** — no symbol reuse before that.

**O-4. dli B-WEAK — register channel ONLY; theorem channel stays adjudicated NOT-NOW (KB #124).** No S/A/E input consumes an endpoint budget for our x4 staircase; a result-PR fails our own consumer test. Actions: keep #1050 refreshed (the E-3 preconditions); add the register update that the four cheapest cells are TESTED-WITH-POWER (934 orbits, zero events), labeled "survival evidence, not census subtraction, rough-odd-part blind spot disclosed." Contingent offer only if he ever opens a conditional-propositions section: B-WEAK in his format, with the 9-round attack record (strongest priced attack 2.25 of 122 bits), the pre-registered falsifier, and the C1' dyadic-K REFUTED disclosure traveling with the statement — our self-refutations are the credibility asset, present them as such.

**O-5. F-round pre-registration discipline** exports through O-2 as the proposed joint falsification standard, not as a PR.

---

## 3. THE MINING PROGRAM — standing production line

Three areas enter production, in this order. Each mint obeys falsification-first: the falsifier is pre-registered in-node before the proof attempt, and ships in the packet.

**M-1 (TOP): Exact staircase determination at his clean rates — the "Complete solved rows" feeder.** His universal cap covers ρ ∈ {1/2, 1/4, 1/8, 1/16} unsafe-side; his corridor table is safe-side-only at 1/4/1/8/1/16; his ledger proves numerator r+1 for r ≤ ⌊R/3⌋ — and the staircase range widens as rate drops. Our wave-10 a_RH pipeline (safe = quadratic-staircase equality; unsafe = universal MDS coordinate-tangent family, rate-generic) re-parametrizes directly.
- *E1 — rate-1/4 exact adjacent family*: a(q) = n − ⌊q/2^128⌋ + 1 for 1 ≤ ⌊q/2^128⌋ ≤ B_Q(1/4), prime q ≡ 1 mod 2^41, k=2^39. Effort 2–4 sessions. Consumer: a new complete solved-row family in his headline section at a rate his corridor already tracks at n=2^41. Falsifier: one exact row in the declared range with a(q) ≠ n − ⌊q/2^128⌋ + 1, adjudicated by integer replay.
- *E2 — rate-1/8, 1/16 analogues + the rate-1/2 seam* {2^39, 2^39+1} via the uniform Hankel split-pencil bound (pre-armed in `critical/nodes/rate_half_band_closure/attack.md`; extends the window to ~(2^39+2)·2^128 and cleans OUR node's honest-residual clause in E-4). Effort: seam = one session; analogues low–medium. Falsifier: a split-pencil instance violating the uniform Hankel bound at either seam budget.
- *E3 — exact list counterparts* via his ledger item (ii) (Λ_μ = 1 for r ≤ R/2): two-sided list-uniqueness thresholds on the same declared families. Effort low. Consumer: his line/curve/list coding ledger. Falsifier: an exact list instance with Λ_μ > 1 inside the claimed range. (Timing note: these are list-side statements on NEW declared families, not the cyclic-floor agreement — still, run the Lane-L race check on submission day.)

**M-2: Engineered-prime / cyclotomic-norm exclusion censuses — the cheap-per-instance closer (~15 banked instances: Pocklington, norm-gcd, recursive-norm, Newton windows, MITM, ideal-index, Burnside, certified enumerator).**
- *A1 — the literal admissible field* = pipeline item E-1 above (his printed TODO; hours). Falsifier: Pocklington certificate fails replay or any of the six comparisons deviates by one digit.
- *A2 — bounded-weight relation exclusion on his declared Lane-L family* (q_0 = 3·2^41+1, already HIS prime): exclude all primitive multiplicative relations of weight ≤ w on the order-2^41 coset via the banked Newton-window/norm-gcd/recursive-norm pipeline — family-rigidity strengthening the declared-family section of experiments.tex, orthogonal to the open holmbuar trio (rigidity, not constants/brackets) — but **timing-gated on triage re-check per the lnl zero-new-race rule**. Effort: 1–2 sessions + compute. Falsifier: exhibition of one primitive relation of weight ≤ w on the coset.
- *A3 — Burnside orbit-completeness certificates as a service*: for any weight-window census he or a contributor runs, the certified "swept space = whole space" lemma with portable stdlib verifier (the #1050 enumerator is the template). Effort: low per instance. Consumer: his bankability contract ("formalized or independently replayed arithmetic gates"). Falsifier: mutation-control suite — any injected orbit-drop must flip the verifier to FAIL.

**M-3: U2 chart-carried quotient descent — the Lean formalization pilot for the joint endgame.**
- *C1 — descent master lemma, axiomatized + Lean-kernelized*: axiom package P (chart words factor exactly onto quotient chart words; retained-zero descends; band ⇔ band; margin-one uniqueness), K4/COL/`qa22_m_le_t_extension` re-proved as corollaries, first-occurrence kernel formalized against his existing `firstocc_general`/`firstocc_nodup_mem` Lean modules. Effort: 1–2 weeks incl. Lean. Consumer: his Lean policy's named high-value class ("first-match/add-back kernels") — and the pilot instance of DAG-nodes → Lean-modules. Falsifier: a chart word violating any axiom of P on a sampled quotient row; Lean kernel-checks are self-adjudicating.
- *C2 — K_2-invariance dyadic collapse* standalone (one M=2 descent prices every dyadic scale). Effort low–medium. Consumer: his imported quotient/extension-reductions package. Falsifier: a dyadic scale where the M=2 descent misprices, exhibited as an exact instance.
- *C3 — margin-one uniqueness with constructive sharpness* (exactly one degree of slack; false at d′, constructively). Effort: packaging only. Consumer: his repo demonstrably banks sharpness examples. Falsifier: already constructive — the d′ counterexample ships in the packet.

**Explicitly on hold in mining:** (d) 13-chamber split-pencil census (new theorems are hard, 0/13 closed — lead-grade communications only); (b) extended-window/orbit repricing (dli-internal, no upstream consumer); (f) powered falsifier-sampling folds into M-2's census offers as evidence, not theorems.

---

## 4. WHAT NOT TO DO — dead, raced, taken; carried fences

**TAKEN (cite as anchors; never re-propose):** rate-half cyclic quotient-rotation floor (78e67c40 → experiments.tex + agents.md Lane L); indirect MCA→list staircase conversion in general form (v4 cor:abf(iii), ChoThresholds26); #1010 four-pair audit; #1013 quotient-cell calibration + scroll-branch-empty census; #1019 pruned-Q toy; #1050 WCL register; finite-track kernel-basis + three cap25_finite packets (486c8fb3); imgfib crosswalk audit (2633895a); plus the A1 baseline set — census-gate trio, 11 XR certs, 7 petal certs, 4 Hankel certs, the E1 Pocklington 250-bit exhibit.

**RACED (stand down until triage):** any constant, bracket, or dyadic-scale claim at agreement 1116691496959 (holmbuar #1097/#1099/#1101, Lean-certified — never race a Lean-certified open PR); M31 fixed-G Hahn caps and complementarity (#1089/#1098/#1103); M31 flatness T-census (#1093–#1096); M31 LIST rank-7 residual incl. the Q=147595 head and the overlap terminal (scottdhughes #1088/#1092/#1105); KoalaBear §0.4 safe side / rank-nine (scottdhughes); M31 descent/locator/syndrome machinery (DannyExperiments); threshold source-entry/owner-leakage (avdeevvadim).

**DEAD (zombie-proposal prevention):** budget-1/2 exact crossings at 3n/4 (below Johnson); indirect list conversion of `rate_half_quadratic_exact_range` (inside Johnson AND generalized upstream); σ*=8,592,912,738 band and fixed-tail floor (superseded); multiplicative-amplification census (raced + wrong format); Johnson safe anchor; corridor-chain list_safe (conditional-on-conditional, no in-tree integers); any MCA numerator sold as a list bound; imgfib full-petal re-pose in #750's over-claiming form (E-6 is the narrower proved fragment WITH the #750 citation, the only sanctioned successor); `paving_rf3_double_prime` (his result, imported by us).

**HOLD (structural, not dead):** the wave-10+ far-CA Hankel 40-node chain (terminal TARGET — stop-rule violation if exported; supporting-layer-on-request inside E-4 only); HGE4 width-collapse and DSP8 standalone packets (different-lane; DSP8 travels only inside O-1 as the named hypothesis layer); XR uniform-branch floors + bridge dichotomy (second wave, after E-1..E-5 land); all Lane-L addenda until holmbuar triage.

**Carried fences on every packet:** the §0.4 quadruple-mismatch fence verbatim wherever rate-half material appears; prime-vs-extension-field mismatch stated first for anything F3/C36; scripts/PR_BODY/log fragments are stripped at integration — tex/note bodies self-contained; conditional statements never assert prize rows or GF inputs and always print their falsifier and F-round record; the C1' REFUTED disclosure travels with any dli statement.

---

## SEQUENCED PIPELINE SUMMARY

- **This week:** E-1 (corridor prime) + E-2 (Proth audit) open together — two S-effort, zero-over-claim gap-fills on his headline sections, different lanes. In parallel: #1050 register maintenance (O-4 preconditions) and start M-1/E1 rate-1/4 minting.
- **Week 2:** after E-1/E-2 triage, E-3 (WCL installment). Re-cut `upr_draft_2` → E-4 with the subtraction table. M-1/E2 seam session (also cleans our own `rate_half_band_closure` residual).
- **Week 2–3:** E-4 ships (post race-recheck). E-5 row-dictionary drafting. O-1 (C36 conditional) drafting begins once three integrations have landed.
- **Week 3–4:** E-5 ships; E-6 (petal, fence-first) prepared and ships last in the queue. O-2 (dossier co-ownership) raised as a milestone proposal, with E-2 as the demonstrated down-payment.
- **Standing production:** M-1 (E1→E2→E3), M-2 (A2 triage-gated, A3 on demand), M-3 (C1 Lean pilot — the bridge to the DAG-nodes → Lean-modules endgame), each mint carrying its pre-registered falsifier.

Key paths: `/home/u2470931/smooth-read-solomin/prize/dag.json`; `/home/u2470931/smooth-read-solomin/prize/notes/upstream_pr_proposals_20260720/upr_draft_2.md`; `/home/u2470931/smooth-read-solomin/prize/notes/lane_l_crosswalk_20260724/lnl_findings.md`; `/home/u2470931/smooth-read-solomin/prize/notes/PRIZE_RESOLUTION_ROADMAP.md` (P-A1/P-A2 ~l.788, packaging tier ~l.2062); `/home/u2470931/smooth-read-solomin/prize/notes/kernel_basis/KB_LOG.md` (#119, #121–#125); node dirs under `/home/u2470931/smooth-read-solomin/prize/background/nodes/` for E-1..E-6 sources; upstream anchors `experimental/proximity_prize_results_v4.tex` (corridor remark, tab:threshold-map, submission package), `experimental/grande_finale.tex` (S+A+E, def:exception-routing), `experimental/notes/wcl_slot_contributor_requests.md`, `agents.md` §1/§4.
---

## E-3 STATUS 2026-07-27: PREPARED AND ARMED — branch pushed, PR deliberately NOT opened

Branch `allen/wcl-register-first-installment` (fork, `379acae2`), cut from
upstream `b13de811`. Fires the moment triage moves; `gh pr create` is the only
remaining step.

**Contents:** five theorems (T1 (2,5) order-1024 norm-gcd; T2 (2,6)
order-1024 recursive-norm + split16 counterfixture; T3 weight-3 ambient
ell=1; T4 weight-4 ambient ell=1; T5 Newton short-window), one certificate,
one stdlib re-checker, and the three register maintenance corrections.

**Precondition audit (the A3 "Norm(u) order-1024 soundness gap") — RESOLVED,
and it was not where the roadmap line implied.** The concern was that a
weight-4 exclusion proved at order 512 might be consumed at order 1024. It is
not: `verify_slot_decomposition.py` sets `AMBIENT_EXCLUDED = {(1,3), (1,4)}`,
i.e. the weight-3/4 theorems are applied at `ell = 1` only, and at `ell >= 2`
the Newton floor `w >= 2*ell+1` removes weights 3 and 4 independently. The two
ell=2 closures compute resultants against `X^512+1` directly and take their
req-parents from their own routers, not from the ambient censuses. **The
shipped completeness derivation is sound as it stands.** A3 gates the (2,7)
COMPUTE request (CQ-3), not these theorems — the roadmap line conflated the two.

What the packet still does about it: the scope is now stated explicitly at the
point of citation in the register, and the shipped re-checker *asserts* the
scope claim with a mutation control proving it is not vacuous. So the
misreading is foreclosed for any future contributor.

**Verified from a clean checkout of the pushed branch**, not the working tree.
