All consumer node ids verified present in dag.json at 8a820bdb; correspondence directory and upstream_dag confirmed as described. Deliverable follows.

# TASK B2 — HARVEST + JOINT-GRAPH PLAN

Baseline of record (re-measured, A3/A4 concordant): critical orbit = 275 nodes = **213 PROVED / 38 CONDITIONAL / 24 TARGET** (brief's 201/36/23 is stale; 24th TARGET is the dossier packaging leaf, so "23 math leaves" stays the roadmap phrase). Upstream of record: b13de811, terminal open inputs per GF v4 `thm:audited-status` = **(S) spread-routing, (A) large-owner, (E) exception-routing, list-completion**, plus workboard K0–K5 / M0–M2 / Lane L / Lane T.

## PART 1 — RANKED HARVEST LIST

### Refutation-shaped first (kill-scope adjudicated)

**R1. Depth-32 shell counterexample** (`prop:m31-shell`, BuarM31Q6426/Q1626; `experimental/notes/thresholds/m31_dyadic_weight_laws_v1.md` :83–132 @ b13de811). Kill-scope: refutes upstream's OWN Q-3+4 rooted-shell proposal only; the certified 1233 rooted degree *reproduces* the census our `l1_m31_t64_quotient_prefix_intercept_fence` was built against — consistency confirmed, **kills nothing of ours**. Import as a scope-boundary node note on the fence (per node-local-notes rule) recording the open ragged (non-T16-aligned) sector. Effort: minutes. Cascade: prevents a future false-generalization of the fence.

**R2. Half-slice effective-image falsifier** (`experimental/lean/sidon_effective_image/`, HalfSliceFalsifier, Lean-certified). Kill-scope: refutes upstream half-slice effective-image flatness claims. Our `f3_h3_shifted_product_sidon` (PROVED) is a different object (exact cyclotomic product-collision Sidon on (1−μ_n)\{0}) — **no kill**, but mandatory 10-minute audit that no petal/imgfib note silently assumes the refuted flatness; bank the audit as a note line. Effort: one sitting.

**R3. Moment-blindness through order 990** (wave 1071–1086). Kill-scope: abstract M31 occupancies blind to all moments ≤ 990. **Advisory only** — our small-t moment certificates in `u2_per_row_certifier` are Row-C window certificates, not occupancy moments. Import as a route-cut citation in that node's status_ruling, fencing off pure-moment repair attempts. Effort: one edit.

**R4. Q-moment order floor** (`prop:q-moment-order-floor`, GF Part III: any finite-Q proof needs moment order ≥ 641,593 at M31). Kill-scope: cuts low-moment routes on BOTH sides of the F2==Q wall. Import as a fence note on `f2_growing_order_myerson` and as a pre-registered route-cut in the F2 flip campaign ladder. High strategic value at trivial cost — this constrains every future F2 session.

**R5. Singleton-MASTER refutations** (AvdeevSIM26: cap-six envelope false, shifted-Jacobi λ>1/10 false). Kill-scope: **none** — no node of ours consumes the MASTER lane. Ledger-line only; no node surgery.

### Positive harvests, ranked by cascade value / effort

**H1 (TOP). Affine-span / fixed-union list compilers → the 13-chamber bottleneck.** Provenance: `experimental/grande_finale.tex` @ b13de811 — `thm:affine-span-list` (:498), `thm:fixed-union-list-johnson` (:439), `thm:affine-span-mca` (:1396), `thm:single-mds-circuit-ray` (:421), `thm:rank-flat-list` (:583); unconditional per `thm:v4-package(i)`. Consumer: `rate_half_list_adjacent_crossing` (wired bottleneck red); secondary ev to `x4_exactlist_staircase_split`, `list_safe`. At our budget-3 point (a = 3n/4−1, K=2^40, w=n/4−1) the pencil (s=1) cap is **2**: any chamber forcing three of four witnesses into one codeword pencil dies; at s=2 the bound is exactly **4** — a rank-2 witness sits at equality and inherits rigidity, a concrete new attack on the open official-subgroup arithmetic. Wiring: **ev now; req only after chamber coordinates are bound to affine spans**. Effort: ~1 session (short self-contained proofs; independent reconstruction + exact-integer instantiation at our row). Cascade: the only upstream item with genuinely new leverage on a wired bottleneck; immediate compute payoff (see Part 2 surgery S6).

**H2. Exact sparsification identity.** Provenance: `thm:exact-sparsification` (grande_finale.tex:140; rs_mca_thresholds.tex:666): B^MCA = max{B^CA, σ} for every linear code, Γ, a. Consumer: `mca_safe` (amber) — the general glue reducing MCA safe side to far-CA + saturation census; our wave-10 Hankel layer already uses a row instance, so this is provenance hygiene + Γ-restricted generality for the `packaging` f(C) compiler. Wiring: ev. Effort: trivial (three-line proof, toy-row replay). Cascade: de-risks the compiler nearly for free.

**H3. Danny's local theorems** (integrated upstream 71f64349, wave 1047–1069). Provenance: `thm:pole` (pole-tolerant scalar-locator localization: |I| ≤ floor((n−g)/(m−g)) ≤ n−m+1, g<m, pole-division normal form), `thm:coordinate-span` (bad slopes in L_B(y0)L_B(y1)^{-1} with syndrome-span dim caps), `prop:syndrome-line-normal-form` / `thm:syndrome-secant-exact` (GF :176/:212). Consumers (three reds at once, all **ev**): `xr_lowcore_spread_heart` (the floor cap is exactly P-B's per-certificate spread shape), `l1_mixed_petal_amplification` (aggregate local first-owner payment in one fixed source — his named missing compiler is precisely the chronology discipline we own), `f3_h3_dsp8_correlation_bound` (syndrome-secant = the line-fiber side of DSP8's correlation). Effort: 1–2 sessions each with exact replays. Cascade: widest ev fan of any single import.

**H4. v4 solved-row inventory → dossier.** Provenance: `experimental/proximity_prize_results_v4.tex` @ b13de811 (f6a20fa3 promote) — `thm:proth` (four complete Proth-prime thresholds < 2^256, all four rates, B = floor(p/2^128)), `thm:f17` (exact F_{17^32} rate-half row, safe [0,6/512)), `cor:binary`, `thm:saturation-finite`, `thm:corridor`, threshold-map table. Consumers: `submission_quality_paper_dossier` (the solved-row half of the joint package; its §8 names the three-part structure our dossier interlocks with), `rate_half_band_closure` **ev** (thm:f17 as calibration point for the pre-registered rate-1/2 band-analogue falsifier family), `adjacency_closing` ev. Wiring: statement-level nodes + ledger rows; carry his "adjacent deployed safe rows not claimed" nonclaim verbatim. Effort: low, zero math risk. Note the export flip-side: the corridor remark admits no literal prime pinned at prize scale — our Pocklington-certified q_0 machinery is a CLEAR outbound item (Part 3), not a harvest.

**H5. Sidon/Fourier chain, GF Part III** (:3831–4185). Provenance: `thm:prefix-flatness-power-sum`, `prop:weighted-weil-minor-arcs`, `thm:unconditional-shallow-mi-ma` (proved flatness for multiplicative and Chebyshev twin-cosets in the shallow range, SFM2 exponential minor-arc decay, SFM3 max-fiber cap), `thm:sidon-resolved-payment`, `prop:high-energy-impossible`, `thm:primitive-q`. Consumers: `f2_growing_order_myerson` **ev** (input-3-shaped: pay/flatten/inverse chain fully assembled modulo (PF)+(MA), certified in the shallow range), `l1_mixed_petal_amplification` ev (`thm:prefix-flatness-power-sum` IS the node's named "global received-word Toeplitz prefix flatness" route), `imgfib`. **Hard fence, printed in the node statement**: his own remark (:4055) — at entropy depth R ~ N/log|B| the Weil parameter is not o(N); prize rows are OUTSIDE the proved range. Ev only, no close. Effort: 2–3 sessions (proof-read + census-scale replays). Cascade: standing-goal fuel — the F2 campaign resumes every session, and this is the best route material it has ever had.

**H6. Four-rate staircases + window compilers** (`rs_mca_thresholds.tex` :1584–1853: `thm:bchks-two-radius`, `thm:literature-integral-completion`, `cor:official-rate-half-staircase`, `cor:half-distance-window-compiler`). **Rate-1/2 portion SUBSUMED** — our wave-10 `rate_half_quadratic_exact_range` + Hankel layer strictly contain his reach; do NOT re-vendor as new. Live remainder: rates 1/4, 1/8, 1/16 staircases and the Γ-restricted compiler form. Consumers: `packaging`, `mca_grand` (ev/req-support). Effort: half a session (replay F(r) positivity per rate). Cascade: cheapest way to make our four-rate scope claim honest.

**H7. Separating-field / scalar-extension regime** (`thm:separating`, `thm:exact-first-adjacent-row`, `thm:extension-capacity`, `thm:unconditional-asymptotic-rs-mca`, `cor:unconditional-rs-mca-capacity`; GF :2301/:2363). Consumers: `adjacency_closing` ev (a completed adjacency endgame in the large-field regime — the template our amber needs), `ext_lift`, `knife_edge_census` (his λ = (φ−h(ρ))_+ boundary clause is our knife-edge in his language). Effort: moderate. Priority: medium.

**Method transplant (not a vendored node): the M31 rank-kill discipline** (`thm:m31-ranks`, HughesM31Rank626/726). His lane and row; harvest only the affine-rank-kill technique for our chamber analysis. RACED as territory (scottdhughes #1088/#1092/#1105) — technique yes, object no.

**Non-harvests, recorded:** dli slots (no contributor response in-tree yet; monitor waves); Holmbuar census machinery (M31 quotient-prefix algebra, no bearing on slot emptiness); band-closure beyond-2^167 (nothing upstream extends past; the frontier stays exclusively ours); pending PRs #1097/#1099/#1101 (unmerged heads — provenance not immutable, RACED, do not vendor, do not race).

## PART 2 — DAG-UPDATE PROGRAM (concrete surgeries)

Ordered; each surgery = node ops + one UPSTREAM_IMPORT_LEDGER.md row with immutable-commit provenance (b13de811 for tex spines; 71f64349 / 07e6d0e7 / cc1d8784 / f6a20fa3 for packet pins) + explicit nonclaims. House law throughout: independent reconstruction, exact-integer replay before status PROVED on any vendored supplier.

- **S1 (notes, same day):** R1 node note on `l1_m31_t64_quotient_prefix_intercept_fence` (ragged-sector scope boundary); R3 route-cut citation in `u2_per_row_certifier` status_ruling; R4 fence note on `f2_growing_order_myerson` + F2-ladder route-cut registration; R2 audit line after the petal/imgfib sweep. No status changes; no maintainer surface (forced-corrections authority covers these).
- **S2 (H1):** mint supplier nodes `upstream_gfv4_affine_span_list_compiler`, `upstream_gfv4_fixed_union_johnson` (PROVED after our replay), ev edges into `rate_half_list_adjacent_crossing`, `x4_exactlist_staircase_split`, `list_safe`. Statement carries the ev-not-req fence until chamber binding.
- **S3 (H1 compute, immediately after S2):** ramguard run of the collinear-triple / pencil-cap-2 check against each of the 13 chambers; s=2 equality-rigidity check second. Bank exact-integer certificates. **Any chamber killed is a status-adjacent event → maintainer-surface before the DAG shows the reduced chamber count.**
- **S4 (H2):** node `upstream_exact_sparsification_identity` (PROVED, toy replay), ev into `mca_safe` and `packaging`. Does NOT flip the `mca_safe` amber by itself; if the compiler audit later concludes the amber resolves, that flip is maintainer-surface.
- **S5 (H4):** nodes `upstream_v4_proth_solved_rows`, `upstream_v4_f17_rate_half_row`, `upstream_v4_saturation_corridor` (statement-level PROVED, upstream replays exist), ev into dossier leaf, `rate_half_band_closure` (calibration), `adjacency_closing`. Nonclaims verbatim.
- **S6 (H3):** nodes `upstream_danny_pole_localization`, `upstream_danny_coordinate_span_secant` with the three-way ev fan (xr / l1 / f3). 1–2 sessions of exact replays first.
- **S7 (H5):** nodes `upstream_gf_prefix_flatness_power_sum`, `upstream_gf_shallow_mi_ma` with the shallow-range fence IN the statement text; ev into F2 summit, petal, `imgfib`.
- **S8 (H6, H7):** `upstream_thresholds_low_rate_staircases` (ev-support into `packaging`/`mca_grand`), `upstream_gf_separating_extension_regime` (ev into `adjacency_closing`, `ext_lift`, `knife_edge_census`).
- **S9 (infrastructure):** refresh `upstream_dag/dag.json` from the stale ab7721e5 reconstruction to the v4 terminal set {S, A, E, list-completion} + K0–K5 / M0–M2 / Lane L/T workboard, retiring the superseded v3 six-input skeleton (incl. the retired BC red).
- **S10 (every surgery batch):** `tools/verify_prize_dag.py` pass; artifact + public-site refresh per standing rule.

**Maintainer-surface register for this program:** (a) any chamber kill from S3; (b) any req-grade promotion of H1 after chamber binding; (c) any amber resolution of `mca_safe`; (d) adoption of the JOINT_CROSSWALK artifact (outward-facing, Part 3); (e) all outbound PRs. Everything else is forced-correction territory.

## PART 3 — JOINT-GRAPH ADOPTION

**Ownership split (proposal, to be surfaced to maintainer then offered upstream).** Grounded in the two proved identifications (F2==Q via the Myerson chain; L1 exact-shell == locator-prefix Q fiber via `l1_exact_shell_fixed_cofactor_prefix_transport`) and the measured fact that his four terminal inputs and our 23 math leaves intersect in exactly ONE identical object:

- **HE closes:** (A) large-owner, (E) exception-routing, K1 legacy-M1 bridge, K5 certificate emission, Lane M M0/M1, M31 Lane-L uppers. All his-only objects inside his partition/chronology; we feed ev (four-pairs replay, M_B weld, regime map — already imported both ways).
- **WE close:** the 14-leaf DLI block (nobody else can; portfolio-capped per D2), the 4-leaf certificate-machinery chain, `u2_per_row_certifier`, the F3 pair (`dsp8`/`hge4` — DSP8 max-P ≤ 24 cascades three ambers), F6 `rate_half_band_closure` (the beyond-2^167 frontier is exclusively ours), and lane-adjacent list leaves under the crosswalk DON'T-fences.
- **JOINT, double-key (neither side flips alone):** `f2_growing_order_myerson` == `U_Q`/K2 (one proof pays both programs' deepest wall — the natural co-signed certificate); `l1_mixed_petal_amplification` ↔ `U_list_int` via chain #2; (S) ↔ the XR pair (first prover, other side replays); `x4_exactlist_staircase_split` ↔ list-completion assembly; the dossier ↔ his package item (3).

**The correspondence artifact — concrete spec.** File: `notes/correspondence/JOINT_CROSSWALK.json` in our tree (single source of truth, next to the import ledger). Format: a JSON array of rows

`{our_node, his_object: {label, file, line}, relation: IDENTICAL|OVERLAP|OURS_ONLY|HIS_ONLY|ANALOGY_ONLY, chain: <machine-verified chain ref or null>, owner: US|HIM|JOINT, status_ours, status_his, lean: <module or null>, provenance: {our_sha, his_sha}, nonclaims: [...]}`

Seeded from A4 §1b + the THREE_VS_SEVEN table; the ANALOGY_ONLY row for Q ↔ `rate_half_band_closure` is written down explicitly with the WP5 "structurally incapable" fence so it can never be silently promoted. Validation: a `tools/verify_crosswalk.py` in the verify_prize_dag.py style — every `our_node` must exist in dag.json; every IDENTICAL row must carry a non-null chain ref; every ANALOGY_ONLY row must carry chain=null plus a fence citation; owner=JOINT rows must name both keys. Maintenance protocol: we own the file; every upstream integration wave produces a crosswalk diff attached to the wave audit; a mirrored copy is exported to his `experimental/notes/correspondence/` as a normal SHA-pinned packet (his packet grammar, "Claude Fable 5 acting for AllenGrahamHart" header, non-claims section, self-run open-PR overlap audit clause citing the A5 registry). This one artifact is simultaneously his submission item (3)'s "formal correspondence files" deliverable — built once, delivered in both packages.

**Double-key rule (formal).** Any status flip on an IDENTICAL or owner=JOINT row requires **(a)** our key: replay-before-build banking — exact-integer verifier PASS in-tree; **(b)** his key: bankability contract satisfied — workboard item, partition digest, quantifier, unit (his §5 YAML block). Until both keys turn, a one-sided proof sits at CONDITIONAL/amber with the other side's key named in status_ruling. This is the existing import-ledger law generalized to symmetric form; it also protects him — his own house rule ("do not treat local discharges as row payments until bound to the active first-match partition") is the same key stated from his side.

**Co-finish mechanics and cadence.** Steady state: (1) harvest pass per upstream integration wave (the wave 19–22 pattern: fetch, read-only audit, three-way governance merge, node-local retrofit); (2) crosswalk-file diff attached to every wave audit; (3) outbound PRs only in his packet grammar, naming live K/M/L/T items, never the superseded v3 checklist; conditional exports only as named-hypothesis packets with route-cut/payment content + deterministic stdlib verifier (his accept/reject record is unambiguous on this — #1084/#1081 rejections vs #1054 acceptance); (4) artifact + public site refresh after every DAG update (standing rule); (5) registry discipline: TAKEN items cited as anchors never re-proposed; RACED items (holmbuar trio at agreement 1116691496959, M31 Hahn caps, rank-7 residual, KoalaBear rank-nine, Danny descent machinery) stand down until triage; live CLEAR exports in priority order = the n=2^41 two-sided a_RH threshold family (best RESULT export, quadruple-mismatch fence verbatim), the corridor literal-prime Pocklington q_0 export (answers his own admission in thm:corridor), the WCL slot back-consumption channel, and — bundle-only after triage — the interleaved-transport addendum.

**What "both finish together" concretely requires:** FROM HIM — (A), (E), K1, his half of (S), K5/M-row certificate emissions, acceptance of the crosswalk into his dossier. FROM US — DLI block, certificate machinery, F3/F6/U2, our half of (S)/XR, the dossier leaf. JOINTLY — one co-signed proof of F2==Q, which simultaneously discharges his K2/`U_Q` null atom and our summit red, collapses the L1/`U_list_int` pair via chain #2, whereupon the 38 CONDITIONAL spine nodes (incl. `mca_grand`, `list_grand`, `prize`) discharge mechanically by gate propagation and the all-green critical DAG — carrying the crosswalk file and the Lean modules keyed node-by-node — IS the joint proof's dependency graph.

## PART 4 — PHASED LEAN PLAN

Aligned to his four-item priority order (agents.md §7: (i) proved local theorems used by live atoms; (ii) first-match/add-back kernels; (iii) endpoint/integer conversion; (iv) final row certificates + counterexample correspondence) and both stop rules (his: no end-to-end GF while inputs are hypotheses; no sorry-laundering — ours: Tier-1 exact-integer certificates, typed non-claims for Python-verifier semantics). The conventions are already compatible: his 109 packages and our 14 stdlib-only modules use the same statement-match + kernel-certificate discipline.

**The DAG-as-module-structure story (the pitch to him, one paragraph):** node → one Lean file, namespace = node id; **req edge → import; ev edge → no import** (evidence never enters a proof term); gate:all → the node's theorem takes its req-parents' theorems as explicit hypotheses until discharged; verifiers split two tiers — exact-integer replays become `decide`/Nat-kernel certificates (his integer-staircase pattern == our Tier-1 pattern), Python-census facts stay typed bridge Props until re-derived (our SigmaCSparseCensusBridge pattern). Measured: **127 of the 213 proved orbit nodes are verifier/certificate-backed (Lean-cheap)** vs 86 analytically hard; the cheap 60% contains everything his priorities (ii)–(iv) need.

- **Phase 0 (now, ~free) — DO FIRST:** statement-level correspondence only. Add the `lean` column to JOINT_CROSSWALK.json, merging our CERTIFICATION_MAP.md with his `*_CORRESPONDENCE.md` convention. First two concrete acts: **(a)** adopt his `firstocc_general`/`firstocc_nodup_mem` as the shared first-match kernel base and write correspondence entries for our U2 objects against it (his priority (ii); roadmap Track-B caps Lean effort ~10%); **(b)** pilot his priority (iii) by replicating our Tier-1 F17 endpoint pattern (17^32 bracket, 506/507 endpoints) for the four deployed Proth rows from H4 — cheap, synergizes with the S5 harvest, and feeds the dossier directly. No new proof obligations anywhere in Phase 0.
- **Phase 1 (cheap kernel):** the 127 verifier-backed nodes as integer/`decide` certificates, ordered (ii) first-match/add-back, (iii) endpoint/integer conversion for deployed rows, then per-node census anchors. Rule: a Phase-1 module lands WITH the node it certifies (same commit as the DAG surgery), never separately.
- **Phase 2 (the co-signed merge point):** one package per terminal object — F2==Q, S, A, E, list-completion — containing the frozen statement, both sides' names for it, and the conditional row-closure theorem (his `thm:mca-completion-certificate` shape) with the open inputs as **declared hypotheses**. Legal under his stop rule because hypotheses are declared, not axioms smuggled as proofs; these are the double-key Lean artifacts, co-signed.
- **Phase 3 (only after flips):** as each terminal input is proved, its hypothesis is discharged in place; K5 / `thm:exact-completion-certificate` row certificates and our `mca_grand`/`list_grand` close as *builds*, not documents. The 86 hard nodes are formalized only where a live certificate imports them.

**Execution order for the whole plan:** S1 notes today → S2+S3 (H1 + chamber check, the one item that can change the strategic picture this week; surface any kill) → S4/S5 (free wins + dossier) → crosswalk file + validator + Phase-0 Lean (one session, unblocks the joint frame) → S6–S8 harvest tail → ownership-split proposal packaged upstream in his grammar with the crosswalk as its exhibit.

Key paths: `/home/u2470931/smooth-read-solomin/prize/dag.json`, `/home/u2470931/smooth-read-solomin/prize/notes/correspondence/UPSTREAM_IMPORT_LEDGER.md`, `/home/u2470931/smooth-read-solomin/prize/notes/correspondence/JOINT_CROSSWALK.json` (to create), `/home/u2470931/smooth-read-solomin/prize/upstream_dag/dag.json` (stale, refresh), `/home/u2470931/smooth-read-solomin/prize/formal/lean/rs_mca_formalization/CERTIFICATION_MAP.md`, `/home/u2470931/smooth-read-solomin/prize/notes/lane_l_crosswalk_20260724/lnl_findings.md`; upstream via `git -C /home/u2470931/smooth-read-solomin/rs-mca show origin/main:<path>` — `experimental/grande_finale.tex`, `experimental/proximity_prize_results_v4.tex`, `experimental/rs_mca_thresholds.tex`, `experimental/agents.md`, `experimental/notes/thresholds/m31_dyadic_weight_laws_v1.md`.