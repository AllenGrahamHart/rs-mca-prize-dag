PLAN B1: verdict=The plan is substantially sound and unusually well-grounded: of ~40 load-bearing factual claims checked against both trees, all node statuses (E-1..E-6, O-1, O-4 sources), all quoted upstream text (corridor remark + radii, thm:proth rows, WCL register, agents.md K4/M1/Lane-L, imgfib crosswalk audit, roadmap P-A2 routing and packaging tier), all provenance commits (78e67c40, 486c8fb3, 2633895a, cc1d8784 via KB #124), the KB #124 NOT-NOW adjudication, the #750 citation, and the holmbuar #1097/#1099/#1101 race registry verified exactly; the §4 DEAD/RACED/HOLD fences are correctly carried and no proposal enters occupied registry territory. Two real defects: (fatal, must fix before this plan is final) M-1/E1 proposes as 'a new complete solved-row family' a result his v4 thm:quadratic + cor:target + thm:official already prints in strictly wider generality — the M-1 premise misreads his safe-side frontier as the ⌊R/3⌋ ledger, and shipping E1 would be precisely the zombie-proposal/credibility damage the plan's own §4 exists to prevent; and (material) E-1's q-constraint fails to pin the corridor packet's B* budget while its edges are budget-sharp to 0.01–0.05 bits, so the promised digit-exact reproduction is not guaranteed as specified. Both have clean surgical fixes (strike E1, promote the E2 seam; add the B*-band constraint), after which the sequenced pipeline E-1→E-2→E-3→E-4→E-5→E-6 with O-2 raised at the milestone is the right order and the effort estimates are realistic under the compute law. The minor items (E-4 claim-(i) self-subtraction, stale E-2 crosswalk pins, O-1 evidence-scale and field-label wording, A5-inherited queue facts) are packet-level spec corrections, not structural problems.; non-minor: [fatal] M-1/E1: "rate-1/4 exact adjacent family: a(q) = n − ⌊q/2^128⌋ + 1 for 1 ≤ ⌊q/2^128⌋ ≤ B_Q(1/4) ... Consumer: a new complete solved-row family in his headline section"; premise sentence: "his ledger proves numerator r+1 for r ≤ ⌊R/3⌋ — and the staircase range widens as rate drops." -> Strike E1 as a theorem export and delete the ⌊R/3⌋ premise sentence. Re-point M-1's TOP slot at M-1/E2's rate-half seam {2^39, 2^39+1} (verified genuinely beyond thm:official's B ≤ 2^39−2, pre-armed in rate_half_band_closure/attack.md and roadmap ~l.784, and it cleans our own residual). Re-screen M-1/E3 the same way before minting: thm:high-ledger(ii) already proves Λ_μ=1 for r ≤ ⌊R/2⌋ for every μ, so only the sharpness/unsafe direction of a two-sided list bracket can be new. Add a standing rule: every M-1 mint runs a subtraction check against v4 §"Exact affine-line staircases" + thm:official before drafting. | [material] E-1: "mint q = c·2^41+1 with 2^255.9 < q < 2^256 ... and replay all six GKL24/Hab25 corridor comparisons with exact budget floor(q·2^-128), reproducing the three printed safe radii ... digit-exactly, plus adjacent-failure checks." -> Constrain the mint to floor(q/2^128) = B* exactly (the band [B*·2^128,(B*+1)·2^128) is width 2^128; with a factored part F | q−1, F ≳ 2^128 needed for Pocklington, expect roughly one admissible q per choice of certified cofactor structure — try several certified large-prime cofactors; ~10^3 candidates, trivial under ramguard tiny). Alternatively pre-verify pass/fail equivalence at all six edges ± one grid step for the chosen q before writing "digit-exact" anywhere. Effort stays S but the B* constraint is load-bearing and must be in the spec.; keep: E-1 literal corridor prime — his own printed TODO (v4 thm:corridor remark verified verbatim, radii digit-checked), zero over-claim surface, correct wave-opener — but only with the B*-band constraint added (floor(q/2^128) = integer 10th root of 2^1279), since the packet's own README shows adjacent failures sharp to 0.01–0.05 bits. ; E-2 Proth-row independent replay audit — source node PROVED with 32,654-instance verifier in-node, fills v4 submission-package item (3) verbatim (line 700), same shape as the proven #1010 channel and the deliberate down-payment for O-2; refresh the stale crosswalk pins (old manuscript labels at 9262f63c → v4 labels at b13de811) first. ; E-3 WCL first-installment theorems into the integrated register — the register exists upstream citing (2,5)/(2,6) by SHA-pin only, all five cited cells are PROVED with predicates matching the register's printed conventions (order-512·ell, 254/24,979 classes), and it is fulfillment of an already-negotiated channel, fully consistent with KB #124's NOT-NOW on the result-PR route (register/CR-004 channel, not a theorem row). ; E-5 L1 m=4,h=3 official emptiness — verified absent upstream (zero grep hits for the stratum), PROVED wave-22 with the outbound crosswalk already in-node, and a direct hit on agents.md M1's printed gap ('Existing one-shell and rooted-shell packets are local reductions, not exhaustive row bounds'); keep the row-dictionary fence against his n=2^21 deployed rows. ; M-1/E2 rate-half seam session ({2^39, 2^39+1} via the pre-armed uniform Hankel split-pencil bound; roadmap ~l.784 and rate_half_band_closure/attack.md both confirm it, extending the determined window to ~(2^39+2)·2^128) — after striking E1, this is the one mining item genuinely beyond his thm:official, and it simultaneously cleans our own node's honest-residual clause feeding E-4.
PLAN B2: verdict=SOUND WITH MATERIAL CORRECTIONS — no fatal defects. The plan's load-bearing skeleton survives adversarial checking unusually well: the 275 = 213/38/24 baseline reproduces exactly (the brief's 201/36/23 is indeed stale), all 20+ consumer node ids exist with the claimed statuses, every upstream theorem label resolves at b13de811 (most at the cited line), all four provenance commits exist with matching content, the moment-blind 990/991 and 641,593 constants replay, the #1081/#1084-reject vs #1054-accept grammar record is real, the upstream_dag reconstruction is verifiably stale, and the registry fences (zero Lane-L races, holmbuar trio RACED, interleaved-transport bundle-only, unmerged #1097/#1099/#1101 untouched) are all honored. Six material fixes are required before execution, all of one family — scope inflation at the edges: wrong leaf counts in the ownership split (12 DLI + 2 cert, not 14 + 4), 'proved identification' overstated for F2==Q (zero-prefix only, vs his PRUNED K2 atom) and for the L1 shell transport (e=0 only), an over-broad R4 route-cut (tau=1 model only), a falsely-CLEAR Pocklington export into demonstrably occupied territory (Paving Proth certificates + e1 250-bit exhibit already upstream), Phase-0/2 Lean deliverables that his own stop rules classify as non-progress (needs an explicit maintainer opt-in gate, cf. the #1084 rejection), and S5's PROVED-without-our-replay wording that contradicts the plan's own house-law preamble. Two minor citation-pin errors (pole/coordinate-span/m31-shell live in v4.tex, not GF) must be fixed before any ledger row is written. With those corrections applied, the five keep-actions above are the strongest available program and none would mislead the maintainer.; non-minor: [material] PART 3 ownership split: 'WE close: the 14-leaf DLI block ... the 4-leaf certificate-machinery chain' -> Re-count from the orbit before packaging: 12-leaf DLI block (13 with the off-orbit factorization leaf, say which), 2-leaf certificate-machinery pair. Total accounting must reconcile to the 24 orbit TARGETs. | [material] 'Grounded in the two proved identifications (F2==Q via the Myerson chain; L1 exact-shell == locator-prefix Q fiber ...)' and the endgame 'one co-signed proof of F2==Q ... whereupon the 38 CONDITIONAL spine nodes discharge mechanically' -> Crosswalk relation for f2<->U_Q/K2 and l1<->U_list_int must be IDENTICAL-at-scoped-instance (chain ref = f2_zero_prefix_q_equivalence, scope 'zero-prefix, level j < char q'; e=0 for the L1 pair) or OVERLAP, with the pruned-vs-raw gap named in the row. The double-key rule stays; the 'discharges mechanically' sentence gets a conditional ('once the scoped chain is extended to the pruned atom'). | [material] R4: import prop:q-moment-order-floor 'as a pre-registered route-cut in the F2 flip campaign ladder ... this constrains every future F2 session' -> Import with the tau=1 hypothesis and the three named escapes verbatim in the fence text; register on the F2 ladder as 'route-cut for fixed raw moments at full mass' only, and surface the ladder change rather than applying silently. | [material] 'our Pocklington-certified q_0 machinery is a CLEAR outbound item' answering the corridor remark -> Reframe the export as 'corridor literal-prime instantiation (fills the v4 thm:corridor remark)', cite his existing Proth/Pocklington certificates as the format anchors, and claim no machinery novelty. | [material] PART 4 Phase 2: conditional co-signed Lean packages with declared hypotheses are 'Legal under his stop rule'; Phase 0 ships statement-level correspondence entries -> Add an explicit gate: Phase-2 packages and any upstream correspondence-file delivery go out only after the maintainer surfaces the co-sign proposal and Przemek opts in (e.g. by accepting the crosswalk into his item (3)); until then they live in our tree only and are never framed as progress-claiming PRs. | [material] S5 mints upstream_v4_proth/f17/corridor nodes '(statement-level PROVED, upstream replays exist)' -> Either run our own ramguard exact-integer replays first (Proth/F17 endpoint arithmetic is cheap and Phase-0(b) already plans it — do S5 and Phase-0(b) together), or mint at CONDITIONAL/statement-import until the replay lands.; keep: H1 + S2/S3 (keep, first): the affine-span/fixed-union list compilers into the 13-chamber bottleneck. Every element verified: thm:affine-span-list/:498, thm:fixed-union-list-johnson/:439, thm:single-mds-circuit-ray/:421, thm:rank-flat-list/:583 all present at b13de811; 'EXACTLY 13 chambers' and the 4-codeword witness at 3n/4-1 are verbatim in rate_half_list_adjacent_crossing (in-orbit TARGET, key); the arithmetic reproduces independently from the theorem's formula (pencil s=1 cap floor((n/2+1)/(n/4))=2; s=2 cap floor(4+2/N)=4 at equality). The ev-not-req fence until chamber binding and the maintainer-surface-on-chamber-kill rule are exactly the right #750-class protections. This is the only harvest with new leverage on a wired bottleneck. ; JOINT_CROSSWALK.json + verify_crosswalk.py + the double-key rule (keep, with the IDENTICAL-row scoping fix): grounded in a verified fact — v4's submission package item (3) literally names 'formal correspondence files', so the artifact is simultaneously our joint-frame spine and his dossier deliverable. The double-key rule matches his own bankability contract (workboard YAML block verified in agents.md) and our replay-before-build law; it is the single mechanism that makes 'both finish together' operational. ; S1 fence notes R1-R4 (keep, same day, with the tau=1 caveat on R4 and the 1233-concordance replay on R1): trivially cheap, all four kill-scopes verified against both trees (moment-blind 0..990/991 confirmed in the wave-1071-1086 certificate; 641,593 floor confirmed at GF :5532; ragged sector confirmed open in the weight-laws note; no node of ours consumes the MASTER lane — all dag.json 'MASTER' hits are master-branch imports), and they permanently fence future false-generalization at four nodes including the standing-goal F2 summit. ; H3, the Danny three-way ev fan (keep): the strongest wiring evidence in the whole plan — l1_mixed_petal_amplification's statement names 'aggregate local first-owner payment in one fixed source' and 'global received-word Toeplitz prefix flatness' verbatim, which are precisely the coordinate-span/pole route and the H5 prefix-flatness route; provenance wave 1047-1069 (71f64349) confirmed in his agents-log with Danny attribution. Fix only the file pins (v4.tex, not GF). ; S9 upstream_dag refresh + Phase-0(b) Proth/F17 endpoint replication (keep, merged with S5): the reconstruction is verifiably stale (contains the ab7721e5 pin and the retired v3-era 'THE binding red' Q node) while the true terminal set {S, A, E, list-completion} is pinned in thm:audited-status @ GF :7530; and replaying the Proth/F17 endpoints ourselves (cheap: 506/507 and [0,6/512) verified in v4; four literal Proth rows verified in Paving v9.2) simultaneously discharges the S5 status-discipline problem and starts his Lean priority (iii) on his own solved rows.