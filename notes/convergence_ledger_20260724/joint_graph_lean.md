# TASK A4 — JOINT DEPENDENCY GRAPH + LEAN ARCHITECTURE
Read state: ours = /home/u2470931/smooth-read-solomin/prize @ 8a820bdb; his = rs-mca @ origin/main b13de811 (read via git show only). All numbers below re-measured from dag.json and upstream files this session.

## 0. Measured baselines (drift note)

Our critical orbit (ancestors of `prize` via req edges) at 8a820bdb: **275 nodes = 213 PROVED / 38 CONDITIONAL / 24 TARGET**. The 24th TARGET is `submission_quality_paper_dossier` (non-math packaging leaf), so "23 math leaves" remains the correct roadmap phrase (roadmap r3 line ~621: "21 of the 23 mathematical leaves" in the clean-rate milestone).

> **Q0 correction, 2026-07-26.** This note's "the task brief's 201/36/23 is a stale snapshot" was **wrong**, and the fact-check endorsed the error. 201/36/23 is the **math orbit** — the same closure rooted at the two grand challenges {`mca_grand`, `list_grand`} instead of at `prize` — and it reproduces exactly. 275 = 260 + a 15-node packaging spine (12 PROVED, the 2 CONDITIONALs `prize`/`packaging`, and the dossier TARGET). Both censuses are live and correct; say which root you mean. Machine-pinned by `tools/verify_orbit_census.py`; see CONVERGENCE_LEDGER_R1.md §"Baseline of record".

His remaining open structure, per GF v4 `thm:audited-status` (grande_finale.tex ~line 7528): the v3 six-input checklist is **superseded**; the terminal open inputs are exactly four: **(S) `prob:mca-spread-routing`, (A) `prob:large-owner`, (E) `prob:mca-exception-routing`, and `prob:list-completion`** (v3's `prob:saturated-bc` split into these; `prob:spread-abundance`/`prob:next` are subsumed intermediates). agents.md's live workboard adds: **K0-K5** (KoalaBear MCA row certificate, `B*=274980728111395087` at a+=1116048, every deployed `U_Q` still null, K1 legacy-M1 source-bound bridge missing), **M0-M2** (M31 list 2^-100, `B^list_C(1116023)<=16777215`, binding ratio 8.4152x mean), **Lane L** open ask ("a broader unconditional upper bound or a matching upper/lower bracket on a precisely declared family" beyond Johnson), **Lane T**. v4 synthesis ("Interplay and the best current submission package") says what is missing verbatim: classification of every positive-dimensional nonproper correction component, the row-sharp large-owner challenge-image bound, chronology-correct exception routing, and one literal exact replay with no unresolved cells — i.e. S+A+E — plus a three-part submission package whose part (3) is a reproducibility dossier.

## 1. THE JOINT GRAPH

### 1a. The two proved identifications (the load-bearing weld points)

1. **His row-sharp Q atom == our F2 zero-prefix instance** — the Myerson identification chain, each link machine-verified; export note at notes/correspondence/MYERSON_IDENTIFICATION_EXPORT.md ("row-sharp Q = shared census kernel = F2's zero-prefix instance"). This binds his K2/`U_Q` (the single binding atom of his whole architecture) to our summit red `f2_growing_order_myerson`.
2. **The scalar-cofactor top exact shell of arbitrary-word L1 == one locator-prefix Q fiber** — node `l1_exact_shell_fixed_cofactor_prefix_transport`; this is the second proved chain the task asked me to find and name: it entered via the **r3-roadmap overlay adopted in wave-20** (notes/wave20_import_20260722/WAVE20_AUDIT_FINDINGS.md line 85, "identification-discipline paragraph ... count updated ONE->TWO proved"; KB_LOG.md ~line 3461 "overlay upgraded the identification count ONE->TWO proved chains"). It ties our L1 interior-list object to his prefix-fiber Q machinery — the conduit by which any Q progress transfers to `l1_mixed_petal_amplification` and vice versa.

Held honestly per roadmap §2: TWO proved; ONE strong lead (six-input #3 <-> F2 summit); ONE speculative (atlas <-> XR); the Q <-> `rate_half_band_closure` bridge is **ANALOGY-ONLY** (WP5: "structurally incapable" — fatal side/quantifier mismatches). Do not promote without a machine-verified chain.

### 1b. Our 24 leaves -> his nearest counterpart

**IDENTICAL (proved chain) — the merge point.**
- `f2_growing_order_myerson` (F2 summit) == his `U_Q` / K2 / the Sidon-Fourier payment (GF sec:fourier, sec:primitive-q-proof). One object, two quantifier forms (his uniform mode-at-null vs our extras-budget average). This is THE shared wall; also where his moment-order no-go (r >= 641,593 at M31) constrains both sides' attack routes.

**OVERLAPPING (same object family, different assembly; his terminal input on the other end).**
- `l1_mixed_petal_amplification` <-> `prob:list-completion`'s `U_list_int` term (arbitrary-word interior) via proved chain #2; his `thm:capf-planted` already refuted the naive poly bound on our side (imported, v13_capf_planted_lower_count).
- `xr_highcore_collision_count`, `xr_lowcore_spread_heart` <-> **(S)** `prob:mca-spread-routing` + spread-core incidence theory. Caution from roadmap Track-C: his moving-root theorem does NOT pay our P-A1/P-A2/P-B (XR charts are arbitrary MDS kernel-ray charts, not proved pencils) — overlap is real but no free transfer either way.
- `f3_h3_dsp8_correlation_bound`, `f3_hge4_norm_gate_count`, `u2_per_row_certifier` <-> his SP/second-moment stratification (his exact identity Sum N^2 = C(n,m)+Sum P_e is imported PROVED; our F3/U2 objects are the primitive-pair residuals of OUR assembly — template-level overlap, per THREE_VS_SEVEN grades).
- `rate_half_list_adjacent_crossing` <-> Lane L / his racing PRs #1097/#1099/#1101 on OUR integrated rate-half row. Lane-L crosswalk verdict of record (lnl_findings.md): our exact crossing at 3n/4 is BELOW Johnson, hence dead as a Lane-L packet; our floor is already the lane's foundation (taken at 78e67c40, co-authored); the constant refinement and bracket asks are RACED — submit zero race PRs.
- `rate_half_band_closure` (F6) <-> his finite adjacent-pair conjecture, SHAPE-IDENTICAL grade only; bridge analogy-only (above).
- `submission_quality_paper_dossier` <-> his submission package item (3) (reproducibility dossier: literal row manifests, primality certificates, source pins, exact replays, formal correspondence files). Directly co-deliverable.

**OURS-ONLY (his assembly avoids the object).**
- The 14-node DLI block: 12 `dli_wcl_slot_*_emptiness` + `dli_c1r3_gated_envelope_bound` + `dli_c2pp_joint_reserve`. THREE_VS_SEVEN: "their assembly has NO tower — it never meets our hardest object (B-WEAK's multi-level joint E_U[Pi rho])". No upstream counterpart exists or is planned.
- Certificate machinery: `generator_economy`, `integer_code_distance_cert`, `far_pair_separation`, `lattice_cone_certificate` (the WCL/ideal-certificate residue chain; CR-004's (1,5) unit-ideal pivot).

**HIS-ONLY (no node of ours).**
- **(A)** `prob:large-owner` (row-sharp challenge-image bound; binding numeric atom = M31 full-owner factor 9.57219783037) — nearest of ours is `xr_highcore_collision_count` but only template-level.
- **(E)** `prob:mca-exception-routing` — our CONDITIONAL spine's `f1_classification`/`f1_case_pole`/`ext_lift` handle the same exceptional species (pole/tower/extension) inside OUR assembly, but his 31-slope routing statement has no node of ours.
- K1 (legacy-M1 -> GF v4 first-match bridge), M0/M1 (M31 2^-100 stress row), the holmbuar M31 Delsarte/Hahn Lane-L program (#1089/#1098/#1103).

### 1c. Proposed ownership split (both-finish-together)

- **HE CLOSES**: (A) large-owner; (E) exception routing; K1 bridge; K5 certificate emission; Lane M M0/M1; M31 Lane-L uppers. All are his-only objects inside his partition/chronology; we feed ev (our four-pairs replay, M_B weld, regime map already imported both ways) but custody stays his.
- **WE CLOSE**: the DLI block (14 leaves — nobody else can; portfolio-capped per D2), the certificate-machinery chain (4 leaves), `u2_per_row_certifier`, F3 pair (`dsp8`/`hge4` — the DSP8 max-P<=24 satellite cascades three ambers), F6 `rate_half_band_closure`, and the Lane-adjacent list leaves under the crosswalk DON'T-fences.
- **JOINT, DOUBLE-KEY** (neither side flips alone): `f2_growing_order_myerson` == `U_Q` (one proof pays both programs' single deepest wall — the natural co-signed certificate); `l1_mixed_petal_amplification` <-> `U_list_int` via chain #2; (S) <-> XR pair (whoever proves pencil-coverage first, the other replays); `x4_exactlist_staircase_split` <-> `prob:list-completion` assembly; the dossier <-> his package item (3).
- **Division-of-labor consequence**: his four terminal inputs and our 23 math leaves intersect in exactly one identical object (F2==Q). A joint finish = he pays S+A+E (MCA geometry endgame), we pay DLI+certificates+F3/F6 (our assembly's residuals), and F2==Q is paid ONCE with both names on it — consistent with roadmap §1's "pay one shared object once — not grind 20 sieges".

## 2. THE LEAN STORY

### 2a. His state

- **109 Lean packages** under experimental/lean/ (counted from git ls-tree), including: `firstocc_general` + `firstocc_nodup_mem` (the first-match kernels, each with audit notes at experimental/notes/audits/lean_firstocc_*.md), a `grande_finale` package with ~10 `*_CORRESPONDENCE.md` statement-matching files (EXACT_ADJACENT_ROW, FIRST_MATCH_ADDBACK, EXACT_PREFIX_LIST/RAY, PROFILE_COMPILER, ...), `towards_prize` (Mathlib-based), Lane-L wave packages (`l_fixedg_endpoint_plotkin`, `l_postjohnson_contract`, `l_rootlift_bridge`), a large M31 family (`m31_flatness_keystone`, `m31_q_rooted_shell`, quotient/mixing/selector packages — what the M31 PR waves Lean-certified), plus `lean-blueprint.json` / `lean-inventory.json` (a blueprint infrastructure already exists).
- **His Lean policy** (agents.md §7, the priority list our roadmap calls "HIS four-item Lean priority order"): Lean verifies frozen mathematics only; high-value targets = (i) proved local theorems used by live atoms, (ii) first-match/add-back kernels, (iii) endpoint/integer conversion, (iv) final row certificates (+ counterexample correspondence). Explicit stop rule: "Do not formalize Grande Finale v4 end to end while its global inputs remain hypotheses"; an axiom/sorry/skeleton is not a success criterion; certification requires build + manual statement-to-source match.

### 2b. Our state

- /home/u2470931/smooth-read-solomin/prize/formal/lean/rs_mca_formalization: **14 modules, stdlib-only (no Mathlib), Tier-1 integer/rational gate certificates** with BUILD_CERTIFICATE.json and a disciplined CERTIFICATION_MAP.md. Lean-certified: the 17^32 field-count bracket (6·2^128 < 17^32 < 7·2^128 via positive-slack addition witnesses), the F17 staircase endpoints 506/507, endpoint/radius conversions, high-agreement ledger monotonicity, the (7,6,3) sigma_C/EMCA/ECA finite anchors (kernel-checked witnesses). Typed-target non-claims: DeepPointIdentity, ARegularCollapse, BetaTwo conductor, the verifier-bridge Props (semantics live in Python verifiers). This matches his policy exactly: statement-level discipline, no sorry-laundering.

### 2c. Critical DAG -> Lean module structure

The mapping is mechanical because the DAG already has the right shape: **node -> one Lean file** (namespace = node id), **req edge -> import**, **ev edge -> no import** (evidence never enters a proof term), **gate: all -> the node's theorem takes its req-parents' theorems as hypotheses until they are discharged**, **verifiers -> two tiers**: exact-integer replays become `decide`/`Nat`-kernel certificates (his integer-staircase pattern, our Tier-1 pattern — identical conventions already); Python-census-backed facts stay typed bridge Props until re-derived (our SigmaCSparseCensusBridge pattern).

Formalizability split of the 213 proved orbit nodes (measured by verifier/certificate/replay markers in statements+notes+refs): **127 verifier- or certificate-backed (Lean-cheap: exact integers, censuses, replays — the same class both repos already know how to certify) vs 86 analytically hard** (structural/algebraic proofs needing Mathlib finite-field/polynomial infrastructure — the class his GF correspondence files and our typed targets both currently only statement-match). So roughly **60% of the proved critical orbit is cheap**, and the cheap part contains everything his priority items (ii)-(iv) need.

### 2d. Realistic phased plan (consistent with his priorities and both stop rules)

- **Phase 0 (now, ~free)**: statement-level correspondence only. Extend our CERTIFICATION_MAP + his `*_CORRESPONDENCE.md` conventions into a shared file (see §3); no new proof obligations.
- **Phase 1 (cheap kernel)**: the 127 verifier-backed orbit nodes as integer/`decide` certificates, prioritized by his list — first-match/add-back kernels (adopt his `firstocc_*` as the shared base, per roadmap Track-B: "U2 formalization on his firstocc partition base... Lean effort capped ~10%"), endpoint/integer conversion (our Tier-1 already does this for F17; replicate for the four deployed rows), then per-node census anchors.
- **Phase 2 (the co-signed merge point)**: the **input-6 / terminal-input certificates as double-key Lean artifacts** — one package per terminal object (F2==Q, S, A, E, list-completion) containing: the frozen statement, both sides' names for it, the conditional row-closure theorem (his `thm:mca-completion-certificate` shape) with the open inputs as explicit hypotheses — legal under his stop rule because the hypotheses are declared, not axioms smuggled as proofs.
- **Phase 3 (only after flips)**: as each terminal input is actually proved, its hypothesis is discharged in place; the final K5/`thm:exact-completion-certificate` row certificates and our `mca_grand`/`list_grand` close as builds, not documents. The 86 analytically-hard nodes are formalized only where a live certificate imports them — never "end to end" for its own sake.

## 3. CO-FINISH MECHANICS

- **The crosswalk artifact (the one missing machine-readable piece)**: we already have upstream_dag/dag.json (36-node reconstruction of HIS program under our laws — but stale @ ab7721e5, pre-GF-v4, still showing 4 reds incl. the retired BC). Refresh it to the v4 terminal set {S, A, E, list-completion} + K/M/L workboard items, then add a first-class **correspondence file** (proposal: notes/correspondence/JOINT_CROSSWALK.json) with rows `{our_node, his_object(label+file+line), relation: IDENTICAL|OVERLAP|OURS_ONLY|HIS_ONLY|ANALOGY_ONLY, chain: <machine-verified chain ref or null>, owner: US|HIM|JOINT, lean: <module or null>}` — seeded from §1b above and the THREE_VS_SEVEN table, validated by tools/verify_prize_dag.py-style checks. This file is also his submission item (3)'s "formal correspondence files" deliverable — one artifact, both packages.
- **Double-key rule for joint flips**: any status flip on an IDENTICAL/JOINT row requires (a) our replay-before-build banking (exact-integer verifier PASS in-tree) AND (b) his bankability contract satisfied (workboard item, partition digest, quantifier, unit — the §5 YAML block), before either DAG shows green. One-sided proofs of a joint object sit at CONDITIONAL/amber with the other side's key named. This is the existing UPSTREAM_IMPORT_LEDGER law generalized to symmetric form.
- **Cadence**: per existing practice — wave-numbered imports on his pushes (waves 19-22 pattern: fetch, read-only audit, three-way merge of governance files, node-local retrofit per the node-local-notes rule), artifact+site refresh after every DAG update (standing rule), PRs outward carrying his exact packet grammar and naming K/M/L/T items (not the superseded v3 checklist). Suggested steady state: harvest pass per upstream integration wave; crosswalk-file diff attached to every wave audit; Lean Phase-1 modules land with the node that they certify, never separately.
- **What "both finish simultaneously with an all-green DAG" concretely requires**: FROM HIM — proofs of (A), (E), K1, and his half of (S); the K5/M-row certificate emissions; acceptance of the crosswalk file into his dossier. FROM US — the DLI block, certificate machinery, F3/F6/U2 leaves, our half of (S)/XR, and the dossier leaf. JOINTLY — one co-signed proof of F2==Q (which simultaneously discharges his K2/`U_Q` null atom and our summit red, and via proved chain #2 collapses the L1/`U_list_int` pair), after which our 24 TARGETs and his four terminal inputs are all closed, the 38 CONDITIONAL spine nodes (incl. `mca_grand`, `list_grand`, `prize`) discharge mechanically by gate propagation, and the resulting all-green critical DAG — carrying the correspondence file and the Phase-1/2 Lean modules keyed node-by-node — IS the joint proof's dependency graph, ready for Phase-3 formalization module-per-node.

Key paths: /home/u2470931/smooth-read-solomin/prize/dag.json; notes/PRIZE_RESOLUTION_ROADMAP.md (§2 identification discipline); notes/correspondence/{MYERSON_IDENTIFICATION_EXPORT.md,THREE_VS_SEVEN.md,UPSTREAM_HARVEST.md,UPSTREAM_IMPORT_LEDGER.md}; notes/lane_l_crosswalk_20260724/lnl_findings.md; upstream_dag/dag.json (stale, refresh target); formal/lean/rs_mca_formalization/CERTIFICATION_MAP.md; upstream grande_finale.tex ~7528-7640 (terminal inputs), proximity_prize_results_v4.tex §"Interplay", agents.md §§4-7.