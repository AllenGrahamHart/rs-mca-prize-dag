# FINAL CAMPAIGN REPORT — the floor falsification campaign
# (2026-07-07; goal prize/notes/floor_campaign/FALSIFICATION_GOAL.md TERMINATED: all seven floors HARDENED)

## Verdict

**All seven critical-red floor conjectures are HARDENED: each survived ≥ 3
genuinely distinct attack families under pre-registered, immutable
falsification standards. Zero deaths. The hardened frontier is the true
frontier of the prize DAG.** Six classification catches fired during the
campaign — every one against a mis-posed instrument or my own premature
claim, never against a floor — which is the protocol working exactly as
designed.

## The hardened frontier, ranked by evidence strength

1. **F2 u2c EXTRAS-BUDGET** (strongest). Calibrated complete window sweeps
   (gate reproduced the banked 192/64/0 transition digit-exact); the
   engineered norm-selection channel STRUCTURALLY empty (coprime
   multi-condition ideals, positively controlled); complete widest-window
   censuses (6e8 subsets/prime) with the structural count pinned at 700
   exactly and zero extras at every globally-sub-balance prime. The
   dichotomy's regime hypothesis is doing real work and is exactly right.
2. **F7 worst_word ROWWISE-ENVELOPE**. The pre-registered census complete
   (135/135 cells, zero third-class hits anywhere); the K_cell/q^sigma
   envelope holds across a 12x q-ladder with the sigma = 2 window-closure
   reproduced exactly; the adversarial layout-sup (60 layouts) bounded at
   2.4x median. Lemma A already proves exhaustion at official cells.
3. **F1 dli B-WEAK**. Joint scaling study (the 43.9x false signal
   classified to quotient-staircase dressing); engineered stacked towers
   correctly priced (lift r-dilution EXACT: E64 = 1.000000 at every
   engineered row; the forced-window constant identified in-data);
   dual-spectrum population sweep clean (24 rows, median K in [0.6, 1.35]).
   Plus the pre-campaign inheritance: nine adversarial rounds never
   touched the joint object; strongest known attack 2.25 of 122 bits.
4. **F5 xr_smallcore 16n^3**. Spread families slope-limited (= q exactly);
   engineered stacking caps at Theta(n) (dimension count CONFIRMED — also
   a proof lead); strip-integrity spectra collapse at d = 2 for the
   accident population. Margins of 4-6 orders of magnitude everywhere.
5. **F6 rate_half BAND-DETERMINATION**. Composite-stack channel empty at
   the razor (exact big-int, full scale); crossing-fidelity 18/18 through
   the transition; anti-concentration absent across word classes. Caveat
   honestly held: the band itself has NO toy analogue, so the fidelity
   evidence lives at accessible scales plus full-scale bound arithmetic —
   this floor's hardening is the most model-mediated of the seven.
6. **F4 petal PETAL-ESCAPE-BUDGET**. Two-regime top band established:
   at-top realizability saturates to the exact binomial window
   p-independently (= the parameterized paid family, exactly chargeable);
   below-top accidents decay with p; Lemma-13 kernel ceiling clean at
   every row of every instrument. One follow-up surface open (deep-c at
   large p; feasible per-c jobs).
7. **F3 u1_x4 n^3-column** (weakest margins only in the sense of coverage,
   not signal). Boundary sweep + adversarially-smooth fields (incl.
   Fermat 65537) + exceptional-regime confinement all zero-trade with the
   instrument gate exact. Follow-up surface: n = 64 full windows need a
   numpy signature rewrite.

## What the campaign changed structurally

- The floors now carry 21 absorbed attack families total, each with a
  replayable Modal instrument, a pre-registered standard, and banked raw
  results in the node folders.
- Proof leads produced as by-products: the F5 dimension-count argument
  (engineered spread <= O(n/sigma)); the F4 saturated-binomial paid
  family C(core, 6); the F2 structural constant (700) and the
  coprime-ideal obstruction; the F1 forced-window invariant.
- Six classification catches (window-vs-worst-word crossing, per-b vs
  global balance, forced-mass constants, field-semantics of "5005", the
  F6 v1 sign, the C1 freeze) — each now encoded as protocol.

## Prepared Pro material

- DLI-CLOSE-6 remains open with Pro (partially superseded: its A1 ask was
  proved by the S1 norm-sieve; its refutation bars stand).
- The full campaign log + dag are mirrored on the fork
  (FLOOR_FALSIFICATION_LOG.md, FLOOR_CAMPAIGN.md, prize_dag.json).
- RECOMMENDED next briefs (not yet written): one per hardened floor in
  the DLI-CLOSE format, presenting the floor + its survival ledger and
  asking Pro to attack the floor directly or prove it — the strongest
  possible use of Pro now that route noise is stripped.

## Recommended next phase (user decision)

1. **Proof phase on the best leads**: F5's dimension-count and F4's
   saturated paid family look provable with bounded effort; F2's
   entropic-suppression remains the deepest (dli-shaped).
2. **Pro brief stack**: the seven floor briefs above, relayed at will.
3. **Residual surfaces** (non-blocking, listed per floor): F3 n=64
   rewrite; F4 deep-c jobs; F6 A3 amortization exploration (route work);
   F2-A1b b=10 sharding.
4. **The conditional-close question**: with all seven floors hardened,
   re-promoting the ambers that consume them (dli's conditional chain,
   x4's wired set) is now an evidence-backed decision rather than a
   hopeful one — but per the F-round rule, that promotion belongs to you.
