# PRO BRIEF v2 — the F2 SUMMIT (supersedes PRO_FLOOR_2_F2_CRUX.md)
# (2026-07-10, campaign log entries 0-61; every cited fact banked
#  with a digest; the map is TOTAL)

## The single remaining statement

F_q-NATIVE FLATNESS: for the official sub-balance rows (q = p^k
generated field, k ~ 2^16, p ~ 2^31, mu_n with n = 2^40, t ~ 7e10
p-free moment conditions), the t-null census at mid-band interior
block sizes has max-to-mean ratio <= 2^{1.05e12} (any per-condition
loss <= 2^15). Equivalent proved-faces: extras-contraction; zero-
prefix Q at maximal prescription; excess intersection on power-sum
varieties; unit-group q-th powers; divisors of X^n - 1 in fixed
affine families. EVERYTHING ELSE IN F2 IS PROVED.

## What is PROVED (do not re-prove; 14 satellites, digests in dag.json)

- b <= t and b >= n - t: EMPTY unconditionally (squarefree/p-th-power).
- Edge band (t, t + ~315]: extras = 0 — every t-null block is a full-
  coset union (dictionary -> shape -> determination -> inverse-
  complement -> semigroup -> maximality; six links, all verified).
- Full-ladder dictionary (t-null <=> p-free coefficient vanishing);
  residual determination (unique residual per core below char);
  trinomial tower theorem (Z <= 4n^{2/3} unconditional); s-sparse
  extension (struct/extras split built in); chord-orbit lemma;
  arc-class recursion; char-0 six-term classification (Lam-Leung);
  energy dichotomy (printed constants); generalized chord lemma.

## The SEVEN no-gos (fenced routes — do not walk)

1. Raw Parseval/L2 (sqrt(SP) >> budget at the peak).
2. Absolute-value Fourier at mid-band (signed cancellation ~2^93).
3. Teichmuller/Mann char-0 lift (norm blowup, quantified).
4. Extremal symmetry classification (no inversion/negation closure).
5. Global norm/resultant counting (vacuous by 20 bits).
6. Trade decomposition (minimal trades unbounded — exhaustive to h=6).
7. CMMM/Halasz bridge at official k (constant 4 compounds as 4^k;
   VALID at k = 1 prime-field instances, with BGK coefficient
   unstructure — the harvest route for prime-field rows).

## Verified external inputs (citation phase complete)

- Schoen BSG (Combinatorica 35 (2015) 695-701): exponents (1, 4).
- CMMM Thm 1.2 (arXiv:1904.11478): F_p containers, all constants
  printed (exp(2^12 log^2 p); 2^16/(rho sqrt|v|); 4/p; 2^18 log p).

## Empirical ground truth (exact instruments, every cell ever tested)

Census L2-flat (M2/flat 0.91-1.17); extras contract at L in
[0.7, 1.4] until EXACTLY zero; deviations from flat are exactly the
struct census; the falsifier (extras contracting worse than q/2^15,
3 scales) has NEVER fired. The statement is empirically true with
four orders of margin everywhere reachable.

## Attack surfaces for new mathematics, in the campaign's order

1. F_q-native exponential sums on census fibers with the PROVED
   structural decompositions as the major-arc ledger (the campaign's
   exact instruments as the minor-arc oracle).
2. The p-adic ladder (Frobenius level structure) beyond the proved
   edge: level-1 splitting filters (empirically total at every toy).
3. The k = 1 harvest as a model theatre: prove the prime-field case
   end-to-end via no-go #7's valid branch (BGK + CMMM), then study
   what breaks at k = 2 — the minimal instance of the true obstacle.

Full spine: prize/notes/f2_campaign/ (charter, log 0-61, target
brief); dag.json nodes f2_* (14 satellites); all verifiers replayable.
