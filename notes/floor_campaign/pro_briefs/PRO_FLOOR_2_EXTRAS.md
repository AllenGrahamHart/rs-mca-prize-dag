# PRO WINDOW — FLOOR 2 of 7: "EXTRAS-BUDGET" (u2c t-null extras). OBJECTIVE: FALSIFY.

*Context as in window 1: the prize proof is conditional on seven floors;
your job is to kill this one, not prove it. Ledger of absorbed attacks
below; a verified kill re-routes the x4 lane.*

## THE CONJECTURE (kill this)

> **EXTRAS-BUDGET: at the official prize-max rows (globally sub-balance:
> q^t ≥ 2^n, which holds there by ~2%), the count of non-coset-union
> t-null blocks (and their trade families) is ≤ n³ = 2^123.**
> The stronger dichotomy (ZERO accidents at sub-balance rows) is route
> material, not the floor — the consumer tolerates 2^123 extras.

Consumed by: x4 via b2_modp_giant_extras (the giant-extras column bound).

## FAILED ATTACKS (do not re-run; learn the traps)

1. Calibrated complete window sweep (MITM censuses, 5 cells, W from 2^5.6
   to 2^-14.5): instrument gate reproduced the banked 192/64/0 transition
   DIGIT-EXACT at p = 257/577/641; extras IDENTICALLY ZERO through the
   entire sub-balance regime.
2. Engineered accident-selection (your round-5 norm-gate mechanism
   transported; 1440 trials, positively controlled against the known
   p=257 accident): ZERO candidate sub-balance primes even pass the
   multi-condition gcd filter — the per-moment ideals are generically
   COPRIME. The engineering channel is structurally empty.
3. Widest-window boundary censuses (b = 16 at N = 32: ALL 6·10^8 subsets
   per prime, sharded exact MITM): below global balance 640 extras (the
   window population ✓); at EVERY globally-sub-balance prime the t-null
   count is pinned at the structural constant 700 EXACTLY (pure coset
   unions, q-independent) — zero extras.

TRAP (fired once on us): the regime marker is GLOBAL balance q^t ≥ 2^n,
NOT the per-b window C(n,b) vs q^t. A prime can be per-b sub-balance while
a larger-b window is still open (witness p = 21313 at N = 32, t = 2:
32 b=12 "extras" that are relatives of the open b=16 window). Such hits
do NOT count.

## WHAT COUNTS AS A KILL (pre-registered, immutable)

Globally-sub-balance (q^t ≥ 2^n) scaled rows with non-coset-union extras
exceeding the transported n³-scaled budget, SUSTAINED across ≥ 3 scales.
Above-balance window accidents, coset unions, and complementation mirrors
of charged structure do NOT count.

## UNTRIED SURFACES (your best openings)

- CRT-composite constructions: q with q−1 engineered to host MANY
  simultaneous subgroup towers (we tested smooth q at the u1_x4 floor,
  not here; the coset-dictionary's completeness under extreme subgroup
  richness is your most refutable projection).
- t ≥ 5 depths (our exact censuses stop at t = 4; the dictionary's
  multi-level zero-sum clause for non-2-power t* is the least-exercised
  clause in the statement).
- Giant blocks DIRECTLY: our complementation lemma maps them to small
  ones — find a structure the complement map does NOT preserve.

Rules of engagement standard; kill = replayable census + Pocklington
cert; we replay within a day.
