# PRO WINDOW — FLOOR 4 of 7: "PETAL-ESCAPE-BUDGET" (imgfib top band). OBJECTIVE: FALSIFY.

*Context as in window 1. Kill this floor and the image-fiber lane
(imgfib: for ALL received words U, #ImgFib_U(k+σ) ≤ n^B) re-routes.*

## THE CONJECTURE (kill this)

> **PETAL-ESCAPE-BUDGET: at the official rows, for every received word U,
> the full-petal (top-defect band d ≥ M(t−2)) contribution to
> #ImgFib_U(k+σ) fits the n^B image-fiber budget.** NO uniformity-in-c
> claim and NO per-c polynomial claim (the old route's demands — dead,
> irrelevant to the floor).

## FAILED ATTACKS (do not re-run; the two-regime structure is measured)

1. Seven-rung prime ladder (109..6421, all gates PASS): the top band has
   a TWO-REGIME structure — at/beyond the top-defect boundary,
   realizability SATURATES to the full binomial window C(core, 6)
   p-INDEPENDENTLY (this is the parameterized paid family: exactly
   countable, exactly chargeable); below top, realized counts are sparse
   window accidents DECAYING with p (4→0, 2→0, 29→8 at matched cells).
2. Adversarial c-sweep (excess pushed to c = 14): the window grows as a
   BOUNDED-DEGREE binomial (C(15,6) → C(20,6); degree 6 in the excess
   parameter) — absorbed by any c-uniform n^B budget.
3. Lemma-13 kernel-ceiling stress (dim K ≤ c+1), measured concurrently by
   every instrument: ZERO violations at ~100 rows/rungs; the dim-K growth
   sequences are exactly p-independent.

## WHAT COUNTS AS A KILL (pre-registered, immutable)

A sustained super-polynomial top-band full-petal family at scaled
official-like rows BEYOND the saturated binomial column (transported
arithmetic), across ≥ 3 scales — i.e. realized structure that is neither
the saturated paid family nor p-decaying accidents. Below-top Lemma-13
territory and single window accidents do NOT count.

## UNTRIED SURFACES (your best openings)

- Deep-c at LARGE p (our per-c cost scales with p; c ≥ 12 at p ≥ 3209 is
  incompletely measured — if the saturation boundary SHIFTS with p out
  there, the paid family's parameterization breaks).
- Mixed-char/multi-petal amplification (the retracted route's threat,
  never re-weaponized against the floor's weaker form).
- An explicit U achieving super-budget top-band mass at a single official
  parameter shape (word-level construction — all our instruments sweep
  chart cells, not adversarial words).

Rules standard; we replay within a day.
