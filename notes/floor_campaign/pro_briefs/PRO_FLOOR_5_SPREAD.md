# PRO WINDOW — FLOOR 5 of 7: "16n³-SPREAD" (xr small-core remainder). OBJECTIVE: FALSIFY.

*Context as in window 1. Kill this floor and the clean-rate residual gate
(R_post(u,v;A) ≤ 16n³ per pair) re-routes.*

## THE CONJECTURE (kill this)

> **16n³-SPREAD: for every pair (u,v) at the official candidates, the
> post-strip spread remainder — aligned aperiodic supports whose agreement
> sets pairwise share cores < k+t−1, after the quotient/tangent strips
> with dihedral and extension columns counted inside — fits
> R_post(u,v;A) ≤ 16n³.** (The consumer's sufficiency is exact integer
> arithmetic at all six candidates; the 2^122–2^127.9 allowance leaves
> ~2^100 of slack above n³.)

## FAILED ATTACKS (do not re-run; the structural caps are measured)

1. Exact spread measurement (5 rungs, k = 2,3, q to 193; gate = the
   node's own rungs-2a/2b certificate, 38 checks PASS): greedy
   pairwise-small-core distinct-slope families are SLOPE-LIMITED — exactly
   q at every rung (one aligned support per slope), 4–6 orders of
   magnitude under budget, adversarial pencil pairs included.
2. Engineered spread stacking (greedy adversarial construction of (u,v)):
   the engineered maximum saturates at Θ(n) — 0.55n measured across three
   scales — matching the DIMENSION COUNT (each alignment beyond free costs
   σ of the 2n degrees of freedom of (u,v)). This cap looks provable.
3. Strip-integrity depth spectra (line-geometric tangent-core counting):
   random-word tangent spectra COLLAPSE by depth 2 at every scale (faster
   than the q^−2 model); adversarial cascades are the planted lines' own
   binomial dressings — the tangent ledger's charged objects.

## WHAT COUNTS AS A KILL (pre-registered, immutable)

A pair (u,v) at a scaled official-like row with post-strip spread
remainder exceeding the transported 16n³ budget, sustained. NOT a kill:
same-slope mass (the exact-list column), tangent-charged big cores,
FM-band-consistent counts, planted-line binomial cascades.

## UNTRIED SURFACES (your best openings)

- Beat the dimension count: the Θ(n/σ) cap assumes alignments cost
  independent conditions — a construction where alignment conditions are
  linearly DEPENDENT (shared syzygies across slopes) could stack past 2n.
  This is the floor's sharpest untried question.
- k ≥ 4 and larger A (our exact instruments ran k = 2, 3; the core
  threshold k+t−1 grows and the packing geometry changes).
- The six official candidates' SPECIFIC arithmetic (we tested scaled
  analogues; the candidates' exact parameters are where the consumer
  actually evaluates).

Rules standard; we replay within a day.
