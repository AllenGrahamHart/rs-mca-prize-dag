# PRO WINDOW — FLOOR 7 of 7: "ROWWISE-ENVELOPE" (worst-word challengers). OBJECTIVE: FALSIFY.

*Context as in window 1. Kill this floor and the list-side planted
arithmetic re-routes.*

## THE CONJECTURE (kill this)

> **ROWWISE-ENVELOPE: at every official-convention row (bg ≤ 1, the
> max-fill cells), the non-planted low-slack contribution obeys the
> certified upper envelope consistent with the verified count law
> ~ K_cell/q^σ, with explicit lower witnesses where the unsafe side is
> invoked.** (Exhaustion — no third class at bg ≤ 1 — is already PROVED
> (Lemma A); the floor is the envelope, not exhaustion.)

## FAILED ATTACKS (do not re-run)

1. The pre-registered E22 census, COMPLETE: 135/135 cells (17 heavy cells
   finished as 408 sharded jobs), gate PASSED, ZERO unclassified hits
   anywhere; 1,689 heavy-cell hits all classify into the two challenger
   columns.
2. Envelope stress across q (4 cells × 9-point ladder, 97..1153): counts
   track 1/q (count·q constant within 1.5x over a 12x ladder); the σ = 2
   window-closure reproduced EXACTLY (identically zero at every q ≥ 97).
3. Adversarial layout-sup (60 layouts × 2 scalar modes × 3 q, 360 exact
   cells): max challenger count ≤ 2.4x median — the adversarial layout
   space buys a bounded factor, no envelope violation; zero unclassified.

## WHAT COUNTS AS A KILL (pre-registered, immutable)

An official-convention (bg ≤ 1) row family with non-planted challenger
counts exceeding the certified envelope, SUSTAINED across the family (not
one Poisson layout); or a verified UNCLASSIFIED hit (third class) outside
the open-window regime. Small-q open windows (the σ = 2 window closes by
q = 97) and bg ≥ 2 rows do NOT count.

## UNTRIED SURFACES (your best openings)

- σ ≥ 3 at scale (our census ran σ ≤ 3 at n ≤ 64 with the heavy cells
  sharded — the deep-σ envelope constant K_cell(σ) is thinly measured).
- Engineered K_cell inflation: cells whose petal locators share algebraic
  structure with the core locator (the census swept layouts, not
  locator-aligned constructions — a two-column-form challenger FLOOD
  within the envelope's letter would still be priced, but a construction
  breaking count·q^σ boundedness kills the floor).
- The K_cell upper-bound lemma itself (the named remaining kernel): a row
  family where K_cell grows super-combinatorially with the cell shape.

Rules standard; we replay within a day.
