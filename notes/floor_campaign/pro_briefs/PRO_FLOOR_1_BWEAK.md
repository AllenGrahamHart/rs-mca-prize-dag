# PRO WINDOW — FLOOR 1 of 7: "B-WEAK" (dli joint skew-mass budget). OBJECTIVE: FALSIFY.

*Context, printed once in each of the seven parallel windows: the Proximity
Prize DAG now assembles a proof of BOTH grand challenges conditional on
exactly SEVEN floor conjectures — each stated in its weakest sufficient
form (precisely what its consumer consumes, nothing stronger). Your job in
this window is NOT to prove this floor. It is to KILL it. Each floor has
already survived ≥ 3 pre-registered attack families (ledger below); a
verified kill re-routes a lane of the proof and is worth more to us than
any partial progress. If all seven survive your parallel assault, they
graduate to standing conjectures and the conditional proof gets posted.*

## THE CONJECTURE (kill this)

> **B-WEAK: at the official prize rows, q^{−t+H} · W_cen ≤ 2^122** — the
> U-weighted central-profile mass of TRUE valid-skew counts taken JOINTLY
> across the tower (equivalently E_U[Π_j ρ_j] within the endpoint budget).
> NO per-level bound, NO cross-level factorization, NO uniform constant is
> asserted — those are strictly stronger statements, all already dead, and
> their corpses do not touch this floor.

Consumed by: x4_exactlist_staircase_split (1 of its 19 wired inputs; the
packet records "polynomial-loss per level suffices").

## FAILED ATTACKS (do not re-run; learn the traps)

1. Nine adversarial rounds of DLI tennis (you fought most of them):
   strongest hit ever priced = your round-5 engineered prime, 2.25 bits of
   122. Every kill in nine rounds was of a strictly-stronger statement.
2. Joint scaling study (exact nested-tower t-null counts, 3 depths x 5
   q-scales): a 43.9x apparent super-polylog trend CLASSIFIED DOWN to
   quotient-staircase dressing of single Poisson accidents (one size-10
   accident counted 256x). Naive baselines manufacture false kills here.
3. Engineered stacked towers (120 trials -> 10 engineered multi-level
   rows, exact D3 E at both levels): lift structure r-diluted EXACTLY
   (E64 = 1.000000 at every row); measured excess within the lift-aware
   ledger + the q-independent forced-window constant (0.598) at 9/10 rows.
4. Dual-spectrum population sweep (24 rows, full exact dyadic spectra):
   median K(j) in [0.6, 1.35] at every well-sampled level — no dual
   structural surplus. Also dead: the dual-geometric attack (the order
   condition omega^{N-1} = -omega^{-1} lands an exact cosine zero; the F8
   factorization locks production base-2).

## WHAT COUNTS AS A KILL (pre-registered, immutable)

Sustained super-polylog PER-JUNCTION growth of the JOINT loss
E_U[Π ρ] in the packet's own nested toy towers — ≥ 3 increasing q-scales,
≥ 2 tower depths, with the coset/structured component routed through the
packet's EXACT accounting — or any replayable construction whose
transported arithmetic exceeds 2^122 at the official rows.

NOT a kill: factorized/per-level/uniform-constant proxy failures;
single-row window accidents; boundary-volume rows (r-diluted — we price
them at ~0.0006 bits each); conditional-mean composition effects through
exactly-priced components.

## UNTRIED SURFACES (your best openings)

- n' = 64+ joint towers (our MITM machinery caps at n = 32 masks; a
  construction argument at larger n is unexplored territory).
- Cross-DEPTH engineered correlation: one q structuring t = 2 AND t = 4
  towers simultaneously (we engineered cross-LEVEL, not cross-depth).
- The H-term: the budget constant's own arithmetic under adversarial
  profile weights.

Rules of engagement as always: exact rational arithmetic, explicit
certificates (Pocklington for primes), replayable verifier shipped with
any claimed kill. We replay everything within a day.
