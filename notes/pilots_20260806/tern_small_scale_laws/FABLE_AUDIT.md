# FABLE_AUDIT — tern_small_scale_laws (round 19, pilot 1 of 4 to report)

**Auditor:** Fable, 2026-08-06. **Verdict: BANKED — the unification's
EXACT content survives the empirical stress test in its strongest
form (the three dictionaries verify vector-for-vector; the efloor
S-count and the z1 weighted mass are ONE functional, Sct = 2^N(Z-1),
proved by disjoint code paths), while its HEURISTIC content is
refuted in two identified places with exact deviation laws, and the
round-18 anomaly is fully closed by the new LEMMA TWT — with the
mechanism proved INSTANCE-LOCAL, establishing that the instances
share the object but sample disjoint prime strata.**

Replay: verify_ssl.py 122/122 exit 0 (coordinator re-run; the
pilot's staged total 412/0 per its persisted log). Licensing
controls all reproduce banked numbers exactly (the efloor 16-cell
row incl. 6560/0/16640/148224; CATCH-Z6's three composite counts by
a different route). Report persisted verbatim (recovered by task-id
from the transcript per the hardened bank rule).

ADOPTED:
- **The dictionary layer (the unification's surviving core)**: D1
  (LEMMA STRAT: I3's binding stratum IS I2, coordinate for
  coordinate), D2 (I1 at R = 1 = I2), D3 (Sct = 2^N(Z-1) — the two
  pilots' functionals are one, exact). This is the STRONGEST
  possible empirical outcome for a statement-level unification.
- **LEMMA TWT + the SELF-ORTH predicate** (self-orthogonal + ternary
  => p | wt; predicate sharp, 83 cells 0 violations, 13 negative
  cells clean) and the complete anomaly ledger (155x = 4.9x TWT x
  32 orbit-quantization; 0.709 expected orbits; the weighted
  convention was never anomalous). The mandate's candidate list was
  incomplete — the pilot found the missing mechanism. Standard
  coding theory honestly subtracted; the identification, predicate,
  and ledger are new.
- **CATCH-19A** (the orbit constant does NOT transport: negacyclic
  iff all-odd T, cyclic iff all-even, NEITHER for I1's mixed-parity
  windows at R >= 2 — orbit size 2, not 2N; LEMMA ROT's 2L factor
  is I2/I3-only; onset mispredictions 2 -> 0 with the
  instance-correct constant).
- **CATCH-19C** (the (3/2)^N weighted/unweighted convention gap —
  "flat model" claims are not cross-pilot comparable until the
  functional is named; a standing reporting rule from here).
- **CATCH-19D** (disjoint prime strata: SELF-ORTH needs |T| >= N/2,
  I1 structurally forces split primes where |T| = R; the three
  instances sample non-overlapping (ord, |T|) strata — a law
  measured on one grid does not transfer without controlling for
  ord_{2N}(p)).
- **CATCH-19B WITH A COORDINATOR SCOPE CORRECTION**: the shift-0
  integer layer (s = 0 in T makes the condition Sigma v_i = 0 over
  the INTEGERS, p-independent; count = T(N) - 1 exactly, central
  trinomial) is REAL and is CATCH-Z6's second door — it fires at
  2-power lengths whenever the window contains s = 0. It
  contaminates (i) this pilot's consecutive-window miniatures at
  a = 0 and (ii) ONE QUARTER OF z1's 696-cell calibration grid
  (a in {0,1,2,3} — verified at z1 PROOFS §7; addendum to the z1
  audit written this bank). HOWEVER the pilot's L4 line "a 2^44
  excess ... at the official I1 row" is MIS-SCOPED: the official
  window is the ODD set {2r+1} and NEVER contains s = 0, and the
  integer-layer mechanism has no odd-s analogue (only s = 0 gives
  bounded integer sums). The official object is NOT affected; the
  correction bites calibration grids, not the prize rows. The L4
  scaling row is re-labelled accordingly of record.
- The scaling verdict as corrected: the exact structure is
  scale-free; TWT dies for I1 at scale (R << N/2, five orders); the
  orbit gap grows as log2 N (bites the near-balance accident-zone
  functionals); the shift-0 layer is a CALIBRATION hazard only.

HONEST RESIDUALS accepted: the pilot's own P1 null missed (reported
as CATCH-19B, the discipline working); 2N = 64 unreached (honest);
**the p = 7, w = 4 484x OVER-representation** — a second,
opposite-signed mechanism TWT does not explain, the round's best
new lead (9 orbits where 0.019 expected; something CREATES ternary
codewords there); TWT certified per cell, not n-uniform; p = 3
degenerate. Process clean; sibling never read.

Coordinator note toward the round verdict: this pilot delivers the
EMPIRICAL half of the graded verdict the adversary sibling is
building formally — the unification survives at the OBJECT and
DICTIONARY level, fails at the TRANSPORTED-HEURISTIC level, and its
instances live on provably disjoint parameter strata. Reconcile
with the adversary's A1-A4 at its bank.

## ADDENDUM (2026-08-06, coordinator, from the round-19 adversary bank)

CATCH-19A is RE-SCOPED: the official I1 window of record is ALL-ODD
(z1 PROOFS:87-94, Lambda = {odd l <= t} from l = 1), which IS
negacyclic-closed — the 2N orbit constant STANDS for the official F2
object. The collapse-to-2 applies to THIS pilot's consecutive-window
miniature convention (T = {a..a+R-1}, mixed parity) only — the second
scope slip from the same root cause as the shift-0 projection (the
I1-mini dictionary differs from the official window). The MECHANISM
(negacyclic iff all-odd T) is confirmed and banked; it is what makes
the official window choice load-bearing.
