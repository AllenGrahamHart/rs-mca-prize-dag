# FABLE_AUDIT — tern_unification_adversary (round 19, pilot 4 of 4 — THE GATE)

**Auditor:** Fable, 2026-08-06. **Verdict: BANKED — the graded verdict
is ADOPTED OF RECORD: the ternary unification SURVIVES at the OBJECT
grade (proved: one negacyclic code, all-odd shift-0 window), at the
REGIME grade as satisfiability (THEOREM SR: an explicit shared
admissible 256-bit prime row for all three instances — the exact
opposite of (ES)'s mutual unsatisfiability; NO shared discharge), and
at the METHOD grade as scaffold (transfers are verbatim but every
instrument is provably vacuous or insufficient at its target's tau);
it is KILLED as a single-theorem unification by THEOREM PT — the
PHASE TRANSITION: I1 sits exactly at the ternary counting threshold
tau = 1 (supercritical: only a MASS bound can be true) while I2/I3
sit at tau = 2 (subcritical: EMPTINESS is the target), the coordinate
reproducing four banked constants it was not fitted to. CATCH-Z1's
mass re-pin was FORCED by sign(Tcrit), not taste.**

Replay: verify.py 54/54 exit 0 (coordinator re-run under ramguard
local; two self-corrections during the pilot's run, both caught by
its own fail-closed gates and disclosed — a Decimal-floor ceil bug
and a vacuously-passing mixed-parity test). The open-conflict anchor
verified by the coordinator: z1_ternary_mass/PROOFS.md:87-94 — the
official I1 window IS the all-odd Lambda starting at l = 1.

COORDINATOR ADJUDICATIONS:
1. **The open conflict (residual 7) is ADJUDICATED**: the official
   I1 window of record is ALL-ODD (z1 PROOFS:87-94 + the banked
   Lambda = {odd l <= t}), so the negacyclic closure holds and the
   2N orbit constant STANDS for the official I1 object. ssl's
   CATCH-19A is RE-SCOPED to its own consecutive-window miniature
   convention (T = {a..a+R-1}, mixed parity) — its second scope
   slip after the shift-0 projection, same root cause (its I1-mini
   dictionary is not the official window). Addendum to the ssl
   audit written this bank. The mechanism (negacyclic iff all-odd)
   is confirmed by both pilots and is what makes the official
   choice load-bearing.
2. **CATCH-19-ADV-4 ACCEPTED — I4 is demoted**: LEMMA TC is a
   crossing lemma and 3^L is the ambient cardinality of I2's cube,
   not an independent instance. THE UNIFICATION OF RECORD HAS THREE
   INSTANCES. My round-18 four-instance phrasing (ledger + memory)
   is corrected here; the instance-inflation parallel to "(ES)
   discharges all four consumers" is exactly the failure mode the
   user's attack-first directive was for.
3. **CATCH-19-ADV-5 (quarantine porosity) — STANDING RULE ADOPTED**:
   during blind rounds, quarantined pilots must not read the
   CAMPAIGN_LEDGER sections dated within the round (the ledger
   leaked the ssl/builder headlines into the adversary's subtraction
   sweep). Timeline verified honest: all A1-A4 work and every
   verify.py number predate the sweep; the -46.0249/+17.9751
   reproduction is a GENUINE blind convergence (three independent
   derivations now: builder, adversary, and the banked z1).
4. **The CRITICALITY-COMPATIBILITY gate is ADOPTED** as a standing
   third test for any future unification claim (alongside the
   shape-pun and regime tests): do the instances lie in a common
   tau-interval where the instruments are non-vacuous?
5. **COROLLARY PT-2 goes to the WATCH LINE**: the crossing
   bracket's proved lower endpoint w = 2^34 clears the ternary
   counting threshold by only 0.336 bits — one step below (v = 33)
   the deep stratum would be supercritical at RECORDED PRIME rows.
   The RHL-LB floor is load-bearing by a third of a bit. Any future
   change to the bracket's lower end must re-run this check.
6. CATCH-19-ADV-1/3 accepted as brief defects (mine): the unsourced
   I1 p-range (wrong in the direction FAVOURABLE to the
   unification — the shared row exists at e = 1, log2 p ~ 256);
   disanalogy (iii) false (I1's mass IS a count of binary pairs —
   PROPOSITION FIB; the real split is constant-weight vs full-cube,
   which is exactly where the phase transition lives).
7. CATCH-19-ADV-2 accepted: CATCH E-3 re-labelled a SHARED-ROW
   property (the v_2(q-1) >= 41 gate blind-spots SP-COVER in all
   three instances simultaneously) — es_ternary node addendum
   queued for mint-4b.

ADOPTED THEOREMS: SR (the shared row, Dirichlet + MR exhibit), PT
(the phase transition; only tau < 1 proved, first-moment side
labelled heuristic — same honesty class as the banked re-pricing),
PROPOSITION FIB (the mu-preimage schema), LEMMA ZB (CATCH-Z6's
counts reproduced from the cyclotomic-degree argument), the
Z-FLOOR≡DSA identification (+ support control), the Z-1 -> I3
transport (min ternary weight >= w+1 — new to the repo, a real
factor-2, seven orders short of CC-sparsity — banked as a minor
instrument), and the transfer matrix (to be reconciled with the
builder's constructive matrix: they agree cell-for-cell where they
overlap; the adversary adds the vacuity column).

HONEST RESIDUALS accepted, one elevated: **the untested cell —
I2's constant-weight fibration against a constant-weight Z-FLOOR —
"the one place a genuinely new instrument might live"** (residual
6): the round-20 candidate. Also: Tcrit first-moment scope; the MR
probable prime (existence by Dirichlet regardless); the unproved
ternary-CS extension used only for a vacuity verdict that holds
either way. DRAFT-ONLY confirmed.

THE ROUND-19 GATE DECISION: the master node IS MINTED, in the
adversary-graded form — the object, dictionaries, spine, MT, PT,
SR, and the per-tau targets (mass at tau = 1, emptiness at tau > 1)
— explicitly NOT as a single-theorem target. Executed this bank as
tern_master_threshold.
