# FABLE_AUDIT — tail_count (round 20, pilot 3 of 4 to report)

**Auditor:** Fable, 2026-08-06. **Verdict: BANKED — the terminal is
re-shaped a second time, and honestly: CATCH-T1 kills the node's
recorded Prop-10 lead (it TELESCOPES to the elementary cost form —
no Dedekind content; THIRD forced correction applied to
f2_z1_mass_knife_edge this bank, dag recompiled, verify PASS); the
criterion is restated FREE OF CONSTANTS (Pr[P >= 2^{cS}] <=
2^{-cS+o(S)} — the +46.02 was exactly the saturation constant, and
the knife edge is re-identified as the c = 1 slack, a beautiful
tail-side cross-validation of the banked constant); THE BINDING
LAYER IS PINNED at c* = 1/ln 2 - 1 = 0.4426950409 where the flat
model saturates with ZERO margin (measured at 0.45 on every
resolvable row); the tail is proved to BE a small-values/box count
of the MDS value code (the structure theorem the brief asked for);
one endpoint layer proved outright; both standard supplies killed
with computed thresholds (the Z-2 moment route recovering Corollary
8's p <= 8.30 layer-by-layer; the interpolation route dead at EVERY
p, killed by position entropy); the common deficit NAMED —
R-LOCALITY, short by the factor 8.60, with the Fourier escape
proved circular. LEAD 2 is FULLY EXPLAINED: 288 = 16 + 16 + 16^2 by
decimation rank-collapse x TWT x composition (the 484x ledger
closes to a 2.6% Poisson residual), and THEOREM 14's decimation
dichotomy — forced by the saturation pin itself — proves the
mechanism CANNOT operate at F2 parameters. A candidate refutation
of the terminal is thereby removed; the terminal is not proved.**

Replay: verify_tail.py 111/111 exit 0 (coordinator re-run).
Licensing controls reproduce the banked Z_1 values (1.25 / 9.387207)
and the banked I3 row + 288-cell by disjoint code paths.

ADOPTED: THEOREM 2 (the telescoping — the collapse is a GAIN: the
cost form enables everything downstream); THEOREM 3/COROLLARY 4
(the normalized ledger + the knife-edge re-identification);
THEOREMS 5/6 (exact R-wise independence via MDS — the trap evasion
stated exactly: a Vandermonde degree fact, no distance theorem, no
count); THEOREM 9 (the structure theorem); THEOREM 12 (the endpoint
layer, honestly labelled "endpoint, not progress into the bulk");
THEOREMS 10/11 (both supplies dead with thresholds — 10's diagnosis
is new: position entropy H(1/L) > 1/L kills it at EVERY p, strictly
worse than Corollary 8); COROLLARY ZM (c* — THE TERMINAL'S EXACT
HARD POINT: no layer can be given away, no per-coordinate loss
survives); §7 (the R-locality deficit, correctly labelled a
structural deficit NOT a no-go; the Poisson-duality circularity a
structural observation); THEOREM 13/14 (LEAD 2 closed + the
transport impossibility, with CATCH-T2's structural symmetry:
creation and discharge die at the same p <= O(1) shape — small-p
over-representations are NOT prize-row danger signals). The T3
measured tail table banked (E_nz < 0 everywhere except one G4 cell;
binding layer at 0.45 thrice; no growth with S).

CATCHES: T1 (the node correction — applied); T2 (banked); the
Corollary-4 re-identification; Theorem 10's diagnosis. HONEST
MISSES reported by the pilot and accepted: P6/H1 (the generator is
not ternary — the X^2-structure is consequence not cause), P7's
reason (right verdict, wrong registered mechanism), the A7 grid row
mis-specified and declared UNREACHED (S is forced by p). The
pilot's residual 8 is the terminal's sharpest honest sentence:
"c* = 0.4427 is a FLAT-MODEL statement. The true rate function at
the official row is unknown — that IS the terminal."

BOARD EFFECT (mystery 2, composing with f2_repose's bank): the
terminal is now ONE question stated three equivalent ways — the
finite window Z_1 <= 2^{22.75} (under (C)), the normalized tail at
the single layer c* with zero flat margin, and the true rate
function of the MDS value code's small-values count. Every standard
instrument is dead with a computed threshold; the named deficit is
R-locality (factor 8.60); no route is named. The leads: a
non-R-local instrument, the constant-weight cell (crossing_gap),
and the fence/contract reconciliation (which decides whether the
finite window is even the right target). DRAFT-ONLY confirmed;
quarantine held; process clean.
