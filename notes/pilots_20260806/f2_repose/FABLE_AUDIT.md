# FABLE_AUDIT — f2_repose (round 20, pilot 2 of 4 to report)

**Auditor:** Fable, 2026-08-06. **Verdict: BANKED, MAINTAINER-LEVEL —
the lane is rebuilt and the terminal COLLAPSES TO A FINITE TARGET
under calibration (C): the consumer's tolerance is PROVED to be the
counting-balance surplus t·L − N (CATCH-R1, THE SEAM'S FIFTH FACE —
reproducing the banked 2^{1.05e12} to 0.047% from the consumer's own
sentence), and the exact-first-moment identity turns (O1) into
**F2-MASS-N^3: Z(L) <= 1 + N^3, i.e. Z_1 in a 4.77-BIT WINDOW
[2^{17.98}, 2^{22.75}]** — open, not refuted, with Z-FLOOR and the
knife edge both INSIDE it. Under the ruled (T*) the lane has NO
candidate at generating rows (CATCH-R6: an EXACT-VALUE obligation,
unreachable by upper bounds as a class). The non-generating scope
question DISSOLVES: the consumer's own banked CATCH #11 rule routes
extension rows through the f1/ext descent — the "generates F"
hypothesis was supplied internally all along, and the k = 1 class
reduces verbatim to the PROVED f2_k1_contraction_theorem. Named
residual: k in {2,4}.**

Replay: verify.py 60/60 exit 0 (coordinator re-run). The CATCH #11
consumer-rule anchor verified at the u2c node ("consumes ONLY the
count ... <= n^3 = 2^123" — the contract's source sentence). The
pilot's independent five-class verification (S14: p = 2^61 - 1
generating and admissible, e_p = 1 outside the banked census)
CONFIRMS the wave-47 G1 refutation by a third route.

ADOPTED:
- **THEOREM C1 + COROLLARY C1.1 (the derived contract)** with its
  honest scope (DERIVED not quoted; the 0.047% reproduction is the
  evidence; A1-A4 are the live fork if the reading is rejected).
- **THE FINITE TARGET F2-MASS-N^3** as the lane obligation of
  record UNDER (C), with its pre-registered falsifier (an
  admissible generating row with Z_1 > 2^{22.75}), and the honest
  gap statement (THEOREM 7 misses by 9.79e11 bits — the exponent
  constant must shrink 1.08e10x).
- **CATCH-R6** (under (T*): exact-value obligation — the strongest
  form of the (T*)-side kill) and the R2 triage (median REFUTED as
  sufficient — PP5.0 = SUM makes the consumer's object the exact
  first moment; C-S refuted at its own falsifier; partial windows
  CONDITIONAL on a PP5.0 Hoelder statement that does not exist).
- **THE SCOPE DISSOLUTION (R3)**: CATCH #11 (banked 2026-07-07) is
  the consumer-side k = e rule — the F2 lane never owed the
  non-generating rows; they route through f1/ext. THE RIGHT SPLIT
  IS k = 1 vs k >= 2, not generating vs non-generating. The k = 1
  reduction to f2_k1_contraction_theorem (PROVED, critical) covers
  the killer exhibit row and the whole e = 1 class, BYPASSING (O1).
  CATCH-R4 accepted: our Z-3/ADM-B non-generating kill was a
  RE-DERIVATION of CATCH #11 at a different scale (subtraction hit
  against rounds 17-18 — recorded).
- **CATCH-R2 (consumer-chain arithmetic defect)**: the banked
  2^{1.05e12} tolerance computed at n = 2^40 vs the same node's
  N = 2^41 budget pin — corrected, the tolerance flips NEGATIVE by
  4.90e10 bits (= BRIEF5's 49.5G threshold; two banked numbers, one
  error). Mint-queued as a node addendum on the extras-contraction
  target.
- **CATCH-R3** (the D5 framing corrected: the SUM reading converts
  (O1) into a finite certificate rather than busting it) and
  **CATCH-R5** ((O1) was UNDER-posed — the chain's own fence says
  o(n) is not a certificate).
- THEOREM D (the exact descent, 89,252/0) with the pilot's OWN
  verdict against my briefing hint: the descent explains the
  non-generating kill but does not answer R3 (fixed-Lambda vs
  ambient-balance t) — my hint was wrong, the pilot's derivation
  governs. Branch-robustness verified: C1/C2/C3/D use only Lemma 1
  + balance + ceil(t/2) + ADM-3 — the contract covers all FIVE
  classes; the minus-branch generating types have the target but NO
  OBJECT MODEL yet (named residual).

**COORDINATOR RE-ADJUDICATION — THE ENSEMBLE RULING IS RE-OPENED.**
My 2026-08-06 ruling fixed the ensemble = (T*) on CATCH-G's fence
analysis. This pilot's A1 pricing shows the consumer's OWN TEXT is
mixed (the mean is slice-calibrated; the window-empty test is the
2^n balance — both in one sentence), and the two readings now
differ not by margin but by EXISTENCE of a candidate (+91 bits
finite target vs -9.68e7 bits exact-value death). CATCH-G analyzed
the (O1) => (O2) link; this pilot analyzed the chain to u2c's
extras budget. The ruling is DOWNGRADED from "of record" to
"provisional pending the fence/contract reconciliation" — a named
task: determine which link's demand governs, or whether the chain
is genuinely inconsistent (in which case the chain itself needs the
repair, which the A2 defect already suggests). Recorded in the
rulings addendum this bank. The (C)-side finite target is the
lane's working obligation meanwhile (conservative: it is the
harder-to-satisfy direction for claims of progress, and both sides'
instruments aim at it).

HONEST RESIDUALS accepted, led by the pilot's own: its headline
pre-registration (B1: the contract is looser) was REFUTED at its
own falsifier — "the one hypothesis I took real risk on is dead" —
the fourth consecutive honest self-refutation this campaign and the
first where the prereg took genuine risk. Nothing bounds Z_1;
PP5.0 remains the largest gap; k in {2,4} uncovered by every route.
DRAFT-ONLY confirmed; quarantine held; no post-marker reads.
NODE-DRAFT (f2_consumer_contract_repose) delivered — MINT after the
wave-47 integration lands the five-class correction (they compose).
