# FABLE_AUDIT — z1_ternary_mass (round 18, pilot 4 of 4)

**Auditor:** Fable, 2026-08-06. **Verdict: BANKED — the terminal is
re-shaped, not closed: THEOREM Z-1 (the DLI distance law transports:
min ternary weight >= 2R+1 — CONVERGENT with the blind adversary
sibling's THEOREM D1, same constant, different derivations);
THEOREM Z-FLOOR (the first moment is a POINTWISE floor,
Z(L) >= 2^m/p^{dim L}, one Cauchy-Schwarz from a banked identity
nobody had drawn, tight within 2x); THEOREM Z-3 ((O1) FALSE at the
OBJECT level on every k < e row — f2_adm CATCH-1's exponent
reproduced exactly by an independent route); and THEOREM Z-NOGO (no
distance+counting bound can EVER close SL-1b' — the family needs
p <= 8 against an admissible floor of 2^39). At k = e the floor is
silent by 46 BITS OUT OF 2.75e11 — one Lambda condition, worth 64
bits, with the two t-readings straddling zero. SL-1b' is a knife
edge with exactly one live route (Weil-type products over the
2^39-subgroup, factor-2 headroom).**

Replay: verify.py 81/81 exit 0, digest Z1_TERNARY_MASS_ALL_PASS
(coordinator re-run under ramguard local). Anchors verified: the
banked collision identity at dli_c1_l1_block_owner_ledger
/statement.md:15,18 (the Z definition + identity lines, verbatim);
the CATCH-Z2 target passage at f2_sl1b/PROOFS.md:288-291 (the
random-subspace scoping, exactly as quoted).

CROSS-PILOT RECONCILIATION (blind pair, the round's cleanest
convergence): Z-1 and the adversary's D1 transported the same DLI
law independently — same threshold 2R+1 = 8,589,934,681, different
hypothesis-verification routes (four-hypothesis match here; the
elementary e_p case split there). Both carry the shift-0 scope
caveat; this pilot adds the 43 shifted counterexamples (transport
legitimate ONLY because the official window starts at 1). The
adversary's heuristic Z_1 ~ 2 is consistent with this pilot's
measured at-or-below-random-median calibration.

ADOPTED:
- **THEOREM Z-FLOOR** (subtraction exemplary: identity banked,
  inequality new), **THEOREM Z-3** (the k < e object-level kill —
  CATCH-Z4's upgrade of f2_adm CATCH-1 accepted), **THEOREM Z-1**
  (with scope), **THEOREM Z-2** (the l1 extension, a genuine gift
  BACK to the DLI lane — CATCH-Z5 queued for the DLI node addendum),
  **THEOREM Z-NOGO** (strengthened to p >= 8), the discharge ladder
  (39.2x -> 28.3x -> 21.3x, honest), the route triage ((a) dead
  quantified, (c) localises only, (b) THE route with its obstruction
  sized).
- **CATCH-Z1 (MY brief defect, accepted)**: the mass form and the
  exact-zero form are NOT equivalent (they differ by log2 3 per
  vector); at the admissible object the MASS form survives at k = e
  while the exact-zero form dies heuristically. THE TERMINAL OF
  RECORD is hereby pinned to the MASS form: prove Z_1 <= 2^{o(m)}
  at k = e. (The f2_sl1b REPORT's "equivalently" gloss carried into
  my brief — addendum to that audit written this bank.)
- **CATCH-Z8 + the (Z4) conditional**: the knife edge — under the
  exact-balance t-reading the floor FIRES (+17.98 bits: ternary
  kernel vectors PROVABLY EXIST at the witness row, killing the
  exact-zero form outright) yet Z stays 2^{o(n)}, so (O1)'s mass
  target survives either way at k = e. The reading question is the
  t_naming/ensemble stack's — a fourth face of the same maintainer
  seam (CATCH-Z3 extends the D5 seam identity).
- CATCH-Z6 (self-caught grid contamination: only 2-power 2N rows
  are valid miniatures — a scope rule all future toy grids must
  carry) and the Z-A10 partial self-falsification (packing constant
  corrected) — the discipline working.

THE ROUND-18 CONVERGENCE, SEALED: all four pilots landed on TERNARY
VECTORS IN p-ARY CODES FROM CYCLOTOMIC WINDOWS as the primitive —
the crossing deep stratum (epsilon in {0,±1}^L, LEMMA TC), the
E_floor engine (LEMMA AB, half-length cyclic), the F2 terminal (this
pilot's whole object), and the adversary's minimal surviving form
(E[T] = 2^{n/2}·Z_1^e). The round-19 unification candidate is posed:
ONE ternary-in-code question family, three instances, with the
proved instruments (Z-FLOOR, Z-1/D1, SP-COVER/SP-UNIFORM, CS, DSA)
as its partial answers. To be drafted as the board's new statement
of record after the maintainer scope answers.

HONEST RESIDUALS accepted (SL-1b' open at k = e; the 2^{17.98}
firing reading-dependent; shifted law open with a thin sample; toys
are no evidence at the official row; route-(b) sizing explicitly
back-of-envelope; C <= 4 inherited). Process CLEAN (second fully
clean pilot; Edit/Write used for all patching). DRAFT-ONLY
confirmed; sibling never read.
