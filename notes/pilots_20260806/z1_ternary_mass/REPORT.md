(Coordinator-persisted verbatim from the pilot's final message, 2026-08-06.)

# PILOT REPORT — Z_1 ternary mass: SL-1b′ on the admissible object (round 18, `z1_ternary_mass`)

**Opus 5, 2026-08-06.** Replay: `tools/ramguard local -- python3 notes/pilots_20260806/z1_ternary_mass/verify.py` → **81 checks, 0 FAIL**, exit 0, digest `Z1_TERNARY_MASS_ALL_PASS`. Artifacts: `/home/u2470931/smooth-read-solomin/prize/notes/pilots_20260806/z1_ternary_mass/{PREREG.md (brief + my appendix Z-A1…Z-A14), PROOFS.md, verify.py, VERIFY_LOG.txt}`.

## HEADLINE

**The crosswalk pays — and what it pays is not the mass.** The DLI hypothesis does now hold on the admissible object (`p = 1.845e19` against a maximum possible weight `S = 2.749e11`, margin `6.71e7`), so the stronger distance law transports: **every nonzero ternary dual vector has weight ≥ `2R+1 = 8,589,934,681`**. But the transported law moves the *mass* by a factor `1 − 2^{-1.743e11}` — the weight floor sits at `S/32` while the mass lives at the binomial peak `S/2`. The real result came from the other direction: **the first moment is not a heuristic but a pointwise floor**, `Z(L) ≥ 2^m/p^{dim L}` for *every* subspace, one line from a banked identity nobody had drawn. That floor **refutes (O1) at the object level on every admissible row with `k < e`** (reproducing f2_adm CATCH-1's `2^{5n/12}` exactly by an independent route), and on the surviving `k = e` rows it is silent **by 46 bits out of 2.75e11** — one condition of `Λ`. SL-1b′ is not a wide-open problem; it is a knife edge, and I prove no bound in the distance+counting family can ever settle it.

## VERDICTS — pre-registered deliverables

**(Z1) The crosswalk — DELIVERED; the hypothesis HOLDS and the theorem transports.** All four DLI hypotheses match exactly: `char > w` (`p/S = 6.71e7`, versus `7.75e-3` on the KoalaBear tower — the flip is real); `ω` of exact order `2N = 2^39` (`v_2(p−1) = 39` exactly); `N = 2^38 = S`, so the DLI exponent range `{0..N−1}` *is* the half-system; and the official `Λ = {odd l : l ≤ t}` starts at `l = 1`, matching "the first `ell` odd powers" with `ell = R`. **THEOREM Z-1: min ternary weight ≥ 2R+1**, double SL-1's characteristic-free `R+1`. **Scope honesty (load-bearing):** the shift is essential — 43 grid configurations with a shifted run and `char > w` violate `2R+1` (smallest `2N=12, p=13, R=1, a=1`: min weight 2 < 3), and f2_sl1b's own smallest witness (`Λ = {5,7}`, i.e. `a = 2`, min weight 3 < 5, `char 7 > 3`) is one of them. The transport is legitimate only because the official window starts at 1.

**(Z2) The mass bound — DELIVERED in both directions, and the terminal is re-shaped.**

- **THEOREM Z-FLOOR (the new instrument).** For every `F_p`-subspace, `Z(L) = Σ_{ε∈L^⊥∩T} 2^{−wt(ε)} ≥ 2^m/p^{dim L}`. Proof: the banked collision identity `Σ_s|F_s|² = 2^m Z(L)` plus Cauchy–Schwarz over the ≤ `p^{dim L}` syndromes. **Subtraction:** the identity is banked (`dli_c1_l1_block_owner_ledger/statement.md:15,18`), and the inequality is literally the non-negativity of the banked fibre-variance identity at `BRIEF1_PRO_DOSSIER.md:47,52` — *nobody had drawn it*. Verified: 696 configurations, exact rationals, 0 violations; it strictly beats the banked `Z ≥ 1` on 92 of them, by up to `675.6x`; and it is **tight** — the ensemble mean exceeds it by at most a factor 2, so no subspace can beat the random baseline by more than 2x.
- **THEOREM Z-3 (the dichotomy).** `Z(L) ≥ 2^{m(1 − (k/e)(tL/n))}`. At `k < e`, the counting balance gives `Z ≥ 2^{m(1−k/e)} = 2^{Θ(n)}`: **(O1) is FALSE, unconditionally, at the level of the object rather than of a necessary condition.** At f2_adm CATCH-1's row this is `2^{5n/12}` — *exactly* the banked excess, by a fully independent route (and `2^{n/6}` under the looser reading).
- **THEOREM Z-2 (new, reported back to DLI).** The Newton short-window exclusion extends verbatim to **integer** coefficients with `w` read as the `l1` weight `Σ|c_e|`; the `{+1,−1}` restriction is unnecessary. This is what licenses `l1` separation `‖c−c′‖₁ ≥ 2R+1` between ternary codewords, hence a sphere-packing bound.
- **The discharge ladder, quantified:** `(M3)` banked needs `R/S > 0.613147` (39.24x short) → with the DLI transport `0.442114` (28.30x) → with `l1` packing `0.333333` (21.33x). Total gain `1.8394x`; still short by 21x.
- **THEOREM Z-NOGO.** Saturation *pins* `R/S = 1/log2 p`, so the whole distance+counting family discharges only if `p ≤ 8`, while every admissible row has `log2 p ≥ 39`. **The family is structurally incapable of ever closing SL-1b′.**
- **Routes as registered:** (a) norm sandwich **DEAD, quantified** — it yields `w ≥ p^{2R/n} = 2.0000`, dominated by `2R+1` by `4.295e9` (and its `Z[ζ]` Claim 4 needs `p > w^{2^37}`, i.e. only `w=1`); (c) `C ≤ 4` localises but cannot narrow (floor and ceiling both scale with `C`); (b) **the only live route**, with its obstruction sized: square-root cancellation on a subgroup of size `2^39` inside `p ≈ 2^64` is `√p·log p = 2^38` — a factor 2 of headroom — and the mass needs a *product* over `2^38` points.

**(Z3) Calibration — DELIVERED, 696 configurations (201 live), and it found a defect in my own grid.** Exhaustive `3^N` for `N ≤ 8` plus meet-in-the-middle for `N ∈ {10,12,16}`, the two agreeing on every `N ≤ 8` row. On the 14 saturated rows (the miniatures of `k = e`) the measured mass is **below the random mean on all 14** (`Z/E_rand ∈ [0.502, 0.947]`, median `0.769`) — Z-A13 clause 2 fires. **The mechanism is exactly the transported weight floor:** the first moment restricted to weight `> 2R` cuts mean absolute error from `0.1236` to `0.0789` (1.57x) and is then **unbiased** (signed residual `−0.0095`, 51.6% below) — so the deficit *is* the excluded low-weight mass and nothing else. Against the *ensemble* (400 random codes per point, since a mean is not a median), the deployed code lands at percentiles `[0.0%, 47.0%]` — **at or below the median at every valid miniature**, confirming the lane's "no worse than a random subspace". **Honest null: "better than random" is NOT established** — the near-extremal look of the first four rows was small-sample selection, self-caught by widening the prime list.

**(Z4) The conditional of record — DELIVERED.** *If* the admissible row has `k < e`, (O1) is dead by counting alone (no conjecture). *If* `k = e` — the only surviving rows, which f2_adm already showed need a hypothesis the rules do not supply — then: the unconditional floor is silent, by `−46.02` bits under the banked `R = ceil(t/2)` reading and **firing at `+17.98` bits under the exact-balance reading**, in which case `Z_1 ≥ 2^{17.98}` and `Z ≥ 2^{71.90}` (C=4), so `L^⊥ ∩ T ≠ {0}` is **proved** at the witness row. Either way `2^{71.9}` is `2^{o(n)}`, so **(O1)'s mass target survives at `k = e` and only the exact-zero form dies.** What remains: prove `Z_1 ≤ 2^{o(m)}` at `k = e` — with THEOREM Z-NOGO showing it cannot be done by distance+counting, and route (b) the only opening. Against f2_adm's shortfall arithmetic (2 rungs × 64 bits against a 1/43 budget → 1.50e8x), nothing here moves a moving rung: the terminal is unchanged in position and sharper in statement.

## CATCHES

1. **CATCH-Z1 (against this brief's §0 and `f2_sl1b/REPORT.md:62`).** "`Z_1 ≤ 2^{o(m)}` (equivalently `L^⊥ ∩ T = {0}` or nearly)" — **the equivalence is false.** `Z` is *weighted*; the forms differ by `log2 3` (as `f2_sl1_powersums/PROOFS.md:304-307` itself says), and at the admissible object the mass form is heuristically TRUE while the exact-zero form is heuristically FALSE by `≈ (3/2)^{2^38}`. Measured: 67 configurations with ≥16 ternary kernel vectors but `Z < 3`; extreme 1,184 vectors with `Z_1 = 2.59`.
2. **CATCH-Z2 (against a banked statement).** `f2_sl1b/PROOFS.md:288-291`: *"A first-moment threshold … is not a property of any particular one."* False in the direction (O1) needs — Z-FLOOR is pointwise for every subspace, and the mean exceeds it by ≤2x.
3. **CATCH-Z3 (the seam gets a third face).** The counting balance `tL ≥ n`, LEMMA 3, and the vacuity of the mass floor are **one inequality** on `k = e` rows — extending f2_adm's D5 seam identity.
4. **CATCH-Z4.** f2_adm CATCH-1 upgraded from "a necessary condition fails" to "the mass itself exceeds its target", with the exponent reproduced exactly.
5. **CATCH-Z5 (to the DLI lane).** `dli_wcl_newton_short_window_exclusion`'s `{+1,−1}` restriction is unnecessary — it holds for all integer coefficients with the `l1` weight, same cutoff, same `char > w`.
6. **CATCH-Z6 (self-caught, methodological).** My own registered grid was **contaminated**: composite `2N` carries `p`-independent cyclotomic ternary relations (`2N=12` → 8 common vectors of min weight 3; `2N=20` → 8; `2N=24` → 80) which the official 2-power object structurally cannot have (`2N=8,16` → 0, the `Z`-basis property). Only 2-power `2N` rows are valid miniatures.
7. **CATCH-Z7.** Route (a) re-priced at the new row: `w ≥ 2.0000`, dominated by `4.295e9`.
8. **CATCH-Z8 (the 64-bit window).** One condition of `Λ` is worth `log2 p = 64` bits of floor (measured 63.99999), and the two defensible readings of the minimal admissible `t` straddle zero.

## OWN-REGISTRATION OUTCOMES

**Z-A1, Z-A2, Z-A3, Z-A5, Z-A6, Z-A7 (point estimate −46 vs measured −46.0249), Z-A8, Z-A9, Z-A14 CONFIRMED.** **Z-A4 CONFIRMED with its subtraction correction** (the identity is banked; only the inequality and its transport are new). **Z-A10 PARTIALLY SELF-FALSIFIED:** `0.61315` and `0.44210` confirmed, but my packing figure `0.2565` was wrong — I had registered a `2^j`-weighted ball volume, which is unavailable for a full-weight codeword; the correct threshold is `0.33333` and the shortfall `21.33x`, not `16.4x`. **Z-A11 CONFIRMED and STRENGTHENED:** the no-go bites from `p ≥ 8`, not `p ≥ 17`. **Z-A12 EXECUTED then SELF-CAUGHT AS CONTAMINATED** (CATCH-Z6). **Z-A13: clause 1 does not fire; clause 2 fires at the level of the mean, but the ensemble study shows the effect is fully explained by the distance floor, so I do NOT claim "better than random".**

## HONEST RESIDUALS

1. **SL-1b′ is not closed.** At `k = e` nothing here proves `Z_1 ≤ 2^{o(m)}`. What is proved: a floor, a dichotomy killing `k < e`, a transported distance law, an `l1` extension, and a no-go.
2. The `2^{17.98}` firing depends on the exact-balance reading of the minimal `t`. **I do not resolve which reading governs** — that is the `t`-naming collision f2_adm flags and the sibling `t_naming` owns. Both are reported; the gap is one condition.
3. THEOREM Z-1 is quoted at shift 0 only. A shifted `2R+1` law at 2-power `2N` is **open**; only 1 of my 43 shifted counterexamples is at 2-power `2N` — a thin sample, and I claim no general shifted failure law.
4. All calibration is at `S ≤ 16` against `S = 2^38`. The `(3/2)^S` count and `Z_1 ≈ 1` mass predictions are **heuristics**; only floor/dichotomy/transport/no-go are proved. **No toy is evidence about `Z_1` at the official row** — f2_adm's residual 1 stands.
5. The route-(b) Weil sizing is back-of-envelope, explicitly not machine-checked and not a theorem.
6. `C ≤ 4` and the nested/new-part readings are carried from f2_adm without independent re-derivation.
7. No status flip proposed for any minted node. DRAFT ONLY — every file written is inside `notes/pilots_20260806/z1_ternary_mass/`; no commit, no push; the sibling `o1_generating_adversary/` was not read.
8. **Process, clean:** every number came from `tools/ramguard local -- python3` runs. No bare `python3` at any point, including file patching (Edit/Write used throughout).
