(Coordinator-persisted verbatim from the pilot's final message, 2026-08-06.)

# PILOT REPORT — (O1) ADVERSARY on GENERATING rows (mystery 2, round 18, `o1_generating_adversary`)

**Opus 5, 2026-08-06.** Replay: `tools/ramguard local -- python3 notes/pilots_20260806/o1_generating_adversary/verify.py` → **187 checks, 0 FAIL**, digest `O1_GEN_ADVERSARY_ALL_PASS`. Artifacts (all inside the pilot dir): `/home/u2470931/smooth-read-solomin/prize/notes/pilots_20260806/o1_generating_adversary/{PREREG.md (brief + my appendix A1–A11), PROOFS.md, verify.py, VERIFY_LOG.txt}`.

## HEADLINE

**The attack lands, and the thing that kills (O1) is a number the campaign has been reading as agreement for two rounds.** Three of the four attack lines fail — the coset costs *exactly* nothing, the generating classes are non-empty, and no ternary construction is reachable. But at zero margin LEMMA 3's requirement is **literally the balance `t·L ≥ n`**, so (O1) at generating rows is decided by *which block ensemble the condition count is calibrated against*: the crude `(C)` uses entropy `n` (all `2^n` subsets) and (O1) survives with ≤ 184 bits of slack; the exact FM+gate `(T*)` uses the fixed-size slice `log2 C(n,n−k−t)` and (O1) is **FALSE by 2^{n/(L² ln 2)} ≥ 2^{4.84e7}** — a `Θ(n)` excess. The gap between the two calibrations is `2n/(L² ln 2)`, whose relative size is **0.0044%** — `f2_tq_pin` CATCH-4's "agreement to 0.0044%" and `t_naming` CATCH-C's Stirling constant, the same number, now carrying the sign of (O1).

## VERDICTS — the four registered attack lines

**(V4) THE EMPTY-CLASS SWEEP — run FIRST as mandated. ATTACK FAILS; scope is non-vacuous, and smaller than posed.** `ord_{2^41}(p)` lives in the 2-group `(Z/2^41)^*`, so `k` is **always a 2-power**; `k = e` therefore forces `e` to be a power of 2. **THEOREM G1:** the generating admissible classes are exactly `(e_p,e,k) ∈ {(≥41,1,1), (40,2,2), (39,4,4)}` — and **`e ∈ {3,5,6}` can NEVER generate**, so the brief's "e = 3, 6 need ord odd parts" is impossible, not merely unrealised. **THEOREM G2:** all three are non-empty, with primality established by me **twice** — deterministic Miller–Rabin *and*, by a disjoint route, a **Lucas (p−1) certificate** (a proof, not a test, since `p−1 = c·2^{e_p}` factors completely): `3·2^41+1` (a=5), `27·2^40+1` (a=5), `5·2^39+1` (a=3), and the banked prize-max witness `18446735827372343297` (a=3, `p−1` primes `{2,13,467,5527}`, `e_p = 39` exactly, `L = 255.999997420 < 256`). `f2_adm` CATCH-4's empty `(40,6)` is independently replayed — and was doubly out of scope, being non-generating anyway. *Reading-independent (no `Λ` enters).*

**(V2) THE COSET ATTACK — ATTACK FAILS at EXACTLY zero cost.** **THEOREM C1:** for any `g ∈ F_q^*`, the map `φ_g : (C_l) ↦ (g^l C_l)` is an `F_q`-linear **bijection of K1(Λ)** with `χ_{φ_g(c)}(x) = χ_c(gx)`, so `L(gW) = L(W)` **as subspaces**, and `E_{c∈K1}[T_{gW}] = E_{c∈K1}[T_W]` **exactly** — not up to constants. Verified on three toy rows (two generating, one not), each with `g` outside `μ_n` **and** outside `F_p`: identical `dim L`, identical ternary dual *sets*, identical `Z(L)`, identical minimum weight, and identical `Σ_c T_W(c)` computed by a **disjoint route** (exact group-ring arithmetic in `Z[X]/(X^p−1)`, no LEMMA-1 shortcut) — reproducing `f2_opening`'s own `3856`. The attack needed a constant; it gets a factor of **1**. Two corollaries: `f2_adm` CATCH-6's gap is **confined to the parity/descent machinery** (which LEMMA 5 already proved is the wrong functional for (O1)) — so **half of that open obligation closes**; and the coset does **not** rescue `k<e` rows even though `F_p(g·μ_n)` can equal `F_q` when `F_p(μ_n)` does not, so **`f2_adm` CATCH-1 is coset-robust**. *Holds under both parity readings — C1 uses only "Λ odd".*

**(V3) THE `Z_1` LOWER-BOUND ATTACK — the DLI gift is REAL; the attack from below FAILS, quantified.** Checked first as instructed: **THEOREM D1 — `dli_wcl_newton_short_window_exclusion` APPLIES on every admissible row.** `p > m ≥ w` always, by an elementary argument: `e_p ≥ 41 ⟹ p > 2^41`; `e_p = 40 ⟹ p > 2^40`; `e_p = 39 ⟹ c ≠ 1` because `3 | 2^39+1`, so `p ≥ 3·2^39 > 2^40 ≥ m`. Hence `wt ≥ 2R+1`, **twice** SL-1's `R+1` — the first row in the campaign where the banked node applies, `f2_sl1_powersums:171`'s "fails by two orders of magnitude" being tower-specific and **reversed** by the field cap. I re-derived two of its six `char ≤ w` counterexamples myself (min weight `= char = 3`, `= 5`), confirming the hypothesis is necessary. **What it buys (exactly):** with distance `d`, (M3) needs `d > 0.6131472(S+1)`; at generating rows **`R/S = 1/log2 p` EXACTLY**, so SL-1 needs `log2 p < 1.63` and DLI needs `log2 p < 3.26` against an admissible floor of 39 — the shortfall halves from **≥23.9x to ≥11.96x** (19.62x at prize-max) and **(M3) stays vacuous**. **The attack from below fails and I can say by how much:** forcing `Z_1 ≥ 2` with all weights `≥ 2R+1` needs `2^{8,589,934,681}` nonzero ternary codewords; the code's only visible symmetry is the **negacyclic** shift group times sign, order `4S = 2^40` — short by `2^{8.59e9}`. The random-subspace baseline gives `S − R·log2 p = 0` exactly, hence `Z_1 ≈ 2` and `Z(L) = Z_1^C ≤ 2^C ≤ 16`, i.e. **(O1) TRUE with `o(n) ≤ 4 bits`** heuristically. And DLI is nowhere near tight here: `2R+1 = 0.03125·S` sits **7.27x below** the heuristic true minimum `γ*S = 0.2271·S` — the *opposite* ordering to the toy scale where `2R+1` is attained. *Reading A throughout; reading B doubles `R`, doubling the DLI distance and improving nothing qualitatively.*

**(V1) THE ZERO-MARGIN ATTACK — LANDS, conditionally, and the condition is decidable.** First a correction to the premise: (O1)'s `+o(n)` **absorbs every `2^{O(1)}` and `2^{o(n)}` loss**, so "a constant > 1 kills it" is wrong; only a `Θ(n)` exponent loss clears the bar. Measured against that bar:

- **Integrality/ceilings: `O(L)` only.** `Δ := dim L·log2 p − m = (L/2)(t + [t odd]) − n/2` is never zero (`n/L` is irrational), but `|Δ| ≤ L ≤ 256` across the whole non-vacuous regime; worst measured `−71.90` bits, and at the banked witness `Δ = +184.10 > 0` (LEMMA 3 **holds**, with my `R` reproducing `f2_adm`'s banked `4,294,967,340`). Short of `Θ(n)` by `8.59e9`. **SURVIVED.**
- **THEOREM Z2, the ensemble dichotomy — the kill.** Under **reading A**, LEMMA 3 makes (O1) require `k·⌈t/2⌉·log2 p ≥ n/2`, i.e. **`t·L ≥ n` — the balance `(C)` itself, with zero slack**. So: under the **full-subset** calibration (`(C)`, entropy `n`), (O1) survives. Under the **fixed-slice** calibration (`(T*)`, entropy `log2 C(n,n−k−t)` + 128 gate), `n − t*L = (2n/(L² ln2))(1+O(L^{-2}))` and `Δ = −n/(L² ln 2) + O(L)`, so with the rules cap `L < 256` the excess is `≥ n/45426`: **(O1) FALSE by `2^{Θ(n)}`.** Measured at all four witnesses — `Δ_(T*)` = **−1.7445e9 / −3.9572e8 / −1.1610e8 / −4.8405e7** bits, every one strictly negative, every one matching the closed form to within an `O(L)` residue, and `(n−t*L)/n` **`n`-invariant** across `n = 2^39..2^42` (spread `1.0000040`) — which *is* the `Θ(n)` statement. My `(T*)` solver reproduces `f2_tq_pin`'s banked `t*` **to the last digit at all four rates** (`8592912739 / 7014660390 / 4722556392 / 2943177800`) and the witness-row `t* = 8,589,556,515`.
- **Reading dependence, registered in advance.** `ratio = k/e` (reading A) vs `2k/e` (reading B). Generating rows are **never refuted by LEMMA 3 under either reading** (1 or 2), so Z2 is **reading-A-only**: under reading B `Δ_(T*) ≈ +n/2` (`+1.0994e12` bits) and the recalibration is swamped. This behaved exactly as my A4 control predicted.

## CATCHES

1. **CATCH-A (maintainer-level, new).** **The banked "0.0044% agreement" between `(C)` and `(T*)` IS the sign of (O1) on generating rows.** At zero margin the calibrations differ by `2n/(L² ln 2)` bits and half of that is LEMMA 3's shortfall: (O1) holds under the full-subset calibration and is FALSE by `2^{4.84e7}` (prize-max) to `2^{1.74e9}` (`L = 42.58`) under the slice calibration. `f2_tq_pin` CATCH-4 and `t_naming` CATCH-C both identify the number and both read it as *agreement*; it is a `Θ(n)` disagreement wherever LEMMA 3 has no margin. Invisible on the tower, where a 7.89x ratio margin swallows a `9.7e7`-bit recalibration without a trace.
2. **CATCH-B (against `f2_adm` D3).** "LEMMA 3 exactly saturated, margin 1.000" is **reading-A-only**; under reading B generating rows carry margin **2.000** and the classes **(1,2) and (2,4) flip REFUTED → SATURATED**. `f2_adm`'s claim of independence from the `t`-collision is true of `t`'s *value* and false of `Λ`'s *parity convention*. CATCH-1's `(1,6)` witness survives both readings and is unaffected.
3. **CATCH-C (against my own brief).** "Any strict loss kills it" is too strong — see V1 above.
4. **CATCH-D (uncited banked gift, with its limit).** The DLI node applies on every admissible row and no F2 file has noticed the reversal; but at prize scale it is 7.27x below the heuristic minimum and moves (M3) only from 23.9x-vacuous to 11.96x-vacuous.
5. **CATCH-E (scope, closing half of `f2_adm` CATCH-6).** (O1) is exactly coset-invariant; the coset gap touches only the parity/descent machinery.
6. **CATCH-F (scope, new).** `e ∈ {3,5,6}` can never generate; (O1)'s surviving scope is exactly three classes.
7. **CATCH-G (the fence is open again).** `(O1) ⇒ (O2)` is lossless only against `(O2)`'s stated target, at which `(O2)` is no constraint at all; against the `b`-resolved scale the step loses `2^{m(1−H(β))} = 2^{Θ(n)}` (`2^{5.84e11}` at `β=0.1`), and THEOREM B' — which supplied the exact slice law — is **vacuous at every moving rung of every admissible row**. Same dichotomy as CATCH-A seen from the consumer's end: the fence demands the slice, and the slice is the ensemble that makes (O1) false.
8. **CATCH-H (upgrade to `t_naming` CATCH-E).** The K1/K2/G trichotomy is a **proved** theorem and class G is *"both parity parts nonzero"*; a non-empty G forces both parities in the ambient condition set, hence `|Λ_K1| = ⌈t/2⌉`. Independently, `f2_deployed_windows/REPORT.md:55` states the parity-pure sector's condition count as **`min(m_j, t/2)`** — `t/2`, in the lane's own words. That upgrades CATCH-E from "5-to-1 by weight of evidence" to "forced by a banked theorem" — an internal-consistency argument, not a proof of intent; I do not close CATCH-E.

## THE MINIMAL SURVIVING FORM OF (O1)

`E_{c∈K1(Λ)}[T_W(c)] = 2^{n/2}·Z_1^e` **exactly**, on `(e_p,e) ∈ {(≥41,1),(40,2),(39,4)}` with `e·log2 p < 256`, for the rules-level coset domain (`g` droppable by C1), where `Z_1` is the ternary mass of the negacyclic prime-field GRS code `[S, S−R, R+1]_p`, `S = 2^{e_p−1} = 2^40/e`, `R/S = 1/log2 p`, evaluation points the half-system of `μ_{2^{e_p}} ≤ F_p^*`, whose ternary sub-code has minimum weight `≥ 2R+1`. **(O1) holds iff `Z_1 ≤ 2^{o(n)/e}`** — subject to two named, unpinned scope conditions: the `Λ`-parity reading, and the ensemble calibration. **Under (reading A + slice) the statement is FALSE and no bound on `Z_1` can save it.**

## HONEST RESIDUALS

1. **Nothing here bounds `Z_1`.** SL-1b′(adm) is untouched and remains THE terminal. `Z_1 ≈ 2` and `γ* = 0.2271` are **heuristics**, explicitly not theorems.
2. **The kill is conditional on two choices, and I choose neither.** Under reading B, or under the full-subset calibration, (O1) at generating rows **survives** and my attack fails. Both conditions are decidable by a maintainer; neither is decided here.
3. `t_naming` (N3) flags "feeding `t*` into LEMMA 3" as a wrong-`t` consumption. I am **not** substituting `t_XR` for `t_F2` — I am asking which *ensemble* `t_F2`'s own balance should use, given that `f2_tq_pin` derives `(C)` from `q^t > 2^n` while `(T*)` uses `C(n,n−k−t)`. A maintainer may judge this the same defect; disclosed rather than buried.
4. LEMMA ADM-2/ADM-3 and THEOREM ADM-A are **inherited** from `f2_adm`, not re-proved at scale; my toys (`p ≤ 41`, `n ≤ 16`, `m ≤ 8`) test structure only and are **no evidence about `Z_1`**.
5. All headline numbers use the **nested** window at the full-domain window; the new-part reading doubles the ratios and changes no verdict in kind. Everything is at `n = 2^41`.
6. **My registration took less risk than it looks.** A1–A10 all confirmed, nothing self-falsified — but they were written after reading the full record, and the one genuinely surprising result (CATCH-A) was registered as A4 only because I had already seen `f2_tq_pin`'s two `t` values.
7. No status flip proposed for any minted node. DRAFT ONLY; no commit, no push; `z1_ternary_mass/` not read.
