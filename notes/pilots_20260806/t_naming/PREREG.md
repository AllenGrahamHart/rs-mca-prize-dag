# PRE-REGISTRATION — the t-NAMING COLLISION: |Lambda| vs A - k

Round 17, 2026-08-06. Coordinator-authored brief; the pilot appends its
own registrations BEFORE any computation. This is round-16 f2_tq_pin's
elevated honest residual 1, verbatim:

> **The `t`-naming collision is real and unresolved.** LEMMA 3's `t`
> is `|Lambda|`; `xr_radius_arithmetic`'s `t` is `A - k`, an
> agreement excess. The repo *adjudicates* them as one quantity and
> three lanes agree on `2^33`, but **no proof identifies them.** If
> they are distinct, the exclusion of `7e10` survives (it rests only
> on `n | q-1` plus the product relation), but the positive
> identification of `t` with `t*` does not.

## 1. Source surfaces (read ALL first; quote verbatim)

- `notes/pilots_20260806/f2_tq_pin/REPORT.md` + `PROOFS.md` — the
  full adjudication chain, (P2)'s derivation t = |Lambda| = n/L, the
  corroborating lanes (official_scale.json n/t = 2^41/2^33;
  `notes/kernel_basis/TARGET_3C_EXTRACTION.md:42-43`), CATCH-2 and
  CATCH-5 (the sliver seam — likely load-bearing here).
- `background/nodes/xr_radius_arithmetic/proof.md` — the (T*)
  formula, t* = A - k semantics, the FM scale + gate derivation, the
  L = 255.9 convention, the open slot xr_ledger_qpower.
- Where LEMMA 3's t enters: `notes/pilots_20260804/f2_opening/PROOFS.md`
  (the window mechanism: t Newton conditions), and the banked product
  t·L ~ 2.199e12 (`archive/compressed_dli_lane_20260705/b2_modp_giant_extras/statement.md:9`
  as corrected by f2_tq_pin CATCH-4).
- The near-collision to reconcile (f2_tq_pin residual 4):
  `critical/nodes/rate_half_cyclic_simple_pole_mca_floor/node.json:9`
  refutes 8,592,912,739 as a FIXED SAFE POINT at excess 8,594,128,895
  — "different object, but a maintainer should reconcile them".

## 2. Pre-registered deliverables

- **(N1) The two definitions, formalized side by side.** t_F2 :=
  |Lambda| (Newton conditions in the F2 window mechanism); t_XR :=
  A - k (agreement excess in the radius arithmetic). For each: the
  exact object it counts, the row it lives at, the formula it
  satisfies, with file:line provenance.
- **(N2) PROVE or REFUTE the identification.** Candidate proof shape
  (coordinator's guess — test it first, discard freely): both are
  forced to n/L at leading order by the SAME counting balance
  (conditions x bits-per-condition = block bits), so the
  identification is an instance of one balance identity — if so,
  prove the identity once and derive both; the identification is then
  EXACT at the balance point and approximate elsewhere, with the
  error term stated. If instead they are genuinely distinct
  quantities that coincide numerically at the prize rows, prove THAT
  (coincidence + the mechanism of the coincidence), and state which
  banked results consume which t.
- **(N3) The dependency repricing.** Whichever way (N2) lands: list
  every banked conclusion that consumed "t" and tag it t_F2, t_XR,
  or balance-identity-safe. In particular: the f2_tq_pin band
  (rungs 1-10/1-9), the |K1| = 2^{n/2} pricing identity, and the
  corridor table.
- **(N4) The near-collision reconciled.** 8,592,912,739 (t* corridor
  edge) vs 8,594,128,895 (the mca_floor refuted safe point): prove
  they are different objects (state both definitions), or surface a
  real conflict. The difference is 1,216,156 — if that number has a
  closed form in the row parameters, exhibit it.
- **(N5) The sliver seam (CATCH-2/CATCH-5 fold-in).** f2_tq_pin found
  the L = 255.9 convention sits 0.011 bits below the sliver's left
  endpoint n/t* = 255.911275, and the sliver is generated by
  t*L >= n, not (T*). State the corrected convention the campaign
  should adopt (a forced correction candidate: the coordinator will
  apply it if it is forced, surface it if it is a choice).

## 3. Pre-registered falsifiers / honesty clauses

- If the identification is UNPROVABLE from banked material (needs new
  modelling input), say so and state the minimal missing premise —
  do not prove an adjacent theorem and call it the identification.
- If any banked conclusion consumed the WRONG t (i.e. its proof needs
  t_F2 but cited t_XR arithmetic or vice versa), that is a defect
  catch — report it with the exact line, whatever it breaks.
- The 7e10 exclusion must be re-checked to be t-naming-independent
  (f2_tq_pin claims it is; verify the claim rather than inheriting it).

## 4. Rules of engagement

- DRAFT ONLY: write only inside `notes/pilots_20260806/t_naming/`.
  Never touch dag.json, node shards, tools/, or push. Do NOT read
  `notes/pilots_20260806/f2_adm/` (sibling this round).
- COMPUTE LAW: never bare python3; `tools/ramguard tiny -- python3 ...`
  (256M/60s) or `tools/ramguard local -- python3 ...` (1G/5min);
  literal `--`; run from repo root
  `/home/u2470931/smooth-read-solomin/prize`.
- Verbatim quotes with file:line for every statement relied on.
- Do NOT write REPORT.md — return the full report as your final
  message; the coordinator persists it verbatim.
