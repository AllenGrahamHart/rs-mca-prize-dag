(Coordinator-persisted verbatim from the pilot's final message, 2026-08-06.)

# PILOT REPORT — the t-naming collision (mystery 2, round 17, `t_naming`)

**Opus 5, 2026-08-06.** Replay: `tools/ramguard tiny -- python3 notes/pilots_20260806/t_naming/verify.py` → **68 checks, 0 FAIL**, digest `T_NAMING_VERIFY_ALL_PASS`. Artifacts (all inside the pilot dir): `PREREG.md` (brief + my appendix U1–U9), `PROOFS.md`, `verify.py`, `VERIFY_LOG.txt`.

## HEADLINE

**The identification is REFUTED, and the brief's own premise is wrong.** `t_F2` is **not** `|Lambda|` — five independent sources read it as the *largest Newton index*, with `|Lambda| = ceil(t/2)`. Correct that, and the two `t`'s differ by a clean **factor 2 at rate 1/2**, not by the famous `0.0044%`. That `0.0044%` turns out to be exactly `2/(L² ln 2)` — a Stirling correction on the central binomial, not evidence of an identity. And the factor 2 is not cosmetic: it decides the rung-16 LEMMA 3 verdict, the `t_F2` interval, and **the `7e10` exclusion, which I partially overturn.**

## VERDICTS — pre-registered deliverables

**(N1) The two definitions, side by side — DELIVERED, with a correction to the brief.** `t_XR := A - k`, an agreement excess in **coordinate space**, governed by (T\*) `t·L >= log2 C(n,n-k-t) + 128`, strongly rate-dependent. `t_F2` := the **largest Newton index** in `Lambda ⊆` **frequency space** (not `|Lambda|`), governed by the counting balance, rate-independent. Full provenance table in `PROOFS.md` §1.1. Also: `critical/nodes/noncontain_degeneracy/statement.md:9` already maintains a *"Disambiguation of the three t's"* — and neither of ours beyond `t_win` is in it. Counting the third `t*` I found (below), the repo carries **at least six** symbols in the `t` family.

**(N2) REFUTED — but a common schema survives.** The coordinator's guess was half right in a precise way. I prove a **unified first-moment balance (UFMB)**: both `t`'s are the unique integer crossing of `t·L >= log2 N + G`. But they instantiate it with *different* ensemble and gate — `t_F2`: `(2^n, 0)`; `t_XR`: `(C(n,n-k-t), 128)`. So it is one **schema**, not one **balance**. Exact criterion: they agree iff `Delta := n - log2 C(n,n-k-t) - 128 = 0`, and in general `t_F2 - t_XR = Delta/L` (verified at all four rates to within one rounding step). `t_XR/(n/L)` deviates by `-0.0044% / -18.37% / -45.04% / -65.75%` at rates `1/2 / 1/4 / 1/8 / 1/16`. The rate-1/2 near-miss is the central binomial: `log2 C(n, n/2-t) = n - (1/2)log2(πn/2) - 2t²/(n ln2) - (4/3)t⁴/(n³ ln2)`, closed to rel `< 1e-7`. **And under the correct normalization (N1) the rate-1/2 gap is a factor 2 as well** — the identification fails at all four rates.

**(N3) Repricing — 15 banked conclusions tagged.** Four `[MIS]` wrong-`t` consumptions: `f2_sl1_powersums/REPORT.md:13`, `f2_sl1b/REPORT.md:54`, `f2_tq_pin/PROOFS.md:337,340` (all feed `t*` into LEMMA 3), and the `|K1|` pricing identity. **The band conclusion survives the retyping** — `f2_tq_pin` independently ran `t = 2^33`, giving identical bands `(15,10)`/`(14,9)`. *Citation defective, band intact.* Two further findings: **LEMMA 3 proper contains no `t` at all** (it enters only via the gloss's unstated `dim L <= t`, which `f2_sl1b`'s own verifier reports broken at rungs 14–16); and the `|K1| = 2^{n/2}` identity is *structural for `t_F2`, approximate for `t_XR`*, and internally inconsistent (`dim K1 = ceil(t/2)` plus `t = |Lambda|` forces `|Lambda| <= 1`). (P6)'s `Θ(n)`-not-`o(n)` verdict is unaffected.

**(N4) Not two objects — a real conflict, and worse than posed.** `sigma_max = dc+c-1 = 2^33 + 2^22 - 1` exactly (a construction ceiling). But `mca_floor`'s `sigma* = 8,592,912,738` **equals `t* - 1` exactly**, and its `sigma` *is* `A - k` by `(SP2)`. So `8,592,912,739` is the **same object** as `t_XR` — the brief's instruction to "prove they are different objects" and `f2_tq_pin` residual 4 are both wrong. Consequence: `t*` sits **inside** the interval `mca_floor` (PROVED) shows is prize-unsafe at `2^-83`. Worse — `8,594,128,895` is **historical**: `rate_half_cyclic_rotated_prefix_floor/claim_contract.md:24-26` supersedes it with `1 <= sigma <= 2^34-1`, so `t*` sits at **0.50017 of the current unsafe reach**, deep in the interior, not `1.2e6` below an edge. The decomposition `1,216,156 = (2^22-1) - (t*-2^33)` is closed in row parameters but **was already banked** at `WAVE9_AUDIT_FINDINGS.md:189-191` — I re-derived it, and say so.

**(N5) The sliver is mis-typed and EMPTY.** CATCH-2's "0.011 bits" is exactly `Delta/t*` — the per-condition share of the entropy deficit, i.e. a *symptom* of N2, not a convention error. `f2_tq_pin` computed the sliver with `t*` frozen, but `dt*/dL = -3.36e7` per bit. Recomputed self-consistently, `{L : t*(L)·L >= n}` is **empty at every rate and every `L < 256`**; it first becomes reachable at `L = 2.43e5` bits — the cap would have to rise **~950x in bits**. **Corrected convention:** (1) *retire the sliver* — **FORCED**, the "move L up 0.011 bits" repair provably cannot work; (2) *keep `L = 255.9` but re-label it* a representative evaluation point of an `L`-indexed family, publishing `dt*/dL` — **a CHOICE, surfaced not applied**; (3) *never cross-audit the two balances* — **FORCED**, it manufactures phantom inconsistencies of size `Delta`.

**The 7e10 re-check — CLAIM PARTIALLY OVERTURNED.** The dependency audit confirms the narrow claim: the exclusion consumes only `(C)`, `(R1)` and `n` — no `C(n,j)`, no gate, no `(T*)`, no `t*`. Independent of the collision *as posed*. **But it is not robust.** Under the count normalization `L = n/t = 31.4 < 41` → excluded; under the degree normalization `L = 2n/t = 62.83`, which is **rules-admissible**, and `7e10` lies inside the forced interval `(2^34, 1.07e11]` → **not excluded**. `f2_tq_pin` (P3) enumerated both *field* readings but not the *normalization*, and the normalization is what decides it. What survives unconditionally: `7e10`'s **origin** is still a unit error. A wrong derivation is not an excluded value.

## CATCHES

1. **CATCH-A (maintainer).** `mca_floor`'s `8,592,912,739` is the *same object* as `t*`, not a different one; and its `8,594,128,895` endpoint is superseded — `t*` sits at half the current proved-unsafe reach. `xr_radius_arithmetic` carries no cross-reference to its own refutation.
2. **CATCH-B.** The identification is refuted at all four rates.
3. **CATCH-C.** The `0.0044%` tightness (`f2_tq_pin` CATCH-4) is exactly `2/(L² ln 2)` — a Stirling correction, not evidence of identity.
4. **CATCH-D.** Four banked conclusions fed a coordinate-space excess into a frequency-space slot. Band survives; citations must be retyped.
5. **CATCH-E (maintainer, NEW).** `f2_tq_pin/PROOFS.md:158`'s *"`t = |Lambda|`"* is contradicted by five sources; the two readings cannot co-hold above `t = 1`. It looked true because the DLI lane's `Lambda` is *all* `r <= t`, where count and index bound coincide. **This factor 2 decides three verdicts**: rung-16 LEMMA 3 (`0.9687x` VIOLATED vs `1.9375x` HOLDS), the `t_F2` interval, and the `7e10` exclusion.
6. **CATCH-F.** The sliver is empty when recomputed; CATCH-2 is a symptom of CATCH-B.
7. **CATCH-G (NEW).** A **third `t*`**: `SOL_TARGET_3_OFFICIAL_EXTRAS_FLOOR.md:20,35` uses `t*` for `#{p-free i <= t} ~ 7e10`, an order of magnitude from the corridor edge, carrying a load-bearing sub-balance hypothesis. The `3C` file renamed it `t*_eff`; the `3` file did not.
8. **CATCH-H (process).** The brief's path `critical/nodes/rate_half_cyclic_simple_pole_mca_floor/` does not exist; the node is in `background/nodes/`.

## HONEST RESIDUALS

1. **I did not prove no deeper correspondence exists.** I proved the two *defining balances* differ. Minimal missing premise for a positive identification: *"the `t`-null block window is in bijection with the aligned `j`-support ensemble"* — nothing supplies it, and the rate-dependence argues against it.
2. **CATCH-E is decided 5-to-1 by weight of evidence, not by proof.** A maintainer must pin `Lambda`'s parity convention where (C) is imposed. Until then the `t_F2` interval, the rung-16 verdict and the `7e10` exclusion each have two live values.
3. `t_XR` remains conditional on the open slot `xr_ledger_qpower`, inherited unchanged.
4. `t_F2` is now **less** pinned than `f2_tq_pin` left it — `t*` is removed as its pin.
5. CATCH-A's conflict is of **use**, not of logic — I did not check formal compatibility of the two predicates.
6. **Three downstream cascades not priced**: `mca_floor → rate_half_band_closure`; CATCH-E across every LEMMA 3 margin; and `f2_sl1b`'s *"every verdict that used `dim L <= t` needs re-derivation"*.
7. `b2b_near_tail_bound` (`dag.json:7559`) substitutes `2^33+1` and `t*` interchangeably — flagged as a **probable** wrong-`t` consumption, not established.
8. **My pre-registration was weak.** Eight for eight on U1–U8 means I registered after reading enough to be nearly certain. The falsifiers were real and tested (S3.F, S12.F), but I took little risk — and **every genuinely surprising result (CATCH-E, the supersession, the `7e10` overturn) came from evidence found *after* registration, and none was predicted.**
9. **Two self-corrections, both caught by the fail-closed harness and disclosed rather than patched silently**: S0.2 failed at 986.27 bits because I omitted the quartic de Moivre–Laplace term (lgamma was never at fault); S21.3 failed because I wrote the divergence predicate as `A != B` when both branches are true.
10. Ten of the sweep's load-bearing quotes I re-opened and verified myself; one line citation drifted (`even_l` is at `selection.py:50`, not `:49`) and is corrected. Quotes I did not personally re-open are not used as load-bearing.

**Recommendation.** Raise CATCH-E to maintainer level as a *definitional* defect — it is upstream of CATCH-A/B/D and of `f2_tq_pin`'s (P3) and (P5) headlines. Pin `t_F2` by fiat in a naming ledger (extending `noncontain_degeneracy:9` to six symbols), then re-derive the `7e10` verdict and the rung-16 band once, rather than per-pilot.
