# FABLE_AUDIT — t_naming (round 17, pilot 1 of 4 to report)

**Auditor:** Fable, 2026-08-06. **Verdict: BANKED, MAINTAINER-LEVEL —
the identification is REFUTED (one schema, two balances: UFMB with
different ensemble and gate; rate-dependence 0.0044%/18%/45%/66%), the
brief's own premise t_F2 = |Lambda| is corrected to the max-Newton-
index reading (5 sources to 1, CATCH-E), the "near-collision" is the
SAME OBJECT with t* now sitting at half the currently-proved unsafe
reach, and the sliver is EMPTY. Round-16 f2_tq_pin's (P3)/(P5)
headlines are DOWNGRADED accordingly — see the addendum written to its
FABLE_AUDIT this bank.**

Replay: verify.py 68/68 PASS exit 0, digest T_NAMING_VERIFY_ALL_PASS
(coordinator re-run under ramguard tiny). Anchors verified by the
coordinator directly: f2_tq_pin/PROOFS.md:158 ("t = |Lambda|, the
number of power-sum (Newton) conditions" — verbatim, the defective
line); critical/nodes/noncontain_degeneracy/statement.md:9 (the
existing three-t disambiguation, with t_win = A-k);
rate_half_cyclic_rotated_prefix_floor/claim_contract.md:24-26 (the
supersession: unsafe band 1 <= sigma <= 2^34-1, the 8,594,128,895
endpoint historical — so t* is interior, exactly as the pilot says).

ADOPTED:
- **UFMB** (the unified first-moment balance schema) and the exact
  divergence criterion t_F2 - t_XR = Delta/L; the central-binomial
  mechanism of the rate-1/2 near-miss; CATCH-C (0.0044% =
  2/(L^2 ln 2), a Stirling correction — f2_tq_pin CATCH-4's
  "tightness" is hereby demoted from evidence-of-identity to
  arithmetic accident).
- **CATCH-E raised to MAINTAINER level** as the pilot recommends: the
  Lambda parity convention (t = max Newton index vs t = |Lambda|) is
  a DEFINITIONAL unpinned constant deciding three verdicts (rung-16
  LEMMA 3: 0.9687x VIOLATED vs 1.9375x HOLDS; the t_F2 interval; the
  7e10 exclusion). Decided 5-to-1 by weight of evidence, NOT proof —
  each verdict carries TWO LIVE VALUES until the maintainer pin.
  The naming-ledger extension (six t-symbols, on
  noncontain_degeneracy:9's pattern) joins mint-4.
- **The 7e10 DOWNGRADE of record**: from "excluded by the rules" to
  "origin is a unit error (unconditional); excluded under the count
  normalization; NOT excluded under the degree normalization". The
  conservative posture stands: 7e10 remains UNUSABLE as a pin (wrong
  derivation), but the exclusion headline softens.
- **The band survives**: rungs 1-10/1-9 rests on the t = 2^33
  endpoint run, which is naming-independent — the WITHDRAWN status of
  "rungs 1-13" is unchanged (withdrawal is the conservative state
  under either reading).
- **N4**: t* = t_XR = the mca_floor object; the brief's "prove they
  are different objects" premise (inherited by me from f2_tq_pin
  residual 4) was WRONG — coordinator defect accepted. The
  conflict-of-USE (t* deep inside a proved prize-unsafe sigma band;
  xr_radius_arithmetic carries no cross-reference) goes to the
  maintainer queue; formal predicate-compatibility unchecked, as the
  pilot honestly flags.
- **N5 forced corrections APPLIED as standing rules** (coordinator
  authority — forced, not choices): (i) THE SLIVER IS RETIRED — the
  set {L : t*(L) L >= n} is empty for all L < 256; no future work may
  cite it; (ii) NEVER CROSS-AUDIT THE TWO BALANCES — comparisons
  between t_F2-lane and t_XR-lane arithmetic manufacture phantom
  inconsistencies of size Delta. The L = 255.9 re-label option is a
  CHOICE and stays SURFACED to the user, with dt*/dL = -3.36e7/bit
  published either way.
- CATCH-G (the third t* in SOL_TARGET_3, unrenamed) and CATCH-D's
  four [MIS] retypings and residual-7's b2b probable → mint-4 hygiene
  queue. CATCH-H accepted (my brief's path defect — the second
  critical/-for-background/ slip this session; brief-writing rule:
  verify node paths before writing).

HONEST RESIDUALS accepted, three elevated: (1) the three unpriced
cascades (mca_floor -> rate_half_band_closure; CATCH-E across every
LEMMA 3 margin; f2_sl1b's dim L <= t verdicts) — next-round
candidates; (2) residual 8's WEAK PREREGISTRATION self-report (8/8
registered post-reading; every surprise post-registration) — accepted
with respect; future briefs will require registration before the deep
read where feasible, while noting the tension with verbatim-quote
requirements; (3) t_F2 is now LESS pinned than f2_tq_pin left it —
correct and honest.

Cross-pilot note: the running f2_adm sibling's own appendix already
registered R = |Lambda| = ceil(t/2) under the max-index reading — it
is on the CATCH-E-correct convention; reconcile at its bank, no
mid-flight correction needed.

## ADDENDUM (2026-08-06, coordinator, from round-18 o1_generating_adversary CATCH-H)

CATCH-E's parity question is UPGRADED from "5-to-1 by weight of
evidence" to "internally forced": the proved K1/K2/G trichotomy
(class G nonempty forces both parities, hence |Lambda_K1| =
ceil(t/2)) plus the lane's own frequency-space statement
(f2_deployed_windows/REPORT.md:55, "codim_j = min(m_j, t/2)" —
coordinator-verified verbatim) both force reading A. This is an
internal-consistency argument, not proof of maintainer intent —
CATCH-E stays OPEN, but the burden has shifted: restoring reading B
now requires overriding a banked theorem and the lane's own text.
Also CATCH-C is sharpened: the 0.0044% is not merely "not evidence
of identity" — at zero margin it IS the sign of (O1) (the ensemble
dichotomy, o1_generating_adversary CATCH-A).
