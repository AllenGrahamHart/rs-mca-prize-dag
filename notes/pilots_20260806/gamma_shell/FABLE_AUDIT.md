# FABLE_AUDIT — gamma_shell (round 20, pilot 1 of 4 to report — THE PRIORITY)

**Auditor:** Fable, 2026-08-06. **Verdict: BANKED, CAMPAIGN-CRITICAL,
CANDIDATE CONFIRMED ON COORDINATOR REPLAY — THEOREM BB: a PROVED
72.07-bit budget-break at the DSA witness row (max-shell
X_{2^34} >= 2^199.575 vs B* = 2^127.510, exact integers), so
agreement k + 2^34 is UNSAFE and a_L(C) > k + 2^34 on an explicit
tower-row region. The pilot's honesty discipline held exactly where
it mattered: the consequence is THRESHOLD RELOCATION, NOT a
grand-challenge refutation — (RHL-ADJ) is an existence statement
(coordinator-verified verbatim at the node), (RHL-LB) is
STRENGTHENED, and both grand challenges are determinations for
which "a counterexample that relocates a threshold is a valid route
to resolution" (the joint protocol's own words). What dies is the
campaign's safe-side pin at w = 2^34 on the break region: no route
whatsoever can establish safety there.**

Replay (coordinator): the mandated toy gates (siglift 6/0 — the
SIG-LIFT identity exhaustive at all three DSA shapes; shell 844/0 —
both parity branches; pipeline 31/0 — the end-to-end inequality
chain against brute force), the prize-row stages (row 16/0, bound
7/0, compare 3/0 — THE EXACT-INTEGER COMPARISON), fail-closed exits
1. INDEPENDENT COORDINATOR ARITHMETIC: the exponent chain
|D|^2/Q = 2^{252-42.585} = 2^209.415, rho_min = 2^{-1.840} ->
N_acc >= 2^207.575, spread over <= 256 shells -> max-shell >=
2^199.575 vs log2 B* = 6·42.585 - 128 = 127.510: margin 72.065 —
re-derived by hand, matches. The RHL-ADJ existence form verified
verbatim ("There is an agreement index a_L(C) such that",
statement.md:15).

ADOPTED:
- **LEMMA SL** (the sig-arithmetic of the periodic lift; second term
  vanishes at the crossing family) and **THEOREM SM** (the shell
  map: the deep stratum occupies 256 of 2^41 shells — concentration
  2^33; structural members exactly equidistribute over 128;
  parity decides new-vs-structural shells). The pilot's registered
  SHELL-CONC prediction (danger regime (ii) is the truth) was
  exactly right, registered before computing.
- **THEOREM AC** (the Cauchy-Schwarz accident count lower bound
  with the LEMMA TC fibre weighting — a 103-bit improvement over
  the single-relation round-18 count, sitting the required 1.5 bits
  below the banked heuristic) with its own CATCH-A scope fix
  (even-U restriction necessary and sufficient).
- **THEOREM BB + the row region** (e >= 3 fully at delta_a = 1;
  partial at e = 2 and (4,2); e = 1 prime rows UNTOUCHED and
  provably unreachable by this method — reproducing DSA's dichotomy
  independently); the structural-only control (within budget by
  10.36 bits — the break is entirely the accidents).
- **THE CONSUMER-CHAIN TRIAGE** (hand-verified req-only paths to
  the root on BOTH grand challenges — a genuine single point of
  failure, AND an existence/determination chain that relocation
  cannot falsify). THE STATEMENT OF RECORD: the safe side of the
  crossing localisation MOVES to w = 2^35 (deep stratum within
  budget by 54 bits); a_L(C) >= k + 2^34 + 1 on the break region
  (proved); THE NEW CRUX = an ACCIDENT UPPER BOUND on shell
  populations (nothing supplies one — THEOREM AC is a lower bound);
  stability across [2^35, 2^39] verified (G4).
- **PT-2 honesty**: the proved break region is a STRICT sub-region
  of the ternary heuristic's (76.9-bit gap in the pigeonhole
  strength); the v = 33 prime-row scenario is NOT reachable by this
  method — PT-2's 0.336-bit alarm stands UN-upgraded on the watch
  line.

CATCHES ACCEPTED: CATCH-B (es_g_lanes' usable-w pricing lacks an
accident term — addendum queued to that audit); CATCH-C (mun [B4]
survives as-written, not as-read — the coset scoping was
load-bearing); CATCH-A/D (self-catches, both caught by the pilot's
own gates).

PROCESS VIOLATIONS, reported by the pilot and accepted with
disclosure: 3 bare-python3 heredoc text patches (no substantive
compute; reproducible under ramguard) — the recurring text-edit
pattern; and ONE QUARANTINE LEAK via a dispatched search subagent
that quoted a post-marker ledger line (content = the pilot's own
brief restated; no sibling findings exposed). RULE ADDITION: pilots
must PASS THE QUARANTINE CLAUSE TO EVERY SUBAGENT THEY DISPATCH —
added to the standing brief template.

HONEST RESIDUALS accepted, the crux elevated: (1) NO UPPER BOUND on
shell populations exists — the safe side's new named obligation;
(2) shallower strata + aperiodic S uncounted (enlarge the break
only); (3) delta_a = 4 and most delta_a = 2 rows crude; (4) the
19-pair labelling not reproduced (region given as inequalities +
one exactly-verified row). NODE CONSEQUENCE: a statement addendum
on rate_half_list_adjacent_crossing (the relocation + the dead
w = 2^34 safe-side pin on the break region) — WRITTEN AT THE
WAVE-47 INTEGRATION (it composes with the W-5 verifier repair and
the audit addendum restore, one edit pass). Node stays TARGET; the
census is unchanged by this bank.

BOARD EFFECT: mystery 4 is RESHAPED — the crossing localisation's
lower side is proved strictly above k + 2^34 on tower rows; the
safe side moves to w = 2^35; the accident upper bound joins the
board as the crossing lane's sharpest named obligation, alongside
the prime-row emptiness question (untouched, crossing_gap's
mandate).
