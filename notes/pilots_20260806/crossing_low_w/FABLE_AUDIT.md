# FABLE_AUDIT — crossing_low_w (round 18, pilot 2 of 4 to report)

**Auditor:** Fable, 2026-08-06. **Verdict: BANKED, CAMPAIGN-CRITICAL —
THEOREM DSA refutes the (ES) crossing instance UNCONDITIONALLY at
admissible tower rows (pigeonhole, no balance functional), with an
explicit witness VERIFIED AT n = 2^41 ITSELF (not extrapolated):
10 of 19 admissible (class, e) pairs killed outright at w = 2^34,
3 at 2^35, partial coverage beyond. The recorded prime rows are
PROVABLY OUTSIDE the refutation regime (B* >= 3 forces
log2 p >= 129.6 > 126) and are re-priced from a 0.089-bit
global-functional failure to a 53-61 bit margin under the pilot's
corrected TERNARY functional (heuristic, labelled). My brief's
lift-constraint conjecture is refuted structurally — the pilot
pre-registered the negation (X1) and was right.**

Replay (coordinator, under ramguard local): the bijection gate
toy_gate.py biject 81,005/0 (the mandated three-shape exhaustive
lift-mechanism check) and prize_exhibit.py verify 2,854/0 — the
witness confirmed at n = 2^41 with the refutation line printed
directly. The witness row (p = 6597069766657 = 3·2^41+1, e = 6) is
the row I have now independently verified THREE times this session
(primality MR-30, admissibility arithmetic) — it is the
triple-refutation row: above global balance (es_g_lanes),
(O1)-false (f2_adm), and now an (ES)-crossing accident carrier.

ADOPTED:
- **LEMMA DS** (the deep-stratum bijection with NO side conditions;
  r'_a = L-2 uniformly — one one-parameter family (2L, L-2), not six
  instances), **LEMMA FREE** (zero lift constraints at the binding
  stratum), **LEMMA OE** (the odd/even epsilon/sigma split — real at
  every shallower stratum, vacuous exactly at the binding one; THE
  RECURSIVE TOOL for the untouched strata a < v-1), **LEMMA TC**
  (the ternary functional: the primitive object is epsilon in
  {0,±1}^L — 3^L, not 2^{n_a}, not C(n_a, r'_a); both banked
  functionals mis-price the stratum, per-weight by 48.75 bits),
  **LEMMA ROT** (2L-orbit over-dispersion — Poisson-style accident
  estimates over-predict by 2L).
- **THEOREM DSA + the witness** (CATCH-18A). STATEMENT OF RECORD
  after this bank: the (ES) crossing instance is FALSE at admissible
  tower rows in the DSA regime (p^{delta_a} < 2^{L-2}); at e = 1
  prime rows DSA provably cannot apply — a clean dichotomy. The
  round-17 es_g_lanes P4 balance failure was DETECTING A REAL
  REFUTATION, not a pricing artefact.
- **The consumer consequence, scoped precisely**: what is refuted is
  the (ES) statement |W_w| = structural count. The crossing NODE's
  own budget question (X_w(gamma) vs B* per gamma-shell) at tower
  rows is RE-OPENED, not decided — the pilot computed the witness's
  sig but not the shell population (named residual). No node status
  flip.
- **The prime-row re-pricing** (heuristic, labelled): 3^128/p =
  2^{-53.1}, orbit-corrected 2^{-61.1} — the first well-founded
  margin estimate for the recorded rows, replacing the 0.089-bit
  cliff-edge. The proof obligation at prime rows remains open and
  now has the right functional to aim at.

COORDINATOR ADJUDICATION — THE SCOPE ESCALATION: one explicit row
now witnesses three refutations across three mutually blind pilots.
The Przemek scope question (are extension/non-generating/tower rows
in the official family?) is no longer a hygiene item — it DECIDES
whether two lanes (F2's (O1), the crossing's (ES) instance) are
false as posed. Either answer is progress: exclusion validates the
surviving mathematics at prime rows; inclusion means the lanes need
re-posing around proved counterexamples. The maintainer note leads
with the DSA witness. The pilot's honest scope clause (if towers
are excluded, CATCH-18A shrinks to nothing and the re-pricing
survives) is exactly right and is carried into the note.

CATCHES ACCEPTED: 18A (above); 18B (MY brief conjecture refuted
structurally — coordinator defect, and the pilot's X1
counter-registration is the prereg discipline at its best); 18C
(both banked functionals wrong at the stratum; the ternary
functional joins the band re-pose toolbox — NOTE the potential
transfer: the band lanes' failed balance forms may have the same
fibred-object mis-pricing); 18D (orbit over-dispersion); 18E
(self-caught birthday-sizing error, honestly reported, DSA
unaffected); the sig-shell/structural-count distinction (one-line
addendum to the es_g_lanes audit written this bank).

HONEST RESIDUALS accepted, three elevated as next-round candidates:
(1) the GAMMA-SHELL question (which shells the accidents populate —
the consumer consequence at tower rows); (2) the shallower strata
a < v-1 via LEMMA OE's recursion; (3) the prime-row emptiness proof
at the ternary functional's margin (the E_floor/coprimality
machinery is the natural partner). AK-UNIT and no-balance-smuggling
self-checks PASS. DRAFT-ONLY confirmed (0 files outside the dir);
sibling never read. Note: the pilot correctly flagged that ramguard
tiny cannot import numpy — brief templates updated mentally to
authorize local for numpy work.
