# frontier: e22_staircase_parametrization

Conditional.

The exact n=16 cells show many mixed-petal profiles, not a single naive
profile shape. The forward algebra is now proved in
`e22_tail_coset_locator_algebra`; the remaining active target is
`e22_agreement_coset_support_forcing`.

A proof needs to derive the tail-plus-full-coset support from the agreement
equations, not fit a count to the observed totals.
