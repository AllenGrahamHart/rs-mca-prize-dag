# tr_perleaf_list_ident

- **status:** TARGET
- **closure:** proof
- **refs (legacy repo):** ['experimental/notes/roadmaps/gap1_terminal_reserve.md']

## Statement

Each per-character set A_r in Conjecture TR is a worst-word deep-agreement list count at a same-rate quotient row — the SAME object as rung 2a's ExactList and the list challenge's kernel, at scale n/M. Identification: the list lane's corridor machinery is TR's per-leaf supply; no separate per-leaf theory needed.

## Attack surface

write the dictionary (K_M-stable agreement <-> quotient-row agreement — #212's tower machinery is the transport); cross-check on the F_13 toy

## Falsifier

a per-leaf count provably exceeding the quotient-row list count (would break the dictionary; toy-checkable)

## Ledger (migrated notes)

X-4: TR's arithmetic must carry the transported quotient-row staircase term explicitly (exact binomials at scale n'/Q) before the primitive per-leaf bound applies. | X-4b: the quotient-row split gains the moment column verbatim.
