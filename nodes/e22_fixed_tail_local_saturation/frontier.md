# frontier: e22_fixed_tail_local_saturation

Conditional.

The common-modulus part of gluing is proved in
`e22_dyadic_local_to_common_saturation`, and the local conversion from
quotient factors to full fibers is proved in `e22_fiber_locator_saturation`.

The live theorem is now `e22_common_tail_invariance_payload`: derive
the common bounded tail, local dyadic moduli, and non-tail kernel invariance
from the actual `L_{T_i} | H_i` constraints.
