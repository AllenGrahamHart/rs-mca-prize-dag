# frontier: e22_cross_scale_duplicate_control

Conditional.

The fixed-scale/root-set recovery and support canonicalization components are
closed. The minimal-scale selector is also proved. The remaining active leaf is
`e22_minimal_scale_pricing_compatibility`, which must reconcile selected
cross-scale representatives with the staircase pricing column.
