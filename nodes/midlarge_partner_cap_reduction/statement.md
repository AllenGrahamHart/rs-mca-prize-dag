# midlarge_partner_cap_reduction

- **status:** PROVABLE
- **closure:** proof

## Statement

ASSEMBLY (via the PROVED value_set_partner_cap: <= floor(n/h) partners per anchored core at every h): the mid/large-h trade bound reduces to bounding ACTIVE CORES per h — the budget tolerates ~3.3e3 active cores/h. Pure reduction on a proved lemma; write-up pending.

## Attack surface

one page: compose the cap with the per-h budget table

## Falsifier

none — arithmetic
