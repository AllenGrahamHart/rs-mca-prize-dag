# ATTACK - petal_squarefree_kernel_classification_payload

This node is now conditional. The live petal sparsity leaf is
`petal_squarefree_classification_ledger_payload`.

The injection from realizable extras into squarefree kernel points is proved
in `petal_realizable_kernel_injection`, and the finite-union counting
consequence of a classification is proved in
`petal_squarefree_classification_counting_soundness`.
The ledger-format soundness rule is proved in
`petal_squarefree_classification_ledger_soundness`.

Needed output:

- classify squarefree locator points inside `K_{I,d}`;
- identify which classes are charged elsewhere;
- bound every uncharged class by a polynomial with exponent independent of
  `c`;
- prove the number of uncharged classes is independent of `c`.

Raw scan counts are evidence only unless converted into such a structural
classification or a proof-grade certificate transfer.
