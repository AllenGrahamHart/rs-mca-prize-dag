# sketch: hankel_sparse_atoms_as_rational_defects (Pro rounds 6-7; verified)
Ann_E(P) = GRS_{n_E-j-1}(E,lambda) + omega RS_t(E) (the RS-dual identity, Lemma
D of the matching-composition note): every sparse annihilator word is u_x =
omega_x h(x) + lambda_x g(x), deg h < t, deg g <= n_E - j - 2 — a sparse atom
is a small DEFECT SET of a rational approximant -lambda g/h to the spectral
weight omega. SEPARATION (verified, 233,130 pairs, 0 violations): two atoms
with distinct saturated closures satisfy |T1 u T2| >= j - t + 2 (eliminate
omega; degree count on F = g1 h2 - g2 h1). SAME-CLASS COLLAPSE: atoms from one
approximant are diagonal rescalings of RS_s on the defect block B — punctured
GRS is MDS, so all saturate to cl_P(B): ONE closure (the pair-family collapse
at arbitrary weight). Hereditary via the proved stability lemma (descent
multiplies omega by ell_A). Write-up pending.
