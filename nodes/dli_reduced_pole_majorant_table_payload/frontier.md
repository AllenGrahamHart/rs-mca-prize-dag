# frontier: dli_reduced_pole_majorant_table_payload

Conditional.

The reduced-phase manifest soundness rule is proved. The remaining work is
`dli_reduced_phase_manifest_payload`: construct the actual reduced-pole
majorant table for DLI odd phases and prove its harmonic total is `o(t)`,
with local pole witnesses tied to the same tuple universe.
