# pcf_norm_gate_flatness

- **status:** TARGET

## Statement
The per-level norm-gate flatness estimate (the sharp residue of the primitive
core, Pro Brief D route A): for every central level-j profile M,
|G_j(M)| <= q^{-L_j + eta_j} U_j(M) with sum_j eta_j = o(t) (prize-row budget:
H = sum eta_j <= 122/256 via the crude W_cen <= 2^n form, or the exact
q^{-t+H} W_cen <= 2^122). G_j(M) = large-support (>= L_j+1) bounded-coefficient
skews whose L_j folded resultants share the fixed ~2^250-bit prime q, plus the
zero skew. Everything upstream (Vandermonde threshold, simultaneous norm gate,
sum L_j = t, the exact cascade) is PROVED; this is the one remaining count.
