# frontier: petal_residue_line_uniformity

Conditional.

The node is decomposed into:

- `petal_residue_kernel_linear_bound`: proved Lemma 13 linear ceiling
  `dim K <= c+1`;
- `petal_residue_kernel_flatness`: refuted literal flatness route;
- `petal_realizable_extra_uniformity`: open exact-realizable-extra target.

The remaining petal branch is now the exact-realizable-extra theorem.
