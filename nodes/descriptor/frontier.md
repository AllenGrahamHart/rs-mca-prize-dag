# Frontier audit: descriptor

Status: TARGET, intentionally left open.

See `artifact_audit.md` for the detailed classification.  This node is a
missing single-source constants generator/regression artifact, not a theorem
that follows from the current predicate graph.  Consumers need either the
artifact itself or a truth-apt descriptor-soundness proposition.
