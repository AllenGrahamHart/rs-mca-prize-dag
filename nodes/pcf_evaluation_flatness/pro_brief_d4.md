# D4 (same thread) — attack EJM itself
Your D3 reduction is verified and banked (Fourier identity + Holder replayed
exactly; pcf_evaluation_flatness is CONDITIONAL on ejm_joint_moment). The ask:
EJM_{2m} — bound J_2m(M) = sum_{lambda!=0} |F_M(lambda)|^{2m} at the central
profiles. Your proved lever: every nonzero frequency is seen by >= 255 L_j + 1
coordinates (deg R_lambda <= L-1), each contributing a factor
|mu_hat_y(a)| < 1 for the short AP domains D_j(M(y)). The question is average
decay over lambda. Routes: (a) direct — the domains are symmetric APs, so
mu_hat is a Dirichlet-kernel ratio; bound the product on average (log-integral
over the circle?); (b) the collision form — signed 2m-fold additive energy of
Phi at the uniform level; (c) falsify: a central profile with structured
J_2m mass (would kill the tower route — say so plainly). Calibrate at the
mu_16/mu_32 toys (we replay everything). MAP: this is the LAST input of the
primitive core; everything else in the chain is proved.
