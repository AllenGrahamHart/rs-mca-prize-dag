# frontier: sov_hminus1_character_sum_bound

Conditional.

The exact remaining theorem is now
`sov_hminus1_affine_piece_decomposition`: a decomposition/structure result for
the map

```text
L -> [X^{h-1}]L
```

on anchored-core locators after fixing the forced-root higher coefficients.
The proved affine cancellation lemma closes the formal character-sum step once
such a decomposition has been supplied.
