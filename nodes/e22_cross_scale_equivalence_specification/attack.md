# ATTACK - e22_cross_scale_equivalence_specification

Status: conditional.

Use `e22_cross_scale_support_canonical_form`: any duplicate is equality of one
support whose admissible quotient scales, recovered tails, and recovered full
fibers are canonical.

The active leaf is now `e22_minimal_scale_pricing_compatibility`: the
minimal-scale representative is canonical, so the remaining question is
whether the dyadic quotient staircase column counts those representatives with
exactly the declared multiplicity.

Do this after `e22_staircase_parametrization` fixes the exact support form.
