# ATTACK - petal_realizable_extra_uniformity

Use light local computation only.

This node is now an assembly node. The injection from exact extras to
squarefree locator points inside the residue-line kernel is proved in
`petal_realizable_kernel_injection`.

The former active leaf `petal_kernel_realizable_sparsity` is now conditional.
The live Petal child is `petal_squarefree_classification_ledger_payload`. It
is possible in principle for the ambient kernel dimension to have linear slack
while the realizable missed-core subsets remain uniformly controlled. Prove
or refute that phenomenon through the squarefree-kernel classification ledger.

Useful proof targets:

- a structural classification of squarefree realizable locator points inside
  the residue-line kernel;
- a separation between charged families and uniformly polynomial uncharged
  families;
- an off-laptop complete scan converted into a theorem explaining why exact
  realizable extras stay within the uniform polynomial budget.

Do not use raw scan counts as a proof.
