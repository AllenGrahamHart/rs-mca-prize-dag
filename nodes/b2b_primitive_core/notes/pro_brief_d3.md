# D3 (same thread) — the evaluation-form flatness
*Your falsifier is verified and banked (collapse identity 200/200; eta
coefficients reproduced exactly; falsification #20). pcf_norm_gate_flatness
is REFUTED; your conditional closure survives verbatim with the corrected
predicate: the TRUE evaluation-form flatness B_j(M) <= q^{-L_j+eta_j} U_j(M),
where the conditions are Q_d(zeta^{2l-1}) = 0 at the SPECIFIC roots.*

## THE MAP: what survives, what died
DIED: counting through resultant divisibility (one condition, not L). The
norm gate remains TRUE but cannot be the counting lever at deep levels.
SURVIVES UNTOUCHED: (1) the second-moment/character-sum route — E_lambda
|sum_d psi(...)|^2 over nonzero frequencies, which works with the actual
evaluations and never passed through resultants; (2) the divisor-pair
bilinear route (both halves of L_A L_{A^c} = X^n - 1); (3) the two-level
unconditional bound (still a valid upper bound). Your own falsifier
construction is also a diagnostic: the pigeonhole fiber has ~3^N/q vectors
satisfying ONE evaluation — the question is precisely the joint distribution
over L independent evaluations (heuristic: (3^N/q)*(collision rate)^{L-1}).
If the L evaluation maps were pairwise "independent enough" (a second-moment
statement), the true count IS balanced. That independence is now the sharp
question.

## The ask
(A) Prove evaluation-form flatness via joint-moment/independence of the L
evaluation maps d -> Q_d(zeta^{2l-1}) on D_j(M); (B) falsify THAT too — a
profile where the joint count exceeds balance by q^{Theta(L)} (this would
kill the tower route for the primitive core entirely and be a major result
about simultaneous vanishing sums); (C) conditional on one named joint-
moment estimate.
