# ATTACK - sov_affine_piece_partition_payload

This is now the live SOV character-sum leaf.

Needed output:

- describe the conditioned anchored-core solution set by local coordinates;
- find affine directions along which `[X^{h-1}]L` has nonzero linear part;
- partition away singular, norm-structured, or paid pieces;
- prove the total size of exceptional pieces is below the active-core
  character-sum budget.

The generic affine cancellation lemma and the partition-certificate soundness
rule are proved. The missing content is the actual anchored-core geometry.
