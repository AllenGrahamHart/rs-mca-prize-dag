# ATTACK - m720_ground_truth_replay

Status: conditional.

The locally cheap n=16 rows are proved in `m720_small_gate_replay`. Do not run
the remaining full gate locally on the WSL laptop unless explicitly cleared.
The h=3 rows at n=128 and n=256 are exact but can allocate large hash tables.

Preferred route for the remaining leaf: replay the gate off-laptop or cite a
completed Modal log with the larger gate payload.
