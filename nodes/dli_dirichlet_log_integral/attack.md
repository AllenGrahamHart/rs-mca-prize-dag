# ATTACK — dli_dirichlet_log_integral (DEEP; decompose, do NOT force)
Target: low-degree odd evaluations P_lambda(sigma(y)) equidistribute on the
square-root section at the circle constant -2 log 2 (geometric mean, not
arithmetic). This is a genuine EXPONENTIAL-SUM / Weil-type estimate. DO NOT
fabricate a proof of the top-level statement. Instead decompose:
(a) the seen-coordinate lever (>= 255 L+1 nonzero coords) is PROVED but
    insufficient (Dirichlet peaks) — do not re-derive.
(b) attempt: a Weil bound for the odd-polynomial evaluation sequence on mu_n;
    or the collision/DWE_2 form (signed 2m-fold additive energy at uniform).
(c) toy-calibrate at mu_16/mu_32 (already flat at ternary 3.6e-7).
Deliverable = a sub-lemma reduction with a proved piece, NOT a claimed close.
