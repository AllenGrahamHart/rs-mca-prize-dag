# frontier: e22_cofactor_coset_saturation

Conditional.

The generic cofactor equation has been proved, and the pointwise equations
are now converted to petal-locator divisibility by
`e22_cofactor_petal_divisibility`.

The remaining active leaf is `e22_common_tail_invariance_payload`:
derive one bounded tail and local dyadic quotient-kernel invariance from the
family of petal divisor constraints for the full mixed/full-petal class. The
full-fiber criterion, tail-removal passthrough, local fiber-locator
conversion, and dyadic local-to-common modulus step are proved.
