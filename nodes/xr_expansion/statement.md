# xr_expansion

- **status:** CONDITIONAL
- **closure:** proof

## Statement

On post-strip strata where pairwise exchange distance is large enough for the
exchange ledger to apply, dense aligned Johnson-neighborhoods are
over-constrained: the anticode bound controls the number of locators, the
exchange ledger supplies q-power suppression, and the v8 cap prevents
per-locator slope amplification.

The small-radius remainder is not claimed here; it is the energy/inverse
pipeline's input.

