# dependency sub-DAG: aqb_family_certificate_semantics

Edges are directed from dependency to consumer.

```text
aqb_family_certificate_semantics [PROVED]
    -> aqb_c2_average_family [CONDITIONAL]
```

This node proves only that a verified averaged-family certificate implies the
family-existence predicate. It does not provide the concrete certificate.
