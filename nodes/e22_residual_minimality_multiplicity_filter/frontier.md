# frontier: e22_residual_minimality_multiplicity_filter

Closed.

The raw residual-profile generating function and formal lower-scale
inclusion-exclusion filter are proved. The exact weighted intersections of
lower-scale admissibility events are now supplied by
`e22_lower_scale_intersection_profile_counts`, so the minimality and
multiplicity filter is proved.
