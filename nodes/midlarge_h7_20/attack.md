# ATTACK — midlarge_h7_20 (medium; bounded band exclusion lemma)
Claim: zero active cores at official-shape rows for h in (6,20]. The scan
(notes/modal_midlarge_h7_20.py) DE-RISKS (evidence only, cannot close). The
PROOF is an exclusion lemma: h <= 20 is a BOUNDED band, so the C1a/C1b descent
machinery (re-parameterized) should give a finite obstruction per h. Write the
lemma: for each h in 7..20, the active-core condition forces a structure that
the paid ledger already prices (or is empty). Cite the C1a/C1b proved inputs.
Do NOT close from the scan alone — the scan is at calibration rows.
