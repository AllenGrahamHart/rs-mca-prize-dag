# frontier: aqb_shared_entropy_gain

Conditional.

The exact deficit threshold and entropy-ledger soundness rule are proved. The
missing content is `aqb_shared_entropy_gain_payload`: the exact or
interval-certified entropy gain ledger of the actual averaged `c=2`
quotient-box family, once `aqb_c2_family_certificate_payload` fixes that
family.
