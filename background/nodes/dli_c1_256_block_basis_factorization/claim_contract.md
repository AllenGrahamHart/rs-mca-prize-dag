# Claim contract

| field | content |
|---|---|
| status | PROVED |
| claim | Every official C1 odd-moment matrix splits into 256 invertible blocks `D_a F`; each block Fourier marginal is exactly iid uniform, while all cross-block dependence is the deterministic companion orbit `C_a=M^a C_0`. |
| assumptions | `N=256L` and an element `omega` of exact order `2N=512L` in the official field. |
| retained difficulty | Any mixing, expansion, anti-concentration, or excess bound for the 256-step orbit, and the C1 consumer. |
| nonclaim | Exact block marginals are not joint independence. |
