# Result

PROVED. On prize-envelope `N=256` rows, every profile `(4,2,0)` collision has
`V=2 mod 8`. An exact logarithmic deficit gives six finite cofactor-specific
variance windows, and an exact Lucas-resultant calculation excludes `V=2`.

The cofactor `1538` is therefore impossible. The strongest remaining chamber
is `m=1028`, where only `V in {10,18}` survives. Six cofactor classes and their
residual vectors remain open, so the aggregate E1 pair budget is not closed.
