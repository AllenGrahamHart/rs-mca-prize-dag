# Source evidence

The proof composes ten PROVED parents. `verify.py` reads their manifests and
the compiled DAG, reconstructs each exact integer label set, rejects overlap,
and checks equality with `{3,4} x {0,...,14}` and the 480-case multiplicity.
