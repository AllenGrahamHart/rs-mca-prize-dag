# Statement

## Claim `(KBP1B4-XI34-ALL-1)`

On the guarded positive deployed `433-1b -> O0a` role-cell-4 branch, every
labeled slice with missing role `xi in {3,4}` and canonical matching index in
`{0,...,14}` is empty for all four source-sign pairs and all four target
lanes.

The direct/transport blocks for matchings 0 and 1/2, followed by the six
four-label matching-exchange blocks

```text
{3,6}, {4,9}, {5,12}, {7,10}, {8,13}, {11,14},
```

partition `{3,4} x {0,...,14}`. Thus all 30 labels and `30*4*4=480` raw
atlas cases are paid. No parallel-`DE`, endpoint, other role-cell, complete
branch, or Prize claim is made.
