# Audit

This node asserts only set-theoretic composition. The independent verifier
does not trust cumulative label counts in parent prose. It assigns each
parent an explicit set of `(xi,pairing)` integers, checks all ten parents and
requirement edges, rejects any overlap, and requires the exact 30-label target
rectangle and 480-case multiplicity.
