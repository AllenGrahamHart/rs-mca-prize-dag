# Frontier

Delete all positive two- and three-loop skeletons from the coordinate
workboard.  In every one-common-loop row the loop is the unique zero of
`B_1` and no outside loop exists.  Normalize that zero before compiling the
remaining four common nonloop Vieta records.  A zero-common-loop row may
have zero or one outside loop; no other loop count is live.
