# Proof

The E18 router proves that every `V=36` candidate lies in one of six profiles
on 1,321 templates. The six-profile exclusion proves that every proper-
conductor vector is removed by the conductor theorem and every full-conductor
norm has positive odd part below `2^250`. These branches exhaust the router,
so the entire `V=36` chamber is empty. Variance is even, hence the next live
positive endpoint is at most 34.
