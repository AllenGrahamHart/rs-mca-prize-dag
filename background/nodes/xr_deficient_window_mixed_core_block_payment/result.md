# Result

The active-defect blocks are now used quantitatively rather than retained only
as a route condition.  A mixed core/block incidence count pays exact affine
dimensions `10,10,9`, advancing the deficient SL-2 residual cutoff to
`11,11,10` with no new assumption.
