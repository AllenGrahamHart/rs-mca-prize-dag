# Proof

The profile/parity/diameter reduction proves that every pair-feasible
`V=66` collision belongs to exactly one of

```text
(5,7), (1,8), (4,5,1), (0,6,1).                       (1)
```

The profile `(0,6,1)` exclusion gives the all-set moment bound 1644. The
profile `(4,5,1)` quotient theorem gives exact live-chamber bounds 1732 and
1670 and removes the subfield chamber. The profile `(1,8)` light-template
theorem gives exact actual-geometry maximum 1356. Finally, the profile `(5,7)`
light-template theorem separates proper conductor from full conductor and
gives exact live maximum 1416. Every bound lies on the safe side of the exact
`V=66` cubic-Hermite threshold 1732, and the four child claims exclude all
cases in (1).

The zero-variance theorem excludes `V=0`, the variance is even, and the
preceding endpoint chain already excludes every even `V>=68`. Therefore the
remaining positive range is exactly as stated. QED.
