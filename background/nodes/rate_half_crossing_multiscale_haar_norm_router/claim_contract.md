# Claim contract

- **claim:** the exact shared-energy identity `(MH2)`, active/zero-scale norm
  product `(MH4)`, all-active simplification `(MH5)`, and the exact
  `w=2^37` route fence
- **scope:** dyadic domains and split prime rows `p=1 mod n`; the numerical
  fence uses `n=2^41`, `r=n/2-w`, and `p<2^256`
- **dependency:** the ideal/Galois multiplicity packet supplies the
  prime-ideal interpretation; the Haar and norm-product calculations are
  proved in this packet
- **consumer:** evidence for `rate_half_list_adjacent_crossing`
- **nonclaims:** no all-active accident exists; no shell population is
  bounded; no extension-field row is closed; no critical status changes
- **falsifier:** a failed Haar identity, norm ceiling, zero-scale norm factor,
  or exact power-checkpoint comparison
- **replay:** `tools/ramguard tiny -- python3
  background/nodes/rate_half_crossing_multiscale_haar_norm_router/verify.py`
