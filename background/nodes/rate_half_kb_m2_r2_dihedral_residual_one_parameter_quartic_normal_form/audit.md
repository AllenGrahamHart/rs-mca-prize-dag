# Audit

- The sibling coordinate is normalized first; the endpoint quadratic is
  then normalized by the explicit target map `m`. The two independent
  target normalizations are not conflated.
- Both source-genus passports give exactly two branch places in the first
  quadratic pullback, hence exactly one endpoint branch value in `{2,-2}`.
- The six coefficients are reconstructed by direct symmetric expansion.
- The two values of `a` come from primitive rotations of exact orders three
  and six.
- The parameter restrictions only say `b` is the other distinct endpoint
  branch value and is not the unused dihedral branch value.
- No irreducibility, existence, deletion, owner, or payment is inferred.
