# Result

The arbitrary six-coefficient residual quartic is reduced to the explicit
one-parameter family `(KBMN-2)--(KBMN-3)`, with `a=-1` for `n=3` and
`a=1` for `n=6`. Both profiles remain open pending irreducibility and the
actual pole/source realization equations.
