# Result

`PROVED`. The two oriented minimum-pair pencils are coupled for every
endpoint-deficit pattern. All of their residual roots have zero heavy
deficit, so the complete supported heavy-incidence divisor is localized to
the common center line and the exact unused slack. The remaining task is to
eliminate that slack or make the localized heavy factors incompatible with
the marked Hankel/Forney identities.
