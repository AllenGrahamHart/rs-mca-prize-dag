# Lineage

This is the `[5,8]` analogue of the proved `[12,13]` duplicate-role transport. Both exchange `B,C` and gauge the outside variables; `[5,8]` fixes the singleton parameter and flips the first source sign, while `[12,13]` fixes source signs and sends its singleton parameter to its negative.
