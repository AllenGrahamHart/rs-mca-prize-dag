# Attack record

The `K'=46` wall was the coarse leaf where supports four and five both used
fallback ceilings.  That leaf forgot the actual deeper defects.  Refining
them exactly exposes many pairs with `s_4+s_5<q`, where the already proved
joint carrier still applies.  No stronger carrier theorem is required.
