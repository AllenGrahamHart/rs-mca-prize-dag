# Audit

1. `M_c` is a maximum over independent deletions and lies in `0..q`.
2. Exact defects are unique and give `(q+1)^2` pairs.
3. Every child intersects all inherited caps.
4. The joint cap is used exactly when `s_4+s_5<q`.
5. There are `q(q+1)/2` ordered joint-eligible pairs.
6. Pareto compression removes only componentwise smaller cap vectors.
7. Rows `q=36..44` are printed because they are the payment node's scope.
