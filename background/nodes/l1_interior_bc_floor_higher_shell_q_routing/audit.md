# Audit - L1 interior BC floor routes to higher-shell Q

## Checked axes

1. Strict interior gives `m'>m`.
2. Prefix cancellation leaves degree below `k`.
3. The witness difference is exactly the degree-`m'` locator.
4. Agreement is complete at `m'`.
5. Every proper level-`m` sub-support has a positive-degree cofactor.
6. That cofactor divides the complement locator.
7. The level-`m` gcd guard rejects every floor sub-support.
8. The level-`m'` guard retains the codeword once.
9. `d_1=(m'-k)+1` is boundary Q at the true level.
10. Binomial inversion cancels each codeword separately.
11. The support floor is not treated as an exact-shell lower floor.
12. Other exact level-`m` interior members are not declared absent.
13. No field-independent upper bound is inferred.

## Route effect

The apparent base-field obstruction in the raw interior support census is
cross-level aliasing.  L1 must pay it through higher-shell Q and attack only
the guard-pruned interior residual.
