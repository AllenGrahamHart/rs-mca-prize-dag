# Audit

1. Mixed statuses are carried per component in an explicit ledger; the
   node-level PROVED covers exactly (DICT)/(CS)/(LB1). The coordinator
   ruled ONE node rather than a split — the POSED `p*(d)` component is
   quarantined in its own section and its named failures are reproduced by
   the verifier, so the separation is enforced by code, not prose.
2. Wiring-audit catch (SCOPE): (DICT) as drafted — and as banked on A1 —
   omitted the `d <= rho` qualifier. The stacked matrix has `2rho` rows,
   so the audit's saturation exhibits force the corrected form; every
   banked measurement lies in scope, so no row is invalidated. Marker
   applied to A1; corrected form carried here.
3. `verify.py` replays the draft's 32-row bank; `verify_audit.py` shares
   no code path — fresh fields (601/1013), a `rho = 3` shape the draft
   never ran, (LB1) checked constructively by exhibiting each annihilating
   error, and (CS) checked entrywise then on syndromes.
4. The `h_r = 2rho` non-polynomial leg and the exhaustive large-`q`
   censuses are carried from the source bank and clearly marked as such.
5. The "conditional on Statement U" closing of the band section was
   updated at wiring: U is refuted, so "LB1-limited" fails even as a value
   at the crossing offset; the statement records what survives (LB1 as the
   fibre floor) and what falls (the nothing-exceeds-it conjecture).
6. Edge character: evidence only; no requirement discharged.
