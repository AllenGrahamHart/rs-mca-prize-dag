# Dependency sub-DAG

```text
syndrome/Hankel algebra + round-36 census rows
                 |
                 v
rate_half_far_ca_hr_dictionary_common_support [PROVED / (PSTAR) POSED]
                 |
                 : evidence / classification
                 v
rate_half_band_crossing_location              [TARGET]
```

No requirement edge in. The outgoing edge classifies the column-far band
and supplies the `f = 1` floor configuration (LB1) that the pigeonhole cap
in `rate_half_far_ca_crossing_offset_value_ledger` prices. Siblings:
`rate_half_far_ca_negation_closure_excess_fence` (the excess lives above
this dictionary) and `rate_half_ca_hankel_split_pencil_equivalence` (the
shape flag on the proved `<= r+1` bound was applied on that supplier node).
