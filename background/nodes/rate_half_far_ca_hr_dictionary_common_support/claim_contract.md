# Claim contract

- **Claim:** for column-far pencils generated by a common-support pair
  `(e_0, e_1) = (1, L)|_T` with `d = deg L <= rho`, the stacked Hankel rank
  is `h_r = rho + d` and `dim K_0 = r+1-rho-d`, independent of the support
  size (`h_r = 2rho` for non-polynomial ratio or `d > rho`); common support
  is FORCED for any column-far pencil with two bad slopes (explicit
  reconstruction); LB1 (`d = 1`) is the unique `h_r = rho+1` structural
  family and its `T_1 = r+1` is forced.
- **Dependencies:** elementary syndrome/Hankel algebra; the banked
  round-36 census rows.
- **Output:** the (DICT)/(CS)/(LB1) classification of the column-far band;
  `h_r` retired as a coordinate; the POSED `p*(d)` law with named failures
  (carried, not claimed).
- **Consumer:** the far-CA lane inside `rate_half_band_crossing_location`;
  LB1 is the `f = 1` floor configuration used by the crossing-offset value
  ledger.
- **Nonclaims:** no `p*(d)` claim (POSED; 5 named failures); the
  `h_r = 2rho` non-polynomial leg is carried from the source bank, not
  re-proved; no total-`T` census at `rho >= 3`; no characteristic-2 or
  razor-scale measurement.
- **Falsifier:** a common-support pair with `d <= rho` and `h_r != rho+d`;
  a column-far two-bad-slope pencil not generated on `S_1 u S_2`; an LB1
  cell with `T_1 != r+1`.
- **Replay:** `tools/ramguard local -- python3
  background/nodes/rate_half_far_ca_hr_dictionary_common_support/verify.py`
  and the independent audit `verify_audit.py` (tiny; fresh fields, rho = 3
  shape, constructive LB1, symbolic (CS), saturation exhibits).
