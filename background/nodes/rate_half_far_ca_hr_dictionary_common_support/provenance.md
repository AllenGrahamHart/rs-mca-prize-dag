# Provenance

Proved by pilot `r36_hrlow` (round 36, bank 3; Round-36 R-HRLOW addendum on
`critical/nodes/rate_half_band_crossing_location/statement.md`, coordinator
hand-checked: the common-support algebra and the classification chain).
Drafted as a standalone package by pilot `r37_mint_drafts` (round 37, bank
2, package 4 of 10) with the status ledger separating PROVED components
from the POSED `p*(d)` law.

Wired 2026-08-11 by the coordinator in the task-#41 mint session.
Coordinator rulings at wiring: ONE node (no split), the ledger carrying
per-component statuses; and the `d <= rho` SCOPE CORRECTION to (DICT),
applied here and as an inline marker on the banked A1 bullet after the
wiring audit exhibited rank saturation at `d > rho`. The "conditional on
Statement U" sentence was updated against the round-37 refutation.

Model at `notes/pilots_20260811/r36_hrlow/f1_family.py`; banked rows at
`notes/pilots_20260811/r36_hrlow/f1_results.txt`. No Modal computation or
external library was used.
