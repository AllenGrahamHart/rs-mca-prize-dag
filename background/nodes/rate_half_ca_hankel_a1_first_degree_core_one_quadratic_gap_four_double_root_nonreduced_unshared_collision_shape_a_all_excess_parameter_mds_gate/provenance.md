# Provenance

- **Inputs:** the all-excess fiber factorization and shape-A norm/excess
  ledger.
- **New content:** a full all-fiber transposed RS compatibility matrix with
  exactly `4e` residual-coefficient columns.
- **Route effect:** exposes a direct linear exclusion target that retains
  positive excess and degree drops, rather than sampling only pure fibers.
- **Computation:** constant-size `e=7` exact rank probes in two prime fields;
  no Modal run.
- **Upstream export:** Section 44 of
  `experimental/notes/thresholds/rate_half_core_one_quadratic_pade_quartic_v1.md`
  in `przchojecki/rs-mca` draft PR `#1161`, commit `ed1d5b1`.
- **Upstream replay:** `39074` exact checks, `144/144` source pins, and
  `118/118` hostile formula-field mutations in normal and optimized Python.
- **Export scope:** small-profile ranks remain evidence; no official
  full-rank theorem, Shape-A exclusion, or endpoint movement claimed.
