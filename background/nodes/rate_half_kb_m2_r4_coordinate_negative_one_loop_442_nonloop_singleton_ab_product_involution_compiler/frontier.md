# Frontier

Apply the outside sign/template orbit compiler to `S0,S1,S2` with the
forced value `m=(18-5b)/22`.  The common coefficient algebra is only the
quadratic `2b^2+3b+2`; use this before any q or interpolation expansion.
