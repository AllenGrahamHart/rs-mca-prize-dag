# Dependency sub-DAG

```text
xr_quotient_boundary_agreement_raise_owner [PROVED]
                         |
                         v
xr_quotient_max_agreement_first_match_owner [PROVED]
                         |
                         +--ev--> xr_tangent_support_mismatch_bridge
```

The boundary owner reduces the threshold image to exact `A` plus the raised
bad set. This node first-matches the exact term against that same raised set
and identifies every survivor with a full agreement set.
