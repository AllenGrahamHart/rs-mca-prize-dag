# Provenance

This node is a local continuation of
`rate_half_mca_m31_boundary_anchor_case_split`.  It reuses the proved
full-lift common/outside-core affine-line packing theorem and adds the
residue-two layer decomposition, including the intersecting-missed-pair
versus pairwise-disjoint dichotomy.

The intended upstream destination is the existing MCA correction and
full-lift continuation PR #1165.
