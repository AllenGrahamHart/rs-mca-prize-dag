# Result

`PROVED`: the residue-two case split pays Mersenne support `e=98231` by

```text
16486411 < 16777215
```

with slack `290804`.  The full-lift residual starts at `e=98232`, where the
residue resets to zero and this theorem no longer applies.
