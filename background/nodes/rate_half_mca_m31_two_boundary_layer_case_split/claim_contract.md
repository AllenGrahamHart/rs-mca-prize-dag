# Claim contract

- **claim:** the residue-two five-case bound `(BA2)` and its official
  Mersenne specialization
- **dependencies:** the one-boundary prefix theorem and full-lift affine-line
  outside/total-core packing
- **output:** Mersenne full-lift support `e=98231` is safe with bound
  `16486411` and slack `290804`
- **falsifier:** an uncovered top/boundary-layer case, a failed
  synchronization intersection, an outside-line family larger than `Q`, or
  an exact arithmetic mismatch

The node claims no result at residue zero and no adjacent unsafe witness.
