# Result

The elementary-modification frontier loses its rational branch. A clean
endpoint failure can only have

```text
K_Q=O + O(1-rho) + O(-rho)^(m-2),
```

with one global section. This is now the exact Picard interface for the
remaining four-Hankel boundary-saturated curve.
