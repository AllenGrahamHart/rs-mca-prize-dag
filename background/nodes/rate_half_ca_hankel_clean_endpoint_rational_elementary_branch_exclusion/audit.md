# Audit

1. The contradiction uses smoothness only after `(REB1)` proves that the
   integral curve itself is isomorphic to `P^1`.
2. Arithmetic genus alone would not exclude a singular rational curve; that
   invalid shortcut is not used.
3. Both bidegrees exceed one for `m>1`, so the adjunction genus is positive.
4. The genuine `m=1` control has genus zero and is outside the theorem, as it
   must be.
5. The unique-section splitting is retained without a confidence promotion.
