# Result

The surviving `d_A=1` collision is no longer an arbitrary profile-I
factorization. Its exact four-core and local contact module force one of
four shapes: an irreducible large factor, that factor plus one `(2,3)`
factor, plus one `(4,6)` factor, or plus two `(2,3)` factors. The large
factor has parameter degree `e-2`, `e-4`, or `e-6`.

The node remains supporting evidence for `rate_half_band_crossing_location`.
The next route-deciding attack is to exclude or classify the low-degree
companions B--D, while retaining a separate source attack on irreducible
shape A.
