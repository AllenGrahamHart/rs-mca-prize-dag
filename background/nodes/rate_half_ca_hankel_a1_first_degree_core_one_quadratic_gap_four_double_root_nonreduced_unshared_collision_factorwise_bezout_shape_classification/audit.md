# Audit

1. The theorem is restricted to `d_A=1`; profile I is supplied by the
   corrected five-root budget.
2. Factorwise Bezout uses projective bidegrees. No affine root at infinity
   is discarded.
3. Classified-grid roots are actual-support intersections and transverse,
   so each contributes exactly one to exactly one factor.
4. The `e-7` padding points are assigned before uniqueness is inferred.
   Equality of total capacities then proves that they are simple and lie on
   only one factor.
5. The Pade syzygy identifies local ideals only because `Lambda(tau)` and
   `L_U0(x_*)` are nonzero; unsharedness supplies both units.
6. The local computation `ell_j=2b_j` is a determinant calculation in the
   quadratic contact algebra. It does not assume that `Q_j` is smooth or
   locally irreducible.
7. The four-core total and collision contact length are both exactly four;
   neither is a degree cap.
8. The integers `t_j` are residual specialized degrees. They are not counts
   of distinct supported roots and need no splitting hypothesis for `T_3`.
9. The table classifies possible shapes only. It is not an existence theorem
   and does not close the collision arm.
10. The verifier replays integer exhaustion and official dimensions; the
    mathematical proof is analytic and does not rely on computation.
