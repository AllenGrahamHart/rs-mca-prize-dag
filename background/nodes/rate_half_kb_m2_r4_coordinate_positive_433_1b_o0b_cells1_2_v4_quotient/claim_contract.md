# Claim contract

- The starting set is exactly the sixteen opposite-source-sign survivors
  certified by common saturation.
- `BC1,BC2` are identical repeated common records; the split `BC+/-` pair is
  not imported.
- The common swap fixes source signs, both BC signs, both outside-cycle
  signs, and all outside labels before composition with `d->-d`.
- The 11,076 count is a representative workload, not an exclusion count.
- No raw row, common point, outside label, route, K3 case, or Prize endpoint
  is closed here.
