# Audit

- The census is four source signs times two representatives times two
  `sigma_c` anchors: 16 rows, each covering both `sigma_o` lanes.
- Every row has degree profile `(J,M,R,Bezout)=(8,4,3,4x4)` and uses the
  quadratic-over-cubic tower norm.
- Candidate parameters are the union of every norm and exceptional-guard
  root; all 176 source points are explicitly listed and unique within rows.
- Direct replay enumerates 512 missing-`f` rows, 64 common-`q` roots, and 128
  final colored-pair lane evaluations.
- All 128 final colored-pair evaluations are nonzero.
- Witness, target-boundary, free-gcd, and unresolved ledgers are empty.
- Formal matching enumeration maps 7 to 10 and 8 to 13 under exchange of the
  two equal positive `de` records; paired symmetry preserves orientation.
- The 16 computed rows pay 32 raw cases, and exact transport pays 32 more.
- Primary and independent verifiers pin the compiler and census hashes.
