# Statement

## Claim (KBP1B3-XI3-P781013-FM-1)

On the guarded positive deployed 433-1b role-cell-3 branch, every atlas case
with missing record `xi=3` (the `df` record), canonical matching index in
`{7,8,10,13}`, any source sign `epsilon in {+-1}^2`, and any target lane
`sigma in {+-1}^2` is empty.

Sixteen exact source rows compute matchings 7 and 8, one for each source sign,
matching, and `sigma_c`; each row checks both `sigma_o` lanes. Exchanging the
two identical positive `de` records maps matching 7 to 10 and matching 8 to
13. Thus 32 computed raw cases transport value-for-value to 32 further raw
cases, and all 64 raw cases in the stated scope are empty.

No other matching, missing role, complete cell-3 exclusion, rate-half band,
LIST, MCA, or Prize result is claimed.
