# Frontier

The paid cell-3 ledger is now 896 raw cases: 720 parallel-`DE` cases and 176
cases at `xi=3,pairing in {0,1,2,3,6,7,8,10,11,13,14}`.

The remaining `xi=3` pairings are `{4,5,9,12}`. Under exchange of the two
positive `de` copies they form two pairs, `{4,9}` and `{5,12}`. Both need a
lower-growth opposite-DE compiler. Every pairing at `xi in {4,5,6}` also
remains.

Do not infer complete cell-3, K3, LIST, MCA, or Prize closure.
