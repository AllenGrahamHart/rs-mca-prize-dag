# Provenance

- Local input: the proved 218-plane projective direction bank.
- Algebraic input: exact fibers of the power map on the cyclic smooth
  domain, proved directly in this node.
- New contribution: the exact two-degree router and the singleton
  degree-2048 row with 72-slot defect.
- Computation: exhaustive integer replay over 2,982 possible `K'` values;
  no field search, Modal run, or heuristic evidence.
- Upstream correspondence: quotient flatness Q and quotient-periodic
  split-pencil classification/payment.
- Exported as an extension of the conditional quotient-pair packet in
  `przchojecki/rs-mca` PR #1170 at commit `e66f6da3`; packet comment
  `issuecomment-5333977778`. The upstream contract separately pins the
  original source interface and this pure-power subclass theorem.
