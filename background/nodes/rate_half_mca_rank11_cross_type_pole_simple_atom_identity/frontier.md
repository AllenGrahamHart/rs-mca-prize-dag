# Frontier

The compatibility theorem now needs only 16 shared pole-simple supports.
The next task is combinatorial/algebraic deck supply: construct connected
cross-type packets satisfying the partial-relative hypotheses, then use the
resulting common atom identities in a chronology-safe census.
