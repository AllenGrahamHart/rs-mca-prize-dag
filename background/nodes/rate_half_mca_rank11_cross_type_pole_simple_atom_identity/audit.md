# Audit

- Checked the incidence threshold for every `r=2..32`; `r=16` is the first
  value exceeding distinct-pair uniqueness.
- Checked shortening invariance of every printed margin.
- Checked that three records per type survive the possible one missing
  support with two records, exactly what pair-core identification needs.
- Split the proportional case at `D=0`; no division by a zero polynomial is
  used.
- Reconstructed the affine-line conclusion from two slopes by polynomial
  subtraction, not pointwise rational evaluation.
- Kept deck existence and aggregate ownership outside the statement.
