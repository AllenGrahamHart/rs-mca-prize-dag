# Claim contract

- Exact finite-field theorem over `F_2130706433`, not heuristic evidence.
- Covers `720` `DE+/DF+/EF` representative systems.
- Covers every deployed base value on the registered nonzero-guard chart.
- Uses exact nested norms and exact finite-field root extraction.
- Replays all `1,584` non-guard norm-root occurrences with all three pairs.
- Does not cover missing `BE` or `CF`.
- Does not cover registered guard or selected-cofactor boundaries.
- Does not cover cell 14 or convert raw systems into distinct affine slopes.
- Books no K3 allocation or Prize endpoint.
