# Statement

Fix the deployed field `F_p`, `p=2130706433`.  In each of the eight exact
repeated-BC cell-11 source towers, and for every missing representative in
`{DE+,DF+,EF}`, outside sign, and one of the fifteen residual matchings, the
three paired-product equations have no common endpoint whenever all
registered construction guards are nonzero.

This covers `8*3*2*15=720` representative systems at every deployed
off-guard source value.  It does not cover missing `BE` or `CF`, any
registered guard or selected-cofactor boundary, cell 14, or a K3 slope
payment.
