# Audit

1. The four planted anchors are paid separately before non-planted
   classification.
2. Touched triples are exact profile data, so the four triple cells are
   disjoint.
3. Affine normalization does not create a lambda orbit: each fixed label
   triple has one cross-ratio.
4. The defect parameter is exact and has fewer than `n` possible values.
5. The theorem assumes no cardinality bound for the LS6 atom itself.
