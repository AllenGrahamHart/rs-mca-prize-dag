# Statement

## Theorem (KBP1B9-XI3-P2)

On the deployed guarded cell-9 route, matching `2` is empty for direct role
`xi=3`, for every source sign and every target lane.

Universal label transport therefore closes the active orbit

```text
{(3,2),(4,2)}.
```

This removes one active representative and two leading-open labels. Cell
`9` as a whole is not closed.
