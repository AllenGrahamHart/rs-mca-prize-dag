# Result

`PROVED`. Recursive chart nongenericity is exactly a collision with another
joint `A`-support extending the chart zero core. Distinct explanations form a
pairwise-`(K-1)`-intersecting-or-less family. The red is now a slope-fiber and
low-core aggregate problem.
