# Lineage

The residual loop workboard isolated `433-1a -> O0b` as the only route
whose common defect already saturates the component budget and whose
outside graph is unique.  This node spends that saturation to remove all
uncontrolled repeated-edge signs and compresses the remaining target data
to two five-cycle lanes.
