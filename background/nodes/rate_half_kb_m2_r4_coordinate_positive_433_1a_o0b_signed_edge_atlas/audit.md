# Audit

1. The common row is the orbit with the loop on a degree-three pair, not the separate `433-1b` orbit whose loop is on the degree-four pair.
2. The `AB` multiplicity-three split costs two defect because each deck-orbit type repeats two signed target edges.
3. The outside multiplicity-two pairs are forced to use opposite signed types because the common row already spends the full budget.
4. The two colored edges originate at the deficient common pairs `B,C`.
5. The sign invariant is a five-cycle product; the forced `+/-` pairs do not add binary lanes.
6. Source-label placement is deliberately left open.
