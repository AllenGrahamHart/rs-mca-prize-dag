# Lineage

The Newton-window router handled all depths below the characteristic. The
present theorem observes that characteristic-p failure is sparse and
structured: power sums at p-multiples are Frobenius repetitions, and one
elementary checkpoint at each such index restores triangular coordinates.
Official field arithmetic bounds the complete checkpoint ledger by 23.
