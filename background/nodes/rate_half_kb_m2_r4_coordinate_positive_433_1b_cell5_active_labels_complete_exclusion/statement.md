# Statement

On the deployed guarded positive `433-1b` route for cell 5, every direct label

`(xi, pairing) in {0,...,4} x {0,...,14}`

is empty. This is a 75-label active-role theorem. It makes no claim about the 30 endpoint labels with `xi in {5,6}`.
