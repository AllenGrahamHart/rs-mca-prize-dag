# Frontier

The active-label branch is closed. The immediate frontier is the 30 endpoint labels with `xi in {5,6}`. A complete cell-5 node must require this theorem and a proved endpoint-role theorem before any transport to duplicate role cell 8.
