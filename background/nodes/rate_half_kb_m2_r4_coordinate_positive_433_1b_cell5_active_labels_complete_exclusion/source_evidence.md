# Source evidence

The mathematical evidence is held by the 16 required owner nodes. Each packet pins its own exact symbolic or finite-field artifacts and hostile audit.

This aggregate adds a separate coverage certificate:

- `verify.py` compiles the universal router and checks all 24 active orbits, all 75 labels, every dependency status, and exact owner partitioning;
- `verify_audit.py` demonstrates rejection after dropping or duplicating an owner representative or demoting an owner dependency.

No aggregate count substitutes for a leaf proof.
