# Audit

Run:

```bash
python3 verify.py
python3 verify_audit.py
```

The hostile audit must reject a missing representative, duplicate ownership, and a demoted dependency. Reviewers should also confirm that endpoint roles are not claimed here.
