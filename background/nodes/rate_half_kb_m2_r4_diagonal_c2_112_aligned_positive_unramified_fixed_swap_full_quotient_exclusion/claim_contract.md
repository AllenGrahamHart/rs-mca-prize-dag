# Claim contract

| field | value |
|---|---|
| claim | No admissible aligned-positive unramified `c2(1,1,2)` fixed-moving swap-allocation candidate over `F_(2130706433^6)` satisfies both the q-slice and full colored quotient identities. |
| field | `F_(2130706433^6)`; exact arithmetic over `F_2130706433` and every relevant subfield |
| template | fixed-moving internal edges `{2,1/2}` and `{2,b}` |
| allocation | swap residual-square allocation only |
| normalization | exact relative `U/V` scale retained before the fixed-`b` quadratic gate |
| open set | endpoint, discriminant, orbit, reconstruction, and scale-denominator factors are explicit |
| consequence | the fixed-swap aligned-positive unramified cell is excluded |
| nonclaim | the q-slice is not empty; no deletion of fixed-mixed, packet assembly, row payment, or Prize close |
