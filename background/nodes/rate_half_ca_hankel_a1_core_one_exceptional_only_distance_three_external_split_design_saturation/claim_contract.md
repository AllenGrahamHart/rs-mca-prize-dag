# Claim contract

- **scope:** official exceptional-only quotient-distance-three chart after
  pair-Lagrange normalization
- **input:** the exact internal pair partition and the proved two-sided
  slope-resultant identity
- **currency:** external locator incidences and their monic product
- **output:** one omitted outside point, an exact `(3e,6e+3)` biregular
  external design, and the power identity `(EDS5)`
- **consumer:** `rate_half_band_closure`
- **load-bearing hypotheses:** `P_X` is squarefree with exactly `8e+6`
  residual-domain roots; every supported fiber is split and squarefree; the
  internal pairs partition `R_A`; resultant multiplicities are exact
- **nonclaim:** biregularity alone does not classify the external blocks or
  reconstruct a valid Hankel pencil
- **failure certificate:** an external multiplicity other than `e` or zero,
  more than one omitted row, an omitted point in `S`, or failure of the
  monic product identity
- **replay:**
  `tools/ramguard tiny -- python3 background/nodes/rate_half_ca_hankel_a1_core_one_exceptional_only_distance_three_external_split_design_saturation/verify.py`
- **upstream mapping:** exact finite SPI split-pencil incidence ledger
