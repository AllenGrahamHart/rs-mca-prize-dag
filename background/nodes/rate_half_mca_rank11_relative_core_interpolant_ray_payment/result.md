# Result

The relative high-complexity branch now satisfies the first two spread
residual conclusions uniformly over arbitrary global-core shortening:

```text
core-compatible slopes <=481,
one complete correction ray <=70227214729216,
core plus one ray < B_*.
```

Any unsafe `(H_C)` line must therefore use many projectively independent
correction directions or a higher-dimensional correction span.
