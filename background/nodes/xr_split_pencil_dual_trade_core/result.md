# Result

The bounded Maxwell anomaly now has an explicit coefficient normal form.
Every active block carries a dual `GRS_4` word of weight at least five, and
every active coordinate joins at least three block words. The coefficients
are low-degree locator quotients in the evaluation points and low-degree
dual-Vandermonde quotients in the distinct slopes.

This identifies the residual as a finite dual-product-code trade, matching
the primitive shift-pair language rather than an arbitrary rank-deficient
matrix. The unresolved task is to classify these `(5,3)` trades into paid
quotient/tangent configurations or prove that the primitive residue cannot
occur at the official rows.

This node does not supply that classification or change either XR status.
The complete Modal verifier fleet passed `165/165` in
`ap-aQEZfcEULuBmrZmpIQv89H`; all five integration gates passed in
`ap-yk5nYMFMDaPNUxbBNZ0ETC`.
