# Claim contract

- Exact row only: `K'=73`, `q=63`, `m=67545`, `n=1048649`.
- The inherited marks, kernel capacity, record floor, and support-six through
  support-nine caps are unchanged from the proved parent chain.
- Conservative leaves are retained unless their exact premium exceeds the
  safe ceiling; exactly those 218 distinct defect tuples are rerouted.
- The pairwise carrier router is exhaustive for every positive support-three
  offset represented by a rerouted tuple.
- Every eligible fixed union uses the joint support-4/support-5 charge on the
  original outside evaluation matroid.
- Lane computations are exact integer enumerations, not samples.
- This node closes `K'=73`; it does not claim `K'=74` or the full rank-nine
  interval.
