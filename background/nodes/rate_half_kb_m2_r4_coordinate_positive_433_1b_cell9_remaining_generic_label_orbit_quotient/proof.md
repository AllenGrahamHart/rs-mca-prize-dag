# Proof

The endpoint theorem removes all 30 labels with `xi in {5,6}`.  The stronger
base-locus theorem removes every role on the exceptional source stratum, so
the cell-9 frontier lies entirely on the leading-open curve and has the 75
labels with `xi in {0,...,4}`.

The universal quotient fixes the endpoint index set and partitions its
complement into 24 whole orbits with size profile `1:1,2:9,4:14`.  Both
generators preserve cell `9`, source signs, target lanes, equations, and
guards.  Thus exclusion transports within each orbit. QED.
