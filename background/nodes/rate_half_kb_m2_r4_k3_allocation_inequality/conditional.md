# Conditional proof

The positive-coordinate premise supplies the exact active value
`U_positive`. The corrected geometry assembly supplies the disjoint active
value

```text
U_geometry = U_source_line + U_source_cover + U_trivial.
```

Both values retain the same row, partition, owner, slope unit, bridge
multiplicity, and chronology. Therefore their disjoint sum is

```text
U_K3 = U_positive + U_geometry.
```

The allocation-definition premise supplies a certificate-bound
`U_K3_allocation` after accounting for the sibling `U_Q` and `U_new` cells.
Exact integer addition, subtraction, and comparison then prove the claimed
inequality whenever the printed slack is nonnegative.
