# Result

Tensor rank three is reduced to an explicit incidence object. Four domain
rows form a projective frame, no off-line slope supports three of them, and
at least `e-8` slopes support exactly one of their six pairs. This frame is
the complete rank-three successor to the rank-two exclusion.

The remaining alternatives are now:

```text
rank 3:  exclude the triple-free four-row frame using support/source data;
rank >=4: obtain a different coefficient-rank or source/Hankel bound.
```
