# Frontier

Apply the same direct-normalization and dual-elimination pattern to the
swapped square allocation. Then process the mixed allocation, the two other
relative `xi` orbits, the moving-moving template, the negative factor loci,
and the separately homogenized `w=0` boundary.
