# Claim contract

- **Claim ID:** `rate_half_kb_m2_r4_diagonal_c2_112_near_positive_fixed_xi_direct_square_exclusion`
- **Input:** a deployed-field positive near-aligned reconstructed source form
  in the fixed-moving, `xi=a`, direct-square affine chart.
- **Output:** failure of the necessary q-slice identity.
- **Deletion interface:** remove only this one allocation chart from the
  near-aligned positive queue.
- **Non-output:** no deletion of the other 17 affine positive charts, the
  projective `w=0` boundary, negative candidates, a packet, row, owner,
  payment, or Prize target.
