# Claim contract

## Proved

- The deployed M31 near-rational and primitive one-pencil constructors compile
  already theorem-certified line-local slope lists into exact paid profiles.
- Their exact numerators are `1` and `2`; their slope denominator is `p`,
  while the calibrated natural profile denominator is `p^67447` and its
  supplied natural numerator is `1993679`.
- The explicit `F_241` received line has eleven genuine, distinct-slope
  explanation states with complete agreement, parity, and ray certificates.
- Exactly two listed states form the certified split-pencil owner; deleting
  them leaves an eight-neighbor actual residual satisfying `3+7` with margin
  `18295`.

## Not proved

- The audited deployed average ceiling `1993678` is not recomputed here; it is
  an explicit calibration input to the profile compiler.
- No deployed C1--C8 classifier is exhaustive, and no count of all realized
  profiles or pencils is supplied.
- No universal deployed M31 residual shell bound, row-sharp Q theorem,
  aggregate first-match payment, adjacent crossing, or Prize endpoint follows.
- The `F_241` regression is not a deployed-row or asymptotic theorem.
- This result does not populate or pay any KoalaBear four-cell MCA ledger.

## Falsifier

A malformed or duplicate support; unequal prefix; failed polynomial agreement;
an invalid parity or ray certificate; duplicate semantic slope; a wrong locator
root set or common core; any additional split pencil parameter; a residual
other than the declared eight states; failed exact arithmetic; or a DAG edge
that promotes this evidence atom to a required deployed payment.

## Verification

```text
tools/ramguard tiny -- python3 background/nodes/l1_m31_semantic_owner_profile_regression/verify.py
```
