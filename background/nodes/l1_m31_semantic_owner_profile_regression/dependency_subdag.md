# Dependency sub-DAG

```text
l1_m31_semantic_owner_profile_regression [PROVED]
                         |
                         ev
                         v
l1_mixed_petal_amplification [TARGET]
```

The finite-field result is proved directly. Upstream PR `#1047` is exact
provenance and a formally checked source, not an unproved logical premise.
The outgoing edge is evidence-only because deployed owner exhaustivity and
the all-residual `3+7` theorem remain open.
