# Proof

The constrained compiler proves that the three common rows have exact
finite nonzero forced products, nonsingular product involutions, and the
same five forced-type interface.  Solving the multiplicative relations
among `X,Y,Z,U,V` after setting each possible type equal to `p` gives the
five residual lists `(KB43Y-2)`.  Guarded products make every cleared
denominator nonzero.

An actual complete paired-product lift must choose one of fifteen matchings
of its residual sextic.  Substitute each pair in

```text
Gamma yz-Alpha(y+z)-Beta=0.
```

Eliminate `x` or `q` between one equation and each other equation, then
eliminate `a`.  A solution forces the resulting universal obstruction to
vanish.  The same 75 universal polynomials used in the `M2/M3` theorem apply
because only the base constants differ.

Each algebra in `(KB43Y-1)` is a quadratic extension of a degree-four
quotient and is free of rank eight.  Normalize its quadratic generator `z`
so that

```text
z^2=s z-1.
```

For an element `u+vz`, its norm is `u^2+suv+v^2`; it is a unit exactly when
that norm is coprime to the degree-four base modulus.  Exact custom
coefficient arithmetic evaluates all 450 obstructions and obtains units.

The audit shares the second rather than first pair equation in every
resultant chain.  It tests units independently by the rank of multiplication
on the basis `1,t,t^2,t^3,z,zt,zt^2,zt^3`.  All matrices have rank eight.
The expensive `DF/EF` templates are split into five-index processes only to
respect the local 60-second wall limit; their union is all 180 cases.

Thus `X2,N1,L1` have no complete product lift.  The parent exclusion removes
`M2,M3`, while the common-`K` census already removed the other four cells.
This exhausts `(4,3,3)`. QED.
