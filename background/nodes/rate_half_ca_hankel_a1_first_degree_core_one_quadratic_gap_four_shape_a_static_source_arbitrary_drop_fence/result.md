# Result

`PROVED`. Every run length `q=0,...,n` occurs with all source weights
nonzero and exact middle-Hankel corank one. The complete static
bordered-source flag can therefore stagnate arbitrarily far.

The next Shape-A attack must use global parameter coupling, the three-class
source partition, the split-fiber weld, or collision geometry.
