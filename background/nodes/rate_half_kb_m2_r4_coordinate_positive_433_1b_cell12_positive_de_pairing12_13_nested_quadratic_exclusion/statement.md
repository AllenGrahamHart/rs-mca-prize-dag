# Statement

Over `F_p`, `p=2130706433`, the guarded cell-12 positive `433-1b` route is
empty for the positive parallel-`DE` missing role `xi=0` at residual
matching `12`. The exact generic label quotient transports this to

```text
{(0,12),(0,13),(1,12),(1,13)}.
```

This is one generic orbit. No other matching is decided.
