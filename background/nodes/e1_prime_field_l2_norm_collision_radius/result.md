# Result

PROVED. At the live `N=256` clean-anchor cells every finite-field E1 collision
has minimum raw-representative swap distance at least five. At `N=512` that
minimum is at least two.

This removes the cheapest local-trade falsifier at rates `1/4` and `1/8`, but
does not bound the total collision-pair ledger in the surviving bands and does
not pay an unsafe row.
