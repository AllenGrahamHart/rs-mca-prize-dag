# Frontier

Classify the remaining five target-sign matching orbits.  The small-field
census identifies cells `11/14` as the only `F_29` common survivors, but
this is route evidence rather than a deployed theorem.
