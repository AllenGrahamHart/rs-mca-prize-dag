# Audit

Modal app `ap-RX2pnnmJRiFhsRSBEJS6To` ran both independent orbit classifiers
in 1.110289 aggregate worker-seconds. The primary canonicalizes every generic
triple; the audit generates full affine group orbits and deletes them from the
exact triple set. Their 57 row packets agree byte-for-byte after metadata.

The local checker independently reconstructs all weld sets, intersections,
unions, inclusion-exclusion support counts, the three shape multiplicities,
and the 243,285,056-vector chamber. It also checks source hashes and exact
coverage of all 325,376 generic triples.

Both tasks used one CPU, 256 MiB, a 60-second cap, and at most two containers.
The campaign remained below the conservative `$0.05` ceiling. The packet is
classification evidence only and is not used as a moment exclusion.
