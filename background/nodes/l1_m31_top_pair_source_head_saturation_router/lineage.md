# Lineage - M31 top-pair source-head saturation router

The upstream proper-`G` packet supplied the reduced determinant only for a
selected source-basis pair. The dense-top router supplies maximal overlap for
many actual pairs. Combining them forces equality in every determinant degree
bound and identifies its scalar with the source head difference. The
fixed-core payment then refines the resulting dense anchor to a colored
core-shadow target.
