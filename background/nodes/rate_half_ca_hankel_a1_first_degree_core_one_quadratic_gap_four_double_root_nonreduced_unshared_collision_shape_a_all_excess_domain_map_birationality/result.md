# Result

`PROVED`: for every official Shape-A survivor and every nontrivial minimal
tensor presentation,

```text
parameter coefficient map degree = 1,
domain coefficient map degree    = 1.
```

The two source lines are therefore the normalizations of both projective
coefficient images. Repeated parameter or row types can occur only as
distinct branches over image singularities.
