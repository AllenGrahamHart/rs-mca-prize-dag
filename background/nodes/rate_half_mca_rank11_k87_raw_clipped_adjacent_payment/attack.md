# Attack

Reconstruct the ordinary/offset partition independently, mutate one offset
count or threshold classification, delete one residual carrier profile,
remove an extension-factor floor, or replace a support-disjoint pair choice
by an unsupported overlapping composition. Any surviving mutation falsifies
closure.

The proof is finite and row-specific. No trend extrapolation to `K'=88` is
part of this node; that row requires a new exact frontier and component
payment.
