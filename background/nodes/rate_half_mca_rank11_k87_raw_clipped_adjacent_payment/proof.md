# Proof

Put `q=77`, `m=67559`, and `n=1048663`. Partition exact support-2/3
completion maxima into the ordinary lane `M_3<=M_2` and the unique positive
offset lane `M_3-M_2=d` for `d in {1,...,76}`.

The ordinary lane is exhausted by the proved single and `T23/A23` completion
carriers. Fresh primary and independently ordered audit traversals agree on
542,840 source units, 3,799,880 raw rows, 121,895 raw-safe units, 4,385
expanded units, 2,940,875 geometry rows, and premium

```text
41460244206367810395288131753780101229368111530<P_87.
```

For the positive offsets, the primary and independently written raw
traversals agree on all 17,801,784 source units and 124,612,488 raw rows.
Exactly 17,290,107 units are raw-safe. The remaining 511,677 units occur
precisely in offsets 1 through 43; offsets 44 through 76 are entirely
raw-safe. The largest raw-safe value is `P_87`, at

```text
s2=55/s3=46/s4=37/s5=30/offset9/c6F/c7F/c8F/c9F.
```

It remains to pay every raw-unsafe carrier profile. The exhaustive pairwise
carrier atlas exposes each fixed-union stratum. For each valid support-5/6
charge, the raw support incidences are divided by their exact eleven-set
extension factors and imposed inside the fixed-union adjacent-support
polytope. The proved fractional-knapsack theorem gives its exact rational
weighted maximum. The router may combine only support-disjoint adjacent
pairs; it never combines overlapping pair bounds.

Paired cached traversals cover all 43 unsafe offsets, all 14,388,660 source
units on those offsets, all 511,677 raw-unsafe units, and 77,179,660 carrier
profiles per implementation. Every resulting raw-clipped value is at most
`P_87`. Together with the raw-safe and ordinary populations, this proves that
`P_87` is the complete carrier frontier.

The exact safe premium ceiling is

```text
41460914669043067085305042221812436226076443389,
```

so its margin over `P_87` is the stated positive integer. Substitution in the
integral component ledger gives total capacity

```text
921060890011284709657056363808900069597352462765767795701103981
```

against required incidence

```text
921060967723676391242250303252610946492991464556444164407248882.
```

Their difference is the stated positive component gap. QED.
