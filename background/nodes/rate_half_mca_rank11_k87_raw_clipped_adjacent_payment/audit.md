# Audit

1. Ordinary plus offsets 1--76 is exhaustive and disjoint after the proved
   support-2/3 carrier-position constraints.
2. The ordinary lane uses fresh complete primary and audit traversals.
3. The raw scan retains every offset unit before threshold classification.
4. Raw-safe pruning is monotone because every later valid charge only lowers
   the premium.
5. Every unsafe unit enters exhaustive carrier geometry.
6. Raw support caps are converted to circuit caps with exact extension-factor
   floors before entering the fixed-union linear program.
7. Only support-disjoint adjacent pairs compose; overlapping pairs do not.
8. Primary and independent traversals agree on all lane counts and bounds.
9. The safe ceiling and component gap are recomputed with exact integers.
10. No numerical pattern beyond `K'=87` is promoted or used in this proof.
