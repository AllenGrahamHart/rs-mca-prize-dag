# Claim contract

- **Claim:** the rank-nine `K'=87` row is paid and the closed prefix is
  `10..87`.
- **Finite partition:** exactly `ordinary + offsets 1..76`.
- **Ordinary custody:** fresh paired primary and audit traversals agree on the
  complete ordinary lane and place it strictly below the global leader.
- **Threshold split:** all offset units are retained until classified; offsets
  `44..76` are entirely raw-safe, and exactly 511,677 unsafe units in offsets
  `1..43` enter residual geometry.
- **Geometry:** the exhaustive pairwise carrier atlas supplies fixed-union
  strata; the proved raw-clipped adjacent-support theorem imposes independent
  support-5/6 incidence caps before weighted optimization.
- **Composition rule:** only support-disjoint adjacent pairs may be composed.
  No overlapping pair bounds are combined.
- **Audit bar:** primary and independent traversals agree exactly on all
  coverage and premium checks in the ordinary, raw, and clipped waves.
- **Nonclaim:** no row `K'>=88`, full rank-nine interval, LIST terminal, MCA
  terminal, or prize terminal is closed here.
