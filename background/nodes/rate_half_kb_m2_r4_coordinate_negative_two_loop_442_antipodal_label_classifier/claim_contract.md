# Claim contract

| field | content |
|---|---|
| claim | Every actual negative `(4,4,2)` two-loop packet obeys the two monomial label identities `(KB44-1)`, and the antipodal five-set condition reduces their solutions to the three root-of-unity loci `(KB44-3a)--(KB44-3c)`. |
| deletion interface | Any signed packet off the three printed loci is deleted before complement-fiber completion. |
| assumptions | The exact coordinate source-facet five-set, the `(1,1,0;1,1,1)` edge skeleton, and the two-loop product-to-q weld. |
| nonclaim | The three exceptional loci are empty; the `(4,3,3)` two-loop skeleton; positive parity; an owner/payment; a row; or either Prize theorem. |
