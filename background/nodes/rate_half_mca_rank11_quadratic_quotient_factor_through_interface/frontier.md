# Frontier

The remaining quotient task is semantic and quantitative: charge all
first-owned records whose scalar-locator map factors through `X^2` or
`X+kappa/X`, without double-counting factor slices or chronology owners.

The algebraic inverse step is complete in degree two. General shifted
inversion and degrees `1,3,...,11` remain separate branches.
