# Audit

- Two distinct fibers force `u,v` to be independent.
- Antipodal locator span: `<X^2,1>`.
- Constant-product locator span: `<X^2+kappa,X>`.
- Quotient maps: `X^2` and `X+kappa/X`.
- Postcomposition: one invertible base-field `2 x 2` basis change.
- Output concerns the actual slope map `f=-u/v`.
- No record currency is paid.
- **Verdict:** GREEN.
