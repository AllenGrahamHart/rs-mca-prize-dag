# Result

The seven non-generating official F2 row families require no separate extras
payment. Their final t-null block census and weighted kernel problem are exact
copies of three generating signed types over `F_p(mu_n)`. The remaining F2
mathematics is common generating-row mass control for five signed types,
together with the already printed finite normalization seam.
