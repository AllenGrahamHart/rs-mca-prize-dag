# Result

The nonconstant order-one color frontier starts at degree two for `h=7` and
degree four for `h=15`. In particular the complete cubic `h=15` color
chamber is now theorem-empty in addition to the already excluded linear and
quadratic chambers.

The constant `h=15` chamber and all degrees at least four remain open.
