# Audit

- The common core is split between `E` and its complement.  The outside
  part has size at most `K-1` because the affine-line direction is nonzero.
- Synchronization uses two top anchors and at least `K` coordinates in the
  intersection of the inside common core with the new explanation support.
- After synchronization, slopes are affine-line parameters; no owner
  multiplicity is charged on the high side.
- The low side uses only a cumulative ordinary-list cap of 26 and the very
  conservative owner multiplier `e`.
- The final `3535161` is a contradiction bound under the unsafe hypothesis,
  not a universal profile asserted for every family.

The primary verifier pins all three parent theorem packets and rejects four
mutations.  The independent audit recomputes the Johnson ratio and strict
integer margin without importing the primary implementation.
