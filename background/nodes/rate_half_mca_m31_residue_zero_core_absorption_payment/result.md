# Result

`PROVED`: Mersenne-31 full-lift support `e=98232` is safe.  An unsafe family
would force a near-maximal top-line core; that core synchronizes every layer
with `h>=30791`, while one ordinary punctured-list cap pays all lower layers.
The contradiction bound is `3535161`, leaving margin `13242054`.

The Mersenne full-lift residual interval is now
`98233<=e<=1044241`.
