# Claim contract

| field | content |
|---|---|
| claim | Every guarded common packet in the cubic-root orbit satisfies the explicit degree-12 equation `G(b)=0`. |
| assumptions | The parent cubic row and regular `c` reconstruction, which already includes the only product-denominator analysis. |
| exact interface | Direct resultants in `t` and `r`, with identity `(2)` in every sign row. |
| nonclaim | Sufficiency of `G`, exact survivor count, outside realization, another matching orbit, coordinate or row closure, or either Prize theorem. |
