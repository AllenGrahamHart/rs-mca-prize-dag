# Audit

1. The theorem assumes `rank B=6`; it does not absorb base-rank drop.
2. The all-zero quotient branch is retained even though it has no nonzero
   pivot chart.
3. The three-minor replacement is valid only after localizing at `q_i!=0`.
4. Overlaps between pivot charts are harmless and are not counted as
   distinct packets.
5. The independent checker exhausts a finite test alphabet of quotient
   vectors and verifies every available pivot chart.
