# Dependency sub-DAG

```text
barycentric support-polynomial compiler [PROVED] --+
                                                     |
normalized c=2 gap-span compiler [PROVED] -----------+
  -> minimum-support barycentric collision router [PROVED]
       -> adjacent crossing [TARGET, evidence]
```

The router removes the generic outer quartic and every collision partition
except one equal pair from the minimum-support case.  The residual normalized
pair-collision curve belongs in the contributor compute ledger; the theorem
authorizes no dense official-degree polynomial construction.
