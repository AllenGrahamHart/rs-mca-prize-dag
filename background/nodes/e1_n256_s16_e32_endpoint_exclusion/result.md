# Result

`PROVED`: every `V=64` case is impossible. The exact three-profile reduction
is exhausted by the proved `(0,8)`, `(3,5,1)`, and `(4,7)` exclusions.

The live positive even variance frontier in folded profile `(3,4,0)` advances
from `V<=64` to `V<=62`. The lower variances, folded profile `(4,2,0)`, and
later swap bands remain open.
