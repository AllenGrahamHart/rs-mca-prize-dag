# Audit

The factorization `L=e_3^(-2)(E-alpha)(E-beta)` uses both multiplicities
being exactly three. It is invalid for every other cubic color partition.

The divisions use `e_3*d*r*(r-1)*g(1)!=0`, all of which follow from the
degree stratum and inherited order-one saturation. The resultant is only a
necessary projection and may retain roots introduced by quadratic leader
degeneration.

No norm gcd has been run while Modal is spend-blocked. A nonunit gcd would
still owe the cubic fiber factorization, exact color values, all cyclotomic
tests, and the inner lift.
