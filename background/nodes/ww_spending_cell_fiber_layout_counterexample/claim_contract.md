# Claim contract - unsafe spending-cell fiber-layout counterexample

- **claim:** extending W3's upper inequality to the unsafe spending cell is
  false.
- **row:** the explicit prime-field rate-`1/4` row in `statement.md`.
- **receiver:** one exact core/petal/background partition with six distinct
  nonzero petal labels.
- **witness:** the factored degree-`k-1` polynomial
  `L_(C\D)(X)(X^256-z_0)`.
- **strict comparison:** seven listed polynomials against `B*=6`.
- **consumer consequence:** clean-rate adjacency cannot use W3 as an upper
  bound at this lower endpoint; it must use the independent safe and unsafe
  corridor theorems.
- **nonclaim:** the counterexample refutes neither the list prize nor W3's
  literal safe-side claim; its cell is already on the globally unsafe side.
