# Claim contract

- **Claim:** locator interpolation embeds a rank-at-least-`r-1` family into
  the restricted Koszul kernel, forcing `sr(G)>=(e+1)/2` officially.
- **Inputs:** the exact three-class row identity, locator `X`-degree,
  class sizes, and restricted Koszul kernel dimension.
- **Output:** all ranks below `91625968982` are excluded.
- **Consumer:** `rate_half_band_crossing_location`.
- **Nonclaims:** no injectivity of the full interpolation map and no
  exclusion of the remaining upper-half rank interval.
