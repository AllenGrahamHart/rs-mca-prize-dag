# Frontier

Remove cells `1,2` from the split workboard. The next comparison starts at
the B/C orbit `[3,6]`: import its common-locus classification only, then
determine whether common emptiness or a compact component cover avoids its
O0a-specific outside ledger.
