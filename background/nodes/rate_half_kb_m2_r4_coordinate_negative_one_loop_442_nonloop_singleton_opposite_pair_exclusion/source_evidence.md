# Source evidence

The bounded common-atlas compiler identifies cells `11,14` as one orbit and
prints the product minors and q welds.  The proof reduces those equations to
the guarded factor table `(3)`; the verifier reconstructs the table directly
from the Vieta rows and weld formula.
