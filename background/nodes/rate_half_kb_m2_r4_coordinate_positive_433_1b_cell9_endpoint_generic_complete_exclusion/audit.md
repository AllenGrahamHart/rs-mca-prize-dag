# Audit

- Missing products are recovered only where the replay proves `A(x)!=0`.
- Every residual matching is enumerated rather than inferred by symmetry.
- The full nonzero/distinct/non-antipodal target guard is inverted.
- The section-base points are not silently included; they have a separate
  regularization node.
