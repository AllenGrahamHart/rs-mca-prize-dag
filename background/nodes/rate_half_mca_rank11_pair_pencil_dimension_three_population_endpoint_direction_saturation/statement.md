# Population-endpoint direction saturation

- **status:** PROVED
- **scope:** the scalar-dimension-three endpoint `q=3170`,
  `4960<=K'<=4982`

Let `R` be the number of projectively distinct scalar directions represented
by saturated 15-point affine lines. Then

```text
41746<=R<=47836.                                    (DS1)
```

Every represented direction polynomial has at least

```text
K'-2609>=2351                                      (DS2)
```

residual domain roots. Summed over all represented directions, unused degree
capacity is at most 30,203,244. Equivalently, aggregate root saturation is at
least

```text
5750430/6589409 > 0.8726.                           (DS3)
```

This is a finite `(Q)`/split-pencil census input. It does not classify the
direction map, exclude the endpoint, or pay rank eleven.
