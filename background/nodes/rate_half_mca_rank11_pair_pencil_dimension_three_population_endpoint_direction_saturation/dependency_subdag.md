# Dependency sub-DAG

```text
population_endpoint_plane_line_design
                    |
plane218_projective_direction_bank
                    |
                    v
population_endpoint_direction_saturation
                    |
                    v
rate_half_band_crossing_location (evidence only)
```

The output is an exact finite `(Q)`/split-pencil input, not a payment edge.
