# Audit - L1 fixed-support cross-determinant fiber bound

## Checked axes

1. The quotient uses selected support locators, not full petal locators.
2. It counts all missing-equation syndromes in one fixed support pattern.
3. Saturation of the base pair is load-bearing in the injectivity step.
4. Monicity lowers the degree of a locator difference to at most `d-1`.
5. Negative cross slack means a singleton, not an empty profile.
6. The aggregate sums defect degrees and does not add a spurious syndrome
   factor.
7. Source-chart first-match multiplicity remains outside the theorem.

## Remaining attack

Handle unbounded `r_cross` inside the proved width window, or charge those
cells to a base-field-normalized split-pencil/signed-profile owner. Separately
bound non-intrinsic source-chart supply.
