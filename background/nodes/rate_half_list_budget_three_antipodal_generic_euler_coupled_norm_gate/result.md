# Result

The generic scalar prefilter now has two independent character tests and one
exact coupling. The zeroth-contact norm must be a fourth power, and together
with the cubic Euler norm it must satisfy `N_T^4N_Q^3=d^(4v)` before any
vector remainder is evaluated.

Uniform scalar rejection remains open.
