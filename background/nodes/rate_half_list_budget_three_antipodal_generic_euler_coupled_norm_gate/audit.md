# Audit

- Checked the resultant orientations at the zeroth and Euler contacts.
- Eliminated `N_U` only after retaining both exact norm identities.
- The verifier checks fourth-power subgroups for fields with
  `gcd(4,q-1)=2` and `4`, plus exact coupling pass/fail controls.
- Character membership is not treated as sufficient for the exact coupling
  or the full Euler remainder.
