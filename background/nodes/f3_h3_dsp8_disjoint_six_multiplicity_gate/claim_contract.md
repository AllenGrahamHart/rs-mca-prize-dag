# Claim contract

- **claim id:** `f3_h3_dsp8_disjoint_six_multiplicity_gate`
- **mathematical statement:** after the proved distance-four and overlap
  deletions, a `P>=25` target retains at least 11 disjoint distance-six edges
  in the antipodal-free class and at least two in the antipodal class; the
  `P>=33` tail forces pure stars of degrees seven and five
- **scope:** every odd-characteristic dyadic row and every DSP8 target
- **consumers:** DSP8 correlation and the CR-001 official-order template
  generator
- **status:** `PROVED`
- **proved dependencies:** rich-excess centroid ladder, oriented
  distance-four pseudoforest, and support-overlap payment
- **new open content:** enumerate or bound official prime divisors of the
  normalized two-edge/star ideals and retain `R(t)` on survivors
- **falsifier:** one retained target violating `(DSM1)` or `(DSM2)`, or one
  selected ideal not contained in the actual degree-one row-prime ideal
- **nonclaims:** no survivor bound, no quotient decorrelation, and no DSP8 or
  C36' promotion
- **replay:**
  `tools/ramguard tiny -- python3 background/nodes/f3_h3_dsp8_disjoint_six_multiplicity_gate/verify.py`
- **upstream mapping:** primitive shift-pair control / exact finite SPI ledger
