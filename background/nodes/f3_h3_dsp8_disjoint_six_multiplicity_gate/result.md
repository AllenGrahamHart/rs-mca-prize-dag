# Result

The mixed low-distance candidate condition now has a pure
distance-six refinement:

```text
cutoff       antipodal-free class       antipodal class
P>=25        >=11 edges; degree >=2     >=2 edges
P>=33        center degree >=7          center degree >=5
```

At the direct DSP8 cutoff, use a two-leaf disjoint distance-six star ideal in
the free class and a same-target two-edge ideal in the antipodal class. At the
paid-tail cutoff, use pure distance-six stars with seven and five leaves.
These templates retain exactly the six-disjoint-atom geometry consumed by the
split-pencil router and are a sharper CR-001 preprocessing contract than a
mixed distance-four/six bi-star alone.

The critical correlation and survivor-count nodes remain open.
