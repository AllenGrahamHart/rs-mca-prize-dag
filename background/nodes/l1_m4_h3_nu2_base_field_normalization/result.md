# Result

The surviving positive `(nu,deg H)=(2,1)` records, if any, are completely
base-field-normalized on the two remaining characteristics. Their inner and
outer polynomials, domain binomial, and complement lie in `F_p[Z]`; one
fiber is fixed by Frobenius and the other two are exchanged. The remaining
task is now an exact prime-field Belyi regular-fiber exclusion or count. L1
remains open.
