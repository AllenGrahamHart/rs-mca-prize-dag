# Upstream crosswalk - nu=2 base-field normalization

This is directly in the upstream language of the base-field-normalized
split-pencil census. In the last positive `(2,1)` Mersenne stratum, every
object in the factorization can be taken over `F_p`, with one fixed and one
conjugate pair of split fibers.

An upstream `(A)` packet can consume this as the entry gate to a finite
prime-field Belyi census. It is not an h=3 exclusion, exact SPI count, or
finite MCA payment.
