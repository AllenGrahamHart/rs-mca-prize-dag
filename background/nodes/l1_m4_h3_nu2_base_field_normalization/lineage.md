# Lineage

The multiplicity theorem normalized the inner map over `F_p`, while the
value-coset certificate normalized the outer cubic up to one scalar. Their
tangent values identify that scalar as prime-field. A fixed split value then
forces the normalized domain coset, and hence the complement, to be
Frobenius-stable as well.
