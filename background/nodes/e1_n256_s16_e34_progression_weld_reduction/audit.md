# Audit

The primary verifier enumerates all heavy triples and proves that the 7,936
progression triples quotient to exactly 62 repeated-length forms with 128
triples per form. It checks the repeated-class cancellation obstruction,
reconstructs the two extension and four singleton-weld positions, proves
their distinctness, and verifies the support count and DAG wiring.
It also constructs an odd unit carrying every form to the representative with
the same 2-adic valuation and checks the exact orbit multiplicities.

The independent audit derives both candidate sets by direct circular-distance
enumeration and recounts the chamber by all 16 occupancy patterns on the four
weld positions. A hostile third repeated-class heavy-light chord makes equal
outer signs arithmetically possible, showing that the proved two-extension
bound is load-bearing.

No large computation is load-bearing. Both verifiers are bounded by the
341,376 heavy triples and 16 special-position masks.
