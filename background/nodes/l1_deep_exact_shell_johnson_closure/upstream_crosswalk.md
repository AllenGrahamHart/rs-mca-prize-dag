# Upstream crosswalk - deep exact-shell Johnson closure

## Paper D coordinates

The boundary `2m=n+k-1` is the same profile boundary used before the shifted-
lattice split-pencil reduction.  Strictly above it, AM--GM places the list in
the ordinary Johnson region.

## Portable payment

All complete agreement shells above the boundary cost at most `n^2` in
total, hence at most `2^88` over the full prize box.  No base-field floor or
split-pencil theorem is needed for this polynomial L1 payment.  The coarse
number is not asserted to fit each finite adjacent-row reserve without the
surrounding compiler.

## Remaining input

The active list-interior obligation is confined to
`2m<=n+k-1`, where the exact-shell balanced shifted-lattice reduction sends
every survivor to the guarded balanced split pencil.
