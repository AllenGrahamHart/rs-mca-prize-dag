# Result

The four active labels in `{(3,11),(3,14),(4,11),(4,14)}` are proved empty. After this payment,
no active labels remain in the 75-label active workboard. The 30 endpoint
labels were closed independently, so exact aggregate composition can follow.
