# Dependency sub-DAG

```text
mean-centered prefix theorem -----------+
                                         +--> recursive line peeling --> MCA router
boundary affine-line-bank theorem ------+
                                         |
common-core absorption theorem ---------+
```

All parents are `PROVED`.  The new node adds the residual recursion and the
pairwise core-packing invariant; neither is assumed from a conditional
node.
