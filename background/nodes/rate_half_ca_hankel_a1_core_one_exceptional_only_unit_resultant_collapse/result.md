# Result

The official-scale unit pair has a small intersection certificate:
`Res_X(Q,B_1)` is only the degree-`e-1` leading cofactor `q_bar`, and the
companion `W` resultant is supported only on `E q_bar`. This is the selected
intersection interface for the corrected exceptional-only classifier.
