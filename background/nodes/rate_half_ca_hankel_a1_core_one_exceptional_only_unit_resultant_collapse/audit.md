# Audit

- Checked the resultant orientation: `r` is odd, `n_X` is even, so the
  symmetry sign is positive.
- Retained the leading-coefficient powers in the nonmonic resultant formula;
  dropping them would create a false inverse-polynomial contradiction.
- Used the newly proved exact degree `deg_X B_1=D_0`; the exponent difference
  `D_0-n_X=1` is essential to `(EUR2)`.
- Parameterized `(EUR3)` by the actual `m=deg_XW`; `m=r-1` is used only when
  `v_inf!=0`.
- Resultant support is not promoted to absence of roots or profile exclusion.
