# Result

`PROVED`. The padded center contributes exactly one simple contact to the
Padé resultant. Thus

```text
C_0(x_*)!=0,
B_src(gamma_0,x_*)!=0,
I_((gamma_0,x_*))(Qbar,P_F)=1.
```

The source-numerator pencil therefore does not send `x_*` to the padded
center.
