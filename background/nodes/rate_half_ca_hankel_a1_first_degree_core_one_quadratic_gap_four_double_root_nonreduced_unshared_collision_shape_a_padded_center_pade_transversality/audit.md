# Audit

1. The formal resultant has no leading-coefficient contribution at the
   center because the center locator has exact degree `d`.
2. The `g_*` center root is simple and `S_B` is a unit there, so the
   resultant order is exactly one.
3. A single exhibited common point already consumes that entire order;
   no separate root classification is needed.
4. Local intersection multiplicity one implies both specialized
   `X`-roots are simple and the two curves are transverse.
5. The derivative of the specialized Pade identity retains no
   `Lambda_X` term because `Lambda` is independent of `X` and vanishes at
   the center.
6. No critical status changes.
