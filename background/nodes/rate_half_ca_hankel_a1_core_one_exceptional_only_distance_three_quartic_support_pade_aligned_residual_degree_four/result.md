# Result

The sole all-deficient support leaf is now a quadratic-in-parameter pencil
of split squarefree divisors of degree one through four, realized at almost
`e` orbit coordinates. Every residual root is used at most twice.
