# Result

PROVED. Profile `(1,2,0)` cannot collide modulo any pair-feasible `N=512` row
prime. Together with the four-singleton exclusion, the entire `N=512,s=2`
band is closed and every surviving collision has raw swap distance at least
three.

The parent pointwise E1 collision target remains open at `s>=3`.
