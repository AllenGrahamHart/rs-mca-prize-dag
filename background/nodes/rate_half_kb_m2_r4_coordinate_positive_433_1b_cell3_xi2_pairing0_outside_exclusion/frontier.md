# Frontier

The aggregate `(KBP1B3-DE-FIRST-1)` now pays matching indices zero, one, and
two for `xi=0,1,2`.  The next missing record is `xi=3`, namely `df`; deleting
it at matching zero leaves residual pairs

```text
(de,de), (-de,sigma_o ef), (bf,sigma_c cf).
```

The existing two-point ledger cannot be substituted directly because `de` is
no longer fixed by the source missing ratio.  Two direct common-resultant
pilots exceeded the 300-second cap.  The next attempt must exploit the shared
`f` structure or a lower subresultant before any wider census.
