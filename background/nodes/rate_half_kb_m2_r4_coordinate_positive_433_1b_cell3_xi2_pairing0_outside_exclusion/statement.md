# Statement

## Claim `(KBP1B3-XI2P0-1)`

Work over `F_2130706433`, with `iota=16711679`.  On the guarded
product-rank-five branch of deployed positive `433-1b -> O0a` role cell `3`,
fix the negative `DE` missing record `xi=2` and canonical residual matching
`pairing=0`.  For every source-sign pair and target-sign lane there is no
guarded target realization.  Thus all

```text
4 source signs * 4 target lanes = 16
```

raw cases in this subfamily are empty.

If the source missing value is `m`, then the target product is `de=-m`, the
first residual pair is `(de,de)`, and the missing squared-sum row is
`(d-e)^2`.  The exhaustive reduction has the following exact shape per
source-sign pair:

1. the target-free paired cut has equal direct six-dimensional and
   quadratic-over-cubic tower norms;
2. the norm numerator has degree `422` and `401` terms, while its denominator
   has degree `156` and `157` terms;
3. the numerator has eight base-field roots, five on route boundaries and
   three live norm roots;
4. exact lifting leaves four guarded common points, exactly two satisfying
   the target-free cut; and
5. the eight resulting point/lane fibers split into four colored quartics
   with no field root and four with two roots; at all eight surviving roots,
   the two residual univariate `u` cuts have gcd degree zero.

Across all source signs this is a census of `32` point/lane fibers and `32`
colored-`f` candidates, with zero witnesses, zero target-boundary solutions,
and zero unresolved rows.

Together with `(KBP1B3-XI0P0-1)` and `(KBP1B3-XI1P0-1)`, canonical matching
zero is now excluded for all three parallel `DE` copies.  This claim does not
cover `xi=3,...,6`, another matching, complete cell-3 or route closure, K3,
LIST, MCA, or either Prize result.
