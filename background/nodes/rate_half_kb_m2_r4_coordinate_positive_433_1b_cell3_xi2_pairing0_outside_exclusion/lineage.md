# Lineage

- The global quadratic quotient supplied the six-dimensional common algebra.
- The `xi=0,pairing=0` packet established the norm, root-replay, and finite
  solver architecture.
- The two positive parallel `DE` copies were paid by direct computation plus
  exact transport.
- For the negative copy, the compiler explicitly changed both necessary
  signs: `de=-m` and the squared-sum row `(d-e)^2`.
- The resulting exact census proves the negative-`DE` subfamily empty and
  completes canonical matching zero for all three parallel `DE` copies.
