# Claim Contract

## In scope

- Necessary source cuts for roles `xi=0` and `xi=2`, matchings `0,1,2`.
- Complete deployed norm-root and direct-lift custody.
- Identification of the `A=B=0` branch with the proved base stratum.

## Out of scope

- Target residual emptiness, orbit transport, cell closure, K3, or Prize
  closure.

## Promotion rule

`PROVED` certifies only `(KBP1B9-DE-SOURCE-1)`.
