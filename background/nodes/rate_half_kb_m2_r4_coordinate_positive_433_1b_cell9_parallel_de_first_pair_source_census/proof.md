# Proof

For matchings `0,1,2`, the first residual pair is common.  Omitting the
positive `DE` record forces `P(m,-m)=0`; omitting `-DE` forces
`P(-m,-m)=0`, with `m=B(-t^2)/A(-t^2)`.

Reduce both cuts in every chart of `(KBP1B9-TOWER-1)` and take the
four-dimensional multiplication norm to `F_p(r)`.  Across the six charts,
the deployed numerator roots agree exactly: 11 for each positive row and 13
for each negative row.  Unioning all numerator, denominator, and inversion
roots gives 160 case-labeled candidate `r` values.

Direct replay against all seven common equations classifies every candidate.
Each source-sign row has four ordinary positive zeros and two ordinary
negative zeros.  The only `A=B=0` points are the two section-base points
already reconstructed by the regularization theorem.  All other candidates
hit a route boundary, have no deployed lift, violate a common equation, have
nonzero cut, or have `A=0,B!=0`.  Nothing remains unresolved. QED.
