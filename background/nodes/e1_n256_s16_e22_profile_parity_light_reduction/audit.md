# Audit

Modal app `ap-R4qgQHRIHJgTwquZvDY2X9` ran one 256 MB worker with a 60-second
timeout.  The source-pinned result is complete.  The independent checker uses
a distinct signed-sum enumeration, fixed-point dynamic program, recursive
profile generator, matching recursion, and hostile parity mutations.  It
reproduces `L<=14`, all nine profiles, the eight survivors, the 1,321-template
atlas, and the exact 26,219,123,456-vector floor.

The fixed cubic-Hermite majorant is absent by contract; no failed inequality
is treated as an exclusion.
