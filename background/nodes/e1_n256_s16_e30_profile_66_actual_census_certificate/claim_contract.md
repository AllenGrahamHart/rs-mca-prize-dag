# Claim contract

## Input

The exact 1,191-mask output of
`e1_n256_s16_e30_profile_66_relaxation_certificate`.

## Output

The complete actual-vector exceptional ledger: 6,244 cubic exceptions,
exactly 1,232 with conductor one.

## Falsifier

An omitted vector, a row mismatch, a failed direct witness replay, or a
different conductor count.  This node does not exclude the retained vectors.

