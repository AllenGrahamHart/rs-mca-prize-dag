# Replay

Run the parameterized compact audit with argument `81` under
`tools/modal_run_script_long.py`. Run all seven pure geometry lanes with the
same runner. The compact audit covers 12,695,949 plain and 40,569,326 reroute
evaluations; the geometry lanes cover 172,265,121 leaves.
