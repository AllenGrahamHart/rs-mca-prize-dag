# Claim contract

- Exact row only: `K'=81`, `q=71`, `m=67553`, `n=1048657`.
- Exactly the 39,570 above-ceiling tuples represented by the canonical digest
  are rerouted through the complete pairwise atlas.
- Every carrier, high-support, and geometry branch is retained.
- Counts and comparisons are exact integers; no sample is used.
- This node closes `K'=81`; it makes no claim about `K'=82`.
