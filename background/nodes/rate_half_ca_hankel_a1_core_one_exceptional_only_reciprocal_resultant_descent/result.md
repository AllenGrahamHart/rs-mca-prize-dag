# Result

The low unit resultant is now a one-step reciprocal Euclidean certificate.
Its `q_bar` factor is exactly the common point at infinity and cancels,
leaving `Res_Y(F,L)=c_XE^(r-1)`. The surviving first coefficient is coprime
to `q_bar`, so future classification needs only the exceptional resultant
support after this descent.
