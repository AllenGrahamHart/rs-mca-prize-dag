# Audit

- Used fixed-degree reversals at the proved degrees `r,D_0`; using a
  specialization-dependent degree would invalidate the reversal sign.
- Checked both signs: `(-1)^(rD_0)=-1` and
  `Res_Y(F,Y)=(-1)^rF(0)=-F(0)`, so the same scalar `c_X` survives.
- Retained the scalar factor `E^r` in `Res_Y(F,EG)` before cancelling the
  single `E` in `Res_Y(F,Y)`.
- Proved `E` does not divide `Delta_inf` from the exact degree-`r-1`
  exceptional fiber; the coprimality conclusion does not assume
  `gcd(E,q_bar)=1`.
- The resultant descent is not promoted to a profile exclusion.
