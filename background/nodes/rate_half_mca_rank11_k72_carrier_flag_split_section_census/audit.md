# Audit

- The node is green by an analytic flat-circuit witness count.
- The cap counts selected incidences, not projective coefficient lines.
- Circuit minimality and first-match ownership are load-bearing.
- The 33/36 flag is an actual fixed nested pair, not two independently chosen
  unions.
- The proof does not import upstream PR #1173.
- Rank-three and rank-four root caps are obtained only after dividing the
  fixed 36-point locator from the five-dimensional polynomial space.
- The exact arithmetic uses ordinary inside counts, so hidden parallel-class
  structure cannot be responsible for the positive margin.
- Satisfying this target alone is not yet declared a full K'=72 closure;
  the complete branch atlas must be replayed against the resulting cap.
