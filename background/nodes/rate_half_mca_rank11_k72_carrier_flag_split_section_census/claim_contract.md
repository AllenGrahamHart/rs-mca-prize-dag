# Claim contract

- Exact row and dimensions: `K'=72`, `(u,g)=(33,8)<(36,5)`.
- `W<=H_3` and `D_3 subset D_45` are part of the flag, not inferred from
  dimensions alone.
- Exact support completion maxima are `M_4=M_5=31`.
- `I_4,I_5` count minimal circuit supports with selected eleven-set
  incidence and one authorized owner.
- The weights are the rank-nine deficits `21` and `15`.
- The printed cap is sufficient for the current exact premium ceiling; it is
  not claimed necessary or sharp.
- The target permits a weighted trade between support four and support five.
- No mode-at-null, generic-position, or random-word premise is assumed.
