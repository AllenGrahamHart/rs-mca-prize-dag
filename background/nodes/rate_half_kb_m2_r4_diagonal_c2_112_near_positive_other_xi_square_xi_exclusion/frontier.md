# Frontier

The fixed-moving other-`xi` direct square allocation is closed. Classify
the swapped square and mixed allocations in this orbit, then move to the
moving-moving template, negative loci, and homogenized `w=0` boundary.
