# Audit

- Traced `q_0|P_X` to the exact `c=1`, zero-row
  `epsilon_0=0`, and one-root exceptional deficit; it is not asserted on the
  `c=2` profile.
- Checked that `P_cl(gamma_0)` is nonzero before deriving `q_0|B(gamma_0)`.
- Checked the coprimality `gcd(E,Q)=1` before cancelling `E` against `Q`.
- Retained the correction `P_clJ`; omitting it would falsely recover the
  already-excluded trace-free degree box.
- The exact equality `deg_X A_1=D_0-r` is a nonclosure guard: the prior
  contradiction requires the strictly smaller bound `D_0-r-1`.
