# Claim contract

| field | content |
|---|---|
| claim | Both reconstruction denominators are units in every sextic sign row, and the representative `c,m` coordinates are `(KB41M-2)--(KB41M-3)`. |
| assumptions | The exact rank-six sextic quotients and explicit common product involution. |
| exact interface | Six-coordinate multiplication matrices with denominator norms `2^19` and `652`. |
| nonclaim | Any outside matching evaluation, outside sums, full interpolation, coordinate or row closure, or either Prize theorem. |
