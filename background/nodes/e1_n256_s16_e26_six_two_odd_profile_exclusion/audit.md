# Audit

The production engine uses folded signed chord classes. The independent audit
forms all ordered sparse products in the negacyclic ring and checks
anti-reciprocity before reading the profile. Both enumerate all 87 light
templates, `binom(124,3)` heavy supports, and 64 relative sign vectors.

The first census launch `ap-58gqxzADkt6TRnH4o03zey` failed at worker import
and completed zero tasks; it is not evidence. Production
`ap-TcZRS6xma3n8xdyUiNOg8b` and audit `ap-jP3CZ7pvZjaZ20jDUQ8LSF` agree on
every row after removing runtime metadata. The deterministic verifier replays
all 17,624 exceptional vectors and checks that the 8,060 norm inputs are
exactly their full-conductor subsequence.

FLINT and PARI run in `ap-R4vDRzg2rsb0k2gJD2xmqB`, agree on all 8,060 norms,
and use separate resultant implementations. The verifier checks their exact
maximum and both sides of `2*N_max<2^250<3*N_max`.
