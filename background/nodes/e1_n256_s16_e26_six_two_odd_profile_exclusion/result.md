# Result

`PROVED`: all six two-odd E26 profiles are impossible at `N=256`, folded
profile `(3,4,0)`, and `V=52` on every pair-feasible row.

The exact live endpoint residual is four six-odd profiles: `(6,5)`, `(5,3,1)`,
`(4,1,2)`, and `(6,1,0,1)`.
