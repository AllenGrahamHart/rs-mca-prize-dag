# Audit

The hostile audit rejects three nearby errors:

1. changing the minus sign in the resultant formula to plus;
2. deleting one of the eight projective chart masks;
3. treating `p2=0` alone as the infinity chart instead of requiring both
   leading coefficients to vanish.

The compiler itself is hash-pinned and its three emitted matching equations
are checked against the exact resultant string.
