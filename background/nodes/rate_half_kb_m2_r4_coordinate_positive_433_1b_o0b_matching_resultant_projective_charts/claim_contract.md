# Claim contract

- The equivalence is over the algebraic closure of `F_2130706433`, matching
  the guarded ideal-emptiness computations.
- A finite chart adds one existential root variable and two quadratic
  equations for that matching pair.
- An infinity chart adds the two leading-coefficient equations and no root
  variable.
- The eight charts replace only `q4,q5,q6`; `q3`, `q7`, the common basis,
  ordinary guards, and rank-cofactor saturation are unchanged.
- This node proves the decomposition, not emptiness of any chart.
