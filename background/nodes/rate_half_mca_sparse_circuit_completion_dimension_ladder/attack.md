# Attack notes

The direct K'=13 payment generalized poorly if sparse circuits were paid
recordwise and their rank-nine shadows were then discarded.  The geometric
part itself does generalize: the generalized-MDS completion ceiling is the
quotient dimension `q`, and equality is again the useful structured case.

This node deliberately retains all four support strata separately.  Their
different rank-nine shadow multiplicities are consumed by the separate
joint-ledger node rather than erased inside one coarse sparse total.

The next geometric wall is not K'=22.  The same completion theorem remains
valid there; the current numerical payment, not this lemma, first fails.
