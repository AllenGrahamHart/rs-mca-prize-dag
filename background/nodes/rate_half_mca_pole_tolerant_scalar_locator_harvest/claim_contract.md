# Claim contract

## Proved

- denominator-division-free exclusive localization;
- the exact `g<m`, `g>=m`, and `g=m` bounds;
- common-pole inclusion and exact-locator cancellation after one possible
  zero-scalar deletion;
- preservation of the reduced certificate, degree profile, and coincidence
  core.

## Not proved

- survival of support-wise MCA nontriviality after cancellation;
- an active first-match source compiler;
- a slope count for puncture-trivialized supports;
- `(E)` or either deployed adjacent MCA row.
