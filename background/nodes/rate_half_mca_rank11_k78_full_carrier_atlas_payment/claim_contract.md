# Claim contract

- Exact row only: `K'=78`, `q=68`, `m=67550`, `n=1048654`.
- Exactly the 11,552 above-ceiling tuples represented by the canonical digest
  are rerouted through the complete pairwise atlas.
- Every carrier, high-support, and geometry branch is retained.
- Counts and comparisons are exact integers; no sample is used.
- This node closes `K'=78`; it makes no claim about `K'=79`.
