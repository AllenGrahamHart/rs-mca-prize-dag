# Audit

1. The survivor generator factors the degree-1224 direct norm into 38 exact
   irreducibles and regenerates the four-point certificate byte-for-byte.
2. The independent verifier rebuilds all four trace equations from the
   pinned source compiler, checks every printed field modulus and coordinate,
   and verifies every open-set factor.
3. Only degree-3 factors embed in the deployed degree-6 field. Their moving
   quadratics split, so the full probe tests four explicit orientations.
4. The q-slice control passes before either full quotient norm is tested.
   Both norms fail for every orientation.
5. The off-common checker classifies every projection factor and replays all
   twelve residual combinations. Its six distinct endpoints are boundary.
6. The claim is deliberately a full-quotient exclusion, not the false claim
   that the moving-mixed q-slice is empty.
