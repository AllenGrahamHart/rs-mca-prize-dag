# Result

The earlier consumer wiring mixed two different depths. The full-subset F2
depth can pass the generated-field guard, but the exact-list corridor depth
misses it by at least `129` bits on every official row. The missing exact-slice
payment is now exposed as `u2c_exact_slice_extras_budget` rather than hidden
behind the guarded F2 terminal.
