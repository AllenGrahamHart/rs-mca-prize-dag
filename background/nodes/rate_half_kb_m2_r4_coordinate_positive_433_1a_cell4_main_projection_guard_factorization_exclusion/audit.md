# Audit

1. The certificate reconstructs the guard product with seven bounded plane
   pseudo-reductions and records the total leading-scale exponent `21`.
2. Ambient divisibility is explicitly false and is not used.  The proof uses
   the correct quotient-ring cross identity.
3. The norm of `f13` is factored exactly.  Its nonlinear factors are recorded
   as irreducible; its four linear roots are all original main-chart guards.
4. The contradiction uses only original guards `N0!=0`, `D0!=0`, and
   `w0!=r^2`; no sampled point or colored converse is used.
5. Exceptional scales and all seven transported sign/cell rows are delegated
   to explicit PROVED dependencies.
6. Mutation tests alter the pseudo-scale exponent, cross identity, norm
   factorization, and input custody; each alteration is rejected.
