# Lineage

The exact-one-repeat argument that closes degree two on `m=8` extends at the
moment-invariant level to `h=15`, but it is replayed independently at the
actual `m=16` row rather than asserted uniformly. The larger color group has
105 omission patterns; the same locator-product torsion condition excludes
all of them. The quadratic router then isolates one final even chamber.
