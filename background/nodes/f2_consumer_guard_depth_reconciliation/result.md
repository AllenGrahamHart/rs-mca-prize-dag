# Result

The F2 scope ambiguity is reduced to a route decision. Under the banked
generated-field guard, the native full-subset balance selects exactly the
five generating signed types. The exact-slice depth selects none. The
Round-20 exact-value calculation remains useful as a route-cut certificate,
but it is not itself an F2 terminal.
