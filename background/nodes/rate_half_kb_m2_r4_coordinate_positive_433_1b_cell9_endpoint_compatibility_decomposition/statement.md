# Statement

## Theorem (KBP1B9-EP-DECOMP-1)

Fix cell `9`, either endpoint role `BF` or `sigma_c CF`, and either source
sign pair.  On the guarded global common curve, the cleared endpoint
compatibility ideal is zero-dimensional and has exactly six deployed points.
Exactly four have nonzero evaluated leading coefficient.  The other two are
the complete deployed base locus of the stored polynomial kernel section.
The two base-locus points agree for the `BF` and `CF` cuts.

This theorem excludes no target lift.
