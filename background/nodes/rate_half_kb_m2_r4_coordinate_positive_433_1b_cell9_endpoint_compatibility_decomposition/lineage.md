# Lineage

This node refines the global cell-9 kernel into the exact finite source
schemes needed by the endpoint roles.  It deliberately separates source
enumeration from target exclusion and from base-locus regularization.
