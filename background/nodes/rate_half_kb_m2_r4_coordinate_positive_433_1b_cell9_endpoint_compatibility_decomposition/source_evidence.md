# Source Evidence

Modal app `ap-i4hLMM03qKEZAI74g2IoHC` computed the eight saturated endpoint
and base-locus schemes.  Modal app `ap-nfsyyUV5Aor88EdzvONefh` factored and
replayed their triangular bases over the deployed field.  The verifier pins
both launchers and result packets and checks the exact Cartesian coverage,
dimensions, basis sizes, guards, and `4+2` point split.
