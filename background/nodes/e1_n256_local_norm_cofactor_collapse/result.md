# Result

PROVED. In profile `(3,4,0)`, a collision norm has exact form
`2^mu p` for one of `mu=1,2,3,4,5`. In profile `(4,2,0)`,
the cofactor lies in an explicit 419-element set
`2^mu(1+256t)<2^17` with `mu in {1,2,4,8,16}`.

On prize-envelope rows the exact field floor first reduces the `(4,2,0)` set
to eight values. The cyclotomic residue-degree sieve excludes 1026, leaving
`{2, 514, 1538, 4, 1028, 16, 256}`. RowC retains all 419.

The remaining task is to exclude or price the corresponding odd norm parts.
