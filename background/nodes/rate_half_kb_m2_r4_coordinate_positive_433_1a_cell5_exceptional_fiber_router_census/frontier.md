# Frontier

Do not scan the full deployed field.  Work only on the 69 printed router
values.

The first exact finite replays already show the intended split:

1. thirteen regular guard-norm candidates replay the finite Bezout identity;
   the sole chart-2 exception `t=1860858030` is excluded on chart 3;
2. ten coefficient-pole fibers retain the basis/factor/map chain and need
   only a fresh colored gcd;
3. 38 map-pole fibers need a dynamically chosen primitive coordinate in the
   specialized localized algebra;
4. eight basis/primitive hazards need a raw fixed-fiber squared-pair basis,
   including the vertical fiber `t=1332924776`.

Preserve the explicit 69-value coverage ledger.  A direct solver must return
`EXCLUDED`, `SURVIVOR`, or `INCOMPLETE` for every value; do not infer closure
from a timeout or a representation pole.
