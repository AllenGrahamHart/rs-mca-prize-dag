# Claim contract

## Consumed theorem

`e1_conductor256_character_eigenvalue_preflight` supplies certified nonzero
eigenvalue rectangles and the strict universal radius
`||lambda||_1<77.202`.

## New proved content

1. Certified outward intervals for the complete real inverse kernel `(IKC1)`.
2. The operator bounds `(IKC4)` on the zero-sum log hyperplane.
3. The exact integer contractions `max|xi_t|<=3` and `sum|xi_t|<=60`.

## Guards

1. This is a necessary-filter theorem, not a shortest-unit theorem.
2. It does not assert that every vector satisfying `(IKC6)` is in the log
   body or arises from a sparse collision pair.
3. The earlier `sum xi_t^2<=101`, coefficient-box, inverse-polynomial, and
   sparse-product filters remain mandatory.
4. No associate count, 367-orbit payment, lower-profile payment, E1 image
   bound, row close, or Prize status movement follows.
5. The verifier uses no library transcendental, FFT, binary floating point,
   optional numerical package, or `assert` acceptance.
6. A separate direct-convolution audit checks all 64 inverse identities.
