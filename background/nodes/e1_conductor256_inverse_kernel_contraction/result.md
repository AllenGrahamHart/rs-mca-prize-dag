# Result

`PROVED`. The conductor-256 inverse convolution contracts every live
fixed-cofactor exponent vector from `[-7,7]^64` to `[-3,3]^64` and proves
the additional exact budget `sum|xi_t|<=60`.

No associate count or E1 status changes.
