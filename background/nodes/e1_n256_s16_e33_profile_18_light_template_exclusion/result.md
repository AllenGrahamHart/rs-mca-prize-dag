# Result

`PROVED`: profile `(1,8)` is impossible at `V=66`. The exhaustive eleven-template
geometry census tests 218,327,296 normalized vectors and gives

```text
profile vectors=17,144,     maximum M_3=1356<1732.
```

The `V=66` residual is now the single five-odd profile `(5,7)`.
