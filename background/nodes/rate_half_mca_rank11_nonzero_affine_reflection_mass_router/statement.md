# Nonzero affine-reflection mass router

- **status:** PROVED
- **scope:** the complete first-owned triple-owner heavy-ruling family

The family has at least

```text
M_3=322359637
```

records on at most `Q_4=58361` pair types. Exactly one of the following holds:

1. an admissible order-32 packet emits `chi>=2299571`; or
2. at least `255011043` records belong to pair types whose synchronized
   rational exception pencil is not a normalized nonzero affine reflection.

In the second case, one such pair type owns at least `4370` records, and all
of their pairwise-disjoint exception locators lie in one fixed coprime
degree-`1..11` pencil outside the nonzero affine-reflection class.

The surviving class may be antipodal, another fractional involution,
extension-field-normalized, nonquadratic, or primitive. It is not paid here.

## Falsifier

Overlap between first-owned pair-type currencies, a synchronized nonzero
affine pencil with more than `1154` records, a small type with at least 29
records, an arithmetic residual other than `255011043`, or failure of the
`4370` pigeonhole threshold.
