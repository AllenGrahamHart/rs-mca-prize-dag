# Frontier

For `X2,N1,L1`, substitute `b=-c^3` before every remaining minor and the
second squared weld.  Treat `M1,M2,M3` separately.  Saturate by all label,
signed-pair, and product-value differences.  Emit low-degree factors cell by
cell; do not form one unsaturated nine-cell ideal.
