# Audit

- The proof uses the exact identities (FJ02), so both directions of the
  router are checked; a merely necessary affine relation is not promoted to
  an equivalence.
- The inherited `D_*!=0` guard is essential when `J_*=-D_*R_J` is used.
- Clearing by `T` can create spurious `T=0` roots. The original chart proves
  `T!=0` via (FJ04)--(FJ05), and the converse retains the same guard.
- The obstruction is checked separately at all four official primes. It
  excludes common zeros over every extension, not only prime-field points.
- `G_2` remains free at the coefficient level. No structural, role, `P_4`,
  saturation, or arithmetic-lift condition has been consumed.
- The verifier sources are complete but intentionally unexecuted locally;
  only syntax, manifest, DAG, and hash checks are authorized without Modal.
