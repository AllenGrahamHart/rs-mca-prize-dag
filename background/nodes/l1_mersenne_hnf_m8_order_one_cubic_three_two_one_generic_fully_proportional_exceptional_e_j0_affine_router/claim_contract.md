# Claim contract

| field | value |
|---|---|
| claim | The retained `E_G=X_*=J_*=L_*=F_b=0` coefficient chart reconstructs `q=5bM/T`, with `T` proved nonzero at every official characteristic, and is equivalent to four explicit univariate filters of degrees at most `6,7,10,11`. |
| status | PROVED |
| dependency | `l1_mersenne_hnf_m8_order_one_cubic_three_two_one_generic_fully_proportional_coefficient_bivariate_compiler` |
| consumer | `l1_mixed_petal_amplification` |
| equivalence guards | Inherited `bD_*!=0` and all earlier saturations; `T!=0` is proved from the original chart by the explicit obstruction (FJ04)--(FJ05) and retained in the converse. |
| retained variable | `G_2` is not reconstructed on the `J_*=L_*=0` chart. |
| open residue | A common-root and ambient-degree verdict for `Bhat,Ehat,Fhat,Xhat`, followed by every retained structural, role, `P_4`, saturation, and arithmetic-lift equation. |
| nonclaim | No common gcd is asserted unit, no ambient quadratic root is asserted to exist or not exist, and no exceptional chart or critical node is asserted empty or closed. |
| replay | `verify.py` is a source-complete sparse-polynomial identity and DAG checker; `verify_audit.py` independently reconstructs the identities over official prime-field samples. Both are intentionally unexecuted locally under the Modal-only computation rule. |
