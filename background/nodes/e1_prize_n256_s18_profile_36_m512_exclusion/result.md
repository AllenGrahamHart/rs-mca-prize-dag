# Result

`PROVED`: cofactor `512` is impossible in the prize profile
`(3,6,S=18)`. Eight profile cofactors remain.

The sibling `514` branch is subsequently closed by an energy-adaptive
contraction, dual exact radius census, and exact norm ledger. The shortest
live cofactor is now `m=256` through `E=23`.
