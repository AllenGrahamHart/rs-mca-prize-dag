# Result

`PROVED`: cofactor `512` is impossible in the prize profile
`(3,6,S=18)`. Eight profile cofactors remain.

The sibling `514` branch has the same energy endpoint but multiplicity one
and a required factor 257; it remains the shortest live cofactor.
