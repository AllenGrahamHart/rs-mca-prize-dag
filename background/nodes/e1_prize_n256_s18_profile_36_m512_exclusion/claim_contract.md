# Claim contract

## Imported facts

From `e1_prize_n256_s18_profile_36_bounded_product_windows`:

- cofactor `512` forces singleton multiplicity `mu=9`;
- its complete residual energy window is `{2,...,17}`;
- a collision quotient must lie in the official prize interval.

## New theorem

The complete normalized residual consists of four vectors. Their exact
cyclotomic quotients by 512 are all below the prize interval. Therefore the
cofactor-512 collision class is empty.

## Exclusions

1. The geometry is not empty; the norm comparison is essential.
2. The radius census is complete because it over-relaxes only mod-four target
   magnitudes and then replays exact integer autocorrelations.
3. Normalization is used surjectively and no free group action is assumed.
4. This removes one cofactor, not the full profile or aggregate pair budget.
