# Rank-eleven global-core rank-drop and high-complexity router

- **status:** PROVED
- **scope:** the nonempty line-global common-core output `(C)` of the
  rank-eleven anchor-star router
- **units:** distinct finite affine bad slopes on one KoalaBear received line

Cancel the complete line-global core `C_*`, of size `c`, once. Put

```text
(n',K',m')=(n-c,K-c,m-c),       d=m'-K'=67472.
```

The dense minimizing pair and the deviation space descend exactly. If
`V'` is the descended deviation space, then

```text
1 <= r=dim V' <= min(10,K').
```

The complete residual family has empty maximal common support.

1. If `r<=8`, support-local transversality pays the complete residual
   family. Including the original disjoint near charge, the largest bound is
   `110390969172308040 < B_*`.
2. If `r=9`, the margin/interleaving split at `T=667` pays uniformly over
   every `9<=K'<=K`. Its worst endpoint is the deployed row and gives
   `61871313426765543 < B_*`, including the near charge.
3. If `r=10`, then `K'>=10`. Eighteen descended dense-pair records, ten
   records whose deviations form a basis of `V'`, and three fillers form a
   fixed 31-record anchor deck with empty maximal support intersection.
   Adjoining any other residual record gives a nonaffine order-32 tuple of
   slope degree `18..31`.

In case 3, support-collapsed extraction on every anchor-star tuple has one
of the following complete-family consequences.

- **`(H_C)`**: one tuple has residual two-cover complexity
  `chi'>=3m'-K'+3`. Its lifted original supports satisfy the official
  `chi>=3m-K+3=2299571`. This is the sole new relative high-complexity
  terminal.
- **`(A)`**: all low-complexity tuples have root-free rational certificates
  which cohere projectively on the fixed 31 anchors and lift to one coherent
  original-row rational atom.
- **`(E)`**: a pure-locator certificate, a denominator root on either the
  residual or deleted coordinates, or a quantified collision/near-sunflower
  exception occurs.

Thus the former `(C)` branch is replaced exactly by a paid rank-drop branch
or `(H_C)/(A)/(E)`. This theorem does not identify `(H_C)` with the deployed
first-match spread terminal `(S)`: its original supports retain the common
factor `C_*`.

## Falsifier

Loss of deviation dimension under exact division; a residual common
coordinate; a shortened rank-at-most-nine endpoint exceeding its printed
deployed endpoint; failure of the fixed 31-anchor intersection at rank ten;
a degree-below-18 star tuple; failure of rational-certificate lift; a
two-cover lift other than `chi=chi'+2c`; or silently calling `(H_C)` a
deployed primitive spread core.
