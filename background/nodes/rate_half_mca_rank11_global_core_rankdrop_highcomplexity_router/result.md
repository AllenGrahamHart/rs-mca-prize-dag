# Result

The rank-eleven line-global common-core residual has been reduced
unconditionally.

- Descended deviation rank `1..8` is paid by support-local transversality.
- Rank `9` is paid uniformly by the `T=667` margin/interleaving split.
- Only full rank `10` survives.
- At full rank, one fixed residual 31-anchor star sends every
  low-complexity tuple to a coherent lifted atom or named exception.
- The only new obstruction is one explicitly typed relative
  high-complexity core `(H_C)`.

This removes rank drop and low-complexity rational behavior from the former
`(C)` terminal. It does not pay `(H_C)` or the shared `(A)/(E)` terminals.
