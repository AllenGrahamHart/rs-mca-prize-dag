# Result

`PROVED`. The large-class interpolation map has exact rank `r-1`; its
one-dimensional defect is `G(gamma_0,X)`. Together with the two small
classes, every center map is onto the center-vanishing hyperplane and
every defect is the actual center fiber.

The alternative multiplication chain would create a residual common point
over an assigned center, contradicting the correction-only support of the
exact four-core.
