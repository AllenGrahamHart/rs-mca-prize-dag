# Audit

1. The contradiction uses the projective effective residual cycle, not
   only the affine resultant degree bound.
2. The mandatory divisor contains common roots over off-line supported
   slopes. The assigned-center point `(gamma_0,x_*)` is not among them.
3. The point is finite and has positive local intersection multiplicity
   because both coprime curve equations vanish there.
4. `x_* notin U_0`, so it cannot be a classified-row intersection already
   removed with multiplicity `e-2`.
5. The nonreduced correction form has its only projective root at `tau`,
   and unsharedness puts `tau` off the center line.
6. No assertion about the multiplicity of `(gamma_0,x_*)` is needed;
   positivity already contradicts the support of `pi_*Z_4`.
7. No critical status changes.
