# Result

Exact-depth selected occupancy is no longer available only by explicitly
discarding every deeper core. It is the binomial inverse of predicate-filtered
raw support moments, and every even truncation is a rigorous upper bound.

The next route-deciding task is therefore concrete: establish a row-sharp
upper bound on one even signed aggregate while retaining the common maximal
pair predicate. Separate per-depth raw upper bounds do not suffice because
the aggregate has alternating signs.
