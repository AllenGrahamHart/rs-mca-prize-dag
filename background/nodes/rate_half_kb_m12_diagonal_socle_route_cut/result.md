# Result

The degree-12 composition kernel has full diagonal simple socle. The actual
quartic suborbit is its synchronized one-point-per-outer-block transversal,
so its outer projection has size four. The `r=2` Dickson branch is empty;
the five `r=4` normal-form families remain the exact `m=12` frontier.
