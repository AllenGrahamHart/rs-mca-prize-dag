# tern_master_threshold

- **status:** PROVED (the object, dictionaries, spine, and threshold
  theorems; the per-instance open questions are stated residuals,
  NOT claims)
- **minted:** 2026-08-06 (round 19 — minted ONLY after the
  attack-first gate: a blind adversary/builder pair plus a blind
  empirical/route pair; the adversary's graded verdict governs this
  node's scope)
- **provenance:** notes/pilots_20260806/tern_master_statement/
  (92,263/0) + tern_unification_adversary/ (54/54) +
  tern_small_scale_laws/ (412/0) + tern_route_b/ (137/137), all
  coordinator-replayed; NODE_DRAFT.md in the builder dir is the
  extended draft; the four FABLE_AUDITs carry the adjudications.

## Statement (THE GRADED UNIFICATION OF RECORD)

**THE OBJECT (survives, proved).** T(P, Lambda) := {eps in {0,±1}^M :
sum_j eps_j theta_j^l = 0 for all l in Lambda}. PROPOSITION HS: for P
the half-system of mu_n and Lambda ⊆ (Z/n)^* Frobenius-stable, T is
EXACTLY the ternary words of the negacyclic F_p-code of length h =
n/2 with defining set Lambda* = <p>Lambda, codimension g = |Lambda*|.
All THREE instances specialize exactly (CATCH-19-ADV-4: the round-18
"fourth instance" is demoted — LEMMA TC's 3^L is I2's ambient
cardinality, not an instance): I1 = f2_z1_mass_knife_edge (h = S,
Lambda = the all-odd window, g = R, mass weight 2^{-U}); I2 =
crossing_dsa_refutation's deep stratum (h = L, Lambda = {1},
g = delta_a, constant-weight fibre weight); I3 =
es_ternary_suppression_instruments' odd sub-object (h = n/2,
g = |Z_w^odd|; the even conditions ARE the next recursion level,
named not carried out). PROPOSITION FIB: every instance's target is
the eps = 0 term times (1 + delta) under a binary-preimage
multiplicity mu — I1's "mass" IS a count of binary pairs
(2^{-wt} = the fibre size); the REAL split is CONSTANT-WEIGHT (I2)
vs FULL-CUBE (I1, I3).

**THE SPINE (proved).** CZ-M: char-0 emptiness iff n is a 2-power
(CATCH-Z6 upgraded to a rank statement, count 3^{N-phi(n)} - 1).
CS-M: THEOREM CS reads verbatim for ANY Frobenius-stable unit
Lambda — no window/consecutivity hypothesis — via LEMMA BR:
r' - a_{n/2}(S) = wt(A - B) (the archimedean ceiling quantity IS the
ternary support size). ROT-M: T is stable under -1, the negacyclic
shift (order n), and the dilates; negacyclic closure holds IFF
Lambda is all-odd (both round-19 empirical pilots) — the official
I1 window is all-odd, so the 2N orbit constant stands there
(coordinator adjudication; mixed-parity miniature conventions
collapse to 2). Z-FLOOR-M: holds for any finite X ⊆ Z^M with
difference-multiplicity weights; NOT the constant-weight functional.
Z-FLOOR ≡ DSA + support control (the same binary-difference
collision; DSA's regime is tau < 1 - 2/N).

**THEOREM MT (the master threshold) + THEOREM PT (the phase
transition — the reason there is NO single-theorem unification).**
With tau := g·log2 p / h: existence is forced for tau < 1 (with
|T| >= 2^h/p^g); ternary vectors are EXPECTED present iff
tau < log2 3 = 1.585 (first-moment side, heuristic; only tau < 1 is
proved, by pigeonhole). The three banked boundaries are this one
inequality: I1's knife edge (-46.0249/+17.9751, step log2 p —
reproduced to four decimals by THREE independent derivations), I2's
DSA boundary, I3's stratum-0 boundary. BUT I1 sits AT tau = 1
(FORCED by the saturation pin R/S = 1/log2 p) — SUPERCRITICAL, the
solution set astronomically populated, only a MASS bound can be
true (CATCH-Z1's mass re-pin was forced by sign(Tcrit)) — while
I2/I3 sit at tau = 2 — SUBCRITICAL, EMPTINESS is the target. NO
single monotone statement specializes to both. THE PER-TAU TARGETS
OF RECORD: mass (tail-count criterion) at tau = 1; emptiness at
tau > 1. COROLLARY MX: the norm and pigeonhole mechanisms are never
simultaneously informative.

**THEOREM SR (the anti-(ES) result).** Any prime with
v_2(p-1) = 41 and log2 p in [255.9113, 256) is admissible for all
three instances AT ONCE (existence by Dirichlet; explicit MR
exhibit) — the regimes are mutually satisfiable, the exact opposite
of the (ES) collapse. NO shared discharge follows (the phase
transition forbids it); the shared row is where cross-instance
METHOD transfer is honest.

**THE INSTRUMENT MATRIX (reconciled, builder + adversary):** 12
instruments are master-level (Z-FLOOR, CZ/LEMMA Z, CS, LEMMA AB =
HS + BR, SP-COVER, COS/SP-UNIFORM, SP-TERNARY, ROT, OE, TWO, Z-1,
Z-2); DSA is subsumed; 4 are instance-only with named obstructions;
2 cited. Every transfer is verbatim BUT provably vacuous or
insufficient at its target's tau (the adversary's vacuity column) —
the method unification is a SCAFFOLD, not a weapon. New instruments
from the round: THEOREM I3-FORCE (below the threshold the
odd-condition exclusion mechanism provably FAILS on named tower
rows — the first existence instrument on the (ES) object); the
Z-1 -> I3 transport (min ternary weight >= w+1); THEOREM 7
(unconditional Z_1 <= 2^{0.8908 S}); LEMMA TWT + SELF-ORTH
(instance-local to non-split primes); the exact 1+cos character
form with the tail-count criterion.

**WATCH LINE (COROLLARY PT-2 — SCOPE CORRECTED, fourth forced
correction 2026-08-06, round-20 crossing_gap CATCH-20D):** the
clearance is p-DEPENDENT: subcritical iff w > w_tern(p) =
2^41 log2(3)/log2(p); "0.336 bits" is the value AT log2 p = 256
ONLY. On the live admissible prime range (log2 p in [129.585, 256),
B* >= 3), the bracket endpoint w = 2^34 sits BELOW the
supercriticality threshold on 57.98% of the range (ternary
functional; 51.65% orbit-corrected; 100% under the GLOBAL form) —
first-moment/heuristic supercriticality inside the tau in
(1, 1.585) band this node itself labels heuristic; the proved
existence regimes (MT tau < 1, DSA) remain unreachable at prime
rows, so this is NOT a refutation of emptiness. RHL-LB's 2^34 is
EXACT and provably extremal for the printed construction (any
improvement moves w UP); raising the endpoint above
w_tern(129.585) = 2^34.6467 would remove the exposure but needs a
NEW construction. The correct watch line is the pair
(w_tern(p), the live p-range), re-run on any bracket change.

## Falsifier

A fourth independent instance that fails the dictionaries; a proved
statement-level unification (would contradict PT); an admissible
shared row refuting SR; an instrument transferring non-vacuously
across the tau = 1 boundary (would be a major positive surprise —
report, not suppress).

## NOT claimed

The I1 mass bound (open: the tail-count criterion, NO named route);
the I2/I3 mid-range primes; CC-sparsity; the even-condition
recursion; the tau in (1, 1.585) first-moment band (heuristic);
any shared discharge.
