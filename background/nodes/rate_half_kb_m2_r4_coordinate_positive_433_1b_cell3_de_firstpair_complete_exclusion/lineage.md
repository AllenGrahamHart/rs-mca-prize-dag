# Lineage

- The `xi=0` and `xi=2` pairing-zero packets supplied exact target-free common
  point ledgers.
- The parallel-edge transport supplied the second positive pairing-zero row.
- Canonical pairings one and two preserve the same first residual `DE` pair,
  so no new common-curve norm was required.
- A direct finite solver paid the missing squared-sum quartic in `d` and then
  the residual paired-equation gcd in `f`.
- Exact residual-list transport adds the second positive copy for pairings one
  and two, completing the 144-case first-pair block.
