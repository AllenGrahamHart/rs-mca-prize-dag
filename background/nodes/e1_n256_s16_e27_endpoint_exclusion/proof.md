# Proof

The exact profile/parity/light reduction proves that every pair-feasible
folded-profile `(3,4,0)` vector at `V=54` has one of the six profiles printed
in the statement and lies over one of eight affine light templates.

The joint exclusion exhausts those templates and profiles using two complete
actual-vector censuses, the proper-conductor theorem, and independent exact
norms. Hence `V=54` is impossible.

The preceding endpoint theorem left only positive even `V<=54`. Removing
`V=54` advances the live frontier to positive even `V<=52`. QED.
