# Claim contract

## Claim

No pair-feasible `N=256` folded-profile `(3,4,0)` vector has `V=54`.

## Inputs consumed

- the exact six-profile E27 reduction;
- the complete six-profile joint exclusion.

## Output supplied

The live positive even variance frontier in this band advances from `V<=54`
to `V<=52`.

## Scope exclusions

- no exclusion at `V<=52`;
- no closure of folded profile `(4,2,0)`;
- no closure of later `N=256` or `N=512` bands;
- no route-wide pair-incidence theorem.
