# Audit

- Every boundary layer is charged separately and owns one slope per
  explanation; the top line is charged once.
- The class count is on normalized codeword directions, not slopes.
- The anchor is included in every class line and is subtracted by `Q_h-1`.
- Outside-core substitution uses both nonzero direction and
  `N-e>m-h>c`.
- Absorption is invoked only after unsafety forces at least two top anchors.
- The low side uses a cumulative ordinary-list cap and the conservative
  multiplier `e`; no hidden one-owner claim is used there.
- The adjacent failure is only for this fixed cutoff and is not unsafe.

The Modal scan used one 56 MB container.  A constant-memory C replay checks
all 2,925 support records and the primary Python verifier checks parent
pins, exact endpoint records, and hostile mutations.
