# Source evidence

- `rate_half_kb_m2_r4_order2_coordinate_source_facet_signature` supplies the
  twelve deck-paired source fibers and all 24 stars.
- `rate_half_kb_m2_r4_coordinate_k_fiber_vieta_rank_compiler` supplies the
  parity normal forms and the Vieta calculation on the five common fibers.
- `rate_half_kb_m2_r4_coordinate_vieta_profile_only_f29_route_cut` supplies
  the exact regression packet.  This node adds its `eta` row and does not
  treat the small-characteristic witness as a deployed-field result.
- The structural proof is a direct Vieta calculation; no external computer
  algebra output is used.  The separate fixed-fixture regression is a finite
  exact enumeration and is not used for the universal compiler claim.
- `rate_half_kb_m2_v4_outer_recurrence_router` supplies the complete-source
  defect budget three used to exclude a constant negative product ratio.
- `verify_fixture_census.py` performs a finite exact `F_29` regression on
  the already-proved abstract fixture.  Its zero complete-survivor count is
  not extrapolated beyond that fixed combinatorial packet.
