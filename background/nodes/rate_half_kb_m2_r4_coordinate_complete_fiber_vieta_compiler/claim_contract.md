# Claim contract

| field | content |
|---|---|
| claim | Every actual coordinate-order-two packet satisfies the complete twelve-fiber positive or negative Vieta system and its product-rank consequences. |
| deletion interface | Reject a packet when its `24 x 8`/`24 x 7` kernel lacks leading support, when its complete product matrix has excessive rank, or, in positive parity, when the `K+eta` determinant is nonzero. |
| exact regression | The `F_29` profile-only witness has `K` rank five but `K+eta` determinant ten and is deleted. |
| separator route cut | On the exact defect-zero fixture, 140 of 5,040 `F_29` signed-square assignments pass `K+eta`; none passes the complete twelve-row product gate. |
| negative cut | The negative product ratio is an injective Mobius map; its common-`K` pair-multiplicity frontier is exactly the seven skeletons in `(KBCV-6)`. |
| assumptions | Only the proved coordinate source-facet partition, deck transport, and coefficient normal forms. |
| nonclaim | Universal complete-product exclusion, exclusion of the seven negative skeletons, coordinate exclusion, endpoint realization, owner/payment movement, a KoalaBear row, or either Prize theorem. |
