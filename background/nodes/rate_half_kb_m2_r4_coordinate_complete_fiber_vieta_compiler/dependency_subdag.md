# Dependency sub-DAG

```text
coordinate source-facet signature
                    \
                     -> complete-fiber Vieta compiler -> rate-half band closure
                    /
coordinate K-fiber Vieta compiler
```

Both incoming edges are requirements.  The outgoing edge is evidence until
the complete coordinate packet census proves universal failure.
