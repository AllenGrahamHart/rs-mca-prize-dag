# Frontier

Classify the two source-facet degree profiles through the square-root-free
product gates first.  For positive parity, use the complete twelve-row rank
gate; the `K+eta` separator alone has exact abstract survivors.  For negative parity, classify
the seven `(KBCV-6)` skeletons whose twelve edge products would have to lie
on one injective Mobius map.  Only product survivors should be lifted to the
full `q` system and independent quotient audit.
