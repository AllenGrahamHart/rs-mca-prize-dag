# Lineage

This node composes the coordinate source-facet census with the existing
five-fiber Vieta compiler.  The `F_29` route cut showed that the common-five
rank gate alone was insufficient; adding the forced `eta` edge exposes the
first nonvacuous positive product determinant and rejects that witness.  A
second exact fixture census then shows that the separator itself has
abstract survivors and that the six complement rows are load-bearing.
