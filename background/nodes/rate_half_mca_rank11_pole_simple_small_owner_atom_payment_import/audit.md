# Audit

- Read upstream `thm:owner-localization` and `cor:small-owner` at the pinned
  source commit.
- Added only the pole-simple puncturing adapter; no pole-free hypothesis is
  silently assumed.
- Checked that root-support incidence, not only root count, bounds removed
  records by `rho`.
- Checked both telescoping identities after restoring pole-touching records.
- Recomputed the official owner threshold `1183521` and record caps.
- Kept the strict large-owner image bound open.
