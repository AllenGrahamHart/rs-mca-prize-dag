# Frontier

A record-covering global atom from the quotient population cannot be a small
owner. It must satisfy `g>=1183521` and therefore enters upstream
`prob:large-owner`. The remaining theorem is its row-sharp image/fiber bound,
not local atom coherence.
