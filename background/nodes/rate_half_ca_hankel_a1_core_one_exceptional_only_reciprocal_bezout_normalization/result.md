# Result

The first reciprocal coefficient is now normalized by an exact Bezout
identity. On every root of the complete infinity form `E q_bar`, it equals
the inverse of `P_cl`; contributor classifiers can compute this value rather
than allocate it as an independent coefficient.
