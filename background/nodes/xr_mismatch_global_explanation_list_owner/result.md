# Result

`PROVED`. Terminal tangent mismatch slopes cost at most `(n-A)(E-1)`, where
`E` is the number of distinct original common-support codeword-pair
explanations. The existing interleaving collapse gives the exact ordinary-
list bounds `(GEO4)` and `(GEO5)`. Recursive depth and locator-flag
multiplicity no longer appear in this currency; generic terminal charts and
the required ordinary-list estimate remain open.
