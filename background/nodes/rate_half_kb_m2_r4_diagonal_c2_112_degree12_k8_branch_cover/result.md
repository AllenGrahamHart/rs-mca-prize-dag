# Result

Both exhaustive `K8=0` leaves are empty over the algebraic closure in every
`F04`--`F07` cell over `R02`. Combined with the separate `K8!=0` node, no
live `R02` degree-12 point remains after the prior `s` and `L6` splits.
