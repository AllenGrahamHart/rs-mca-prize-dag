# Audit

- Modal app `ap-nemQUnWFBEbZAmjzlGmQcp` ran both branches in parallel.
- The `A0` chart completed in `43.74` seconds and the residual in `30.95`
  seconds, each at about `0.39 GB` peak child RSS.
- The residual branch deliberately omits the quartic cores, proving emptiness
  of a superset rather than relying on an invalid leading-coefficient
  cancellation at `K10=0`.
- Every source pseudo-remainder identity is asserted before factor stripping.
