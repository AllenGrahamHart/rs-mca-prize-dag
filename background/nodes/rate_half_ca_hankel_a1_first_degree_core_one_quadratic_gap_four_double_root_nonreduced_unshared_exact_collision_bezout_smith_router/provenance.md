# Provenance

Derived in Cycle 176 by combining the Cycle-175 normalization collision
with the exact Pade-Bezout contact-module presentation. The local
quadratic multiplication matrix replaces the earlier abstract partition
enumeration. No Modal computation or external result is used.

Cycle 177 removed an invalid automatic original-source separation and
restored the unit-`a` corank-one `[4]` row of the same multiplication
matrix. The contact-module calculation itself was unchanged.

Cycle 178 corrected the exact evaluation from `F_0=b` to
`F_0=b+c_0R(z,0)`. The order-six correction leaves `ord b=2` and the
Smith calculation unchanged.
