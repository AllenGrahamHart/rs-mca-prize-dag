# Result

The exact nonreduced locator collision has a quadratic contact algebra and
regular corank at most two. The constant and first parameter coefficients
of the `y`-term in `P_F mod q` select `[4]`, `[1,3]`, or `[2,2]`. The
remaining attack surface is global geometry excluding these three exact
profiles; original-source separation cannot be assumed.
