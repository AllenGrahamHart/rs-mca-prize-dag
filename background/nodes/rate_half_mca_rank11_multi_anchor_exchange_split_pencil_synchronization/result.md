# Result

The dense-anchor exchange argument extends to every triple-owner pair type
with at least 29 records. Unless one packet reaches `chi>=2299571`, each such
type owns one synchronized rational exception pencil containing all of its
record locators. This supplies the missing owner-preserving transport for
per-pencil caps.
