# Provenance

This is a local composition of the proved nodes
`l1_general_first_layout_domination` and
`l1_fpc5_shifted_johnson_grs_shell_cap`. The ownership pattern follows the
already proved `M=4,t=3` first-layout atom collapse. All threshold arithmetic
is exact and local; no Modal computation is used.
