# Result

The shifted-Johnson FPC5 cap now survives the canonical outer composition on
four explicit smallest-row upper field slices. Source-layout multiplicity is
removed completely and touched subsets are paid exactly. At rate `1/16`,
only defects `248` and `292` survive this aggregate below the prize cap; six
other shifted defects require a different local bound or allocation.
