# Upstream crosswalk

In upstream terminology this is an exact finite split-pencil census result:
the three-value stratum is empty on every official `m=4` Mersenne row. The
packet should be vendored with the depressed-pencil/Euler reductions and the
three terminal local exclusions. It leaves the primitive shift-pair ledger,
two-value classification, and `m=8,16` strata open.
