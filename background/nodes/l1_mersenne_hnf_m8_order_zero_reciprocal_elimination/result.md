# Result

All `m=8,h=7` order-zero outer HNF survivors are excluded on the four
official endpoint rows. The first three bounded reciprocal equations produce
two eliminants whose common factor has only the prime-field roots
`0,1,-1,...,-7`. This contradicts the required `s notin F_p` and removes
every cubic and higher colored degree along with the already closed low
degrees.
