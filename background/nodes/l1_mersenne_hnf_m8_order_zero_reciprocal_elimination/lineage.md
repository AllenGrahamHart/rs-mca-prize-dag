# Lineage

The low-degree color theorems closed constant, linear, and quadratic strata.
Fable's reciprocal gate retained a degree-independent necessary system in
`(s,t=s^p)`. This node attacks that system directly: two low-index
eliminants already force `s` into the prime field, closing every remaining
color degree on `m=8` without a generic saturation.
