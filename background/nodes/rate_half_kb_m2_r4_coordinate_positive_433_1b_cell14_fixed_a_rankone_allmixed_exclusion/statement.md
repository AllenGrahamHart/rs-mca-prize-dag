# KoalaBear m2 r4 positive 433-1b cell-14 fixed-a all-mixed exclusion

- **status:** PROVED
- **scope:** the final guarded principal quadratic-cover cases of deployed
  positive `433-1b -> O0a` role cell `14`
- **dependencies:** the cell-14 quadratic curve, signed outside atlas,
  complete-fiber Vieta compiler, rank-one boundary certificates, and the
  preceding fixed-`a` frontier census

## Claim `(KBP1B14-FIXEDA-ALLMIXED-1)`

Write the seven signed outside product records as

```text
y = (de,de,-de,df,sigma_o ef,bf,sigma_c cf).
```

For every source-sign pair, every target lane `(sigma_c,sigma_o)`, every
missing-record index in `{0,1,2}`, and every matching index in `{6,7,8}`, the
guarded cell-14 outside system is empty over `F_2130706433`.

The claim excludes exactly

```text
4 source signs * 4 target lanes * 3 missing de roles * 3 matchings = 144
```

cases. Together with the disjoint 144-case linear-pair theorem, 960-case
target-projection theorem, and 432-case fixed-`a` chain theorem, this excludes
all 1,680 raw cell-14 cases.

This theorem closes cell `14`. It does not by itself close `433-1b -> O0a`,
the rate-half band, K3, LIST, MCA, or either Prize problem.

## Falsifier

A guarded deployed-field zero in any one of the 144 systems, a live boundary
discarded by coefficient or torus clearing, a common projection factor with a
live original-equation fiber, an omitted Cartesian case, or a mismatch in the
independent field-root replay.
