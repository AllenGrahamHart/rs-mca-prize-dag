# Dependency sub-DAG

```text
kernel_nine_shadow_coupling
                 \
                  -> multistep_shadow_hierarchy
                 /
two_step_nineshadow_hierarchy
```

The first parent supplies looplessness, dual-shadow language, and the
same-rank closure-extension count.  The second supplies the rank-raising
extension and target-coloop double-counting pattern generalized from pairs
to arbitrary `t`.
