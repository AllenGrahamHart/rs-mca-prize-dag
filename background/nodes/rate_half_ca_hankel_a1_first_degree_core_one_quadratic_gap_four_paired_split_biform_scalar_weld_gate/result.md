# Result

The two coefficient directions are now coupled by one exact sparse linear
system. Pair-boundary exclusion reduces to showing that the fixed-domain
coefficient matrix augmented by two-entry weld rows has no full-support
kernel, or to exhibiting such a kernel and testing the remaining Hankel/source
identities.
