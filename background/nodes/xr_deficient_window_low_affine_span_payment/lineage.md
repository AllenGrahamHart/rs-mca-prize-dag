# Lineage and provenance

Derived in the Codex resolution worktree on 2026-08-03 after commit
`aa4046d7`. The analytic compiler is the locally reconstructed and audited
`upstream_gfv4_affine_span_list_compiler`; the punctured currency is local
node `xr_deficient_window_active_defect_list_router`.

The official table is deterministic integer arithmetic. No Modal job or
unmerged upstream PR is used.
