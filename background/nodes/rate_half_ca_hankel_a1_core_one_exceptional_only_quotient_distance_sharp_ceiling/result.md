# Result

The macroscopic high-distance interval loses its former top value. At the
new endpoint all `4e` ordinary complement sizes are forced to one of two
near-constant multisets, so the next attack can target two exact incidence
profiles rather than an unrestricted endpoint family.
