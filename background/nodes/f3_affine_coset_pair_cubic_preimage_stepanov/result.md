# Result

The in-house one-fiber Stepanov construction has an exact parameter choice
giving

```text
#{x:L_1(x),L_2(x) in K}<(51/16)|K|^(2/3)
```

for subgroup orders `n` and `3n` in the official H3 ambient range. This is
the constants-explicit input needed to count all nodal trace branches through
one cube-preimage subgroup.
