# frontier: m720_overceiling_complete_window_certificates

Closed.

The only cells here are:

```text
n=32, h=16, q_exp=2
n=32, h=16, q_exp=3
```

They arise because the Modal window initializer sets `W=2h=n` before applying
the count ceiling. Complement-pair algebra shows the only anchored trade is the
paid toral even/odd coset partition.
