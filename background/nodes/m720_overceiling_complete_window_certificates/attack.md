# ATTACK - m720_overceiling_complete_window_certificates

Closed by complement-pair algebra. The complete-window cost at `n=32,h=16` is
far above the Modal count ceiling, but no enumeration is needed.

Historical routes were:

- run the two cells off-laptop with a certificate payload;
- produce a compact exact certificate for zero unpaid non-toral anchored cores;
- amend the certificate policy if over-ceiling initializer cells should be
  treated as incomplete rather than complete obligations.
