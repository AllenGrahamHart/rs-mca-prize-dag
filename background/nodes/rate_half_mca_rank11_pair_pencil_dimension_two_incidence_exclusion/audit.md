# Audit

1. At a common-gcd root, all selected pair codewords agree, but the
   coordinate enters `J` only if the received pair matches them.
2. Away from the gcd, evaluation is a nonzero functional because the
   primitive scalar basis is coprime.
3. Core owners at one noncommon coordinate lie in one affine fiber, so the
   already proved 15-point line cap applies directly.
4. The common core is bounded by `K-1` using two distinct pair types.
5. The contradiction uses only 520 types; any additional types strengthen it.
