# Frontier

Combine `(KB43S-1)` with the forced `xi=-M` product and explicit product
involution.  Split the five possible internal `eta` edge types, quotient by
permutations of `D,E,F` preserving the `B,C` attachments, and impose full
twelve-label Mobius interpolation before q/resultant work.
