# Audit

1. Counts are edge-orbit counts: ten/four stars become five/two records.
2. The no-new-loop argument uses all twelve complete Vieta rows, not merely
   the common-`K` loop-budget statement.
3. `A_1` is nonzero because the common skeleton contains nonloops with both
   `q` and `B_2` nonzero.
4. The multiplicity cap two is product injectivity plus the two possible
   signed products between fixed antipodal pairs.
5. The verifier exhausts all ordered `r` and `m` values; the audit verifier
   independently solves the degree equations in closed form.
6. Choosing an `eta` edge type does not assign it to a field value or prove
   compatibility with the forced `xi` product.
