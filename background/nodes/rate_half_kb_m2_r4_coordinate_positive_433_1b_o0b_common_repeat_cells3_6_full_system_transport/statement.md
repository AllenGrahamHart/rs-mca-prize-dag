# Repeated-BC cells 3 and 6 full-system transport

- **status:** PROVED
- **scope:** complete common and outside formal systems

Apply `B<->C` and `E<->F` on target vertices. The common roles exchange
`AB<->AC`, so role cell 3 maps to cell 6 with both source-root signs fixed.
The repeated `BC` pair and its sign are fixed. Outside, the map exchanges
`BE<->CF`, `DE+<->DF+`, and `DE-<->DF-`, fixes `EF`, and preserves the
outside-cycle sign.

The seven outside records have 105 labels: choose one record over the missing
singleton mate and one of fifteen matchings of the other six records into
three deck pairs. The target-record permutation induces an involution of all
105 labels. Including four source-root signs, two BC signs, and two
outside-cycle signs gives an exact bijection of `105*4*2*2=1680` formal
systems in each cell.

Thus any full cell-3 exclusion or survivor theorem transports to cell 6.
This theorem itself excludes no system and proves no route, K3, or Prize
claim.

## Falsifier

A target record, sign lane, or outside label not preserved bijectively, or a
coefficient/Vieta equation not equivariant under the stated variable swap.
