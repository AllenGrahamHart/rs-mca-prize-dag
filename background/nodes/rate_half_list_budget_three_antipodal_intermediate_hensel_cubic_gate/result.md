# Result

The third forbidden coefficient makes the intermediate scalar generically
unique even on the previously degenerate first gate. After reducing its cubic
by the monic quadratic, either one printed scalar remains, the branch rejects,
or the explicit coefficient pair `A=B=0` retains at most two candidates.

The exceptional pair and the exact downstream candidate checks remain open.
