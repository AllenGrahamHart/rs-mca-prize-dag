# Frontier

Attack the five printed rows rather than a generic degree-five outer map.
For genus zero, normalize the two finite branch values and derive the rigid
degree-five polynomial forms before imposing the split five-zero fiber and
source-star data. For genus one, combine the degree-12 cover
`Gamma -> C` with the bidegree-`(2,4)` discriminant and source-pole divisor.
Keep the dihedral `r=2` survivor separate.
