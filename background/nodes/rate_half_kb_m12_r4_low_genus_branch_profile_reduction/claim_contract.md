# Claim contract

## Proved

1. The normalization of the actual bidegree-`(4,4)` component has genus at
   most three, using its imported birational bidegree-`(2,4)` model.
2. The degree-12 map to the `r=4` outer component forces outer genus at most
   one.
3. The tame degree-five polynomial branch ledger has exactly seven
   two-transitive profiles, five of genus at most one.
4. The five surviving profiles have geometric group `A5` or `S5`; tame
   polynomial `AGL(1,5)` does not occur.

## Imported proved inputs

- the actual bidegree-`(2,4)` model and birational self-correspondence lift;
- the inner-degree-12 outer divisor profile and challenge-field descent;
- the transverse identity with `(r,delta)=(4,12)`.

## Not proved

None of the five profiles is deleted or realized by an endpoint record. The
`r=2` survivor is untouched. No `r=4`, `m=12`, owner, charge, carrier/data
bridge, `u=2`, cap-`68`, endpoint, or row closure is asserted.
