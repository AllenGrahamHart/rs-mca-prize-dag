# Audit

- The genus bound uses the bidegree-`(2,4)` model, not merely the
  bidegree-`(4,4)` image; the resulting upper bound is three.
- Riemann-Hurwitz is applied to normalizations on both sides.
- The outer genus is computed on the full 20-sheet ordered-pair orbit over
  the output line, not from a potentially singular affine quartic model.
- All compositions of finite branch index four are enumerated; no branch
  count or cycle type is omitted.
- The primary replay derives groups, subdegrees, and genera from
  permutations. The audit replay uses explicit representatives and the
  independent ordered-pair index table.
- The five low-genus rows remain open; this is not an `r=4` deletion.
