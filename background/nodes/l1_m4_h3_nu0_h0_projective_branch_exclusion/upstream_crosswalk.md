# Upstream crosswalk - nu=0 constant-eliminant branch

This is an exact base-field-normalized split-pencil component deletion. The
constant-eliminant, nonzero-constant cubic branch has only one projective
outer parameter after the tangent-at-zero component is removed.

An upstream `(A)` or SPI ledger can replace a two-parameter coefficient cell
by the relation `9bR(0)=4a^2+6aR(0)^2`. This is not an emptiness theorem or
MCA row payment.
