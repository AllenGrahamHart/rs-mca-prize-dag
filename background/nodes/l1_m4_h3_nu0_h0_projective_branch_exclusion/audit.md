# Audit - L1 m=4, h=3, nu=0, h=0 projective branch exclusion

1. `r=R(0)` is nonzero at `nu=0`, so projective normalization is valid.
2. Both factors in `(PBE2)` retain the signs from
   `Delta=-4a^3-27b^2`.
3. The first factor is exactly `2ar+3b=0`, not a new tangent assumption.
4. `b!=0` and `a!=0` make `r=y_0` nonzero.
5. `XR'!=0` rules out a `p`th-power `S`; since `deg S=p`, its zero
   multiplicity is not divisible by `p`.
6. The two left factors each have order `m`; `D(0)!=0` contributes none.
7. The right side is a polynomial in `R`, and `Phi'(r)=8alpha` is nonzero on
   every official characteristic.
8. The order contradiction excludes only the first factor; the second factor
   is retained as necessary, not declared sufficient.
