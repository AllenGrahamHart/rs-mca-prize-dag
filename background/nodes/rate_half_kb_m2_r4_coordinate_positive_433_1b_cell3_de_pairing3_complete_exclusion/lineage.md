# Lineage

- The compact cell-3 kernel and global quadratic quotient supplied the exact
  six-dimensional source algebra.
- Two earlier SymPy implementations of a higher-degree common resultant hit
  the 300-second cap.
- Replacing only `F_p(r)` arithmetic with reduced python-flint polynomial
  fractions made the lower-degree nested-quadratic route complete in under
  90 seconds per row.
- A direct exceptional-root lift removed the unsafe assumption that every
  vanishing elimination coefficient was a genuine route boundary.
- Exact parallel-edge transport adds the second positive `DE` deletion.
