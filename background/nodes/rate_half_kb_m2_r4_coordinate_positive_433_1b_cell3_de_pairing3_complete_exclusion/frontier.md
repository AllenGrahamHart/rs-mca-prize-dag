# Frontier

The parallel-`DE` block is now proved for matching indices `0,1,2,3,4`:
the previous theorems pay 144 first-pair cases, 48 pairing-3 cases, and
48 pairing-4 cases.

The next cell-3 route decision is between:

1. matching indices `5,...,14` for `xi in {0,1,2}`, where the two
   residual `DE` records remain separated; and
2. `xi=3,pairing=0`, where the missing record is `df` and the direct
   colored/missing-sum quartic resultant exceeded the 300-second cap.

Pairing 5 may transport from pairing 4 under a target-label and lane-sign
swap; check that exact symmetry before another norm run.  Do not return to
dense SymPy rational-function resultants, and do not infer complete cell-3
closure from the five paid matching indices.
