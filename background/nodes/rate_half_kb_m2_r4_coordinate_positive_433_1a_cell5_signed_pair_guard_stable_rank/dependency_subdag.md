# Dependency sub-DAG

```text
reciprocal trace quadratic ----+
                               |
deployed rational lift atlas --+--> signed-pair guard stable rank
                               |                |
signed-family target-free -----+                +--ev--> rate-half band closure
```

All three incoming suppliers are proved.  The child is a finite generic
pair ledger, not the colored signed-family deletion.
