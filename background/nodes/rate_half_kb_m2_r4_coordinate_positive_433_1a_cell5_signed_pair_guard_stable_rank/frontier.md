# Frontier

Do not run another generic standard basis for the pair.  The downstream
generic-reducedness theorem now proves that the certified 24-dimensional
localized algebra is generated by `ell=x1+2*x0+3*b` and reduced.  Use this
primitive coordinate as the finite backend:

1. compute and factor its degree-24 primitive polynomial over `F_p(t)`;
2. factor or take norms of the residual colored `BE` cubic and unsquared
   sum on each factor;
3. retain source-square lift, source-distinctness, and exceptional-`t`
   obligations explicitly;
4. repeat charts 3--5 only after determining whether chart 2 survives;
5. transfer to the `DF` family only after the first colored family is
   classified.

A unit colored norm closes this chart's generic pair branch.  A nonunit norm
must return exact residue factors and original unsquared-row witnesses.  Do
not recompute a radical or treat degree 24 as a component count.
