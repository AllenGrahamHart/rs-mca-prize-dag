# Statement

The proved matching-11 exclusion and the exact parallel-`DE` involution
exclude

```text
(0,11), (1,11), (2,11), (2,14)
```

in positive `433-1b` role cell 4. These are four labels in two quotient
orbits, hence

```text
4 labels * 4 source signs * 4 target lanes = 64 raw cases.
```

Together with the already-proved 39-label pairing-8/13 complete block, 43
of 105 labels in 23 quotient orbits are paid. The live ledger is therefore
62 labels in 37 quotient orbits.

The involution does **not** send `(0,11)` or `(1,11)` to matching 14. The
positive-`DE` labels `(0,14),(1,14)` remain open.
