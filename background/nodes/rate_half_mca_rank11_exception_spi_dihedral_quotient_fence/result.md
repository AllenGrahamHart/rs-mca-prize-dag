# Result

The abstract twenty-fiber exception-SPI endpoint admits a second large
survivor family. Besides cyclic power-map fibers, dihedral inversion
quotients produce at least `262144` disjoint degree-`2`, `4`, or `8` fibers
on the official domain. The live theorem must therefore classify or price
cyclic and dihedral pullbacks before treating the primitive remainder.
