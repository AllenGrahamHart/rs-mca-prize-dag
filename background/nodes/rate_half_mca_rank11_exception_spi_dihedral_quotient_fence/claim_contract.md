# Claim contract

- **Claim ID:**
  `rate_half_mca_rank11_exception_spi_dihedral_quotient_fence`
- **Quantifiers:** every power-of-two `N` and `d in {1,2,4}` dividing `N`,
  with a nonsquare `a in mu_(N/d)`.
- **Conclusion:** the displayed degree-`2d` pencil has exactly `N/(2d)`
  pairwise-disjoint split squarefree domain fibers.
- **Used from dependency:** only the shape of the bounded exception-SPI
  interface and its twenty-fiber threshold.
- **Not claimed:** a lift to a heavy-ruling certificate, a received-word
  construction, or an MCA counterexample.
