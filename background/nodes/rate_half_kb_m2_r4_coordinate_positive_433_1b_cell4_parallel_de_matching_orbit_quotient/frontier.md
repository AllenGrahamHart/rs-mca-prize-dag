# Frontier

Use only one representative from each two-cycle in

```text
(3,6), (4,9), (5,12), (7,10), (8,13), (11,14).
```

The first new pairing representative is `3`. For parallel-`DE` omissions,
compute `xi=0` and `xi=2`; transport `xi=0 -> xi=1`, and transport
`pairing=3 -> 6` at every fixed missing role. Keep target lanes separate
until an exact sign action is proved.
