# Lineage

- The signed-edge atlas establishes the two identical positive `DE` records.
- The first-pair block pays the three fixed matching indices for the three
  parallel-`DE` omissions.
- This node computes the complete residual matching action once, so every
  later representative exclusion has an exact transport partner.
- The eventual consumer remains `rate_half_band_closure`.
