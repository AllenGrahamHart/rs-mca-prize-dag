# Source Evidence

Proof inputs:

- the PROVED positive `433-1b -> O0a` signed-edge atlas;
- the PROVED cell-4 DE first-pair exclusion.

`verify.py` regenerates all fifteen canonical perfect matchings, applies the
record transposition, checks all `105` missing/matching labels, and derives
the `60 -> 54` orbit ledger. `verify_audit.py` checks the same permutation
from an explicit independent matching table.

No numerical computation is used.
