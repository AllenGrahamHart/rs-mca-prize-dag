# Result

`PROVED`. The quadratic `u=4` sharp pair boundary is empty in both root
patterns. Every actual support pair has union at least `rho+3`; a common
center pencil contains at most about one third of the supported slopes, and
every pair has at least `2e+3` expanding thirds. The next boundary is a
rank-two coefficient-map fibre at union `rho+3`.
