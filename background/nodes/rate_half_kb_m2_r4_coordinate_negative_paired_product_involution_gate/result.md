# Result

PROVED.  Every negative coordinate packet must pass a coefficient-free
`6 x 3` paired-product involution gate.  Two distinct antisymmetric product
pairs force all six pairs to be antisymmetric, which symbolically deletes the
exact defect-zero fixture over every odd field.
