# Frontier

Apply `(KBNP-1)` to canonical complete packets built from the seven negative
common-`K` skeletons.  Only paired-involution survivors should be checked
against the full `12 x 4` product matrix and then the `q` equations.  A
universal close requires all packet graphs; the printed fixture is now
symbolically deleted and needs no further sampling.
