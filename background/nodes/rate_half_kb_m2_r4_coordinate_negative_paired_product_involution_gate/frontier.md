# Frontier

Apply `(KBNP-1)` to canonical complete packets built from the five negative
common-`K` skeletons retained by the loop-budget gate.  Only
paired-involution survivors should be checked
against the full `12 x 4` product matrix and then the `q` equations.  A
universal close requires all packet graphs; the printed fixture is now
symbolically deleted in negative and positive linear-product parity and needs
no further sampling.  Positive quadratic product maps remain separate.
