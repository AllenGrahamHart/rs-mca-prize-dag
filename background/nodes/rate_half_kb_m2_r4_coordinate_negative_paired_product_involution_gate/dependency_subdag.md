# Dependency sub-DAG

```text
coordinate complete-fiber Vieta compiler
                    |
                    v
negative paired-product involution gate --ev--> rate-half band closure
```

The incoming edge is a requirement.  The outgoing edge is evidence until
all canonical negative packet graphs are classified.
