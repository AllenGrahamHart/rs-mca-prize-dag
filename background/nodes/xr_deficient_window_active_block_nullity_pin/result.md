# Result

The tuple-incidence worst case is now localized to bounded stacked-window
nullity: at most six on the two larger-`h` rows and at most two on the
rate-`1/16` row. The subsequent two-block kernel-slack router shows that the
positive-nullity branch has additional forced-root geometry absent at
nullity zero. The deficient and full-rank censuses should therefore share
maximality machinery only after that asymmetric structure is retained.
