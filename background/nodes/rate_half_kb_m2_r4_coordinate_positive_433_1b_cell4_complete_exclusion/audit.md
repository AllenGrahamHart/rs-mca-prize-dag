# Audit

The verifier separates the exceptional rank branch from the principal label
census. It requires the global rank-drop parent independently, then treats
the three principal suppliers as sets of `(xi,pairing)` integers. It rejects
overlap, requires equality with `{0,...,6} x {0,...,14}`, and derives 1680
from the fixed `4*4` sign/lane multiplicity rather than trusting a cumulative
counter.
