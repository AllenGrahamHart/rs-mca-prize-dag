# Statement

## Claim `(KBP1B4-COMPLETE-1)`

The complete deployed positive `433-1b -> O0a` route is empty in source role
cell 4.

The proved rank-drop theorem excludes the product-row-rank-at-most-four
branch. On product rank five, three proved suppliers form a disjoint cover
of all principal labels:

```text
parallel-DE roles xi={0,1,2}: 45 labels,  720 raw systems;
outside roles     xi={3,4}:    30 labels,  480 raw systems;
endpoint roles    xi={5,6}:    30 labels,  480 raw systems.
```

Their union is exactly `7 missing records * 15 canonical matchings * 4
source-sign pairs * 4 target lanes = 1680` raw systems. This closes role cell
4 only; it does not transport to role cell 7 or close the positive route,
K3, LIST, MCA, or either Prize problem.
