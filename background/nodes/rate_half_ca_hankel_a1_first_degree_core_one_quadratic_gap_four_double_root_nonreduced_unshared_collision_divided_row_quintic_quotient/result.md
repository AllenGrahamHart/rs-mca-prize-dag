# Result

The nonreduced collision now carries a bounded quotient vector:
`M(t)u(t)=g_*S_BC(t)` with `deg C<=5`. Its six parameter coefficients are
linked by an exact recurrence, and its correction value is a nonzero
geometric progression in the heavy coordinate. This is weaker than the
squarefree cubic quotient but valid on the previously untreated
nonreduced locus.
