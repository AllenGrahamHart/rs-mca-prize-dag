# Audit

1. The fixed heavy row loses the center factor `J_*`, but multiplication by
   `Lambda=J_*Lambda_0` restores it in the Pade syzygy.
2. The moment divisor is therefore `g_*S_B`, not merely `g_off S_B` and not
   the false stronger divisor `D_1=g_*S_B^2`.
3. The degree bound uses `deg F_0<=e+1` and `deg(g_*S_B)=e-4`, leaving
   exactly the quintic cap in both deficit profiles.
4. Cancellation occurs only after common polynomial divisibility is proved.
5. `C_0(tau)` is nonzero because `Lambda_0` and the center-adjusted
   residual are both nonzero there.
6. No Smith profile is excluded by the quintic quotient alone.
