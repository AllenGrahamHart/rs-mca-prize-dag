# Result

Intermediate polynomiality is now a finite cube-part classification problem.
Every candidate direction lies in the cube part of `Rbar`, obeys two exact
derivative-gcd divisibilities, and is recovered precisely when its cofactor
differs from `B` by one scalar multiple of `z^hC`.

Uniform control of that official cube part and the final split/matching test
remain open.
