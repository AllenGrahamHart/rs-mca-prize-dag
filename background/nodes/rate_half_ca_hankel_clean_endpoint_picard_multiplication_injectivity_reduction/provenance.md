# Provenance

This is the cohomological form of the exact Picard pin. It replaces the
open-ended phrase "exclude an irreducible cyclic norm" by a concrete map and
records the relative vector bundle classified by the next node. The initial
injectivity idea is retained only as a route candidate that the child
refutes.

No computation is load-bearing. The verifiers independently replay all
cohomology dimensions, ranks, degrees, and the negative-degree fallacy fence.
