# Claim contract

## Consumed content

- The profile-(3,6) payment supplies the prior residual edge budget.
- The split-prime router and cofactor-1538 exclusion supply
  `T_210<=266` and `|D_210|<=68096`.
- The exact weighted-kernel dictionary supplies every profile multiplicity
  and the oriented-to-unordered factor one half.

## New proved content

1. Profile `(2,10,S=18)` has exact worst-case charge
   `41794840999781162066129578393300739162112`.
2. The residual edge budget is
   `22622986807805210095050326195532090878375`.
3. The next maximum-weight profile is `(1,14,S=18)`.
4. Its tight residual uniform oriented cap is `39193`.

## Guards

- The payment charges all 266 possible ideal families; it does not assume
  the still-open `514` or `1028` branches are empty.
- Every orbit restores all 256 oriented shift/sign vectors before division by
  two in the unordered edge ledger.
- No later profile is paid or declared empty.
- The parent E1 target remains open.
