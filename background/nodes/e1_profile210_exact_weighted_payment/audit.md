# Audit

- The 266-orbit envelope is ten pure families plus 128 families for each of
  cofactors `514` and `1028`; no open family is omitted.
- Each shift/sign orbit contributes 256 oriented vectors.
- The dictionary's division by two is applied once and only once.
- The charge and residual are exact integers.
- All 271 eligible profiles are regenerated from the pinned dictionary; the
  next-profile ordering is not copied from prose.
- The residual cap is checked on both sides of the exact floor boundary.
- Profiles after `(2,10)` remain unpaid.
