# Cell-3 `BC-` complete uncolored outside exclusion

- **status:** PROVED
- **field:** `F_2130706433`
- **scope:** missing `DE+/-`, `DF+/-`, or `EF` in repeated-`BC` cell 3

Every uncolored cell-3 repeated-`BC` `BC-` outside system is empty. The exact
census is

```text
DE+/-: 240 systems,
DF+/-: 240 systems,
EF:    120 systems,
total: 600 systems.
```

The 360 representative systems are generically empty. Exhaustive replay of
2,336 relevant exceptional common fibers gives 3,072 endpoint rows, every
one with residual gcd one, and no survivor or unresolved branch. The exact
`d -> -d` quotient supplies the opposite `DE` and `DF` signs.

Colored missing `BE` and `CF`, cell 6, and the remaining route are open.
