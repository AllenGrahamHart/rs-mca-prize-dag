# Source Evidence

The retained adapter extracts the pinned pairing-4 compiler by syntax tree
and supplies only the exact cell-5 tower and compact-kernel packets. It does
not alter the compiler function. Pilot app `ap-Ecx21odPCc08JkqkmHRaZb` and complete app `ap-XadN0KGQxn6OQnBbxLkk8c` produced the primary packet.

Independent root reconstruction ran as Modal app
`ap-YDq3UwLeD4BRPYUnsWYE5t`: the packet completed and was written before
the client was interrupted. Direct audit app `ap-yfMG7vQfVKsexBoGGb4Jxu`
then consumed that exact packet and passed, including 24 `q` lifts and 96 nonzero final lanes.
