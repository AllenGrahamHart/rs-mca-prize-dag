# Frontier

Split first by loop count.  Three-loop rows have both quotient branch values
pinned and are the smallest interpolation targets; two-loop rows have one
or two branch values pinned.  Retain cross multiplicities three/four until
the quadratic product ratio is imposed.
