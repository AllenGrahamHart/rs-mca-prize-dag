# Attack surface

The fixed `R20` block is closed. The aligned-positive source-line frontier is
now only `M01-R11` and `M02-R11`; attack their existing portability review
gate rather than recomputing fixed charts.
