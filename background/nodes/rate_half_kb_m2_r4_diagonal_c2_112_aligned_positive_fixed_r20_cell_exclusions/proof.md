# Proof

For each fixed `R20` cell, split on `V`. The complete fixed rank-drop theorem
excludes `V=0`.

On `V!=0`, exact quadratic reconstruction gives precisely two cubic factors
and one degree-12 factor. The representative generic-route theorem directly
classifies the `F04/F05` cubic pairs; the literal `F06` replay and proved
complete-system inversion classify `F06/F07`. In every cell one cubic is
empty directly and the other is the unique route excluded by the all-literal
cubic/full-`J` theorem. The four-cell degree-12 theorem excludes the final
factor. These charts and factors are exhaustive, so all four complete cells
are empty. QED.
