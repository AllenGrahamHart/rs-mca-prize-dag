# Audit

- The affine-line argument is applied separately to each exact-deficit
  layer; no common direction between layers is asserted.
- Three missed sets cost at most `3r`, which is why the theorem requires
  `e-3r>=K`.  Pairwise overlap alone would not synchronize all directions.
- Every top-third layer has owner weight one because `r<e/2`.
- The printed `N-m>s` hypothesis makes every outside universe strictly
  larger than its high-layer agreement set.
- The outside common core is bounded by zeros of one nonzero degree-`<K`
  codeword and hence has size at most `K-1`.
- The prefix imports only positive-Johnson cumulative caps through `H`.
- Constants `31` and `47` are conservative rational-cap bounds.
- Endpoint maximality of the line sum is proved termwise.  The independent
  verifier uses quotient grouping rather than the primary term loop.
- All arithmetic is exact and uses negligible memory.
