# Dependency sub-DAG

```text
rate_half_mca_sparse_direction_punctured_johnson_profile (PROVED)
       |
rate_half_mca_sparse_direction_terminal_deficit_line_payment (PROVED)
       |
rate_half_mca_sparse_direction_top_third_affine_line_payment (PROVED)
       |
rate_half_band_crossing_location (TARGET, evidence only)
```

The new node generalizes the terminal line mechanism separately on each
exact high-deficit layer.
