# Claim contract

## Proved

1. Every exact layer `h=e-r` with `e-3r>=K` lies on one affine codeword
   line.
2. The exact-layer cap `(TT1)` and profile compilers `(TT2)` and `(TT3)`.
3. Uniform official Johnson caps `J_u<=31` and `J_H<=47`.
4. The endpoint-maximal affine-line sums and exact row comparisons.
5. Every sparse-direction support `1<=e<d` is paid on both official rows.

## Not proved

1. Any payment at a support `e>=d`.
2. A common affine line across different exact-deficit layers.
3. Any theorem for the remaining middle-support cells.
4. A complete MCA or prize theorem.

## Promotion rule

Keep `PROVED` only while both verifiers accept the source pins,
triple-overlap threshold, restriction injectivity, exact-layer packing,
the printed `N-m>s` scope, uniform Johnson comparison, grouped endpoint
floor sums, and hostile
mutations.
