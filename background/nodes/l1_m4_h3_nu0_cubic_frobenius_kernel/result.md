# Result

Every `nu=0` Mersenne cubic record has a canonical sparse cubic Frobenius
kernel. The former arbitrary derivative-zero ambiguity is exactly three
scalars, with no quartic or constant term, uniformly across all four
eliminant degrees. This is a new classification interface; no `nu=0` case is
yet excluded and L1 remains open.
