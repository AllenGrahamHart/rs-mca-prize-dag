# Statement

Work over the deployed KoalaBear field
`F_p`, `p = 2130706433`, and on the guarded product-rank-five branch of
positive `433-1b -> O0a`.  In source role cell `12`, `BC-` is the singleton
and the matched role pairs are `(LA,AB)` and `(AC,BC+)`.

1. The `AC` pivot covers all `4 * 6 = 24` source-sign/product-cofactor
   charts.  Every guarded common ideal has dimension one, compact basis size
   `15`, and lex basis size `8`; the pivot boundary is the unit ideal.
2. On the open set where the displayed `b`- and `c`-leading coefficients do
   not vanish, the full common ideal is generated by a quadratic relation
   `F(t,r)`, a palindromic quadratic `G(b;t,r)`, and a linear relation
   `H(c;b,t,r)`.  Thus its generic coordinate algebra has basis
   `{1,t} * {1,b}` over `F_p(r)` and `c` is recovered linearly.
3. The discriminant of `F` has one doubled linear route factor and two
   distinct factors of degrees one and three.  Deleting the square factor
   leaves a square-free quartic.  In odd characteristic, the normalization
   of the associated quadratic base curve therefore has genus one.
4. The leading-open complement is classified exactly over `F_p`.  For each
   source-sign pair there is one `b`-leading and two `c`-leading
   zero-dimensional fibers, all with lex basis size `4`.  The four
   `b`-leading fibers each have the two guarded rational `b` values
   `695491477` and `910818720`, giving eight points in total.  Every
   `c`-leading fiber has an irreducible quadratic `b` eliminant and hence no
   `F_p`-point.

This is a common-locus structure theorem only.  It does not exclude any
outside record and does not close cell `12`, cell `13`, their role orbit, or
either Prize problem.
