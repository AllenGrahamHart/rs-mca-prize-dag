# Repeated-BC cells 3 and 6 compact common locus

- **status:** PROVED
- **scope:** all sixteen source-root/BC-sign rows in role cells 3 and 6

The product-rank atlas proves rank five throughout the guarded open set in
these cells. Let `(u_0,...,u_5)` be its cofactor kernel for the five product
rows. If `lambda_0` is the loop label and `lambda_1` the selected nonloop
pivot label, put

```text
S=lambda_1(lambda_0-lambda_1),
gamma=q_1(u_0+u_1 lambda_1+u_2 lambda_1^2),
K=(S u_0,...,S u_5,-lambda_0 gamma,gamma).
```

The source and target guards make `S` and the chosen product cofactor
nonzero. Hence `K` is the exact projective coefficient kernel after imposing
the loop and pivot sum rows. Direct symbolic products show that the five
product rows, loop row, and pivot row vanish identically on `K`.

For every one of the sixteen sign rows, the remaining three sum-row dot
products generate a complete guarded common ideal. Singular gives dimension
one and all ten common rows reduce to zero. Basis sizes are:

```text
cell 3: BC- 23, BC+ 18
cell 6: BC- 23, BC+ 16,
```

independently of source-root signs.

This does not parameterize the curves, append any of the seven outside
source records, prove outside incompatibility, close the route, K3, or either
Prize result.

## Falsifier

A product-rank exception, a zero kernel, a failed row identity/reduction, or
a common survivor outside the guarded three-equation locus.
