# Audit

The verifier pins all inputs, reconstructs all sixteen cases, checks the
kernel and compact-equation custody, the seven identities, all ten zero
remainders, dimensions, and basis sizes. Hostile mutations remove a case,
change a dimension, row remainder, identity bit, and kernel digest.
