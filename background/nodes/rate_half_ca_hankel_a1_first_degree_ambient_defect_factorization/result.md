# Result

Each first-degree profile is represented by a parameter-constant, linear,
or quadratic ambient biform. All domain rows with deficit above that tiny
parameter degree split off as one common `X`-only factor.
