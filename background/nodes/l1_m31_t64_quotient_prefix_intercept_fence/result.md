# Result

Exact replay produces seven distinct `479`-subsets of the pinned
`1022`-point quotient domain. Their first `63` locator coefficients agree,
each neighbor has deficiency `64`, and

```text
d_64(A)>=6,       floor(4H_64/p^32)=0.
```

Thus coefficient-four rooted-shell bounds with intercept `3`, `4`, or `5`
are refuted on this profile. The L1 target remains open; a valid successor
must pay this block-swap class through an earlier owner, use intercept at
least six, or replace the pointwise shell estimate.
