# `A=1` distance-three quartic support low-degree fiber reduction

- **status:** PROVED
- **closure:** proof
- **consumer:** `rate_half_band_closure`
- **dependencies:**
  `rate_half_ca_hankel_a1_core_one_exceptional_only_distance_three_quartic_support_pair_crossing_rank_gate`,
  `rate_half_residual_prime_field_collapse`

Retain the official generic saturated distance-three packet

```text
N=2^41,       e=2^38-1,       mu_N subset F_p^*,
p>2^167,
```

and suppose that every support-only pair-crossing matrix `R_l` passes its
necessary gate. Choose the quartics `P_l` supplied by that gate and put

```text
r_l(X)=P_l(X)/D_l(X)^2.                              (QFR1)
```

Write

```text
H(X)=X(X-s)(X-x_0)B(X)^4A'(X)^4                    (QFR2)
```

for the actual smooth weight. Then at least one of the following two
alternatives holds.

1. **Global antiweight:** every matched pair satisfies

   ```text
   H(a_k)+H(b_k)=0.                                  (QFR3)
   ```

2. **Low-degree common fiber map:** there is a separable rational map

   ```text
   psi:P^1 -> P^1,       d=deg psi in {2,3,4},       (QFR4)
   ```

   such that at least

   ```text
   e-4-9d^2                                                (QFR5)
   ```

   of the exceptional pairs are contained in fibers of `psi`. In
   particular, at least `e-148` pairs satisfy

   ```text
   psi(a_k)=psi(b_k).                                 (QFR6)
   ```

The map in the second alternative is defined over the official base field.
It is obtained from the common function field generated by the pairwise
quartic ratios, not posited as an extra geometric premise.

Thus an all-rank-deficient support packet is no longer an unrestricted
matching. The remaining support theorem splits into one exact smooth-weight
identity and three bounded-degree fiber geometries. This node does not
exclude those four branches and does not assert that an arbitrary
degree-at-most-four fiber matching lifts through the lambda-cube or external
design conditions.
