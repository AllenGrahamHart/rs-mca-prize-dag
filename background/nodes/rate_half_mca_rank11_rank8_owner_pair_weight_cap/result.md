# Result

The rank-eight fixed chart has marked capacity

```text
981105*C(n'-9,2).
```

At `K'=37996` this is `579155144020629315`. The cap is a proved weighted
interface; its comparison with selector demand is handled by the successor.
