# Lineage

This node generalizes
`l1_interior_bc_floor_higher_shell_q_routing`: the planted floor rays are one
instance of nontrivial coefficient content.  It also gives an algebraic
owner implementation for the support-moment identity in
`l1_pade_split_section_support_moment_inversion`.

The proof uses only the interpolation-module basis and squarefree split
locator already present in
`l1_exact_shell_balanced_shifted_lattice_reduction`.  It does not import an
unproved upstream census.
