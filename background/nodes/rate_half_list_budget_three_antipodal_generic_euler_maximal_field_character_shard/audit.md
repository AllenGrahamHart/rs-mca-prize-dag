# Audit

- Checked that `2^41 | q-1` activates the fourth character in both allowed
  ambient field-degree branches.
- Checked the prime-field split modulo three and the identity
  `p^2=1 mod 3` for every quadratic characteristic `p!=3`.
- Kept the exact coupled-norm equality in the shard where cubing is
  bijective; a vacuous character test does not remove the equality.
- Explicitly separated ambient `F_(p^2)` characters from any later
  specialized descent to `F_p`.
