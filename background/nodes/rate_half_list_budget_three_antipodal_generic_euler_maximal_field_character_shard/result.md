# Result

The generic scalar campaign has an exact maximal-field shard table. Every
branch uses the fourth-power character; every quadratic-extension branch also
uses the cubic character; and only the prime-field `p=2 mod 3` shard skips
the cubic character. The exact coupling and vector remainder remain open in
all branches.
