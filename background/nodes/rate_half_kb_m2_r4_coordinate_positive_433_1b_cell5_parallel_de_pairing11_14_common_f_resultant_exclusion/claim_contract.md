# Claim contract

- **Direct scope:** `xi in {0,2}`, pairing 11, 32 source-sign/lane rows.
- **Boundary:** all rational complements hit explicit route guards; the row-6
  tower has no leading-boundary remainder.
- **Transport:** only the exact orbits `{(0,11),(1,11)}` and
  `{(2,11),(2,14)}` are used.
- **Conclusion:** four generic labels are empty; pairing 14 at `xi=0,1` is
  outside this node.
- **Out of scope:** the 36 labels in 11 active orbits that remain, K3, LIST,
  MCA, or Prize closure.
