# Audit

The structural verifier pins the adapter, compiler, result, root certificate,
and direct-audit packet. The independent verifier reconstructs
the root union, replays all tower relations, recomputes both quadratic cuts
and every missing quartic, and checks the terminal partition.

Positive-`DE` pairing 14 at `xi=0,1` is deliberately fenced out.
