# Result

```text
owner parameter space:             affine plane
record objects:                    distinct-direction affine lines
record-pair ledger:                sum C(t_p,2)=C(g,2)
common owner core:                 J_B subset Z(u), |J_B|>=10
off-root owner petals:             pairwise disjoint
extension incidence per record:    at least 45153
fixed-owner slope cap:             981105
fixed-cell record cap:             45567659
```

This packet is directly portable to the upstream base-field-normalized
split-pencil census program, with the explicit nonclaim that cells remain
unaggregated.
