# Audit

1. Nonemptiness of the affine owner plane comes from full-rank component
   extensions, not from arbitrary interpolation of the received word.
2. Record lines have distinct directions because their slopes are distinct.
3. Their unique intersections genuinely own both explanations, so the block
   identity counts every record pair exactly once.
4. On `Z(u)` all owner points coincide; off `Z(u)` the plane evaluation map
   is bijective. This proves exact, not approximate, petal disjointness.
5. Full-rank extensions are off `Z(u)` and therefore consume petal
   incidences.
6. Fixed-owner multiplicity uses the complete owner core and support-wise
   noncontainment; the uniform maximum is attained at deficiency one.
7. `B subset J_B` gives the exact `n'-10` petal resource ceiling.
8. The cap is per cell. No multiplication by an unproved cell count is made.

No Modal computation is used.
