# Frontier

Apply the `45 x 12` gate first to the two coordinate pair-degree profiles
and to each diagonal source-facet packet, and apply it without stabilizer
pairing to all five trivial-branch profiles. For a survivor, reconstruct the
unique `H` and impose exact degree, irreducibility, deck distinction, and
the available branch coefficient compiler. Do not enumerate endpoint
biforms before this smaller source gate.
