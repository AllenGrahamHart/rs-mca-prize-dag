# Result

The unique high-distance endpoint reconstruction is now a remainder stream.
Each coefficient needs one division by `E q_bar`; a remainder of degree at
least two rejects immediately, while an affine remainder supplies both the
correction and the next quotient coefficient. No modular inverse is needed.
