# Result

The three center maps no longer carry anonymous diagonal weights. They are
restrictions of explicit corank-one residue forms, and the only remaining
rank loss is

```text
kappa=dim(intersection_gamma W_X^(perp,beta_gamma)).
```

At the current separation-rank floor, Shape A needs
`kappa>=183251937960`. The next rank attack is therefore a concrete
common-kernel theorem; `kappa<=e-4` already removes the boundary rank.
