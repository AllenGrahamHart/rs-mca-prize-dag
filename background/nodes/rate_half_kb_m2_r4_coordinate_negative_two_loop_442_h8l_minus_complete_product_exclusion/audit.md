# Audit

1. The forced-`DE` and forced-`DF` intrinsic parameterizations are derived
   without identifying endpoint and quotient coordinates.
2. The signed-pair parent is used only for `DE`, whose residual set has two
   complete signed pairs; all 15 `DF` matchings are checked directly.
3. Every ordinary obstruction is factored and normed separately against
   `P_4`.
4. Identically zero projected eliminants at `DF` indices `6,7,8` are not
   called survivors; the complete ideals are checked over the deployed
   field.
5. The alternate chain removes the projection degeneracy and independently
   covers every `DF` matching.
6. The row deletion uses the previously proved colored cells and exactly
   exhausts the three canonical forced types and two signs.
