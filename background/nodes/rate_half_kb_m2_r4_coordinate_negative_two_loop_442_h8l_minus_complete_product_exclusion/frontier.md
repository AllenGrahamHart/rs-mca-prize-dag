# Frontier

Test whether exchanging the singleton placement transports this row
deletion to `H8-M,tau=-1`.  Otherwise derive the analogous one-parameter
`H8-M` intrinsic forms.  Then treat the two `tau=+1` eighth-root rows before
the sixth-root rows.  Do not carry `H8-L,tau=-1` into interpolation.
