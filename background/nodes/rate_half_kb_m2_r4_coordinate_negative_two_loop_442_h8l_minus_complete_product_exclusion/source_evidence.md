# Source evidence

- The colored-cell parent supplies the quartic descent, involution, and two
  already-empty cells.
- The invariant router supplies the exact forced-type and matching coverage.
- The signed-pair theorem supplies the three valid forced-`DE` template cuts.
- The primary verifier reconstructs every ordinary factor norm and all six
  deployed-field unit ideals.
- The audit changes the projection chain and reconstructs nonzero factor
  norms for every forced-`DF` matching.
