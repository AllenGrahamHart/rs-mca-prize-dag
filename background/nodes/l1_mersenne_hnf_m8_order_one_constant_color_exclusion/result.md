# Result

The constant-color chamber of the `m=8,h=7` order-one HNF endpoint is empty
on all four official rows. Combined with the base-field branch exclusion,
the live `h=7` residue has `t notin F_p` and a genuinely nonconstant
rootwise `mu_8` color assignment on the six reduced roots.

No nonconstant color degree has been excluded, so L1 remains open.
