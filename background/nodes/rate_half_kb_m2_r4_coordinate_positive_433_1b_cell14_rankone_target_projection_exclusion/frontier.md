# Frontier

All cases with missing record `df`, `ef`, `bf`, or `cf` are exhausted. This
node handed 576 missing-`de` cases to the fixed-a route. Its downstream chain
theorem has now reduced the current retained cell-14 frontier to

```text
4 source signs * 4 target lanes * 3 missing de roles * 3 matchings = 144,
```

with matching indices `6`, `7`, and `8`.

The next attack should keep `a=+/-B_xi/A_xi` and `a f^2=uv`, then form two
independent resultants from the four mixed projections. Do not return to dense
five-variable standard bases.
