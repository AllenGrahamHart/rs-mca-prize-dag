# Claim contract

## Proved

1. The `r=8` outer component is the connected ordered-adjacency orbital on
   120 pairs of two-subsets.
2. Its genera in the four exhaustive passports are `3,6,4,13`.
3. The actual source normalization has genus at most three and maps
   separably with degree two to that outer component.
4. Riemann--Hurwitz excludes all four passports.
5. The complete inner-degree-four transverse row is empty, leaving exactly
   the eight independent `m=2,3` types.

## Imported proved inputs

- source-component descent and birationality from the actual `(2,4)` curve;
- the transverse degree identity `delta*r=4m`;
- the `m=4` outer route cut;
- exhaustiveness of the four `A6/S6` genus-zero passports.

## Not proved

- No `m=2` or `m=3` type is deleted.
- No parameter-to-carrier/data/slope bridge or chronology-valid owner is
  constructed.
- No owner charge, endpoint cap, adjacent unsafe certificate, KoalaBear row,
  or MCA prize result is closed.
