# Claim contract

- **scope:** official exceptional-only quotient-distance-three chart with
  the exact pair-Lagrange external split design
- **input:** `Q`, the supported-slope product `P`, and the saturated-domain
  product `P_X=A B C`
- **currency:** both complement equations, the two-sided weld, and the
  corrected reciprocal square
- **output:** interpolation formulas `(CSC2)--(CSC3)`, weld `(CSC4)`, and
  automatic entry into the proved exceptional factor-descent chain
- **consumer:** `rate_half_band_closure`
- **load-bearing hypotheses:** exact row degree `e`, exact ordinary fiber
  degree `r`, squarefree split incidences, one omitted row, and parameter
  primitivity of `Q`
- **nonclaim:** no construction or exclusion of the external design; no
  certification that an original lifted endpoint pair is column-far or has
  no additional close slopes
- **failure certificate:** a saturated row where `Q(z;x)` does not divide
  `P`, an ordinary slope where `Q(z;X)` does not divide `P_X`, a failed
  interpolation degree bound, or failure of either weld identity
- **replay:**
  `tools/ramguard tiny -- python3 background/nodes/rate_half_ca_hankel_a1_core_one_exceptional_only_distance_three_corrected_square_converse/verify.py`
- **upstream mapping:** exact finite SPI two-sided split-pencil converse
