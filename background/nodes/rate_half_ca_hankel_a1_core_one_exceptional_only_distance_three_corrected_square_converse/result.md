# Result

The exact distance-three split design reconstructs both complementary
factorizations by coefficientwise interpolation. Coprimality then forces the
two-sided weld, placing the packet automatically in the already-proved
exceptional factor-descent and reciprocal-square chain.

There is no separate corrected-square search on this branch. After the
pair-Lagrange external design is realized, the remaining independent checks
are at the original endpoint level: column-farness and the absence of extra
close slopes.
