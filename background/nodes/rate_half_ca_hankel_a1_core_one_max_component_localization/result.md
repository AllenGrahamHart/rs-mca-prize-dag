# Result

The core-one maximal-degree sharp cap has one dominant component of bidegree
`(2e_*+1,e_*)`; all residual components are slope-two balanced and carry at
most one fifth of the parameter degree. At the official row the dominant
component has separation rank at least five.

This excludes all separated and rank-at-most-four component models. The
remaining mixed high-rank component is still open. No Modal computation was
used.
