# Result

PROVED: `Z_BC` now has a concrete source-bound shifted-lattice certificate
semantics and a canonical one-per-slope selector with unit fibers.

The next unresolved interface is realization of those certificates as actual
legacy `Q=6,s=6,u=2` endpoint records, or an alternative same-owner route
through the active v4 `(S)/(A)/(E)` endgame.
