# Claim contract

## Proved

1. The symbolic BC predicate is resolved to the explicit finite certificate
   relation in `certificate_schema.json`.
2. Its first-match slope projection is exactly `Z_BC`.
3. Public lexicographic minimization gives one canonical certificate per
   `Z_BC` slope.
4. Selection and slope projection are inverse, so every selected fiber is
   exactly one.

## Imported source theorems

- exact shifted-lattice support census and balanced-profile dichotomy;
- active KoalaBear row constants and first-match tangent/Q/BC chronology.

## Not proved

No `Q=6,s=6,u=2` endpoint hypothesis, endpoint component, parameter/carrier
bridge, slope count, BC payment, K3 close, or row close.
