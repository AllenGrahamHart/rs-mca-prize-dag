# Proof

Fix one of the four listed cells. If `s=0`, apply the literal degree-12
`s`-zero exclusion. Otherwise split on the cell's irreducible degree-six
`x`-leading factor `L6`. Its zero branch is empty by the literal leading-curve
exclusion.

On `s L6!=0`, split on the `B0` leading factor `K8`. The `K8!=0` chart is
empty over `F_(p^6)` by the exact zero-dimensional field-quotient
certificate. The complementary `K8=0` branch is empty over the algebraic
closure by the exhaustive `K10` dichotomy, including the `K8=K10=0`
residual.

The argument was replayed literally in each assignment; no symmetry
transport is used. The five cases partition every field point of each
selected degree-12 branch. QED.
