# Result

The cubic `3+2+1` profile now has an exact common-quadratic factor model.
After triangular coefficient elimination, each role packet has only four
p-free variables and five explicit equations.
