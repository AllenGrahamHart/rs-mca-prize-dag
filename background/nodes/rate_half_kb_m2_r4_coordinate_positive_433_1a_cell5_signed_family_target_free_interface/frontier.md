# Frontier

Use `(KBSF-4)--(KBSF-7)` as the next cell-5 algebraic interface.  The exact
saturated common quotient is available, but both its four target-free cuts
and its six sparse unsquared generators exhausted bounded standard-basis
caps.  Compute a regular chain or factor the first signed pair before adding
the colored edge.  The `BE` endpoint has already been reduced to its residual
cubic, so append only that cubic and the colored unsquared sum row.  Do not
retry raw six-record, scalar-resultant, expanded
squared-sum, or larger generic standard bases.

The favorable outcome is a guarded unit ideal for either signed family,
which deletes every outside case in this fixed cell/sign row without the
267-case matching ledger.  A nonunit outcome must be classified and then
intersected with the colored third edge, second family, or missing-mate cut.
The signed pair alone has already timed out under generic `std`; request a
regular-chain or subresultant component ledger directly.

The finite-algebra pilot now sharpens that request.  Recomputing the chart
honestly over `F_65521` with `iota=24297` gives a saturated generic fiber of
dimension zero, vector dimension four, and one minimal component.  Its
three-element lex basis has a reciprocal degree-four primitive polynomial in
`b`, so it descends exactly to a quadratic in `b+b^{-1}`.  Eliminating `r,c`
then leaves one reciprocal quartic and four signed-pair cuts with
`(degree,terms)=(9,24),(8,32),(9,24),(8,32)`.  Even this presentation hit a
240-second `slimgb` cap.  This is probe-field evidence, not a deployed-field
theorem.

The deployed reciprocal quartic, trace-quadratic descent, and `F_p` rational
lift atlas are now PROVED.  The latter gives one guard-unit `r` formula and
four `c` charts whose simultaneous exceptional cubic has no deployed-field
root.  Reduce the target-free cuts separately on those four charts and take
subresultants or finite-algebra norms branchwise.  Singular 4.3.1 cannot
perform the analogous function-field remap at the deployed characteristic
because its backend caps the characteristic at `2^29`; do not retry that
backend or another generic standard basis.
