# Frontier

The paired-product determinant-zero fibers, colored `BE/CF` records, and the
selected-cofactor boundary are now excluded on the deployed symmetric tower
chart.  The remaining cell-11 work is the registered guard complement.  Only
after that complement is paid should the closure be transported to cell 14.

The guarded signed-pair witness in upstream PR #1155 is a route fence: at
least one necessary quadratic-cover incidence survives, so a guard transplant
cannot replace these payments.
