# Source evidence

The prerequisite common-pole cancellation is the immutable upstream theorem
harvested in
`rate_half_mca_pole_tolerant_scalar_locator_harvest`. The new content is the
elementary uniqueness argument after cancellation. It uses only the source
degree inequality `t<=m-k`, exact support inclusion `P subset S_i`, and the
original support-wise MCA nontriviality.
