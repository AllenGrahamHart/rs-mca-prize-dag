# Claim contract

This node changes only the weighted sparse completion premium at `K'=43`.
It reuses all kernel coranks, the all-core rank-nine chart, all 55-shadow
deficits, and the sharp isolated-incidence lower bound from the preceding
payment.

The 27 descending-support leaves are exhaustive.  Every inherited fallback,
terminal source ceiling, and valid cross-support carrier is intersected with
the prior supportwise cap before weighting.

The node closes only the rank-nine component target at `K'=43`.  It does not
pay a rank-eight alternative, close rank eleven, move active-v4 mass, or
assert closure at `K'=44` or later.
