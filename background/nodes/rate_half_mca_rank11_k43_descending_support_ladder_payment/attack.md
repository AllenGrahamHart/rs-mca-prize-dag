# Attack record

The support-five payment failed at `K'=43` only because its broad fallback
priced every lower source support independently.  Descending through source
supports four, three, and two replaces that fallback by 22 terminal leaves
and one simultaneous all-fallback leaf.  Those leaves all fall below the
already explicit support-five defect-two branch.

The resulting premium closes `K'=43` with a larger gap than the two-stage
support-five/support-four split.  At `K'=44`, the same active defect-two
branch is already too expensive, so merely extending the fallback ladder
cannot close the next row.
