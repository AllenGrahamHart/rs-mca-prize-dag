# Audit

1. All 27 leaves of the proved descending-support ladder are evaluated.
2. Every leaf retains all fallback ceilings inherited from earlier stages.
3. Every terminal source ceiling and valid carrier cap is intersected with
   the preceding supportwise cap.
4. All support deficits `C(11-d,2)` for `2<=d<=9` are retained.
5. The maximum premium is independently identified as `c=5,s=2`.
6. Every kernel corank and every rank-nine core offset is retained.
7. Demand subtracts the exact global isolated cap `C(n,11)` once.
8. The record coefficient and exact floor-record cross are positive.
9. `K'=44` is replayed independently and remains a negative control.
10. No rank-eight or chronology target is declared paid.
