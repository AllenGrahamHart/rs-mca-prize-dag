# Source Evidence

The deterministic symbolic transport script checks 20 common role rows,
four target lanes, 420 lane/label bijections, all outside products and sums,
and the target guard divisor. The node verifier pins and runs it; the audit
independently reconstructs the role-cell and 105-label permutations.
