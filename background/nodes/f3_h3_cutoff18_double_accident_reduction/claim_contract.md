# Claim contract

- **Claim:** the single-quotient layer of `X_18` costs at most `(n-1)^2`, so
  only the double-accident moment `Y_18` consumes the residual C36' budget.
- **Quantifiers:** every official row; in fact every finite set `A` in a
  multiplicative group with nonnegative integer product and quotient fibers.
- **Dependency:** elementary counting for `(DA1)--(DA5)`;
  `f3_h3_shifted_product_sidon` only for the finite-characteristic accident
  interpretation.
- **Consumer:** preferred sufficient route for
  `f3_h3_mobius_excess_half`.
- **Exact remaining input:** `17Y_18<=283n^2+34n-17`.
- **Nonclaims:** no estimate for `Y_18`, no pointwise overlap theorem, and no
  assertion that product and quotient obstructions are independent modulo
  the row prime.
- **Falsifier:** a nonnegative integer profile violating `(DA1)` or `(DA2)`,
  or constants in `(DA3)` that fail to imply `17X_18<=300n^2`.
- **Replay:** `tools/ramguard tiny -- python3
  background/nodes/f3_h3_cutoff18_double_accident_reduction/verify.py`.
