# Claim contract

- **claim:** the nonidentity maximum-fiber statement `P<=24` is exactly one
  divisibility between the cutoff-12 unordered multiplicity gcd and the
  diagonal double-root gcd.
- **scope:** every dyadic split row `p=1 mod n`.
- **dependency:** the proved integral unordered/diagonal product compiler and
  its identities `P=2U-D`, `D<=2`.
- **falsifier:** a row where the displayed divisibility and the direct
  nonidentity maximum-fiber verdict disagree, or a failure of either local
  valuation formula in `(P24G3)`.
- **certificate:** monic gcd, quotient, and Bezout witnesses, the removed
  `T=1` valuation, and one exact divisibility quotient.
- **nonclaims:** no efficient official polynomial construction, prime
  candidate generation, row certificate, `P<=24` theorem, DSP8 promotion, or
  C36' close is asserted.
