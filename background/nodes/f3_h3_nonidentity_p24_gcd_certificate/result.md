# Result

`PROVED`. The vacuous DSP8 statement `max_(t!=1)P(t)<=24` is equivalent to
one exact polynomial divisibility `G_12^neq|H_D`. This supplies a compact
certificate/checker endpoint once the unordered polynomial is available.
