# Provenance

Derived by re-auditing the Cycle-160 local valuation argument against the
fully cancelled exact resultant `(ERQ3)`. The audit shows that center roots
belong to the same exact-order-two branch as all other roots which are not
off-line supported, removing the earlier center-disjoint restriction.

No external theorem or computation is used.
