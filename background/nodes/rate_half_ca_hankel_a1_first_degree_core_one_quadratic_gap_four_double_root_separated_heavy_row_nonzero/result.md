# Result

On the complete separated double-root locus,

```text
G(t,x_*)!=0,
T_j!=0                 for every j=0,1,2,3.
```

Thus the zero-row obstruction is gone even when both correction roots are
center roots. The remaining separated wall is exact divisibility of a
nonzero barycentric remainder by `H`.
