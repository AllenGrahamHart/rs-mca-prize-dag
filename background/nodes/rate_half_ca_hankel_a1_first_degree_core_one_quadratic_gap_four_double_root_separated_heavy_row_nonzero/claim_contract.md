# Claim contract

- **Claim:** the heavy row cannot vanish anywhere on the squarefree,
  supported-disjoint separated double-root locus, including correction-center
  overlap.
- **Dependencies:** heavy-quotient row factorization, exact regular-quartic
  resultant, all-excess vertical factorization and transversality, and the
  barycentric remainder gate.
- **Output:** `(HSN2),(HSN3)`; every overlap polynomial `T_j` is nonzero.
- **Consumer:** the separated double-root route in
  `rate_half_band_crossing_location`.
- **Nonclaims:** no exclusion of a nonzero passing remainder, nonreduced
  correction, or supported/correction collision.
- **Falsifier:** a separated packet with `G(t,x_*)=0` satisfying the exact
  resultant and all-excess fiber identities.
- **Replay:** `tools/ramguard tiny -- python3
  background/nodes/rate_half_ca_hankel_a1_first_degree_core_one_quadratic_gap_four_double_root_separated_heavy_row_nonzero/verify.py`
  and `verify_audit.py`.
