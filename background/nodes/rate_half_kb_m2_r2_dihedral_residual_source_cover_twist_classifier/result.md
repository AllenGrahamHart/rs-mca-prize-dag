# Result

The actual second endpoint twist must send the two roots of
`z^2-b*d*z+b^2+d^2-4` to the endpoint branch values `{2,b}`. The source is
rational exactly when `b^2=a+2` and elliptic otherwise. Both regimes remain
open pending the common degree-30 function and complete source locators.
