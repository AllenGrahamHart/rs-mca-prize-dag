# Claim contract

- **Claim ID:** `rate_half_kb_m2_r2_dihedral_residual_source_cover_twist_classifier`
- **Input:** an actual residual `Q_(a,b)` together with its second endpoint
  quotient and one of the proved source V4 passports.
- **Output:** the exact branch-preimage quadratic `(KBMT-1)` and the genus
  classification `(KBMT-3)`.
- **Dependencies used:** the dihedral normal form, the one-parameter endpoint
  normalization, equality of the two quadratic source subextensions, and
  the source V4 branch-place counts.
- **Not claimed:** existence for either genus regime, the common degree-30
  function or pole/source realization, profile deletion, owner, or payment.
