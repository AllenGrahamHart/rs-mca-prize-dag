# Frontier

The relative second-endpoint branch geometry is now exact. In the rational
regime specialize `b^2=a+2`; in the elliptic regime retain its complement.
Next impose that both reflection quotients compose to the same degree-30
outer function with the exact six order-five pole divisor. Then express the
twelve source locators through the forced root matching `(KBMT-1)`. Do not
set `ell=1` or merge the two genus regimes.
