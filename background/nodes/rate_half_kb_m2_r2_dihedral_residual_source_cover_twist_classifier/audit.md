# Audit

- `Z_0` is the standard quotient by the second reflection; the actual
  endpoint coordinate retains an explicit projective twist `ell`.
- The proof compares quadratic function-field square classes, not values at
  only the twelve source labels.
- Squarefreeness of `Q_b` uses both standing fences `b!=+/-2` and
  `d^2 in {1,3}`.
- The parity argument records all four odd-support points and does not assume
  `ell` is the identity.
- Genus zero and genus one use their exact second-pullback branch counts two
  and four.
- No existence, pole/source realization, profile deletion, owner, or payment
  is inferred.
