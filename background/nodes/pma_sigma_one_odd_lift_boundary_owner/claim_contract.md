# Claim contract

## Input

- an official even-dimensional `sigma=1` row;
- the scale-two quotient map `pi(x)=x^2`;
- a nonzero source word admitting `U(x)=(x-a)V(x^2)`;
- exact agreement size `k+1` or `k+3`, with only `a` retained from the split
  fiber.

## Output

A chart-free global owner of size less than `3Q_2(k+2)`.  Together with the
two preceding quotient owners, the complete charge is less than eleven of the
available 719 first-scale units.

## Consumer effect

The binomial-scale #171 odd-lift obstruction at its first two exact levels is
removed before the B11/source chart route.  The remaining below-floor
full-petal target is the non-lift class plus higher exact odd-lift levels and
other unowned quotient-boundary sources.

## Falsifier

A nonzero source word with two distinct split factorizations, an owner member
whose exact support is not one singleton plus complete scale-two fibers, or an
official row on which the displayed support count reaches `3Q_2(k+2)`.
