# Claim contract

| field | content |
|---|---|
| claim | Every remaining H6 invariant-product cell is empty, so the complete paired-product frontier of the `(4,4,2)` two-loop skeleton is empty. |
| assumptions | Exact H6 row classifier, protected forced-product denominator, product injectivity, the invariant router and signed-pair theorem, and the proved deletion of all H8 rows. |
| exact interface | Quadratic rows `(KB44F-1)`, forced value `(KB44F-2)`, involution `(KB44F-3)`, factor-norm table `(KB44F-5)`, and collision divisor `(KB44F-6)`. |
| nonclaim | Closure of `(4,3,3)`, another coordinate skeleton, the full coordinate orientation, a Prize row, or either Prize theorem. |
