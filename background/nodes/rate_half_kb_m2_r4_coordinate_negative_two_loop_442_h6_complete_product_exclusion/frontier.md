# Frontier

Remove the `(4,4,2)` skeleton from all downstream complete-source work.
For `M2/M3`, saturate the existing 20 invariant-product cells.  Separately,
extend the outside forced-mate and invariant-product compiler to the exact
`X2/N1/L1` common-`K` ledgers; those three rows are not covered by the
20-cell router.  Reuse the signed-product machinery, but do not assume the
H6 collision identity persists on their quotient rows.
