# Audit

1. The cancellation giving `p_xi=-b` uses the compiler's protected
   denominator.
2. Forced `DF` is removed by a product collision, not counted as a failed
   matching.
3. Both H6 rows, both `sigma` signs, and both colored forced types are
   covered.
4. The six representatives cover twelve mixed matchings by the proved
   `F -> -F` symmetry; indices `0,1,2` come only from the signed-pair parent.
5. Opposite-sign cells have two independent resultant orders and explicit
   deployed-characteristic norm checks.
6. Aligned exact ideals are either unit or supported on `a^2=b^2`; that
   divisor is excluded by a direct injectivity collision.
7. The independent audit saturates the original equations rather than
   relying on the primary Groebner basis presentation.
8. The conclusion closes only the `(4,4,2)` skeleton.
