# Claim contract

## Proved

1. The `r=2` survivor has dihedral geometric monodromy and is geometrically
   affine-equivalent to a Dickson quintic.
2. Its divided difference has the two printed quadratic factors over
   `sqrt(5)`.
3. Four of the five `r=4` branch profiles have one rigid polynomial normal
   form; the fifth has the printed one-parameter form.
4. Every live inner-degree-12 outer map belongs to one of the six printed
   families after geometric affine source and target changes.

## Imported proved inputs

- only `(r,delta)=(2,24),(4,12)` survive at inner degree twelve;
- the `r=4` type has exactly the five low-genus branch profiles.

## Not proved

No family is deleted, realized, or descended to a specific supplied
endpoint record. The affine normalizations are geometric; a challenge-field
coefficient classifier still has to carry its own descent proof. No owner,
charge, carrier/data bridge, `m=12`, `u=2`, cap-`68`, endpoint, or row close
is asserted.
