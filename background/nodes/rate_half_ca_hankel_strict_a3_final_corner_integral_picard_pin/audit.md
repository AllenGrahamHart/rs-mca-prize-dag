# Audit

1. The interpolant degree is the actual pole length `d`, not the omission
   upper bound `O`; both possible values are replayed.
2. The restriction kernel in `(5)` is `(-5+d,-e)`, so its `H^1` vanishes for
   `d<=1` without a smoothness assumption.
3. `A_d` is nonzero because the product section is nonzero on a
   contact-active component.
4. An `X`-only polynomial cannot vanish on a mixed component. This upgrades
   nonvanishing of `s_F` from one component to every component.
5. The degree-zero Diophantine branch is killed by
   `gcd(e,e+1)=1`; no component classification is assumed.
6. The conclusion is geometric over the algebraic closure. It is enough to
   contradict a putative endpoint over the official base field once the
   final divisor identity is excluded.
