# Dependency sub-DAG

```text
proved evidence: saturated c112/c202 closes (0); negative coordinate close (0)
                                      +
positive payment + corrected geometry assembly + allocation inequality
                                      |
       exact K3 ledger U_K3=U_positive+U_geometry <= allocation
```
