# Attack

1. Freeze the active row, partition digest, owner order, and K3 allocation.
2. Extract the literal route integers from the positive-coordinate payment.
3. Extract the literal source-line, source-cover, and trivial-stabilizer
   integers from orientation assembly.
4. Audit genuine-pencil hypotheses, line counts, and higher-dimensional ray
   compilers for every nonzero term.
5. Sum once in distinct affine slopes and check the exact balanced-core
   allocation after sibling `U_Q` and `U_new` charges.

If all residual terms are eliminated, record `U_K3=0`; do not assume zero
before the route and source-cover compilers establish it.
