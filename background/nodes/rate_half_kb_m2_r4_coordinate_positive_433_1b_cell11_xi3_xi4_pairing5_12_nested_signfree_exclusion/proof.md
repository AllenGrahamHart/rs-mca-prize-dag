# Proof

Delete the `DF` record and use the exact source-role cell-11 tower and compact
kernel supplied by the parent nodes. Put `m=df`, `S=(d+f)^2`, `z=1/d`,
`y=z^2`, and `q=de`. The missing Vieta equation is

```text
M(y)=1+(2m-S)y+m^2y^2=0.
```

The pinned cell-4 pairing-5 compiler eliminates the first two residual
pair equations by its nested sign-free construction. A syntax-tree adapter changes
only the source algebra: it supplies the cell-11 row-6 four-basis tower and
compact kernel, while retaining the audited compiler function unchanged.
The resulting linear remainder is normed through the exact basis
`1,t,b,bt`.

All 8 required rows terminate. Their complete ledger contains 56
target roots, 140 candidate roots, 240 guarded source routes,
208 checked finite points, 16 missing-impossible exits, 16 target-product
boundaries, 40 `r`-boundaries, 16 `t`-boundaries, 16 cell-11 leading
boundaries, and 52 nonsquare
no-`b` exits. No row exports a witness, free branch, or unresolved case.

A separate python-flint reconstruction covers the combined pairings 3--5
packet: 73 distinct polynomial profiles, 370 field roots, and degrees through
6510. A second independent program replays candidate-root coverage,
source relations, missing records, and the original target equations while
keeping the three pairing ledgers separate. The direct replay checks 448
nested root intersections, retains 16 compatible `q` lifts, and evaluates all
32 final colored lanes. Every final value is nonzero.

The universal generic-label orbit quotient transports `(3,5)` exactly to
`(3,12)`, `(4,5)`, and `(4,12)`. Therefore every label in
`{(3,5),(3,12),(4,5),(4,12)}` is empty. QED.
