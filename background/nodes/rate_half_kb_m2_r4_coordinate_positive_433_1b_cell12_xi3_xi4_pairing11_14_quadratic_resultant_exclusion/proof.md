# Proof

After deleting `DF`, matching `11` pairs the residual products as

```text
(de,bf), (de,sigma_c cf), (-de,sigma_o ef).
```

Write `m=df`, `S=(d+f)^2`, `z=1/d`, `y=z^2`, and `q=de`. The missing Vieta
equation is

```text
M(y)=1+(2m-S)y+m^2y^2=0.                         (RS12-11)
```

The first two matching equations are quadratics in `q`:

```text
B(q,z)=paired(q,bmz),
C(q,z)=paired(q,sigma_c cmz).
```

Their exact Sylvester resultant is degree eight in `z`. Reduce it modulo the
quartic `M(z^2)`, split the cubic remainder into even and odd parts, and
multiply the two signs. The resulting polynomial in `y` has a linear
remainder modulo `M` in every source-sign/`sigma_c` row. Its division-free
common-root cut is normed through the exact basis `1,t,b,bt`.

A pinned AST adapter applies the audited quadratic-resultant/sign-free
compiler to the cell-12 tower and compact kernel unchanged. Eight rows cover
all source signs and both `sigma_c` anchors. Every deployed root of all norm
and inverse numerators and denominators is included. The 108 case-labeled
roots replay to 136 guarded source points. The boundary ledger has 40
`r`-guard exits, 32 `t`-guard exits, and eight cell-12 `b`-leading exits; the
last lie in the complement paid by the rational-boundary parent.

Direct replay retains 16 compatible `(z,q)` lifts. At each lift both
remaining `sigma_o` lanes evaluate

```text
paired(-q,sigma_o ef).
```

A separate SymPy/Galois-tools audit reconstructs all norm and inverse roots.
The local audit rebuilds the source kernel and all three paired equations at
every lift. All 32 final values are nonzero. There is no witness, final
boundary, or unresolved branch.

Exchanging the identical positive `DE` records maps matching `11` to
matching `14`; outside `D/E` transport maps missing `DF` to missing `EF`.
The exact generic orbit quotient proves both transports and closes all four
labels. QED.
