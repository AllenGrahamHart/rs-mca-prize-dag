# Result

The attempted all-admissible extension of the F2 direct-sum reduction is
REFUTED by an official generating Mersenne-prime row. The proof surface is
now correctly split:

```text
p=1 mod 4: bounded prime-field class decomposition [PROVED]
p=3 mod 4: coupled extension-field kernel [OPEN]
```

The counterexample prevents downstream nodes from treating the plus-branch
GRS mass terminal as an all-row terminal.
