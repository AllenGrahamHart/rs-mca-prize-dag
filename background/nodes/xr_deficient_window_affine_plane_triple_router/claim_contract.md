# Claim contract

| item | contract |
|---|---|
| claim | The triple-flag incidence inequality `(APT1)`, its restriction-generic cap `(APT2)`, and the official `ell=1` arithmetic slice `(APT3)`. |
| inputs | The active-defect punctured-list router, its two disjoint selected blocks, the `phi` fiber cap, and the generalized-weight core-incidence lower bound. |
| proof type | Exact affine-plane determinant geometry, Bezout, double counting, and exact integer arithmetic. |
| checker | `python3 background/nodes/xr_deficient_window_affine_plane_triple_router/verify.py` |
| nonclaim | No bound on `I_deg`; no unconditional payment at affine dimension `11,11,10`; no critical status promotion. |
| falsifier | A nondegenerate fixed core-plane/triple supporting four affine target parameters, or failure of an `(APT3)` integer inequality. |
