# Result

The first affine-plane mixed-incidence layer is exact.  It pays the printed
next-dimension `ell=1` slice unless the residual two-dimensional direction
code has zero or repeated projective evaluations on a selected-block triple.
At this stage the obstruction is a named restriction-degeneracy incidence,
not an unspecified proportional-conic case.  The downstream affine-plane
component payment subsequently removes that incidence.
