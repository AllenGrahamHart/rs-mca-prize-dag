# Frontier

The remaining heavy-plane problem is now precise:

1. prove base-field descent/normalization for the two pencils, or isolate
   the generated-field obstruction exactly;
2. attach the 41 used ruling planes and the <=11 exception record to one
   upstream split-pencil selected-support ledger; or
3. derive a direct contradiction from the base-free Segre evaluation code.

The recursive common-base output of the parent node remains a separate horn.
