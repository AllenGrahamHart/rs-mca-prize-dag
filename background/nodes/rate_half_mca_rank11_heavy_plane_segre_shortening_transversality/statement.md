# Rank-eleven heavy-plane Segre shortening and transversality

- **status:** PROVED
- **scope:** the heavy rank-four plane output of the rank-two residual split

Let `B_*` be the heavy residual plane. It has first-owned mass at least

```text
M_*=9965407986
```

and at least `37733` common-zero coordinates after the rank-two triple has
already been deleted. Cancel its full squarefree locator of degree `z`.
Then

```text
37733<=z<=1048569,
(N,K,m)=(2097149-z,1048573-z,1116045-z),
4<=K<=1010840.                                          (HS1)
```

Writing `Q=B_*/L_Z`, one has

```text
dim P=dim Q=2,
dim span(PQ)=4,
P tensor Q -> span(PQ) is an isomorphism.               (HS2)
```

Both pencils are base-free on the remaining anchor-good universe. In chosen
bases, every evaluation column of the correction space is a Segre column

```text
(p_0q_0,p_0q_1,p_1q_0,p_1q_1)(x),                      (HS3)
```

and every associated factor slice is one ruling plane `gQ`. At least

```text
ceil(M_*/R_2)=41,       R_2=248644099,                  (HS4)
```

distinct projective factor slices are used.

The support-local transversality compiler, uniformly over the full range in
`(HS1)`, forces a selected slope and `b in span(PQ)` with

```text
|{x in S_gamma:r_1(x)!=b(x)}|<=11.                     (HS5)
```

Thus the heavy plane is an exact field-internal coherent `2 x 2` Segre
split-pencil bucket with retained mass, many used ruling planes, and one
low-exception direction record.

## Nonclaim

Field-internal Segre normalization is not the base-field normalization
required by upstream's split-pencil census. The theorem does not pay the
bucket, aggregate unrelated heavy planes, or close the recursive common-base
output.
