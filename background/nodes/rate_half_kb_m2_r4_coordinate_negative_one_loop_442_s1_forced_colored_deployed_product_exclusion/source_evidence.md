# Source evidence

The preceding deployed exclusions supply the checked quotient-field solver.
The sign classifier supplies the two parity representatives, and the mate
compiler supplies the unit values `c` and `m`.
