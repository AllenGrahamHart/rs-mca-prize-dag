# Claim contract

| field | content |
|---|---|
| claim | Both deployed `S1` parity cells with forced colored record `CE/CF` are empty in common sign row `(1,1)`. |
| assumptions | The preceding deployed sparse solver, exact sign-orbit classifier, and unit `c` in the common quotient. |
| exact interface | Delete two canonical cells; accepted product frontier at most 76. |
| nonclaim | Common-sign transport, forced-loop or forced-`EF` deletion, outside `q`, interpolation, coordinate or row closure, or either Prize theorem. |
