# Frontier

The representative common sign row retains the two forced-loop and four
forced-`EF+/-` cells of `S1`, plus its `S0` and `S2` cells.  Attack one
rational forced-`EF` type next, or prove common-sign transport.
