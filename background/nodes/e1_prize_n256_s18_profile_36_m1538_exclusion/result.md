# Result

`PROVED`: cofactor `1538` is impossible in the prize profile
`(3,6,S=18)`. Eleven cofactors remain.

The shortest remaining windows are now `m=1024` and `m=1028`, both with
even `4<=V<=34`. Their different 2-adic multiplicities (`10` and `2`) should
be classified separately before any norm computation.
