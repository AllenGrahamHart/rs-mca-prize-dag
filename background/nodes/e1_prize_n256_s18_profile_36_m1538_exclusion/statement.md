# E1 prize N=256 profile-(3,6) cofactor-1538 exclusion

- **status:** PROVED
- **closure:** proof plus dual exact finite enumeration
- **scope:** prize-envelope `N=256`, profile `(a,b)=(3,6)`

There is no profile-`(3,6,S=18)` collision whose norm cofactor is

```text
m=1538=2*769.
```

More strongly, if the six singleton exponents have binary root multiplicity
`mu=1`, no coefficient vector with three entries of magnitude two and six
entries of magnitude one has autocorrelation energy

```text
E=sum_(d=1)^63 A_d^2 in {2,3,4,5,6}.                (1)
```

The preceding cofactor-window theorem gives `mu=1` and
`V=2E in {4,6,8,10,12}` for every putative `m=1538` collision. Equation (1)
therefore excludes the entire cofactor class. Eleven profile-`(3,6)` prize
cofactors remain.
