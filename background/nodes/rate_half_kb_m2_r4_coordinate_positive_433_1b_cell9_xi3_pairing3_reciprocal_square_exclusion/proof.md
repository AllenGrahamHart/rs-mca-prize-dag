# Proof

Write `m=df`, let `s=(d+f)^2`, put `z=1/d`, `y=z^2`, and set `q=de`.
Then

```text
f=mz,  e=qz,
M(y)=1+(2m-s)y+m^2 y^2=0.
```

The colored pair in matching 3 is

```text
C(z)=Pair(bmz,sigma_c cmz).
```

Splitting `C(z)=E(y)+zO(y)` gives the sign-free quartic
`K(y)=E(y)^2-yO(y)^2`. Division of `K` by `M` in the exact four-basis
source tower leaves a linear remainder. Its division-free common-root cut
is normed through each of the six exact charts.

The full compiler covers four source signs, two `sigma_c` signs, and six
charts: 48 rows. External Frobenius-gcd custody reconstructs 69 distinct
norm/guard profiles, 324 deployed roots, and degrees through 1,428.

A separate direct replay rebuilds all 896 candidate roots and 672 guarded
source routes. It finds 96 common roots of `M(z^2)` and `C(z)`, and recovers
`d=1/z`, `f=mz` for each. For each `sigma_o` in `{-1,1}`, it independently
intersects

```text
Pair(q,-q)=0
Pair(q,sigma_o q m z^2)=0.
```

All 192 lane intersections are empty. The aggregate terminal census is

```text
target roots                          600
candidate roots                       896
source routes                         672
missing-impossible points              48
missing-free regularized points        96
target-product boundaries              48
ordinary checked routes               480
common z roots                         96
z/d/f lifts                            96
final q-lane records                  192
common q roots                          0
final-pair solutions                    0
```

The 48 `b`-leading and 96 `c`-leading exits lie on proved tower boundaries;
the 96 missing-free rows are owned by the proved regularized-base packet.
No witness, free branch, remote error, or unresolved stratum remains.

Finally, universal transport maps `(3,3)` to exactly
`{(3,3),(3,6),(4,3),(4,6)}`. All four labels are empty. QED.
