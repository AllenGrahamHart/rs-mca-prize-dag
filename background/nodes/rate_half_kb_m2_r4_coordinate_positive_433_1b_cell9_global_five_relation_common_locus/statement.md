# Statement

## Claim `(KBP1B9-COMMON-1)`

In deployed positive `433-1b -> O0a` role cell `9`, the singleton is `BC+`
and the paired roles are `(LA,AB)` and `(AC,BC-)`.  Over
`F_2130706433`, on the printed source/target guards and the product-rank-five
branch:

1. the `AB` sum-row pivot has empty boundary in all four source-sign rows;
2. saturation by the ideal of all six product cofactors gives a global
   one-dimensional common locus with compact basis size `17` and lex basis
   size `7` in every sign row;
3. this global ideal equals the independently saturated product-cofactor
   chart `1` ideal in both directions; and
4. if its lex elements are `K0,...,K6`, each of
   `{K0,K1,K3,K4,K6}`, `{K0,K1,K3,K5,K6}`, and
   `{K0,K1,K4,K5,K6}` generates the complete lex ideal.  Thus `K2` is
   redundant and three exact five-relation presentations are available.

This is a common-locus theorem only.  It excludes no outside label, does not
close cell `9` or its cell-`10` duplicate, and proves no rate-half, K3, LIST,
MCA, or Prize conclusion.
