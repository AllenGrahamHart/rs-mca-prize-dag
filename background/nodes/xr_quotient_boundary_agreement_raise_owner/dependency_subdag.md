# Dependency sub-DAG

```text
xr_quotient_image_remainder_one_boundary_descent [PROVED] --+
                                                            +--> xr_quotient_boundary_agreement_raise_owner [PROVED]
xr_mismatch_terminal_tangent_agreement_raise [PROVED] -----+                         |
                                                                                     +--req--> xr_quotient_max_agreement_first_match_owner
                                                                                     |
                                                                                     +--ev---> xr_tangent_support_mismatch_bridge
```

The boundary descent isolates all higher-threshold factors to exact-`A` or
one-point tails. The agreement-raise theorem owns every such tail in the
single original set `B_(A+1)`. The downstream first-match owner removes that
same set from the exact-threshold term.
