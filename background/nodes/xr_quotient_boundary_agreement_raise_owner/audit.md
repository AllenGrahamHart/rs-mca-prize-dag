# Audit

## Verdict

The composition is exact and removes a real apparent currency. It is an
ownership theorem, not a numerical bound.

## Scope checks

- The stopping support has size `B>A`, hence `B>=A+1`.
- Its pure-fiber predecessor has size `B-1>=A`, so the recovered codeword pair
  supplies all core agreements required by the agreement-raise theorem.
- The added point is a genuine discrepancy coordinate and the same codeword
  `p_z=c_0+z c_1` agrees there.
- The owner `B_(A+1)(u,v)` is defined on the original received line and is
  independent of the pure-fiber core and quotient scale.

## Limitations

The exact-`A` quotient image remains load-bearing, and the raised bad-slope
set must still be handled by a terminating first-match induction. The theorem
does not permit replacing `B_(A+1)` by `B_(A+2)`.
