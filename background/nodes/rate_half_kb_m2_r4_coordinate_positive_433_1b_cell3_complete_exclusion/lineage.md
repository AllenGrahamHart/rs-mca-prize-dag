# Lineage

- The cell began as a 1,680-case principal outside ledger plus a separate
  product-rank-drop branch.
- Parallel `DE` deletions paid 720 cases; six matching blocks paid `xi=3`;
  exact endpoint and finite-source arguments paid `xi=6` and `xi=5`.
- The final `xi=4` slice closed by a signed outside-role involution, not by a
  new elimination campaign.
- This node is a set-theoretic and rank-stratum aggregate.  It introduces no
  new mathematical hypothesis and narrows its conclusion to source role
  cell 3.
