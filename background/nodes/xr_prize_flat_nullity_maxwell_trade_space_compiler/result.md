# Result

Every surviving prize P-A flat-nullity cell, including the interior and the
no-persistent-root endpoint, now has one uniform structural output:

```text
minimal core + at least (u+v)(t-2)+e+1 trades.
```

Rank-one trades are no longer anonymous. Each owns a canonical shared circuit
of size at most `a+1` on which both received directions restrict to the kernel
space. The remaining P-A problem is therefore a census/payment for these
local-tangent circuit owners and the separate trade-rank-at-least-two stratum.
