# Audit

The verifier pins all files, reconstructs the exact thirty-case product,
checks six cofactors per row and the aggregate degree histogram, and requires
the exact guard-only set. Hostile mutations remove a case or cofactor and
alter the guard-only set and histogram.
