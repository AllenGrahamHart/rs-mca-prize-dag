# Audit

Date: 2026-07-18.

## Convention audit

The C36' set deletes the unique zero shift. The cited energy papers use the
ordinary multiplicative energy of the full shifted set. Zero-product pairs
must therefore be removed before comparing constants. Their multiplicity is
exactly `2n-1`, not `2n`, because `(0,0)` lies in both coordinate-zero lists.

## Consumer-backward audit

Subtracting `(2n-1)^2` from `(ZN2)` lands exactly on the proved `145/4`
zero-deleted endpoint. No critical premise is silently supplied. The strict
official inequality in `(ZN5)` uses primality to rule out `p=n^2`.

## Replay scope

The verifier checks `(ZN1)` in four finite subgroup fixtures, all official
threshold expansions and the rational `39.24` floor, a mutation of the zero
multiplicity, and the DAG dependencies. It uses no Modal resource.
