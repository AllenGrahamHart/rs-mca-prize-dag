# Audit

- The exact computation covers `xi=0,2`; only the identical positive-copy
  transport supplies `xi=1`.
- Matching 8 is pinned as `((0,3),(1,5),(2,4))`.
- The degree-eight eliminant is cleared before reduction. Both quadratic
  leading-coefficient drops force the target-free cut to vanish and hence
  enter the candidate-root union.
- Norm numerator, norm denominator, and every rational inversion numerator
  and denominator are included in that union.
- Every candidate root reaches a guard, no-lift, or directly replayed finite
  terminal. No free fiber is accepted as excluded.
- An `f=0` row is recorded as a target boundary, never as an ordinary
  contradiction.
- Positive `DE` at matching 13 is not claimed.
