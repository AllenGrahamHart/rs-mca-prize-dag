# Frontier

The theorem pays `(xi,pairing)=(0,8),(1,8),(2,8)`. The parallel-`DE`
matching involution separately transports `(2,8)` to `(2,13)`, but it does
not pay `(0,13)` or `(1,13)`. Those positive-`DE` matching-13 cases are the
next exact target in this orbit.
