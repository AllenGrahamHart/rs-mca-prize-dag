# Result

Both surviving positive-valuation strata `(nu,deg H)=(1,2),(2,1)` are empty
on every official `m=4,h=3` row. Their tangent multiplicities would all be
at most three across at most three roots, forcing `p<=9`.
