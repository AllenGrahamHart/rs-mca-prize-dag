# Claim contract

| field | value |
|---|---|
| claim | The two positive-valuation `m=4,h=3` strata are empty. |
| status | PROVED |
| exact input | The tangent-radical reduction and Euler quotient identity. |
| exact output | Repeated tangent multiplicity `e` equals `ord(P)` for a cubic `P`, forcing `p<=9`. |
| excluded scope | `nu=0`, `h=2`, wider `m`, and wider exchanges. |
| consumer | `l1_mixed_petal_amplification` |
