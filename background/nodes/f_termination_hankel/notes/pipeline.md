# f_termination_hankel — the pipeline (highest leverage: +4, discharges all of Conjecture F)

## The reduction (via f_support_lattice, PROVED — Theorem 4 accounting identity)
Termination of the Hankel-family descent  <=>  #closed-sets of the support
lattice generated by low-weight words in Hankel kernels is poly(n). Trivial
for fiber flats; PROVED for MDS (f_termination_mds); Hankel is the one open
case. So the target is EXACT and finite-structural, not open-ended.

## History (why evidence-first is forced)
Coset-pattern prediction (QF.14) FALSIFIED by E17: a j=5,t=3 kernel has a
weight-2 word on support {2,3} — non-coset, but SMALL and LOCAL (adjacent).
Restated live conjecture: Hankel sparse-word supports are LOW-DIAMETER, so
#closed-sets stays poly. A blind Pro proof of an unvalidated structure is a
coin-flip; the census pins the target first.

## PHASE 0 (done, this note): object validated + a subtlety found
phase0_naive_probe.py (F_17): random Hankel matrices are generically FULL
RANK -> trivial kernels; the object lives at RANK-DROP slopes (the sketch's
"tangent slopes are rank-drop singularities"). Where kernels appeared:
low weight, diameter <= 5, #closed-sets = 2 — consistent with the restated
low-diameter conjecture. CONSEQUENCE: the faithful census must CONSTRUCT
rank-drop kernels via the fiber/tangent machinery (M(Z) at tangent slopes,
per s3b_iii_3), NOT sample random Hankel matrices. Next concrete step: pin
M(Z) from the fiber code + reproduce E17 as the correctness gate.

## PHASE 1 (Modal): the structure census
Reuse E30/E35 flat+column machinery restricted to Hankel-kernel flats at
rank-drop slopes; scale to mu_32, mu_64; weights 2..4. Measure, vs n:
(a) support-diameter distribution (bounded by D(w)?), (b) max-over-flats
#closed-sets growth (poly?). Deliverable = the pinned structure conjecture
+ its falsifier check (super-poly #closed-sets or unbounded diameter kills
the descent approach for the Hankel family — decisive either way).

## PHASE 2 (GPT Pro): the proof
Self-contained brief: Hankel displacement identity (Z H - H Z^T = low rank)
+ the PROVED accounting identity (#closed-sets poly => termination) + the
census-pinned structure conjecture + the ask. Same shape as the norm-gate
packet (Pro 16/16). Success discharges the +4:
f_termination_hankel -> f_descent_termination -> f_dim_induction ->
f_primitive_case -> conj_f (all of Conjecture F).

## PHASE 0 COMPLETE — object pinned + E17 reproduced + a reframing
M(Z) PINNED (v12 extractor, verify_f17_32_m5...a384_bucket): flat P =
ker(Hankel(S, t, j)), Hankel[i][c] = S[i+c], S = length-(n-k) syndrome
window of u + Z*v; weight-2 dual word = proportional evaluation-column pair
over mu_n. E17 = n16 k8 j5 t3 F_17.
GATE PASSED (phase0_e17_reproduction.py): non-coset adjacent (diameter-1)
weight-2 supports reproduced — e.g. exponent pairs (0,1),(6,7),(12,13).
KEY REFRAMING: at generic S, weight-2 supports are UNIFORM over all cyclic
diameters (~random-flat baseline), so "low-diameter" is FALSE per-flat. BUT
generic flats carry <= 1 sparse word => TRIVIAL support lattice (#closed <= 2).
The termination quantity is MAX-over-flats #closed-sets, set by RARE
structured (coset/degenerate) flats. New Phase-1 target: does max #closed-sets
grow POLY in n? Conjecture (to test): the max is achieved by coset-structured
flats (poly lattice); non-coset words are isolated (O(1) each), so they never
build a large lattice. If so the Pro target becomes: bound max #closed-sets by
the coset-lattice size + O(1) isolated words.

## ROUND 5 (Pro's critique of v4 — adjudicated, largely CORRECT)
My referee verdict on the five gaps:
1. f_many_sparse_structure strength: SUBSTANTIALLY CORRECT. The node's PROVED
   is a legitimate modus-ponens over its four green predicates, and the
   weight-2 branch now has a CONSTANT threshold (>=4 disjoint matching, the
   E35 restatement — Pro's quadratic B(j)=j+(j-1)^2 citation is the OLDER
   f_matching_composition note, whose own header admits the linear seed is
   open). BUT the packet's HIGHER-WEIGHT branch is "transported to the Face-4
   configuration machinery" — a WALL — i.e. routed, not proved. v4's
   hereditary use of the node overreached its real strength. CONTENT-AUDIT
   FLAG SHARPENED to this exact seam.
2. Paid class too narrow: CORRECT. E35's own ledger corrections force the
   projective form X^e g(X^M); the roadmap taxonomy is tangent + Luroth/
   rational-pullback lattice + extension. v4's mult+dihedral list was stale.
3. Local few atoms =/=> poly reachable closures: CORRECT, and QF.12's Remark
   4.7 says so verbatim ("neither bounds #L itself — see Non-claims"). My v4
   contrapositive chain was a logical overreach — the missing piece is the
   HEREDITARY closure-entropy theorem. This is the true core.
4. Moment-clean leaves separate: AGREED (already flagged in v4); Pro adds the
   monic-slice/affine pinned-value seam — correct per QF.12 Lemma 5.
5. Dihedral scale recursion missing: CORRECT. f_scale_recursion is
   multiplicative-only; the y = X + 1/X quotient REDUCTION at the Hankel
   instance is a separate needed lemma (dihedral_quotient_stratum classifies,
   does not reduce).

## NEW PROVED LEMMA (Pro round 5, verified 1800/1800):
DESCENT STABILITY: descending through a forced divisor ell_A transforms the
syndrome by convolution S^A_m = sum_r e_r S_{m+r}, and M_S(ell_A g) =
M_{S^A} g — the child of a Hankel-kernel flat IS a Hankel-kernel flat (t
unchanged, j -> j-|A|). Recursion stays inside the family.
(verify_descent_stability.py)

## THE v5 DECOMPOSITION (adopted from Pro; the node's internal structure):
L1 stability (PROVED above); L2 paid Luroth-quotient reduction (subsumes the
missing dihedral reduction); L3 HEREDITARY primitive closure-entropy bound
(the true core: super-poly reachable saturated closures force a descendant
with abundant independent atoms -> paid); L4 moment-clean Hankel leaves (or
the pinned-value variant). Pro's break-attempt verdict: no surviving
counterexample class after saturation — the known escapes all collapse or
broaden the paid ledger.
