# Rank-eleven rank-two-triple shortening and transversality

- **status:** PROVED
- **scope:** the genuine rank-two horn of the base-free `2 x 5` factor branch

Let `T` be the three-coordinate output of the balanced clone/line theorem.
Thus the evaluation map on the residual five-space `B` has rank exactly two
and the residual classes vanishing on `T` carry at least

```text
M_T=388650911452
```

first-owned slopes. Its common kernel

```text
H=ker(ev_T:B->F^3)
```

has dimension three. With the squarefree degree-three locator `L_T`, exact
division gives

```text
H=L_T bar(H),        dim bar(H)=3.                      (RT1)
```

The correction space `V=span(PH)` has dimension five or six. Indeed
`dim V<=6`, while `dim V<=4` would cap the complete bucket by
`R_4=63397365764<M_T`.

Common-core cancellation transports the bucket reversibly to

```text
(n',K',m')=(2097149,1048573,1116045),                  (RT2)
bar(V)=span(P bar(H)),       dim bar(V) in {5,6},
```

preserving its slopes, first-match ownership, and pair noncontainment. Every
transported residual class retains at least `37733` common-zero coordinates.

Apply the proved support-local transversality compiler to the shortened
bucket. If `dim bar(V)=5`, some selected slope and `b in bar(V)` satisfy

```text
|{x in S_gamma:r'_1(x)!=b(x)}| <= 9.                   (RT3)
```

If `dim bar(V)=6`, the corresponding bound is

```text
|{x in S_gamma:r'_1(x)!=b(x)}| <= 294.                 (RT4)
```

## Nonclaim

The low-exception record is not paid here and is not identified with a
split pencil. This theorem does not aggregate different rank-two triples or
close the active-clone horn.
