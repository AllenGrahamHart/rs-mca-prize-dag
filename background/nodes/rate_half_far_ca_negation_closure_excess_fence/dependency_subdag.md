# Dependency sub-DAG

```text
Lagrange/residue identities + e22 orbit-invariant carrier algebra
                 |
                 v
rate_half_far_ca_negation_closure_excess_fence [PROVED]
                 |
                 : evidence / route restriction
                 v
rate_half_band_crossing_location               [TARGET]
```

No requirement edge in. The outgoing edge restricts the counting routes
(first-moment arguments on structured domains at small rho) and supplies
the `T_sym` carrier mode of the (R36-D) decomposition; it discharges no
critical requirement. Siblings sharing the `2r > R` inequality:
`rate_half_type2_ledger_vacuous_by_sign_fence` and the pigeonhole cap
inside `rate_half_far_ca_crossing_offset_value_ledger`. The shape fence on
the proved `B_ca^far(n-r) <= r+1` bound is recorded on
`rate_half_ca_hankel_split_pencil_equivalence`.
