# Provenance

Found and proved by pilot `r36_hrlow` (round 36, bank 3; Round-36 R-HRLOW
addendum, `critical/nodes/rate_half_band_crossing_location/statement.md:
4457-4474`; mechanism text in `notes/pilots_20260811/r36_hrlow/f4_close.py`,
banked scan rows in `.../f4_results.txt`). Drafted as a standalone package
by pilot `r37_mint_drafts` (round 37, bank 2, package 5 of 10), which made
three corrections to the source of record: the general covering law
`C(m-off, r/2-off)` (the banked `C(m-1, r/2-1)` is its `off = 1` face), the
locator-vs-slope distinction at H3 (330 vs 329), and the shape scoping of
the proved `B_ca^far(n-r) <= r+1` bound. Coordinator spot-replay at
drafting: PASS, including the 1158-locator collapse and the
bad-set = covering-set identity.

Wired 2026-08-11 by the coordinator in the task-#41 mint session. Wiring
updates: the draft's rho >= 3 symmetric-T gap marked CLOSED (rounds 37-38);
the Statement-U cap references repointed at the value ledger after the
round-37 refutation; and the wiring audit added the generic-qualifier
exhibit at H4 over q = 1009. No Modal computation or external library was
used.
