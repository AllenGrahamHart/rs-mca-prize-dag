# Claim contract

- **claim:** the high-core absorption and complementary capped-core peeling
  branches exhaust every recursively selected line and pay
  `130222<=e<=130225`.
- **dependencies:** the exact weighted prefix, total-core synchronization,
  outside zero bound, line cap, lower-aware convex exchange, and distinct
  inside-core intersection bound.
- **quantifiers:** all official Mersenne full-lift instances covered by the
  parent route at the four printed supports.
- **not claimed:** safety or unsafety at `e>=130226`, optimality of
  `b_abs=65450`, or closure of the global MCA target.
- **falsifier:** an explanation outside the high-core line above the printed
  threshold, a feasible capped core vector exceeding `(CD4)`, or an exact
  endpoint replay mismatch.
