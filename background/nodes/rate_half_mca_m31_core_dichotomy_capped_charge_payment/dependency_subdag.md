# Dependency sub-DAG

```text
lower-aware joint charge ----------------+
                                         +--> core dichotomy --> MCA router
weighted-prefix core absorption theorem -+
```

The node adds an exhaustive high/low actual-core split to the existing
recursive line bank.  It supplies evidence to the two rate-half MCA routers
without changing a critical-node status.
