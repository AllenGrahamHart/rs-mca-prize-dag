# Frontier

The maximum-weight associate problem is now concentrated in cofactor `2`.
For that branch the universal deficit only gives `D<7.539`, and the same
sharp entropy extremum reaches approximately `31.77` per collision, above
the Schinzel half-threshold `64 log(phi)`.

The next route must therefore do at least one of the following:

1. use a profile-specific invariant to improve the cofactor-`2` log body;
2. strengthen the height lower bound for units in the conductor-256 real
   cyclotomic field; or
3. consume the exact sparse product and inverse equations in the contracted
   exponent body.

No computation is authorized merely to recount the three collapsed
high-cofactor families.
