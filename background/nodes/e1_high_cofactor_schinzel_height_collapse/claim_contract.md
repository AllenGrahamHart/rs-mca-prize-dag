# Claim contract

## Consumed content

- The common-prime router supplies `beta=u alpha`, the 64-coordinate product
  identity, and `p>2^255`.
- The full-unit theorem supplies a totally real representative modulo the
  `256` roots of unity.
- The character preflight supplies hash-pinned directed logarithm arithmetic
  and independently certifies the worst-cofactor deficit formula.
- Hoehn--Skoruppa/Schinzel supplies the unconditional height lower bound for
  nontrivial totally real algebraic integers.

## New proved content

1. The 64-coordinate entropy implication `D<6.845 => L1(log z)<30.645`.
2. Every same-cofactor associate unit at `m=4,8,16` is torsion.
3. Those three branches contribute at most three shift/sign orbits in total.

## Guards

- The theorem is pairwise inside one row, quotient root, and cofactor.
- It bounds associate orbits; it does not prove a collision exists or is
  absent.
- It does not merge different roots or cofactors.
- It makes no assertion for cofactor `2`.
- It does not pay any lower-weight profile.
