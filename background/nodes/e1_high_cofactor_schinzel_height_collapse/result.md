# Result

`PROVED`. At one fixed prize row and primitive quotient root, each of the
profile-`(3,6,S=18)` cofactor branches `4`, `8`, and `16` contains at most one
negacyclic shift/sign orbit. Together they contribute at most three orbits.

The cofactor-`2` associate count and every lower-profile charge remain open.
