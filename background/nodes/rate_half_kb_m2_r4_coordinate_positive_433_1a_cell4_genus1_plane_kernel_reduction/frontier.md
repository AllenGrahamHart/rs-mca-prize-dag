# Frontier

Do not rerun the global target-free basis or extend its timeout.  Continue in
two exact pieces:

1. Factor the univariate denominator/common/leading scales and decide their
   intersections in the original guarded common ideal.
2. On the main genus-one chart, decompose the two signed-pair equations first.
   Then append the colored `BE` equations componentwise.  Use
   `B_1(W)=b10(1-W)` and pseudo-reduce by the 21-term plane relation after each
   elimination step.

A unit component ledger closes `[4,7]`; a proper finite algebra must be tested
for deployed-field points and every omitted source/target guard.
