# Dependency sub-DAG

```text
common_kernel_uniqueness --------------------+
                                              |
remaining_common_curve_profile --------------+--> cell4_genus1_plane_kernel_reduction
                                              |                 |
common_root_sign_symmetry_quotient -----------+                 +--ev--> rate_half_band_closure
```

All incoming edges are `req`; the outgoing edge is evidence only because the
cell orbit remains open.
