# Audit

1. The lex scout runs two independent variable orders for each of the five
   remaining common representatives.  Cell 4 is selected because its best
   exact basis has seven short elements; no basis-size comparison is used as
   an emptiness claim.
2. Every remaining first eliminant is reciprocal, but this node claims genus
   only for cell 4, where the second cover is explicitly factored.
3. The second-discriminant denominator is retained as four squared factors.
   The numerator's repeated `(s+4)^2` is removed before applying the genus
   formula; calling the raw degree-six equation genus two would be wrong.
4. Kernel denominator clearing, both common-scale divisions, and every plane
   pseudo-division are exact FLINT operations.  The `B1` opposition is checked
   before and after projection.
5. The global target-free basis reached a bounded timeout.  It is not treated
   as unit, nonunit, emptiness, or survival.

Mutation controls alter reciprocal reconstruction, second-cover
square-freeness, kernel opposition, and timeout status; each must fail.
