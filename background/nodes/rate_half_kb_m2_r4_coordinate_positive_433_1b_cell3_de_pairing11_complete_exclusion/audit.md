# Audit

- The source census is the Cartesian product of four source signs, both
  `sigma_c` values, and `xi in {0,2}`: 16 rows.
- Every row is `COMPLETE`, has quadratic `P_b,P_c`, and has matching direct
  six-basis and tower norms.
- The candidate parameter set is exactly the union of all norm and
  exceptional-guard roots.
- All 168 source points are explicitly listed and unique within each row.
- The common-`f` roots are intersected directly; the monic quartic source
  relation is solved over the deployed field without a discarded leading
  case.
- All 16 boundaries have `f=0`, fail exactly `nonzero_5`, and cover both
  `sigma_o` lanes for their fixed `sigma_c`.
- All 64 nonboundary final-pair evaluations are nonzero.
- Witness and unresolved ledgers are empty in every row.
- The theorem counts 32 computed and 16 transported raw atlas cases; source
  rows are not confused with raw target-lane cases.
- Primary and independent verifiers pin the compiler and census hashes.
