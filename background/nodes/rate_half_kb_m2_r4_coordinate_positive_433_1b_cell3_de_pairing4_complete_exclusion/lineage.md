# Lineage

- The pairing-3 FLINT function-field backend supplied the exact
  six-dimensional arithmetic and exceptional-root replay.
- Matching 4 replaces the `(second_de,ef)` quadratic with
  `(second_de,bf)` and reduces the missing relation modulo the
  `u=df` quadratic.
- The resulting degree-eight `f` eliminant remains practical: all final
  source rows complete below 180 seconds.
- Lane-independent norm work is shared across all four target lanes, while
  the third paired equation is still checked lane-by-lane.
- Exact parallel-edge transport adds the second positive `DE` deletion.
