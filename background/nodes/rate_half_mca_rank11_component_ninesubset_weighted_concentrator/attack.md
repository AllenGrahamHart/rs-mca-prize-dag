# Attack

Consume `W_B` before deduplicating records. For each typed chart, bound the
number of component extensions through `B`, including owner or kernel
multiplicity, and compare in the same incidence unit.
