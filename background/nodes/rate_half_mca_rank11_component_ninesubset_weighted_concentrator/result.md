# Result

The selector retains at least `5868470021012020` component extensions even
at its shortest endpoint. The former `2578110` figure is only the result of
dividing this weight by the largest possible per-record extension count.
