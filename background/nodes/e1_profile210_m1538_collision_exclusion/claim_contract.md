# Claim contract

## Consumed content

- The split-prime router supplies profile `(2,10,S=18)`, cofactor `1538`,
  local valuation one, and the prize field floor.
- The sharp product-window theorem supplies the universal two-moment
  extremum; this node certifies the new `V=6` threshold.
- The exact variance-two Lucas identity supplies the one-lag norm formula.

## New proved content

1. Cofactor `1538` forces `V<=4`.
2. The `V=4` and valuation-one branch reduces to 640 finite-field hits and
   five Galois types.
3. All five exact norm quotients are below the prize interval.
4. Profile `(2,10)` has at most 266 ideal orbits and 68,096 oriented vectors.

## Guards

- The finite-field screen is necessary only after the sharp moment reduction;
  it does not enumerate coefficient supports.
- A zero of the squared-magnitude polynomial is used only as a necessary
  consequence of `769` dividing the norm.
- All five necessary Galois types are retained through the exact norm step.
- No primality heuristic is used; interval exclusion alone closes the branch.
- Cofactors `514` and `1028` remain open.

