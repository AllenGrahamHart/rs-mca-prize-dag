# E1 profile-(2,10) cofactor-1538 collision exclusion

- **status:** PROVED
- **closure:** sharp moment envelope, finite-field orbit screen, and exact real-cyclotomic norms
- **scope:** binding prize rate-`1/8` row, profile `(2,10,S=18)`

No profile-`(2,10,S=18)` collision on a prize-envelope row has cofactor

```text
m=1538=2*769.
```

The sharp fixed-mean/fixed-variance product envelope forces `V<=4`.
Variance zero and two are impossible. At `V=4`, the autocorrelation energy is
two, so exactly two positive-half autocorrelations are signed units.
Divisibility by `769`, together with local valuation one, reduces the signed
lag pairs to five Galois types. Their five exact real-cyclotomic norms are all
divisible by `1538`, but every quotient `Norm/1538` is strictly below the
prize field floor.

Thus the profile-`(2,10)` ideal envelope sharpens from

```text
394 to 266 shift/sign orbits,
100864 to 68096 oriented vectors.
```

Only the split cofactors `514` and `1028`, both above `257`, remain.

