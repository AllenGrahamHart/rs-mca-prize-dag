# Frontier

The split-prime occupancy frontier now lies entirely above `257`:

```text
m=514,       local valuation 1,
m=1028,      local valuation 2.
```

The generic sharp moment envelope leaves `V<=34` for `514` and `V<=12` for
`1028`. The next highest-value attack is therefore the `1028` low-energy
branch. It has only energies `E=2,...,6` after variance zero and two are
removed, and the same finite-field/resultant route should be attempted before
any profile-support census.

