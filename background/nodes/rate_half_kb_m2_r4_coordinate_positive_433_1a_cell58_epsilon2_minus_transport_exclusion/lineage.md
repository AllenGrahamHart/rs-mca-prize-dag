# Lineage

- The common Vieta compiler proves that cells `5,8` differ only by the two
  identical `AB+` roles.
- Universal target elimination makes source-record relabeling explicit for
  every outside case.
- The complete cell-5 theorem excludes the seed row `(-1,-1)` at every
  deployed parameter.
- This node takes the exact orbit closure of that seed under the two proved
  transports.
