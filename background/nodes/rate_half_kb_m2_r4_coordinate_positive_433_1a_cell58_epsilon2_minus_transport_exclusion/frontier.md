# Frontier

Do not recompute any of the four excluded rows.  Cells `5,8` now have one
genuinely different common-root orbit, represented by
`(epsilon_1,epsilon_2)=(-1,+1)`.  Compile that representative first and use
the same first-sign and duplicate-role transports if it is excluded.

No analogous transport to another role-cell orbit is currently proved.
