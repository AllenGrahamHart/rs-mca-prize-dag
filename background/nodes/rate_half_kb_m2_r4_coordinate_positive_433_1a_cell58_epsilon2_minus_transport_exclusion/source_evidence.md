# Source evidence

- `rate_half_kb_positive_433_1a_common_vieta_compiler.py` defines the exact
  fifteen matching cells and duplicate-`AB+` action.
- `check_rate_half_kb_positive_433_1a_cell58_epsilon2_minus_transport.py`
  independently reconstructs cells `5,8`, verifies the duplicate-role
  equality, checks every common label/product/sum row, checks all ten common
  source guards, and verifies the full eight-column common and arbitrary
  outside matrix identities under `B_1 -> -B_1`.
- The complete-sign-row parent supplies the seed exclusion uniformly over
  the deployed field.
