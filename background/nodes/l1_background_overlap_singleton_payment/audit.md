# Audit - L1 background-overlap singleton payment

## Checked axes

1. Background agreement sets may vary between codewords; only their forced
   intersection is used.
2. The exact background capacity is `b=ell-g`, not `ell`.
3. The root count uses the fixed petal support plus common background points.
4. A possibly negative inclusion-exclusion lower bound is never treated as a
   root count: under `(BO2)` it is strictly larger than `N-h>=0`.
5. The strict boundary is retained and has an exact populated chart.
6. The aggregate sums defect degrees but not background subsets.
7. Non-intrinsic chart multiplicity remains open.

## Remaining attack

The bounded-polarity sub-Johnson tail lies in
`a^2/N<=c<=2a-ell-g`, with `a,c` and the previous excess parameters
unbounded. Attack this balanced strip through source-coupled incidence or a
natural-scale owner.
