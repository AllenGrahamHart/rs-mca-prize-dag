# Result

Open exact certificate leaf.

The required class-group information is now only the nonprincipality of two
explicit norm-`257^2` ideals. Proving both promotes the conditional class
orbit and every downstream profile-018 payment without reconstructing the
complete published 64-coordinate ledger.
