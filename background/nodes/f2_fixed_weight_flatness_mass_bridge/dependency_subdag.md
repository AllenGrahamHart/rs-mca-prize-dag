# Dependency sub-DAG

```text
f2_weighted_kernel_collision_floor [PROVED]
  --req--> f2_fixed_weight_flatness_mass_bridge [PROVED]
  --ev---> f2_conditional_close [TARGET]
```

The remaining leaf is `(FW-1)` for the explicit weighted odd-prefix maps
on the five signed generating F2 types. The theorem allows either all
weights or a central band with a `2^(S/2+o(S))` omitted tail.
