# Result

The F2 full-cube mass no longer requires a full-cube max-fiber theorem.
A discrete mean-plus-one max-fiber estimate on each fixed-weight slice,
or only on a sufficiently wide central band, is enough. This matches the
normalization used by the upstream split-locator flatness program while
preserving the weighted-map and owner seams explicitly.
