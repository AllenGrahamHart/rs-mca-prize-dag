# Lineage

The 20-cell invariant router exposed only two horizontal variables per
matching.  Eliminating them before specializing the common-`K` ledger turns
the apparently large saturation into 300 unit tests in a fixed rank-twelve
base algebra, closing both unconstrained cells.
