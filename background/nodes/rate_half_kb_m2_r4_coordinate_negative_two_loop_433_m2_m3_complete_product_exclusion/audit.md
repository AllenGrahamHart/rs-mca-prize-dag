# Audit

1. `M,b` remain geometric base-algebra variables; no false base-field
   rationality assumption is made.
2. `D,E,F` remain independent horizontal variables and are eliminated only
   through the exact products `(KB43V-2)`.
3. All five forced types, both signs, both cells, and all fifteen matchings
   are present.
4. Cleared equations are necessary on the guarded nonzero charts, so a unit
   obstruction proves deletion without a saturation omission.
5. Primary projection degeneracies are rerouted before any claim is made.
6. The custom quotient multiplication is checked by both norm/gcd and full
   multiplication-matrix rank.
7. Alternate audits are sharded only for runtime; their union is exactly all
   300 cases.
8. The conclusion excludes only `M2/M3`.
