# Frontier

Do not run full interpolation or q equations on `M2/M3`.  Build the outside
forced-mate/invariance compiler for the exact constrained ledgers
`X2,N1,L1`; these are now the only surviving `(4,3,3)` common-`K` rows.
