# Result

Every clean high-distance quotient incidence is simple in both variables and
has an exact corrected-square velocity with a degree-`r` smooth-domain
numerator.
Any endpoint classifier or proof must retain these first-jet weights rather
than only the four resultant multiplicities.
