# Audit - XR nongeneric-explanation Plotkin width

## Checked axes

1. Explanation supports are canonically truncated to equal size `A`.
2. The truncation map is injective because `A>K`.
3. The intersection cap is `K-1=A-(h+1)`, preserving the crucial `+1`.
4. The binary distance is exactly at least `2(h+1)`.
5. Strict Plotkin and equality Rankin regimes are kept separate.
6. Same-pattern puncturing covers logarithmic excess above equality.
7. Every live descent transition drops length by at least `h+1`.
8. The level branching factors are `8H`, `4`, and `2`.
9. In a fixed logarithmic window the post-root factors are `8`, `2`, and `1`.
10. The tangent charge excludes generic-chart mismatch slopes.
11. No field-size or support-subset enumeration factor is introduced.

## Remaining attack

Aggregate distinct slopes across generic canonical full-external-zero charts,
and bound the slope fiber entering each low-core explanation before every
fixed logarithmic terminal window. The theorem removes near-terminal
explanation breadth as a source of super-polynomial loss but does not close
either currency.
