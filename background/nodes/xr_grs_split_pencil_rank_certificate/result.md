# Result

The common high-core base and low-core one-loop obstruction now have one
finite algebraic certificate. A genuine support/color family forces an extra
kernel direction in the block parity matrix `M_F`, so its rank is at most
`2N-9`; full rank modulo the unavoidable cubic-pair kernel excludes it. The
second component of the genuine extra class is `q notin K`; arbitrary rank
defect without this projection property is only a relaxation of the census.

The associated five-set coloring restricts on every six-set to a
fractional-linear function of the omitted coordinate. Equal adjacent colors
therefore close to an entire monochromatic six-set. This is the exact
Plucker coherence omitted by arbitrary colored-hypergraph models.

The unresolved theorem is now sharp: classify or bound rich block families
with an extra kernel class whose second projection is noncubic. Bounding all
extra-kernel families is a valid but potentially coarser route. Generic-rank
or distance-only MDS packing does not supply this universal classification.

The complete Modal verifier fleet passed `163/163` in
`ap-R7lysAgwGebSJ7KthaYjvs`. All five integration gates passed in
`ap-5K1CFhDDP8cK7At5Sz1PRC`.
