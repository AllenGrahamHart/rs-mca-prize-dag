# Result

The finite packets left by the base-field conic router are all impossible.
No packet replay is needed. The live `m=8,h=7` order-one curve is now exactly
the non-base-field parameter branch `t notin F_p`.

L1 remains open on that branch and its other declared chambers.
