# Result

The F2 weighted odd-prefix map is no longer a separate analytic object.
Every full-cube syndrome fiber embeds without loss into a standard central
split-locator prefix fiber on the doubled cyclic domain. This gives a clean
joint target with upstream `prob:capfr1-master-flatness`; only its theorem,
parameter scope, and common-divisor/first-match deployment remain open.
The quotient-periodic bucket is provably empty on every selector image.
