# Result

**PROVED.** The deployed-field positive near-aligned moving-moving `xi=a`
mixed allocation `c,d->{1/2,1/d}` is empty. This closes affine positive chart
12 of 18, completes the `xi=a` orbit, and supplies one evidence edge to
`rate_half_band_closure`.
