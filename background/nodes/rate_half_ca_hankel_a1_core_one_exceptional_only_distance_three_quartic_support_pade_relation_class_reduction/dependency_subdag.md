# Dependency sub-DAG

```text
bounded_error_pade_circuit_reduction [PROVED]
             |
             +--req--> pade_relation_class_reduction [PROVED]
                                      |
                                      +--ev--> rate_half_band_closure [UNPROVED]
```

The new leaf is a uniform fixed-factor/class-size bound, with exact target
sizes `172409` and `2127`.
