# Lineage

- The cell-4 signed-pair projection isolated the exceptional chart `L=M=0`.
- The projection decomposition reduced it to one irreducible curve `H` plus
  an already-excluded zero-dimensional residual.
- The colored cross-variable elimination reduced the generic `H` chart to a
  finite Frobenius ledger.
- Direct content and subresultant-scale replays close every chart omitted by
  the generic reductions.
- Next: the main `L!=0` component of the same signed-pair projection.
