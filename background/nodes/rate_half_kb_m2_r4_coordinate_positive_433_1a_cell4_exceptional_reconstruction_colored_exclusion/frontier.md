# Frontier

Do not return to `L=M=0`, the curve `H`, its content fibers, or any of the six
subresultant scale charts.  On the remaining `L!=0` component, work in the
function field of the irreducible projection `F(w0,b,t)=0`, reconstruct
`w1=-tM/L`, and append the colored equations before projecting.  Preserve the
plane, source-label, and denominator guards at every reduction.
