# Result

PROVED.  The entire exceptional reconstruction chart `L=M=0` is empty for
the necessary colored signed family over the deployed field.  The generic
`H` chart, its two admissible removed content fibers, and all six
pseudo-subresultant scale loci are exhausted exactly.  Every possible lift is
absent or violates an original denominator guard.

OPEN: the main `L!=0` component of `F(w0,b,t)=0`, and therefore cell orbit
`[4,7]` and all downstream Prize claims.
