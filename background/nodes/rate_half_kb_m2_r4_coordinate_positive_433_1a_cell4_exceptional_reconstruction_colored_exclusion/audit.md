# Audit

1. The `w2` resultant is necessary and may contain extra squared solutions;
   the argument uses only exclusion.
2. The factor `x^4` is canceled only after the symbolic signed-pair proof that
   `x!=0` on an actual guarded packet.
3. The degree-15 Frobenius gcd, not a partial factorization, supplies the
   complete deployed `t` census.
4. The two admissible coefficient-content roots are replayed before content
   division and have no deployed `H` point.
5. All six scales used by `H` reduction and the `b` subresultant chain are
   reconstructed, projected by Frobenius, and replayed from `P,L,M,F`.
6. `D_0=0` and `D_1=0` are pre-existing denominator guards, not newly imposed
   saturation factors.
7. The residual away from `H` is owned by the projection dependency.
8. The main `L!=0` component is explicitly retained.
