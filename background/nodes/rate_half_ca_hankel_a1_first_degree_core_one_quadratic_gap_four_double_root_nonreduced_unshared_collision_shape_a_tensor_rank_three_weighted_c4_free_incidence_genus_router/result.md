# Result

`PROVED`. Rank-three Shape A is now an exact weighted `C_4`-free bipartite
incidence problem between the two rational plane coefficient images. The
weights satisfy both plane-genus collision budgets, both image sets have at
least ten vertices, every domain vertex has at least two neighbours, and at
least `e+7` parameter branches lie on at least two zero-deficit vertices.

The critical target remains open. Its rank-three branch no longer needs to
track the original biform coefficients independently of this graph.
