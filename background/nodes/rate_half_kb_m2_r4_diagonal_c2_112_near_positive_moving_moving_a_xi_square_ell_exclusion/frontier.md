# Frontier

Seven affine positive charts remain, all moving-moving: the `xi=a` mixed
allocation and all three allocations for each of `xi=tau(a)` and the other
reciprocal orbit. The projective `w=0` boundary and negative factor loci also
remain.
