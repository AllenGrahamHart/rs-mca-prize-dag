# Result

**PROVED.** The deployed-field positive near-aligned moving-moving `xi=a`
square allocation `c->1/d,d->1/2` is empty. This closes affine positive chart
11 of 18 and supplies one evidence edge to `rate_half_band_closure`.
