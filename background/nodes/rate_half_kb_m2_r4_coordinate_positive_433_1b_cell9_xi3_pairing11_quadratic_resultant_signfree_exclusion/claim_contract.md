# Claim Contract

## In scope

- Cell-9 direct role `xi=3` at matching `11`.
- All four source signs, both `sigma_c` lanes, both `sigma_o` target
  lanes, and six exact tower charts.
- Exact transport to the printed four-label orbit.

## Out of scope

- The separate aggregate composition of all 105 cell-9 labels; cell `10`;
  K3, LIST, MCA, or Prize closure.

## Promotion rule

`PROVED` certifies only `(KBP1B9-XI3-P11)` and its proved orbit transport.
