# Claim contract

## Consumed content

- The parity/trace router supplies exact local parity multiplicity two.
- The conductor-256 moment dictionary supplies the three moments in `(7)` of
  `proof.md`.
- The prize field floor supplies `p>=p_min`.

## New proved content

1. Exact parity multiplicity forces an odd number of odd lags.
2. The parity-split signed relation count gives `M_3<=510`.
3. A global cubic Hermite majorant puts `(12;12,0,0)` below `514*p_min`.
4. Eleven cofactor-`514` magnitude profiles remain, all at energies 5 through
   11.

## Guards

- The relation count is an upper bound; it does not assert simultaneous
  attainability of its even and mixed-parity terms.
- No coefficient realization, fixed-root equation, or support enumeration is
  assumed.
- The prior energy-twelve autocorrelation witness remains a valid route cut,
  but this theorem now excludes its whole magnitude profile by norm.
