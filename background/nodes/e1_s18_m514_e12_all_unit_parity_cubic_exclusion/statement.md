# E1 square-mass-18 cofactor-514 energy-twelve all-unit exclusion

- **status:** PROVED
- **closure:** parity-split relation cap and cubic Hermite norm majorant
- **scope:** every conductor-256 square-mass-18 collision of cofactor 514

No cofactor-`514` collision on a prize-envelope row can have positive-half
autocorrelation magnitude profile

```text
(E;n_1,n_2,n_3)=(12;12,0,0).
```

Local multiplicity two forces an odd number of the twelve positive lags to be
odd. Splitting the oriented lag set by parity then gives the exact universal
relation bound `M_3<=510`. The cubic Hermite interpolant to `log y` with
double contacts at `17` and `40` is a global majorant and places the norm
strictly below `514*p_min`.

Consequently the profile-`(0,18)` cofactor-`514` frontier contracts from
twelve magnitude profiles to eleven, and its live energy window is now
`E=5,...,11`.
