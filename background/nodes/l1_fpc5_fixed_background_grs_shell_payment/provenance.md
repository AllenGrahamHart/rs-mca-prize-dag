# Provenance

The proof composes the exact fixed-background incidence identity with the
FPC5 GRS syndrome-shell specialization. The relation between `J_fix` and
`J_bg` is a direct substitution in the existing FPC5 parameter dictionary.
