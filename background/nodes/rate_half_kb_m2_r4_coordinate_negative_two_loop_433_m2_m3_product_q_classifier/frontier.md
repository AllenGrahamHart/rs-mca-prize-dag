# Frontier

The `(4,3,3)` common-`K` algebra is complete.  Carry only the five finite
ledgers `X2,N1,L1,M2,M3`, with aggregate cap 48, into construction of the
`eta` record and six complementary `L^c` records.  Apply the paired-product
involution before the remaining full q equations.  Do not enumerate the
original 240 labeled assignments or reopen the four deleted cells.
