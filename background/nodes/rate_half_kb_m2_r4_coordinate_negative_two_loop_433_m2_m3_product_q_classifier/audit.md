# Audit

1. The product equations come from a homogeneous maximal-cofactor vector;
   no affine Mobius chart boundary is discarded.
2. The primary verifier checks the integral eliminants, every collision
   branch in `(7)`, and the converse ideal reductions.
3. The independent verifier instead saturates by the minimal actual guard
   product.  In both cells its only univariate basis row is exactly `P_6`.
4. The saturation does not divide by `c`, `c^2-1`, or `b^2-c^2`; those
   remaining original guards are applied to the finite ledger afterward.
5. The quadratic has leading coefficient `4` and the `c` locator coefficient
   is `8`, both nonzero in odd characteristic.
6. The squared weld is promoted only at common `K`, where the unused `BC`
   orientation realizes its sign.  It is not a complete source facet.
7. The `F_41` examples prove geometric nonemptiness, not realization over
   the deployed KoalaBear field.
