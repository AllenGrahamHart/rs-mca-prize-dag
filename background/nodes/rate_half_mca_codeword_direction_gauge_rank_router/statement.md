# Codeword-direction gauge equivalence

- **status:** PROVED
- **closure:** field-general equivalence and affine-rank stability
- **scope repair:** the former affine-incidence bound and official rank walls
  are retracted

For any codeword `b in C`, replace

```text
(r_0,r_1,c_gamma) by (r_0,r_1-b,c_gamma-gamma b).     (G1)
```

This preserves every slope, exact and maximal agreement support, and
same-support pair containment or noncontainment.  If `r_c` and `r_b` are
the affine dimensions of the original and transformed selected explanation
families, then

```text
|r_c-r_b|<=1.                                         (G2)
```

These statements are independent of the refuted affine-span incidence
compiler and remain available to the sparse-direction puncturing nodes.

The former formula `(G3)` and all KoalaBear/Mersenne rank walls derived from
it are not part of this repaired node.

## Falsifier

A changed agreement coordinate under `(G1)`, failure of the inverse
pair-containment transport, or an affine-rank change larger than one.
