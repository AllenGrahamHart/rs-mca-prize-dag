# Proof

The pairing-3/6 orbit parent proves the four labels

```text
(0,3), (1,3), (2,3), (2,6)
```

empty. The positive-DE pairing-6 parent proves the two disjoint labels

```text
(0,6), (1,6)
```

empty. Their union is exactly the displayed six-label block. Every parent
has all four source-sign rows and all four target lanes, so the raw count is
`6*4*4=96`.

The exact parallel-DE quotient partitions these six labels into

```text
{(0,3),(1,3)}, {(2,3),(2,6)}, {(0,6),(1,6)}.
```

The prior first-pair block contains nine labels in six disjoint orbits.
Therefore the aggregate paid set has 15 labels in nine orbits. Subtracting
from the complete 105-label/60-orbit ledger leaves 90 labels in 51 orbits.
QED.
