# Result

PROVED.  Outside skeletons `Z2` and `Z3` are deleted at the complete product
gate.  Skeletons `Z0`, `Z1`, and `Z4` survive that necessary gate and move to
quotient placement and q interpolation.
