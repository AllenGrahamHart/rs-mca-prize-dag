# Frontier

For `Z0`, `Z1`, and `Z4`, reconstruct the common quadratic `A_1` and assign
the one `eta` plus six `L^c` quotient labels.  Outside loops must occupy roots
of `A_1`; only then impose the remaining q and interpolation rows.  Do not
rerun the deleted `Z2/Z3` product cells.
