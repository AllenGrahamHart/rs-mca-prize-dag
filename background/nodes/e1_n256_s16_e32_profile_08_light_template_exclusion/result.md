# Result

`PROVED`: profile `(0,8)` is impossible at `V=64`. Its zero-odd light geometry
has six affine-unit templates, and two independent exact engines each test
119,087,616 normalized signed vectors with zero profile vectors retained.

The `V=64` residual is now `(4,7)` or `(3,5,1)`.
