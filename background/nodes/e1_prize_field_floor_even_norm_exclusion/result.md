# Result

PROVED. The exact prize field floor and the unavoidable factor two in every
same-size class-difference norm exclude `S<=16` at `N=256` and `S<=4` at
`N=512`.

This removes all four `S=16` profiles from the binding prize rate-`1/8`
weighted budget. It is a row-specific improvement and does not apply to
RowC.
