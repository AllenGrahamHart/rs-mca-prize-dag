# Frontier

The prize rate-`1/8` weighted-kernel target no longer contains any `S=16`
profile. Its first live mass is `S=18`, where `(4,2)` has the largest
class-pair multiplicity. The corresponding uniform oriented-vector cap rises
from 27,520 to 66,866.

The analogous prize rate-`1/16` floor rises from `S=4` to `S=6`. RowC keeps
the old floors and must be attacked separately.
