# Claim contract

- **Claim:** the rank-nine `K'=86` row is paid and the closed prefix is
  `10..86`.
- **Finite partition:** exactly `ordinary + offsets 1..75`.
- **Ordinary custody:** the dedicated slice checker accepts the two complete
  ordinary jobs retained by the otherwise incomplete route-pilot capture.
- **Threshold split:** all offset units are retained until classified; offsets
  `43..75` are entirely raw-safe, and exactly 415,413 unsafe units in offsets
  `1..42` enter residual geometry.
- **Geometry:** only proved single, pairwise, fixed-union support-4/5,
  fixed-union support-5/6, and adjacent-support charges are used.
- **Best-single rule:** for each residual profile, use the smallest upper bound
  supplied by either no adjacent charge or one individually valid adjacent
  edge. No simultaneous composition of overlapping edges is assumed.
- **Audit bar:** primary and independent traversals agree exactly on all
  coverage and premium checks in every finite wave.
- **Nonclaim:** no row `K'>=87`, full rank-nine interval, LIST terminal, MCA
  terminal, or prize terminal is closed here.
