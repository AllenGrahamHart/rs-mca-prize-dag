# Attack

Reconstruct the ordinary/offset partition independently, mutate one offset
count or threshold classification, delete one residual carrier profile, or
replace the best valid single-edge price by an unsupported simultaneous
composition. Any surviving mutation falsifies closure.

The proof is finite and row-specific. No trend extrapolation to `K'=87` is
part of this node; that row requires a new exact frontier and component
payment.
