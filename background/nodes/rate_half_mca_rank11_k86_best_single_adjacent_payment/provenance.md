# Provenance

Derived on 2026-08-17 by extending the proved `K'=85` finite-row program. A
dedicated checker recovered the paired complete ordinary slice from an
otherwise incomplete route pilot; a paired 150-job raw wave isolated the
exact unsafe population; a paired 84-job wave proved the best-single statement
on the whole residual; and an independent Modal payment reconstructed the
positive component gap. App IDs, source hashes, archive hashes, and capture
hashes are pinned in `source_contract.json` and
`notes/PRIZE_COMPUTE_REQUESTS.md`.
