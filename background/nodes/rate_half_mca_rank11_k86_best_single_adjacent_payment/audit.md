# Audit

1. Ordinary plus offsets 1--75 is exhaustive and disjoint after the proved
   support-2/3 carrier-position constraints.
2. The ordinary slice checker verifies both complete jobs without treating the
   broader incomplete pilot as a complete batch.
3. The raw scan retains every offset unit before threshold classification.
4. Raw-safe pruning is monotone because every later valid charge only lowers
   the premium.
5. Every unsafe unit enters exhaustive carrier geometry.
6. Best-single pricing uses one valid adjacent edge at a time and assumes no
   overlapping-edge composition.
7. Primary and independent traversals agree on all lane counts and bounds.
8. The safe ceiling and component gap are recomputed with exact integers.
9. No numerical pattern beyond `K'=86` is promoted or used in this proof.
