# Proof

For each literal cell, reconstruct the generic `V!=0` branch from the pinned
atlas and select the retained degree-12 resultant factor. Substitute `s=0`
directly into that factor, both essential reconstructed rows, and every
transported named-unit factor. No unit is discarded or inferred by orbit.

Over the deployed prime, direct `slimgb` computation gives a two-element
dimension-one basis. The `F04/F07` cells share basis hash
`ad25aa76...`, and the `F05/F06` cells share `8149fcf7...`; both targets are
replayed literally. Sequential reduction of all 24 transported unit factors
reaches zero at factor 14 in every cell. Thus the localizer belongs to each
specialized ideal, and every named-open `s=0` branch is empty over the
algebraic closure, hence over the deployed extension. QED.
