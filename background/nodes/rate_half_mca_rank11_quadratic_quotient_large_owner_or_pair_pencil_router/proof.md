# Proof

The quadratic quotient population router gives at least 520 first-owned
quotient types, each with at least 29 records. The degree-18 atom-weld
compiler therefore applies to every pair. Under the no-high-complexity
assumption, all pairwise outputs are pole-simple rational atoms.

Apply the atom-weld gauge dichotomy. If every triple determinant vanishes,
the pair-type vectors have rank at most two over `F(X)`, which is output 2.

Otherwise every canonical pairwise weld has one normalized global atom. The
global record-extension theorem then puts every large-type record in that
same atom. In the quotient-only branch these are all `M=255011043` residual
records.

Since

```text
255011043>2097152,
```

the pole-simple small-owner payment excludes owner size `g<=2m-K=1183520`.
Thus `g>=1183521`, proving output 1. QED.
