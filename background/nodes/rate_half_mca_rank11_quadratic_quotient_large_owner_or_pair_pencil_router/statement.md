# Quadratic quotient large-owner or pair-pencil router

- **status:** PROVED
- **scope:** the 255,011,043-record synchronized residual in the
  no-high-complexity, quotient-only branch

Assume every surviving synchronized pair type is cyclic or dihedral
quadratic quotient type and no compiled order-32 packet emits

```text
chi>=2299571.
```

Then at least 520 large pair types occur, and exactly one of the following
global targets is forced.

1. **Exclusive large owner.** One pole-simple scalar-locator atom certifies
   every one of the at least `255011043` residual records. Its nonpole owner
   set has size

   ```text
   g>=1183521.                                         (LR1)
   ```

2. **Base pair-type pencil.** The vectors

   ```text
   T_p=(1,a_p,b_p)
   ```

   of all quotient pair types have rank at most two over `F(X)`.           

The first output is exactly upstream's `prob:large-owner` interface after
pole-simple routing. The second is a concrete base pair-type split-pencil
census. Neither output is paid here.

## Falsifier

Fewer than 520 quotient types in the quotient-only residual; failure of the
pairwise packet or gauge dichotomy; a global atom with owner size at most
1,183,520 carrying 255,011,043 records; or rational pair-type rank above two
when no global atom exists.
