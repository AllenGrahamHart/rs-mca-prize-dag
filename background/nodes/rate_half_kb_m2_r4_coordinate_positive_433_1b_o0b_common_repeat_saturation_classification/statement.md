# KoalaBear `433-1b/O0b` repeated-BC common saturation classification

- **status:** PROVED
- **field:** deployed KoalaBear field `F_2130706433`
- **scope:** all guarded repeated-BC principal common systems

Exact standard-basis computations on the nine duplicate-role cell
representatives, four source-root sign rows, and two BC signs complete all
72 representative cases. Duplicate-role transport then classifies all 120
algebra rows:

```text
unit common ideals (80 rows):
  all rows in [0], [4,5], [7,8], [9,12], [10,13]
  equal-root-sign rows in [1,2]

surviving common ideals (40 rows):
  opposite-root-sign rows in [1,2]                 8 rows, dimension 2
  all rows in [3]                                  8 rows, dimension 1
  all rows in [6]                                  8 rows, dimension 1
  all rows in [11,14]                             16 rows, dimension 1.
```

Here dimension is Singular's exact dimension in the guarded presentation
`F_p[z,t,r,c,b]/(minors,z*guard-1)`. Both BC signs occur in every listed
block. Since each repeated algebra row serves both outside-cycle signs, the
classification deletes 160 of 240 formal repeated-lane common systems and
leaves 80 for outside analysis.

This theorem does not classify the surviving varieties geometrically,
construct coefficient kernels, append outside rows, handle split-BC lanes or
base-rank drop, close the route, pay slopes, prove K3, or prove either Prize.

## Falsifier

A nonunit ideal in a declared unit block, a missing guarded case, failure of
duplicate-role polynomial-set identity, or an incorrect survivor dimension.
