# Audit

The verifier pins launcher, compiler, and result hashes; reconstructs all 72
cases; checks unit and dimension patterns; and independently transports the
classification across the nine exact duplicate-role polynomial-set orbits.
The hostile audit flips a unit, deletes a case, changes a dimension, breaks a
paired digest set, and changes the formal count; all mutations must fail.
