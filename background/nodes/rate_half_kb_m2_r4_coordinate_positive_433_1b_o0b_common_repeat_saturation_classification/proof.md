# Proof

For each of the nine representative cells, four source-root signs, and two
BC signs, the repeated compiler supplies six exact stripped minors in
`t,r,c,b`. The computation saturates by the product of all ten source-label
differences and the target-open-set factors

```text
r,t,b,c,b-1,b+1,c-1,c+1,b-c,b+c
```

using the Rabinowitsch equation `z*guard-1`. Singular computes a deterministic
standard basis for the first-three-minor chart and for the full six-minor
ideal. All 72 cases complete. Exactly 44 representative full ideals are the
unit ideal, with the cell/sign pattern in the statement; 28 survive with the
displayed dimensions.

Swapping `BC1` and `BC2` fixes three role cells and pairs the other twelve.
For every root-sign and BC-sign row, the sealed compiler output gives the
same unordered six-polynomial digest set on paired cells. Therefore unit
status and dimension transport exactly. Expanding the representative blocks
gives 80 unit and 40 surviving algebra rows. The outside-cycle sign is absent
from all five common records, so each row represents two formal repeated
target lanes; hence 160 deleted and 80 surviving formal systems. QED.
