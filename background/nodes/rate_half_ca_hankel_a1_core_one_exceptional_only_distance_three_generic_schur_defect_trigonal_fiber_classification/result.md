# Result

The generic rank-three distance-three branch now has an exact, executable
two-way split. Its quadratic-product rank is either `3e+1`, or exactly `3e`
with one projectively unique recovered degree-three rational map `B/R`.
There are no deeper generic defects. At most one pair lies in `R=0`; the
recovery is one linear system and requires no support enumeration.
