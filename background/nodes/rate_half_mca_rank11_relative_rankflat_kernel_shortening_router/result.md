# Result

Every individual high-core-absorbing evaluation rank-flat component has an
exact paid route:

```text
vertical component -> one slope,
slope-dominating component -> affine translation + flat shortening
                           -> explanation rank <=9 -> paid.
```

With clone components already reduced to affine owners, the remaining
rank-eleven relative obstruction is purely aggregate: control the number and
overlap/chronology of component owners.
