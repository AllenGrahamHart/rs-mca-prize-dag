# Claim contract

| field | value |
|---|---|
| claim | Every official `m in {8,16}`, `h=m-1` survivor reduces to an exactly saturated degree-`p` polynomial Belyi map with critical values `0,1` and both fibers supported on the official domain. |
| status | PROVED |
| exact rows | Four `m=8,h=7` rows and one `m=16,h=15` row. |
| analytic reduction | Every nonexceptional tangent branch is empty; the no-nonzero-tangent branch is binomial. |
| finite certificate | The linear-tail binomial has at most one admissible normalized split value versus `m-2` required. |
| open residue | The exact domain-supported Belyi normal form in `(NMR5d)` is not yet contradicted. |
| excluded scope | Lower `h`, `m=4,h=2`, wider widths, and full L1. |
| consumer | `l1_mixed_petal_amplification` |
