# Lineage

This generalizes the local tangent mechanism used at `m=4,h=3` to the
next-to-maximal value degree on the `m=8,16` Mersenne rows. The constant
eliminant exception mirrors the hard constant-level arm of the cubic proof
and is retained explicitly. The only new finite input is a five-row
root-product packet over quadratic extensions.
