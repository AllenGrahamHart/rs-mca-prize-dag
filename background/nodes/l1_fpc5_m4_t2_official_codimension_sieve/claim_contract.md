# Claim contract

- **Scope:** official `k=2^40`, `M=4,t=2`, rates `1/2` and `1/4`.
- **Input:** the exact `2s+2`-dimensional petal-equation locator envelope.
- **Output:** positive codimension, at least `2` and `(k+4)/5` respectively.
- **Nonclaim:** positive codimension alone gives an absolute-exponent split
  count or pays the rate-half codimension-two petal envelope.
