# Claim contract

- **claim id:** `l1_m4_h3_nu2_fixed_point_certificate`
- **status:** PROVED
- **scope:** the base-field-normalized positive `(nu,deg H)=(2,1)` stratum
  at `p=524287,2147483647`
- **proved content:** the normalized domain is `cK`; its two prime-field
  points split as one fixed-fiber root and one complement root; the domain
  scalar is `A=c^n`; every multiplicity triple satisfies one of the two
  printed sign equations and the canonical scalar-free divisibility
  `F_e^3-2F_e+1 | W^n-1`
- **dependency used:** simultaneous base-field normalization of the inner
  and outer maps, domain, and complement
- **nonclaim:** the sign equation is not sufficient and the divisibility has
  not been classified; no official stratum is excluded or counted; `(1,2)`,
  `nu=0`, wider `m`, and full L1 remain open
