# KoalaBear m2 r4 coordinate negative one-loop 433 complete exclusion

- **status:** PROVED
- **scope:** the complete negative one-loop `(4,3,3)` matching atlas over
  the deployed field
- **dependencies:** the six terminal matching-orbit dispositions listed
  below
- **consumer:** `rate_half_band_closure`

The fifteen common matching cells have the disjoint orbit partition

```text
[0] | [1,2] | [3,6] | [4,5,7,8] | [9,10,12,13] | [11,14]. (KB433C-1)
```

The first two orbits are empty at the common stage.  Orbit `[3,6]` has
sixteen exact common packets, all deleted at the complete paired-product
gate.  The last three orbits are empty at the common stage.  Hence no
negative one-loop `(4,3,3)` packet exists.

This theorem does not close a zero-loop or another common degree profile,
the complete coordinate orientation, a Prize row, or either Prize result.

## Falsifier

A missing or duplicated cell in `(KB433C-1)`, or a nonterminal parent orbit.
