# E1 N=256 E=20 endpoint exclusion

- **status:** PROVED
- **closure:** proof synthesis

At `N=256`, folded profile `(3,4,0)`, square mass 16, variance `V=40` is
impossible. The exact profiles `(4,4)`, `(0,5)`, `(3,2,1)`, `(2,0,2)`,
`(4,0,0,1)`, and `(0,1,0,1)` are exhausted by the proved joint exclusion.
The live positive even variance frontier is `0<V<=38`.
