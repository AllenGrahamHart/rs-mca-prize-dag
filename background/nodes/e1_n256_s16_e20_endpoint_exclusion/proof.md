# Proof

The E20 router proves that every `V=40` candidate lies in one of six profiles
on 154 templates. The six-profile exclusion proves that every
proper-conductor vector is removed by the conductor theorem and every
full-conductor norm is positive and below `2^250`. These branches exhaust the
router, so the entire `V=40` chamber is empty. Variance is even, hence the
next live positive endpoint is at most 38.
