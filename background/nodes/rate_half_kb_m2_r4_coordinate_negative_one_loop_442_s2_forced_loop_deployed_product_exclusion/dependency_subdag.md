# Dependency sub-DAG

```text
three preceding S2 exclusions --+
all-row common product data -----+--> S2 forced-loop exclusion / S2 close
```

The close supplies product-level evidence to `rate_half_band_closure`.
