# Audit

1. Both cubic components are replayed independently.
2. The forced square root disappears because all remaining products occur
   in full signed pairs.
3. Each equation has seven monomials and both reductions reach a raw unit.
4. All four forced-record orbits per row are accounted for before declaring
   the `S2` product close.
5. Common-sign transport uses only exact `b,c,m` identity.
6. No source-root, `q`, or interpolation close is asserted.
