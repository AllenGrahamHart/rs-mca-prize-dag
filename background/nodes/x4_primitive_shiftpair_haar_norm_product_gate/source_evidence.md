# Source evidence

The proof is local.  It consumes the per-fold theorem
`x4_primitive_shiftpair_dyadic_norm_router` and the effective-depth identity
from `x4_general_shiftpair_difference_degree_partition`.  Its verifier
exhausts small signed support pairs and checks the Haar identity, every
active-subset product bound, and exact cyclotomic norm ceilings.
