# Result

The X4 dyadic norm conditions now share one exact energy budget.  For each
active-fold pattern, a single integer inequality decides whether that pattern
is arithmetically impossible.  Surviving patterns still require a locator and
first-owner population bound.
