# Dependency sub-DAG

```text
round-34/35 placement arguments (A1 addenda; POSED)
                 |
                 v
rate_half_bivcurve_out_m_identity_and_deg_m [POSED; arithmetic proved]
   |
   +-- symbol disambiguation vs:
   |   rate_half_ca_hankel_a1_core_one_active_partition_incidence_reconstruction
   |   [PROVED; keeps deg_H]
   |
   : evidence / configuration-space constraint in the (BIV-CURVE) lane
   v
rate_half_band_crossing_location [TARGET]
```

No requirement edge in (the arithmetic is self-contained; the geometry is
POSED). The a1_core reference is a naming disambiguation, not a
dependency.
