# Audit

1. POSED is inherited from the source deliberately, and the package ships
   WITHOUT proof.md — the only proved content (the double count and the
   floor arithmetic) is stated and machine-checked inline; a proof file
   would overstate the item. This was the drafting pilot's call and the
   coordinator endorses it.
2. The two corrections to the original rider are PART of the statement,
   not footnotes — the false form is displayed with its refuting witness
   value.
3. The D11 rename is applied at wiring exactly as ruled: `deg_Sh` here and
   in all future wired documents; `deg_H` untouched on the PROVED a1_core
   node; the A1 addenda keep their original symbol with this node as the
   disambiguation of record.
4. `verify.py` runs 200 synthetic configurations; `verify_audit.py` shares
   no code path — deterministic structures, the two sides of (OUT-ID)
   computed by different traversals, the aggregate maximum proved by
   strict charge ordering, and the (DEG-m) floor re-derived for m = 3..10.
5. Scope is explicit that both verifiers check arithmetic only — the
   placement geometry and every DFS ceiling are source-bank claims with
   their own provenance. ZERO POWER over existence is declared twice.
6. Edge character: evidence only; no requirement discharged.
