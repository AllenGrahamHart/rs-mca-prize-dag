# Provenance

(OUT-m) was posed by pilot `r34_bivcurve_m34` (round 34, bank 3) and
carries the two coordinator corrections from that bank's audit (the false
aggregate rider; the `O <= m-3` qualifier). The (OUT-ID) refinement and
(DEG-m) are from pilot `r35_bivcurve_m4` (round 35, bank 3, coordinator
hand-checked as a trivial double count once stated). The completion-level
record is from round 36 bank 4. Drafted as a standalone package by pilot
`r37_mint_drafts` (round 37, bank 2, package 8 of 10), which flagged the
`deg_H` symbol collision as BLOCKING (D11).

Wired 2026-08-11 by the coordinator in the task-#41 mint session with the
D11 rename applied (`deg_Sh`); the a1_core node keeps `deg_H`. No proof.md
by design. No Modal computation or external library was used.
