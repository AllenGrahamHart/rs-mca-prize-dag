# Result

(OUT-m) survives as a POSED configuration-space constraint with its
arithmetic core proved: the deficiency bookkeeping is an exact double
count, the corrected aggregate is `(m-1)(1+O)` attained only by outside
deficiency, and (DEG-m) forces middle support for degree-1 slopes from
`m = 4` — zero selection power, decisive at completion (the 2-sharing
`m = 4` negative upgrades to dead-objects-at-the-ceiling).

The node exists to keep the constraint citable with its corrections
attached and its symbol collision resolved (`deg_Sh`); it is not progress
on the residual direction and claims nothing about existence.
