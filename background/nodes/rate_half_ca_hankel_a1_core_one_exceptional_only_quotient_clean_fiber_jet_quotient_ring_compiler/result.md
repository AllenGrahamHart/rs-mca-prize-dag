# Result

Clean-fiber values and second-jet derivative data have an exact quotient-ring
compiler. The official exponent reduces below `r`, and no selected root need
be enumerated. Any official implementation must nevertheless remain
compressed because `r` itself is enormous.
