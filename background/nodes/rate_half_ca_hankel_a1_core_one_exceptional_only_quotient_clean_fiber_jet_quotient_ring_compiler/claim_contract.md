# Claim contract

- **Claim:** the clean-fiber value and parameter-derivative polynomials are
  canonical quotient-ring representatives given by `(QRC1)--(QRC6)`.
- **Scope:** every clean fiber in both sharp high-distance endpoint profiles.
- **Dependencies:** clean first- and second-jet identities.
- **Consumer:** the high-distance branch of `rate_half_band_closure` and any
  future endpoint classifier.
- **Falsifier:** a clean packet for which a displayed denominator is not a
  unit, an exponent reduction fails, or the canonical representatives differ
  from direct rootwise interpolation.
- **Nonclaims:** no dense official-scale computation is feasible merely from
  these formulas, and no endpoint profile is excluded.
- **Verification:**
  `tools/ramguard tiny -- python3 background/nodes/rate_half_ca_hankel_a1_core_one_exceptional_only_quotient_clean_fiber_jet_quotient_ring_compiler/verify.py`
