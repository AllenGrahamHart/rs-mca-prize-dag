# Audit

The primary verifier reconstructs the projective partition extremum and the
ordered-pair division exactly, then applies hostile mutations to every
numerical hinge.  The independent verifier derives the same extremum by
enumerating the possible number of nonempty projective classes and checking
the concentration maximizer for each class count.

No finite-field search or floating-point computation enters the proof.
