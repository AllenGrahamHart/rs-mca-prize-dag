# Result

All eight degree-6 leading-drop leaves are PROVED empty after exact
generic localization. The live degree-12 branch now has `s != 0` and
`L6(s,p) != 0`; aligned-positive literal coverage remains `26/36`.
