# Dependency sub-DAG

```text
fiber-two cycle boundary transfer -------------------+
deleted-pair even factorization ---------------------+--> matched lift field router [PROVED]
maximal-row field-degree collapse -------------------+                |
                                                                     +--ev--> rate_half_list_adjacent_crossing [TARGET]
```

The router duplicates the parameter-uniform cyclotomic-resultant argument at
`N=2^39`; it does not silently instantiate the earlier fixed `N=2^38`
branch-collapse statement. Its endpoint is prime-field descent. The later
scalar, Jacobi, and norm gates still need a doubled-order coverage audit.
