# Lineage

This node extends the split-BC `433-1b -> O0a` common compiler to the only
new common signed stratum in `433-1b -> O0b`. The outside cycle sign creates
two formal lanes for each BC sign but does not change the common equations.
