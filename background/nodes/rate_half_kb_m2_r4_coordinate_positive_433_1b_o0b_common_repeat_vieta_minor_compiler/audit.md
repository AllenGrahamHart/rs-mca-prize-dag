# Audit

The verifier checks exact file hashes, all 240 mode cases, all 120 algebra
rows per mode, every role partition, the nine duplicate-role cell orbits,
six distinct nonzero minors per row, and the exact aggregate histograms. The
hostile audit deletes a case and a minor, corrupts a role, a cell orbit, and
a histogram entry, and requires every mutation to fail.
