# Proof

The source-facet theorem gives two opposite source-label pairs and one
singleton. Choosing the singleton and one of three matchings of the other
four roles gives `5*3=15` cells. The two source-root signs give four rows per
cell, and the `SBC` common-triangle invariant gives the two values of
`sigma`, hence `15*4*2=120` algebra rows.

The signed-edge atlas supplies `(KBO0BR-1)`. The complete-fiber Vieta theorem
then constructs five product rows and five sum rows in eight coefficient
unknowns. Let `B` be the five product rows together with the zero-sum loop
row. On `rank(B)=6`, the full matrix has rank at most seven exactly when no
pair among the other four sum rows raises the rank to eight. This is
equivalent to the six displayed determinant conditions used by the compiler.

For each row the compiler forms these determinants symbolically over the
deployed prime field and makes them primitive. Guard stripping repeatedly
divides only after checking zero remainder. The divided factors are the ten
source-label differences and

```text
r,t,b,c,b-1,b+1,c-1,c+1,b-c,b+c,
```

all units on the stated admissible open set. Thus raw and stripped systems
have the same zero locus there.

The bounded Modal replay completed all `2*15*4*2=240` mode/cell/root-sign/
BC-sign cases. The sealed output records every case and all six minor
summaries. The verifier reconstructs the case product, role partitions,
duplicate-role cell orbits, histograms, and file custody. QED.
