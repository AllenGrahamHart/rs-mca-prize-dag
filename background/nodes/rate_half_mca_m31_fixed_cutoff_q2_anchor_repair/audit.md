# Audit

- The two-top case removes both `D1` and `D2` coarse charges before pricing
  their union with the top line.
- The one-top and no-top cases remove only `D1`; `D2` remains in `F`.
- The outside-line cap uses exact outside agreement `15>K-1=5`.
- The no-top intersection case uses the saved repeated missed coordinate;
  without it the triple overlap is one coordinate short.
- The two-top case is a contradiction payment: unsafe line size forces the
  core used by absorption.
- Adjacent residue zero is a theorem-scope wall, not unsafe.

The primary verifier reconstructs all `65,258` prefix caps and `2,181`
boundary charges and rejects five mutations;
the independent audit recomputes the five cases without importing it.
