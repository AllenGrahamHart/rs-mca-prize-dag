# Provenance

This is the residue-two successor of the fixed-cutoff boundary-stack
interval.  It reuses the earlier two-boundary anchor/missed-set split and
the common-core absorption theorem at the new official endpoint.  Only
constant-size exact arithmetic is required; no Modal computation is
load-bearing.
