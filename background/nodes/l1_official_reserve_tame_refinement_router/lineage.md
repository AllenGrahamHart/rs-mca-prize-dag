# Lineage

`l1_tame_fixed_petal_refinement_census` left a wild characteristic guard and
an `F_9` coefficient-level obstruction. The present router combines the
official generated-field order, field cap, and entropy-reserve cutoff to show
that the guard always holds on the actual L1 lower-cutoff problem. It does
not alter the tame theorem or invalidate its out-of-scope wild fixture.
