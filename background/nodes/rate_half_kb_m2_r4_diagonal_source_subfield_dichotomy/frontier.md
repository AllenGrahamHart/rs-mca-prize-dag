# Frontier

In the source-line branch, impose the common-five facets and complete-source
divisor on the explicit reciprocal form `(KBDS-5)`; individual stars may now
be transported by `(KBDS-3)`. In the biquadratic branch, classify the
rational and elliptic `V4` passports against the six-pole source data and
the twelve whole-fiber quartics. Keep the parent full-support interpolation
gate in both branches.
