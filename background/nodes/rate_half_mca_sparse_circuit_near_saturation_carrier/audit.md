# Audit

1. The result is used only after the parent `q`-completion branch is
   separated.
2. Private completion coordinates prove dimension `q-1`; no genericity is
   assumed.
3. Every representation comparison stays within the `K`-point
   Vandermonde range.
4. The support-five extension is not claimed.
5. The deletion count still divides by all `c` representations of one
   circuit.
