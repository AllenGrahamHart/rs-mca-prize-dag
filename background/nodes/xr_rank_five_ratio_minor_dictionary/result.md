# Result

The RowC base obstruction is now an explicit maximal-minor problem on one
rank-six lift matrix, independent of which selected member is used as the
base point. The optimized rich-line hierarchy forces every retained member
to carry `23,560`, `12,600`, or `228` external vanishing-minor records on the
three rates.

This is the missing representability condition that a purely combinatorial
four-set packing does not see. The next theorem must aggregate or classify
these external zero minors; counting abstract rich-line designs is no longer
the right target.

Two bounded Modal probes of smooth monomial MDS extensions found no high-core
family at `n=16`: monomial received directions had no 9-rich hyperplanes, and
two-term directions had at most one. This is only adversarial evidence, not a
proof input. The probe apps were `ap-t6Wct4uEOLOL0qbA2zO0xX` and
`ap-vN7Lo2yV8hNT65R6QJzw2w`, both at `56 MB`.

Focused Modal replay: `ap-8ap9c1TE7FipDikNnwpEZP` (`59 MB`).
The complete fleet passed `154/154` in `ap-wOWOqYwHrjnsd7h54h0o3K`; all five
integration gates passed in `ap-u8MJxqpDdRmbuSWwy07Xex`.
