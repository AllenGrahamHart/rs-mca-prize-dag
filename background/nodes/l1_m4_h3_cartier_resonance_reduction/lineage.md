# Lineage

The Mason defect node reduced the cubic branch to five valuations and
produced a quartic-or-smaller Wronskian eliminant. Multiplying the associated
differential identity by the unique small power of `X` that cancels its
Euler weight exposes the image of formal differentiation in characteristic
`p`. The missing `jp-1` coefficient slots remove the top eliminant degree in
every case and kill `nu=4` outright.
