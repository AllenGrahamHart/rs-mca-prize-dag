# Dependency sub-DAG

```text
l1_m4_h3_mason_defect_budget [PROVED]
                 |
                 +---- req ----> l1_m4_h3_cartier_resonance_reduction [PROVED]
                                      |
                                      +---- ev ----> l1_mixed_petal_amplification
```

The dependency supplies a nonzero polynomial `H` with degree at most
`4-nu`. The new node applies the characteristic-`p` derivative image to
exclude the top degree and the entire `nu=4` case. It supplies evidence to
L1 and does not promote the target.
