# Upstream crosswalk - m4/h3 Cartier resonance reduction

For the upstream split-pencil census, this is a characteristic-`p` refinement
of the four-unit Mason ledger. The low-degree Wronskian equation has an exact
integrating factor, and the missing `jp-1` slots in the derivative image
exclude `nu=4` while reducing the other eliminant degrees to `3,2,1,0`.

An upstream `(A)` packet can vendor the derivative identity and four printed
coefficient constraints as exact finite algebra. It is not an h=3 emptiness
theorem, an SPI count, or a finite MCA payment.
