# Audit

1. The threshold 189 is the first integer for which
   `3t-3*15>520`; the proof does not assume that rich planes are disjoint.
2. A 189-rich selected subset spans its plane because an affine line contains
   at most 15 selected types.
3. Scalar dimension three survives the shortening, so `K'>=3`; the bound
   `2(K'-2)` is therefore nonnegative.
4. Plane recurrence uses two independent direction polynomials and their
   gcd, giving `K'-2`; using one polynomial would give only `K'-1`.
5. Residual common-gcd coordinates not in the received common core have owner
   multiplicity zero and do not escape the incidence cap.
6. The 30 in the ledger is the excess `218-188`, while the factor 60 pays at
   most two rich planes. These are coordinate incidences, not records.
7. Endpoint adjacency is adjacency for this particular capacity formula. It
   is not the prize's safe-and-adjacent-unsafe certificate.
