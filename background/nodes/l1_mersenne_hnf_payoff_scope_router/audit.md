# Audit - L1 Mersenne HNF payoff-scope router

- Recounted the nine live Mersenne atlas rows directly from
  `checkpoint_atlas.tsv`: four `m=4`, four `m=8`, one `m=16`.
- Kept `h=2` as the first possible pair degree, removed `h=m` by the proved
  maximal-value theorem, and removed only `m=4,h=3` by the aggregate
  official emptiness theorem.
- Distinguished compatible embedded even-degree cells from claimed occupied
  cells; the node does not assert that every even degree occurs.
- Treated the complement expression as an upper cap, not an exact mass.
- Applied the width improvement only after all `t=p` pairs are either
  excluded or removed by an already-paid owner.
- Compared unfloored binomial ratios only; no false identity between floored
  caps is asserted.
- No arithmetic verifier or Modal task was run. The proof is exact and the
  verifier sources are retained for deterministic replay when permitted.
