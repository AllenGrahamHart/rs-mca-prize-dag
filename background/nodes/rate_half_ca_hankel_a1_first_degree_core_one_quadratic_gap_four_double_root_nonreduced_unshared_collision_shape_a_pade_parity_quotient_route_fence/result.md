# Result

The lower-boundary common kernel has the exact form

```text
kappa=e-3+xi,
xi=dim(E_3 intersect J S_d)-(e+1)>=0.
```

The first `e+1` right-radical dimensions are forced by the locator
coefficient space through the global Padé identity. The common-kernel
normal form is therefore an exact diagnostic quotient, but not a route to a
rank contradiction by itself. Future Shape-A work must couple this quotient
to an independent collision, Hankel, fixed-factor, or split-row condition.
