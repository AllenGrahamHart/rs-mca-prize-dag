# Audit

1. Every eleven-set has exactly 55 contained nine-subsets.
2. Rank-nine shadows can occur only inside corank-one eleven-sets.
3. Their extension cap is `C((K'-1)-9,2)=C(K'-10,2)`.
4. Lower-rank shadows use only `C(m'-9,2)`, with no closure shortcut.
5. The sign in the substitution is correct because `E_0>E_1`.
6. The zero-`E_1` case forces `I_1=0` before division.
