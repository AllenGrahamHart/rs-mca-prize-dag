# Result

The first nine-shadow inequality counted only rank-preserving shadows. The
full-containment count now prices the other `55-C(d+2,2)` nine-subsets as
well. In particular, a large corank-one population consumes both the small
rank-nine closure resource and the complementary lower-rank support
resource.

This second constraint is independent of the first and cuts its boundary
optimizer.
