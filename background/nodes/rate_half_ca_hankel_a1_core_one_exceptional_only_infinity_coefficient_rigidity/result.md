# Result

The corrected exceptional-only square now has an exact infinity normal form.
Its `A_1` and `B_1` corners are saturated, and all top coefficients reduce to
`q_bar`, `j_inf`, and one optional coefficient `v_inf`. The remaining
classifier must retain this normal form together with the Hankel and
irreducibility constraints.
