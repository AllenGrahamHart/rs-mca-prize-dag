# Audit

- Compared coefficients only after proving the inherited strict bound
  `deg_X V<=D_0-2` and the new bounds for `B_1,K_1`.
- Used monicity only for `P_X`, which is a printed product of domain factors.
- Kept `v_inf` possibly zero; the theorem does not manufacture exact degrees
  for `V,W,K_1`.
- The nonzero `j_inf` and `q_bar` prove that `B_1` really reaches
  `X`-degree `D_0` and parameter degree `e-1`.
- A contributor truncation at the old `D_0-1` bound is therefore a coverage
  failure, not a harmless optimization.
