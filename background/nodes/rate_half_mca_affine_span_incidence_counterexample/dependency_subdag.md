# Dependency sub-DAG

```text
exact GF(1009) construction
  -> affine_span_incidence_counterexample [PROVED]
       -refutes-> supportwise_affine_span_compiler
       -refutes-> direction_support_affine_basis_payment
       -refutes-> direction_support_common_zero_envelope
       -evidence-> rate_half_band_crossing_location
```

No conjectural premise is used.  Downstream packages that used only gauge
equivalence, directional Johnson, or shortening are scope-repaired rather
than refuted.
