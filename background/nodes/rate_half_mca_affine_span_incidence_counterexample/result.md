# Result

`PROVED`: 31 exact same-support MCA-bad slopes exceed the claimed bounds 23
and 22 despite satisfying the original direction-separation hypothesis.

The recent no-separation affine-incidence lane is retracted.  Independent
direction-distance, sparse-puncturing, gauge-equivalence, and shortening
lemmas survive after dependency repair.

The correction was exported to `przchojecki/rs-mca` as draft PR `#1165`
from exact upstream commit `809c8e31`.  Dependency alerts on PRs `#1163`
and `#1164` retract only their inherited fixed-core affine-span payments;
the separate selector-free all-LineRay error-affine-core theorem survives.
