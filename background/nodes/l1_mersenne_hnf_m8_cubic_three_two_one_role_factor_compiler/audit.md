# Audit

Use the four homogeneous polynomials (RFC2), not a division by `A^3`.
Identity (2) shows that no root is introduced at `A=B=0`; retain the explicit
`lambda*(lambda-1)` saturation from the role compiler.

The degree statement is over characteristic zero. Reduce the primitive
integral factors before squarefree processing in an official characteristic;
special-characteristic collisions merge packets and do not authorize their
deletion.

The four factors preserve ordered multiplicity roles. They are not four
unordered color sets, and a factor root is not yet a common-quadratic HNF
solution or a base-field color assignment.
