# Result

The omitted minus branch is no longer structurally unmodelled. Its four top
windows are explicit Frobenius-closed negacyclic root codes over `F_p`, with
exact scalar rank `hR`, exact weighted collision identity, pointwise mass
floor, and ternary signed distance `2R+1`.

The remaining generating-row terminal is an upper bound for the mass of one
coupled code. It is not the plus-branch class mass `Z_1` and does not
factorize over the `2^40` singleton proportionality classes.
