# KoalaBear Q6 u2 primitive-subdegree-four route cut

- **status:** PROVED
- **scope:** residual actual KoalaBear `Q=6,s=6,u=2` branch
- **upstream:** PR `#1129`, head `59c4449ca0f5cee929dd39fc7b5ae8b0a33877f4`
- **dependency:** `rate_half_kb_q6_u2_complete_source_conic_exclusion`
- **consumer:** `rate_half_band_closure`

After the proved line and conic exclusions, every residual actual
birational-quartic component maps birationally to a non-diagonal
bidegree-`(4,4)` component of the self-correspondence

```text
f(T)=f(W),       deg(f)=60.                           (KBP-1)
```

Such a component gives a point-stabilizer suborbit of size four in the
geometric monodromy group of `f`. The exhaustive nine primitive permutation
groups of degree `60` have subdegree rows

```text
[1,12,12,15,20]  twice,
[1,15,20,24]     three times,
[1,59]           four times.                         (KBP-2)
```

No row contains four. Therefore the monodromy is imprimitive and `f` is
geometrically functionally decomposable. The exact pole-order and
Riemann-Hurwitz ledger leaves only the following possible inner degrees:

```text
{2,3,4,5,6,10,12,30}.                               (KBP-3)
```

Thus the primitive `u=2` branch is empty, and every residual survivor is
routed to one finite eight-row functional-decomposition adapter.

This is not yet a domain-compatible quotient owner, deletion of the eight
decomposition rows, `u=2` closure, cap `68`, or KoalaBear row payment.
Ledger movement is zero.

## Falsifier

A primitive degree-60 group with subdegree four, failure of the component to
suborbit correspondence, or an actual residual component not covered by the
eight inner degrees.
