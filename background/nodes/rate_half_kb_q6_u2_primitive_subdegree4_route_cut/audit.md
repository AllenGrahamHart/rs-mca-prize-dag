# Audit

- The full five non-two-transitive primitive groups are retained; checking
  only the four two-transitive groups would be incomplete.
- `verify.py` replays the nine subdegree rows and derives the pole ladder by
  direct divisor enumeration.
- `verify_audit.py` derives the same ladder by scanning all `(a,b)` pole
  profiles and independently checks the quartic defect partitions.
- Catalogue completeness remains an explicit imported classification input.
- Decomposability is not promoted to a quotient owner; domain and data
  descent are the live semantic gates.
