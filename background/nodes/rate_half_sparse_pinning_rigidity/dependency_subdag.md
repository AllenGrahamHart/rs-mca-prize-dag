# Dependency sub-DAG

```text
rate_half_mca_sparse_layer_reduction [PROVED]
  --ev--> rate_half_band_closure [TARGET]

rate_half_sparse_pinning_rigidity [PROVED]
  --ev--> rate_half_band_closure [TARGET]
```

The first theorem identifies the sparse term exactly. This theorem gives its
non-tangent normal form and pays tangencies in the high-field slice, but does
not yet count the remaining coupled system.
