# Result

The next-to-maximal order-zero HNF endpoint has no constant or linear
colored-Frobenius interpolant. A hypothetical linear interpolant would
identify `P_s` with an affine image of `mu_m` minus one color; its first
three coefficients force
`s=1` or `s=-m`, both in the prime field. The finite colored classification
therefore starts at degree two.
