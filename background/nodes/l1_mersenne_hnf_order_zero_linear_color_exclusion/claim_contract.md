# Claim contract

| field | value |
|---|---|
| claim | The order-zero colored Frobenius interpolant has degree at least two. |
| status | PROVED |
| dependency | `l1_mersenne_next_to_maximal_hypergeometric_normal_form` |
| exact rows | Four `m=8,h=7` rows and one `m=16,h=15` row with `n=m(p+1)`. |
| consumer | `l1_mixed_petal_amplification` |
| excluded strata | `ord_0(T)=0` and `deg E_s in {0,1}`. |
| open residue | Order-zero color degrees `2,...,h-1`, then the actual Frobenius, cyclotomic, and inner-lift equations; the full order-one chamber. |
| nonclaim | No endpoint row, full HNF branch, or critical node is closed. |
