# Audit

The two engines share the proved atlas and output schema, but not their
enumeration or cubic-kernel construction.  The verifier checks all 64 rows,
not only the summaries.  Translation and odd-unit normalization are supplied
by the dependency; no random sampling or floating-point arithmetic is used.

