# E1 N=256 E=30 profile-(5,4,1) relaxation certificate

- **status:** PROVED
- **closure:** two independent exact 64-shard censuses

For profile `(5,4,1)`, the complete six-odd atlas has `1,234` masks and one
affine light-support orbit per mask.  The production and audit relaxations
agree on all

```text
1234*6*binom(57,4) = 2,924,654,040
```

magnitude-layer assignments.  Exactly `1,456` assignments on `321` masks
have `M_3>1087`, and the global maximum is `1278`.

