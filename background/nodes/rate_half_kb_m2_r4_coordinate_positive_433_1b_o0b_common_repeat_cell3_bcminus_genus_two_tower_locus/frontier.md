# Frontier

Build the exact generic outside-record rank atlas over the tower
`F_p(q)(y)(r)`, then enumerate only roots of the resulting one-variable guard
norms in the deployed field. Quotient by the cell-3/cell-6 transport before
finite exceptional-fiber replay. This is the genus-two analogue of the
completed `BC+` torus workflow.
