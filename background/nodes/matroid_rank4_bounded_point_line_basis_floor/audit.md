# Audit

The primary verifier checks the coloop and contraction formulas, unfolds the
recurrence, and compares it with the residue-class closed evaluator over a
two-dimensional finite grid.  The independent audit computes every
increment directly and verifies the one-sign reset minimizer without using
the closed summation routine.

The proof carries the universal quantifier; finite checks guard arithmetic
and contract drift.
