# Audit

The production engine uses folded signed chord classes. The independent audit
forms all 49 ordered products of the seven sparse coefficients in the
negacyclic ring. Both enumerate all 111 light templates,
`binom(124,3)` heavy supports, and 64 relative sign vectors, and agree on every
row after removing runtime metadata.

The deterministic census checker replays all 31,280 printed exceptions,
recomputes profile, conductor, and third moment, and verifies the aggregate
ledger. The norm checker binds the exact primitive subsequence, compares all
16,984 FLINT and PARI outputs, and independently reconstructs every profile
maximum and candidate record.

Modal applications:

```text
complete dual census: ap-GPkfA9swDimrWIrdVL3u7Z
complete dual norms:  ap-P7nLJ3MSSHmUrHb9P2RSoX
```
