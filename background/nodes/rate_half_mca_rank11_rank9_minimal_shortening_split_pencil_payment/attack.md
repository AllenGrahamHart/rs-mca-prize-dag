# Attack surface

1. Recompute the full `990810934` ppb incidence density and the marked
   nine-subset identity without halving.
2. Search for an eleven-set where degree-below-ten evaluation has rank below
   ten, or a nine-set where it has rank below nine.
3. Check that `Z_D(u)=B`, rather than merely `B subset Z_D(u)`.
4. Try to make a marked extension use coordinates in two different petals.
5. Recompute all three capacity terms and the exact rational demand.
6. Reject any attempted propagation of this single-row argument to
   `K'>=11`, where the dimension equality no longer holds.
