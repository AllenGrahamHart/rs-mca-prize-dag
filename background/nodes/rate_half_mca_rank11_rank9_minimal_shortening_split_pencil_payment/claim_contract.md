# Claim contract

## Inputs

- The positive-dimensional component-incidence density from
  `rate_half_mca_rank11_dense_locator_component_incidence_dichotomy`.
- Ten-dimensional residual correction-space saturation.
- The residual rank-nine owner-plane core and disjoint-petal geometry.
- `rate_half_mca_weighted_split_pencil_selected_support_cap`.

## Output

At `K'=10`, all component incidences lie in rank-nine affine owner planes,
their full-density weighted demand exceeds the split-pencil capacity, and
the component target is impossible.

## Scope pins

- The component density is doubled only after both the kernel and rank-eight
  lanes are proved empty by `V'=F[X]_<10`.
- `J=B` uses the exact degree-nine locator zero set on the evaluation domain.
- The line partition uses each record's chosen size-`m'` support. It does not
  assume that the explanation has no additional agreements.
- The theorem closes only the single residual row `K'=10`.
