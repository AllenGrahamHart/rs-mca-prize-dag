# Provenance

This node is a deterministic refinement of
`rate_half_bivariate_deficiency_clone_kernel_reduction`, motivated by the
exact `m=1` rank fence and the bounded `m=2` bad-pattern campaign. It uses
only the leading coefficient of each printed clone column and standard
Vandermonde/Schur elimination.

No external computation or generic-rank assumption enters the proof.
