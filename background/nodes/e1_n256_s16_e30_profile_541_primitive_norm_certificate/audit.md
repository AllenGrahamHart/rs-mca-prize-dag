# Audit

The two computer algebra systems use independent resultant implementations.
The verifier requires identical ordered norm arrays and checks the exact
maximum and threshold inequality.  SHA-256 pins provide custody only; the
mathematical comparison is on the exact integers.

