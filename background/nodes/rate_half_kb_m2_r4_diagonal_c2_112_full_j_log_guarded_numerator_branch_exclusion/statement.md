# Statement

On each `F04-R02` and `F04-R20` surviving cubic/`J11` route, the guarded
degree-67 logarithmic numerator branch is empty over the deployed field and
all its extensions.

The descended numerator has degree `15` in `w`. On the generic chart
`V!=0`, evaluate it at `w=-U/V` by homogeneous Horner reduction, reducing
after every step modulo the exact route basis. The resulting remainders are

```text
R02: degree 39, 1788 terms;
R20: degree 41, 1945 terms.
```

After adjoining each remainder, the complete transported named localizer
reduces to zero at factor `15`. Hence neither guarded numerator intersection
contains an admissible point.
