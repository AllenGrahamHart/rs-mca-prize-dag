# Audit - L1 full-locator Pade section

## Checked axes

1. `d=h-k=e+w` at every cofactor degree.
2. The full locator series stops at degree `a` but its reciprocal continues.
3. Exactly the quotient coefficients `e+1,...,e+w` must vanish.
4. The recovered cofactor includes degrees `0,...,e` in reversed order.
5. The difference degree is strictly below `k`.
6. Split-locator and complement gcd guards are both retained.
7. Below the cap, `a-d=k-e` tail locator coordinates are genuinely free.
8. The below-cap ambient section size is exactly `q^e q^(k-e)=q^k`.
9. No graph, dimension, or cardinality conclusion is extended over `e=k`.
10. The formal `q^(-w)` density is not promoted to transversality.

## Route effect

The L1 global route now has one exact algebraic object for every shell:
split points in a received-word full-locator Pade section.  The former
`e>=k` residual is no longer untyped; its only new difficulty is possible
dependence/alignment of the continued reciprocal equations.  The proof target
is uniform section transversality with first-match payments.
