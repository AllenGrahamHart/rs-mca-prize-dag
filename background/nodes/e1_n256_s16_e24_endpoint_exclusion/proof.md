# Proof

The profile/parity/light reduction proves that every `V=48` candidate belongs
to one of the six printed profiles on one of 154 exact affine templates.  The
six-profile exclusion proves that two profiles are empty, every proper-
conductor representative is excluded by the conductor theorem, and every
full-conductor norm is positive and below `2^250`.  These branches exhaust the
router, so no `V=48` collision candidate remains.  Variance is even, hence the
next live positive endpoint is at most 46.
