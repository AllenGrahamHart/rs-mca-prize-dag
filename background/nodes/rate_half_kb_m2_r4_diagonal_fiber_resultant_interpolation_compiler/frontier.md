# Frontier

Attack the full-support kernel using the three exact facet classes and the
paired divisor transport. If it survives, reconstruct the unique biform and
impose the outer self-correspondence factor identity. Do not impose
individual-star equivariance: the diagonal normalization automorphism need
not descend to the source `X`-line.
