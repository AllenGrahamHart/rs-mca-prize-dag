# Result

PROVED. A diagonal-order-two component yields twelve split fiber quartics
with exact facet supports, diagonal whole-fiber transport, global product
`A^4`, and a full-support kernel of the printed `35 x 12` matrix. The
matrix gate is exactly equivalent to bidegree-`(4,4)` interpolation, but
its universal failure is not proved.
