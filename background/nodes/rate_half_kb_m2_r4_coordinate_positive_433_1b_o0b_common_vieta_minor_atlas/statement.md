# KoalaBear m2 r4 positive `433-1b/O0b` common Vieta atlas

- **status:** PROVED
- **scope:** principal common-rank equations for all ten target-gauge lanes

The exact signed-edge atlas has six lanes in which `BC` is split and four
lanes in which its two signed records repeat. Each lane has fifteen common
role cells and four source-root sign rows, hence sixty formal common systems.

The six split lanes have common products `(-1,b,c,bc,-bc)` and transport
exactly to the existing split compiler. The four `SBC` lanes have products
`(-1,b,c,sigma*bc,sigma*bc)`. Their common equations depend on the
common-triangle sign `sigma` but not the outside-cycle sign, so they transport
to the repeated compiler's two blocks of sixty algebra rows.

Consequently the complete principal common atlas has:

```text
10 target lanes = 6 split + 4 repeated
600 formal lane/source systems
180 distinct compiled algebra rows = 60 split + 120 repeated
3600 formal minors per mode
1080 compiled minors per mode.
```

This node neither solves the minor systems nor handles base-rank drop,
outside-row compatibility, source-label extensions, distinct slopes, K3,
LIST, MCA, or either Prize result.

## Falsifier

A missing lane, an incorrect common product transport, a dependence on the
outside-cycle sign, or an incorrect formal/distinct census.
