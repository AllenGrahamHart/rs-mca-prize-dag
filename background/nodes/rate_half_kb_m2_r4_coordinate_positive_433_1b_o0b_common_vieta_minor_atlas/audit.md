# Audit

The verifier imports the assembly script under pinned custody, checks the
exact lane keys, every common-product transport, all counts, parent statuses,
and DAG edges. The hostile audit removes a lane, changes a compiler class,
and corrupts both formal and distinct counts; all mutations must fail.
