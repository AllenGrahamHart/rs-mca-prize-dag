# Frontier

The next algebraic obligation is to solve or exclude the 180 guarded common
rows, keeping base-rank drop separate. Outside-row equations should only be
appended after each common survivor is reduced to a compact kernel.
