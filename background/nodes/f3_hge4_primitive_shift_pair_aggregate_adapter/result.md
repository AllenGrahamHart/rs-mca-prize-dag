# Result

HGE4 now has an exact primitive shift-pair interface. A finite-row theorem

```text
SP_h^prim<=7000n max(1,B_h)
```

at every retained width is enough to close `f3_hge4_norm_gate_count`. The
constant includes the whole width sum; no width cap or fixed-`h` census is
required.

