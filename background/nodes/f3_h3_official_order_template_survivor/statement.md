# Official-order template generator + survivor bound (CR-001)

- **status:** TARGET (minted 2026-07-19 at the C36 amber ceremony; alt leaf, gate=any)
- **consumer:** `f3_h3_mobius_excess_half` (alt)

An efficient official-order rich-prime template generator with a
survivor bound: via the audited coupled-ideal sieve (positive Y_18
forces p | Norm(alpha_F, alpha_G, theta, lambda) <= 6^{n/4}/4;
zero-coupling locus exactly {x,y,z} = {q,-q,-q^2}, w = q^4), enumerate
the official-order templates and bound the survivors below the
allowance — closing the C36 red on the fixed-order route. The compute
side is CR-001 (notes/PRIZE_COMPUTE_REQUESTS.md); the proved
infeasibility fence (>= 5.3e8 orbits at n=8192) means the generator
must be structural, not brute-force. FALSIFIER: a survivor census
provably exceeding the allowance.

The selected structural generator is now class-sensitive. At the direct
`P>=25` DSP8 cutoff, antipodal-free targets force a two-leaf star of disjoint
distance-six collisions. Antipodal targets force two such edges at one
target, certified by the two collision generators and one cross-target
generator. On the `P>=33` paid tail, the corresponding pure star degrees are
seven and five. The older mixed `(8,6)` bi-star remains a complete fallback,
but a donated CR-001 implementation should generate the pure
distance-six families first.

The accident-depth compiler supplied the original fixed-order campaign. Paying
three quotient layers analytically means fixed-order templates need only cover
`P>=33,R>=4`. Each survivor must combine its degree-seven or degree-five pure
distance-six star with four distinct quotient lifts at the same degree-one
prime ideal; at least three of their product-to-quotient couplings are
nonzero. After choosing a nonzero anchor, the full four-coupling batch is
odd-locally equivalent to that anchor plus the three-spoke quotient-collision
star. This separated joint-star packet is the established `(33,4)` generator.
A product-only rich-prime union or a one-coupling union remains complete but is
not an admissible generator for that declared row.

The proved joint-star/depth and single-quotient compilers now permit a
complete fixed-order campaign on any one of twelve exact corners:

```text
(P,R)>=(25,12),(26,11),...,(34,3),(35,2),(36,1).
```

At the odd joint-star/depth cutoffs `E=7,9,11,13,15`, swap parity pays the
last low-excess unit only on diagonal targets. Their improved uniform moment
allowances are respectively

```text
89023/43520, 72837/27200, 72837/21760,
72837/19040, 380371/87040
```

times `n^2`. A row evaluator should instead retain
`S_(D,E)=sum_(t!=1,D(t)>0)rho_E(t)` and use the stronger exact budget

```text
B_par,E=300n^2-17(17-E)(n-1)^2
                 -17(E-1)(n-2)^2-17S_(D,E).
```

These changes alter only the allowance, not the row's star or quotient packet.

The new coordinate-minimal `(36,1)` alternative is the preferred first
symbolic attempt. It uses pure product stars of degrees eight and six and one
quotient lift. For fixed quotient lift, the classified zero-coupling locus
contains at most two product centers, so one star vertex supplies a nonzero
coupling anchor. No second quotient lift or quotient-collision spoke is
needed. Its row-sensitive sufficient survivor moment is

```text
Dbar_17^0+(29/22)Dbar_17^A <=(29/153)B_par(n),
B_par(n)=300n^2-272(n-1)(n-2)-17S_D,
Dbar_17^c=sum_(t in class c,P(t)>=36)N_6^disj(t)R(t).
```

This improved allowance uses swap parity and the proved affine payment for
the `O(n)` diagonal-target set. The all-row fallback is
`(234697/48960)n^2`. It does not alter the candidate support.

The candidate compressor makes the nonzero-center designation optional.
The ideal generated by the full star and the coupling at its canonical center
is exactly independent of the center and remains nonzero when that coupling
vanishes. At official primes the entire `(36,1)` support is also exactly

```text
p divides s_(n,35)^X,
```

where `s_(n,35)^X` is the quotient-algebra scalar annihilator; it divides the
older cutoff-18 annihilator. A preferred implementation should compile this
single scalar support and reconstruct the exact `Dbar_17` moment only on its
official-range prime divisors. No efficient annihilator algorithm is proved.

Odd Galois dilation decomposes this scalar support into exactly
`3(n-s-1)` orbit blocks for `n=2^s`. There are `3(2^j-1)` blocks of degree
`2^j`, `1<=j<=s-1`, and the radical of `s_(n,35)^X` is the radical of the
product of their scalar annihilators. At `n=8192` this gives `24,534`
independently checkable blocks of degree at most `4,096`. Total degree is
unchanged, so this is a bounded-memory/checkpoint decomposition rather than a
complexity bound.

Every orbit block also has a unique formula-generated identifier. At degree
`2^j`, the three families are `(j,+,w)` for nonidentity odd `w`,
`(j,+,w)` for nonzero even `w`, and `(j,-,w)` for nonzero even `w`, all
modulo `2^(j+1)`. Their orbit polynomials are explicit cyclotomic resultants
against `S_w(Z)=1+Z+...+Z^(w-1)`; the minus family is the exactly normalized
reciprocal resultant. Thus a contributor need not enumerate Galois orbits to
build the block manifest. Reciprocal blocks must still be processed
separately because no equality of their scalar supports is proved.

Exact scalar elimination is no longer the only complete candidate route. For
each block `q_O`, take the three ordinary resultants against
`Pcal_n^[0],Pcal_n^[1],Pcal_n^[2]` and their positive odd gcd `g_O`. They
are not all zero, and `s_O,35` divides `g_O`. Hence factoring all `g_O` and
checking every retained official prime exactly is a complete superset screen.
At `n=8192` it has `73,602` independent resultant tasks. It may overgenerate
primes and has no proved resource bound, but it avoids multi-generator Smith
elimination if ordinary resultants and their factorizations are tractable.

There is also an exact support-level resultant route. For each block form

```text
C_O,35(X)=Res_T(q_O,sum_(i=0)^35 Pcal_n^[i](T)X^i).
```

The odd coefficient content `c_O,35` has exactly the prime radical of
`s_O,35`. This needs one polynomial resultant per block and creates no
candidate false positives. At `n=8192`, its maximum auxiliary degree is
`143,360`. No coefficient-growth or implementation bound is proved, so a
complete small-order comparison against the exact Smith and three-resultant
routes is required before an official campaign.

The Taylor construction is exact at every cutoff `c>=2`, and its prime
supports descend with `c`. Any `c<=35` is therefore a complete cutoff-35
pre-screen followed by exact row filtering. The low-degree endpoint `c=2`
keeps exactly quotient-supported triple product collisions and has maximum
auxiliary degree `8,192` at `n=8192`, while excluding the different-root
false positives of the three scalar resultants. A pilot should compare
several cutoffs because coefficient cost rises with `c` while genuine
lower-multiplicity candidate inflation may fall.

The preceding `(35,2)` alternative has an incomparable survivor rectangle,
the even-row normalized allowance, two quotient lifts, and the equivalent
one-anchor/one-spoke normal form:

```text
Dbar_16^0+(29/22)Dbar_16^A <=(319/153)n^2.
```

The nodal-degree router proves that the degree-eight/six star centers carry
respectively at least five and three smooth-trace leaves. A complete generator
may designate those leaves and saturate by their nonzero trace discriminants;
it must retain the union over valid designations.

The intermediate `(34,3)` row keeps degree-seven/five product stars but
needs only three quotient lifts and two quotient spokes.

A complete generator and bound for any one compiler row closes this target.
The rows are alternatives: a campaign may not combine one row's product
cutoff with another row's quotient depth or allowance.
