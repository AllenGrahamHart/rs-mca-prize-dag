# Statement

Over `F_p`, `p=2130706433`, the guarded cell-11 positive `433-1b` route is
empty for parallel-`DE` missing roles `xi=0,2` at residual matching `4`.
The exact generic label quotient transports this to eight labels:

```text
{(0,4),(0,7),(1,4),(1,7)}, {(2,4),(2,7),(2,9),(2,10)}.
```

These form two generic orbits. No other matching is decided.
