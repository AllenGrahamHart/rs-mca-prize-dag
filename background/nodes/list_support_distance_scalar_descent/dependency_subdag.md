# Dependency sub-DAG

```text
list_support_distance_scalar_descent
                    |
                    +--ev--> rate_half_list_adjacent_crossing
```

The theorem is self-contained. It removes an extension-field quantifier only
on rows satisfying its explicit incidence inequality; it does not supply the
remaining prime-field list bound.
