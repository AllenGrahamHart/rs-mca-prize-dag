# Result

`PROVED`. A support-distance/projective-averaging argument reduces an
extension-field Reed-Solomon list threshold exactly to the base field whenever
`LtH_r<gN_r`. The Mersenne-31 quartic `2^-100` upper predicate is therefore
equivalent to its prime-field version.

No list upper bound or adjacent crossing is proved.
