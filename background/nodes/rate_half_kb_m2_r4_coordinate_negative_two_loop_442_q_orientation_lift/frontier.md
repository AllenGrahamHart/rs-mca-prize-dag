# Frontier

Do not spend further effort deleting the six rows with common-`K` q signs.
For each of their at most twelve roots, combine the two compatible orientation
classes with the exact source-facet completion.  Reconstruct `eta` and the
six `L^c` product records, then apply the complete rank-three and paired-
product involution gates.
