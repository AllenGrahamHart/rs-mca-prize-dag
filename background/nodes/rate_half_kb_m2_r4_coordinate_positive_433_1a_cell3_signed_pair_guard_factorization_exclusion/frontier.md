# Frontier

Orbit `[3,6]` is closed.  Do not return to its genus-three normalization,
exceptional scales, retired colored resultants, or signed-pair projection.
For `[9,10]`, `[11]`, `[12,13]`, and `[14]`, first build compact kernels and
test the same full signed-pair guard factorization before colored elimination.
