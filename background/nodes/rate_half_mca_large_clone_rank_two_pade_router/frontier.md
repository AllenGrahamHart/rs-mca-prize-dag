# Frontier

The next route decision is between:

- a degree-reduction theorem for the fixed owner, from denominator at most
  `2d` back to the degree-`d` large-owner input or an exact extension cell;
- a direct split-pencil count on the finite clone band
  `m<=c<=m+2d`;
- exclusion/payment of the equality residue `Omega=mu Lambda_C`.

The obstruction polynomial is now the canonical invariant. Preserve it in
any next decomposition instead of reopening arbitrary owner-pencil
components.
