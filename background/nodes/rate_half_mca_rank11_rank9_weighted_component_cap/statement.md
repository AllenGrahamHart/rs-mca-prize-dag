# Rank-nine weighted component cap

- **status:** PROVED
- **scope:** one fixed rank-nine nine-coordinate affine-owner chart

Let `W_B` count the affine-owner component incidences `(gamma,T)` with
`B subset T`, `|B|=9`, and `|T|=11`. Uniformly over every residual
shortening,

```text
W_B <=(n'-m'+1)(m'-10)n'
     =981105*(m'-10)*n'.                            (WP1)
```

The count retains the same extension multiplicity as the weighted
nine-subset concentrator.

## Nonclaim

This is not a cap on all component lanes or a chronology payment. It applies
to the rank-nine affine-owner chart after lane typing.
