# Attack

Apply the same weighted owner-point charge to higher-kernel charts. The live
question is whether a fixed kernel can similarly charge every component
extension to a bounded root or quotient resource.
