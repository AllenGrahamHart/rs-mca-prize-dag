# Audit

- The capped KKT classification includes every possible number `k=0,...,7`
  of upper-bound coordinates and every feasible lower multiplicity.
- Lower-bound coordinates have product zero and cannot maximize against a
  positive feasible tuple.
- The certificate uses integer square roots and exact rational products only;
  the floating-point probe is not load-bearing.
- All 11023 removed chambers are checked individually. No monotonicity in
  variance is assumed.
- Every preceding even boundary has an exact lower endpoint above its
  cofactor floor, preventing a silent stronger claim.
- Route probe Modal app: `ap-fjMSdUdXuYdwoI9NyoCnUx`.
- Exact certificate Modal app: `ap-9bABu2qYhJVmkjNPhYzzKi`.
