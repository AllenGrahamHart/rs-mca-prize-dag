# Result

`PROVED`: all nine residual profile-`(3,6,S=18)` cofactor windows are
contracted by the sharp capped product envelope. The shortest are now
`m=512,514`, both with `4<=V<=34`; `m=256` follows with `4<=V<=60`.

No cofactor is removed by this node. Successor attacks should consume these
windows rather than the superseded parent endpoints.
