# Claim contract

## Imported facts

From `e1_prize_n256_s18_profile_36_cofactor_windows`:

- the exact nine cofactors addressed here and their former variance windows;
- positivity and evenness of `V`, with `V=0,2` impossible;
- the identities in (1) and the prize lower endpoint `R>=m B_P 2^128`.

## New theorem

The pointwise cap `y_u<=144` and exact two-moment data reduce every product
maximum to a capped two-level chamber. Exact rational comparisons sharpen all
nine residual windows to the endpoints in `statement.md`.

## Exclusions

1. This node does not exclude any of the nine cofactors completely.
2. It maximizes over a real relaxation and does not assert realizability of
   any extremizing tuple by a cyclotomic polynomial.
3. It makes no claim below each printed endpoint; the verifier confirms that
   the preceding even variance survives this method.
4. The already excluded cofactors `1024`, `1028`, and `1538` are not restored
   or recounted.
