# Audit

1. Conservative and geometry lanes are exhaustive and disjoint.
2. Above-ceiling filtering uses exact integers and strict `>`.
3. The 1,995-tuple frontier is pinned by canonical digest.
4. Every carrier alternative retains inherited and joint charges.
5. Every reroute and geometry maximum is below the safe plain maximum.
6. Tied reroute maxima are not treated as unique.
7. Floor placement and record coefficients are replayed exactly.
8. Heavy reconstruction is Modal-only; the local wrapper uses the shared
   compact-contract validator.
