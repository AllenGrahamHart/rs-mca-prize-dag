# Proof

Instantiate the proved carrier-atlas and flat-coupled charge theorems at
`K'=75`, `q=65`, `m=67547`, and `n=1048651`. The conservative stream has
9,479,358 leaves and exactly 1,995 above-ceiling defect tuples. Their
lexicographically ordered SHA-256 is

```text
49a191e500624b4f98761863cbce004d3dc2911055c7e24b8b966a2b99e2d440.
```

The largest already-safe conservative leaf is

```text
s2=43/s3=36/U23/s4=37/s5=32/c6F/c7F/c8F/c9F/ordinary,
```

with premium `(P75)` and margin `(M75)`.

The exhaustive pairwise carrier atlas expands the exceptional set to
1,228,878 exact evaluations. Every evaluation is safe; their maximum is

```text
33540409167198343349228340842477255779539196790,
```

below the ceiling by
`7632076084725349834237524767441204623742530587`. Tied tuples attain this
maximum, so no unique representative is claimed.

The seven disjoint geometry lanes contain 131,010,586 exact evaluations.
Their maximum is the one-step value

```text
36818718942605286540367916391174777496642019789,
```

strictly below `(P75)`. Therefore `(P75)` is the global premium. Substitution
into the inherited exact floor-aware component ledger gives `(G75)`, so
demand exceeds complete capacity. QED.
