# Replay

Run the parameterized compact audit with argument `75` to reconstruct the
9,479,358-leaf frontier, canonical digest, 1,228,878 reroute evaluations, and
final payment. Run the lane wrapper with `75` and each of
`carrier32_geom,one_geom,...,six_geom` for the 131,010,586 geometry leaves.
Observed peak RSS was 58--62 MB. Default local verification repeats none of
these enumerations.
