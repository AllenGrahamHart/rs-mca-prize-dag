# Claim contract

- Only duplicate copies are exchanged: `DE` in `SDE`, `DF` in `SDF`.
- Opposite-signed split records are not treated as duplicates.
- The action preserves every common/source row and every target guard.
- The 10,620 count is a representative workload, not an exclusion count.
- No raw row, common row, outside label, route, K3 case, or Prize endpoint is
  closed by this node.
