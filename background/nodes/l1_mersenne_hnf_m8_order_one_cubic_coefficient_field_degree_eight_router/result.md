# Result

The official normalized coefficient field has degree eight over `F_p`.
For univariate `b` certificates, the exact eligible irreducible degrees are
`1,2,4,8`; higher degrees and degrees not dividing eight cannot contain an
official value of `b`.

No packet was executed and no chart status changed.
