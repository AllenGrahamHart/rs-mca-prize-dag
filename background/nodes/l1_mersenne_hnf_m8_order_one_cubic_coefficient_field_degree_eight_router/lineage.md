# Lineage

The initial quotient packet used the role field `F_(p^2)` as a tentative
coefficient-field filter. A field-scope audit removed that unsupported
closure rule. This node supplies the correct descent from the cyclotomic
normalization: the normalized HNF data lie in `F_(p^8)`, giving the exact
factor-degree set `{1,2,4,8}`.
