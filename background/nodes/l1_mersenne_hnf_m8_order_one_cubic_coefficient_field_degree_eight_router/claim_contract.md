# Claim contract

| field | value |
|---|---|
| claim | On every official `m=8,h=7` order-one cubic row, the normalized HNF roots and cubic factor coefficients, hence `b=4dg_1-15`, lie in `F_(p^8)`; an irreducible `b` factor is eligible exactly when its degree divides eight. |
| status | PROVED |
| dependencies | Belyi shifted-value gate and cubic `3+2+1` common-quadratic compiler. |
| exact rows | `p=2^t-1`, `t in {13,17,19,31}`, `n=2^(t+3)`. |
| guards | The normalized split values are nonzero and distinct; all divisions already belong to the inherited HNF saturation. |
| open residue | Every actual common-gcd value, all role and reconstructed-variable saturations, norm/Frobenius/cyclotomic conditions, inner lift, and other color shapes. |
| nonclaim | No factorization has been run and no retained chart or critical node is asserted empty. |
