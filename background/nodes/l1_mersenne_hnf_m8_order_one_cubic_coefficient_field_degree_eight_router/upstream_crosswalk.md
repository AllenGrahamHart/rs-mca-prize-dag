# Upstream crosswalk

This is a base-field-normalization lemma for the finite split-pencil census.
It belongs beside the exact `h=7` cubic packet in PR #1120: certificate
factors are filtered by the generated cyclotomic field `F_(p^8)`, not by the
quadratic role field and not by an unrelated ambient evaluation field.
