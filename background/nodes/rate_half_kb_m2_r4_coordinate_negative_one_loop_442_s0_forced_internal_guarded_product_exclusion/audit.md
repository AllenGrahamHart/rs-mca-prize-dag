# Audit

1. Both `tau_0` parities and both cubic components are replayed.
2. Clearing `d^2` is restricted explicitly to `d!=0`.
3. Every completed basis contains the one-term monic element `f`.
4. The conclusion is guard-saturated, not raw-unit.
5. The exact `6+10+4` forced-cell count is reconciled in all four rows.
6. The close is scoped to one common orbit; no broader coordinate or row
   close is asserted.
