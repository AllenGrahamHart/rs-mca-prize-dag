# Dependency sub-DAG

```text
preceding S0/S1/S2 exclusions --+
all-row common product data -----+--> final S0 exclusion / 80-cell close
```

The close supplies product-level evidence to `rate_half_band_closure`.
