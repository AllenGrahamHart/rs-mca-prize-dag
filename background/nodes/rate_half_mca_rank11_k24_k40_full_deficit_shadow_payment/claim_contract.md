# Claim contract

This node is a finite exact payment for the rank-eleven component lane.  It
uses only proved inputs and checks every residual row and every core offset
in the declared interval.

The closed statement is precisely `24<=K'<=40`.  Together with its proved
predecessor it yields the component prefix `10<=K'<=40`.

The `K'=41` computation is only the first wall of this payment formula.  The
node does not pay rank nine beyond the prefix, move an active-v4 ledger,
construct chronology, close KoalaBear, or close either prize problem.
