# Audit

Run `verify.py` and `verify_audit.py`.  The independent audit reconstructs
the `F_p` root part of every retained norm and inverse polynomial using
coefficient-list arithmetic, checks the complete candidate unions, and
replays source and residual coverage without importing a constructor.
