# dependency sub-DAG: petal_squarefree_classification_counting_soundness

Edges are directed from dependency to consumer.

```text
petal_squarefree_classification_counting_soundness [PROVED]
    -> petal_kernel_realizable_sparsity [CONDITIONAL]
```

This node proves only the counting consequence of a classification. It does
not supply the classification itself.
