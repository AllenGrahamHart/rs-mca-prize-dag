# Attack

Test small representable matroids after contraction, especially inside sets
whose contraction creates loops or parallel classes.  Independently mutate
the coefficient `N-R-4+i`, the range `i<=3`, and every low-outside floor.
