# Result

**PROVED.** Every fixed R20 selected degree-12 `B0` chart with `K10!=0` is
supported on the forbidden boundary `1+s+pvar=0`, so its complete named-open
part is empty over every field extension.

The four literal R20 cells remain open only through the separate degree-12
`K10=0` leading-drop branch. Aligned-positive whole-cell coverage therefore
remains `30/36`.
