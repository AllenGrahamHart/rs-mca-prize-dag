# Frontier

Attack the 36 cells by exact saturation, first using the forced-value and
binary-invariance equations, then full twelve-label Mobius interpolation.
Carry both q orientations only for product survivors.  Keep `D,E,F`
independent of `l`.  If exact elimination exceeds the local budget, shard
one cell per external task with deterministic proof certificates.
