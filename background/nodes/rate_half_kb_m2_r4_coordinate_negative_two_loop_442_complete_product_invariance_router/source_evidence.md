# Source evidence

- The exceptional-product classifier supplies the six exact common rows.
- The outside-product compiler supplies `xi`, its protected forced value,
  and the row-specific nonsingular product involution.
- The complete-edge skeleton supplies the seven signed product types and
  residual `D,E` exchange symmetry.
- The coordinate normal form keeps endpoint `T` and source/quotient `W`
  coordinates independently normalized.
- Primary and audit verifiers replay the sign quotient, occurrence orbits,
  involution matrix, cell count, and naive-to-invariant reduction.
