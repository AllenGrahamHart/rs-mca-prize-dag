# Audit

1. The sign gauge leaves the invariant `sigma=wuv` and retains both sign
   orbits.
2. The residual `D <-> E` and `F -> -F` action has occurrence orbits of
   sizes `2,1,4`; it does not merge the three printed edge types.
3. Binary-form proportionality is division-free and compiles all 15
   residual perfect matchings in each cell.
4. Product squarefreeness and fixed-point coprimality are required for the
   converse; proportionality alone permits repeated or fixed roots.
5. The 36 cells compile only products.  Both q orientations and full
   twelve-label interpolation remain mandatory.
6. The independent `T`/`W` normalization fence forbids substitutions of
   `D,E,F` by powers of `l`.
