# Result

`PROVED`. The collective-span frontier collapses from dimensions `7..10` to
the single full-rank case: every unsafe survivor has `V_nt=C'` and both have
dimension ten.

The survivor is also macroscopic, with at least `16384884` distinct promoted
rich containers. The next theorem should exploit full-span basis exchange,
container incidence, or a global owner/factor relation; another bounded-span
payment cannot advance this route.
