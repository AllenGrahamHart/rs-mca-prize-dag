# Result

The full-domain layer-A object cannot decompose into `m` rational parameter
branches once `m>=2`. At the official row it contains one
`F_q(X))-irreducible component of parameter degree at least
`103079215105`. The live attack moves to that component's interaction with
the multiplicative evaluation domain and the existing Hankel/norm
identities.
