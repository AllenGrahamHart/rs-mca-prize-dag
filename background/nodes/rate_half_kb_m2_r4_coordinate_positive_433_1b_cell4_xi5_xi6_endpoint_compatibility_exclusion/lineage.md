# Lineage

- Wave 62 completed all 45 parallel-`DE` labels in cell 4.
- The remaining roles split into variable roles `df,ef` and endpoint roles
  `bf,cf`.
- Endpoint products determine the signed second endpoint from the source
  point, yielding one compatibility equation before any pairing is chosen.
- A bounded eight-row Modal census closed both endpoint roles; each row took
  under four seconds and used no large computation.
- The cell-4 frontier is reduced from 60 labels in 36 quotient orbits to 30
  labels in 18 quotient orbits.
- The result remains background evidence toward `rate_half_band_closure`.
