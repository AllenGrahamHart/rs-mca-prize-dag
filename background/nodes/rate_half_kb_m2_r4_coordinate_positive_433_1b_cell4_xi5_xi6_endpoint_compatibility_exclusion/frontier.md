# Frontier

Cell 4 now has 75 of 105 missing/matching labels proved empty. Exactly 30
labels remain: `xi in {3,4}` across all 15 canonical matchings. Under the
parallel-positive-`DE` exchange these form 18 quotient orbits.

The next route should exploit the symmetry between missing `df` and missing
`ef`, beginning with a target-free or low-degree matching block. Do not
repeat endpoint compatibility at `xi=3` or `xi=4`: neither missing product
has a source-fixed endpoint.

Do not infer complete cell-4, K3, LIST, MCA, or Prize closure.
