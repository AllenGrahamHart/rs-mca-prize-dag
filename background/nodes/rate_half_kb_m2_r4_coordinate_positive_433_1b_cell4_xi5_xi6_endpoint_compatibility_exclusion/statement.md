# Statement

## Claim (KBP1B4-XI56-ENDPOINT-1)

On the guarded positive deployed `433-1b` role-cell-4 branch, every atlas
case with missing record `xi=5` (the `bf` record) or `xi=6` (the
`sigma_c cf` record), any canonical matching, any source sign
`epsilon in {+-1}^2`, and any target lane `sigma in {+-1}^2` is empty.

Let `m` be the missing product, `s` its squared-sum record, and put
`x=b` at `xi=5` or `x=c` at `xi=6`. Every target necessarily satisfies

```text
(x^2+m)^2-s x^2 = 0.
```

An exact eight-row source census over the cell-4 four-basis tower proves
that no route-open source point satisfies this compatibility. The cut does
not use a matching or target sign, so the eight rows pay all
`2*15*4*4=480` raw cases, or 30 missing/matching labels in 18 exchange
orbits.

No `xi in {3,4}`, complete cell-4, K3, LIST, MCA, or Prize result is
claimed.
