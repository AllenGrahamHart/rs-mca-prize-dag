# Audit

- The cleared endpoint identity is checked formally for both roles.
- Each exact row uses the proved four-basis tower and direct source lifts.
- Candidate parameters are the exact union of compatibility-norm and every
  inverse-guard numerator/denominator root.
- Each of the eight rows has five compatibility roots and seven candidates.
- Per row, five candidates hit `r` guards; the two remaining `r` values each
  have one `t`-guard lift and one lift with no base-field `b` root.
- The independent verifier reconstructs every polynomial root, base lift,
  and nonsquare `b` discriminant without importing the compiler.
- Compatible-source, free-lift, finite-source, and unresolved ledgers are
  empty.
- The condition is source-only, so eight rows cover 30 labels, 18 quotient
  orbits, and `2*4*15*4=480` raw cases.
- No pairing equation is used and no `xi in {3,4}` case is claimed.
