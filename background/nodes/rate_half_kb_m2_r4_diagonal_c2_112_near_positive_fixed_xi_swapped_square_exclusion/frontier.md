# Frontier

Classify the mixed residual allocation in the same fixed-moving `xi=a`
template. Then move to the other relative `xi` orbits, the moving-moving
template, negative factor loci, and the homogenized `w=0` boundary.
