# Result

The first core-one packet is equivalent to a two-point defect: the
distinguished row is `D L_alpha L_beta`, its Forney specialization is `D`
times a cubic, and `O_C(rho+2,-e-1)` is the effective divisor
`P_alpha+P_beta`.
