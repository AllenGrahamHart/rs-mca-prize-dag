# Claim contract

## Inputs

- the exact profile-`(4,4)` local valuation and cofactor list;
- the official low-energy exclusion `E>=5`;
- the energy-floor product majorant.

## Output

Nine valuation branches have `E>=6` and cofactor ceiling `853574`; the exact
global frontier contracts from `657` to `645` values.

## Nonclaims

- no coefficient or collision count;
- no one-orbit-per-cofactor assertion;
- no profile payment or critical status promotion.
