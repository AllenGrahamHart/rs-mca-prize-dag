# Proof

The exact profile/parity/light reduction proves that every pair-feasible
folded-profile `(3,4,0)` vector at `V=50` has one of the nine profiles printed
in the statement and lies over one of 111 affine light templates.

The joint exclusion exhausts those templates and profiles using two complete
actual-vector censuses, the proper-conductor theorem, and independent exact
norms. Hence `V=50` is impossible.

The preceding endpoint theorem left only positive even `V<=50`. Removing
`V=50` advances the live frontier to positive even `V<=48`. QED.
