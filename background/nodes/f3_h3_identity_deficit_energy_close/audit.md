# Audit

Date: 2026-07-18.

## Forward audit

Relabeling variables identifies product and quotient multiplicative energies.
Expanding the squared `L2` distance gives the correlation identity exactly,
with no inequality or omitted parameter. The identity coordinate contributes
one nonnegative summand, and the proved strict bound on `P(1)` makes the final
C36' comparison strict. The remaining comparison is the displayed polynomial
`G(x)`; its value and derivative signs are checked symbolically for all
`x>=20`, which contains every official order.

## Consumer-backward audit

The C36' consumer needs a strict three-to-one bound. This node supplies it
only under `4E_x(A)<=145(n-1)^2`; no prose elsewhere in the repository proves
that premise. The implication therefore remains a `PROVED` background node
with evidence edges. In particular, it does not discharge
`f3_h3_mobius_excess_half`, whose cutoff-18 assertion is a different
sufficient route.

## Replay scope

The verifier checks the exact identities in four finite subgroup fixtures,
all 29 official integer orders at high decimal precision, a nearby failed
constant mutation, and the required DAG wiring. It does not use Modal.
