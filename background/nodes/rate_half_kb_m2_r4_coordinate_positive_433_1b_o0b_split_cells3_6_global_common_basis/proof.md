# Proof

The parent compact-curve theorem supplies the three exact determinants,
six rank-five cofactor charts, and sixteen route guards in every source-sign
row. The cached-input compiler reproduces all determinant and kernel hashes
from those certificates.

For each sign row, Singular computes the ideal quotient by every ordinary
guard and then the saturation `I:C^infinity`, where `C` is the ideal of all
six cofactors. Algebraically this is the closure of `V(I)` outside `V(C)`,
exactly the union of the six product-rank-five chart opens. The complete
transcript reports dimension one and prints 21 basis elements. The checker
recounts and hashes every element. The split common adapter and B/C
full-system involution provide the stated lane and cell transports. QED.
