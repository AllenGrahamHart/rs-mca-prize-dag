# Closed attack record

1. The strict-caller verifier is pinned to `spi_point_counting`; any new
   `conj_f` requirement re-opens the compiler.
2. LIST's unsupported prose charge was removed and its actual FPC5 residual
   exposed as a direct target.
3. The full Pade section remains typed as polynomial, not linear.
4. The SPI descriptor preserves one occurrence per slope incidence and the
   exceptional-component first-owner order.
5. The downstream quantitative payment remains open in
   `f_global_packing_step`; this compiler only types its actual input.
