# Proof: prize Conjecture-F consumer scope

The manifest verifier scans every critical and background node for strict
`req` edges from `conj_f`. There is exactly one: `spi_point_counting`.

`f_imgfib_consumer_descriptor` proves why the former LIST edge is absent.
The repaired `imgfib` assembly partitions full-petal contributors into the
proved top-band/fixed-excess payments and the exact FPC5 target, while
mixed/diffuse partial petals have their own direct target. No LIST branch is
passed to a linear-flat theorem.

`f_spi_hankel_consumer_descriptor` proves that each remaining slope
incidence is exactly

```text
P(ker M(z)) cap D_j(H).
```

It emits the unchanged domain, projective dimension
`dim ker M(z)-1`, exact root threshold `j`, no punctures, and the established
exceptional-component first-owner order. Emitting one occurrence per slope
preserves incidence multiplicity even when a locator occurs at several
slopes.

The sole strict caller therefore has an exact typed descriptor, and there is
no untyped LIST caller. This proves the current prize-consumer scope compiler.
The result is guarded by the caller-inventory verifier: adding any strict
caller re-opens the claim. QED.
