# Prize consumer flat-scope compiler

- **status:** PROVED
- **consumers:** `f_global_packing_step`, `f_primitive_case`

The current strict DAG has exactly one consumer of `conj_f`:
`spi_point_counting`. `f_imgfib_consumer_descriptor` proves that the repaired
LIST assembly has no Conjecture-F call and instead exposes the direct FPC5
and mixed/partial L1 targets. For the remaining SPI call,
`f_spi_hankel_consumer_descriptor` emits
one exact descriptor containing

```text
(domain, locator degree j, projective flat dimension r,
 root threshold, punctures, first-owner chronology).
```

The descriptor is passed without multiplicity loss to the quantitative
payment/packing regime. The compiler is exhaustive only for the current
strict callers; a new strict caller must add a new descriptor and re-open
this node.

The full-locator Pade section remains a polynomial section, not a linear flat,
and is not silently passed to Conjecture F. The proved punctured projective
cell inside that program is evidence for future routes, not a current strict
consumer. The broad statement "every linear flat" is also not substituted
for this caller-scoped theorem.

## Falsifier

An actual strict caller with no emitted descriptor, a current LIST call
hidden behind a direct residual, or a transport that loses first ownership
or slope-incidence multiplicity.
