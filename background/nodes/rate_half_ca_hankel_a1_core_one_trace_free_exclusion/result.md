# Result

The trace-free nonzero-weld leaf is impossible. Its reduced complement would
make every supported component fiber divide `P_X`, charging full deficit at
every bad row. Capacity leaves one boundary profile, and its exceptional
degree drop contradicts the available quotient degree of `A_1`.

Every surviving profile on this face therefore has an active defect trace of
`K`. That active-trace branch remains open. No Modal computation was used.
