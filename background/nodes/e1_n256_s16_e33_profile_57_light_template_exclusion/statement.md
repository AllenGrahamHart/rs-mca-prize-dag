# E1 N=256 E=33 profile-(5,7) light-template exclusion

- **status:** PROVED
- **closure:** proof plus two complete finite censuses

No pair-feasible folded-profile `(3,4,0)` collision at `N=256`, `V=66` has
autocorrelation magnitude profile

```text
(n_1,n_2)=(5,7).
```

The five odd coefficients force the five non-diameter light-light chords into
distinct circular distance classes. After normalizing the unique light
diameter, the 7,200 normalized diameter-Sidon light supports form exactly 100
orbits under odd cyclotomic units and the diameter stabilizer.

Two independent exact censuses cover

```text
100 * binom(124,3) * 64 = 1,984,793,600
```

normalized signed vectors and agree on

```text
profile-(5,7) vectors                 28,048;
full-conductor profile vectors        17,768;
unrestricted maximum M_3                1758;
full-conductor maximum M_3               1416.        (1)
```

The unrestricted maximizer has proper conductor and is excluded by the
proved proper-conductor theorem. Since `1416<1732`, the exact `V=66`
cubic-Hermite norm certificate excludes every full-conductor candidate.
