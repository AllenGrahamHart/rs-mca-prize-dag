# Lineage

The quadratic collision router controls multiple repeats but leaves the
injective possibility. This node quotients the quadratic reflection
`W -> S-W`. The reflected locator product has the invariant coordinate
`U=W(W-S)`, turning a collision-free quadratic color map into the same
punctured-cyclotomic linear template excluded at the previous degree.
