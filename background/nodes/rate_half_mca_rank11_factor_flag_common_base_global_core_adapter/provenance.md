# Provenance

Local continuation of upstream PR `#1173` after the full-span and exact
`2 x 5` factor-flag nodes. The target vocabulary `(C)` is inherited from the
proved rank-eleven 31-anchor globalization router.
