# Result

The common-base horn of the exact `2 x 5` factor-flag recursion is not a new
terminal. It is the existing nonempty line-global-core route `(C)`. Within
that model, only the deeper `18166`-coordinate residual flag remains as a new
factor-router output.
