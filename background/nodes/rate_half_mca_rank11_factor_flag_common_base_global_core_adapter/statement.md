# Rank-eleven factor-base to line-global-core adapter

- **status:** PROVED
- **scope:** the common-pencil-base output of the exact `2 x 5` factor-flag
  router
- **output:** the existing line-global shortened-family route `(C)`

Let `e_0` be the actual anchor pair, let `G_0` be its anchor-good set, and let
`C'` be the complete post-gauge correction direction space for the selected
post-near family. Suppose

```text
C'=span(PB)
```

and some `x in G_0` is a common zero of every polynomial in `P` or of every
polynomial in `B`. Then every polynomial in `C'` vanishes at `x`. Both
components of every selected pair
differ from `e_0` by elements of `C'`, so every selected pair equals `e_0`
at `x`. Since `x in G_0`, the anchor pair equals the received pair there.
Thus `x` belongs to every selected record's maximal agreement support, and
the complete line-global common core `C_*` is nonempty.

Consequently either factor-space common-base horn enters the already-proved identity-fiber
shortened residual `(C)`. In the exact `2 x 5` model, the only genuinely new
output of the factor-flag router is therefore the deeper residual flag on at
least `18166` actual coordinates; the alternative is an existing `(C)`
route.

## Nonclaim

This adapter does not pay `(C)`, prove the `2 x 5` presentation, or show that
the pencil has a common base coordinate. It only identifies the semantics of
that output when it occurs.
