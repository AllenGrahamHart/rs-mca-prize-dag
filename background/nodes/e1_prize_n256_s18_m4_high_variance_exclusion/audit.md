# Audit

- Every logarithmic inequality is replayed with rational atanh-series bounds;
  no floating-point comparison is load-bearing.
- `verify.py` reconstructs the chord inequality, all 702 universal layer
  profiles, their four exact `Phi` maxima, every Hermite interpolant, and all
  four quadratic certificates.
- The exact third-moment frontier was computed twice over the complete
  `320292000`-vector normalized universe. The primary direct-convolution run
  is Modal app `ap-FWSBvehhSdCJZMGDPjEIab`; the folded audit is
  `ap-TNwtdWkeeVT1yPGaLxB941`. Their row populations, minima, and maxima agree
  exactly.
- The independent counts-only run `ap-OKdHMMiWlBCDidicP9701v` agrees with the
  third-moment packets through `V=162`; it is corroborating rather than
  load-bearing.
- The exact field-floor comparison is performed after raising to the fifth
  power.
- Normalization is used only for a zero/nonzero existence statement. No
  normalized count is interpreted as a weighted class-pair count.
