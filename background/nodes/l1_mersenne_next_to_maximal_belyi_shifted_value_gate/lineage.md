# Lineage

This transports the rational-derivative and auxiliary-fiber product methods
that closed the constant `m=4,h=3` branch. The new point is that the method
works uniformly for `m=8,16` once both Belyi projective invariants descend to
the prime field; it records the failure of that descent as the exact residue.
