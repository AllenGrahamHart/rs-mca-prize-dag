# Claim contract

| field | value |
|---|---|
| claim | The shifted split-value polynomial divides `W^n-1` and obeys the printed differential divisibility; the `z=0` and every prime-field-normalized Belyi passport are empty. |
| status | PROVED |
| dependency | `l1_mersenne_next_to_maximal_exceptional_reduction` |
| exact rows | Four `m=8,h=7` rows and one `m=16,h=15` row. |
| open residue | `z!=0`, and at least one of `z/r_0` and `2a/(r_0z)` is genuinely outside `F_p`. |
| excluded scope | Lower `h`, `m=4,h=2`, wider widths, and full L1. |
| consumer | `l1_mixed_petal_amplification` |
