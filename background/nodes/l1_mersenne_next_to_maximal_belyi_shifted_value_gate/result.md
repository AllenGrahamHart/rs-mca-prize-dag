# Result

Every next-to-maximal survivor has a degree-`h` shifted-value polynomial
dividing `W^(m(p+1))-1` and satisfying a constant weighted-derivative gate
at every nonzero root. The entire nonzero prime-field-normalized Belyi
branch is empty. The endpoint residue now has genuinely non-prime-field
normalization; the separate `z=0` chamber is empty as well.
