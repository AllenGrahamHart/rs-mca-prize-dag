# Affine-plane cap 218 and sharpened high-dimensional routers

- **status:** PROVED
- **scope:** the scalar-dimension-three/four quotient pair-pencil branches

No affine scalar plane contains 219 selected quotient types. Hence the
affine-plane occupancy cap is

```text
218.                                                   (P218-1)
```

Consequently:

1. in scalar dimension three, the common received-pair core has size

   ```text
   |J|>=407831;                                        (P218-2)
   ```

   reversible shortening at the minimum floor gives

   ```text
   (n',K',m')=(1689321,640745,708217),
   pair-core size s'=708215,
   218n'-520s'=178;                                   (P218-3)
   ```

2. in scalar dimension four, either the same `407831` common-core floor and
   shortening hold, or one actual noncommon coordinate belongs to at least
   219 quotient pair cores. Those types have affine span exactly three and
   own at least

   ```text
   219*29=6351                                        (P218-4)
   ```

   first-owned records whose supports contain that coordinate.

## Falsifier

An affine plane with 219 selected types; failure of the exact line-packing
contradiction after plane-specific shortening; a dimension-three common core
below 407831; or a dimension-four counterexample to both sharpened outputs.
