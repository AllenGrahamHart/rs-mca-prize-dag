# Result

Pricing all contained nine-subsets moves the exact kernel exclusion boundary
from `K'=15445` to `K'=15670`. The remaining rank-eleven interval ledger is

```text
10..15670       rank eight only,
15671..22525    rank eight plus kernel,
22526..37995    dense-owner chronology plus kernel,
37996..1048576  kernel only.
```

At the new wall, coranks 1 and 2 are both fractional and the two shadow
resources are exactly tight. Smaller-shadow totals alone do not cut this
optimizer.
