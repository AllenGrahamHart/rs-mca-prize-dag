# Frontier

Do not recompute pairings `1` or `2`; they are paid by the common first-pair
cut. The next new matching is `3`, whose first pair is `(0,2)`.

Before launching a norm, classify the canonical matching orbits under
exchange of the two positive parallel `DE` copies and under the target-lane
sign action. Then choose one orbit representative whose first pair gives a
source-only or target-free eliminant in the existing four-basis tower.
