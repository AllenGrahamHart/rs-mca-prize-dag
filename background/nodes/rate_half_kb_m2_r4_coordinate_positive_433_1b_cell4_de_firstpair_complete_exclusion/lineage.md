# Lineage

- Matching-zero `xi=0` and `xi=2` are exact four-basis norm exclusions.
- Matching-zero `xi=1` is exact parallel-copy transport from `xi=0`.
- This node observes that canonical matchings `0,1,2` share the same first
  pair and composes the three parent exclusions without new computation.
- The eventual consumer remains `rate_half_band_closure`.
