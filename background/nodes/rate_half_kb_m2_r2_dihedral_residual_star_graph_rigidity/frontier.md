# Frontier

The source-star defect gate is now exhausted on the full-V4 residual: both
`n=3` and `n=6` attain defect zero with a unique graph shape up to relabeling.
The next attack is the realization problem for the birational quartic
coefficient map

```text
X -> [H_2(X):H_1(X):H_0(X)].
```

It must pass through the 24 prescribed pair quadratics encoded by the
printed graphs while respecting the quadratic source fibers and the
genus-zero/genus-one V4 branch passport. A proof should either show that
this forces the already-excluded line/conic image, or construct and then
test the finite residual normal forms. More defect counting cannot help.
