# Claim contract

- **Claim ID:** `rate_half_kb_m2_r2_dihedral_residual_star_graph_rigidity`
- **Input:** an actual full-V4 `m=2,r=2` residual with dihedral factor
  degree `n=3` or `n=6`.
- **Output:** the complete 24-unit source-star graph is respectively two
  copies of `K_(2,2,2)` or the two-point blow-up of `C_6`; every weight is
  one and every source-label degree is four.
- **Dependencies used:** regular dihedral coset incidence, the normalized
  source cross-edge lemma, `c eta c^(-1)=eta*a`, the preserving lift
  `(tau,b)`, and the complete-source divisor identity.
- **Not claimed:** existence or nonexistence of either profile, deletion of
  an `m=2` type, an owner or payment, or any Prize-row close.
