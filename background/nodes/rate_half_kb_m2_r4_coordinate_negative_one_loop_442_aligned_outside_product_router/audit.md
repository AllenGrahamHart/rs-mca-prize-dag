# Audit

1. Two distinct common negation pairs force the global involution to be
   negation, not merely an arbitrary matching of outside values.
2. Product injectivity prevents the forced `b^2` from using a member of an
   already complete outside pair.
3. Every pairing of the residual `S0` and `S1` singleton values is exhausted.
4. The two surviving `S1` branches retain all four edge signs.
5. The quartic is recorded only as a consequence, not as a sufficient
   replacement for the three branch equations.
6. Independent guarded `F_73` witnesses show that both retained skeleton
   types occur at the product level.
