# Claim contract

| field | content |
|---|---|
| claim | Negation is the forced product involution; `S0` is empty; `S1` passes exactly through `(KB41R-2)`; `S2` passes exactly through `(KB41R-4)`. |
| assumptions | Either guarded aligned common-`K` family, one of the three exhaustive outside skeletons, and product injectivity. |
| exact interface | One forced outside value `b^2` plus three negation pairs, with the signed products printed in the statement. |
| nonclaim | Outside q equations, fixed source placement, full interpolation, other common matching orbits, coordinate or row closure, or either Prize theorem. |
