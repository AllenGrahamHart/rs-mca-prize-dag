# Lineage

This is the first weld between the exact aligned common-`K` atlas and the
complete outside graph census.  It converts a generic seven-record search
into two `S1` branches and one `S2` branch.
