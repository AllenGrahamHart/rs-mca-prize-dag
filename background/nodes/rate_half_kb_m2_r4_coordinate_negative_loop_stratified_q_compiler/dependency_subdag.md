# Dependency sub-DAG

```text
complete-fiber Vieta compiler ----\
                                   -> loop-stratified q compiler --ev--> band closure
negative loop-budget gate --------/
```

Both incoming edges are requirements.  The outgoing edge is narrowing
evidence until every signed survivor is classified.
