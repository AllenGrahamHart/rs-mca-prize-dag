# Audit

1. The matrix uses only nonloop rows; loop rows have already been consumed
   as factors of `A_1`.
2. The columns for `C` carry the factor `R_Lambda(kappa)`.
3. The determinant condition alone does not enforce `B_2` leading support.
4. Edge-orbit orientations change signs of the nonzero `q_kappa` and must
   be retained in a signed packet classifier.
5. The gate concerns the five common fibers only; complete-product,
   complement-fiber, irreducibility, and outer-factor checks remain.
