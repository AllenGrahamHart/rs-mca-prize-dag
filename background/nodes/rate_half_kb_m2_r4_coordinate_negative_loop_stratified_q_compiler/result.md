# Result

PROVED.  The five negative multiplicity skeletons now have exact
loop-stratified q determinants of sizes five, four, or three.  The two-loop
rows reduce to a `3 x 3` signed determinant with `A_1` already pinned.
