# Lineage

The parent cell-14 theorem exposed a global quadratic-cover model but retained
the full outside ledger.  Dense five-variable, quotient, and latent-root
prototypes timed out.  Reducing every outside equation immediately in the
quadratic pair algebra exposed a target-free compatibility equation exactly
when a `de` record is missing and the two residual `de` records are paired.
This node banks the resulting complete subfamily before attacking the
remaining target-dependent cases.
