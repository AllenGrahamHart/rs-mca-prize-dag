# Frontier

The target-free linear-pair route is exhausted. The sibling rank-one target
projection now excludes all 960 cases with missing record `df`, `ef`, `bf`,
or `cf`. The downstream fixed-a chain theorem excludes 432 more cases. The
current retained frontier has exactly 144 cases: a `de` record is missing and
the matching index is `6`, `7`, or `8`.

Do not rerun the dense full-block ideals. Keep the fixed `de` value and use
the rank-one relation before the final all-mixed elimination.
