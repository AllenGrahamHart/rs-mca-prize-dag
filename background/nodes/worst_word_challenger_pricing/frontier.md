# Frontier audit: worst-word challenger pricing

Status: HISTORICAL CONDITIONAL; no longer on the prize requirement path.

The direct upper premise `ww_row_envelope_clause` remains open but is retired
from the prize path. At the explicit admissible unsafe spending cell, six
plants plus one quotient-fiber non-plant exceed `B*=6`. The counterexample is
within the layout, scalar, field, subgroup, and background quantifiers, but
outside W3's literal safe-side scope.

The useful positive result survives: `ww_lower_witnesses` gives the exact
qcore unsafe witness at every clean-rate row where that column is spent.  The
clean-rate adjacency consumer now uses this lower-side content through
`list_unsafe`, while its upper side comes independently from `list_safe`.

The old implication "if W3 and W2 then the floor" remains a formally valid
historical conditional, but it is not consumed. QA.22 remains MCA calibration
only. No replacement red is owed in this node; the live list leaves are
`l1_mixed_petal_amplification` through
`list_safe` and `rate_half_list_adjacent_crossing`.
