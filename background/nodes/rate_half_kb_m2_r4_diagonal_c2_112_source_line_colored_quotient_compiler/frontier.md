# Frontier

Split the quotient attack into the aligned case `chi=P_(J_1)` and the
near-aligned case `chi=P_{xi,ell}`. In each, combine `(KBQ2-2)` with the
`4/3` or `6/5` square-fiber coefficient cut and the twelve source-line edge
orbits. Do not enumerate arbitrary four-root colored divisors.
