# Result

PROVED.  The first deployed-field cell-5 common-curve pilot now has a compact
branch-complete interface:

```text
generic:      b=-a0/a1 plus two equations in (x,r,t)
exceptional:  a0=a1=0 plus the two remaining quadratics.
```

The generic eliminants have `244` and `340` terms, replacing the failed raw
four-variable exact basis attempt.  The exceptional branch and every outside
condition remain open.
