# Lineage

Finite probes repeatedly left cell `5`, positive cycle sign, as a difficult
source-side survivor.  Its deployed localized common ideal had dimension one,
but raw exact determinants and a generic exact standard basis both timed out.
Split-prime reconstruction exposed small Gaussian coefficients and the safe
ratio `x=c/b`.  The final theorem discards that heuristic trust path and
replays the reduction directly over the deployed field from the proved Vieta
compiler.
