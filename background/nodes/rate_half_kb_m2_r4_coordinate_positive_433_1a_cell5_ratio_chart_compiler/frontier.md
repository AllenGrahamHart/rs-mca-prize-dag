# Frontier

1. The explicit denominator branch `a0=a1=L1=L2=0` is now closed by
   `rate_half_kb_m2_r4_coordinate_positive_433_1a_cell5_ratio_exceptional_branch_exclusion`.
2. On the remaining generic branch, reduce the universal four product equations before
   the seven squared-sum equations in the two-eliminant `(x,r,t)` ring.
3. Preserve source-deck distinctness and replay unsquared `q` signs before any
   cell deletion.
4. Transport the ratio coordinate only through proved cell/sign symmetries;
   otherwise compile the remaining live charts separately.

Do not retry the raw exact determinant or ambient seven-variable basis.
