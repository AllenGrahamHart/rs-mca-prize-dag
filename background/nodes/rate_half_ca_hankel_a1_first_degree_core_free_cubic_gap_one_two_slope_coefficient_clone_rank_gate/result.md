# Result

The union excess of any two exact error supports bounds the complete
parameter-coefficient rank on their difference. Minimum-union pairs force a
literal clone class of `c_alpha+1` proportional domain-row forms.
