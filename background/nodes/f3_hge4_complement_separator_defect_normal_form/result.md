# Result

The retained HGE4 exact-level widths are stratified by an exact separator
defect `e=m-3h`. The near-third boundary is now a linear/quadratic
differential classification problem; larger defects remain in the aggregate.
