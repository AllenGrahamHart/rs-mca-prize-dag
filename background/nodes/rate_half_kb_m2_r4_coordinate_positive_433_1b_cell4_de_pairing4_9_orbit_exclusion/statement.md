# Statement

The proved matching-4 exclusion and the exact parallel-`DE` involution exclude

```text
(0,4), (1,4), (2,4), (2,9)
```

in positive `433-1b` role cell 4. These are four labels in two quotient
orbits, hence

```text
4 labels * 4 source signs * 4 target lanes = 64 raw cases.
```

Together with the already-proved first-pair and pairing-3/6 blocks, 19 of
105 labels in 11 quotient orbits are paid. The live ledger is therefore 86
labels in 49 quotient orbits.

The involution does **not** send `(0,4)` or `(1,4)` to matching 9. The
positive-`DE` labels `(0,9),(1,9)` remain open.
