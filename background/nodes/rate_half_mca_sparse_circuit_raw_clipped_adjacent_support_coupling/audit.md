# Audit

The primary verifier fixes the lower-support total and allocates it in
increasing `b_i/a_i` order. The independent verifier fixes the upper-support
total and allocates in increasing `a_i/b_i` order. They agree on the exact
K'=87 `(u,g,d)=(34,6,5)` specialization, including the nonintegral optimum
with denominator three.
