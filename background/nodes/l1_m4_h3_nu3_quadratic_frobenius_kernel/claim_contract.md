# Claim contract

- **claim id:** `l1_m4_h3_nu3_quadratic_frobenius_kernel`
- **status:** PROVED
- **scope:** the clean `nu=3` case of the four official
  `n=4(p+1),h=3` first-checkpoint rows
- **proved content:** existence and uniqueness of the canonical integral and
  quadratic Frobenius-kernel normal form `(QFK2)--(QFK4)`, including
  `q_2^p=a` and the constant relation `(QFK5)`
- **dependencies used:** Cartier exactness/zero defect and the Euler quotient
  degree ledger
- **nonclaim:** no `nu=3` emptiness or component classification, no statement
  for `nu=0,1,2`, no nonembedded `h=2` classification, and no L1 promotion
