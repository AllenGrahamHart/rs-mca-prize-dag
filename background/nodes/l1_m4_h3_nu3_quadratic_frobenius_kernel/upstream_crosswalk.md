# Upstream crosswalk - nu=3 quadratic Frobenius kernel

For the upstream split-pencil census, this is an exact wild-kernel normal
form for the clean `m=4,h=3` valuation. The characteristic-`p` ambiguity is
not a degree-`p` polynomial: after canonical integration it is the `p`th
power of one quadratic and its quartic complement.

An upstream `(A)` packet can vendor `(QFK2)--(QFK5)` as a finite-parameter
reduction. It is not an h=3 exclusion, exact SPI count, or finite MCA
payment.
