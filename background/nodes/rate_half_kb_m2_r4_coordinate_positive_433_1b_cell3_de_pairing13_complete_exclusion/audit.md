# Audit

- The source census is the Cartesian product of four source signs, four target
  lanes, and `xi in {0,2}`: 32 rows.
- Every row is complete and has degree tuple `(2,2,8,1)`.
- Direct six-by-six and tower norms agree in every row.
- Candidate parameters are exactly the union of norm and exceptional roots;
  all 544 source points are explicitly listed and unique within their row.
- All 32 boundaries have `f=0`, fail exactly `nonzero_5`, and cover their
  single computed target lane.
- All 64 nonboundary final-pair evaluations are nonzero.
- Witness and unresolved ledgers are empty in every row.
- The theorem counts 32 computed and 16 transported raw atlas cases.
- Primary and independent verifiers pin the compiler and census hashes.
