# Lineage

- The global quotient and compact kernel supply the exact source algebra and
  direct-lift maps.
- Pairing 13 is pairing 8's nested-quadratic geometry with the two residual
  `DE` inputs exchanged between `P_u` and `P_f`; it is independently
  computed rather than transported by an unproved target symmetry.
- Positive parallel-copy transport is the exact residual-record deletion used
  in pairings 0 through 12.
- No target-label exchange is used; that map leaves role cell 3.
- The result remains background evidence toward `rate_half_band_closure`.
