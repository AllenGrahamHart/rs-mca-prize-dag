# Statement

## Claim (KBP1B3-DE-P13-1)

On the guarded positive deployed 433-1b role-cell-3 branch, every atlas case
with a missing parallel `DE` record, `xi in {0,1,2}`, and canonical matching
index 13 is empty for all source and target signs. Matching 13 is

```text
((0,5),(1,3),(2,4))
= (de,sigma_c cf), (second_de,sigma_o ef), (df,bf).
```

The raw scope is 48 cases. Thirty-two exact rows at `xi in {0,2}` cover all
source signs and target lanes, paying 32 cases. Exact parallel-copy transport
from `xi=0` pays the 16 `xi=1` cases.

No other matching, missing role, complete cell-3 exclusion, rate-half band,
LIST, MCA, or Prize result is claimed.
