# Statement

For case

```text
(cell=3,S0,sigma_o=-1,epsilon=(-1,-1),xi=2,pairing=0),
```

take the infinity branch `mip2=miq2=0` for each matching pair `i=4,5,6`.
After adjoining `q3`, `q7`, and these six equations to the proved common
basis, saturation by all admissibility guards and the rank-cofactor ideal is
the unit ideal. Hence this exact projective chart is empty on the admissible
locus.
