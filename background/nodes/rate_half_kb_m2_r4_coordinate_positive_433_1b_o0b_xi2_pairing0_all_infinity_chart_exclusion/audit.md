# Audit

The checker pins the case, all-infinity mask, seven-variable ledger, six chart
equations, 40 guards, and six rank cofactors. Its hostile audit rejects an
incomplete collection, a changed chart mask, and a missing chart equation.
The node verifier additionally pins every source hash and the transition from
`SAT=4,DIM=3,SIZE=22` to `SAT=5,DIM=-1,SIZE=1`.
