# Result

`PROVED`: cofactor `256` is impossible in the prize profile
`(3,6,S=18)`. Six profile cofactors remain:

```text
2, 4, 8, 16, 32, 64.
```

The next shortest proved product window is `m=64`, with `V<=130` or
`E<=65` and multiplicity six.
