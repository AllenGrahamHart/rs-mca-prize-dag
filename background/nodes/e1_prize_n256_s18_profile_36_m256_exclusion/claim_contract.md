# Claim contract

## Imported facts

- Cofactor 256 forces multiplicity eight in the six-singleton parity support.
- The energy-adaptive parent leaves `E=2,...,23`.
- Affine normalization and global-sign normalization are surjective.
- The bounded-product extremum and adaptive cap may be specialized at exact
  autocorrelation energy `E` and `L1` norm `L`.

## New theorem

Exact product certification, a complete multiplicity-eight affine atlas, two
independent radius engines, and dual exact resultants exhaust every live
cofactor-256 vector. All 54 exact quotients lie below the prize interval, so
the cofactor-256 collision class is empty.

## Exclusions

1. The radius equations are necessary filters followed by exact integer
   autocorrelation replay; they are not treated as sufficient conditions.
2. The block index is complete by an `r+1`-block pigeonhole argument, not a
   heuristic nearest-neighbor search.
3. No free group action is assumed in affine normalization.
4. This removes one cofactor, not the full profile or aggregate pair budget.
