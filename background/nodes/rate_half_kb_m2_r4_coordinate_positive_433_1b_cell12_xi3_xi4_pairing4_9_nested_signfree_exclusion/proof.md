# Proof

After deleting `DF`, matching `4` pairs the residual products as

```text
(de,-de), (de,bf), (sigma_o ef,sigma_c cf).
```

Write `m=df`, `S=(d+f)^2`, `z=1/d`, `y=z^2`, and `q=de`. The missing Vieta
equation is

```text
M(y)=1+(2m-S)y+m^2y^2=0.                          (RS12-9)
```

The first pair `A(q)=paired(q,-q)` is even, hence quadratic in `u=q^2`.
Multiply the second pair `B(q,z)=paired(q,bmz)` by `B(-q,z)`, rewrite in
`u,z`, and reduce modulo `A`. Its linear-in-`u` remainder gives a
division-free polynomial in `z`. Reducing modulo `M(z^2)`, separating even
and odd powers, and multiplying the two signs leaves a polynomial in `y`.
Its remainder modulo `M` is linear in every source-sign row. The resulting
division-free common-root cut is normed through the exact basis `1,t,b,bt`.

A pinned AST adapter applies the previously audited nested sign-free
compiler to the cell-12 tower and compact kernel without modifying the
compiler function. Four rows cover all source signs. Every deployed root of
all norm and inverse numerators and denominators is included. The 72
case-labeled roots replay to 120 guarded source points. The boundary ledger
has 20 `r`-guard exits, 16 `t`-guard exits, and four cell-12 `b`-leading
exits; the last are in the complement paid by the rational-boundary parent.

Direct replay evaluates the original missing equation and both first pair
equations. It retains eight compatible `(z,q)` lifts. At each lift all four
target lanes evaluate the final pair

```text
paired(sigma_o ef,sigma_c cf).
```

An independent audit reconstructs the source kernel and all three paired
equations at every lift. All 32 final values are nonzero. There is no
witness, final boundary, or unresolved branch.

Exchanging the identical positive `DE` records maps matching `4` to
matching `9`; outside `D/E` transport maps missing `DF` to missing `EF`.
The exact generic orbit quotient proves both transports and closes all four
labels. QED.
