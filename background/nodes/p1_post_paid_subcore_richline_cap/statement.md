# p1_post_paid_subcore_richline_cap

- **status:** PROVED
- **closure:** proof
- **refs (legacy repo):** ['experimental/notes/roadmaps/e33_deep_link_staircase.md']

## Statement

Codex P1 (CONDITIONAL, replay pending PR integration): the exact near-k subcore reduction of the deep-link staircase is PROVED, but the raw per-subcore partner cap is FALSE before paid stripping — rich lines (structured configurations through a subcore) can carry many partners and must be charged first. Remaining statement: after the paid strip (tangent/quotient-with-tails/dihedral), partners through any fixed near-k subcore number <= C (E33 observed <= 5, density-independent, in post-organic data — consistent with the strip removing the rich lines).

## Attack surface

characterize rich lines through a subcore (they should be exactly the staircase/pencil families — the paid classes); the post-strip cap then follows from the same interpolation rigidity that bounds tame partners; E33's data calibrates C

## Falsifier

a post-strip toy subcore with > C partners (extend E33's scan with a strip filter)

## Ledger (migrated notes)

P3 (10/10): the reduction's residue is NAMED — p3_affine_net_richline_residue (multi-direction affine nets in the subcore plane); the strip-first route alone is insufficient; see the residue node's two routes. | ASSEMBLABLE (post-F3): P1's subcore reduction (proved) + the charged affine-net residue (U3, proved) => the post-strip cap follows; remaining work = the assembly packet writing the two together with E33's constants.
