# Frontier

The rational branch no longer requires a census of arbitrary `F(X)` lines.
It requires a base-field census of at least 520 distinct polynomials in a
space of dimension at most four, with every listed difference split at
least 134940 times on the official domain. Common content and its chronology
must be priced rather than discarded.
