# Dependency sub-DAG

```text
quotient endgame rank_F(X){(1,a_p,b_p)}<=2
  + four-dimensional correction space
  --> determinant identity
  --> fixed coprime direction (U,V)
  --> dim_F span{R_p}<=4

quotient cores |H_p|=m-2
  --> H_p intersect H_q subset Z_D(R_p-R_q)
  --> at least 134940 roots per listed difference
  --> base-field-normalized rank-one support census [OPEN]
```
