# Conditional proof

The proved Galois/norm occupancy dictionary identifies every occupied
cofactor-514 ideal in a fixed row-root fiber with a factorization

```text
(alpha)=P_r(1-zeta_256)Q_s.
```

The conditional theorem
`e1_profile018_qzeta128_class_descent_two_ideal_bound` applies uniformly to
these factorizations. Under
`e1_qzeta128_p257_class_orbit_certificate`, it gives at most two occupied
ideals. Since `2<=5`, the stated payment premise follows.

No energy or magnitude-profile filter is needed for this implication.
