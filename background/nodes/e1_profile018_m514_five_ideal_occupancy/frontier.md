# Frontier

Current exact inputs:

```text
primitive ideals above 257:          128
live energies:                       5,...,10
live magnitude profiles:             10
local parity multiplicity:           2
payment threshold:                   at most 5 occupied ideals
class-descent conditional bound:     at most 2 occupied ideals
```

The profile-independent descent to `Q(zeta_128)` is now complete. Its sole
open premise is the exact class-orbit certificate for the 64 primes above
257. Bernstein's published S-unit coordinates predict 64 distinct classes;
the required independent class-group replay is registered separately.
Pricing the stronger bound gives 12 full profile orbits and next-profile cap
1971, rather than the conservative five-ideal cap 329.

The Hermite moment refinement removed `(9;1,2,0)` and `(11;7,1,0)`, and the
cubic relation refinements removed `(10;6,1,0)`, `(11;11,0,0)`, and
`(12;12,0,0)`, without enumeration. The first route-deciding question is whether one exact official-admissible
prime quotient occurs in six realizable diagonal Galois orbits. If so, repair
the weighted route. If not, turn the norm-multiplicity obstruction into a
proof; a failed mod-257 screen alone is not enough.

The former energy-twelve route-cut target is now rejected profile-wide by the
parity-split cubic norm theorem. Coefficient realization remains the first
missing selective gate on the ten live profiles.

The former 16-shard low-energy search is no longer the first authorized
compute. The route-deciding task is the bounded n=64 class-orbit certificate;
profile enumeration is only a fallback or adversarial audit.
