# Audit

- The catalogue is applied to the outer degree-15 map, not the inner
  degree-four map.
- Only indecomposable outer maps are assigned primitive monodromy.
- A decomposable outer map is routed before any subdegree claim.
- Degree 20 is excluded by the existing exhaustive source-fiber theorem;
  it is not silently omitted because it was absent from the live list.
- The remaining `A6/S6` action really contains a `5^3` element, so the pole
  profile does not close it.
- No status beyond the three-type cut is promoted.
