# Claim contract

## Consumed content

- The profile-(2,10) payment supplies residual cap `39193` and identifies
  `(1,14,S=18)` as the active profile.
- The square-mass-18 split-prime router and height leaves supply the cofactor,
  ideal-family, and one-orbit-per-family arguments.
- The cofactor-1538 and cofactor-1028 energy leaves supply norm statements
  depending only on square mass, local valuation, and autocorrelation.
- The exact dictionary supplies the next profile and payment weights.

## New proved content

1. Fourteen singleton exponents realize every local valuation one through ten
   allowed by the cofactor ceiling.
2. The live cofactor and ideal-family partition is the same thirteen-family
   partition as for profile `(2,10)`.
3. Cofactor `1538` is empty and cofactor `1028` is confined to energy four.
4. Emptying that one certificate gives `T_114<=138`, safely below the current
   cap, and yields the exact next residual and cap `3994`.

## Guards

- The node does not declare the energy-four certificate empty.
- Transport is used only for proofs whose formulas contain square mass,
  local valuation, and autocorrelation, not coefficient-profile counts.
- All 128 cofactor-`514` ideals remain charged in the payment trigger.
- The parent E1 target remains open.
