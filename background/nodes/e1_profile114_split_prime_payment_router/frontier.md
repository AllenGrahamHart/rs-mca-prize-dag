# Frontier

The serial E1 obligation is now one exact certificate:

```text
square mass:                  18
local valuation:              2
cofactor:                     1028=4*257
autocorrelation energy:       E=4
fixed-root signed types:      8385
expected exact verdict:       every Norm/1028 below p_min
```

The single-container CRT Modal launcher is already prepared. Once its
checkpoint is complete and independently replayed, promote the universal
energy-four exclusion, pay profile `(1,14)`, and move immediately to
`(0,18,S=18)`. Do not spend serial effort closing cofactor `514` first.
