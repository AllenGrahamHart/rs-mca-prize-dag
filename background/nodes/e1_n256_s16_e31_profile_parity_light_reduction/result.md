# Result

`PROVED`: at `V=62`, exact slack gives `L<=17`; the cubic cutoff is
`M_3=1302`; and parity reduces 15 integer profiles to `(3,7)`, `(2,5,1)`, and
`(1,3,2)`. Their common light geometry has exactly 960 normalized supports and
eight affine odd-unit orbits, all repeated-distance wedges with one light
diameter.

This reduction does not itself exclude the three profiles. Its child joint
exclusion consumes the eight-template router and closes all three.
