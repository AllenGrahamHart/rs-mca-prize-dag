# Claim contract

- **Claim (template):** order-3 tuple-sharing in the (BIV-CURVE) lane is a
  Lüroth-lattice pullback with `deg_w = 3` exactly and zero waste (maximal
  sharing free in the budget); the demand arithmetic makes supply meet
  demand only at `m = 3`; and the constant-norm mechanism structurally
  forces lines through `>= 8` split cubics at cost `1/N`, not `1/q`. The
  headline `m = 4` object remains OPEN (node-level POSED), with the live
  route now DOOR B (round-38 state).
- **Dependencies (REQUIRES):** `f_weight2_inverse` (GLOBAL PULLBACK
  Theorem 1) and `payment_completeness` (the Lüroth-lattice unification) —
  the banked machinery (LUR) consumes.
- **Output:** the template, the demand table with its three named text
  defects (one ruled not-a-defect), the constant-norm supply census, the
  MISS-2 raw-lines vs verified-fibres separation, and the honest state of
  the `m = 4` gap.
- **Consumer:** `rate_half_band_crossing_location`.
- **Nonclaims:** no `m = 4` witness; no completion or bivariate gating at
  drafting (the round-38 pipeline run is cited, not claimed); the
  flat-supply death regime is CONDITIONAL on measured supply; sporadic
  sharing is closed UPSTREAM (round-38 dichotomy), cited not re-proved;
  (OUT-m)/(DEG-m) inheritances stay POSED.
- **Falsifier:** an `m = 4` eleven-merge non-degenerate object (would
  close the gap positively and falsify C38); a waste computation `!= 0`; a
  norm class deviating from 651; a field with the fix-one-root line
  construction failing to reach 8 members.
- **Replay:** `tools/ramguard local -- python3
  background/nodes/rate_half_share3_luroth_template/verify.py` and the
  combinatorial audit `verify_audit.py` (tiny).
