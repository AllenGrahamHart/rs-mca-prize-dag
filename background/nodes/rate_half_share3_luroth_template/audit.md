# Audit

1. Node-level POSED is deliberate: the headline object does not exist, and
   the proved internals (arithmetic, mechanism, calibrations) are listed
   per component. Nothing inheriting (OUT-m)/(DEG-m) is upgraded.
2. The draft's three text-defect findings are preserved, with D7's
   round-37 ruling (not-a-defect; the display stands m >= 7-scoped)
   recorded inline rather than silently resolved.
3. The MISS-2 guard is structural in the statement: raw line supply and
   structurally-verified complete fibres are declared different quantities
   in both directions.
4. Two sections were stale at wiring and now carry dated updates rather
   than rewrites: the m=4 gap (demand minimality, Door A/B, the 14-ceiling
   correction, C38) and sporadic sharing (closed by dichotomy; the
   less-than-1e-4 price withdrawn as 11 dex optimistic). The draft's
   original text is left visible where it was superseded, because the
   supersession history is itself banked evidence.
5. `verify.py` replays the draft's arithmetic and the q = 193 census;
   `verify_audit.py` shares no code path — the equidistribution is proved
   by full enumeration as a Z/64 fact, the demand table is re-derived from
   printed definitions, and the line mechanism is exhibited at a fresh
   field with collinearity checked against the closed form.
6. The source bank's compliance censure is carried in provenance per the
   standing brief rule, explicitly marked as not bearing on the
   mathematics.
7. Edge character: evidence to the crossing node + two requires edges for
   the banked Lüroth machinery. No critical requirement discharged.
