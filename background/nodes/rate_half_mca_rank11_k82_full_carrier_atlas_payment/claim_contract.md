# Claim contract

- Exact row only: `K'=82`, `q=72`, `m=67554`, `n=1048658`.
- Exactly the 49,900 above-ceiling tuples represented by the canonical digest
  are rerouted through the complete pairwise atlas.
- Every carrier, high-support, and geometry branch is retained.
- Counts and comparisons are exact integers; no sample is used.
- This node closes `K'=82`; it makes no claim about `K'=83`.
