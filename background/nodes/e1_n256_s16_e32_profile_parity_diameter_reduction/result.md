# Result

`PROVED`: the `V=64` endpoint has `L<=18`, exact cubic threshold `M_3=1517`,
and only three surviving magnitude profiles:

```text
(4,7), (0,8), (3,5,1).
```

Even energy forces zero or two light-light diameters and
`D_64 in {0,2,4,8,12,16,18,20}`. The three residual profiles remain open.
