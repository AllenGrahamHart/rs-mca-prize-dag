# Audit

The quotient remainder is denoted `rho_0`; the inherited structural variable
is denoted `R_c` during reconstruction and remains `R_0` in the original
system. They are not identified.

The simplified `Z_R` equation uses `Z_D=0`; the proof records their exact
residual difference, so the three structural filters are asserted jointly,
not independently.

All denominator factors are retained as saturations. The univariate filters
do not include the role, `P_4`, or arithmetic-lift obligations and make no
common-root or emptiness claim.
