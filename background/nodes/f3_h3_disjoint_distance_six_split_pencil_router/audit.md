# Audit

- Expanded the quadratic and cubic coefficient identities independently.
- Kept the `cX` shift distinct from the constant-shift top stratum.
- Checked the factor eight from endpoint and internal-root orientations; the
  quotient pair is already ordered by `R(t)`.
- The verifier exhausts several small subgroup rows and checks the
  representation bijection, support-distance equivalence, cubic identity,
  quotient line fiber, and orientation factor.
- The node makes no incidence estimate and cannot promote C36'.
