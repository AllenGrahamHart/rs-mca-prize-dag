# Result

The sole H3 analytic endpoint is now posed as a quadratic split-pencil /
affine-line correlation. Every retained edge gives two disjoint split cubics
whose difference is exactly `t(s-r)X`, and undoing canonical orientation
turns the desired rational bound into the integer target

```text
10J_25^0+17J_25^A<=892n^2.
```

Bounding this correlation uniformly remains open.
