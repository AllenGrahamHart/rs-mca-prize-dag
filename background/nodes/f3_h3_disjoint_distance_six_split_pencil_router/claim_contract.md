# Claim contract

- **claim id:** `f3_h3_disjoint_distance_six_split_pencil_router`
- **mathematical statement:** the disjoint distance-six C36' moment is
  exactly a correlation between disjoint split members of the quadratic
  pencil `Q_(t,r)` and an affine subgroup-line fiber; before orientation its
  sufficient bound is `10J_25^0+17J_25^A<=892n^2`
- **scope:** every official H3 row and every retained nonzero target
- **consumer:** `f3_h3_mobius_excess_half`
- **status:** `PROVED`
- **proved dependencies:** low-distance edge-quotient incidence router and
  distance-six support-overlap payment
- **new open content:** a constants-explicit bound for the split-pencil /
  line-fiber correlation
- **falsifier:** one exact row and target for which `(DSP2)--(DSP7)` fails
- **nonclaims:** no point-count estimate, no primitive/quotient
  classification, and no C36' promotion
- **replay:**
  `tools/ramguard tiny -- python3 background/nodes/f3_h3_disjoint_distance_six_split_pencil_router/verify.py`
- **upstream mapping:** base-field-normalized split-pencil census / primitive
  shift-pair control, with the coefficient shift pinned to `cX`
