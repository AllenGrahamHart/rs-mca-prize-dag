# Statement

## Claim `(KBP1B3-CURVE-1)`

In deployed positive `433-1b -> O0a` role cell `3`, the singleton is `AB`
and the paired roles are `(LA,AC)` and `(BC+,BC-)`.  On the
product-rank-five branch and under the printed source/target guards:

1. the common Vieta condition is equivalently generated by three compact
   `7 x 7` compatibility determinants;
2. for every one of the four source-sign pairs and all six product-cofactor
   charts, the saturated common locus is a one-dimensional curve with compact
   basis size `21`;
3. within each source-sign row all six charts have byte-identical two-variable
   projections, of shapes `(degree,terms)`

   ```text
   (t,r): (6,14),   (r,b): (16,58),
   (b,t): (19,106), (r,c): (13,49);
   ```

4. all four projection polynomials are irreducible over `F_2130706433`; the
   three projections involving `b` or `c` are reciprocal and reconstruct
   exactly from their reciprocal-coordinate quotients;
5. the product-kernel beta scale has empty intersection with every guarded
   common chart after the boundary intersection is re-saturated by the route
   and selected product-cofactor guards; and
6. the resulting eight-coordinate polynomial kernel is independent of the
   source signs, annihilates seven Vieta rows identically, and annihilates the
   remaining three rows modulo the compact common ideal in all four sign rows.

This is a structural theorem.  It does not exclude the common curve, decide an
outside signed-edge ledger, close cell `3`, close `433-1b -> O0a`, or prove K3,
LIST, MCA, or either Prize problem.
