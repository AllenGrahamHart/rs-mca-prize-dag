# Result

The fourth forbidden coefficient adds a second linear equation for the
intermediate scalar. On the previous `A=B=0` exception, it selects one scalar,
rejects, or leaves at most two candidates only on the smaller explicit locus
`A=B=C=D=0`.

That exceptional quadruple and the exact downstream candidate checks remain
open.
