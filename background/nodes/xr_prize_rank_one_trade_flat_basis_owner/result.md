# Result

Rank-one trades now feed existing currencies:

```text
spanning support -> persistent basis pencil, cap floor((R-v)/h);
nonspanning support -> proper flat F, |F|<=rk(F)+u.
```

At `u=0`, only the first route exists. The remaining rank-one problem is the
aggregate first-match sum over these canonical basis and flat owners, not the
classification of individual trades.
