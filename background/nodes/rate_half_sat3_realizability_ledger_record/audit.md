# Audit

1. HEURISTIC is the deliberate status and the statement's first section
   explains why (three counting deaths in-lane); no proof.md exists and
   none should.
2. The D9 blocking flag is resolved the strong way: the formula is printed
   with its source, and `verify_audit.py` EVALUATES it at both calibration
   points (+13.75, -0.94, to two decimals) — the check that was impossible
   at drafting. `verify.py`'s original refusal stance is preserved with a
   dated note so the recovery history stays readable.
3. The arithmetic rows are upgraded from point checks to polynomial
   identities in the audit (coefficient tuples over Z).
4. The sign-convention flag is carried verbatim: the two bookkeeping rows
   agree in verdict only after a flip and must never be added.
5. The `-1952 m^2` and `-61.3` figures remain carried-not-recomputed and
   are labelled as such in both statement and certificate.
6. The round-38 wiring note (the T = 3 cell inhabited) is scoped
   carefully: it is a calibration point in the gate's favour at that cell,
   not evidence for any other cell's verdict.
7. Edge character: evidence + one requires edge ((ERC2)). No requirement
   discharged; ZERO POWER over existence declared.
