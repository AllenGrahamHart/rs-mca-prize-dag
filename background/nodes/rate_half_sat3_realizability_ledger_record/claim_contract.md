# Claim contract

- **Claim (HEURISTIC record):** the (SAT3) realizability ledger's state of
  record — the (L2)/(BIV-G) overdetermination quadratic `4m^2-7m+2`, the
  DEF-ID coincidence, the three independent corrections stacking the m=2
  cell to `~+8..+10`, the recovered `C(16m,4m-1)` first-moment gate with
  its m=1 double calibration, and the sign-convention separation of the
  two bookkeeping rows. The ledger's emptiness conclusion is explicitly
  NOT licensed: four instruments, zero mechanisms.
- **Dependencies (REQUIRES):** `rate_half_ca_hankel_exceptional_root_charge`
  ((ERC2), which forces `e = m` and hence the dim-18 ambient — the largest
  correction).
- **Output:** a single citable home for the ledger, its corrections, the
  gate formula (D9 recovery), and the standing warning that a negative
  first moment proves nothing.
- **Consumer:** `rate_half_band_crossing_location`.
- **Nonclaims:** nothing about existence (ZERO POWER); the `-1952 m^2` and
  `-61.3` figures are carried, not recomputed; the two sign-convention
  rows must never be added; the pb_design_ceiling blind spot is live.
- **Falsifier:** a realized (SAT3) family at `m >= 2` (would inhabit the
  cells the instruments call empty — and the round-38 `T = 3` cell shows
  inhabitation does happen where the gate is positive); an arithmetic
  error in the rows (excluded — polynomial identities in the audit).
- **Replay:** `tools/ramguard tiny -- python3
  background/nodes/rate_half_sat3_realizability_ledger_record/verify.py`
  and the formula-evaluating audit `verify_audit.py`.
