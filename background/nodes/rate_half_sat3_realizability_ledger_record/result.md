# Result

The realizability ledger is now a single corrected record: the m=2 cell
sits at `~+8..+10` after two independent corrections (dim-18 forcing and
the automorphism quotient), the round-33 conjecture is doubly re-posed to
`m <= 1`, and the first-moment gate has its formula back with both
calibrations reproducing.

The record's core message is negative discipline: four counting
instruments point the same way and none is a mechanism — the lane's
history (the dead `+4`, the 3400x moment refutation, the inhabited
+62.5-bit cell) is a standing demonstration that ledger verdicts in this
lane must be treated as priors, never as exclusions.
