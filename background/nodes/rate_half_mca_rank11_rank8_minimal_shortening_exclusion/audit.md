# Audit

1. The argument uses the residual row dimension `K'`, not the original code
   dimension or the effective `k+1` lattice envelope.
2. Dense-root saturation supplies both containment `V'<=RS_{<K'}` and the
   exact dimension `dim V'=10`.
3. At `K'=10`, equal finite dimensions justify equality of the two spaces.
4. Nine-point interpolation uses degree at most eight and therefore lies
   inside the degree-below-ten space over every field with distinct domain
   points.
5. The nonzero Vandermonde determinant proves rank exactly nine, not merely
   at least eight.
6. The `K'=11` fixed-chart witness prevents accidental monotone propagation
   of this one-row exclusion.
