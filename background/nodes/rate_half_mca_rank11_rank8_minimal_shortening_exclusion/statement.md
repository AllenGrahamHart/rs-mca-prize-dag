# Rank-eleven rank-eight minimal-shortening exclusion

- **status:** PROVED
- **closed residual row:** `K'=10`
- **first row not covered by this argument:** `K'=11`

At residual dimension `K'=10`, the rank-eight affine-owner alternative of
the component nine-subset target is empty.

The dense-root saturation theorem supplies a ten-dimensional correction
space `V'` inside the residual degree-below-`K'` Reed-Solomon space.  At
`K'=10` these spaces have the same dimension, hence

```text
V' = {f in F[X] : deg f < 10}.
```

For every nine-set `B` of distinct evaluation coordinates, restriction of
this space to `B` has rank nine.  Therefore the target router cannot produce
`rank(ev_B)=8` on this row.

## Sharp boundary

At `K'=11`, the proved eight-petal route fence constructs a ten-dimensional
correction subspace with a nine-set of evaluation rank eight.  Thus the
dimension-equality proof closes exactly `K'=10` and no adjacent row.

## Nonclaim

No rank-eight payment for `K'>=11`, chronology payment, aggregate
rank-eleven payment, active-v4 ledger movement, or prize closure is claimed.
