# Claim contract

## Input

The ten-dimensional residual correction space from dense-root saturation,
its containment in the degree-below-`K'` residual RS space, and the
rank-eight fixed-nine-set alternative of the component target router.

## Output

At `K'=10`, every nine-coordinate restriction of `V'` has rank nine.
Consequently the rank-eight affine-owner target is impossible on that row.

## Nonclaim

The claim does not extend to `K'=11`; the proved fixed-chart fence is an
explicit adjacent-row obstruction.  No remaining rank-eight interval,
chronology terminal, rank-eleven family, active-v4 atom, or prize problem is
closed here.
