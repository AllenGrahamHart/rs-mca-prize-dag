# Attack

The exact remaining lower rank-eight interval now begins at `K'=11`.

1. At `K'=11`, represent `V'` as a hyperplane in the degree-below-eleven
   RS space and classify the circuit of every rank-eight nine-set.
2. Retain all 55 nine-shadows of each rank-ten eleven-set and couple the
   rank-eight mass to the already eliminated rank-nine shadows.
3. Route high-multiplicity owner lines through the guarded dense-owner
   chronology interface; keep the exact line, support, slope, degree guard,
   and pair-noncontainment predicates.
4. Treat the eight-petal construction as a mandatory hostile control.  Any
   proposed fixed-chart cap using only the current local output must reject.
