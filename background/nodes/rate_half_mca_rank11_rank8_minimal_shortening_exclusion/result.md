# Result

The rank-eight affine-owner component target is impossible at the shortest
residual row:

```text
K'=10: rank-eight target empty.
```

Combined with the fixed-kernel closure and the existing rank-eight capacity
and bridge results, the current rank-eleven component boundary is

```text
K'=10:             no component target,
K'=11..22525:      rank eight only,
K'=22526..37995:   dense-owner chronology terminal only,
K'>=37996:         no component target.
```

The row `K'=11` is the honest next lower endpoint.
