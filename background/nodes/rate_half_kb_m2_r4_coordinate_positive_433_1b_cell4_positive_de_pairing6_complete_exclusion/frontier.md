# Frontier

Together with the proved quotient transport of `(xi,pairing)=(2,3)` to
`(2,6)`, all three parallel-DE omissions at matching 6 are now paid.

A separate aggregate node composes pairing 6 with the complete pairing-3
block and updates the live cell-4 quotient ledger.
