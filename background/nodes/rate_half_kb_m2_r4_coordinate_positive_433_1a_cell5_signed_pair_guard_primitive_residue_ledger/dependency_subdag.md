# Dependency sub-DAG

```text
signed-pair generic reducedness
              |
              v
primitive residue ledger --ev--> rate-half band closure
```

The parent proves the primitive coordinate and squarefreeness.  This node
computes its exact irreducible factor ledger.
