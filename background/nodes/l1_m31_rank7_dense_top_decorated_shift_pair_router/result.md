# Result - dense top decorated shift-pair router

Every forbidden proper-`G` zero-excess class forces one member with at least
`215,793` primitive decorated top-stratum neighbors satisfying
`A C_i-B C_j=c!=0` at residual degree `w+1=67,448`. Proving a uniform local
cap of `215,792` for this exact source-bound stratum eliminates the terminal.
Since one projective difference direction contains at most `14` neighbors,
the same class forces at least `15,414` monic degree-`4,980` divisors of one
anchor support locator inside the six-dimensional direction space. A cap
`15,413` for this fixed-support divisor census is also sufficient.

No critical status changes.
