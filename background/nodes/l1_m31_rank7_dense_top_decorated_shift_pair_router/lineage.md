# Lineage - dense top decorated shift-pair router

- Parent: `l1_m31_rank7_zero_excess_two_block_incidence_router`.
- Existing normal form: `l1_growing_cofactor_decorated_shift_pair_compiler`
  and `l1_decorated_shift_pair_gcd_descent_sharpness`.
- New step: the deployed dangerous mass forces a dense graph specifically in
  the sharp reduced branch `e=w+1`, and the top identity itself removes
  common cofactor gcds.
- Successor: prove the local neighbor cap `215792`, or map every such
  neighbor injectively into a chronology-valid upstream owner with exact
  add-back. Equivalently, prove that one fixed support locator has at most
  `15413` maximum-zero divisor directions inside the source direction space.
