# Attack

Enumerate small non-simple matroids with loops and parallel classes, since
those are the cases most likely to expose an incorrect flat-size or witness
multiplicity convention.  Test mutations of `N-B`, `B-r`, and `r+2`.
