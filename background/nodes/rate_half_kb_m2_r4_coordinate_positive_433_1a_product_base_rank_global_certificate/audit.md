# Audit

1. The product-rank claim concerns all six maximal minors, not one selected
   chart minor.
2. The hand proof for cell 3 splits the possibly vanishing factor `b+c^2`;
   it never divides by it silently.
3. The localization guard `H` contains every target antipodal-pair and
   source-label collision factor in the `R,T` coordinates.
4. Unit ideal is proved over the algebraic closure, hence is stronger than
   absence of deployed-field rational points.
5. Cells `2,6,7,8,10,13` are transported only by swapping the identical
   `AB+` records.
6. The hand cell-3 ledger is Modal run `ap-eSnVIfKloKBWzr2XJHqgyC`.
7. The clean three-cell Singular replay is
   `ap-BAjOEqhrDgulBTrCY3YLpq`; its exact hashes and terminal outputs are in
   `rate_half_kb_positive_433_1a_product_base_rank_singular_result.json`.
8. The Debian `singular` package builds a large cached image; reruns should
   reuse it and remain limited to the three declared cells.
