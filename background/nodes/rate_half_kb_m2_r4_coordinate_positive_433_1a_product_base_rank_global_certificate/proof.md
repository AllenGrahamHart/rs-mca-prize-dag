# Proof

The parent theorem proves product rank five in five role orbits.

For cell 3, direct collection of the two degree-seven minors gives
`(KBPBG-1)`.  If `b+c^2` is nonzero, their two residual linear forms in
`b,c` have determinant `U^2-V^2`.  Since

```text
U-V=-4R(T-1),       U+V=-2(R-1)(R+T),
```

this is `(KBPBG-2)` and is nonzero by source-label distinctness.  If
`b=-c^2`, the exact division ledger gives `(KBPBG-3)`, also nonzero.  The
duplicate `AB+` transposition supplies cell 6.

For each of cells `1,9,12`, rank below five is exactly simultaneous
vanishing of the six maximal minors.  Admissibility is exactly `H!=0`.
Introducing `zH-1` therefore represents localization without changing or
losing any admissible point.  The deterministic Singular standard-basis
calculation gives the unit ideal `(KBPBG-5)`, so no such point exists even
over the algebraic closure of the deployed field.  Duplicate-role transport
supplies `2,10,13`.

These six cells plus `[3,6]` and the five parent orbits exhaust all fifteen
matching cells.  The loop sum row is independent of the product block, as in
the parent proof, so the base has rank six everywhere. QED.
