# Result

`PROVED`. The formerly opaque correction

```text
sum_delta q_delta
```

is exactly the nested common-root mass of the first omitted Hankel
recurrence defects. Hence

```text
deg T=e-sum_(r=0)^(n-1)
          deg gcd(H_off,R_(d+1),...,R_(d+1+r)).
```

The next shape-A theorem should bound or classify this gcd flag using the
unique scalar weld and the collision jets. Repeating tangent interpolation
without this source flag adds no information.
