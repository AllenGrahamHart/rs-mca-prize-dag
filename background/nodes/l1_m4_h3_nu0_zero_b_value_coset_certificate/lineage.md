# Lineage

The positive-valuation value certificate used the split values themselves as
fiber products. At zero valuation, the three products are shifted by
`R(0)`. Their ratios still lie in the same Mersenne subgroup and satisfy a
two-term affine relation, which admits the same exact 16-case Frobenius
reduction.
