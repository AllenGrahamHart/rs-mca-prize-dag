# Result

The `nu=0,b=0` branch is empty at `p=8191,131071`. At the two larger
official characteristics it survives only under one scale-free
constant-term invariant. Together with the nonzero-`b` tangent exclusion,
the first two characteristics now retain only the nonzero-`b` eliminant
endpoints. L1 remains open.
