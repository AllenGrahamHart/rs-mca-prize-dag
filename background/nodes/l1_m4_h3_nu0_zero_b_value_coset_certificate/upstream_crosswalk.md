# Upstream crosswalk - nu=0 zero-b value-coset certificate

This is an exact base-field-normalized split-value product census for the
zero-constant cubic arm. It deletes that arm on two official
characteristics and prints the only surviving scale invariant on the other
two.

An upstream `(A)` or exact SPI ledger can consume the 16-case table before
any coefficient census. It is not a full split-pencil classification or MCA
row payment.
