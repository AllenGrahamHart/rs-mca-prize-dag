# Frontier

The scalar-dimension-three split-pencil branch is now bounded on both sides:

```text
4836<=K'<=595763.
```

The lower 87 rows `4836..4922` lie inside the numerical shortening range of
the proved rank-eleven large-shared-pair-core payment. The source-interface
audit does not permit transport: the payment premise covers every
low-margin minimizing pair in the full post-near family, whereas this route
controls the retained synchronized quotient-type subfamily. The DAG has no
inclusion theorem or budget-safe complementary charge bridging those scopes.

Closing these 87 rows therefore requires either a chronology-safe
decomposition that charges every omitted low-margin record, or a theorem
extending the quotient common core to the complete low-margin pair family.
Numerical threshold overlap alone is not a branch payment.

Above 4922, progress requires a stronger overlap/plane synchronization or a
different first-owner payment.
