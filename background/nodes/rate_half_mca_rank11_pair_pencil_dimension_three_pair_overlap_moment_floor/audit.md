# Audit

1. The pairwise cap is applied after shortening: division by the common-core
   locator leaves a nonzero difference polynomial of degree below `K'`.
2. The identity for `sum C(d_x,2)` counts coordinate incidences of unordered
   core pairs exactly; it is not a record count.
3. Balanced integer multiplicities minimize the convex second moment. A
   real-valued Cauchy bound is weaker and does not exclude `K'=4835`.
4. The three floor-average intervals and their transition values are checked
   explicitly; endpoint monotonicity is not inferred from one sample.
5. The upper residual-dimension bound is inherited from the separate
   rich-plane theorem. The second moment itself proves only the lower bound.
6. The 87 rows `4836..4922` numerically overlap the existing shared-core
   payment range, but no source-interface transport is asserted here.
7. The payment theorem quantifies over every minimizing pair used by every
   `theta<=387` record in the complete post-near rank-eleven family. The
   quotient pair-pencil chain instead starts from the synchronized quotient
   types in the triple-owner residual. Its complete-graph and scalar-span
   conclusions are complete only at that retained subfamily's interface.
8. No current dependency proves that every other low-margin minimizing pair
   lies in this scalar family, or gives a disjoint total charge for the
   omitted low-margin records. Therefore even a common core of size
   `K-4922` for all retained quotient types does not satisfy the payment
   theorem's printed premise.
