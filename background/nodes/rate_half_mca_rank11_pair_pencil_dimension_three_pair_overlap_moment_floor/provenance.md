# Provenance

- Local input: the proved dimension-three rich-plane recurrence sharpening.
- New contribution: the exact pair-core overlap second moment, its balanced
  integer minimizer, and the adjacent residual-dimension threshold.
- Computation: deterministic integer replay only; no Modal or external
  computation is used.
- External literature is not a proof input.
- Upstream alignment: the base-field-normalized split-pencil overlap ledger
  in draft PR `#1170`.
