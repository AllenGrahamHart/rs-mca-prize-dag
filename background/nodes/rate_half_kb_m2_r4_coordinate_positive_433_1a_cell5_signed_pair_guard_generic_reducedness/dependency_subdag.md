# Dependency sub-DAG

```text
signed-pair guard stable rank
              |
              v
signed-pair generic reducedness --ev--> rate-half band closure
```

The incoming theorem identifies the 24-dimensional stable image with the
guard localization.  This node proves that image reduced, but does not apply
the colored edge.
