# Result

The fixed-tuple owner constant is linear in affine dimension, not
exponential. This extends the next-dimensional `ell=1` payment by another
`47,026,163` values of `d+1` at rates `1/4,1/8` and `14,845,057` at rate
`1/16`.

The target remains open on the short final `ell=1` tail, higher `ell`, and
the split-pencil endpoint.
