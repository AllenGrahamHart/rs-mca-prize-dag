# Audit

- Expanded the Euler cancellation independently for an arbitrary
  `S=DV^2K`.
- Checked that only one factor of `V` is forced in the generic `q=2` stratum;
  no repeated-factor or derivative-gcd claim is made.
- Checked the exact official degrees and the load-bearing coprimality with
  `TU`.
- The verifier uses tiny symbolic polynomial fixtures and does not construct
  official-degree arrays.
