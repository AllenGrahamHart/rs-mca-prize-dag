# Result

The endpoint incidence ledger determines the norm of the top normalized
split-frame coefficient using only `P_ord(0)` and, in the swapped profile,
the two distinguished slopes. No exceptional roots or dense locator
coefficients are needed.
