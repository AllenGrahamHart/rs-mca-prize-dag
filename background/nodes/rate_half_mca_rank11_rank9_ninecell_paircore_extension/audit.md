# Audit

1. The input records are original-row records, so the support-intersection
   floor is still `2m-n=134944`.
2. Nine fixed coordinates belong to the common plane core.
3. The affine-plane line design and owner-petal disjointness do not require a
   tenth fixed coordinate.
4. Replacing `n-10` by `n-9` adds exactly `981105` to the ordered resource.
5. The next quadratic integer still fails, now by `1655415`.
6. The theorem does not count planes or pay a shared core.

No Modal computation is used.
