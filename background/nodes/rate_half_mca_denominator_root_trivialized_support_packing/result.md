# Result

PROVED: puncture-trivialized supports have disjoint `k`-shadows. The exact
packing bound is more than `2^58` at both deployed parameter families, so
support intersection alone cannot supply the required exception payment.
