# Claim contract

- The result applies exactly to the canonical `xi=2,pairing=0`,
  `epsilon=(-1,-1)` representative.
- It excludes both `FFI` and `FIF` before using any outside equation.
- The proof depends on the previously proved infinity-leading collapse and
  the exact common-ideal packet.
- The unit certificate uses all 16 route guards and all six rank cofactors;
  the first unit stage is specifically `b+1`.
- It does not exclude `FFF` or `IFF`, which do not currently imply
  `k2=k5=0`.
