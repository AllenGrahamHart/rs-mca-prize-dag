# Lineage

Earlier 16-variable and 14-variable outside ideals timed out. The successful
route moved the infinity consequence ahead of all outside equations,
revealed a degree-65 finite common scheme, and removed it at the already
printed `b+1` guard.
