# Frontier

Attack `IFF` through its exact infinity consequence
`(be-cf)z2=0`, split into `z2=0` and `be=cf`. Attack `FFF`
separately; it has no infinity pair and therefore does not inherit the
collapsed-common unit certificate.
