# Sparse-direction heavy-fiber profile

- **status:** PROVED
- **closure:** cumulative deficit and ratio-fiber compiler
- **scope:** any gauged sparse-direction MCA family of bounded transformed
  explanation rank

## Statement

Use the notation of the sparse-direction affine-rank payment.  Thus

```text
q=r_1-b,       E=supp(q),       |E|=e<d,
a_gamma=c_gamma-gamma b,
rank_aff{a_gamma}<=r.
```

For each distinct selected transformed explanation `a`, define its outside
agreement deficit

```text
h_a=m-|{x outside E:a(x)=r_0(x)}|.
```

Then `1<=h_a<=e`.  Put

```text
B_0=0,
B_h=floor(C(R-e+r,r)/C(d-h+r,r))       (1<=h<=e).
```

The number of selected slopes satisfies

```text
|Z| <= P(R,d,r,e)
    := sum_(h=1)^e (B_h-B_(h-1))*floor(e/h).          (HF1)
```

The bound is independent of the ambient shortened dimension.  Its exact
certified paid prefixes improve the preceding scalar-fiber bound to

```text
KoalaBear:   r=12 -> e<=1407; r=13 -> e<=89;
             r=14 -> e<=5;    r=15 -> no e>=1.
Mersenne-31: r=4  -> e<=287;  r=5  -> e<=18;
             r=6  -> e<=1;    r=7  -> no e>=1.
```

For every displayed prefix, the adjacent next value is strictly above the
official budget.

## Nonclaims

This does not force a small support or rank, prove that the displayed paid
prefix is the only paid interval, control interactions beyond cumulative
outside-deficit counts, or close a deployed or prize row.

## Falsifier

A legal selected family exceeding `(HF1)`, an explanation with deficit
outside `[1,e]`, a violation of a cumulative cap `B_h`, or an incorrect
printed adjacent boundary.
