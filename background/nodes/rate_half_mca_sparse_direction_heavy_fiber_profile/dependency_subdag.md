# Dependency sub-DAG

```text
sparse-direction affine-rank payment [PROVED] --+
affine-span list compiler [PROVED] --------------+--> heavy-fiber profile [PROVED]
                                                           |
                                                           +--ev--> rate_half_band_crossing_location
```

The first supplier provides the gauged sparse-direction setting.  The
second is reapplied to nested outside-agreement lists instead of only the
single weakest threshold.
