# Result

`PROVED`: slope multiplicity over one sparse-direction explanation is
governed by its outside-agreement deficit, not merely by the full support
size `e`.  Cumulative affine-list caps yield the weighted profile `(HF1)`.

The first residual cells remain joint middle-rank/middle-support cases, but
the paid prefixes expand from `1144` to `1407` at KoalaBear rank `12`, from
`87` to `89` at rank `13`, and from `282` to `287` at Mersenne rank `4`.
