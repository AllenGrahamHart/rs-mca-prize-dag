# Claim contract

| field | value |
|---|---|
| claim | The generic coefficient core and conic have two exact linear equations in `d`; they eliminate `d` on two rational charts, leaving it only when both linear coefficients vanish. |
| status | PROVED |
| dependency | `l1_mersenne_hnf_m8_order_one_cubic_three_two_one_generic_linear_d_router` |
| consumer | `l1_mixed_petal_amplification` |
| equivalence guards | Inherited `q*d*(q-d)*D*K_6*R*S!=0`; generic `Delta*W!=0`; the relevant nonzero linear coefficient on each rational chart. |
| open residue | Unit or retained-point verdict for the two rational charts and the doubly singular chart, for all 21 role alternatives and every arithmetic-lift filter. |
| nonclaim | `Omega=0` is a necessary parameter equation, not an emptiness or dimension certificate; the `C_1=M_1=0` chart is retained. |
