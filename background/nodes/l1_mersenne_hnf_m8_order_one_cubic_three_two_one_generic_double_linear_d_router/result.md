# Result

The generic `3+2+1` coefficient core now has two linear equations in the
HNF parameter `d`. Both one-coefficient-nonzero charts reduce to three
variables; only the doubly singular `C_1=M_1=0` chart still retains `d`.
No role packet is declared empty.
