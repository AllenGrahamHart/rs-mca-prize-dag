# Lineage

This node continues the hand Euclidean reduction of the generic
coefficient core. It combines the printed sixth coefficient with the exact
`h=7` conic and reduces the result modulo the existing `P_4`. No upstream
theorem or computation is used.
