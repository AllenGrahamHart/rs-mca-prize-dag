# Audit

1. Ordinary plus offsets 1--74 is exhaustive and disjoint after the proved
   support-2/3 carrier-position constraints.
2. The raw scan retains every offset unit before threshold classification.
3. Raw-safe pruning is monotone because every later valid charge only lowers
   the premium.
4. Every unsafe unit enters exhaustive carrier geometry.
5. Best-single pricing uses one valid adjacent edge at a time and assumes no
   overlapping-edge composition.
6. Primary and independent traversals agree on all lane counts and bounds.
7. The safe ceiling and component gap are recomputed with exact integers.
8. No numerical pattern beyond `K'=85` is promoted or used in this proof.
