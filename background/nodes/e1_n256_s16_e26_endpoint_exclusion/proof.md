# Proof

The exact profile/parity/light reduction proves that every pair-feasible
folded-profile `(3,4,0)` vector at `V=52` has one of the ten profiles printed
in the statement. Six use two odd light classes and four use six odd light
classes.

The two-odd exclusion exhausts all six profiles using two complete
actual-vector censuses, the proper-conductor theorem, and independent exact
norms. The six-odd exclusion does the same for all four remaining profiles.
Hence `V=52` is impossible.

The preceding endpoint theorem left only positive even `V<=52`. Removing
`V=52` advances the live frontier to positive even `V<=50`. QED.
