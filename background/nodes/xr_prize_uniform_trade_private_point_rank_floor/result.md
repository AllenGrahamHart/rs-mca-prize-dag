# Result

The complete prize uniform-cell branch is empty through selector ranks

```text
11,243,370;       9,629,972;       2,241,633
```

at rates `1/4,1/8,1/16`. The proof uses only active-block private points,
the pairwise intersection cap, and bounded host-core absorption, so it covers
every trade rank. Above these floors the uniform branch remains open; all
nonuniform cells remain separate.
