# Claim contract

- **Claim:** at an unshared nonreduced correction root, two explicit Hasse
  coefficients are exactly the obstruction to full `D_1` divisibility of
  the canonical divided-row moments.
- **Dependencies:** double-root contact/vertical divisors, exact `Q` and
  `D_1` factors, contracted Pade value, and the Hankel recurrence.
- **Output:** `(HNJ3)--(HNJ6)` and Smith type `[4]` on the vanishing branch.
- **Consumer:** the nonreduced double-root route in
  `rate_half_band_crossing_location`.
- **Nonclaims:** no jet decision and no shared nonreduced-root statement.
- **Falsifier:** an unshared nonreduced packet violating forced order two,
  recurrence propagation, or the equivalence in `(HNJ6)`.
- **Replay:** `tools/ramguard tiny -- python3
  background/nodes/rate_half_ca_hankel_a1_first_degree_core_one_quadratic_gap_four_double_root_nonreduced_unshared_correction_two_jet_gate/verify.py`
  and `verify_audit.py`.
