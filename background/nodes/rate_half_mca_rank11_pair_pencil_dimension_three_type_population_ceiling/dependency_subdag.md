# Dependency sub-DAG

```text
quadratic_quotient_population_router
                 |
affine_plane_cap_218_sharpening
                 |
dimension_three_pair_overlap_moment_floor
                 |
                 v
dimension_three_type_population_ceiling
                 |
                 v
rate_half_band_crossing_location (evidence only)
```

The node reuses the pair-overlap mechanism with the actual type population
`q`; it does not add a payment edge.
