# Dimension-three type-population ceiling

- **status:** PROVED
- **scope:** the scalar-dimension-three output of the synchronized quotient
  pair-pencil branch

Let `q` be the total number of retained first-owned quotient pair types in
the scalar family. Then

```text
520<=q<=3170.                                       (TP1)
```

The retained quotient residual has `M=255011043` chronology-disjoint
records. Consequently one pair type owns at least

```text
ceil(M/3170)=80446                                  (TP2)
```

records.

At the adjacent population endpoint, `q=3170` forces

```text
4960<=K'<=4982,                                    (TP3)
# {x:d_x=218}>=985788.                             (TP4)
```

The value `q=3171` is impossible with exact cross-product deficit
`18372095406`.

This is a population and dense-owner router. It does not pay the dense type,
the shortened family, rank eleven, or MCA.
