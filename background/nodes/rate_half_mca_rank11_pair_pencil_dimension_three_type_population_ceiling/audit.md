# Audit

1. `q` counts the complete retained quotient-type family at this branch,
   rather than an arbitrary 520-type subset.
2. The common-core shortening preserves all first-owned records and every
   retained pair core.
3. The affine-plane cap applies coordinatewise because scalar dimension is
   exactly three.
4. Identity `(5)` is valid at both endpoints `d=0` and `d=218`; it is not a
   real-valued approximation.
5. The pair-incidence upper bound uses residual intersections `<=K'-1`.
6. Cross multiplication uses `P(q)>0` and `q-218>0` throughout the stated
   population range.
7. The 80,446 conclusion is chronology-safe first-owner averaging, not a
   payment or an upper bound on the dense type.
