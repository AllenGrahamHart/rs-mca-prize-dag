# Frontier

The diagonal-socle child deletes the Dickson twist. Build exact coefficient
invariants for the surviving `A5 (3),(3)` and one-parameter `S5` twists in
`(KBA-3)`. For the three descended forms, substitute the canonical outer
coefficients immediately and impose the synchronized diagonal source-star
incidence. Do not re-open the arithmetic/geometric constant-field quotient.
