# Audit

1. The 54.05-percent record fraction follows from averaging incidence
   density; it is not assumed recordwise.
2. The absolute record floor deducts the near charge and eighteen dense
   records exactly once through the preceding theorem.
3. Every component eleven-subset has eleven ten-subsets, giving the exact
   star identity with no independence assumption.
4. A full-rank ten-subset determines one owner curve, so all its component
   extensions use the same owner.
5. In rank nine, the kernel word has already spent ten distinct roots; only
   `K'-11` further roots are available.
6. Agreement on `B` puts both owner-component differences in the same
   one-dimensional kernel. Ownership at `gamma` imposes the displayed
   pencil direction.
7. The uniform constants 22320 and 45153 are evaluated at the correct
   maximum shortening dimension.
8. The three outputs are recordwise and may overlap arbitrarily across
   records.

No Modal computation is used.
