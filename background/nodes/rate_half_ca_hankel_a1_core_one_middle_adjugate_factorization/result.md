# Result

The surviving core-one maximal-degree face now has an exact middle-Hankel
cofactor certificate: every maximal minor is a common linear regular factor
times a product of two residual-kernel coordinates. The unique root of that
factor is the only possible rank-drop and root-omission slope.

This supplies a compact algebraic interface for the mixed rank-at-least-five
component. It does not close the face. No Modal computation was used.
