# Audit

- Exact residue enumeration gives eight multiplicity-ten parity supports,
  all of weight four and all containing a separation of valuation one. This
  proves the `{0,2}` normal form.
- The normalized scan examines exactly `binom(126,4)=10009125` singleton
  supports and finds 32256 of multiplicity ten.
- The low-chord counts sum to 800 normalized supports and canonicalize to 68
  affine orbits.
- The pair-plus-third and triple-XOR engines use different heavy-support
  lookup directions but agree on all target and candidate counts.
- Primary Modal app: `ap-K9JXDDlMQ9euS1aFcApP20`.
- Audit Modal app: `ap-oC0W1HKzJDE4lP3amswBRW`.
- The earlier mixed launch `ap-GRSdTfZF2LJhLQe9ZlurY3` correctly found a
  `mu=2`, energy-five vector and was not used as an emptiness certificate.
