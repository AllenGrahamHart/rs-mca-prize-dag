# Result

`PROVED`: cofactor `1024` is impossible in the prize profile
`(3,6,S=18)`. Ten profile cofactors remain.

The sibling `1028` branch has the same five-variance window but is
geometrically nonempty; its required factor `257` must be tested explicitly.
