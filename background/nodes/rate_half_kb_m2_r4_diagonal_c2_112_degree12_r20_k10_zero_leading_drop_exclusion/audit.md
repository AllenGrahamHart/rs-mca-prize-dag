# Audit

- Modal app `ap-hLCTOg8uduAe5wGCWu5ogU` proved `F04-R20` in `82.11`
  seconds with peak child RSS `406544 KiB`.
- Modal app `ap-vn7qRQuhGuFu3oLeVL1F5K` proved `F06-R20` in `74.98`
  seconds with peak child RSS `405356 KiB`.
- Both computations used the exact deployed prime and returned unit
  saturation bases. The primary verifier pins both complete JSON payloads;
  the independent audit separately checks the source-equation census,
  localizer, pre-saturation dimensions, and unit terminals.
- The broad necessary ideal `(R12,K10)` survives in dimension one. A forced
  block-order continuation timed out, and the self-`B0` determinant is zero
  modulo `K10`. Neither failed route is used in the proof.
