# Attack surface

This branch is closed. Do not rerun the broad ideal `(R12,K10)`, which has a
genuine one-dimensional open survivor, or the timed-out generic block-order
route. The determinant obtained by evaluating `B0` at the first-pair root
reduces identically to zero modulo `K10`; it is redundant, not an additional
equation. Reuse the two remaining source-row evaluations if this certificate
is transported to another target.
