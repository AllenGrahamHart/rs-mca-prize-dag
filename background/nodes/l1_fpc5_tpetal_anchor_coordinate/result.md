# Result

`PROVED`. At arbitrary `t`, one exact anchor turns the complete monic pair
slice into a degree-at-most-`e-1` affine coordinate body and recovers every
common-defect owner by one gcd. Aggregate split-locator counting remains open.
