# Dependency sub-DAG

```text
interpolation kernel: dim I_264 >= 938
                    |
                    +--> quotient monomial count --> wdeg(P) <= 217
                    |
common-factor mass -+--> higher degree d>=2 --> 5083 sections
                                              |
                                              +--> 126266 factor points
```

The higher-degree full gcd may be reducible; no irreducible component
classification is implicit here.

The degree-one branch is separately classified by
`rate_half_mca_m31_linear_factor_projective_star_router`.  The two
remaining MCA obligations are the star population and higher-degree gcd
classification.
