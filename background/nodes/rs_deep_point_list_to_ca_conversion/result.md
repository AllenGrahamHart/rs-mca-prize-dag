# Result

The self-contained deep-point conversion and its exact integer-numerator
corollary `(DP5)` are proved. This supplies the one-dimension RS bridge needed
to turn a CA/MCA numerator estimate for `RS(N,K)` into an ordinary list bound
for `RS(N,K+1)`.
