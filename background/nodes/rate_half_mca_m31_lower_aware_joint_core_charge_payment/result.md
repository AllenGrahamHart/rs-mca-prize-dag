# Result

`PROVED`: the lower-aware joint-core charge pays the two Mersenne full-lift
supports `e=130220,130221`.  Both terminate after 38 lines with packing lower
bound `142893`.

The remaining full-lift interval now starts at `e=130222`.  No critical node
changes status.
