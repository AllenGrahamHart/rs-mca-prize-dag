# Provenance

Derived on 2026-08-13 from the proved M31 joint-core charge recursion.  The
new observation is that each previously forced total-core lower bound is a
constraint in the convex maximization, so the parent endpoint-concentration
argument can be sharpened by majorization.

The official arithmetic was replayed locally under RAMguard with exact
integers and rational numbers.  No Modal computation was used.
