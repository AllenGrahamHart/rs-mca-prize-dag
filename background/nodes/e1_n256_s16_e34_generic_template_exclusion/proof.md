# Proof

The generic affine-weld theorem proves that all generic heavy triples reduce
to 57 representatives and that their complete three-weld chamber contains
243,285,056 signed vectors. Affine odd-unit transport preserves coefficient
magnitudes, conductor, autocorrelation profile, and `M_3`.

The primary census groups the 21 unordered signed chords. The independent
audit constructs `F(X)F(X^-1)` from 49 ordered products in
`Z[X]/(X^128+1)` and independently reconstructs each weld set from circular
distances. Each uses one shard per affine orbit. The checker pins the orbit
packet, proves all 57 heavy representatives and support counts match it,
compares every count and maximum shardwise, and replays retained witnesses.

The exact packets agree on 418,464 representative full-conductor
profile-`(6,7)` vectors and maximum `M_3=1770`. Every pair-feasible residual
has full conductor by `e1_n256_proper_conductor_collision_exclusion`. The
rational cubic-Hermite certificate inherited from the E34 three-profile
reduction has positive norm margin at `M_3=1947`; hence `M_3<=1770` puts the
collision norm strictly below `2^250`. The collision-norm criterion and row
threshold exclude every generic vector. QED.
