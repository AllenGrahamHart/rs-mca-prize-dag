# Result

The common-fiber-size-two four-cycle residue is now an exhaustive three-way
quartic-pencil router indexed by `c=0,1,2` antipodal deleted-root pairs. The
`c=0` branch is the same matched primitive quotient-pencil census produced by
the fiber-four antipodal branch. The `c=1,2` branches have one or two
coefficient-square roots replaced by exceptional-pair squares.

All three have quotient order `2^40`, member degree `2^38-1`, and lower
pencil degree at least `2^37-2`. This removes the unclassified small-fiber
geometry but leaves one matched and two mismatch censuses open, so the
rate-half adjacent crossing remains open.
