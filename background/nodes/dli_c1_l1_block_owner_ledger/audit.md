# Audit

1. `kappa_j(0)=1` is not formal without the short-relation exclusions; all
   three inputs are wired as requirements.
2. `A_j` is weighted landing mass on a structured target set, not a count of
   primitive relations and not automatically first-owner mass across other
   decompositions.
3. The Haar increment is exactly `15*2^(4j)/q`, because one of the 16 block
   units is the zero pattern already multiplying `Z_j`.
4. The verifier is a full-row replay at one split prime; the proof, not that
   sample, establishes the algebraic identity on every official row.
5. Localizing the black hole does not pay the `3+1/q` budget.
