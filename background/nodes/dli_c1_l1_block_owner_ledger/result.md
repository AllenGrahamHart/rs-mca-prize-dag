# Result

PROVED.  The `L=1` C1-ZERO problem is exactly the aggregate owner estimate

```text
sum_j (actual four-block landing mass - Haar landing mass) <= 3+1/q.
```

This isolates the inverse-flatness burden but does not bound it.
