# Result

Every zero-excess parameter fiber now splits at full biform degree, with the
padded-heavy roots restored rather than discarded. This raises the guaranteed
fiber census from the clean counts to `2e` in the extremal profile and `p+2`
at the first strict boundary.
