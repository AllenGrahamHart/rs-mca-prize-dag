# Result

`PROVED`. The two previously anonymous small-class defect forms are exactly
the center rows `G(gamma,X)`. The large class is reduced to two exact
possibilities: one center-row defect, or a two-dimensional defect that
forces

```text
G(gamma_0,X)=(X-x_*)B_0(X),       B_0 in W_X,
T_3=ell_(gamma_0)T_2.
```

This splits the next Shape-A attack into a center-fiber compatibility lane
and a domain multiplication-chain lane.
