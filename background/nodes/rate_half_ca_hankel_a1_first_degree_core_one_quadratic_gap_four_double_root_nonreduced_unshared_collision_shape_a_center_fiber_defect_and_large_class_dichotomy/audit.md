# Audit

1. The coordinate-vanishing argument uses `q_gamma(gamma)!=0`; the other
   two split quadratics vanish at that center.
2. Two distinct center forms span `S_1`, so a pairwise inverse
   prolongation automatically lies in the three-form prolongation `J`.
3. The identification `V^* -> W_X` is contraction of a minimal tensor;
   it does not depend on choosing dual-looking bases.
4. The small locator has no padded root. The large locator has exactly the
   already proved padded root `x_*`.
5. On `n+3` points, the dual of degree-at-most-`n` evaluation has dimension
   two and numerators of degree at most one.
6. The polynomial identity in the rank-`r-2` branch is justified at
   `n+3` points against a degree-at-most-`n+1` difference.
7. The conclusion `ell_(gamma_0)|T_3` uses the center-adjusted `g_off`,
   from which the unique center factor has already been removed.
8. No critical status changes.
