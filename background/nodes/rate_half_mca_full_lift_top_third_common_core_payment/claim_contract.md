# Claim contract

## Proved

1. The top-third affine-line reduction remains valid for `e>=d`.
2. Pair noncontainment bounds the total affine-line agreement core by `m-1`.
3. The piecewise exact-layer cap `(FC1)` and profile `(FC2)`.
4. Every new KoalaBear support through `95943` and Mersenne support through
   `67452` is paid.
5. Both adjacent stopping records.

## Not proved

1. A payment at `e=95944` or `e=67453`.
2. A replacement for the failed prefix Johnson cap.
3. A complete MCA or prize theorem.

## Promotion rule

Keep `PROVED` only while both verifiers accept source pins, triple overlap,
total-core pair noncontainment, outside packing, exact strip scans, endpoint
records, and hostile mutations.
