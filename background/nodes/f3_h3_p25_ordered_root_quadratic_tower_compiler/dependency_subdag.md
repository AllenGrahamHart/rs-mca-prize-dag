# Dependency sub-DAG

```text
P25 divisor tower [PROVED]
          |
          v
P25 ordered-root tower [PROVED] --evidence--> DSP8 [TARGET]
```

The ordered system trades a smaller presentation for a `25!` symmetry. It
changes no critical status.
