# Claim contract

## In scope

- Cell 5 of the deployed positive `433-1b` branch.
- All four source-sign rows.
- Endpoint roles `xi=5` and `xi=6`.
- All 15 complete residual pairings for each endpoint.
- Exact arithmetic over `F_2130706433`.

## Out of scope

- The 75 active labels with `xi <= 4`.
- Other role cells or other deployed fields.
- A characteristic-free endpoint theorem.
- Complete cell-5 aggregation or duplicate-role transport.

The conclusion uses source-only incompatibility. Since no endpoint source candidate exists, no target pairing census is needed.
