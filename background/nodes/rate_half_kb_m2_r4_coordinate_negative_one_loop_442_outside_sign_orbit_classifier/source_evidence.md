# Source evidence

The outside skeleton classifier supplies the singleton and multiplicity-two
edge types.  The remaining calculation is the exact action of sign changes
of `D,E,F` on the singleton edge signs.
