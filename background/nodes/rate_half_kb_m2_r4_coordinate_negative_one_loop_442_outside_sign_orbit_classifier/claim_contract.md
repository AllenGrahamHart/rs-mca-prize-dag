# Claim contract

| field | content |
|---|---|
| claim | Outside representative changes reduce the raw signed skeleton cells to exactly the five parity cells `(KB41SG-1)`. |
| assumptions | The exhaustive unsigned skeletons `S0,S1,S2` and the printed singleton/full-pair product structure. |
| exact interface | Two `S0` parity cells, two `S1` parity cells, and one `S2` cell per common sign row. |
| nonclaim | Skeleton-automorphism quotient, product realization, outside sums, full interpolation, coordinate or row closure, or either Prize theorem. |
