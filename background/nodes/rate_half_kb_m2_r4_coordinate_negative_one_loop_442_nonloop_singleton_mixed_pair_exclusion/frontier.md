# Frontier

Return to the two finite aligned loop-singleton families.  Their product
router retains only two `S1` branches and one `S2` branch; impose the
one-loop q weld and full interpolation on those genuine product survivors.
