# Result

`PROVED`.  All 63 character eigenvalues have certified outward intervals.
On the full prize envelope every exponent vector satisfies `|xi_t|<=7` and
`sum xi_t^2<=101`, but the resulting zero-sum envelope has more than `2^143`
points and the weighted ellipsoid itself contains at least
`38,482,585,013,041` explicit sparse points.  Generic unit-lattice
enumeration is rejected; sparse multiplication and inverse constraints must
become the primary theorem or generator.
