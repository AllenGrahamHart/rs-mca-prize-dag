# Result

`PROVED`. The first strict pair boundary now contains an exact biform of
bidegree `(e-1,p-2)` that splits in both directions on explicit large sets:
at least `2p+r_A` fixed-domain rows split over off-line slopes, and at least
`(e+15)/2+r_A` clean fixed-slope fibers split over `U_0`. The branch is
reduced to a second concrete split-pencil census target rather than an
unstructured support design.
