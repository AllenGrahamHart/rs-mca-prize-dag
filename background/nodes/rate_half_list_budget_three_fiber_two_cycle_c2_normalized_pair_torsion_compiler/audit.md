# Audit

1. Normalization scales denominator-square roots by `A^(-1)`, while the
   canonical outer coefficients have weights `2h,3h,4h`.
2. Both terms of `K_A` have weight `12h`; its zero set is unchanged.
3. The selected-pair trace is `(1+t)^2/t`, not `1+t`.
4. The complementary torsion compiler needs both terminal equations
   `T_40=2` and `P_40=1`.
5. Reversing the selected pair sends `P` to `P/t^2`, not `P/t`.
6. The role-labelled chamber is modulo an involution; it is not a unique
   ordered representative.
7. Normalization does not discharge the canonical-span or gap equations.
