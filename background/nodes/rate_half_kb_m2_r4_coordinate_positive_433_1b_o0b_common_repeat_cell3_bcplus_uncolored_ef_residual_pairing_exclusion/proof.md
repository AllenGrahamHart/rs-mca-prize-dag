# Proof

The generic-rank node excludes every system away from its five case guards.
At each exact guard root, lift the torus equation and replay the original
common guard and cofactor kernel. A surviving missing-label equation either
has inconsistent coefficients, has forbidden zero product, or supplies a
nonzero product `q` and squared sum `s`.

In the latter case, exact `gcd(P,x^p-x)` certifies all roots of
`x^4+(2q-s)x^2+q^2`. At every one of the 896 endpoint roots, reconstructing
the three residual paired-product equations gives gcd one. Hence all 120
missing-`EF` systems are empty. Since `EF` is fixed by `d -> -d`, this is the
whole record orbit. QED.
