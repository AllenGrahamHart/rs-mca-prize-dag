# Audit

1. Ordinary plus offsets 1--73 is exhaustive and disjoint after impossible
   support-2/3 pairs are removed by the proved carrier-position theorem.
2. Every offset retains exact pairs before geometry; no provenance is lost
   to pre-charge Pareto compression.
3. Raw-safe pruning is monotone because every later charge only lowers caps.
4. Adjacent pair bounds compose only on disjoint support sets.
5. Primary and audit agree on all 74 lane maxima and coverage keys.
6. Audit geometry covers at least the primary geometry for every lane.
7. The safe ceiling and component gap are recomputed by exact integers.
8. No numerical pattern beyond K'=84 is promoted or used in this proof.
