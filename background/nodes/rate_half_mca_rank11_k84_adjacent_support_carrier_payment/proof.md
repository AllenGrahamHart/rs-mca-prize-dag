# Proof

Put `q=74`, `m=67556`, and `n=1048660`. Partition exact support-2/3
completion maxima as follows.

1. `M_3<=M_2`, including the zero endpoints, is the ordinary lane. Positive
   pairs use the exhaustive `T23/A23` fixed-union alternatives; a lone
   positive stratum uses its single completion carrier.
2. `M_3>M_2>0` belongs uniquely to offset
   `M_3-M_2 in {1,...,73}`.

No support-2/3 pair remains. Every offset retains every exact pair before
the support-4/5 geometry is applied. The full pairwise carrier atlas then
enumerates its exhaustive `T/A/F` alternatives.

For every resulting fixed union, apply the proved fixed-union
adjacent-support coupling. Several pair bounds are composed only when their
support sets are disjoint. Raw-safe threshold pruning is valid because all
subsequent fixed-union and adjacent-support charges can only decrease the
premium.

The primary and independent routers were each sharded into one ordinary
lane and 73 exact offsets. The compact merger accepts only successful
checkpoints, reconstructs both complete lane sets, and requires exact
agreement on source units, raw rows, threshold decisions, maximum, margin,
and normalized active label. It certifies all 148 implementation/lane jobs.
The global value is

```text
s2=74/s3=55/s4=45/s5=37/ordinary-single/
c6d3/c7d2/c8d1/c9d0/raw-safe,
```

with premium `P_84` and the printed positive ceiling margin. The nearest
offset lane is offset 15 and remains strictly smaller.

Finally, substituting `P_84` in the exact integral component ledger gives
total capacity

```text
920610888896792835227342245208088849044544034113385622333558298
```

against required incidence

```text
920611111786972543926647666320421141253960527393538734334971880.
```

Their difference is the stated positive gap. QED.
