# Claim contract

- **Claim:** every official corank-two canonical-basis chart has at most
  `84416263` records.
- **Range:** `0<=t<=K'-10<=1048566` after deleting all global zero normals.
- **Matroid input:** loopless rank three with maximum parallel-class size
  `t+1`.
- **Basis floor:** at least `3*67472*(67473+t)` ordered bases per record.
- **Endpoint mechanism:** the cap ratio has sign
  `2t+3*67472+3-1048576`, hence one turn.
- **Status:** PROVED.
- **Nonclaim:** this is a fixed-chart record cap.  It does not by itself
  select the kernel lane, remove rank eight, or close either prize.
