# Dependency sub-DAG

```text
complete corank-two chart cap --------\
integer-gap shortening coordinates ----+
support-local transversality -----------+-> uniform corank-two cap
rank-three bounded-parallel basis floor /
```

The new matroid parent pays the parallel multiplicities that invalidate a
naive promotion of complete-chart paving.  The one-turn endpoint comparison
then covers every official shortening gap.
