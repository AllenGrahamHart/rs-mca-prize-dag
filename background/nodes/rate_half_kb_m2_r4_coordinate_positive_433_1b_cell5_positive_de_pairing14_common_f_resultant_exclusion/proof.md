# Proof

The pinned cell-4 positive-`DE` pairing-14 compiler is evaluated on the exact
cell-5 four-basis tower. The two paired-product equations are quadratic in a
common variable `f`; their division-free resultant is normed to the base
field. External `gcd(P,x^p-x)` reconstruction supplies every base-field root
of the norm and inverse guards.

The 16 rows contain 96 target roots, 208 root-or-exception candidates, and
240 guarded source points. Direct replay routes 32 missing-record
inconsistencies and 32 zero-product boundaries. The remaining 176 checked
points produce 192 compatible common-`f` lifts, and every lift has a nonzero
colored-pair cut. There is no witness, leading-boundary remainder, or
unresolved branch.

An independent Frobenius/`gcd` certificate reconstructs 136 roots in 37
distinct polynomial profiles, including every profile visited by the direct
replay, with maximum norm degree 984. The replay visits all 240 route points
without importing a root from another pairing packet.

The generic-label orbit theorem maps `(0,14)` to `(1,14)`, proving both
labels.
