# Upstream crosswalk - universal constant packet exclusion

This is an exact `(A)` / split-pencil component exclusion. The normalized
packet `(A,B)=(6,20)` yields a rational derivative whose divisor cannot be a
multiple of `p`; no inner polynomial can lift it.

An upstream exact SPI packet can use this theorem to delete the entire
nonzero-`b` constant-eliminant cell on three official characteristics. Only
the exceptional largest-characteristic packet remains a compute or theorem
request; no search over the universal packet should be proposed.
