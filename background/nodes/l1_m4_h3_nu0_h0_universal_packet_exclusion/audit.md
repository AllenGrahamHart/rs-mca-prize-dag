# Audit - L1 m=4, h=3, nu=0, h=0 universal packet exclusion

1. `r=R(0)` is nonzero and every official characteristic exceeds three.
2. The tangent factor and the right-side auxiliary cubic share exactly
   `R+5r`; cancellation occurs in a polynomial domain.
3. Identity `(3)` is checked after normalization `Y=rZ`, so no root or
   scalar extension is assumed.
4. A zero rational derivative is used only over the finite, perfect
   coefficient field.
5. At zero, neither `D` nor `R-4r` contributes a valuation.
6. The case `ord_0(R-r)=p` is explicitly excluded before reducing modulo
   `p`.
7. At a nonzero root of `R-r`, the numerator factors are nonzero and `D`
   contributes only zero or one because it is squarefree.
8. The congruence uses `p=3 mod 4` and the full interval `0<4e+1<4p`.
9. The conclusion deletes only `(6,20)`; the exceptional packet remains a
   live necessary possibility.
