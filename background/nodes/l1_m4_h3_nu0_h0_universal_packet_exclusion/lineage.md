# Lineage

The quarter certificate exposed one outer packet common to all four
characteristics. Its unusually simple shifted-value factorization makes the
Euler equation logarithmically exact. The resulting `p`th-power rational
function has incompatible divisor multiplicities, deleting the packet
without a search.
