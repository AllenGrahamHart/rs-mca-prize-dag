# Frontier

The full-V4 `(r,delta)=(2,4)` row is reduced to

```text
n=2,3,6.
```

All three use only generic unramified dihedral pole fibers. The next attack
must impose the induced two-, three-, or six-cycle source incidence on the
actual quartic/star equations. The other `m=2` types are unchanged.
