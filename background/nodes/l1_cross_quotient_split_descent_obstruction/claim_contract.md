# Claim contract - L1 cross-quotient split-descent obstruction

## Input

The proposed route premise that every full-support cross quotient in an exact
saturated L1 cell is a split polynomial that can serve as a smaller defect
locator.

## Output

An exact threshold, fixed-polarity, nonpositive-Johnson counterexample to that
premise over `F_23`.

## Consumer rule

Consumers may retain quotient injection and degree bounds. They may not infer
base-field splitting, recursive defect-locator status, or a smaller L1 chart
without an additional theorem.
