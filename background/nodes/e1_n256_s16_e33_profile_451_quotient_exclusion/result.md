# Result

`PROVED`: profile `(4,5,1)` is impossible at `V=66`. The complete quotient
census gives exact caps

```text
order 128: M_3<=1732,
order  64: M_3<=1670,
```

and the outer-`4Z` chamber has small-field norm below `2^250`. The V=66
residual is now `(5,7)` or `(1,8)`.
