# Statement

## Theorem (KBP1B9-DE-P4)

On the deployed guarded cell-9 route, pairing `4` is empty for both direct
roles `xi=0` and `xi=2`, for every source sign and every target lane.

Universal label transport therefore closes the two active orbits

```text
{(0,4),(0,7),(1,4),(1,7)},
{(2,4),(2,7),(2,9),(2,10)}.
```

This removes two active representatives and eight leading-open labels. Cell
`9` as a whole is not closed.
