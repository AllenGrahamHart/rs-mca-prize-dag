# Frontier

The exceptional ledger is closed.  Do not rerun its six common fibers or two
pointwise signed-pair bases.

The only remaining cell-3 task is the main genus-three chart.  Use the four
sealed target-free equations from the plane-kernel node as a sequential
resultant problem: eliminate `z2`, then `z1`, pseudo-reducing by `P(b,t)`
after every step.  Exploit `B_1(W)=b10(1-W)` before expansion.  A unit chain
closes `[3,6]`; a proper terminal algebra must be checked for deployed-field
points and all source/target guards.
