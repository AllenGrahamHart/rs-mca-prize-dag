# Frontier

The exceptional ledger is closed.  Do not rerun its six common fibers or two
pointwise signed-pair bases.

The only remaining cell-3 task is the main genus-three chart.  The squared
`w2` and direct `z2` resultants are now retired by exact size and bounded
timeout fences.  Decompose the signed pair in squared variables `w0,w1`, then
append the colored equations componentwise, pseudo-reducing by `P(b,t)` after
every multiplication.  A unit component ledger closes `[3,6]`; a proper
terminal algebra must be checked for deployed-field points and all guards.
