# Audit

1. Every source support `2..9` has exactly `11-c` disjoint alternatives.
2. Their Cartesian product has `9!` leaves and classifies every integer tuple
   of completion maxima.
3. Every child intersects, rather than replaces, all inherited caps.
4. The joint support-four cap is applied only when supports four and five are
   both terminal.
5. The joint theorem's condition is checked for every terminal defect pair.
6. All deficit weights `C(11-c,2)` for `2<=c<=9` are retained.
7. The canonical stream digest, premium sum, paired/unpaired counts, and all
   three extrema are independently replayed.
8. Every kernel corank and rank-nine chart term is retained.
9. Demand subtracts `C(n,11)` exactly once.
10. Both endpoint signs are positive at `K'=45`; `K'=46` is replayed as a
    negative control.
