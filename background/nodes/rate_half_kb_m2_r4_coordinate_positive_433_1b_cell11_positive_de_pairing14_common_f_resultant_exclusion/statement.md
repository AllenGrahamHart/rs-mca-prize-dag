# Statement

On the guarded product-rank-five positive `433-1b -> O0a` route in
source-role cell 11, the positive parallel-`DE` label `(0,14)` is empty over
`F_2130706433` for all four source-sign rows and all four target lanes. Exact
duplicate-copy transport closes `{(0,14),(1,14)}`. No other label or role
cell is claimed.

An incomplete resultant root set, an unpriced leading boundary, or an
unreplayed common-`f` lift falsifies this statement.
