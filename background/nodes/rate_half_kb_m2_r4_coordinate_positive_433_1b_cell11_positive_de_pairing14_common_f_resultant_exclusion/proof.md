# Proof

The pinned cell-4 positive-`DE` pairing-14 compiler is evaluated on the exact
cell-11 four-basis tower. The two paired-product equations are quadratic in a
common variable `f`; their division-free resultant is normed to the base
field. External `gcd(P,x^p-x)` reconstruction supplies every base-field root
of the norm and inverse guards.

The 16 rows contain 136 target roots, 208 root-or-exception candidates, and
208 guarded source points. Direct replay routes 32 missing-record
inconsistencies and 32 zero-product boundaries. The remaining 144 checked
points produce no compatible common-`f` lift. It also validates 16
`b`-leading and 16 `c`-leading payments against the exact tower. There is no
witness or unresolved branch.

An independent Frobenius/`gcd` certificate reconstructs 150 roots in 37
distinct polynomial profiles, including every profile visited by the direct
replay, with maximum norm degree 1396. The replay visits all 208 route points
without importing a root from another pairing packet.

The generic-label orbit theorem maps `(0,14)` to `(1,14)`, proving both
labels.
