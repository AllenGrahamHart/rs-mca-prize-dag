# Lineage

This is the second exact signed-route atlas after the closed
`433-1a -> O0b` campaign.  It applies the same target sign-gauge method to
the adjacent one-loop 433 skeleton, whose active graph has cycle rank two
rather than one.
