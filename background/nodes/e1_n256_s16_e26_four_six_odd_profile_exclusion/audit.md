# Audit

The production engine uses folded signed chord classes. The independent audit
forms all 49 ordered products of the seven sparse coefficients in the
negacyclic ring before reading the profile. Both engines enumerate all 1,234
light templates, `binom(124,3)` heavy supports, and 64 relative sign vectors,
and agree on every output row after removing runtime metadata.

The deterministic verifier replays every one of the 45,408 printed primitive
exceptions from its sparse coefficients, recomputes its profile, conductor,
and third moment, and checks the exact aggregate ledger. It independently
checks that the proper-conductor remainder is `74,614-45,408=29,206`.

FLINT and PARI use separate resultant implementations. Their 45,408 outputs
agree in order; the maximum and all four profile maxima are recomputed from
the stored vectors and norms. The local audit checks source separation and
stored-result agreement without rerunning the census.

Modal applications:

```text
failed infrastructure attempt: ap-Jq5ilys1UDMuhHb04wAVdk
complete dual census:          ap-w01euXu1uuSZMynixEsU9m
complete dual norms:           ap-B13nYXtQQsbfCqFKDPTeUr
```

The failed application imported the local module from the wrong remote path
and completed zero templates; it supplies no mathematical evidence.
