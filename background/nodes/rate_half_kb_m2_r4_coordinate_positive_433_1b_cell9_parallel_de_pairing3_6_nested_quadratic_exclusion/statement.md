# Statement

## Theorem (KBP1B9-DE-P3)

On the deployed guarded cell-9 route, pairing `3` is empty for both direct
roles `xi=0` and `xi=2`, for every source sign and every target lane.

Universal label transport therefore closes the two active orbits

```text
{(0,3),(0,6),(1,3),(1,6)},
{(2,3),(2,6)}.
```

This removes two active representatives and six leading-open labels. Cell
`9` as a whole is not closed.
