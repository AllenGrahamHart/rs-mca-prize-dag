# Claim Contract

## In scope

- Cell-9 direct roles `xi=0,2` at pairing `3`.
- All four source signs, four target lanes, and six exact tower charts.
- Exact transport to pairings `3` and `6` in the two printed orbits.

## Out of scope

- The other 16 active cell-9 representatives, including pairings `4`, `5`,
  positive `14`, and the DF/EF block; cell `10`; K3, LIST, MCA, or Prize
  closure.

## Promotion rule

`PROVED` certifies only `(KBP1B9-DE-P3)`.
