# Result

`PROVED`: after a minimum-lift gauge, no full affine-incidence basis can be
chosen entirely outside the direction support.  Removing those bases gives
the support factor `(AB1)`, valid on both sides of `e=d`.

The largest new uniform walls include KoalaBear rank `12` through support
`15903`, Mersenne rank `5` through `62235`, and Mersenne rank `6` through
`1486`.  The remaining MCA branch must jointly have larger transformed rank
and larger direction support than the appropriate printed wall.
