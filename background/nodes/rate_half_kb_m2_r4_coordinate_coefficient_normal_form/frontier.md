# Frontier

For each of the two exact `K`-fiber degree profiles, apply the `45 x 12`
source-row gate, reconstruct `H`, and substitute it into `(KBCO-2)` and
`(KBCO-3)`. A survivor must retain nonzero odd part, exact bidegree,
irreducibility, the common-five facets, and outer-factor divisibility.
