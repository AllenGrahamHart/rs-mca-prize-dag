# Audit

- In the printed interval `H>=m`; the optional cross-layer top-union slot is
  absent.  Thus every forced slot has one exact layer owner.
- `(EL1)` counts incidences only inside the gauged support.  It is stronger
  than, and does not rely on, the preceding total-core lower bound.
- A padded singleton cannot be selected because every used threshold is at
  least two.
- The high-core and complementary branches remain exhaustive for every
  selected line.
- The table cutoffs are checked directly against all line-bank guards; they
  need not follow the older conservative moving-cutoff rule.
- The adjacent size-two wall is not an unsafe certificate.  Its negative
  large-`s` first-order packing expression is simply noninformative.
- The primary verifier recomputes all prefix and bank arithmetic.  The
  independent audit reconstructs `(EL1)`, the three-line packings, and the
  adjacent capped charge from printed ledgers.
