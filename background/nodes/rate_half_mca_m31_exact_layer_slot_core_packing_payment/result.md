# Result

`PROVED`: exact-layer incidence forces enough inside common core for three
selected lines to contradict packing on every Mersenne support
`130226<=e<=130236`.

The remaining full-lift interval now starts at `e=130237`.  No critical node
changes status.
