# Result

`PROVED`. The extremal equality branch now contains an exact biform of
bidegree `(e-2,p-3)` that splits in both directions on macroscopic named
sets: almost all fixed-domain rows split over off-line slopes, and at least
`e+6+d_A` fixed-slope fibers split over `U_0`. This replaces the loose
minimum-word family by a concrete split-pencil census target.
