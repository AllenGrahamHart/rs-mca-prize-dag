# Audit

The coefficient polynomial from (FEQ4) is denoted `V_E`; the reconstructed
structural variable is `V_e`. They are not identified.

The simplified `Z_R^e` equation uses `Z_D^e=0`; the proof records their
exact residual difference, so the structural filters are asserted jointly.

The scope requires `J_*!=0`. The `J_*=L_*=0` chart still retains `G_2`
and is not covered. Likewise `S_1=0` and `a_2=0` are retained rather than
divided away.

All denominator factors remain saturations. No role, `P_4`, lift,
common-root, or emptiness conclusion is claimed.
