# Result

The generic part of the exceptional `E_G=0` coefficient endpoint now has
three explicit structural filters, all univariate after its q reconstruction.
Five-way common-root, singular-chart, role, and lift verdicts remain open.
