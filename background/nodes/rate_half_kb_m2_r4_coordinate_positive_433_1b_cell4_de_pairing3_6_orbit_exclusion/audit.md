# Audit

- The direct theorem contributes exactly three labels at matching 3.
- The involution fixes matching indices for the `xi=0,1` exchange; it does
  not send either positive omission to matching 6.
- The matching transposition `3 <-> 6` applies to fixed missing roles
  `xi>=2`, and therefore contributes exactly `(2,6)` here.
- Source signs and target lanes are not quotiented or identified.
- The new four-label block is disjoint from the nine first-pair labels.
- Counts are maintained separately as labeled slices, quotient orbits, and
  raw sign/lane cases.
