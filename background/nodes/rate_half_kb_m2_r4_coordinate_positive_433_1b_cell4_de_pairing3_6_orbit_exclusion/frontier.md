# Frontier

The two positive-DE omissions at pairing 6, `(0,6)` and `(1,6)`, remain the
nearest members of this matching orbit. A direct nested-quadratic replay or
an independently proved target swap could pay them.

All `xi=3,4,5,6` slices outside the paid first-pair and `(2,6)` labels also
remain open. Cell 4 has 92 live labels in 52 parallel-DE quotient orbits.
