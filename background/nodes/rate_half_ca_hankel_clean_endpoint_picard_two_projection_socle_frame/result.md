# Result

The clean Picard point now has an exact two-axis socle frame. Its parameter
coordinate gives `ev_S`, while its domain coordinate gives the Veronese
direction `ev_x0`. The remaining incompatibility can therefore be posed in
the same domain coefficient geometry as the four Hankel forms rather than
through an abstract Picard class.
