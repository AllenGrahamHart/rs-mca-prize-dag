# Result

`PROVED`. Taylor-content support is exact at every cutoff and nested by
cutoff. The `c=2` endpoint gives a degree-`8,192` exact triple-collision
pre-screen at `n=8192`, followed by mandatory exact cutoff-35 row filtering.
