# Claim contract

| field | value |
|---|---|
| claim | Reducing the degree-at-most-six coefficient compatibility polynomial modulo the quadratic `F_b` gives one affine equation in `q`; off its leading and linear-coefficient singular charts, the generic coefficient endpoint is one explicit univariate polynomial `U(b)` of degree at most 58. |
| status | PROVED |
| dependency | `l1_mersenne_hnf_m8_order_one_cubic_three_two_one_generic_fully_proportional_coefficient_bivariate_compiler` |
| consumer | `l1_mixed_petal_amplification` |
| equivalence guards | The generic coefficient guard `E_G!=0`, then the printed `a_2` and `R_1` chart guards. |
| open residue | A nonzero/unit or retained-root verdict for `U`, the `R_1=R_0=0` chart, the fixed `a_2=0` chart, and every substituted structural, role, `P_4`, and arithmetic-lift equation. |
| nonclaim | The recurrence and degree bound do not assert that `U` is nonzero or that any chart is empty. |
