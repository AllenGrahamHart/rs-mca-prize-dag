# Audit

The quotient recurrence divides only on the `a_2!=0` chart. The known
`a_2=0` chart is retained with its exact reconstructed `q`.

The affine remainder is split on `rho_1`. Its simultaneous
`rho_1=rho_0=0` locus is retained and is not inferred empty from the generic
univariate formula.

The names `rho_1,rho_0` are reserved for this quotient remainder and do not
collide with the inherited structural variable `R_0`.

The polynomial `U` is only the coefficient compatibility endpoint. Every
structural, role, `P_4`, saturation, and arithmetic-lift equation must still
be substituted at each candidate root. No resultant or squarefree claim is
made.
