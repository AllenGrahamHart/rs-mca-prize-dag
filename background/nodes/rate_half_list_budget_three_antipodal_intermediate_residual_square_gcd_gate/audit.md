# Audit

- Expanded the Euler cancellation and the factor `V^2` independently.
- Checked `P'=U^2W` and the load-bearing coprimality `gcd(P,U)=1` before
  replacing `gcd(P,P')` by `gcd(P,W)`.
- Recomputed `4YDW=UA-3T^2`, the degree-six leading coefficient of `A`, and
  the degree-eighteen leading term of `J` independently.
- The verifier covers the general polynomial identities and a repeated-factor
  fixture with `P=V^2`.
- No official-scale gcd or dense polynomial was computed; the annihilator
  proves the uniform bound symbolically.
