# Result

The maximal intermediate boundary is empty. A survivor would force
`gcd(TU^3+d,T'U+3TU')` to have degree at least `(2^38-4)/3`, but that gcd
divides the explicit polynomial `dA^3+27T^7` of exact degree eighteen.

No official-scale computation is needed for this exclusion.
