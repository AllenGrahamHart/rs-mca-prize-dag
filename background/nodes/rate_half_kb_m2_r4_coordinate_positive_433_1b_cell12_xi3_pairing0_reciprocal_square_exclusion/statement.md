# Statement

Over `F_p`, `p=2130706433`, the guarded cell-12 positive `433-1b` route is
empty for missing `DF`, residual matching `0`, and every source and target
sign lane.  Exact `D/E` outside transport gives the same conclusion for the
partner missing `sigma_o EF` label.

This closes two labels forming one generic orbit.  No other matching is
decided.
