# Claim Contract

## In scope

- Cell `12`, product rank five, missing roles `DF/sigma_o EF`.
- Residual matching `0`, all source and target signs.
- Generic leading-open and rational leading-boundary pieces.

## Out of scope

- Matchings `1..14`, other missing roles, cell `13`, or cell closure.
- Any claim over a nondeployed field.

## Promotion rule

`PROVED` requires all three `q` branches, every norm/inverse root, every
direct lift, and both final `sigma_c` lanes to terminate without a witness or
unresolved branch.
