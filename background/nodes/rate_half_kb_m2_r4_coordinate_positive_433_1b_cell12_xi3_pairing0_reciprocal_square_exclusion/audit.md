# Audit

Run `verify.py` and `verify_audit.py`.  The independent audit checks the AST
adapter boundary, reconstructs every finite-field norm and inverse root set,
checks complete candidate-root coverage, evaluates the cell-12 leading
coefficient at each transported boundary point, and recounts all final lanes.
