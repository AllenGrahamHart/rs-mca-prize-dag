# Dependency sub-DAG

```text
collision_norm_criterion [PROVED]
  --req--> integer_code_distance_high_field_folded_box_exclusion [PROVED]
  --ev---> integer_code_distance_cert [TARGET]
```

The evidence edge pays only the order-128 branch above `253^32`. The target
retains every lower-field row, every other quotient order, the exact row
registry, and its value-set budget check.
