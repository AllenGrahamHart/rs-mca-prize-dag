# Audit

## Finding

The worst-word conditional described QA.22 as a proved link in its list-side
assembly, and W3 defined its residual using the consumer's QA.22 convention.
The upstream QA.22 packet instead states that `16n^3` is the post-strip MCA
reserve for `R_post(u,v;A)` at six candidate rows. No per-pair-to-per-word
aggregation theorem was wired.

## Repair

W3 was re-posed using the exact two-class identity and one actual non-planted
upper bound. QA.22 remained evidence and a useful exact regression, but its
requirement edge was demoted.

## Later disposition

An exact fiber-layout counterexample then refuted applying the envelope at the
unsafe spending cell. Literal safe-side W3 remains open but was removed from
the prize path as unnecessary. A future conversion theorem may legitimately
import an MCA bound only for a newly posed consumer, and it must print its
fibers and multiplicities in that consumer's quantifier order.
