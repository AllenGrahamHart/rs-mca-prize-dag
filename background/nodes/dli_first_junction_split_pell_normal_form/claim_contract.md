# Claim contract

## Proved

- Exact interpolation of every binary antipodal pair word by `A,W`.
- Exact equivalence between the even/odd moment prefix and the two printed
  degree gaps.
- Exact split-value and Pell congruences encoding the binary alphabet.
- Exact converse reconstruction and the equivalence `Prim iff W!=0`.
- Exact polynomial count for `Z_0-C_1`, the quotient-degree cap when
  `L<=h/2`, and identity collapse when `L>h/2`.

## Not claimed

- No count or classification of split-Pell pairs.
- No bound on `J_prim`, `K_tail`, or the tower marginals.
- No closure or status change for C2''.
