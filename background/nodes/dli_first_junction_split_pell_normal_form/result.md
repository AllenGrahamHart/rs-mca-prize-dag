# Result

The primitive first-junction DLI numerator is exactly a bounded-degree
split-Pell census over `F_q[Y]`: count nonzero `W` satisfying the two degree
gaps and the two congruences modulo `Y^h-1`. This supplies a concrete
algebraic classification target for primitive prefix flatness.

Status: **PROVED** exact normal form. The census bound remains open.
