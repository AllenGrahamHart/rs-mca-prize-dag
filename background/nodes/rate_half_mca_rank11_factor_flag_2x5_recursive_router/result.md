# Result

`PROVED`. The primitive base-free `2 x 5` factor model cannot remain flat at
both levels. If it supports an unsafe line, one residual dimension-two/three
space extends inside the fixed five-space to a larger space with at least
`18166` common actual zeros.

This is the first exact recursive factor-flag payment after PR `#1173`: it
does not sum local locator cancellations and it keeps one disjoint owner
partition. The remaining horns are a common pencil locator or a deeper
residual flag.
