# Result

`PROVED`. The nested shape-A flag now has two exact pieces:

```text
padding singular piece:   gcd(g_off,R_(d+1),...,R_(d+1+r)),
regular source piece:     gcd(H_reg,B_0,...,B_r).
```

The regular piece is a bordered-Hankel rank-stagnation locus on `2e+7`
off-line slopes. Every `B_s` is an explicit source subset sum and satisfies

```text
B_s=c g_*(S_B Lambda Theta_s)^2.
```

The next route-deciding theorem can now target either the padding singular
piece or the squarefree-times-square source sums, without reconstructing
the biform again.
