# Claim contract

- **Claim:** the three source-root classes force
  `sr(G)>=ceil((e+1)/3)` on Shape A.
- **Inputs:** independence of the first-degree primitive-kernel coefficient
  chain, its identification with `Qbar`, the `d_A=1` three-center source
  partition, and the dual-MDS identity relating `Qbar`, `H_x`, and `G`.
- **Output:** all tensor ranks below `61083979322` are excluded officially.
- **Consumer:** `rate_half_band_crossing_location`.
- **Nonclaims:** no exclusion of the remaining macroscopic-rank interval.
