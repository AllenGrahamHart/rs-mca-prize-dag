# Result - M31 top-neighbor core-shadow payment

A fixed one-root-swap core carries at most `240` actual top neighbors. The
forced dense-top anchor must consequently spread across at least `4,477,705`
degree-`4979` cores. The geometry-only counterfixture is paid locally; the
remaining theorem is a cross-core owner or aggregation bound.
