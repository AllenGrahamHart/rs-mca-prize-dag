# Lineage - M31 top-neighbor core-shadow payment

The dense-top router forced `215793` actual neighbors and the divisor route
cut exhibited `67449` abstract one-root-swap directions in one polynomial
plane. The harvested upstream affine-span list compiler supplies the missing
semantic distinction: only `241` listed words, including the anchor, can
occupy that plane. Double counting deleted roots turns this local payment
into the codimension-one shadow floor `4477705`.
