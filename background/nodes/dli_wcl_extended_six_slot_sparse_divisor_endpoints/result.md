# Result

The six widened WCL leaves are no longer unbounded support-census problems.
They reduce exactly to six fixed sparse cubic-or-lower unit ideals with
`76--142` variables and `78--147` equations. Their integer certificates are
appropriate contributor CAS requests. Blind affine support enumeration is
rigorously fenced by class lower bounds from `6.4e9` to `1.5e22`.

No finite characteristic has been excluded, so all six leaves remain
`TARGET`.

