# Claim contract

## Direct claim

The explicitly printed Jacobi product `alpha` generates the fractional ideal
`(J_63/bar(J_63))^(2*21121)`.

## Imported theorem

The classical Stickelberger factorization of Jacobi sums, specialized in
`(SR3)--(SR4)`.

## Replayed finite claim

The 32 printed coefficients satisfy all 64 prime-valuation equations in
`(SR6)` over the integers.

## Consumer

`e1_qzeta128_p257_j63_residue_obstruction`.
