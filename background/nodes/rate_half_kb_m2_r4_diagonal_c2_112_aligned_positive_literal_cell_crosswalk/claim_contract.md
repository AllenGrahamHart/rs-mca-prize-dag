# Claim contract

| field | value |
|---|---|
| claim | The local canonical source templates and residual allocations map exactly to `F00/M00` and `R20/R02/R11`. |
| source map | fixed-moving = `F00`; moving-moving = `M00` |
| target map | same = `R20`; swap = `R02`; mixed = `R11` |
| proved scope | six literal systems only |
| route fence | matching-centralizer transitivity is not full q-slice covariance |
| nonclaim | no deletion or transport for any other literal `Fxx/Mxx` assignment |
