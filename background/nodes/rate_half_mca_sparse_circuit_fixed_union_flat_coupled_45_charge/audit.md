# Audit

1. The original evaluation matroid is loopless by empty common zero set.
2. Closure points annihilate every polynomial vanishing on the independent
   generating set.
3. Locator division preserves the dimension of `W`.
4. The polynomial gcd bound uses dimension, not merely two chosen sections.
5. `X_4` includes both the completion cap and nonnegative `(FC)` cap.
6. Integer loss uses a ceiling before proving endpoint monotonicity.
7. Lower strata stop at `j=d-1`; entirely-outside circuits are counted once
   through the coupled matroid census.
8. Selected-set extension factors use the original domain size `m`.
