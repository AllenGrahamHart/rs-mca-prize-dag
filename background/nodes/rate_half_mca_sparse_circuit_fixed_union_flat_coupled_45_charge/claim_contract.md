# Claim contract

- Circuits and flats are those of the original evaluation matroid restricted
  outside `D`.
- `W` supplies polynomial common-root bounds; no circuit-minimality transfer
  to the subspace evaluation matroid is used.
- The hypotheses `g>=5`, `R>=0`, `N>B`, and `K>=g+5` are explicit.
- Lower strata use no parallel-class refinement and no target-specific
  completion maximum smaller than `R`.
- The weights `21,15` and extension factors are specific to selected
  eleven-sets.
- The theorem is a joint cap, not two separately attainable maxima.
