# Attack

Attempt to construct an original evaluation-matroid circuit that becomes a
smaller dependency after restriction to `W`; this must not affect the proof,
which never transfers circuits.  Separately test the gcd-dimension bound and
the endpoint slope at `K=g+5`, where monotonicity is weakest.
