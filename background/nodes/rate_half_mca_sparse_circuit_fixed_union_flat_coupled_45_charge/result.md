# Result

The fixed-union flat-coupled support-4/5 charge is `PROVED`.  Every
admissible pairwise carrier union of dimension at least five now supplies a
single weighted cap that prices the support-four/support-five collision
instead of combining two independently worst cases.
