# Result

The complete official census sharpens the averaging lower bound to the exact
uniform statement

```text
max_(c in F_p^*) #{x in H:c-x in H}=2308.
```

Consequently every fixed nonzero affine-reflection pencil has at most `1154`
disjoint split quadratic domain fibers, and this cap is sharp. Applying it to
the rank-eleven mass now requires a separate packet-pencil synchronization
and ownership theorem.
