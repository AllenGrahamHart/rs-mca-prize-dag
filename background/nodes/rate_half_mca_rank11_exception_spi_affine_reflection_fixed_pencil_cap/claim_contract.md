# Claim contract

- **Claim ID:**
  `rate_half_mca_rank11_exception_spi_affine_reflection_fixed_pencil_cap`
- **Field/domain:** the official KoalaBear base prime
  `p=2130706433` and its order-`2^21` subgroup.
- **Quantifier:** every fixed `c in F_p^*`.
- **Conclusion:** `R_c<=2308`, hence at most `1154` nonfixed quadratic
  reflection fibers; the bound is attained on exactly one `H`-coset.
- **Used from dependency:** the identification of nonfixed reflection orbits
  with disjoint fibers of `X^2-cX+gamma`.
- **Not claimed:** the `c=0` antipodal class, one canonical `c` per heavy pair
  type, synchronization across packet choices, a first-match payment, an
  unsafe MCA line, or prize closure.
