# Claim contract

- **scope:** official `A=1`, core-one, exceptional-only sharp-cap profile
- **input:** the quotient-distance gap and exact two-sided row saturation
- **currency:** quotient support plus an exact exceptional-root/ordinary-fiber
  incidence double count
- **output:** `delta_A(h_1)=3` or
  `183251937965<=delta_A(h_1)<=412316860416`
- **consumer:** `rate_half_band_closure`
- **load-bearing hypotheses:** `h_0 in W_A`; every ordinary clean locator is
  split, squarefree, and has degree `r`; there are exactly `4e` ordinary
  supported slopes; all `2e` exceptional roots are saturated with exactly
  `e` supported incidences
- **nonclaim:** the remaining macroscopic high-distance interval is not
  excluded or made computationally enumerable
- **failure certificate:** an ordinary clean locator for which
  `G_z\R_A` does not represent `[h_1]`, or an exceptional root whose supported
  incidence count is not `e`
- **replay:**
  `tools/ramguard tiny -- python3 background/nodes/rate_half_ca_hankel_a1_core_one_exceptional_only_quotient_distance_ceiling/verify.py`
- **upstream mapping:** exact finite primitive shift-pair quotient-distance
  ceiling
