# Result

The second of the three rigid `m=4` passports is now explicit over `Q`, and
its pole divisor splits over the KoalaBear field. As for the first rigid
cover, any deletion must use the fixed active fiber or quartic source-star
incidence rather than coefficient or pole descent.
