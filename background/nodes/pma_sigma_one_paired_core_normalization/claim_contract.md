# Claim contract - PMA sigma-one paired-core normalization

## Claim

At `sigma=1`, unordered maximal-sunflower source layouts for a fixed received
word are canonically and bijectively indexed by `(k-1)`-cores whose quotient
map has fiber signature `(2,...,2,1)`. The core determines all chart data,
and any two planted anchors determine the core and chart. First-match Post is
therefore an exact disjoint sum over paired cores.

## Consumes

- `pma_sigma_one_b11_scope`: the official identities `ell=2`, `b=1`, and
  `n-k=2M`;
- `l1_program_frontier`: the carried maximal-sunflower source-layout
  universe and first-match convention;
- elementary interpolation and the factor theorem.

## Serves

- `pma_wide_residual`: replaces an ambiguous source-layout multiplier by the
  exact paired-core weighted census `(PC4)`;
- `petal_mixed_amplification`: supplies the same canonical global index for
  the complementary Post branch;
- upstream first-match/image-scale work: identifies the precise exceptional
  quotient-fiber signature which a global atlas theorem must count.

## Promotion boundary

This node is PROVED because every assertion is an algebraic bijection or a
first-match identity. It does not promote `pma_wide_residual`: first-layout
domination removes the weighted-census multiplier, but the unowned residual in
that first layout remains open.
