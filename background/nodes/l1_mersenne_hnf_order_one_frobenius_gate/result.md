# Result

The order-one next-to-maximal endpoint now has a bounded-degree algebraic
prefilter with only `m` torsion chambers. A unit saturation closes the chamber
before any official large-exponent remainder; retained components remain
necessary candidates only.
The automatic resultant factor from the zero split value is cancelled first,
so the reciprocal system has degree `h-1` rather than `h`.
