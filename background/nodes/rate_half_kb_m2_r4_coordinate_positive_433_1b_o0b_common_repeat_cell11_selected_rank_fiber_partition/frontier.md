# Frontier

The sibling deployed-boundary exclusion proves that the finite degree-4/6
fiber has no base-field point on the symmetric tower chart: its only
base-field norm roots are registered tower guards.  The remaining cell-11
work is therefore those omitted guard loci.  Exact duplicate-role transport
can subsequently move cell 11 to cell 14.
