# Source evidence

The rank-fiber launcher consumes the exact cell-11 input packet and runs one
localized Singular program for each epsilon/BC-sign row.  It prints the
common dimension and basis size, both normal forms, and the dimension, basis
size, and vector-space dimension of the selected-cofactor fiber.

The result contains eight complete rows and no timeout or fallback.
