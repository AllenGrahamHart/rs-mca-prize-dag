# Audit

The verifier pins both artifacts and the exact source-input digest.  It checks
the eight-row Cartesian census, zero `t` remainders, nonzero cofactor normal
forms, common dimensions, finite-fiber dimensions, and `vdim` values.

The hostile audit mutates a relation, cofactor remainder, dimension, degree,
case coverage, and source custody.
