# O0b split cell-0 common-component classification

- **status:** PROVED
- **scope:** split-principal O0b role cell `0`

The six-cofactor common atlas has 24 cell-`0` rows: four source-sign rows
times six nonzero-product-cofactor charts. Both mixed source-sign rows are
unit on every chart. Since product rank is five, at least one chart applies,
so no mixed-sign common point exists in any of the six O0b outside lanes.
This closes

```text
2 source signs * 6 lanes * 105 outside labels = 1,260 raw labels.
```

The two equal source-sign rows have dimension one and are covered by the four
necessary families `A_s,B_s`, `s in {-1,1}`. Their exact coefficient kernels
satisfy all ten common Vieta rows. The equal-sign outside question remains.

The proved commuting quotients represent the 1,260 closed labels by 354
orbits. Hence the live owner frontier falls from `39,480/11,076` raw/quotient
rows to `38,220/10,722`. Branching each equal-sign survivor over its two
components gives 708 exact component-case representatives for the next
outside exclusion.

## Falsifier

A missing cofactor chart, a nonunit mixed-sign chart, failure of A/B coverage,
an invalid common kernel, an untransported O0b common row, or a different
raw or quotient census.
