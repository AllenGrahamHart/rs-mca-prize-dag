# Result

`PROVED`. Every primitive FPC5 Hankel split locator is exactly one
weight-`d` error in a GRS syndrome coset. Fixed charts are singleton when
`d+c>=N`; below that range the unresolved local count is literally an
ordinary GRS exact-shell list size with additional guards.
