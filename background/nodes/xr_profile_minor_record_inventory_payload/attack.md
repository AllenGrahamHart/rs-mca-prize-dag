# ATTACK - xr_profile_minor_record_inventory_payload

This is now the live XR coverage leaf.

Do not run E32 locally on the WSL laptop.

Needed output:

- enumerate every budget-meeting unpaid non-boundary light-triangle profile;
- for each profile, name a maximal minor of the XR normal-form matrix and an
  admissible specialization;
- classify the record as triangular, monomial/noncancellation, or remote
  table;
- include the determinant evidence required by that record type; and
- prove the inventory covers the full profile quantifier, not only toy
  E32-MERGED/E32-COORD samples.

The formal coverage and accepted-record dispatch rule is proved in
`xr_profile_minor_record_inventory_soundness`.
