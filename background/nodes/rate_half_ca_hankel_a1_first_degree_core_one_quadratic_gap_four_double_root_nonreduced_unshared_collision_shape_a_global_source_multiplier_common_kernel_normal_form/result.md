# Result

The formerly separate center kernels are one exact owner-sensitive Padé
intersection:

```text
K_cap=S_n intersect J(W_X+varphi W_X+varphi^2W_X)^perp.
```

At the lower rank boundary the multiplier code has dimension `n+5`, while
the required intersection has dimension at least `e-3`. The subsequent
Padé-parity quotient theorem shows that this entire floor is automatic:
`J` times the `e+1` primitive-locator coefficient rows is already in the
right radical. Thus even an owner-sensitive ambient rank estimate is not a
closure route by itself; an independent collision, Hankel, fixed-factor, or
split-row incompatibility is required.
