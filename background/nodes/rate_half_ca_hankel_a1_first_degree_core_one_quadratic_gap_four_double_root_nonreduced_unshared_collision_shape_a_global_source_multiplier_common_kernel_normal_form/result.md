# Result

The formerly separate center kernels are one exact owner-sensitive Padé
intersection:

```text
K_cap=S_n intersect J(W_X+varphi W_X+varphi^2W_X)^perp.
```

At the lower rank boundary the multiplier code has dimension `n+5`, while
the required intersection has dimension at least `e-3`. The next valid
rank attack must use the algebraic structure of `J`, `varphi`, and `W_X`;
an ambient generic-rank argument is explicitly insufficient.
