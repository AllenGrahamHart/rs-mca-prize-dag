# Frontier

The mathematically essential denominator `A(lambda_missing)` is now removed
from the cell-11 guard frontier.  Remaining guards should be classified into
actual endpoint nonvanishing conditions and implementation pivot artifacts;
only genuine guarded source fibers require direct replay.
