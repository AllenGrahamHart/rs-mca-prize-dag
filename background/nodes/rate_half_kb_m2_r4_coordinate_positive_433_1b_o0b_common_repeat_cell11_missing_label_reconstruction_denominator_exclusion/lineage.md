# Lineage

This node closes the rational-open qualification in the common-kernel
reconstruction at its missing label.  It is separate from the selected
product-rank cofactor boundary: `A(lambda_missing)` is an interpolation
denominator, not the degree-seven rank cofactor.
