# Attack

The next row is a real capacity wall. Do not extend `4598` by interpolation
or floating-point evidence. A continuation must reduce the realizable basis
census, exploit overlaps between basis charts, or couple the kernel ranks
to dense-anchor chronology.
