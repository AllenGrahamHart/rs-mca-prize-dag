# Result

On the separated double-root locus,

```text
G(t,x_*)=0       => S_B|Lambda       => S_B|J and j>=2.
```

Hence all center-disjoint and one-root-overlap configurations have a nonzero
heavy row. Only the fully correction-centered overlap cases `j=2,3` retain
the zero-row possibility.
