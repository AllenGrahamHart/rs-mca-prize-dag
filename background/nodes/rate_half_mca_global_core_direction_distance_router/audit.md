# Audit

The primary checker evaluates the closed integer threshold at every shortened
dimension.  The independent checker scans every positive denominator and
recomputes the budget comparison directly.  Both start at `s=1`; neither
imports a small-dimension affine payment.
