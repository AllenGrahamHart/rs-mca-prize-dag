# Result

`PROVED`: composing the whole-line global-core owner with the exact
direction-distance ray compiler extends the unconditional-in-`j` staircase
to an exact two-parameter paid region.

The former opaque large-dimension branch is replaced by a specific
low-direction-distance residual.  The first dimensions at which even a
rank-regular direction is not paid are `4983` on KoalaBear and `4980` on
Mersenne-31.
