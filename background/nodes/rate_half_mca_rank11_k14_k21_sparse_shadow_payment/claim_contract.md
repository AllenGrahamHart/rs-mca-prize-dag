# Claim contract

This node closes the complete positive-dimensional rank-eleven component
population on exactly `K'=14..21`.

It partitions incidences into rank-deficient and full-rank classes.  The
kernel class retains every nonzero corank term.  The full-rank class uses
one joint rank-nine resource for sparse and high circuits; it does not add
an independent sparse total to `G/45`.

Every core size `9<=j<K'` is evaluated.  Both completion branches are
maximized recordwise.  Persistence above the record floor is proved by a
positive unfloored cross, not inferred from one rounded endpoint.

The explicit negative gap at `K'=22` is a method wall.  No row `K'>=22`,
rank-eight payment, chronology owner, active-v4 movement, KoalaBear closure,
or prize closure is claimed.
