# Dependency sub-DAG

```text
canonical-basis kernel globalizer
  + every nonzero corank and extension factor C(q,d+1)
  -> absolute rowwise kernel capacity K_cap                         [PROVED]

rank-nine residual petal geometry
  + weighted core-offset theorem for every 9<=j<K'
  -> global marked rank-nine capacity G                            [PROVED]

sparse-circuit completion dimension ladder
  -> structured/unstructured recordwise vectors L_c               [PROVED]

joint sparse/high rank-nine shadow ledger
  -> full-rank capacity floor((G+R P_*)/45)                        [PROVED]

dense-locator full component density
  -> demand exceeds kernel plus full-rank capacity on K'=14..21    [PROVED]
  -> all eight component targets are empty                         [PROVED]

same formula at K'=22
  -> negative gap; next route starts at K'=22                      [METHOD WALL]
```
