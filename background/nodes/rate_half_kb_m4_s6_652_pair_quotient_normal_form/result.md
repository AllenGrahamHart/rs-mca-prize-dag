# Result

One of the three rigid `m=4` passports is now an explicit rational map rather
than an abstract branch-cycle row. Its pole divisor automatically splits over
the KoalaBear field, so coefficient-field or pole-field descent cannot delete
this passport. The remaining attack must use the fixed active fiber or the
quartic source-star incidence.
