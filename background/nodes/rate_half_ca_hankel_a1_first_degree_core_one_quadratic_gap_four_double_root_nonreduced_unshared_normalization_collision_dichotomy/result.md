# Result

The entire unshared nonreduced branch now has one exact dichotomy:

```text
ord_tau F_0=4  =>  kappa_2=kappa_3=0, Smith [4];
ord_tau F_0=2  =>  kappa_2!=0 and Q(tau,X) has (X-x_*)^2 exactly.
```

There is no first-nonzero third jet. Thus every nonzero-jet survivor lies on
one explicit double quotient-root collision; all noncollision higher-corank
profiles are empty in the normalized geometric packet.
