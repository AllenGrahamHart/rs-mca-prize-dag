# Attack surface

To refute this node, exhibit a fixed basis chart in which the zero normals
used by `T minus B` differ from the zero normals deleted to define `t`, or
an official row/corank for which the printed weighted ratio is not strictly
decreasing after `t=1`.  A large noncomplete record cap alone is not a
falsifier because its extension count shrinks simultaneously.
