# Audit

The proof keeps three quantities distinct: records over `B`, extensions of
one record to `T`, and basis decorations of one `(record,T)` incidence.
The factor `d+2` is applied only after summing decorated incidences.  The
noncomplete branch is bounded by the rational product at `t=1`; no invalid
separate flooring or complete-cap promotion is used.
