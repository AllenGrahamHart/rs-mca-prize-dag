# Dependency sub-DAG

```text
kernel_projective_paving_record_caps
                 \
kernel_projective_paving_integer_gap_fence
                   \
kernel_multibasis_decoration_compression ---> shortening_weighted_extension_cap
                   /
uniform corank 1 / 2 / 3 caps
```

The new edge retains `z` through both the record cap and the extension
count.  Earlier capacity nodes replaced both factors by separate maxima.
