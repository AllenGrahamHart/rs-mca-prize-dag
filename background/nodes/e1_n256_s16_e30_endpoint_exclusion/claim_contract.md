# Claim contract

## Claim

No pair-feasible `N=256` folded-profile `(3,4,0)` vector has `V=60`.

## Inputs consumed

- the exact eight-profile E30 reduction;
- the three-profile quotient exclusion;
- the two-odd-profile exclusion;
- the individual `(4,2,2)`, `(5,4,1)`, and `(6,6)` exclusions.

## Output supplied

The live positive even variance frontier in this band advances from `V<=60`
to `V<=58`.

## Scope exclusions

- no exclusion at `V<=58`;
- no closure of folded profile `(4,2,0)`;
- no closure of later `N=256` or `N=512` bands;
- no route-wide pair-incidence theorem.
