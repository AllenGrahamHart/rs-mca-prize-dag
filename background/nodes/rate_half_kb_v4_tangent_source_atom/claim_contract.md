# Claim contract

- **Claim:** one canonical source-coordinate tangent owner has at most
  `n-a=981104` distinct bad finite slopes on the deployed KoalaBear MCA row;
  the declared four-cell first-match partition is exact.
- **Quantifiers:** every admissible received line at agreement `a=1116048`.
- **Dependencies:** the exact far/sparse translation theorem
  `rate_half_mca_sparse_layer_reduction`, finite-image cardinality, and exact
  integer arithmetic.
- **Consumer:** `rate_half_band_closure`, by an evidence edge only.
- **Paid value:** `U_paid=981104`.
- **Unpaid values:** `U_Q`, `U_BC`, and `U_new`.
- **Nonclaims:** no cap on an unpaid cell, no import of the legacy M1 stack, no
  KoalaBear safe row, no unsafe predecessor, and no adjacent crossing.
- **Falsifier:** a received pair for which the chosen sparse translation
  changes the complete MCA-bad slope set; a paid slope outside the coordinate
  ratio image; a ratio image larger than its source support; or a mismatch in
  the printed row arithmetic or partition digest.
