# Claim contract

## Consumed content

- The parity/trace cap supplies the exact 15-profile cofactor-`514` ledger.
- The square-mass moment dictionary supplies `sum x_u=0` and
  `sum x_u^2=128E` over 64 positive conjugate pairs.
- The prize field floor supplies `p>=p_min`.

## New proved content

1. A quadratic Hermite interpolant gives the exact moment majorant `(6)` in
   `proof.md` for every deviation vector bounded above by `M`.
2. The profiles `(9;1,2,0)` and `(11;7,1,0)` lie below `514*p_min`.
3. Thirteen magnitude profiles remain.

## Guards

- The bound uses no coefficient-profile realization and therefore transports
  to profile `(0,18)`.
- Only an upper bound `x_u<=2L` is used; no symmetric lower trace bound is
  assumed.
- The other 13 profiles and their split-ideal occupancy remain open.
