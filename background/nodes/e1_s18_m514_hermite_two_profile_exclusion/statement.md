# E1 square-mass-18 cofactor-514 Hermite two-profile exclusion

- **status:** PROVED
- **closure:** quadratic Hermite moment majorants with exact endpoint comparisons
- **scope:** conductor 256, square mass 18, cofactor `514`

No square-mass-18 cofactor-`514` collision on the binding prize row has
either autocorrelation magnitude profile

```text
(E;n_1,n_2,n_3)=(9;1,2,0),
(E;n_1,n_2,n_3)=(11;7,1,0).
```

For the two rows, the positive-half conjugate deviations obey respectively

```text
sum x_u=0,   sum x_u^2=64v,   x_u<=M,
(v,M)=(18,10), (22,18).
```

The quadratic Hermite majorant for `log(18+x)` at `-v/M` (with derivative)
and `M` gives exact two-point product bounds. Exact integer
cross-multiplication puts both bounds strictly below `514*p_min`.

Consequently 13, rather than 15, magnitude profiles remain in the
cofactor-`514` branch inherited by profile `(0,18,S=18)`.
