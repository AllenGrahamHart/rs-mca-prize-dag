# Audit

- The Hermite remainder has sign `(x-a)^2(x-M)`, so the majorant remains
  valid when `x<a`; no hidden lower endpoint is used.
- The two-point weights reproduce mean zero and variance `v` exactly.
- The exponent clearing in `(7)` and `(8)` accounts for all 64 conjugate
  pairs before denominators are cross-multiplied.
- The verifier checks the two strict comparisons as integer inequalities and
  pins independent margin hashes.
- This node removes magnitude profiles, not individual lag configurations or
  occupied ideals.
