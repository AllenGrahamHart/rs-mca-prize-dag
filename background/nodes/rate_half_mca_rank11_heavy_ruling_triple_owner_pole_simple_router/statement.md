# Triple-owner heavy-ruling pole-simple router

- **status:** PROVED
- **scope:** one heavy Segre ruling orientation on the official KoalaBear
  sextic row

Discard pair types owning at most two records. They cost at most

```text
2*58361=116722
```

records, leaving at least `322359637` records on triple-owner pair types.
Their common pair core has size below `K-2`, and one type owns at least
`5524` records. An anchor plus at most four further types recovers the
complete triple-owner core.

Choose three records from every further type and `32-3t` from the anchor.
After core-saturating the exact supports, their common intersection is the
recovered core. Exact cancellation gives dimension at least three, empty
common selected support, at least 20 explanations on the anchor line, one
off-line explanation, and slope-error degree in `20..31`.

The pure-locator output is impossible. Hence either

```text
chi>=2299571,
```

or there is a nontrivial scalar-locator rational certificate with
`deg Q<=67472`. In the rational branch:

1. there is no residual domain point with `Q=A=B=0`;
2. every root of `Q` belongs to at most one selected exact support; and
3. the total denominator-root support incidence is at most `67472`.

This is a pole-simple local rational interface. It does not pay the
rational profile or the high-complexity output and does not create a
whole-line owner.
