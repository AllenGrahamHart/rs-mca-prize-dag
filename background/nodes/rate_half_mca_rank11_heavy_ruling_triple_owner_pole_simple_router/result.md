# Result

A second actual heavy-ruling packet trades four units of slope degree for
three records from every represented pair. The resulting degree-`20..31`
packet has only two outputs:

```text
chi>=2299571,
```

or a rational profile in which common domain poles are impossible and all
denominator-root support incidences are singleton, with total at most
`67472`.
