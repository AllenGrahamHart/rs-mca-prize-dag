# Frontier

The rational output now has a root-free-on-support skeleton after deleting
at most `67472` singleton pole incidences across the complete packet. The
next useful theorem should do one of the following:

1. preserve enough exact support under that deletion to invoke rational-atom
   collision or packing;
2. use the 20-record anchor pair to classify the low-degree exception pencil
   `u+gamma v` of degree at most 11;
3. convert the high-complexity alternative using the explicit one-large-plus-
   at-most-four-triples pair-block decomposition.

Do not promote singleton pole incidence to a root-free whole-domain
certificate without an explicit repair.
