# Result

The complete `h=7` quadratic-color chamber is reduced, before any collision
split, to one fixed degree-fourteen polynomial intersected with the eight
`p+1` norm fibers on each row.

The 32 exact gcds are a small external-compute closure packet. No verdict is
recorded while Modal is spend-blocked.
