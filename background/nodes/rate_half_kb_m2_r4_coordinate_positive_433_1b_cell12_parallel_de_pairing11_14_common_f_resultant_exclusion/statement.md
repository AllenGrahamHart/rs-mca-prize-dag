# Statement

On the guarded product-rank-five positive `433-1b -> O0a` route in cell 12,
the parallel-`DE` labels `(0,11)` and `(2,11)` are empty over
`F_2130706433` for all four source-sign rows and all four target lanes.
Exact duplicate-copy and matching transport therefore close
`{(0,11),(1,11),(2,11),(2,14)}`. No other label or role cell is claimed.

An incomplete resultant root set, an unpriced leading boundary, or an
unreplayed common-`f` lift falsifies this statement.
