# Proof

The pinned cell-4 pairing-11 compiler is evaluated on the exact cell-12
four-basis tower. Its two paired-product equations are quadratic in the same
variable `f`; their division-free resultant is normed to the base field.
External `gcd(P,x^p-x)` reconstruction supplies every base-field root of the
norm and inverse guards.

The 32 rows contain 256 target roots, 464 root-or-exception candidates, and
592 guarded source points. Direct replay routes 96 missing-record
inconsistencies and 96 zero-product rows, then checks the remaining 400 source
points. Their two quadratic root sets have no compatible nonboundary quartic
survivor. There is no witness or unresolved branch. The rational leading rows
are paid by the boundary parent.

The generic-label orbit theorem maps `(0,11)` to `(1,11)` and maps `(2,11)`
to `(2,14)`. These bijections preserve every equation and guard, proving all
four labels.
