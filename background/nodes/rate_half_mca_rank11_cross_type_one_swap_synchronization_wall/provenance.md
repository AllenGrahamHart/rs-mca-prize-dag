# Provenance

- Packet and degree threshold:
  `rate_half_mca_order32_partial_relative_harvest`.
- One-swap synchronization:
  `rate_half_mca_rank11_multi_anchor_exchange_split_pencil_synchronization`.
- Aggregate target:
  `rate_half_mca_rank11_quadratic_quotient_population_router`.
- Collision theorem: upstream `experimental/grande_finale.tex`,
  `thm:collision-rigidity` and `cor:primitive-unique` at `93fba1be3`.
- The overlap bound and official collision-gap arithmetic are proved here.
