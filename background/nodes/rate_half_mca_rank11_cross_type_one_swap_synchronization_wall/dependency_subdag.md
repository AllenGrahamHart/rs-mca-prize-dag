# Dependency sub-DAG

```text
order-32 partial-relative interface ----------------+
multi-anchor one-swap synchronization --------------+--> cross-type
quotient population router -------------------------+    one-swap wall [PROVED]
```

The wall redirects the 520-type target toward lower-overlap rigidity, larger
packets, or chronology payment.
