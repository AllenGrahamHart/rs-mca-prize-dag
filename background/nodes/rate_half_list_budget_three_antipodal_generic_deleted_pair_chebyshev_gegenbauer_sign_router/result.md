# Result

The correctly posed first rejection is now a standard orthogonal-polynomial
system in one trace variable:

```text
T_(8L)(y)=epsilon,       C_L^(1/4)(2y^2-1)=0,
```

followed by one of six unsquared Legendre equations `(CGR5)`. This keeps the
source-torsion premise identified by the torsion-necessity fence while
removing the square and reducing the branch-polynomial degree.

CR-002-L should use this trace/sign router instead of the larger cleared
`r`-polynomials whenever its implementation can certify the coverage map.
The next theorem target is a characteristic-explicit gcd, subresultant, or
doubling descent for these six systems at `L=2^36`.
