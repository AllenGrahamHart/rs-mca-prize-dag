# Result

The `e = m = 2` stratum is nonempty — a theorem by certificate, killing
the emptiness route at `m = 2` constructively and re-pricing it as
expected-dead at every `m` (the `11m-4` count). The decisive endpoint
exclusion was never this route: the proved pole-interpolation node already
covers official-row endpoint profiles, and the witnesses live outside its
hypotheses.

The residual difficulty is undiminished and relocated: all witnesses have
`T = 0`, so possession of a syndrome pencil buys nothing at the splitting
layer — the live gate is (SAT3)-on-(L2), attacked through the sibling
(PAR) parametrization.
