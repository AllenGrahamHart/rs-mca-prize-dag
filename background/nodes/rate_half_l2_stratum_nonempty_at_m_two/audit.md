# Audit

1. Existence is claimed exactly as far as certificates reach: twelve
   objects, five fields, the published one replayed by BOTH verifiers.
   The eleven others are carried from the source bank (the draft's
   not-verified list), not re-certified here.
2. `verify_audit.py` is the coordinator's round-35 from-scratch verifier
   (no pilot code), pinned with its data in certificate.json — the
   strongest available form of second-code-path independence for this
   node.
3. The counting statements are separated by status and the two forced
   corrections (the `+4` mis-pricing, the dimension-21 excess component)
   are stated as corrections, not silently absorbed.
4. The a* convention ambiguity (D1) is carried with its resolution: the
   PROJECTIVE ruling of round 38, including the operational-inertness
   proof that de-fangs the choice on supported pairs. The round-35 F1
   sentence is annotated as convention-sensitive.
5. The stake section records why this node does NOT contradict the proved
   pole-interpolation exclusion — hypotheses checked on both counts.
6. Edge character: evidence only; no requirement discharged.
