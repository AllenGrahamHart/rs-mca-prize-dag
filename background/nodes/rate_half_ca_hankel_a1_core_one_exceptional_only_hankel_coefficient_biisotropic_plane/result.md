# Result

The remaining Hankel data now have a compressed geometric form: all
parameter coefficients of the minimal kernel span one maximal common
isotropic plane, with exact and transverse endpoint radicals. This replaces
quadratically many separate coefficient equations by one subspace contract.
