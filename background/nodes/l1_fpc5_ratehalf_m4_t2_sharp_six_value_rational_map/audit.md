# Audit

1. The complete fiber assertion is made only for `phi=1`; the other
   displayed containments need not exhaust their fibers.
2. Coprimality of `U_1,U_2` uses both candidate-wise primitivity and the
   fact that `F` has roots only in the core.
3. The two untouched petals impose exclusions, not prescribed fibers.
4. The six marked values are distinct because all four source labels are
   distinct and nonzero.
5. Full-domain coverage does not imply that `phi` takes only six values.
6. No map count or FPC5 payment is inferred from the descriptor.
