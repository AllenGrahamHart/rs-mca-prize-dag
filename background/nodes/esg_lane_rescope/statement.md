# esg_lane_rescope

- **status:** PROVED (audit theorems + closed forms; the re-scope is
  the coordinator adjudication of record)
- **minted:** 2026-08-06 (mint-4, round 17), coordinator-audited.
- **provenance:** notes/pilots_20260806/es_g_lanes/ (1,413/0,
  certified 140-digit comparator), coordinator-replayed; FABLE_AUDIT
  with round-18 addendum.

## Statement

THE (ES-G) TERMINAL RE-SCOPE OF RECORD: the round-15 "unified
terminal of four lanes" is WITHDRAWN — the four lanes' field
regimes are MUTUALLY UNSATISFIABLE (u2c needs log2 Q >= 255.9113;
band 1/4-1/8 low depth >= 255.99999994; band 1/16 >= 256 exactly,
i.e. NEVER — deficit >= 512 bits even at the cap; dli RES needs
< 256 STRICTLY by its own proved H2/A2). No single row satisfies
all four; the unification was of statement SHAPE, not regime.

Per lane: **u2c** — its pin IS the global form verbatim
(q^t >= 2^n); the five round-16 witnesses independently re-excluded.
**crossing** — (ES-G) applies at w >= 2^37 (19/19 admissible pairs
incl. deep strata); at w = 2^34 no admissible row clears the binding
deep stratum (requirement log2 p >= 256 = the rules cap) — and
round 18's THEOREM DSA proved this was detecting a REAL refutation
at tower rows, not a pricing artefact. **band (both nodes)** —
(ES-G) unavailable at rate 1/16 anywhere and on the low-depth 22.5%
of 1/4-1/8 scope at the banked pin; the q >= 2^209 pin computes the
RETIRED per-weight threshold (47.5 bits short); needs a re-posed
form (the round-18 TERNARY functional is the transfer candidate) or
a non-balance argument. **dli RES** — UNWIRED: above global balance
by its own proved scoping hypothesis (2^N >= q^L strict at every
admissible q); its flatness instruments are the route of record;
round-15's "discharges all four consumers" is REFUTED for this lane.

**|Z_w| CLOSED FORMS (banked, all 8 admissible p-classes):**
delta = 1: w-1; the two epsilon = -1 delta = 2 classes: 2(w-1);
2^40+1: 3·2^{v-1} - 1; the epsilon = +1 delta = 4 classes:
11·2^{v-2} - 1; the epsilon = -1 delta = 4 classes: 6·2^{v-1} - 2 —
with the bracket top delta(w-1) NEVER attained at delta = 4 (max
3(w-1), a 33% over-credit if priced at the top) and an orbit merge
at w = 2^38, 2^39 dropping the epsilon = -1 delta = 4 ratio to
exactly 2 (ratios are not w-uniform; do not transport).

**THE MECHANISM.** By THEOREM Q the extension degree is free for
the count while e divides the available log2 p: tower rows buy no
balance and pay full price — the adversary's best choice, confirmed
by round 18's refutations on exactly such rows.

## Falsifier

An admissible row satisfying all four lanes' field requirements; an
attained delta = 4 bracket top; a crossing row at w >= 2^37 above
global balance at some admissible pair.

## NOT claimed

The band effective-base question (if the base moves to the generated
field every band verdict worsens — open); the syzygy rank-bracket
end; any repair of the band pin (queued: re-derive under the band's
adopted functional).
