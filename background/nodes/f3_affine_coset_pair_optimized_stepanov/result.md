# Result

Every fixed nondegenerate affine intersection of two multiplicative
`H`-cosets has size strictly below `4n^(2/3)` at official aspect. This is the
correct pointwise input for the nodal DSP8 parameter branches and quotient
line fibers; aggregate uses still require their own ledger.
