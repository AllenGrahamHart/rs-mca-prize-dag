# Proof

Here `q=73`. In the active full-completion cell, `B_2` has size 24.
Write

```text
B_3=B_2 union A,       |A|=2,
B_4=B_2 union C,       |C|=3, A intersect C=empty,
B_5=B_2 union A union D, |D|=3.
```

The pairwise atlas labels this cell `F23__N4_t0__N5_t2`. Its two charges
are both `(u,g)=(29,6)`. More explicitly,
`W_4=H_3 intersect H_4` has dimension at least six and vanishes on
`B_2 union A union C`, while the positive support-`3/5` overlap gives
`H_5<=H_3`, with dimension six and zero set
`B_2 union A union D`.

Both six-spaces lie in the eight-dimensional `H_3`. Hence their
intersection has dimension at least four and vanishes on a union of size

```text
|B_2 union A union C union D|=32-|C intersect D| in {29,30,31,32}.
```

The exact replay applies the two `(29,6)` charges, the proved
nonseparable support-`4/5` ledger, and then each forced `(u,4)` charge.
For all four overlap values, the premium remains
`41411584407693108041789796771180703922717609427`. Exact comparison gives
the negative margin `-(W83)`. QED.

Independently, direct substitution in the proved baseline-cap formula gives
the stated branch-free premium and deficit. Hence neither deleting all
carrier information nor retaining only pairwise information pays this row.
