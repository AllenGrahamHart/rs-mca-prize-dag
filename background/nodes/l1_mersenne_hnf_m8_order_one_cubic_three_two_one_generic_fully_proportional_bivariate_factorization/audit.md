# Audit

The `z=9` exclusion uses `q!=0` and the fact that the official
characteristics do not divide `63*648`. It is scoped only to the fully
proportional endpoint.

The chart `1575-247z=0` is retained and solved linearly; it is not lost by
using the quadratic formula. Its denominators 247 and 231 are official
units.

The discriminant in (FBF5) is an ambient-field expression. No prime-field
square-class inference, Frobenius constraint, or norm conclusion is made.

The bivariate equation is only one part of (FPR5). All coefficient-zero,
role, `P_4`, generic, and arithmetic-lift obligations remain.
