# Statement

Over `F_2130706433`, the guarded cell-11 positive `433-1b` route is empty for
`xi=3`, residual matching `0`, and every source and target sign lane. Exact
outside-role transport gives the same conclusion for the partner label:
`{(3,0),(4,0)}` is empty.

An omitted rational `q` branch, norm root, lift, or colored lane falsifies
the statement. No other matching or role cell is claimed.
