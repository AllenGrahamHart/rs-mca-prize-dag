# Result

The complete `c=-1` order-one component is empty on all five official
next-to-maximal rows. Future order-one work may saturate by `c+1` and replace
`Phi_h=0` by the lower-degree residual equation `Psi_h=0`.

The order-one chamber and the global L1 target remain open.
