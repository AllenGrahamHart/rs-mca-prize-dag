# Dependency sub-DAG

```text
literal crosswalk ---------------------------+
F00-R02 / F00-R11 / F00-R20 exclusions -----+--> F01 inversion transport
colored quotient set-locator compiler -------+             |
                                                            v
                                      literal coverage TARGET (evidence)
```

The transport is restricted to the literal involution `b -> b^-1`.
