# Audit

The verifier checks exact custody, complete sign coverage, extension degrees,
all tower/lift zero tests, all product and sum identities, nonzero cofactors,
guard counts, and the compact missing-coordinate degree profile.  The hostile
audit mutates each semantic class.
