# Result

Column-farness removes the minimum-source equality branch. At the true
minimum union `rho+1`, the actual error values satisfy one exact barycentric
formula tied to `Q_min`, the moving locator `Q`, and the excess factor.
