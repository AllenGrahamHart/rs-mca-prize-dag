# Dependency sub-DAG

```text
442 outside-product compiler --\
                                    matching-template exclusion --ev--> band closure
442 invariant-product router ----/
```

This is a universal symbolic cut inside the 36-cell router, not an
enumerative or field-sampling result.
