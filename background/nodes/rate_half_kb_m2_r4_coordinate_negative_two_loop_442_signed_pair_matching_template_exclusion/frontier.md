# Frontier

Classify the remaining twelve matching templates in each affected cell,
starting with templates where one unsigned value meets a member of each
signed pair.  Treat the forced-`DF` cells separately because their residual
multiset has only one complete signed pair.  Use the invariant-sextic form
for whole-cell elimination before full twelve-row interpolation.
