# Audit

1. The affine Mobius chart is used only when `b+R!=0`; its excluded boundary
   is deleted separately from all five raw maximal product minors.
2. The boundary basis consequences are divided only by actual guards
   `R(R-1)(R+1)`, and its final resultant is exactly `4`.
3. The squared weld is sufficient for deletion: every unsquared survivor
   would satisfy it.
4. `R^2+1!=0` is a label-distinctness guard in the `M^2+1=0` cell.
5. The second interior basis is recomputed after adjoining `T`; it is not obtained by
   dividing the first ideal by a guard polynomial.
6. Both interior membership reductions have integral coefficients; no
   exceptional denominator characteristic is suppressed.
7. `b=-1` means the signed `B` pair equals the normalized signed `A` pair.
8. This cell deletion says nothing about seven-fiber completion of the three
   already compiled cells.
