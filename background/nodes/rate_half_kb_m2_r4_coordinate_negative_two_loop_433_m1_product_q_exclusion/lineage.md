# Lineage

The six-cell product cut left `M1` without a simple one-minor relation.  The
product map splits into a raw-minor boundary deletion and a short two-stage
interior elimination certificate.  This covers the projective chart
boundary explicitly rather than suppressing it in an affine normalization.
