# Claim contract

| field | content |
|---|---|
| claim | Cell `M1` has no guard-compatible common-`K` product/q packet. |
| deletion interface | Remove `M1` from the `(4,3,3)` two-loop frontier. |
| assumptions | The exact `M1` label cell, all five product minors, the second squared weld, odd characteristic, and actual distinctness guards; the proof covers both `b+R=0` and `b+R!=0`. |
| nonclaim | Deletion of `M2,M3` or compiled `X2,N1,L1`; complete packet/orientation closure; owner/payment movement; a row; or either Prize theorem. |
