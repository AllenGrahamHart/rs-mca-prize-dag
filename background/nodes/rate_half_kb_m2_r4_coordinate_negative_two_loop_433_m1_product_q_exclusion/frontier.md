# Frontier

Classify `M2,M3` with their own product-map parameterizations and chart-
boundary checks.  Do not include `M1` in any later common-`K` elimination.
After `M2,M3`, merge the surviving finite rows with compiled `X2,N1,L1`
for complete source-facet/seven-fiber assembly.
