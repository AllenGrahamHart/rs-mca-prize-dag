# Result

The cubic `2+2+2` core now carries its exact three-color restriction through
one symmetric affine invariant. The ordered degree-42 role layer has only
five characteristic-zero unoriented color shapes, and the generic branch is
an overdetermined four-equation system in `(x,q,d)` after eliminating `b`.
