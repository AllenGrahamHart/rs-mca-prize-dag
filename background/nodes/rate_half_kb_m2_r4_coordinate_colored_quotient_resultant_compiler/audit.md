# Audit

1. `C_H` is preserved by `b` because star transport preserves the `I-J`
   category; its colored roots are free, so the quotient divisor is reduced
   of degree two.
2. The right colored degrees are therefore `(2,2,0,0,0,0)`, not an
   arbitrary four-edge distribution.
3. `P_I,P_J` are even only because the coordinate theorem already proved
   `tau(I)=I`, `tau(J)=J`.
4. Both `Phi` formulas are direct products at `+/-t`; no square-root choice
   remains in the final `W` identities.
5. The coefficient bounds are inherited unchanged: `8` positive and `7`
   negative parameters.
6. No colored quadratic or resultant-system solution is constructed or
   excluded.
