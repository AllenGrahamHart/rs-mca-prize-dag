# Result

The surviving rank-two dual gate is now a degree-`e` split-orbit problem with
a fixed degree-seven boundary weight. It no longer requires evaluating the
degree-`2e` exceptional locator separately at both members of every pair.
Future symbolic or computational work should target `(DOR4)` in the two
retained field-order strata.
