# Result

`PROVED`: cofactor `514` is impossible in the prize profile
`(3,6,S=18)`. A subsequent exact child also removes cofactor `256`, so six
profile cofactors now remain.

The next shortest live branch is `m=64`, whose capped-product endpoint is
`V=130`, or `E=65`.
