# Result

`PROVED`: cofactor `514` is impossible in the prize profile
`(3,6,S=18)`. Seven profile cofactors remain.

The next shortest live branch is `m=256`, whose energy-adaptive endpoint is
`V=46`, or `E=23`.
