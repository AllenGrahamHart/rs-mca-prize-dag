# Result

At the tuple-incidence obstruction, every deficient target has exactly two
selected live rays. Their blocks cover all but at most five forced-root
points on the rates `1/4,1/8` rows and all but at most one on the rate
`1/16` row. After removing the forced-root locator, the complete kernel is
carried by polynomial cofactors of degree below six or two.

The next deficient attack should count this near-binary two-ray geometry.
The full-rank `c=0` split-divisor anti-concentration problem remains a
separate branch rather than the zero endpoint of a structure-free nullity
census.
