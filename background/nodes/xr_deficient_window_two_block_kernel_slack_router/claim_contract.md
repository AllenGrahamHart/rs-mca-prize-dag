# Claim contract

| field | value |
|---|---|
| claim | The deficient kernel-slack inequalities `(TKS1)`--`(TKS3)` and the exact official two-block pins `(TKS4)`. |
| inputs | Primitive multiplier-space factorization, `D subset G_d`, and pairwise-disjoint active blocks for selected live slopes. |
| output | Exactly two selected blocks covering all but `sigma` forced-root points, and multiplier cofactors of degree below `sigma+1`. |
| load-bearing hypotheses | Nonempty deficient `D`-local family and `sigma<r` for the exact two-block conclusion. |
| falsifier | A third selected block, excess uncovered forced roots, an overdegree cofactor, or an incorrect official remainder. |
| nonclaim | The router does not count target parameters, close the deficient target, or apply to the full-rank branch. |
