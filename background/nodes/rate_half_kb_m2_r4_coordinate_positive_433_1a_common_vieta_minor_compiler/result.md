# Result

PROVED.  The first saturated residual route now has an exact sixty-row
common matching atlas and six guarded determinant equations per row.  Exact
localization reduces their degrees from `18..23` to `6..12` and their term
range from `64..240` to `16..88`.  The uncontrolled common placement problem
is replaced by 360 explicit reduced minors; their solution sets and the
base-rank-drop branch remain open.

As route evidence, exact joint-gcd calculations completed on 32 of the 60
stripped rows and returned gcd one in every case.  The other 28 rows exceeded
the 220-second per-case cap.  This partial audit is not a universal
coprimality claim.
