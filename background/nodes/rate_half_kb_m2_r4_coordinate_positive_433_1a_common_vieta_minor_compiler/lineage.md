# Lineage

The signed atlas left source placement open.  The common source-label
geometry reduces that placement to the same fifteen matching cells used in
the banked negative common atlases, but the positive coefficient space has
eight columns.  This node derives the corresponding positive minors and
keeps the base-rank-drop branch visible.
