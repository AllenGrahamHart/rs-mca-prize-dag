# Audit

1. The two `AB+` records are distinct source records with identical target data; quotienting them gives nine role orbits but does not remove either row.
2. The source signs `epsilon_1,epsilon_2` concern square-root lifts and are not the outside five-cycle sign.
3. The loop sum row is retained in the six-row base even though its target sum is zero; it forces the linear `B_1` root.
4. Six pair minors are sufficient only under the explicit base-rank-six guard.
5. The first Modal attempt failed only in unsupported optional factor reporting; the corrected direct determinant run completed 60/60 rows.
6. Guard stripping removes only factors that are nonzero on the declared
   admissible stratum.  It is not saturation by unknown product-equality
   factors such as `b^2-c`.
7. The all-row raw/stripped run is `ap-rLz9QWatn3oqLIS2EFulCr`.  Gcd runs
   `ap-rVdUeiw77uxNVUWZhNYPqF` and
   `ap-pomv4nCcFPgOGLY1EkrD45` establish gcd one on 32 rows; 28 rows timed
   out and no claim is made about them.
8. No reduced minor ideal has yet been solved.
