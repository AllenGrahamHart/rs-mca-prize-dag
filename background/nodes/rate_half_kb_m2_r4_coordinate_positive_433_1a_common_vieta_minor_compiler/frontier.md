# Frontier

Factor and guard-strip the 110 distinct minor polynomials by role orbit,
then determine which three minors generate the pair-dependence condition
after localization.  Probe those nine representative ideals over the
deployed field before appending outside rows.  Audit `rank B<6` separately;
do not silently absorb it into the principal minors.
