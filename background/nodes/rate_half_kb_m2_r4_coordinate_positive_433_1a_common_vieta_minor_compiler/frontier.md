# Frontier

Use the rank-two quotient directly: on a chart where one nonloop sum-row
image is nonzero, its three pair minors generate the pair-dependence
condition; the all-zero-image branch is separate.  Compile that four-chart
cover instead of spending further time on general-purpose multivariate gcd.
Then probe the nine representative three-equation ideals before appending
outside rows.  Audit `rank B<6` separately; do not silently absorb it into
the principal minors.
