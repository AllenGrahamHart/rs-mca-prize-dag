# Frontier

Use the rank-two quotient directly: on a chart where one nonloop sum-row
image is nonzero, its three pair minors generate the pair-dependence
condition; the all-zero-image branch is separate.  The global product-base
successor proves `rank B=6` in all fifteen cells, so no base-rank-drop audit
remains.  Append outside rows to the principal chart systems instead of
spending further time on general-purpose multivariate gcd.
