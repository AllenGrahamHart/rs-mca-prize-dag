# Audit

1. The basis `B` has exactly `dim W` coordinates and injective evaluation on
   `W`.
2. High-core absorption is used before coefficient cancellation.
3. The received line contributes no coefficient of degree at least two.
4. Cancellation is polynomial-global because a word of `W` vanishing on an
   evaluation basis is zero in `W`.
5. The `981105` bound uses residual support-wise badness and invariant
   `n'-m'=R-d`.
6. Multiple affine owners are not summed, and rank-flat components are not
   silently classified as clones.

No computation is load-bearing.
