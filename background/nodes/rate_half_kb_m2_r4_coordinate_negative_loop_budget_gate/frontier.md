# Frontier

Split the five survivors by loop count `0,1,2`.  In the two-loop rows,
substitute the forced quadratic `A_1` before solving for the linear `B_2`;
in the one-loop rows, retain one residual root.  Apply the paired-product
involution gate to complete packet graphs before any remaining `q` lift.
