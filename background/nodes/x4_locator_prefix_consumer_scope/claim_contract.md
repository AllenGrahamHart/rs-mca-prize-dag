# Claim contract

- **Proved:** exact prefix-list bijection for polynomial boundary words.
- **Proved:** `t`-nullity controls only `p`-free locator-prefix coordinates in
  characteristic `p`.
- **Proved:** raw mode-at-null fails at `(F_17^*,A,t)=(F_17^*,9,1)`.
- **Consequence:** the live upper target is maximum-prefix, unless an explicit
  strip-aware max-to-null theorem is supplied.
- **Not claimed:** raw mode-at-null fails after every possible paid strip; the
  maximum-prefix target is proved; every received word is a boundary word.
