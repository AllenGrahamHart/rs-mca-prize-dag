# Dependency sub-DAG

```text
x4_locator_prefix_consumer_scope [PROVED]
  --ev--> u2c_exact_slice_extras_budget [TARGET]

u2c_giant_tnull_dichotomy [historical null/p-free route]
  --ev--> u2c_exact_slice_extras_budget
```

The proved node fixes the consumer quantifier. By DAG discipline the open
maximum-prefix target remains a leaf, so this theorem is attached as evidence
rather than as a logical premise.
