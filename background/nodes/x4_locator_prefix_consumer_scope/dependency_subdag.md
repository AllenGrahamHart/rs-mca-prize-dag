# Dependency sub-DAG

```text
x4_locator_prefix_consumer_scope [PROVED]
  --ev--> u2c_exact_slice_extras_budget [TARGET]
  --ev--> x4_primitive_star_u1_coverage [TARGET]
  --ev--> x4_exactlist_summed_budget [TARGET]

u2c_giant_tnull_dichotomy [historical null/p-free route]
  --ev--> u2c_exact_slice_extras_budget
```

The proved node fixes the consumer quantifier. The three open leaves divide
the structured population, primitive coverage, and final list-side sum, so
this theorem is attached to each as evidence rather than as a size premise.
