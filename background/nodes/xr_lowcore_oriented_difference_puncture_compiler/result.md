# Result

Every live repeated oriented difference is an invariant-excess puncture of
P-B itself. Its common supports are realized, not abstract, in two smaller
generic RS-line instances. The affine-rank compiler pays the two boundary
widths `K-1` and `K-2` per fixed difference.

The remaining obstruction is aggregate ownership across differences and the
lower reduced dimensions `K-t>=3`; neither is hidden by this result.
