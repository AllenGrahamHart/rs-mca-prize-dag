# Claim contract

| field | content |
|---|---|
| claim | Both deployed `S1` forced-loop parity cells are empty in common sign row `(1,1)`. |
| assumptions | The two cubic common components, exact mate `m`, nonsquare quadratic extensions, and uniform coefficient equations. |
| exact interface | Delete the final two `S1` cells in the representative common sign row; accepted product frontier at most 70. |
| nonclaim | Common-sign transport, `S0` or `S2` deletion, outside `q`, interpolation, coordinate or row closure, or either Prize theorem. |
