# Frontier

Prove a transport of the complete `S1` product deletion across the other
three common root-sign rows, or replay them explicitly.  In parallel, apply
the sparse solver to the six `S0` and four `S2` cells of the representative
common row.
