# Audit

- Used the ordered-pair action. A scale that swaps the two sides does not
  stabilize the ordered pair and creates no missing factor of two.
- Distinguished a left-anchor from an orbit canonicalization: the former has
  exactly `h` representatives per primitive ordered orbit.
- Used the unique order-`c` subgroup of the cyclic domain; a nontrivial
  stabilizer really is a common quotient scale.
- Kept the orbit census stronger than the stripped record target: no
  invariance of the other four deletion columns is required.
- Proved the official sparse-mean and target-below-support-count inequalities
  symbolically from `p>=n^2`, `h>=4`, and `n>=8192`.
- The max-route statement concerns the crude uniform-degree summation only;
  varying-degree or sparse-active-support arguments remain valid routes.
- No computation or emptiness claim is used.
