# Result

The abstract middle-Hankel regular factor is now the same exceptional form
`E` used by the reciprocal normal form. Contributor classifiers can divide
all maximal minors by `E`, reject any further common factor, and compare the
specialized quotient directly with the nonzero rank-one exceptional kernel
outer product.
