# Result

Rank two is now completely classified at support level. Pair cap three
excludes it. Under pair cap four, every trade uses four to six active block
rows and only six to eight active coordinates, with exactly five possible
zero-set profiles.

The contraction is uniform in the official agreement size and improves the
previous caps `12/12/9` and `13/13/10` to eight. Exact finite-field witnesses
realize all five profiles, so generic dual-product-code constraints cannot
remove the residual. A further payment must use the embedding of these
support atlases into the Maxwell block family, the strip ownership rules, or
the source-specific divided-difference data.

No Modal resource is used and no XR critical status is promoted.
