# Audit

1. The denominator is the one reconstructed by the product rows, not a new
   independent linear form.
2. Leading support gives `D(s)!=0` on all five common labels.
3. Constant and linear `A_1` specializations are included in the rank cap.
4. The sufficiency statement retains three distinct interpolation anchors.
5. No claim is made about the seven fibers outside `K`.
