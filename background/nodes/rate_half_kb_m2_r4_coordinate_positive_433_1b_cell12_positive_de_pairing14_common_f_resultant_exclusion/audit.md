# Audit

The structural verifier pins the adapter, compiler, result, shared root
certificate, and shared direct-audit source. The direct verifier reconstructs
the root union, replays all tower relations, recomputes both quadratic cuts,
all 64 missing-quartic lifts, and their nonzero colored cuts.

No pairing-11 or `xi=2` assertion is imported into this leaf.
