# Audit

The primary checker iterates every defect available at the repaired `s=1`
base.  The independent checker recomputes every printed frontier by a
separate target-dimension recurrence.  No value from the refuted
small-dimension affine payment is imported.
