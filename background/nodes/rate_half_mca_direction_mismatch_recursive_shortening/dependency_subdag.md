# Dependency sub-DAG

```text
global-core direction-distance router [PROVED] --+
common-core cancellation adapter [PROVED] -------+--> recursive shortening [PROVED]
                                                             |
                                                             +--ev--> rate_half_band_crossing_location
```

The direction router supplies the direct bound and defect coordinate.  The
cancellation adapter supplies the reversible one-coordinate child map.
