# Statement

The proved matching-5 exclusion and the exact parallel-`DE` involution exclude

```text
(0,5), (1,5), (2,5), (2,12)
```

in positive `433-1b` role cell 4. These are four labels in two quotient
orbits, hence

```text
4 labels * 4 source signs * 4 target lanes = 64 raw cases.
```

Together with the already-proved 21-label pairing-4/9 block, 25 of 105
labels in 14 quotient orbits are paid. The live ledger is therefore 80
labels in 46 quotient orbits.

The involution does **not** send `(0,5)` or `(1,5)` to matching 12. The
positive-`DE` labels `(0,12),(1,12)` remain open.
