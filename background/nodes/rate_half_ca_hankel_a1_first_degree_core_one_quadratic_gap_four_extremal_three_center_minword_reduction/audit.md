# Audit

1. At `j=p-1`, the macroscopic feasibility lower and upper bounds are both
   three; the third center is forced, not assumed.
2. The line deficit is at most one, so the three missing sets are disjoint
   and cover all or all but one of `U_0`.
3. `a_delta` is the triple-union excess over `d_min`, not its negation; both
   formulas in `(ETR4)` have the same sign.
4. The total excess `e` includes every off-line slope and already accounts
   for all heavy deficits.
5. At most `e` positive integer excesses among `3e` slopes leaves at least
   `2e` exact-zero excesses.
6. The second-difference coefficients annihilate any affine received line.
   Its codeword value is nonzero exactly because the third center is off the
   endpoint codeword line.
7. Support containment plus minimum distance at equal cardinality proves
   support equality; no cancellation pattern is assumed.
