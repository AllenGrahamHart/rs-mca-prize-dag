# Result

`PROVED`. The only pair attaining `3rho/2-1` lies on a unique three-center
line of total deficit at most one. Its `3e` off-line slopes have total
triple-union excess exactly `e`, so at least `2e` yield nonzero RS
minimum-weight second differences. The next gate is to couple or count this
large minimum-word family.
