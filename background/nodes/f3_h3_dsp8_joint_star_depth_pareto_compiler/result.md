# Result

The fixed-order H3 route now has six exact joint template choices rather than
one selected corner. Product-star strength increases while quotient-star
width decreases across

```text
(P,R)>=(25,12),(27,10),(29,8),(31,6),(33,4),(35,2).
```

Each corner has its own class-sensitive disjoint-six moment in `(JDP5)` and
an exact odd-local packet. The last corner is the coordinate-minimal
alternative:

```text
Dbar_16^0+(29/22)Dbar_16^A <=(319/153)n^2,
rho_16=(R-1)_+,       P>=35,
```

with pure product stars of degrees eight and six, one nonzero coupling
anchor, and one quotient-collision spoke. The prior `(33,4)` corner remains a
separate valid alternative with a wider three-spoke quotient packet.

No displayed moment or survivor bound is proved, so the two H3 target nodes
remain open. No Modal computation was used.
