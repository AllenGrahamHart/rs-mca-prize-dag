# Result

The fixed-order H3 route now has eleven exact joint template choices rather than
one selected corner. Product-star strength increases while quotient-star
width decreases across every integer point from

```text
(P,R)>=(25,12),(26,11),...,(34,3),(35,2).
```

Each corner has its own class-sensitive disjoint-six moment in `(JDP5)` and
an exact odd-local packet. Swap parity and the diagonal-target affine payment
more than double the uniform allowances at the five odd cutoffs
`E=7,9,11,13,15`; the exact row-sensitive budgets retain the measured
`S_(D,E)` term. The last corner in this eleven-row compiler is the
coordinate-minimal alternative:

```text
Dbar_16^0+(29/22)Dbar_16^A <=(319/153)n^2,
rho_16=(R-1)_+,       P>=35,
```

with pure product stars of degrees eight and six, one nonzero coupling
anchor, and one quotient-collision spoke. The intermediate `(34,3)` corner
retains the `(33,4)` product-star degrees with only two quotient spokes. All
three are separate alternatives with incomparable survivor sets.

The separate `E=17` single-quotient endpoint has the same parity mechanism.
No displayed moment or survivor bound is proved, so the two H3 target nodes
remain open. No Modal computation was used.
