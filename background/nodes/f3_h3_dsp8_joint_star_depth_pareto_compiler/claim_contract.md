# Claim contract

- **claim id:** `f3_h3_dsp8_joint_star_depth_pareto_compiler`
- **mathematical statement:** six exact accident-depth corners reduce C36' to
  class-sensitive disjoint-six moments and attach a matching joint packet of
  one product-side star or same-target edge packet, one nonzero coupling
  anchor, and the complete anchored quotient star
- **scope:** every official H3 row and
  `E in {6,8,10,12,14,16}`
- **consumers:** DSP8 correlation and the CR-001 fixed-order survivor route
- **status:** `PROVED`
- **proved dependencies:** accident-depth payment, disjoint-six multiplicity,
  excess-to-small-vector conversion, and coupling-batch odd saturation
- **new open content:** prove one complete moment in `(JDP4)` or `(JDP5)`, or
  generate and bound the corresponding official-order joint ideals
- **falsifier:** one retained abstract or actual fiber violating `(JDP2)` or
  `(JDP6)`, one table row failing to imply the accident-depth budget, or one
  official odd prime where `(JDP7)` has unequal localizations
- **nonclaims:** no moment bound, candidate enumeration, ideal-index gain,
  survivor bound, DSP8 promotion, or dominance between different corners
- **replay:**
  `tools/ramguard tiny -- python3 background/nodes/f3_h3_dsp8_joint_star_depth_pareto_compiler/verify.py`
- **independent audit:**
  `tools/ramguard tiny -- python3 background/nodes/f3_h3_dsp8_joint_star_depth_pareto_compiler/verify_audit.py`
- **upstream mapping:** primitive shift-pair control / exact second-moment
  ledger with a base-field-normalized split-pencil product packet
