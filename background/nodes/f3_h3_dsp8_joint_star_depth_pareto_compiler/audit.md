# Audit

The ratios in `(JDP3)` are sharp for the abstract endpoint profiles permitted
by the current centroid, distance-four, and overlap ledgers. This does not
assert that every endpoint profile occurs in an official row. Improving a
ratio requires additional product-fiber structure, not different rounding.

Every row of `(JDP5)` is a separate sufficient route. The product cutoff,
quotient depth, `rho_E`, class weight, and allowance come from the same row.
Mixing the `P>=35` product packet with the `R>=4` quotient payment is unsound.

The moments use disjoint distance-six edges. Replacing `N_6^disj` by all
distance-six edges enlarges the moment and remains a valid stronger target,
but the printed constants no longer describe the sharp current disjoint
floor.

The quotient packet retains `r_E` distinct lifts even though only `r_E-1`
couplings are guaranteed nonzero. The potentially zero coupling is inert as
an ideal generator but its lift records the exact quotient-depth predicate.
The number of nonzero generators gives no ideal-index lower bound.

The coordinate-minimal `(P,R)>=(35,2)` corner does not dominate the existing
`(33,4)` corner: each contains targets omitted by the other. It is a smaller
symbolic packet, not a proved smaller survivor set.

No numerical row evidence is used in the proof, and no Modal computation was
run.
