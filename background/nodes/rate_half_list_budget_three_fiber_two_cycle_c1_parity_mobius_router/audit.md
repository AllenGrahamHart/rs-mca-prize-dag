# Audit

1. The denominator parity order is `L=2^39`; source lifts have order
   dividing `4L=2^41`.
2. There are two source role patterns, not the matched square-root-lift tuple.
3. The outer ratio is eliminated through its unordered trace
   `(alpha^2-2gamma)/gamma`; no choice of `lambda,mu` is needed.
4. All three perfect matchings are retained in each role pattern.
5. The trace recurrence has 39 levels because `q in mu_(2^39)`.
6. The old matched harmonic exclusion does not automatically cover the new
   source equations. Only `R0` and `P0` are closed here.

