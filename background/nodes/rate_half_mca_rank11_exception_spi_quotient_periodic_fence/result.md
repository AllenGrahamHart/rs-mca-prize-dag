# Result

The bounded exception SPI has a real structured survivor. On the official
power-of-two domain, the power-map pencils `X^e-y` for `e=1,2,4,8` have
hundreds of thousands of pairwise-disjoint split fibers. The remaining
theorem must classify and pay quotient-periodic pencils rather than trying
to exclude twenty split fibers abstractly.
