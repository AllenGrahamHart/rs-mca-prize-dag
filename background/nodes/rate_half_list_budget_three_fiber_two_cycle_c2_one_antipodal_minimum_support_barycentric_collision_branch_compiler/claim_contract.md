# Claim contract

- **claim:** the one-pair completion curve factors into the exact `L` and
  `Q` branch union with the two printed intersections, its selected-ratio
  lift is `(BCB5)`, and outer splitting is equivalent to the single
  square-class test `(BCB6)`.
- **scope:** complete canonical one-antipodal `c=2` candidates attaining
  minimum barycentric mismatch support.
- **dependencies:** the proved one-pair collision normal form and the
  selected-pair torsion/field router.
- **falsifier:** a covered one-pair packet violating `(BCB2)--(BCB7)`.
- **nonclaims:** neither branch is excluded, no official-order gap is
  evaluated, and no larger-support case or adjacent crossing is closed.
