# Audit

1. The coefficient `-27` in `(BCB2)` is invertible only because the official
   characteristic exceeds three.
2. The two signs on the `Q` branch are exchanged by the allowed choice
   `rho -> -rho`; they are not two new selected-pair chambers.
3. The `L` and `Q` components meet at two points.  A search must use the
   printed first-match convention rather than count those packets twice.
4. Formula `(BCB5)` retains the factor `t` produced when denominators are
   cleared.  Dropping it changes the square branch.
5. The square-class equivalence is asserted only on the retained separable
   locus, where both discriminants and `z+12` are nonzero.
6. The `z=0` conclusion concerns the selected denominator pair.  It does not
   say the full denominator quartet is a scaled fourth-root quartet.
7. No official-degree polynomial, subgroup, or root cell is materialized.
