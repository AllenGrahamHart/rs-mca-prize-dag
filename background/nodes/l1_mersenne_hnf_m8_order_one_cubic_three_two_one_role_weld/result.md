# Result

The cubic `3+2+1` chamber is now four explicit p-free systems of five
equations in `(g_1,y,r,d)`. The role variable `lambda`, 42 scalar
specializations, and cyclotomic coefficient extensions are absent.
