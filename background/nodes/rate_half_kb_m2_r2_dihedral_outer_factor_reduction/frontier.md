# Frontier

The `(r,delta)=(2,4)` row is now the finite union

```text
source genus 0 or 1;
outer factor q_n Dickson/Chebyshev with n=2,3,5,6;
Gamma->C branch inertia:
  g=0: a,c,ac;
  g=1: c,c,ac,ac.
```

Next impose the actual source and active locator divisors on these four
factor degrees. Degree-only routing is recurrent and should not be repeated.
The `(4,2)` and `(8,1)` rows are unchanged.
