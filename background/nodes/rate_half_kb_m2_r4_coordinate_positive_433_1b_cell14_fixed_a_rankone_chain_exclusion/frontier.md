# Frontier

The only retained cell-14 cases are

```text
4 source signs * 4 target lanes * 3 missing de roles * 3 matchings = 144,
```

with matching indices `6,7,8`. All projected components in these three shapes
depend on both torus targets `z,f`; there is no univariate component for the
two-stage chain used here.

The next attack should construct two independent inner resultants from the
four mixed projections, then eliminate the remaining target. Pilot the three
structural shapes before scaling signs. Do not alter the frozen 432-case
compiler or return to dense five-variable bases.
