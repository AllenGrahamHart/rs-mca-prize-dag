# KoalaBear m2 r4 positive 433-1b cell-14 fixed-a rank-one chain exclusion

- **status:** PROVED
- **scope:** the guarded principal quadratic-cover branch of deployed positive
  `433-1b -> O0a` role cell `14`
- **dependencies:** the cell-14 quadratic-curve structure, signed outside
  atlas, complete-fiber Vieta compiler, and established rank-one chart
  boundary certificates

## Claim `(KBP1B14-FIXEDA-CHAIN-1)`

Write the seven signed outside product records as

```text
y = (de,de,-de,df,sigma_o ef,bf,sigma_c cf).
```

For every source-sign pair, every target lane `(sigma_c,sigma_o)`, every
missing-record index in `{0,1,2}`, and every matching index in

```text
{3,4,5,9,10,11,12,13,14},
```

the guarded cell-14 outside system is empty over `F_2130706433`.

The claim excludes exactly

```text
4 source signs * 4 target lanes * 3 missing de roles * 9 matchings = 432
```

of the previously retained 576 cases. Exactly 144 cell-14 cases remain:
the same source, lane, and missing-role census with matching indices
`{6,7,8}`.

This theorem does not close cell `14`, `433-1b -> O0a`, K3, LIST, MCA, or
either Prize problem.

## Falsifier

A guarded deployed-field zero in any one of the 432 systems, a live boundary
discarded by denominator or torus-monomial clearing, an omitted Cartesian
case, or a mismatch in the independent field-root replay.
