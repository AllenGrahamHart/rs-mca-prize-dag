# Result

The boundary `e`-th-root and dual `r`-th-power filters are now one exact
matching-free support test. Exceptional roots are labeled in
`F_q^* x F_q^*/(F_q^*)^r`; a valid perfect matching exists exactly when the
label multiset is invariant under one explicit involution.

This gives both full matching reconstruction and the compact aggregate
obstruction `Res(A,C)/(-Res(B,C))^e in (F_q^*)^r`. It uses no Modal resource
and does not close the distance-three chart.
