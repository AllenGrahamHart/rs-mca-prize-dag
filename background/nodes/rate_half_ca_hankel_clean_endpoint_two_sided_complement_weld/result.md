# Result

The clean irreducible cyclic norm now has complements in both directions.
Their elimination forces a nontrivial matrix-factorization identity

```text
W B=X-x_0 mod Q,       deg_z B<m.
```

This is strictly narrower than the norm statement: the remaining curve must
carry a low-parameter-degree element that divides a single domain fibre.
The next node extracts the resultant consequences of this unit equation.
