# Result

At maximal domain size, the B*=3 branch has no cubic or higher extension
fields. It consists only of prime rows with `p=1 mod 2^41` and quadratic rows
with `p=+/-1 mod 2^40`. This is the exact field split for subsequent bounded
cyclotomic or base-field-normalized chamber arguments.
