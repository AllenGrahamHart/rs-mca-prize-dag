# Audit

Coordinator sweep, 2026-08-17.

1. Every envelope identity recomputed independently (`verify.py`):
   both totals, both slacks, both adjacent-cutoff walls, the `#1171`
   ray-cap slack, and the inclusion-exclusion origin of the `#1172`
   overlap bound (`134066 = 2*1115609 - n`). All exact.
2. Structural clauses carried from upstream review (each packet:
   independent mathematics review GREEN, certificate/custody review
   GREEN; `#1171` additionally survived its own hostile-scope repair —
   the identically-zero-error import defect — before publication).
3. Subtraction check: no local anticode content; the local
   `relative_core_interpolant_ray_payment` (H_C frame, per-direction
   bound `70227214729697`) and `#1171`'s global pair-difference ray cap
   (`8147918`) are distinct objects in distinct frames — complementary,
   not colliding.
4. Controls noted upstream: exhaustive `Mat_2x2(GF(3))` maximal-clique
   and degree-`<3` `GF(5)` common-root controls (#1171); all `67471`
   legal cutoffs scanned + `GL_2(F_5)`/`GL_2(F_7)` projective-owner
   controls (#1172); exhaustive legal-cutoff scan (#1173).
5. The tight `#1173` slack (`2007222636724`, about 7.3e-6 of `B_*`)
   is a genuine near-boundary optimum — the adjacent-threshold wall
   (`17108854816460` over) is 8.5x the slack, so the cell is not
   knife-edge fragile, but any repricing of its inputs must be
   re-scanned.
