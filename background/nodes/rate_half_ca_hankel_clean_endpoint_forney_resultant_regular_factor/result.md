# Result

The marked cofactor ledger is now a normalized resultant theorem. After the
explicit power `a^(2rho+2)` is removed, the full Forney resultant is only the
degree-`m-1` regular factor `Delta`. The remaining clean theorem is therefore
a low-degree interpolation obstruction for the split-locator Forney products,
not a large resultant computation.
