# Source evidence

The proof is local.  It consumes the official corridor upper bound from
`x4_exact_slice_f2_guard_route_cut` and the residual wedge identified by
`x4_low_difference_johnson_positive_wedge_aggregate`.
