# Frontier

Work only with algebraically realized records in

```text
4e^2<=N(e+d),       e>=t_XR+d+1.
```

At `d=1`, use the fixed-projection `1024` codegree and bound a locator
projection.  For larger `d`, seek a split-locator determinantal constraint or
an exact first-owner payment.  Do not pursue another abstract constant-weight
packing bound.
