# Claim contract

- **Direct scope:** `xi in {0,2}`, pairing 11, 32 source-sign/lane rows.
- **Boundary:** all rational complements hit explicit route guards; all row-6
  `B`- and `C`-leading cases are checked against the exact tower equations.
- **Transport:** only the exact orbits `{(0,11),(1,11)}` and
  `{(2,11),(2,14)}` are used.
- **Conclusion:** four generic labels are empty; pairing 14 at `xi=0,1` is
  outside this node.
- **Out of scope:** the 30 labels in 9 active outside-role orbits that remain,
  K3, LIST, MCA, or Prize closure.
