# Lineage

- 2026-08-04: extracted the exact fiber-multiplicity extremum from the coarse
  triple count and isolated the one/two-member split-pencil endpoint after
  merging canonical `prize` commit `c21ad24b`.
- The terminology follows upstream's split-pencil census lane, but the claim
  here is local to the active-defect blocks and uses no upstream census as an
  imported theorem.
