# Frontier

Compile the 180 representatives by lane class and common-cell orbit, then
seek a second exact quotient inside `S0`. Do not exchange the opposite-signed
`BC` records. Any proposed further quotient must transport the complete
source normalization and outside labels, not only the target graph.
