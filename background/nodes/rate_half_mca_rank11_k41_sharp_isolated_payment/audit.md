# Audit

1. The prior `K'=41` endpoint is reused without deleting a kernel corank or
   changing a shadow deficit.
2. The rank-nine chart still scans every core and is maximized at core 40.
3. The active completion branch and premium are unchanged.
4. Demand is the exact integer `R C(m,11)-C(n,11)`, not a rounded density.
5. The isolated subtraction is made once globally.
6. Both the cleared record coefficient and the floor-record cross are
   positive.
7. `K'=42` is replayed and retained as a negative adjacent control.
8. No rank-eight or chronology target is declared paid.
