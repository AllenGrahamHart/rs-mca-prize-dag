# Claim contract

This node changes only the isolated-incidence lower bound.  Every capacity
term is the already audited `K'=41` endpoint of the full 55-shadow payment:
all kernel coranks, every rank-nine core offset, the shared support-two-
through-five branch maximum, and supports six through nine.

It closes the rank-nine component target at `K'=41`.  It does not pay the
rank-eight alternative on that row, close rank eleven, move an active v4
atom, or assert closure at `K'=42` or later.
