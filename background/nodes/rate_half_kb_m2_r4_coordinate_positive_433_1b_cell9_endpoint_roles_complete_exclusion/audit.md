# Audit

- The `A=0` branch is not discarded by division; product and sum row
  equations force the simultaneous `A=B=beta=0` scheme.
- The zero global section is not mistaken for an unconstrained pointwise
  kernel; the base child regularizes it directly.
- The final count is 30 role/matching labels, not an unproved quotient-orbit
  count.
- No conclusion about the other five roles is imported from their base-locus
  exclusion alone.
