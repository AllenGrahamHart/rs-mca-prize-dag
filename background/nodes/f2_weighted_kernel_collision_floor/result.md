# Result

The random-subspace first-moment scale is now a pointwise theorem in the
direction needed for falsification:

```text
Z(A)>=2^m/p^rank(A).
```

It recovers the canonical Round-18 `Z-FLOOR` result without importing the
false all-admissible proportionality-class classification. Saturated rows
remain open because this is a lower bound.

It also fixes the mass route's Fourier normalization: use `1+cos`, not
`1+2cos`.
