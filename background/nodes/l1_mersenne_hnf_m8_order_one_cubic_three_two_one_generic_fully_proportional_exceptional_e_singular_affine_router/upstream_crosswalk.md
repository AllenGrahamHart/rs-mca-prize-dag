# Upstream crosswalk

This is a finite adjacent-row primitive shift-pair compiler for the official
`h=7`, cubic `3+2+1`, fully proportional exceptional coefficient residue. It
replaces the upstream simultaneous `S_1=S_0=0` singular chart by two quartics
without assuming quotient flatness or a split-pencil census. It belongs in
the existing upstream h=7 cubic reduction packet.
