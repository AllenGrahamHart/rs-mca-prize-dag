# Result

`PROVED`: the simultaneous `S_1=S_0=0` exceptional coefficient chart is no
longer a bivariate singular locus. It is exactly a pair of quartics in
`z=b^2`, with `b` reconstructed by one rational formula.

The quartic common-root decision and every downstream `q`, structural, role,
`P_4`, saturation, and arithmetic-lift condition remain open. No Modal
computation was used.
