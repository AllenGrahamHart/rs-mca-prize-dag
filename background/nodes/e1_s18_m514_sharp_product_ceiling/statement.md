# E1 square-mass-18 cofactor-514 sharp product ceiling

- **status:** PROVED
- **closure:** sharp two-moment product extremum and exact rational certificate
- **scope:** every conductor-256 square-mass-18 collision, cofactor `514`

Let `F` have coefficient square mass 18 and integral positive-half
autocorrelation energy

```text
E=sum_(d=1)^63 A_d^2.
```

If `|Norm(F(zeta_256))|=514p` on a prize-envelope row, then

```text
E<=17.
```

Equivalently, the sharp fixed-mean/fixed-variance product envelope is below
`514*p_min` at every energy `E>=18`. At the boundary `E=18`, all 57 feasible
two-level chambers are certified by exact rational square-root intervals; the
closest chamber has 63 lower entries. The same envelope is still above the
field floor in that chamber at `E=17`, so no lower cutoff is asserted.
