# Claim contract

## Consumed content

- Collision norm divisibility identifies the nonzero conductor-256 norm.
- The prize field floor gives `p>=B_P 2^128`.
- Odd-character orthogonality and autocorrelation Parseval are rederived in
  the proof for arbitrary coefficient square mass 18.

## New proved content

1. The sharp two-moment product extremum has the two-level form `(2)`.
2. Each feasible chamber decreases strictly with positive variance.
3. All 57 chambers at `E=18` lie below `514*p_min`.
4. Every cofactor-514 collision therefore has `E<=17`.

## Guards

- The theorem uses square mass only; it does not assume a coefficient
  magnitude profile or an autocorrelation realization.
- The exact certificate encloses irrational square roots outward.
- The `E=17`, `j=63` lower bound is above the threshold, so the method is not
  silently extended beyond its proved endpoint.
- No middle energy or prime-ideal occupancy is decided here.
