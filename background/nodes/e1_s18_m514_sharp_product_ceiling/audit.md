# Audit

- The moment normalization is pinned: `V=2E` and
  `sum_u(y_u-18)^2=64V=128E` over 64 positive conjugate pairs.
- Boundary points have zero product and cannot beat the positive interior
  maximum.
- Every feasible interior extremum has at most two levels.
- The monotonicity derivative is negative for each fixed feasible chamber.
- At `V=36`, exactly `j=7,...,63` are feasible, giving 57 comparisons.
- Every irrational endpoint is enclosed with denominator `2^192`.
- The closest excluded chamber is `(V,j)=(36,63)`.
- The adjacent boundary `(34,63)` remains above threshold.
