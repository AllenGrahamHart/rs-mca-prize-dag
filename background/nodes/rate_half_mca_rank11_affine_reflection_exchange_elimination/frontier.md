# Frontier

The nonzero additive-reflection class no longer survives as a rational leaf.
It is routed into the existing high-complexity terminal.

The rational split-pencil frontier now consists of:

1. the antipodal `c=0` quotient-periodic class;
2. other cyclic or dihedral fractional pullbacks;
3. extension-field normalization or descent failures not forced into the
   base-field reflection form; and
4. genuinely nonaffine/primitive degree-`1..11` pencils.

In parallel, the route still needs a payment for `chi>=2299571`. Further
affine cyclotomic computation has no current value.
