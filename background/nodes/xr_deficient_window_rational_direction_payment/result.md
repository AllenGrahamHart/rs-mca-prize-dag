# Result

- **result:** NARROWED
- **DAG delta:** no new open premise and no status change
- **mathematical delta:** every deficient pair exposing any selected
  off-core point outside `G_d` is paid with total cost at most
  `n-|G_d|`; the red leaf now counts only the all-selected-rays
  `G_d`-local family.
- **budget delta:** the red leaf receives the exact complementary allowance
  `17n^2-25(n-|G_d|)`.
- **next action:** formalize the scalar-error factorization for the local
  primitive Padé family and use it to count or strip the remaining
  parameters.
