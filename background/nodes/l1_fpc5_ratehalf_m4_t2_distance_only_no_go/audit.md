# Audit

1. The forbidden neighborhood includes intersections at least `L`, so the
   selected family has intersections at most `L-1` exactly.
2. The complement of a `(2L+1)`-set has size `3L+4`, producing the second
   binomial factor in (NG3).
3. The fixed background block is retained; it is not discarded to obtain an
   easier asymptotic model.
4. Positive entropy exponent refutes only the sufficiency of distance data.
5. No locator-flat membership or received-word realization is inferred from
   the greedy set-system construction.
