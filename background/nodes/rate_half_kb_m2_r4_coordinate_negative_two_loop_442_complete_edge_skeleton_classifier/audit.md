# Audit

1. The proof counts edge orbits, not individual stars.
2. Both common loops are already distinct roots of the same global `A_1`.
3. Nonloop common records prove `A_1` is nonzero.
4. The internal multiplicity cap two follows from product injectivity, not a
   simple-graph assumption.
5. Both colored records use the deficient `C` pair, but distinct `I` pairs.
6. The verifier exhausts the degree equations; the audit independently
   computes the same/split attachment solutions.
