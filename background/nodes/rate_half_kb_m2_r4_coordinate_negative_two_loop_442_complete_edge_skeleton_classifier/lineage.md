# Lineage

The `(4,4,2)` common-`K` campaign left seven outside records.  Spending the
two loop roots of `A_1` and solving the exact source-facet degree ledger
reduces them to one signed-pair skeleton, parallel to but distinct from the
`(4,3,3)` completion.
