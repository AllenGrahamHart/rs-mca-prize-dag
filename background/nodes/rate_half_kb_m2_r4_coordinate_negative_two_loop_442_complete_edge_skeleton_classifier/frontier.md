# Frontier

For each of the six exact common-`K` product rows, combine `(KB44S-1)` with
the product involution fixed by its two internal antipodal label pairs.
Gauge the three singleton edge signs, classify the forced missing-label
product, and use invariant binary sextics before full interpolation.
