# Dependency sub-DAG

```text
rate_half_arbitrary_line_syndrome_router [PROVED]
                         |
                         | ev / exact attack model
                         v
              rate_half_band_closure [TARGET]
```

The edge is evidentiary. The router creates no conditional close.
