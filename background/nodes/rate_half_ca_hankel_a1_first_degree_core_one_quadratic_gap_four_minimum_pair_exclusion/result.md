# Result

`PROVED`. The `rho+3` actual-support boundary is empty in both core-one
quadratic `u=4` arms. Every pair has union at least `rho+4`, giving a
four-per-center line cap and a stronger linear-size triple-expansion set.
The next live boundary is rank three at pair union `rho+4`.
