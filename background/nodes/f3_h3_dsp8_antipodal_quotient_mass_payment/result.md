# Result

The expensive antipodal overlap cap is now charged only on antipodal
quotient mass. The live DSP8 primitive shift-pair target becomes

```text
10K_25^0+17K_25^A+152S_A<=750n^2-375(n-1)(n-2).
```

A uniform proof with constant `36781/100` also suffices. Mattarei's pointwise
bound improves the preceding `29031/80` constant while preserving every
class weight and normalization.
