# Result

The expensive antipodal overlap cap is now charged only on antipodal
quotient mass. The live DSP8 primitive shift-pair target becomes

```text
10K_25^0+17K_25^A+152S_A<=750n^2-375(n-1)(n-2).
```

A uniform proof with constant `29031/80` also suffices. This replaces the
former constant `223`, relaxing the open disjoint-correlation estimate by
about `62.7%` while preserving every class weight and normalization.
