# Result

`PROVED`. The first surviving quadratic pair boundary is an explicit
rank-two split pencil

```text
eta_x L_X'(x)Qbar(-;x)=A+xB.
```

Its row coordinate is injective, its residual root sets are disjoint after a
common gcd, and positive endpoint rank loss forces a linear-size common gcd.
The remaining task is to contradict or pay this exact pencil.
