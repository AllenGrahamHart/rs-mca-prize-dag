# Provenance

Derived locally on 2026-08-18 from the heavy-plane Segre normalization and
the general support-local margin/interleaving theorem. All endpoint and
pair-list arithmetic was recomputed exactly in two implementations.

The upstream comparison targets are PR `#1173` at `2788d5ec3`
(factor-flag synchronization) and the primitive shift-pair/split-pencil
interfaces surrounding PR `#1170` at `d296510e80`. No upstream theorem is
used to identify or pay the new ruling buckets.
