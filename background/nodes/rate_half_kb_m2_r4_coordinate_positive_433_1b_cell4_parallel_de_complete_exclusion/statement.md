# Statement

## Claim `(KBP1B4-DE-ALL-1)`

On the guarded positive deployed `433-1b -> O0a` role-cell-4 branch, every
labeled slice with missing role `xi in {0,1,2}` and canonical matching index
in `{0,...,14}` is empty for all four source-sign pairs and all four target
lanes.

The seven parent blocks partition the label rectangle
`{0,1,2} x {0,...,14}` into matching sets

```text
{0,1,2}, {3,6}, {4,9}, {5,12}, {7,10}, {8,13}, {11,14}.
```

Thus all 45 labels and `45*4*4=720` raw atlas cases are paid. No endpoint,
outside-role, other role-cell, complete branch, or Prize claim is made.
