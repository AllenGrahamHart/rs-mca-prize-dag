# Source evidence

The proof is a finite set-theoretic composition of seven PROVED parent
nodes. `verify.py` reads their manifests and the compiled DAG, checks the
exact requirement edges, independently constructs every parent label set,
and proves pairwise disjointness and equality with the 45-label rectangle.
