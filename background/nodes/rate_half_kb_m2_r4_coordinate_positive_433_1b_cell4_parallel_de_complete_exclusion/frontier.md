# Frontier

The parallel-`DE` part of positive role cell 4 is closed. The complete cell
also needs the already proved endpoint block `xi in {5,6}` and the separate
outside-role assembly `xi in {3,4}`.
