# Result

`PROVED`: profile `(6,6)` is impossible at `N=256`, folded profile `(3,4,0)`,
and `V=60` on every pair-feasible row.

Together with the quotient, two-odd, `(4,2,2)`, and `(5,4,1)` exclusions, this
removes the final profile in the exact `V=60` reduction.
