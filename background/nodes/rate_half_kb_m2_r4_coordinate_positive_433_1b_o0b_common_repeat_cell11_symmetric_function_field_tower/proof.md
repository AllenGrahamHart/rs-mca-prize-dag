# Proof

The symmetric-tower certificate substitutes the already certified `t`
relation into the exact guarded cell-11 common ideals and adjoins `x=bc` and
`y=b+c`.  Exact elimination over `F_2130706433` gives four generators in
`r,y,x` and an ordered five-generator lift in `b,r,y,x` for each of the eight
sign rows.  In every ordered lift the last generator is monic linear in `b`.

The two-relation certificate localizes by `x*y*(x-1)*(x+1)` and computes
standard bases for the full four-generator ideal and the ideal generated by
the plane equation and generator 1.  Both ideals have dimension one, and all
four full generators have zero normal form modulo the two-relation ideal.
The leading terms give the six- and four-element bases printed in the
statement.

The projected closure meets the chart boundary, so closure-level saturation
alone is insufficient.  The separate chart-coverage certificate returns the
unit ideal after adjoining
`b*c*(b+c)*(bc-1)*(bc+1)=0` to each original guarded source ideal.  Thus no
actual guarded source point is lost by the chart.  QED.
