# Dependency sub-DAG

```text
complete-chart projective caps ----\
                                     +-> integer-gap envelope
support-local transversality -------/
```

The first parent controls exactly `t=0`.  The second controls every chart
pointwise and supplies the one-turn endpoint argument on `t>=1`.
