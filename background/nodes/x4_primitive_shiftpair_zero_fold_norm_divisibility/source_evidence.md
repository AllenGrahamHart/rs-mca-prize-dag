# Source evidence

The proof is local and uses only the dyadic router, the Haar norm-product
gate, cyclotomic irreducibility, and exact norms of dyadic cyclotomic values.
The verifier checks every signed pair in small dyadic orders and every
resulting active/zero pattern.
