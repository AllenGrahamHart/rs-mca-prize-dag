# Frontier

Replay `(ZF-2)` at each consumed row and exact `(e,d)` cell.  For every
surviving zero pattern, use the integral factorization

```text
(X-1) product_(a in Z)Phi_(n_a)(X) | C(X)
```

as the structural input to a quotient/staircase owner or a direct count.
Do not assign that owner from divisibility alone.
