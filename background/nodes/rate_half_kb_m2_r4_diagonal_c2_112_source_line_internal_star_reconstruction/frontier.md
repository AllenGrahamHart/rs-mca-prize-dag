# Frontier

For each of the at most eight reconstructed source-deck pairs, evaluate the aligned or
near-aligned quotient identities `Q_J~K_5^2 chi` and
`chi Q_I~R_7^2`. Only survivors need the remaining source-row and endpoint
tests. This is a finite exact calculation per label record, not a parameter
search.
