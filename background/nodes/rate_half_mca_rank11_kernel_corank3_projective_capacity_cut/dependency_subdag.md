# Dependency sub-DAG

```text
proved corank-two cut --------------\
uniform corank-three cap target -----+-> conditional corank-three cut
multi-step shadow hierarchy --------/
```

The first parent carries the proved uniform `M_2` resource and cutoff.
The remaining target adds uniform `M_3`; the hierarchy supplies the exact
primal inequalities.  The implication stays amber until that one target
closes.
