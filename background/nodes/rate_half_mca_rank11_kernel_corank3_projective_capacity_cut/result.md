# Result

CONDITIONAL: the corank-two cap and cutoff are proved.  A uniform
corank-three projective cap would move the exact dominant-kernel cutoff
through `K'=796598`; the conditional resource comparison first reverses at
`796599`.
