# Frontier

Retain only `12:{Z2,Z3,Z4}`, `13:{Z1,Z2,Z3}`, and
`14:{Z1,Z2,Z3}`.  Reconstruct `A_1`, place any outside loops at its roots,
then assign the `eta` and six `L^c` q rows.  Do not enumerate quotient
placements for the six deleted product types.
