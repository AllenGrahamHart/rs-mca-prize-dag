# Lineage

This extends the old-orbit product router to the three BC-singleton common
cells.  Their negation and reciprocal involutions create a different, but
again exact, product-level deletion pattern.
