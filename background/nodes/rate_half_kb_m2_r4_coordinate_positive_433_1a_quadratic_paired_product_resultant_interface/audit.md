# Audit

1. The theorem distinguishes the missing mate `xi` from `eta`; they agree
   only when `L=I`.
2. The five `eta` choices are internal target records, while the seven
   `xi` choices range over every outside target record.
3. The resultant is used only as a necessary condition.  It can retain a
   root already used by `K`, a zero source label, or coincident source
   pairs; those guards remain mandatory.
4. Formula `(KBPQI-4)` is the full quadratic resultant and remains valid
   on degree-drop loci.
5. Equation `(KBPQI-7)` is squared only after retaining the original
   nonzero source-label guard; it is necessary and may admit a sign lift
   that later rows reject.
6. The finite-field probe covers only aligned `xi=eta`, permits relaxed
   source and target choices, and imposes only the missing-mate sum row.
7. Exact aligned counts are in
   `rate_half_kb_positive_433_1a_outside_product_probe_result.md`; Modal
   runs are `ap-GbxO9Tzy9FDXWd2iXX7urQ`,
   `ap-qsdj1fpjiAdp2fcONyLWT9`, and
   `ap-AfDUnxANc4VDtbuKAv219S`; the clean missing-mate-sum replay is
   `ap-zH5YzdeJ1cG4hfyK6Q9eTJ`.
8. Two single-case exact aligned eliminations timed out under explicit
   caps.  They justify avoiding raw Groebner fanout and prove no
   mathematical nonexistence statement.
