# Frontier

Split aligned `xi=eta` from near-aligned `xi in L^c`.  In each branch,
substitute `(KBPQI-3)` and `(KBPQI-7)` before expanding any three-resultant
ideal.  Use target sign and matching symmetries to reduce the 525 formal
cases, then append one of the six remaining outside sum rows.  The aligned
small-field probe localizes its `F_29` product survivors to role cells
`5,12`; the missing-mate sum retains only cell `5`, cycle `+1`.  This is
evidence, not a universal cell deletion.
