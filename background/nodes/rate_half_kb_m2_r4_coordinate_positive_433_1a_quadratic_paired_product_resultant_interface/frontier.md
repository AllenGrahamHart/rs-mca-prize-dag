# Frontier

Split aligned `xi=eta` from near-aligned `xi in L^c`.  In each branch,
substitute `(KBPQI-3)` before expanding any three-resultant ideal.  Use
target sign and matching symmetries to reduce the 525 formal cases, then
append the outside sum row at the forced mate.  The aligned small-field
probe localizes its only `F_29` relaxed survivors to role cells `5,12`;
this is evidence, not a universal cell deletion.
