# Audit

1. The selected `n=6` pole fiber is generic because all six poles of `F`
   are distinct and unramified in the dihedral quotient.
2. Both internal sign involutions are fixed-point-free on that fiber; this
   is what reduces two perfect matchings to the coincident, `V4`, and `S3`
   cases.
3. The commuting exception is rejected as a second Dickson-six structure,
   not merely because one preferred coordinate fails.
4. The order-three matrix normalization uses scalar matrix freedom, not an
   unlicensed change of the first Dickson coordinate.
5. The source-cover comparison retains the relative projectivity and uses
   the already-proved `a=1`, `d^2=3` specialization.
6. Both integer resultants are nonzero modulo `p=2130706433`; no
   characteristic-zero-only division is imported.
7. The conclusion deletes one factor profile and moves no owner or ledger
   quantity.
