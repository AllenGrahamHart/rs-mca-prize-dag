# Frontier

Do not continue a two-profile search. The full-V4 dihedral frontier is now
only `n=3`, where the six poles split as two generic cubic fibers in each
endpoint coordinate. The next useful theorem must classify a six-point set
that is simultaneously a union of two `D_3` fibers under the forced
relative branch projectivity, then impose the degree-ten common outer map
and complete source locators. A contradiction deletes the full-V4 type; a
survivor requires an actual owner/payment analysis.
