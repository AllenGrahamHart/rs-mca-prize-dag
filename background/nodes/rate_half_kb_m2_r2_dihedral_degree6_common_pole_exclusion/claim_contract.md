# Claim contract

- **Claim ID:** `rate_half_kb_m2_r2_dihedral_degree6_common_pole_exclusion`
- **Input:** an actual residual full-V4 `n=6` profile, its common degree-30
  outer function, six distinct order-five poles, and the proved relative
  source-cover branch quadratic.
- **Output:** the `n=6` profile is impossible; `n=3` is the sole residual
  dihedral factor degree in the full-V4 type.
- **Dependencies used:** the exact unramified `n=6` pole profile and the
  source-cover twist classifier with `a=1`, `d^2=3`.
- **Not claimed:** nonexistence or construction of `n=3`, deletion of the
  other two `m=2` transverse types, an owner, a payment, K3, an endpoint
  row, KoalaBear, or either Prize problem.
