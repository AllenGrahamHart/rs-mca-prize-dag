# Result

The general-pullback evaluation kernel is exactly
`max(0,k-sb)`. For every listed codeword it is at most
`max(0,z-ell+1)`. Thus low partial loss automatically removes or bounds the
kernel, and the positive Johnson router is polynomial without a separate
sparse-coverage assumption. Only the nonpositive gate, growing loss excess,
wild maps, or missing anchors remain.
