# Lineage - L1 pullback coverage/kernel tradeoff

The general pullback descent exposed `kappa` as a sum over residue-class
dimensions, while the partial router treated it as a separate bounded
parameter. Summing those dimensions exactly identifies `kappa` with uncovered
code dimension. The list threshold then forces every such deficit to reappear
as partial agreement loss, eliminating one independent escape axis.
