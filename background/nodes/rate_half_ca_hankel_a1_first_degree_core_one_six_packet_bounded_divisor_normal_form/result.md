# Result

Every core-one scalar packet is now a bounded divisor problem: tails have
degrees at most `6`, `4`, and `7`, and the remaining line-bundle class has
degree exactly two with at most six positive and four negative points.
