# Result

Conditional exact payment.

Once the class-orbit leaf is proved, profile `(0,18)` leaves the stronger
next-profile cap `1971`, allowing at most seven complete 256-vector orbits in
profile `(4,4,S=20)` under a profile-only debit.
