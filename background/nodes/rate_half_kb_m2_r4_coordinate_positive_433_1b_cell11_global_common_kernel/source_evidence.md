# Source Evidence

`rate_half_kb_positive_433_1b_cell11_compact_kernel_result.json` records four
completed exact Singular reductions, one per source-sign pair. The source
run was Modal app `ap-XVGCXYXryoBPGqTwLHfFy2`.

The verifier pins launcher and result digests, checks embedded custody of the
common compiler, product-cofactor compiler, and 24-chart common structure,
and verifies the complete shape and zero-remainder ledgers. It also
reconstructs the seven formal row identities directly from the stored
kernel. The independent audit rebuilds all ten sign-specific rows from the
source compiler and includes a kernel mutation test.
