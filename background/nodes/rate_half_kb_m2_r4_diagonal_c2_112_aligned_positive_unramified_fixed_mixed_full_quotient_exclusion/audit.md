# Audit

1. The reciprocal degree-five route uses an affine minor-conic resultant,
   so function-field denominator and rank-drop specializations are retained.
2. It regenerates all 25 norm factors, applies only the exact `d|6` field
   criterion, and replays every deployed `p,w,b` candidate in the original
   equations and forbidden set.
3. The four-point quadratic-field certificate is regenerated byte-for-byte;
   every point first reproduces the q-slice and then fails both quotient
   norms.
4. The linear route uses the raw first-pair kernel conic. Its degree-116 norm
   therefore includes the three exceptional factor families absent from the
   degree-28 residual-conic norm.
5. All ten linear norm factors and all twelve nonboundary `w` branches are
   replayed; nine `w` branches are forbidden and three have no common `b`.
6. The off-common checker classifies the full `5 x 4 x 1` cofactor grid. Its
   five distinct deployed endpoints are all base boundary.
7. The claim is a full-quotient exclusion, not q-slice emptiness.
