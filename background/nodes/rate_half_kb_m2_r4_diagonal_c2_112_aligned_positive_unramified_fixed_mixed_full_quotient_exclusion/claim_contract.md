# Claim contract

| field | value |
|---|---|
| claim | No admissible aligned-positive unramified `c2(1,1,2)` fixed-moving mixed-allocation candidate over `F_(2130706433^6)` satisfies both the q-slice and full colored quotient identities. |
| field | `F_(2130706433^6)`; exact arithmetic over `F_2130706433` and every relevant subfield |
| template | fixed-moving internal edges `{2,1/2}` and `{2,b}` |
| allocation | mixed residual-square allocation only |
| normalization | exact relative `U/V` scale retained before the fixed-`b` quadratic gate |
| open set | endpoint, discriminant, orbit, reconstruction, and scale-denominator factors are explicit |
| consequence | the last aligned-positive unramified cell is excluded; all six allocations in this packet are closed |
| nonclaim | the q-slice is not empty; no automatic promotion of packet assembly, row payment, `rate_half_band_closure`, or either Prize theorem |
