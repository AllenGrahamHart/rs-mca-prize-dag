# KoalaBear c2 (1,1,2) aligned positive unramified fixed-mixed full-quotient exclusion

- **status:** PROVED
- **scope:** the aligned positive, unramified, fixed-moving internal-edge
  template with the mixed residual-square allocation over
  `F_(2130706433^6)`
- **requires:**
  `rate_half_kb_m2_r4_diagonal_c2_112_source_line_internal_star_reconstruction`,
  `rate_half_kb_m2_r4_diagonal_c2_112_source_line_q_slice_resultant_gate`,
  and
  `rate_half_kb_m2_r4_diagonal_c2_112_source_line_colored_quotient_compiler`
- **consumer:** `rate_half_band_closure` (evidence edge)

Normalize `J_0={2,1/2,b,1/b}`, `q(T)=T^2+tT+p`, and `w!=0`. Exact scale
retention makes the four q-slice equations quadratic in `b`.

On the reciprocal degree-five common component, a direct affine
minor-conic resultant gives a degree-338 norm with 25 factors. Exact
deployed-field replay gives 19 endpoint candidates: nine boundary, six
empty, and four quadratic-field q-slice points. Every survivor reproduces
`(KBQS-1)` but fails both full quotient norms.

The common linear component `4p+5t+4=0` carries a quadratic rank curve in
`w`. Its intersection with the raw kernel conic has degree 116 and ten
factors. Exact replay of all ten fields gives four base-boundary cases and
twelve remaining `w` values: nine forbidden and three with no common `b`.

After common/open removal, the three projections have `5`, `4`, and `1`
residual cofactors. All 20 off-common combinations yield only five distinct
deployed `(p,t)` values, all base boundary. The remaining common factors are
`p=0` and `t^2-4p=0`, also boundary. Therefore no deployed-field fixed-mixed
candidate satisfies the full colored quotient system. All six
aligned-positive unramified allocations are closed, while later packet and
row assembly remain open.
