# Bounded spread probe

The exact cyclic construction was also tested for every `7<=e<=30` against
the support conclusion `(ICS11)--(ICS12)`. Every pair had the required
number of expanding third blocks. The observed minimum was

```text
e+3,
```

well above the required `3`, `4`, or `5`.

This is numerical evidence only. The proved statement of this node is the
degree-sequence realization `(AID2)`; no general triple-expander theorem and
no codeword-center assignment is claimed.
