# Result

The exact `u=4` locator degree sequence has a uniform explicit cyclic
realization for every `e>=7`. Any closure must use the center-spread,
field-valued, Hankel, or Forney structure rather than block degrees alone.
