# Audit

The primary verifier constructs the primal optimizer and solves a full exact
dual system.  The independent audit reconstructs the same certificate using
backward hierarchy recurrences.  Both replay all 494 new rows and check the
opposite signs at `18101/18102`.
