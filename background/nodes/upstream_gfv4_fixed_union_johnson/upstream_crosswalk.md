# Upstream crosswalk

This node is `IDENTICAL` to the three named theorem statements in upstream
`experimental/grande_finale.tex` at `b13de811`. The local proof is an
independent reconstruction.

The one-fixed-union and transversality scopes are part of the identification.
No all-chart compiler is claimed.
