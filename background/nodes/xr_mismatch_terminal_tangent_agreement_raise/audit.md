# Audit

## Verdict

The extra agreement coordinate is outside the lifted pair's common agreement
set by definition of discrepancy support. Therefore the proof gains one
distinct coordinate, rather than recounting an existing agreement.

## Scope checks

- The codeword in `(TAR2)` is global because the accumulated-locator identity
  has zero terminal residual.
- All terminal pairs use the original received pair and original domain, so
  their slopes land in one common set `B_(A+1)(u,v)`.
- The union is not multiplied by the number of explanation pairs or paths.
- A witness contained entirely in the common agreement support is not a
  retained support-wise mismatch and needs no extra charge.

## Limitations

The carried set may enter a different quotient or structured profile at
agreement `A+1`. A closing induction must re-run the complete first-match
partition at the raised threshold and sum every paid and generic currency.
The present theorem alone supplies no numerical bound.
