# Frontier

The next orbit-distinct parallel record is the negative `DE` record
`xi=2`.  At pairing zero, deleting it leaves the two positive `de`
records paired together, so the target-free specialization differs from the
proved opposite-`DE` cut and must be derived in the four-basis algebra.

After the three parallel-`DE` missing roles are understood at matching zero,
test the exact matching transports used in cell 3 before launching unrelated
norm jobs.
