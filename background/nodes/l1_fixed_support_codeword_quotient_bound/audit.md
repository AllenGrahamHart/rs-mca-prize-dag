# Audit - L1 fixed-support codeword quotient bound

## Checked axes

1. The map uses reconstructed codewords, not cross-determinant quotients.
2. The degree bound is `k-1=N`, not `k`.
3. The whole exact petal support divides every codeword difference.
4. The aggregate does not add a defect-degree, numerator, syndrome, or
   background multiplier.
5. No split-polynomial claim is made for the quotient.
6. The result is per chart and retains non-intrinsic first-match supply.

## Remaining attack

The bounded-polarity sub-Johnson tail now has `N-h=a-s` unbounded, in
addition to unbounded root excess and retained-core size. It needs a
growing-codimension owner or a higher-order source-coupled incidence theorem.
