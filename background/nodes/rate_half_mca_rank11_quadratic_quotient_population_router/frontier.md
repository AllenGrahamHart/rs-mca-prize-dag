# Frontier

Prove that fewer than 520 first-owned pair types can carry compatible cyclic
or dihedral factor-through certificates in one heavy-ruling orientation, or
charge such types by a disjoint factor/denominator/chronology resource.

If fewer occur, `(QP2)` leaves an explicit synchronized nonquotient mass for
the shifted or nonquadratic split-pencil routes.
