# Provenance

- Residual mass and first-owner partition:
  `rate_half_mca_rank11_nonzero_affine_reflection_mass_router`.
- Quotient classes and factor-through maps:
  `rate_half_mca_rank11_quadratic_quotient_survivor_identification` and
  `rate_half_mca_rank11_quadratic_quotient_factor_through_interface`.
- The per-type disjoint-exception cap and aggregate tradeoff are proved here.
