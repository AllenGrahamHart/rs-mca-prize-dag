# Lineage

This is the `BC-` counterpart of the existing cells-3/6 `BC+` complete
outside aggregate.  It merges only already closed packets and exact
transport; no new elimination is hidden in this node.
