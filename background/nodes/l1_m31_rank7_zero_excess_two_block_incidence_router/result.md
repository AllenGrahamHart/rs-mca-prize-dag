# Result - M31 rank-seven two-block incidence router

The zero-excess proper-`G` terminal is reduced to a collective middle-band
locator problem. At most `40` members have `q<=4980`, at most `7` have
`q>=67448`, and a violating class contains at least `2,157,882` distinct
locators with `4981<=q<=67447`. Its mean `q` also obeys the exact quadratic
constraint (TB3), in particular `23945<Q/M<24860` at forbidden mass.

No row or critical node changes status.
