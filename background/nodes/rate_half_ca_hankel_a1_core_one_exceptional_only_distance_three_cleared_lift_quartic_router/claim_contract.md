# Claim contract

- **Claim:** the calibrated kernel lifts admit a common polynomial clearing
  of `X`-degree at most `4e+6`; every external fiber contains its exact
  degree-`4e+2` nonincidence locator with a residual cofactor of degree at
  most four. After known scalar normalization, all these cofactors are fibers
  of one biform of bidegree at most `(e-2,4)`.
- **Scope:** the exact pair-Lagrange distance-three external design.
- **Dependencies:** pair-Lagrange normal form, external design saturation,
  and the calibrated conic kernel-lift normal form.
- **Consumer:** `rate_half_band_closure`; narrowed theorem interface for the
  live saturated generic branch and `CR-003-CLIFT`.
- **Falsifier:** a valid design violating `(CLQ4)--(CLQ13)`, or the exact
  `F_17,e=1` fixture producing a quotient of degree other than four.
- **Nonclaims:** the residual quartics need not split or be fixed, and their
  quartic-valued parameter curve need not be a projective line.
