# Result

`PROVED`. The `R0` primary/torsion/constant packet is two degree-`2^36`
Jacobi gcds and reuses the exact CR-002 torsion norms already requested for
the matched packet. No additional large norm computation is needed.

