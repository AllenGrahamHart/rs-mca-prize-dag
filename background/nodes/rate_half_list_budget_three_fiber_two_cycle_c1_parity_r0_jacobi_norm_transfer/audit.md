# Audit

1. The primary index is `L=2M`; the constant Legendre index is
   `2L-1=4M-1`.
2. The torsion sign is `epsilon=r^(8L)=r^(16M)`, giving
   `tH^2=epsilon P^2` exactly.
3. The constant equation contains `+x^2-1`; reversing that sign breaks the
   lift-free gate.
4. There is one scalar polynomial per torsion sign, not the six signed
   equations from the matched source tuple.
5. The factor `2x` in the transformed `U` equation is removable only because
   the primary Jacobi polynomial has no zero at `x=0`.
6. Shared torsion norms do not mean shared scalar packets. `F_epsilon` must
   still be checked for `R0`.
7. No separate contributor norm run is authorized for this node.

