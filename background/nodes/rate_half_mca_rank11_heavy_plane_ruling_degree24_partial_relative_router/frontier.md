# Frontier

The packet construction and local extraction are closed. The remaining
work is branch payment:

- exclude or absorb the pure-locator output using the heavy ruling and
  first-owner structure;
- control denominator roots and pay the nontrivial rational profile;
- convert the `chi>=2299571` alternative into the required spread,
  alignment, or exception budget;
- synchronize these local outputs across the heavy orientation if a
  whole-line owner is required.

The stronger `24..31` degree pin and the retained anchor/ruling labels are
the extra data available beyond the generic degree-18 router.
