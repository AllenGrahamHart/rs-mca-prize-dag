# Attack

The exact next row is `K'=13`, where `dim(V'^perp)=3`.  The quotient-label
space is a projective plane rather than a line.

The next route should retain the successful split:

1. compute the absolute rank-deficient capacity at `K'=13`;
2. extend rank-nine common-core offsets to `j in {9,10,11,12}`;
3. charge circuits of size at least six by their rank-nine shadows; and
4. replace the line root count by a plane-curve or incidence bound for
   support-at-most-five quotient labels.

Do not assume that a plane contained in a secant determinantal locus has
fixed support.  The K'=12 proof deliberately avoided that false shortcut.
