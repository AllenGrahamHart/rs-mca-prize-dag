# Lineage

This is the finite exceptional-fiber child of the `BC-` generic-rank atlas;
it performs only common-tower lifting and deliberately leaves outside replay
to separate record-specific leaves.
