# Cell-3 `BC-` uncolored guard-lift atlas

- **status:** PROVED
- **field:** `F_2130706433`
- **scope:** exceptional base values from the uncolored generic-rank atlas

The 47 generic-rank guards have 73 deployed-field root incidences and 48
distinct roots `q`. Exact lifting through

```text
y^2=N(q)/D(q)
```

and the four sign-dependent `r` quadratics gives this exhaustive census:

```text
19 q-values: no base-field y,
 3 q-values: projection denominator boundary,
 3 q-values: no guarded common point,
23 q-values: lifted.
```

Each lifted `q` has two guarded `y` values and, in each of four root-sign
rows, two guarded `r` values. Thus exactly `23*2*4*2=368` guarded common
points remain for outside-record replay. No outside equation is paid here.
