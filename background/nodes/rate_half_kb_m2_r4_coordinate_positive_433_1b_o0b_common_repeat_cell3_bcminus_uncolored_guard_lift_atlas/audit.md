# Audit

The verifier pins four artifact hashes, validates all guard polynomial
digests and field-root rows, checks the 48-value union and 73 incidences, and
recounts all nested sign lifts and 368 guarded points. Hostile mutations
alter a root, status count, point status, source hash, and total.
