# Frontier

For each formal outside case, intersect its own guard hashes with the 23
lifted base values, append the missing-product quartic, and compute the three
residual pairing gcds. Shard results by missing record.
