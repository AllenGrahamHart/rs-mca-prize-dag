# Statement

Over `F_p`, `p=2130706433`, the guarded cell-11 positive `433-1b` route is
empty for the positive parallel-`DE` missing role `xi=0` at residual
matching `9`. The exact generic label quotient transports this to

```text
{(0,9),(0,10),(1,9),(1,10)}.
```

This is one generic orbit. No other matching is decided.
