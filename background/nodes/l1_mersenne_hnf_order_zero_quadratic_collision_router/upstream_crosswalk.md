# Upstream crosswalk

This is an exact finite-track HNF passport router. In split-pencil language,
it converts multiple quadratic color collisions into an antipodal symmetry
class and removes that class on the four `m=8` rows. It has no current exact
upstream label and proves no split-pencil census, asymptotic Q-flatness,
endpoint threshold, or prize close. It can accompany a later HNF passport
packet after the remaining quadratic chambers are classified.
