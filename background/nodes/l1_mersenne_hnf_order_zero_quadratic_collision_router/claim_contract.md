# Claim contract

| field | value |
|---|---|
| claim | A quadratic order-zero color interpolant with two repeated colors is even; on `m=8,h=7`, no quadratic interpolant has two repeated colors. |
| status | PROVED |
| dependency | `l1_mersenne_next_to_maximal_hypergeometric_normal_form` |
| exact rows | Four `m=8,h=7` rows and one `m=16,h=15` row with `n=m(p+1)`. |
| consumer | `l1_mixed_petal_amplification` |
| excluded strata | Nonzero-center quadratic multi-collisions on all five rows; every quadratic multi-collision on the four `m=8` rows. |
| open residue | Quadratic collision-free/single-collision systems; the even quadratic multi-collision `m=16` system; all higher degrees and inner tests. |
| nonclaim | No complete degree stratum, endpoint row, HNF branch, or critical node is closed. |
