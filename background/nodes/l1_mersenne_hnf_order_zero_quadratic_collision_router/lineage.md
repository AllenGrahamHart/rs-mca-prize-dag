# Lineage

The low-degree exclusion starts the colored HNF classification at degree
two. This node exploits the involution intrinsic to a quadratic map. Two
full fibers synchronize that involution with Frobenius; the resulting affine
root transport is controlled by the hypergeometric weighted-derivative
identity. The residual zero-center branch becomes an even/odd subresultant
calculation.
