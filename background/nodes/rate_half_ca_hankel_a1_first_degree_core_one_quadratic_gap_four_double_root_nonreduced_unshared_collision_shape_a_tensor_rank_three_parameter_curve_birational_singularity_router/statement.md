# `A=1` shape-A tensor-rank-three birational singularity router

- **status:** PROVED
- **closure:** rank three forces a degree-`m` birational parameter curve
  with six quantified singular vertices
- **consumer:** `rate_half_band_crossing_location`

Retain an official Shape-A all-excess survivor and suppose that its biform
has tensor separation rank exactly three. In a minimal representation

```text
G(t,X)=A_0(t)B_0(X)+A_1(t)B_1(X)+A_2(t)B_2(X),    (TBR1)
```

the parameter coefficient triple defines a basepoint-free morphism

```text
a:P^1_t -> C_a subset P^2.                          (TBR2)
```

If `d` is its degree onto the image and `c=deg C_a`, then

```text
m=dc.                                                (TBR3)
```

The all-rank parameter-map birationality theorem uses the exact excess and
padding ledgers to prove that at most one slope in `Gamma` has no actual
`U_0` incidence and that every other slope lies in a complete reduced
degree-`d` normalization fiber. Consequently

```text
d divides 3e-z for some z in {0,1}.                  (TBR4)
```

Since `d` also divides `m=e-2`, the official arithmetic gives `d=1`. This
is now available independently of the rank-three frame.
Thus

```text
c=m=183251937961,                                   (TBR5)
```

and `a` is the normalization map of a rational plane curve of degree
`m`.

For the four-row projective frame, let `p_ij` be the six distinct
intersection points of its four coefficient lines. The pair overlap
`|A_i intersect A_j|` is exactly the number `r_ij` of geometric branches
of `C_a` at `p_ij`. Therefore

```text
sum_(i<j) r_ij >= e-8=m-6,                          (TBR6)
max_(i<j) r_ij >= 30541989660.                      (TBR7)
```

In particular, one vertex has local delta invariant at least

```text
466406566180502462970,                              (TBR8)
```

and the six distinct vertices together contribute at least

```text
2798439396930304829525                              (TBR9)
```

to the delta invariant.

## Scope

This is an exact birational/singularity router, not a rank-three exclusion.
A closing argument must show that the official source, collision, or
scalar-weld structure cannot realize this complete-quadrilateral
singularity ledger.
