# Claim contract

| field | content |
|---|---|
| claim | The ten `S1` deployed product ideals are identical in all four common root-sign rows under the canonical cubic component maps, so all forty cells are empty. |
| assumptions | The exact sextic component quotients, mate reconstruction, outside sign-cell count, and representative-row `S1` exclusions. |
| exact interface | Delete every `S1` product cell; reduce the accepted invariant-cell frontier from 70 to 40. |
| nonclaim | Any `S0` or `S2` deletion, outside-`q` transport, interpolation, coordinate or row closure, or either Prize theorem. |
