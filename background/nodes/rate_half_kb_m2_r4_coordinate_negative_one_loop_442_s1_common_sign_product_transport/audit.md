# Audit

1. The component maps are rebuilt from the exact common generators for all
   four sign rows; no numerical root matching is used.
2. Both irreducible cubic components are checked in every row.
3. The exact `c,m` coefficient triples are compared, not only norms or
   polynomial fingerprints.
4. Nonsquareness of `-m` is replayed in both components.
5. Rational and quadratic-extension cells are distinguished, including the
   four representative forced-EF guard-saturated deletions.
6. No source-root, `q`, or interpolation transport is asserted.
