# Audit

## Forward audit

1. The first norm inequality is the proved quarter-width mechanism retained
   below its emptiness threshold.
2. The Haar identity is exact over the integers and separates odd-frequency
   energy from the next collapsed defect.
3. A zero cyclotomic transform is equivalent to half-periodicity because the
   dyadic cyclotomic polynomial is `X^(m_v/2)+1`.
4. A nonzero transform sees distinct split prime ideals for every allowed odd
   exponent and therefore has the stated norm floor.
5. The exact last-stage condition compares the largest inherited defect
   ceiling directly with the smallest norm floor, so it controls every
   earlier stage by monotonicity.
6. Vanishing through level `T` repeats `f_1` in `2^T` blocks; integrality then
   contradicts its strict norm-derived size bound unless it is zero.
7. The constant `64` makes the exact upper side smaller than `m/2` and the
   exact lower side greater than `m/2`, so it is a genuine corollary rather
   than a separate numerical claim.

## Consumer-backward audit

The conclusion is only a router. At even widths it supplies a zero-cost
deletion through the proved swap theorem. At odd widths it removes the free
class but leaves the exact swap orbit count untouched. The deeper
lower-quarter widths remain in both classes.

## Scope checks

- `p>=n^2` and `m|n` imply the used strict inequality `p>m^2`.
- The band condition implies every iterated order `m_v` used in the proof is
  at least four and every indexed moment remains below `h`.
- Natural logarithms are used consistently.
- No finite computation is promoted to a uniform theorem.

## Independent replay

The main verifier checks the exact inequalities at every admitted `(m,d)`
through `m=2^41`, the Haar identities, the first nonempty level, and DAG
wiring. The audit verifier independently checks the sharp repetition logic
and mutations that omit the odd-parity `d+1` or the strict prime inequality.
