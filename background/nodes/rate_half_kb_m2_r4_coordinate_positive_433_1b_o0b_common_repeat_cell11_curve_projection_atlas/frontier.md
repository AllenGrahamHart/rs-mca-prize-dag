# Frontier

The projection is not an outside exclusion.  The next dependency is to expose
the source function field and split the product-rank-five open from a finite
selected-cofactor boundary before running residual resultants.
