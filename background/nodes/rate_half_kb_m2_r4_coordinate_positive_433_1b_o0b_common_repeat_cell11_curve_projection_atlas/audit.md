# Audit

The primary verifier pins all six artifacts, rechecks the full eight-row
census, dimensions, basis sizes, exact elimination equations, and compact
symmetric forms.  The hostile verifier deletes a row and mutates dimensions,
equations, source hashes, and symmetric reductions.

The product-rank cofactor is not inverted in the projection calculation.
