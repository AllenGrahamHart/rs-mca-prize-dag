# Proof

Anchor `gamma_0`. The selected pair space generated by

```text
(gamma-gamma_0,h_gamma-h_gamma0)
```

has dimension `a`: if `delta r_1-c=0`, same-support pair noncontainment
forces `delta=c=0`. Its slope projection is nonzero, so it contains a pair
`(1,b)`. The displayed gauge leaves every error word unchanged:

```text
r_0+gamma(r_1-b)-(h_gamma-gamma b)
 = r_0+gamma r_1-h_gamma.
```

Thus every exact agreement support and containment predicate is preserved.
The transformed explanation differences are precisely the kernel of the
slope projection, of dimension `a-1`. Adding `b` back is the literal
inverse, so maximum-over-received-lines slope counts are unchanged.

Apply the support-local transversality compiler with explanation rank
`s=a-1`. The proved near-rational theorem contributes a disjoint `2w`.
At ranks `s=8,9,10,11`, exact evaluation gives the least paying margins
`1,13,388,12050`. Therefore failure to pay at error ranks `a=10,11,12`
emits a selected support with at most one less than the respective margin.
The `a<=9` endpoint is the `s=8,theta=1` cap plus `2w`. All constants are
replayed with exact rational arithmetic.
