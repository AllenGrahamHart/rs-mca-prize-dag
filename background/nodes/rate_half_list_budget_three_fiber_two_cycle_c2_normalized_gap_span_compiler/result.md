# Result

`PROVED`. The generic `c=2` mismatch lane is now one exact normalized
gap/span recurrence interface with a final scalar invariant equation. No
outer coefficients or labelled pair choices remain free; exclusion and a
compressed official-order algorithm remain open.

Bounded falsification rules out a primary-only rigidity shortcut. The
combined primary-plus-secondary rigidity route remains viable but unproved;
the pure fourth-root endpoint it would produce is now excluded separately at
the official row.
