# Claim contract

- **claim:** the full generic `c=2` primary-gap, secondary-gap,
  canonical-span, and completion-coupling packet is one exact recurrence
  interface in normalized variables `(t,S,P)`, with no free outer coefficient
  or six-pair enumeration.
- **scope:** the maximal official generic `c=2` denominator-mismatch chamber.
- **dependencies:** maximal-row characteristic control, the doubled-order
  boundary transfer, and exact selected-pair normalization.
- **falsifier:** an incorrect fourth-root recurrence, a gap/span survivor lost
  or added by the interface, a free choice hidden in `alpha,beta,gamma`, or a
  selected-pair coupling not equivalent to `(C2G10)`.
- **nonclaims:** the interface is not proved empty and no official-order
  algorithm, cost bound, or compute authorization is supplied.
