# Proof

After deleting `DF`, matching `3` pairs the residual products as

```text
(de,-de), (de,sigma_o ef), (bf,sigma_c cf).
```

Write `m=df`, let `S=(d+f)^2`, put `z=1/d`, `y=z^2`, and set `q=de`.
Then

```text
f=mz,  e=qz,
M(y)=1+(2m-S)y+m^2y^2=0.                         (RS12-6)
```

The colored source pair is the quartic

```text
C(z)=paired(bmz,sigma_c cmz).
```

Split it uniquely as `C(z)=E(y)+zO(y)` and form the sign-free quartic
`K(y)=E(y)^2-yO(y)^2=C(z)C(-z)`. Division of `K` by the quadratic `M` in
the exact four-basis source algebra has linear remainder
`R(y)=r_0+r_1y`. If `M(y)=p_0+p_1y+p_2y^2`, every common root satisfies

```text
r_1^2p_0-r_1r_0p_1+p_2r_0^2=0.                  (RS12-7)
```

The previously audited pairing-3 compiler norms this cut through the exact
basis `1,t,b,bt`. A pinned AST adapter supplies the cell-12 tower and compact
kernel without modifying the compiler function. Eight rows cover four
source signs and both `sigma_c` lanes.

The exact candidate union contains every deployed root of all norm and
inverse numerators and denominators. Its 120 case-labeled roots replay to
176 guarded source points. The boundary ledger has 40 `r`-guard exits, 32
`t`-guard exits, and eight cell-12 `b`-leading exits. The last are in the
leading complement paid by the rational-boundary parent.

Direct scalar replay intersects the complete quartics `M(z^2)` and `C(z)`,
leaving 40 common nonzero `z` lifts. For each lift and `sigma_o`, the two
remaining pair equations are

```text
paired(q,-q)=0,
paired(q,sigma_o q m z^2)=0.                     (RS12-8)
```

An independent audit reconstructs the source kernel at every lift and
computes the gcd of the two polynomials in `(RS12-8)`. All 80 gcds are
constant, so no `q` and hence no target representative exists. There is no
witness or unresolved branch.

Exchanging the two identical positive `DE` records maps matching `3` to
matching `6`. Outside `D/E` transport maps missing `DF` to missing `EF`.
The exact generic orbit quotient proves both transports and closes all four
labels. QED.
