# Frontier

The unit-associate multiplicity problem for the maximum-weight profile
`(3,6,S=18)` is complete:

```text
T_36(p,r)<=4
```

at every fixed prize row and primitive quotient root. No exponent-lattice or
sparse-product enumeration is needed for this profile.

The next E1 step is aggregate rather than another recount of `(3,6)`: import
this four-orbit charge into the exact weighted-kernel ledger, identify the
largest lower profile still capable of exhausting the residual edge budget,
and apply the same ideal/height mechanism only where its hypotheses survive.
Different roots and lower profiles must retain their exact orientation,
stabilizer, and class-pair multiplicities.

