# E1 cofactor-2 Smyth height collapse

- **status:** PROVED
- **closure:** sharp entropy bound plus Smyth's totally real measure spectrum
- **scope:** profile `(3,6,S=18)`, one prize row and primitive quotient root

At one fixed prize row and primitive quotient root, the cofactor-`2` branch of
the profile `(3,6,S=18)` contains at most one negacyclic shift/sign orbit.

For two collisions in that branch, the associate ratio has

```text
||lambda(u)||_1 < 63.878.
```

Every non-torsion unit of `Q(zeta_256)` has a totally real representative
whose normalized Mahler measure is greater than `1.29`; hence its same log
norm is greater than `256 log(1.29)>65.188`. Therefore the associate ratio is
torsion.

Together with `e1_high_cofactor_schinzel_height_collapse`, all four live
cofactors `2,4,8,16` contribute at most four shift/sign orbits to this
maximum-weight profile.

This does not assert that any of the four orbits exists, and it does not pay
the lower-weight profiles in the official E1 pair budget.

