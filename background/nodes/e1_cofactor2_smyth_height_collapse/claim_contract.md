# Claim contract

## Consumed content

- The common-prime associate router fixes one row, primitive root, and
  cofactor before supplying `beta=u alpha` and the product identity.
- The high-cofactor height node supplies the normalized-square entropy setup
  and the exact relation `||lambda(u)||_1=256h(v)` for the real unit
  representative.
- The full-unit theorem supplies `v in Q(zeta_256)^+` modulo the `256` roots
  of unity.
- Smyth's 1981 theorem supplies the ordering of the first two normalized
  Mahler measures for nontrivial totally real algebraic integers.

## New proved content

1. The entropy implication `D<7.539 => L1(log z)<31.939`.
2. The least Smyth value cannot occur in the conductor-`256` real field.
3. The second Smyth value is greater than `1.29` by exact rational polynomial
   signs.
4. Every same-cofactor associate ratio for `m=2` is torsion.
5. Combined with the high-cofactor node, the profile `(3,6,S=18)` contributes
   at most four shift/sign orbits across all live cofactors.

## Guards

- The theorem is pairwise inside one row, quotient root, and cofactor.
- It bounds associate orbits; it proves neither existence nor emptiness.
- It does not merge different quotient roots or cofactors.
- It does not pay any lower-weight profile or close the official E1 budget.

