# Frontier

The correct outside representation is the four-basis tower

```text
F_p(r) -> F_p(r)[t]/(F) -> [b]/(G),  c=-H_0/H_c.
```

The first route test should reduce the `xi=0`, pairing-`0` opposite-`DE`
target-free identity after every multiplication, take the quadratic-over-
quadratic norm, and classify its univariate `r` roots together with all norm
and recovery boundaries.  Only after one case returns a complete low-degree
ledger should the four source signs or further matchings be launched.

Do not retry the ambient denominator-cleared standard basis without a new
algebraic reduction: the one-case prototype exhausted its 300-second cap
before a mathematical result.
