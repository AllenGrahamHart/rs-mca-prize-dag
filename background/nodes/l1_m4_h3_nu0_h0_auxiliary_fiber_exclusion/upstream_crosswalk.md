# Upstream crosswalk - auxiliary-fiber endpoint exclusion

This is an exact `(A)` / split-pencil endpoint closure. A local
root-multiplicity ledger proves that the complement consists precisely of two
auxiliary fibers, converting their root products into a subgroup divisibility
test. The sole exceptional outer packet fails that test.

An upstream exact SPI packet can delete the entire nonzero-`b`
constant-eliminant cell on all four characteristics. The former donated-
compute request is retired and should not be copied into a PR as live work.
