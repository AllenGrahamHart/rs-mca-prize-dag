# Dependency sub-DAG

```text
weighted selected-support split-pencil theorem (zero offset)
  -> balanced cross-owner pair injection
  -> clean dominant heavy/light payment
  -> heavy-owner pair collision census

uniform common-core offset rP
  -> balanced line count from X_L>=floor(P^2/4)
  -> at most rP extra charge per selected line
  -> clean-line count charged by disjoint light mass
  -> weighted split-pencil core-offset cap                    [PROVED]

rank-nine chart with j=9 or 10
  -> P=m'-j and r=j-9
  -> K'=11 rank-nine shadow capacity                          [NEXT]
```
