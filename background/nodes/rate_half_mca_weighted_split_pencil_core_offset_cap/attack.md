# Attack

Use this theorem only after preserving the selected support on each record
line.  For a rank-nine chart with common core `J`, set

```text
P=m'-|J|,       r=|J|-9,       S=n'-|J|.
```

At `K'=11`, the kernel root bound leaves only `r=0,1`.  Compute both caps and
take their maximum before summing over rank-nine shadows.  Do not split the
incidence lower bound between the two core sizes.
