# Result

The arbitrary-`W` four/five support packet is now compressed to:

```text
one plane L subset (W|X)^perp,
one projective map [F:G] on X,
four/five certified Segre coefficient directions,
their disjoint zero fibers (multiplicity at most two at five blocks).
```

Repeated five-block directions are already persistent-basis/proper-flat
owners. What remains is an aggregate count and cross-core first-match rule for
these dual-plane fiber packets.
