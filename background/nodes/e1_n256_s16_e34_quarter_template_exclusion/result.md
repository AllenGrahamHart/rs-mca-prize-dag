# Result

The quarter heavy-position template is excluded completely at `V=68`. It is
geometrically nonempty, but its exact maximum `M_3=1188` is 759 below the
cubic norm threshold.

The E34 frontier now consists only of the nonquarter diameter, progression,
and generic heavy templates. The universal E1 and adjacent-unsafe targets
remain open.
