# Audit - L1 joint core/background Johnson bound

## Checked axes

1. The cross determinant is nonzero for distinct primitive monic pairs.
2. Petal-support, common-defect, and common-background locators are pairwise
   coprime and all divide the same cross determinant.
3. Background agreement sets are canonically truncated to the common size
   `u` before incidence counting.
4. The two Cauchy-Schwarz terms use their actual universe sizes `N` and `b`.
5. Strict joint-denominator positivity is retained.
6. The identity `d+u-r=ell` is used in the numerator.
7. The `F_17` fixture attains `(CJ4)` exactly.
8. The `F_23` fixture is exact, saturated, and populated at `J=0`.
9. The aggregate does not enumerate background subsets or add a field-size
   factor.
10. Non-intrinsic chart multiplicity remains open.

## Remaining attack

For `b>0`, the bounded-polarity per-chart residue satisfies `(CJ7)`, the
background-overlap upper bound, and the existing unbounded-parameter pins.
The next useful theorem must exploit structure beyond an uncoupled second
moment or provide a natural-scale owner for this joint sub-Johnson region.
