# Audit - L1 root-free rational-Q projective packing

## Checked axes

1. The projective space includes the affine offset `G`.
2. Nonemptiness is used to prove `G` is independent of `W_1 F[X]_<d`.
3. `W_1` witnesses gcd-triviality on the punctured domain.
4. The infinity hyperplane is excluded using both root-freeness and degree.
5. The full projective intersection, not only an affine injection, is counted.
6. Residual projective dimension is `d=k-r`.
7. Locator degree is `j=m-r`, so codimension is exactly `w`.
8. Common reduced agreements are roots of `R-R'`.
9. The packing subsets have size `d`, not `w`.
10. The exact integer floor is retained.
11. Fixed `d` is polynomial and `d=o(n)` is subexponential at linear density.
12. Reserve absorption uses `d=o(R log |B|)`, not subexponentiality alone.
13. Linear `d` is not declared paid.
14. No average or finite reserve normalization is inferred.

## Route effect

The list-side rational boundary object is now an exact instance of upstream's
projective Conjecture-F language, with its high-anchor range paid by a closed
packing formula.
