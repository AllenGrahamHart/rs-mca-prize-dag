# Upstream crosswalk - root-free rational-Q projective packing

## Exact dictionary

The residual space `V` is gcd-trivial on `H'` and has projective dimension
`d=k-r` inside degree-`j=m-r` locators.  Its split-locator intersection is
exactly the L1 boundary cell because the infinity hyperplane is empty.  This
places the object directly in Paper D v13.2's Conjecture-F framework, rather
than assuming it is an identity-prefix fiber.

## Paid range

The formula

```text
floor(binom(n-r,k-r)/binom(m-r,k-r))
```

extends the dimension-one voting payment to this special codeword section and
pays every fixed residual dimension.  At linear locator density it is
`exp(O(k-r))`, so it also pays `k-r=o(n)` for the asymptotic Q loss, subject
to the active reserve rule `k-r=o(R log |B|)`.  It is a theorem-level
contribution to the declared planted/fixed-dimensional priority classes.

## Remaining input

The active Q lane must control linear-residual-dimensional gcd-trivial
projective cells at the base-field image scale, or prove that its compiler
transports them to identity-prefix/quotient-pullback atoms with no
unaffordable owner multiplicity.
