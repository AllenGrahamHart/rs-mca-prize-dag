# Result

Every squarefree core-free cubic packet at `u=1` has exactly one corrected
root row. The correction has one of three explicit vertical/contact forms,
and the complete contact divisor has one extra point beyond the reduced
root-row incidences. The resulting Picard class has degree `e+1`, so this is
a structural classification rather than an exclusion.
