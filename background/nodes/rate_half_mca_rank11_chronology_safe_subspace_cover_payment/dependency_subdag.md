# Dependency sub-DAG

```text
rank-eleven full-span residual forcing ------------------+
ordinary affine-span / sub-square interleaving ----------+--> chronology-safe
anchored first-match row-space partition ----------------+    subspace-cover payment
                                                             |
                                                             +--> rate-half crossing
```
