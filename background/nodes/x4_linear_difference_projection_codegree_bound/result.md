# Result

The first nonconstant X4/SP branch now has certified bounded projection
multiplicity.  At official base rows no added locator and no removed locator
can participate in more than `1024` linear-difference records.  Closing the
`d=1` branch is therefore reduced to bounding one distinct-locator projection;
the total projection population remains open.
