# Repeated-BC cell-3 BC- colored missing exclusion

- **status:** PROVED
- **scope:** all missing-`BE` and missing-`CF` cell-3 `BC-` systems

The exact norm atlas has the following deployed-field root census:

```text
rows                                              48
root incidences                                  136
distinct q values                                  8
projection-denominator boundaries                  3
q values with no base-field y                      2
q values with only Mobius/target-guard y lifts     3
guarded common points                              0
```

Every zero of a cut norm numerator, every norm pole, and every registered
construction boundary is in this union.  No union element lifts to a
guarded common point.  Therefore neither missing-colored necessary cut can
vanish on the guarded locus.  This excludes

```text
missing BE: 4 root signs * 2 outside signs * 15 matchings = 120,
missing CF: 4 root signs * 2 outside signs * 15 matchings = 120.
```

## Falsifier

A missing base-field root, an unlisted tower lift, or any guarded common
point above the eight-value exceptional union.
