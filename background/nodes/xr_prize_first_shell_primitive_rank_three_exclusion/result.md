# Result

The primitive first residual shell `|X|=a+3` is empty at every prize row and
every affine kernel rank. Rank two is excluded by the large shell band; rank
three has a simple edge-zero normal form whose Maxwell deficit is positive.

Only proper local first-shell circuits remain. Their ownership and aggregate
count, rather than a primitive first-shell anomaly, are the next obligation.
