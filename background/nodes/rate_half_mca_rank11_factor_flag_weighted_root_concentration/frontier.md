# Frontier

Cancel or price the forced coordinate inside its dimension-at-most-eight
bucket while retaining first-match ownership. A successful bucket-local
shortening bound below `303637675671716` can be iterated on the remaining
flag mass; a generic dimension-eight affine-list cap is too large and is not
the intended endpoint.
