# Rank-eleven factor-flag weighted root concentration

- **status:** PROVED
- **row:** KoalaBear MCA, post-near affine error rank eleven
- **scope:** the base-free `2 x 5` factor-presentation branch

Assume

```text
C'=span(PB),  dim P=2,  dim B=5,
```

and every promoted rich container is contained in `g_iB` for some nonzero
`g_i in P`. Assume also that `B` has no common zero in the anchor-good
universe; otherwise the existing global-core adapter applies.

At factor cutoff `T=650`, orient every residual container as in the proved
factor-flag router. Every residual-oriented `B_i` has at least `37736`
common zero columns. Charge the classes whose annihilators are
`11216`-transverse. Then every unsafe line leaves at least

```text
E_flag=30210771209598495                                  (RC1)
```

first-owned slopes in nontransverse residual classes. There are at least

```text
ceil(E_flag/R_6)=1877                                    (RC2)
```

distinct such residual classes, and each emits a proper residual extension
with at least `S=11217` labelled common-zero coordinates.

Weight each emitted `11217`-coordinate set by the number of slopes in its
residual class. Some anchor-good coordinate `x` is then common to residual
classes carrying at least

```text
ceil(S E_flag/1116048)=303637675671716                   (RC3)
```

first-owned slopes. All corrections in those classes lie in

```text
P B_x,  B_x={b in B:b(x)=0},  dim(P B_x)<=8,             (RC4)
```

and every corresponding pair agrees with the anchor and received pair at
`x`.

## Nonclaim

The dimension-at-most-eight bucket in `(RC4)` is not paid here. The theorem
does not prove the `2 x 5` presentation for an arbitrary unsafe survivor,
synchronize the remaining residual flags, or turn this bucket-local core
into a line-global core.
