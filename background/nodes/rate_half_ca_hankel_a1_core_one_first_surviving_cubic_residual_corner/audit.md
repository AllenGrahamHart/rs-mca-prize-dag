# Audit

- The first surviving degree is the floor `floor(16m/13)`, and the official
  residue is `16m-13e=6`.
- The unique slack follows by matching a proved lower bound to the ledger's
  upper bound; it is not a numerical search over `ell`.
- `p>=Delta-3` uses the exact residue `Delta mod 5=3`.
- The three-unit statement applies to the complete chain
  `p<=O<=sum c<=Delta`; it does not assert equality at any link.
- `P_p` is a pushforward divisor with multiplicity. The residual factor can
  be reducible, repeated, or supported at infinity.
- The `2e+3` clean fibres are a lower bound. Additional clean fibres are
  allowed when the total rank loss is below `Delta`.
