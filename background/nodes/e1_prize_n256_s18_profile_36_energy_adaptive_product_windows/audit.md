# Audit

- The certificate derives every low-energy `(q,L)` row from integer magnitude
  partitions; the table is not trusted as input.
- All square roots use outward rational intervals with denominator `2^192`.
- Every excluded chamber uses upper endpoints; every claimed relaxation
  boundary uses a lower endpoint.
- Final certificate Modal app: `ap-Jj1DnCEMdtU9oUi9fn6Lfb`.
- A 64-shard congruence-preserving search, app
  `ap-vCwCehrnyit7WrDEjorD0c`, falsified both modular emptiness and the trial
  lower bound `E>=17`: it found one exact `E=15` and four exact `E=17`
  multiplicity-one vectors vanishing at a primitive root modulo 257.
- FLINT and PARI/GP agree on all five witness norms in app
  `ap-dle2qIanefNmtFx0fwT8nL`; every quotient `Norm/514` is below the prize
  interval. These witnesses lie in the analytically excluded `V=30,34`
  chambers and are not load-bearing.
- Exact normalized multiplicity-one chord counts were checked in app
  `ap-xT7QanPDtsEoOnuFDhhWHh`.
