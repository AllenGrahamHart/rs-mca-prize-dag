# Claim contract

## Imported facts

- The 64 conjugate squares have mean 18 and variance `V=2E`.
- The capped two-moment product extremum has cap points and at most two
  interior levels.
- Cofactor 514 forces multiplicity one.
- The complete multiplicity-one geometry is empty for `E=2,...,6`.

## New theorem

Integer energy gives the adaptive cap `y_u<=min(144,18+V)`. Exact product
comparison contracts `m=256` to `V<=46` and `m=514` to `V<=22`.
Parity-weight refinement leaves exactly the nine printed `(E,q)` chambers for
`m=514`.

## Exclusions

1. No residual `(E,q)` chamber is declared empty merely because the product
   relaxation survives there.
2. Cofactor 514 is not closed; factor 257 must still be spent in the nine live
   chambers.
3. The five adversarial witnesses are not completeness evidence. They only
   falsify stronger geometric premises and audit the analytic exclusions.
4. Cofactors other than 256 and 514 receive no improved endpoint here.
