# Statement

## Claim `(KBP1B3-DE-P9-1)`

In the coordinate-positive `433-1b` cell-3 outside systems, the subfamily

```text
xi in {0,1,2},  pairing = 9 = ((0,4),(1,2),(3,5))
```

is empty for all four source-sign pairs and all four target-sign lanes. This
is exactly

```text
3 parallel DE missing copies * 1 matching
* 4 source signs * 4 target lanes = 48 raw cases.
```

## Proof envelope

For `xi in {0,2}`, eight exact source rows each cover all four target lanes,
paying 32 nonidentical raw cases. A degree-eight nested eliminant in `u=df`
and `f` is normed both directly in the six-basis source algebra and through
the quadratic-over-cubic tower. Every norm and intermediate exceptional
root is lifted. The finite census has zero witnesses and zero unresolved
branches. The `xi=1` rows transport from `xi=0` because the two positive
parallel `DE` deletions have identical residual systems.

The claim does not cover another matching, `xi=3,...,6`, complete cell 3,
complete `433-1b`, K3, the rate-half band closure, or either prize.
