# Result

The last generic coefficient chart now has two further linear equations in
`d`, obtained from the conic and the selected role packet modulo `P_4`.
Only their simultaneous coefficient-zero locus still retains `d`; every
other chart is a parameter-only system in `(x,Y,q)`. No packet is excluded.
