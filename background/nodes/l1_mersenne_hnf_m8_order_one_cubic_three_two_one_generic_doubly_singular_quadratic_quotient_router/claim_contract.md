# Claim contract

| field | value |
|---|---|
| claim | On the doubly singular generic coefficient locus, the conic and each official role quadratic reduce modulo `P_4` to two exact linear equations in `d`; only their simultaneous coefficient-zero locus retains `d`. |
| status | PROVED |
| dependency | `l1_mersenne_hnf_m8_order_one_cubic_three_two_one_generic_double_linear_d_router` |
| consumer | `l1_mixed_petal_amplification` |
| equivalence guards | Inherited `q*d*(q-d)*D*K_6*R*S!=0`; generic `Delta*W!=0`; the selected nonzero quotient coefficient on each rational chart. |
| open residue | Unit or retained-point verdict for both rational quotient charts and the simultaneous `N_1=U_1=N_0=U_0=0` chart, for all 21 role packets and every arithmetic-lift filter. |
| nonclaim | A quotient coefficient determinant is not a dimension or unit certificate; the fully proportional chart is retained. |
