# Result

`PROVED`: exact same-support pair noncontainment supplies the full-rank step
in the affine-span MCA compiler, so its global direction-separation
hypothesis can be removed for support-wise bad slopes.

The whole-line route now has no direction-list residual. Its first numerical
walls are `s=14` for KoalaBear and `s=6` for Mersenne-31.
