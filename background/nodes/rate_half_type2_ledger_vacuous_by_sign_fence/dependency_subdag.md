# Dependency sub-DAG

```text
banked razor constants + (C2) floor definition (round-35 R-FG-RAZOR)
                 |
                 v
rate_half_type2_ledger_vacuous_by_sign_fence [PROVED]
                 |
                 : evidence / route restriction
                 v
rate_half_band_crossing_location             [TARGET]
```

There is no requirement edge into the fence. The outgoing edge restricts a
class of proof routes (ledger transports into the bracket); it does not
discharge a critical requirement. Sibling fences sharing the `2r > R`
inequality: `rate_half_far_ca_negation_closure_excess_fence` (the razor
row lies at `r > R/2`, outside the proved shape bound) and the pigeonhole
cap inside `rate_half_far_ca_crossing_offset_value_ledger`.
