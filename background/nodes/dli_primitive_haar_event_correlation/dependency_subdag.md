# Dependency sub-DAG

```text
even/odd moment split at one junction
                 |
                 v
recursive Haar event equivalence (H3)
                 |
binomial/Vandermonde marginals ----> (H4)
                 |
antipodal first-owner theorem ------> (H5)
                 |
                 v
primitive multi-event correlation identity (H6)
```

All inputs are elementary or already proved. The square-root inequality is
an exposed target, not a dependency.

