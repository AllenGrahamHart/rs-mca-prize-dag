# Provenance

Derived by auditing precisely where `gcd(g_*,S_B)=1` entered the separated
heavy-quotient proof. The shared parameter retains two correction jets but
the supported and correction branches need not contribute a third base jet;
that missing coefficient is isolated as `kappa_tau`.

No external theorem or computation is used.
