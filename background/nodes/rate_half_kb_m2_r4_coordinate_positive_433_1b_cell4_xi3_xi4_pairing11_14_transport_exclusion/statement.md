# Statement

## Claim `(KBP1B4-XI34-P1114-TR-1)`

On the guarded positive deployed `433-1b -> O0a` role-cell-4 branch, every
case with missing role `xi in {3,4}` and canonical matching index in `{11,14}`
is empty for all four source-sign pairs and all four target lanes.

This is exactly four labeled slices:

```text
(xi,pairing)=(3,11),(3,14),(4,11),(4,14).
```

Thus `4 source signs * 4 target lanes * 4 labels = 64` raw atlas cases are
paid. No other matching, missing role, role cell, complete positive branch,
or Prize result is claimed.
