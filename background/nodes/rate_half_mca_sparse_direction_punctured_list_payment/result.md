# Result

`PROVED`: a direction supported on `e<d` coordinates reduces the whole
support-wise MCA family to a punctured ordinary list with exact slope fiber
at most `e`.

This pays the extreme high-defect tails at the first large dimension:
`e<=5` on KoalaBear and `e=1` on Mersenne-31.
