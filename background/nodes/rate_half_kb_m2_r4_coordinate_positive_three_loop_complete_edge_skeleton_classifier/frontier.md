# Frontier

Compile the signed product multiset for `(KBP3S-1)` and impose the complete
positive `12 x 6` rank-five product matrix.  The three common loop labels
are already normalized to `0,infinity,1`; only the two common nonloop labels
and seven outside quotient labels remain.
