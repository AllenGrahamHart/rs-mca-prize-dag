# Audit

Date: 2026-07-27.

The threshold table is source-pinned to
`e1_clean_anchor_exact_collision_allowance/statement.md`. The proof uses the
strict official field cap, not a guessed RowC prime or an ambient-field
bit-length approximation.

The conclusion is deliberately one-way. Proper generated subfields are below
the pair-feasibility threshold, but some may still have more than `B*` values
and admit a sharper direct-image proof. Those rows remain with the universal
router.
