# Claim contract

- **claim:** canonical covariance identifies every `c=1` mismatch survivor
  with one normalized role-labelled triple `(S,P,c)`, and the printed
  forty-step recurrence is equivalent to all three normalized roots being in
  `mu_(2^40)`.
- **scope:** the official generic canonical `c=1` fiber-two cycle chamber.
- **dependencies:** the coefficient-resultant compiler and the canonical
  covariance in its inherited boundary chain.
- **falsifier:** a valid packet not preserved by repeated-root normalization,
  or split nonzero pair roots for which `(C1N6)` has the wrong torsion
  verdict.
- **nonclaims:** no canonical gap, span, or norm nonvanishing is proved.

