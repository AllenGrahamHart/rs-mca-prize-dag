# Provenance

- **Inputs:** the paired coefficient-MDS gate, connected scalar-weld
  dichotomy, all-excess residual factorization, and shape-A norm theorem.
- **New content:** an exact residual parity matrix whose initial zero runs
  are the fiber degree drops and whose only unknown is the common weld
  vector.
- **Route effect:** replaces the false static non-stagnation route by the
  first flag that simultaneously retains source supports, padding, row
  splitting, and the global scalar weld.
- **Computation:** two constant-size finite-field replays; no Modal run.
