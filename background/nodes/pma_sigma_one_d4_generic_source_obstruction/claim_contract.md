# Claim contract - PMA sigma-one generic defect-four source obstruction

## Claim

One valid official rate-half source word has more than `n^6` primitive,
post-owner, exact-defect-four source codewords. Therefore the finite
`#Post<=B_post` clause of `pma_wide_residual` is false.

## Consumes

- the maximal-sunflower source normal form;
- `pma_exact_periodic_owner`, for its exact support-stabilizer predicate;
- `pma_sigma_one_dyadic_near_coset_owner`, for its literal owner predicate;
- `pma_sigma_one_odd_lift_boundary_owner`, for its source-word predicate;
- `pma_sigma_one_post_top_allowance`, for `#Top<=N_top` and
  `B_post=n^6-N_top`.

## Serves

- `pma_wide_residual`: realizes its finite falsifier;
- `petal_mixed_amplification`: kills the current `B_post` premise;
- `imgfib`: requires a downstream re-pose rather than another attempt to prove
  the false `n^6` suballocation.

## Nonclaims

No larger polynomial image-fiber bound, asymptotic lower bound, or replacement
finite profile ledger is proved.
