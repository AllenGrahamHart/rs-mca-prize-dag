# Repeated-BC cell-3 BC- colored norm atlas

- **status:** PROVED
- **scope:** exact function-field descent of the missing-`BE` and missing-`CF` cuts

Let `am=A(-r^4)`, `bm=B(-r^4)`, and `betam=beta(-r^4)` be obtained from the
direct common cofactor kernel.  If `k=b` for missing `BE`, or `k=c` for
missing `CF`, eliminating the unknown endpoint gives the necessary cut

```text
r^4 k^2 betam^2 + (k^2 am + bm)^2 = 0.             (CM-1)
```

In the certified tower

```text
y^2=(q^3+2q^2+q+4)/(q^3+6q^2+q),
r^2=alpha(q,y) r+beta,
```

successive quadratic norms send `(CM-1)` to `F_p(q)`.  All eight sign and
missing-record rows complete.  The normalized numerator degrees are `92`
for `BE` and `100` for `CF`; denominator degrees are `104` and `112`.
Exactly four distinct construction guards record every inversion.

This node does not claim that the cuts have no deployed-field point.

## Falsifier

An incorrect cofactor kernel, a lost denominator, a failed tower relation,
or a mismatch in any printed norm coefficient.
