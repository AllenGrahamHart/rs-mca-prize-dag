# Statement

Work over the deployed KoalaBear field
`F_p`, `p = 2130706433`, and on the guarded product-rank-five branch of
positive `433-1b -> O0a`. In source role cell `11`, `BC+` is the
singleton and the matched role pairs are `(LA,BC-)` and `(AB,AC)`.

1. The `AB` pivot covers all `4 * 6 = 24` source-sign/product-cofactor
   charts. Every guarded common ideal has dimension one, compact basis size
   `20`, and lex basis size `8`; the pivot boundary is the unit ideal.
2. On the open set where the displayed `b`- and `c`-leading coefficients
   do not vanish, the full common ideal is generated by a quadratic relation
   `F(t,r)`, a palindromic quadratic `G(b;t,r)`, and a linear relation
   `H(c;b,t,r)`. Thus its generic coordinate algebra has basis
   `{1,t} * {1,b}` over `F_p(r)`, and `c` is recovered linearly.
3. The discriminant of `F` is a square-free degree-eight polynomial. In
   odd characteristic, the normalization of the associated quadratic base
   curve has genus three.
4. The leading-open complement is classified exactly over `F_p`. For each
   source-sign pair there is one `b`-leading and one `c`-leading
   zero-dimensional fiber, each with lex basis size `4`. Every fiber has a
   linear `t` eliminant and an irreducible quadratic `b` eliminant, so none
   contains an `F_p)-point.

This is a common-locus structure theorem only. It does not exclude any
outside record and does not close cell `11`, any complete role orbit, or
either Prize problem.
