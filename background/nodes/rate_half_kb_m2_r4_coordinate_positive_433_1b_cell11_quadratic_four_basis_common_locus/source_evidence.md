# Source Evidence

- `rate_half_kb_positive_433_1b_cell11_complete_pivot_scout_result.json`
  contains 24 completed exact Singular charts. Modal app:
  `ap-AEk2qREdFiOHiz3sBdHWxV`.
- `rate_half_kb_positive_433_1b_cell11_four_basis_tower_result.json`
  contains both linear-recovery presentations for all four signs. Modal app:
  `ap-PXOGCuPw5mZOz08P4sVPQb`.
- `rate_half_kb_positive_433_1b_cell11_tower_boundary_result.json`
  contains all eight exact leading fibers and their deployed point census.
  Modal app: `ap-sB5It4ZDs5pdiYZa2XBXPu`.

The verifier pins every launcher and result digest, checks source-custody
digests embedded in the JSON, recomputes the discriminants and their
factorizations, and checks the 24-chart and eight-boundary ledgers. The audit
independently reconstructs cell `11` and proves every quadratic boundary
eliminant is nonsplit over the deployed field.
