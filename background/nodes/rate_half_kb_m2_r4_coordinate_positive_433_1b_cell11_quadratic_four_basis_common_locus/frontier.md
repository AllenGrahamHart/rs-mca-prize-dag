# Frontier

The next cell-11 task is to compile one primitive common coefficient kernel
and then impose each outside-record restriction against the exact four-basis
tower. The eight leading fibers need no outside replay over `F_p`, because
their quadratic `b` eliminants are nonsplit.
