# Lineage

Cell `11` is the fixed `B/C` role and therefore cannot inherit any earlier
duplicate-role closure. A four-pivot pilot found a stable eight-element lex
basis but showed that the three-generator compact shortcut used for cell
`5` is nonexact. The present node keeps the full lex ideal, proves that
three triangular relations generate it after leading localization, and
restores all removed leading fibers explicitly.
