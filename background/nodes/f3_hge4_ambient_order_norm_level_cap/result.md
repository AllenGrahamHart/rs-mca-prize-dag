# Result

The HGE4 exact-ratio tower now retains the official ambient order in its
first norm gate. The live range at level `m=2^a|n=2^s` is reduced to

```text
4<=h<=min(m/4-1,2ceil(ma/(8s))-1).
```

The parity sharpening `(AON4)` should be applied after the closed cap. For
`n=2^41`, the combined theorem removes `27,793,519,353` width-level cells
from the proper-level tower before any free/swap generator is allocated. It
introduces no computation request and does not close the surviving aggregate.
