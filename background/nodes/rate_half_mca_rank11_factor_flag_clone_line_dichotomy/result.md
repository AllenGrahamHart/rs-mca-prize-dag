# Result

`PROVED`.

The rank-at-most-two output is quantitatively separated. At the balanced
cutoff, an unsafe factor branch has either an active 10,001-coordinate
projective clone whose common residual bucket owns at least `388650911452`
slopes, or a genuine projective-line triple owning at least the same mass.
The large-clone output is now usable by a shortening adapter rather than
being a possibly inactive feature of the ambient residual code.
