# Audit

1. The proof uses a common scalar for `B_0,B_2,A_1`; no untyped separate
   rescaling is made.
2. Every displayed denominator is replaced by a protected polynomial scale.
3. The pointwise `B_2!=0` premise is inherited from the complete Vieta node.
4. The q argument uses only the outside loop, not speculative nonloop rows.
5. Both common families and both `S1` branches share the same loop ledger.
6. No complete one-loop 442 composition is claimed by this node alone.
