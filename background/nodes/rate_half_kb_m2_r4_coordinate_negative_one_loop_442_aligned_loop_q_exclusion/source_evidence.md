# Source evidence

- The complete-fiber Vieta compiler supplies `(KB41Q-1)` and the pointwise
  nonvanishing of `x_W B_2(W)`.
- The aligned classifier supplies families A and B and all common guards.
- The aligned outside-product router supplies the only retained `S1/S2`
  equations and proves that each retained skeleton has one outside loop.
- `experiments/prize_resolution/rate_half_kb_one_loop_442_aligned_loop_q_exclusion.py`
  performs exact quotient-algebra reduction in the deployed field.
- `verify_audit.py` independently scans both families over `F_73`.
