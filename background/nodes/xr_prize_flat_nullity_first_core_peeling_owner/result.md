# Result

Every retained nonterminal slope in every prize flat-nullity cell now has one
canonical first-core/trade owner. The only unowned slopes are the initial
exceptional set and the terminal density remainder, whose combined size is
less than `n` on each prize row and is already paid.

The remaining P-A task is therefore a count of canonical nonterminal
basis/flat/dual-plane owner packets, not a union bound over all possible
minimal cores and trades.
