# Result

Every cutoff-18 double accident now carries a nonzero sparse principal
coupling norm. For a target of quotient multiplicity `R(t)`, the selected
low-distance product-star center admits at least `R(t)-1` nonzero coupling
generators in the same row-prime ideal.

The coarse fixed-order candidate object drops the second quotient lift and is
the three-generator ideal `(alpha_F,alpha_G,lambda)`. The full target-local
batch remains available for survivor screening. Zero coupling is exactly the
binary telescoping family and can be excluded by an exponent-pattern test.

The critical C36' node remains `TARGET`. Efficient official-order template
generation and a bound for surviving primes are still open. No Modal compute
was used.
