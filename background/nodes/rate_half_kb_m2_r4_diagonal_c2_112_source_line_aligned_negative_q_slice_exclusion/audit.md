# Audit

1. The theorem uses the aligned target `tau^*q`; it does not substitute that
   target in the near-aligned branch, where `tau^*chi_Omega` is different.
2. `P!=0` is proved on the retained locus. Solving `B=0` for `b` is not a
   genericity assumption.
3. Monic normalization is used only for a putative q-slice passer, whose
   resultant must have the printed degree eight.
4. The moving `C=0` locus is not omitted: `b->1/b` preserves the unordered
   moving-moving edge pair and sends it to the represented `B=0` locus.
5. The contradiction uses the previously proved `A!=0`; it does not divide
   out a new exceptional factor.
6. The exploratory `3 x 3` reconstruction minor degenerates at `cd=-1`.
   The proof uses the parent rank-four full matrix there, and the independent
   audit solves all five equations directly on that specialization.
