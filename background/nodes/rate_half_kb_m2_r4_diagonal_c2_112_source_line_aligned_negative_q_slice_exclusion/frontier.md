# Frontier

Apply the q-slice mismatch directly to aligned positive reconstructions.
For the near-aligned branch, retain both signs and replace the target
`tau^*q` by the actual `tau^*chi_Omega`; the aligned coefficient identities
cannot be transferred without that substitution.
