# Audit

The symbols `R_j,S_j` are scaled role data. The color-scale value used in
`Fhat` is `S_c=S_j+qd/3=eta R_j`, not the pre-`d` value `S_j`.

The exact-fiber reduction is conditional on a matched normalized color pair.
Without that match, `eta*(eta+1)` and the distinctness of
`1,beta,gamma` cannot be silently assumed; the candidate must be rejected as
`NO_COLOR_PAIR` or retained without using (JGC8)--(JGC10).

`D_Q*D_F!=0` is not asserted numerically. It is the complete residual
squarefreeness test only because the role guards already make
`Qhat(Y_j)`, `S_c`, and `lambda` nonzero. Likewise `Lhat(-1)` is a test,
not a proved unit.

The ledger does not subsume the norm, assignment-preserving modular
congruence, or degree-`p` inner Belyi lift. It also applies only to the
exceptional `J_*=0` fully proportional cubic `3+2+1` chart.

Verdict: NO ISSUE.
