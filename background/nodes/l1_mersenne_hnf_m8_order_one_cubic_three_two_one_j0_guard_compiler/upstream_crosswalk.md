# Upstream crosswalk

The accepted upstream surface for the predecessor outer theorem is
`experimental/experiments.tex`, exported in
`przchojecki/rs-mca` PR #1121. This node sharpens the candidate interface
behind that theorem by identifying the exact guard ledger.

It should be vendored only as a proved local compiler, preferably as a small
follow-up after PR #1121 is triaged. It is not a computed exclusion, a row
payment, an outer-replay result, or an inner-lift theorem.
