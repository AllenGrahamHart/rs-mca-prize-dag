# Claim contract

| field | value |
|---|---|
| claim | On a retained J-zero role/eta/color branch, every named denominator, HNF, role, exact-fiber, and split-root guard reduces to the explicit finite ledger `G_alg*D_Q*D_F*Lhat(-1)!=0` in `F_(p^8)`. |
| status | PROVED |
| dependencies | J-zero role/`P_4` compiler; common-quadratic compiler; scaled quadratic core; coefficient-matrix router; J-zero outer-lift compiler |
| consumer | `l1_mixed_petal_amplification` |
| exact identity | `a*B*(lambda-1)*Q(y)=R_j^2/d^6`; the former four-factor common-quadratic saturation is automatic from `d*R_j!=0`. |
| exact-fiber test | `disc(Qhat)*disc(Fhat)!=0`; all other factors in `disc(Lhat)` are already guarded. |
| split test | `Lhat(-1)!=0`; also `K_6=Lhat(0)`. |
| rejection semantics | A zero ledger entry rejects that reconstructed factor/eta/color candidate with a named exact reason. |
| open residue | Every actual eight-filter common-root result, every ledger outcome, norm and pointwise-congruence outcomes, the separate inner lift, other h=7 shapes, and the critical consumer. |
| nonclaim | No candidate is asserted to exist, pass, or lift; no emptiness or critical status change is claimed. |
| replay | `verify.py` symbolically checks the low-degree discriminant, resultant, role, and saturation identities; `verify_audit.py` independently checks exact rational samples and gcd degrees. Both remain unexecuted locally under the Modal-only computation rule. |
