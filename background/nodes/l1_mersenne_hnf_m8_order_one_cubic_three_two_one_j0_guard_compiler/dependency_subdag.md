# Dependency sub-DAG

```text
J-zero role/P_4 compiler -----------+
common-quadratic compiler ----------+
scaled quadratic-core compiler -----+--> J-zero exact guard compiler
coefficient-matrix router ----------+              |
J-zero outer-lift compiler ---------+              v
                                      l1_mixed_petal_amplification (evidence)
```

The node pays only the compilation of inherited nonzero and exact-fiber
conditions. Eight-filter common roots, guard outcomes, outer replay outcomes,
the inner lift, other shapes, and the critical target remain open.
