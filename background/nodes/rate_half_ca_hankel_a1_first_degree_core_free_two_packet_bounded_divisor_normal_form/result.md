# Result

Both core-free scalar packets reduce to degree-at-most-three data and one
signed degree-one Picard class. In the packet with an ordinary incidence,
the regular determinant is completely factored.
