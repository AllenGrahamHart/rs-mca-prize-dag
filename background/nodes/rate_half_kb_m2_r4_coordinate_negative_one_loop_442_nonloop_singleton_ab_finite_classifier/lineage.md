# Lineage

This node follows the complete cell numbering in
`critical/nodes/rate_half_band_closure/notes/kb_one_loop_442_common_atlas.py`.
It uses the same product and q determinant interfaces as the earlier
opposite-pair and cubic-root classifiers, but records the genuine finite
survivor found in cells `[3,6]`.
