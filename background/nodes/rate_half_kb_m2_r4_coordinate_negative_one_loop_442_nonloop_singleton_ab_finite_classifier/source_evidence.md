# Source evidence

- Exact compiler:
  `experiments/prize_resolution/rate_half_kb_one_loop_442_cell36_sparse_router.py`.
- The primary verifier rederives both product minors, both q welds, the
  guarded linear-`t` factor, the saturated resultant, and all sixteen
  converse packets.
- The audit independently reconstructs the sixteen packets with modular
  arithmetic and evaluates the original determinant and weld formulas.
