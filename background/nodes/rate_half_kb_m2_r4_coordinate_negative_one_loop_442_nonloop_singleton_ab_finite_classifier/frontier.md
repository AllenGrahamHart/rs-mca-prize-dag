# Frontier

Compile the unique product involution induced by the common pairs
`(-b^2,-b)` and `(c,-c)`, determine the outside mate of singleton
`b`, and route the three one-loop 442 outside skeletons over the finite
four-dimensional common algebra.  The only unclassified common matching
orbit is now `[4,5,7,8]`.
