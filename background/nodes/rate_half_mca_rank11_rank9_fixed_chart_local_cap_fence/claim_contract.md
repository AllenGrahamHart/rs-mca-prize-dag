# Claim contract

## Input

Only the fixed rank-nine chart conclusions of the nine-subset target router:
one nine-subset, one rank-nine owner plane, exact rich supports, and
same-support pair noncontainment.

## Output

An explicit official-row realization of those local conclusions with
`4070408` distinct slopes, exceeding the selector floor `2578110`.

## Nonclaim

The realization does not reproduce the full dense-anchor ancestor packet or
an unsafe line. It does not refute an argument that retains weighted
component incidences or couples different charts through one chronology.
