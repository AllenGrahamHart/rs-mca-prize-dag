# Attack

Do not seek a cap below `2578110` from one rank-nine chart using only its
common core, exact supports, owner-plane line geometry, and pair
noncontainment; the explicit construction exceeds that threshold.

Retain data before the selector divides by `C(m'-9,2)`. The live options are:

1. assign weighted component incidences to a chronology-correct owner;
2. couple the owner labels arising from different nine-coordinate charts;
3. prove that the dense-anchor normalization forbids the displayed weighted
   plane while identifying the exact ancestor identity responsible.

Any proposed repair should be tested against the eight-heavy-fibre model
before it is promoted.
