# Fixed rank-nine chart local-cap fence

- **status:** PROVED
- **scope:** the local conclusions printed by the fixed nine-subset target
  router, without the discarded multiplicities of its ancestor incidence
  census

On the official residual row

```text
(n,K,m,w)=(2097152,1048576,1116048,67472)
```

there is a ten-dimensional RS correction space `V`, a nine-subset `B` with
`rank(ev_B|V)=9`, and a same-support pair-noncontained rank-nine owner-plane
family with

```text
4070408 > 2578110
```

distinct finite slopes. Its complete owner plane has common received-pair
core `J` of size `K-1`; every selected record has exact support `m`, error
affine rank at most two, and a rank-ten eleven-subset containing `B` on a
positive-dimensional affine-owner component.

Thus the fixed-chart conclusions alone do not imply a cap below the selector
floor. Any payment must additionally use information discarded by that
selector, such as weighted component multiplicity, compatibility between
different charts, or a chronology-correct aggregate owner.

## Nonclaim

The construction is not an unsafe KoalaBear line and does not instantiate
the fixed 32-anchor/18-dense-root ancestor packet. It fences only a proof
that treats the target-router output as its complete hypothesis set.
