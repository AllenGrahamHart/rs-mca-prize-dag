# Audit

- Exact sparse integer polynomials verify all seven product and squared-sum
  rows in all four target lanes.
- The transport transposes only rows 3 and 4 and fixes the compact residual
  order, hence every matching index.
- The complete nonzero and pairwise-not-opposite guard divisor is invariant
  up to units.
- Both sign-cycle invariants and all target lanes are fixed.
- A complete 15-cell common-role census verifies that the outside map fixes
  every source role cell.
- The primary certificate covers 3,600 cell/sign/lane/matching system
  bijections. An independent integer audit checks 31,104 coordinate/lane
  rows without importing the primary implementation.
- No open `xi=3` or `xi=4` exclusion is inferred from the transport alone.
