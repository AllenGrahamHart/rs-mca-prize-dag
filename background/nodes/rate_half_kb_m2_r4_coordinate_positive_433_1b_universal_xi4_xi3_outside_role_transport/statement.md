# Statement

## Claim (KBP1B-XI43-TRANSPORT-1)

For every guarded positive deployed `433-1b -> O0a` common role cell,
source sign, target lane `(sigma_c,sigma_o)`, and canonical matching, set

```text
D = sigma_o e,       E = sigma_o d,       F = f.
```

This involution maps the complete system with missing `xi=4`
(`sigma_o ef`) bijectively to the system with missing `xi=3` (`df`). It
fixes the source role cell and source signs, target lane, compact residual
record order, matching index, and target guard divisor.

Consequently every proved `xi=3` exclusion block transports with identical
scope to `xi=4`. The theorem proves a system equivalence only; it does not
itself exclude either role in a role cell where `xi=3` remains open.

## Falsifier

A role cell, target guard, product record, squared-sum record, lane, compact
matching index, or source sign not preserved by the displayed involution.
