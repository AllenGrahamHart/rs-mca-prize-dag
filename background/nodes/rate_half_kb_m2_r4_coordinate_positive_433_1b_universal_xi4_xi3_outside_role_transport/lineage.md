# Lineage

- The exact signed outside-role map first closed cell-3 `xi=4` from its
  proved `xi=3` partition.
- Reinspection showed that its common-role action is the identity, so the
  algebra applies to every one of the 15 role cells.
- The generalized certificate verifies all role cells rather than extending
  the old cell-3 exclusion node beyond its pinned scope.
- For live cell 4 this halves the independent variable-role campaign from 18
  quotient orbits to nine.
