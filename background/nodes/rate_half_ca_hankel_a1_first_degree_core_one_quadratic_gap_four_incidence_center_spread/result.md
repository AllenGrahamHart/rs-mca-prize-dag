# Result

The quadratic `u=4` double-root survivor is now pinned to an exact
nonuniform locator design and a coding-theoretic spread law. A center line
with `r` deficient slopes carries at most `rho+1-r` of the `rho+4` centers;
a locator pair therefore has at least three, four, or five third blocks
whose union exceeds `2rho`.
