# Proof

The equal residual pair again gives the three rational branches
`q=B_i/A_i`. With `m=df`, `S=(d+f)^2`, and `z=1/d`, the missing Vieta
equation is the even quartic

```text
1+(2m-S)z^2+m^2z^4=0.                         (RL-1)
```

For pairing 1, the next paired cut uses `bf`; for pairing 2 it uses the
anchored `sigma_c cf`. Dividing the corresponding quadratic cut into
`(RL-1)` leaves an exact linear remainder in every row. The pinned compiler
norms that remainder through the cell-5 row-6 four-basis algebra.

Pairing 1 has 12 rows: 76 target roots, 180 candidate roots, 52 nonsquare
no-`b` lifts, and 280 guarded source routes. Its finite partition is 24
missing-impossible, 24 zero-product, 12 empty-branch, and 220 checked points.
The exact intersections produce 48 `z/d/e/f` lifts and 192 nonzero final
colored lanes.

Pairing 2 has 24 rows because both `sigma_c` anchors are explicit: 112 target
roots, 320 candidate roots, 88 nonsquare no-`b` lifts, and 432 guarded source
routes. Its finite partition is 48 missing-impossible, 48 zero-product, 24
empty-branch, and 312 checked points. The intersections produce 16 lifts and
32 nonzero final lanes. Neither packet exports a leading boundary.

Independent Frobenius reconstruction covers 129 profiles and 596 roots,
with maximum degree 508. A separate replay reconstructs all 36 rows and keeps
the two pairing totals distinct. It checks every candidate union, source
relation, missing value, common `z` root, lift, and colored cut. Thus labels
`(3,1)` and `(3,2)` are empty; exact outside-role transport pays `(4,1)` and
`(4,2)`. QED.
