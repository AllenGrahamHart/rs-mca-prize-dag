# Audit

The verifier pins all primary, root, replay, tower, kernel, template, and
transport artifacts. The external root program uses a Frobenius gcd. The
direct Modal replay recomputes both reciprocal-linear routes and prints
separate exact totals for pairings 1 and 2.

The audit verifier checks method markers and rejects a hostile mutation in
either pairing ledger.
