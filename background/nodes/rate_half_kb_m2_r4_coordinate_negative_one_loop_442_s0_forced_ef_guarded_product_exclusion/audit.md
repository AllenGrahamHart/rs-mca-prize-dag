# Audit

1. Both `tau_0` parities and both cubic components are replayed.
2. Clearing `e^3` is restricted explicitly to `e!=0`.
3. Every completed basis contains the one-term monic element `e^2`.
4. The conclusions are guard-saturated, not raw-unit.
5. Common-sign transport uses exact `b,c,m` equality.
6. No source-root, `q`, or interpolation conclusion is asserted.
