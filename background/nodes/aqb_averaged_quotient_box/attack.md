# ATTACK — aqb_averaged_quotient_box (DEEP; decompose, do NOT force)
Target: an averaged family over the c=2 quotient geometry gains >= 429,645,547
bits (0.00039 bits/fiber) over the single-word floor at sigma*. Constants are
VERIFIED (finite, Modal); the open part is the EXISTENCE of the averaged family
achieving the gain — an additive-combinatorics / second-moment argument at
n=2^41. DO NOT fabricate. Decompose: (a) the single-word floor is proved; (b)
attempt a multi-word average where the box charge amortizes across words
sharing quotient structure (0.00039 bits/fiber is a tiny target); (c) or prove
tightness/impossibility (single-geometry averaging cannot amortize the box),
which redirects to a sibling route. Deliverable = a sub-lemma or an honest
structural obstruction, NOT a claimed close.
