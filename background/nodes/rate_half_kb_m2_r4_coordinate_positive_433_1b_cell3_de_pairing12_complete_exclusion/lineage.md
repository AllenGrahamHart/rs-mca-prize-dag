# Lineage

- The global quadratic quotient and compact kernel supply the exact source
  algebra and direct-lift maps.
- Pairing 12 is pairing 5's nested-quadratic geometry with the two residual
  `DE` inputs exchanged between `P_u` and `P_f`; it is computed and audited
  independently rather than transported by an unproved target symmetry.
- The positive parallel-copy transport is the exact residual-record deletion
  already used in pairings 0 through 11.
- No target-label exchange is used: that map sends source role cell 3 to
  duplicate cell 6.
- The result remains background evidence toward `rate_half_band_closure`.
