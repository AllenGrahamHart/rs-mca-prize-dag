# Audit

- The source census is the Cartesian product of four source signs, both
  `sigma_c` values, and `xi in {0,2}`: 16 rows.
- Every row is complete in the six-element basis and has degree tuple
  `(2,2,8,1)` for `P_u,P_f`, the eliminant, and its remainder.
- Direct six-by-six and quadratic-over-cubic tower norms agree in every row.
- The candidate parameter set is exactly the union of norm and exceptional
  guard roots; all 272 listed source points are unique within their row.
- All 16 boundaries have `f=0`, fail exactly `nonzero_5`, and cover both
  `sigma_o` lanes for their fixed `sigma_c`.
- All 64 nonboundary final-pair evaluations are nonzero.
- Witness and unresolved ledgers are empty in every row.
- The theorem counts 32 computed and 16 transported raw atlas cases.
- Primary and independent verifiers pin the compiler and census hashes.
