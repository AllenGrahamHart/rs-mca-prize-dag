# E1 N=256 E=31 endpoint exclusion

- **status:** PROVED
- **closure:** proof plus complete exact census

No pair-feasible `N=256` folded-profile `(3,4,0)` collision has
autocorrelation variance `V=62`.

The exact E31 reduction leaves only `(3,7)`, `(2,5,1)`, and `(1,3,2)`, and
the proved joint exclusion exhausts all three profiles. Consequently every
unresolved positive-variance vector in this folded profile has

```text
0 < V <= 60,       V even.
```
