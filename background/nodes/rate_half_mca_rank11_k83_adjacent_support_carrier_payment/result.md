# Result

`K'=83` is `PROVED`, extending the rank-nine closed prefix from `10..82` to
`10..83`. The old K'=83 node remains a valid limitation of the former
pairwise-only route, but it is superseded as the active frontier by the
fixed-union adjacent-support payment.
