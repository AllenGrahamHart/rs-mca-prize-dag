# Audit

1. Ordinary plus offsets 1--72 is exhaustive and disjoint after impossible
   support-2/3 pairs are removed by the proved carrier-position theorem.
2. Pareto compression retains fixed-union adjacent-pair provenance.
3. Raw-safe pruning is monotone because every later charge only lowers caps.
4. Adjacent pair bounds compose only on disjoint support sets.
5. Primary and audit agree on all 73 lane maxima and coverage keys.
6. A timed-out attempt is ignored; its successful repair is required.
7. The safe ceiling and component gap are recomputed by exact integers.
