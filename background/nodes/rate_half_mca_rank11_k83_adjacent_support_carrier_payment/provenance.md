# Provenance

Derived on 2026-08-16 while repairing the K'=83 pairwise-atlas wall. The
analytic repair is the proved fixed-union adjacent-support coupling. Exact
Modal app IDs, source hashes, partial-checkpoint handling, and output hashes
are pinned in `source_contract.json` and `notes/PRIZE_COMPUTE_REQUESTS.md`.
