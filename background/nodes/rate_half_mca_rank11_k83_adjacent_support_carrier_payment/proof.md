# Proof

Put `q=73`, `m=67555`, and `n=1048659`. Partition exact support-2/3
completion maxima as follows.

1. `M_3<=M_2`, including the zero endpoints, is the ordinary lane. Positive
   pairs use the exhaustive `T23/A23` fixed-union alternatives; a lone
   positive stratum uses its single completion carrier.
2. `M_3>M_2>0` belongs uniquely to offset `M_3-M_2 in {1,...,72}`.

No support-2/3 pair remains. Every offset retains every exact pair before
the support-4/5 geometry is applied. The full pairwise carrier atlas then
enumerates its exhaustive `T/A/F` alternatives.

For every resulting fixed union, apply the fixed-union adjacent-support
coupling. Several pair bounds are composed only when their support sets are
disjoint. In particular, the former ordinary wall is paid by the pair set
`{4/5,6/7}` in both `T23=(39,7)` and `A23=(38,8)`.

The primary and independent routers were sharded into one ordinary lane and
72 exact offsets. After one checkpointed timeout, only the missing audit
offsets were rerun with a longer remote wall. The compact merger accepts a
lane only after a complete successful checkpoint and requires exact
agreement on source units, raw rows, threshold decisions, maximum, and
margin. It certifies all 146 implementation/lane jobs. The global value is

```text
s2=56/s3=54/s4=41/s5=33/offset2/c6F/c7F/c8F/c9F/raw-safe,
```

with premium `P_83` and the printed positive ceiling margin.

Finally, substituting `P_83` in the exact integral component ledger gives
total capacity

```text
920461099628656106224487212071282760146970379519297839205001496
```

against required incidence

```text
920461204198951229983425260617556238139848888096439280368333890.
```

Their difference is the stated positive gap. QED.
