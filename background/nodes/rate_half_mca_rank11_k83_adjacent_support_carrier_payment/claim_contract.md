# Claim contract

- **Claim:** the rank-nine `K'=83` row is paid and the closed prefix is
  `10..83`.
- **Finite partition:** exactly `ordinary + offsets 1..72`.
- **Geometry:** only proved single, pairwise, fixed-union support-4/5,
  fixed-union support-5/6, and
  support-disjoint adjacent-pair charges are used.
- **Audit bar:** every lane has a successful primary and independent
  checkpoint with equal coverage keys and premium.
- **Nonclaim:** no row `K'>=84`, full rank-nine interval, LIST terminal, MCA
  terminal, or prize terminal is closed here.
