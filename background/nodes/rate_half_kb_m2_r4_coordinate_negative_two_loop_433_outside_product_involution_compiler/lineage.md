# Lineage

The common-`K` classifier left at most 24 `M2/M3` points but no explicit
outside records.  This node uses the two already-present antipodal pairs to
fix the product involution and the singleton's mate, reducing the remaining
paired-product work to three copies of one scalar bilinear equation.
