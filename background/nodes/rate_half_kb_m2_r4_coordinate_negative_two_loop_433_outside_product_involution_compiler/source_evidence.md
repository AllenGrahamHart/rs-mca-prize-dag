# Source evidence

- The `M2/M3` classifier supplies the exact common-`K` equations and
  homogeneous Mobius cofactor vectors.
- The coordinate source-facet signature identifies `I` as antipodal and
  fixes the aligned/unaligned location of `xi=I minus K`.
- The paired-product involution gate supplies the six-row rank interface and
  its conjugated-negation interpretation.
- Primary and audit verifiers reconstruct the cross product, determinant,
  forced mate, and both `2^32` resultants from exact integer polynomials.
