# Frontier

Enumerate only source-facet edge-type assignments compatible with the
forced `xi=-M` product and the three residual bilinear involution rows.
Split aligned `xi=eta` from unaligned `xi in L^c`, and split the latter by
whether `xi` is colored.  For each survivor, impose the full twelve-label
Mobius interpolation before any remaining q or resultant equations.
