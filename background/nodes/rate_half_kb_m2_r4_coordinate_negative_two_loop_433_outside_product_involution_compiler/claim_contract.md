# Claim contract

| field | content |
|---|---|
| claim | The two internal antipodal `K` pairs determine the explicit product involution `(KB43O-2)--(KB43O-4)`; `xi=-M` has the forced finite nonzero product `(KB43O-6)`; all three remaining outside pairs obey the same bilinear gate. |
| assumptions | A guard-compatible `M2/M3` root from the exact parent classifier, the coordinate source-facet signature, odd characteristic, and the negative paired-product theorem. |
| exact interface | Replaces the generic six-row paired-product determinant by one forced singleton record and three copies of a printed bilinear equation. |
| nonclaim | Edge-type assignment, full twelve-row Mobius interpolation, complete q/resultant closure, coordinate orientation, a row, or either Prize theorem. |
