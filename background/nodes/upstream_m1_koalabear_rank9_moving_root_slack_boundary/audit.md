# Audit

## Source and fidelity

The proof was read from upstream commit `951ad203`, not inferred from its
title or experiment. The local statement retains every load-bearing premise:
full outside, full gcd, the exact moving-set cardinality, the reduced-degree
budget, the source-rational survivor inequality, `J>=21`, and the C5 owner.

## Checks

1. Cancelling `H` is legal only because moving points are outside the common
   zero set; this is explicit.
2. The full moving set is used. One selected root would not imply `x<=e`.
3. The `-1` in `r=s-t-1` comes from the degree bound `k-1` and is retained.
4. C5 absorption uses two distinct selected slopes and a base locator `H`;
   it is not asserted for a nonbase gcd twist.
5. The official integer bounds are exact.
6. Both nonbase one-slack shapes are populated by exact toy algebra in the
   upstream and local controls. They cannot be removed by a universal local
   emptiness argument.
7. No selector completeness, aggregate count, safe certificate, or status
   promotion is inferred.

## Route consequence

Further work must use the deployed regular-locator equations, the eight
rank-nine outlier directions, or a new first-match owner. Repeating generic
gcd or moving-root divisibility cannot close either surviving cell.
