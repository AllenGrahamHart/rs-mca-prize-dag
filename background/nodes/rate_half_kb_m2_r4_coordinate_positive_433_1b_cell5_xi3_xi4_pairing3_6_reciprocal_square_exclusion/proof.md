# Proof

Delete the `DF` record and use the exact source-role cell-5 tower and compact
kernel supplied by the parent nodes. Put `m=df`, `S=(d+f)^2`, `z=1/d`,
`y=z^2`, and `q=de`. The missing Vieta equation is

```text
M(y)=1+(2m-S)y+m^2y^2=0.
```

The pinned cell-4 pairing-3 compiler eliminates the first two residual
pair equations by its reciprocal-square construction. A syntax-tree adapter changes
only the source algebra: it supplies the cell-5 row-6 four-basis tower and
compact kernel, while retaining the audited compiler function unchanged.
The resulting linear remainder is normed through the exact basis
`1,t,b,bt`.

All 8 required rows terminate. Their complete ledger contains 32
target roots, 88 candidate roots, 80 guarded source routes,
48 checked finite points, 16 missing-impossible exits, 16 target-product
boundaries, 40 `r`-boundaries, 32 `t`-boundaries, and 24 nonsquare
no-`b` exits. No row exports a witness, free branch, or unresolved case.

A separate SymPy/Galois-tools reconstruction covers the combined pairings
3--5 packet: 69 distinct polynomial profiles, 332 field roots, and degrees
through 4560. A second independent program replays candidate-root coverage,
source relations, missing records, and the original target equations while
keeping the three pairing ledgers separate. The direct replay finds no compatible `q` lift after the complete pairing-3 root intersection, so there is no target representative.

The universal generic-label orbit quotient transports `(3,3)` exactly to
`(3,6)`, `(4,3)`, and `(4,6)`. Therefore every label in
`{(3,3),(3,6),(4,3),(4,6)}` is empty. QED.
