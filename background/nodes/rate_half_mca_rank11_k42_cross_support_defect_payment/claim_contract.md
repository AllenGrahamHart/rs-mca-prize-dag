# Claim contract

This node changes only the weighted sparse completion premium at `K'=42`.
It reuses all kernel coranks, the all-core rank-nine chart, all 55-shadow
deficits, and the sharp isolated-incidence lower bound from the preceding
payment.

The six support-five cases partition the maximum deletion-completion count:
defects zero through four are carrier branches, and their complement is the
`q-5` deletion branch.  Every new cap is intersected with the existing
supportwise cap before weighting.

The node closes only the rank-nine component target at `K'=42`.  It does not
pay a rank-eight alternative, close rank eleven, move active-v4 mass, or
assert closure at `K'=43` or later.
