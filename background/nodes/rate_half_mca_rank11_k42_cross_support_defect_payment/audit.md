# Audit

1. Every support-five deletion has at most `q` completions.
2. Defects `s=0..4` and the all-at-most-`q-5` fallback are exhaustive.
3. Each near-saturated branch applies both the exact source deletion count
   and every valid cross-support carrier cap.
4. Every new support cap is intersected with the prior cap.
5. All support deficits `C(11-d,2)` for `2<=d<=9` are retained.
6. Every kernel corank and every rank-nine core offset is retained.
7. Demand uses the exact isolated subtraction `C(n,11)` once globally.
8. The record coefficient and exact floor-record cross are positive.
9. `K'=43` is replayed independently and remains a negative control.
10. No rank-eight or chronology target is declared paid.
