# Audit

1. The factor census is over `QQ[s,p]` and is replayed for both targets.
2. Nonnamed means absent from the exact transported-unit factor set, not
   merely absent from a hand-written shortlist.
3. The pseudo-remainder prefix is a necessary-condition transformation only;
   reversing it requires the printed leading coefficient to be nonzero.
4. The `480`-second function-field run timed out after conversion at roughly
   `19 GB`. Its uncomputed remainder is not evidence.
5. The growing pseudo-remainder prefix is a no-go fence, not a closure.
6. App `ap-SyHnQl54duiHou83jukuJA` completed two bounded large-curve
   attempts. Both timed out only after their seed bases and reduced rows were
   printed; peak child RSS stayed below `0.4 GB`.
7. App `ap-vewm2QDSIuDJ9Euo4ANPdC` completed both 30-step curve-reduced
   pseudo-remainders and timed out only in the final intersection; peak child
   RSS was about `0.44 GB`.
