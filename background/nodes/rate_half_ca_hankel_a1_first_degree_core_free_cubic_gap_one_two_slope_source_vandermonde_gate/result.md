# Result

The local first-jet constraint is now expressed directly in the two-slope
RS error supports and values. Minimum support difference is `c_alpha`; at
equality the excess recurrence roots are exactly the newly introduced error
locations.
