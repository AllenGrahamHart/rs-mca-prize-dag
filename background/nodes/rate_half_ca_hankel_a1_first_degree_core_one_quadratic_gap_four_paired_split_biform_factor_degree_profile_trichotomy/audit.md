# Audit

1. A content root is first forced into `U_0` by an exact clean fiber; it
   is then excluded because every dual-MDS source row on `U_0` is a
   nonzero polynomial. No assertion about the exceptional row's root set
   is needed.
2. Constant content upgrades `sum n_j<=N` to the exact identity
   `sum n_j=N`; the trichotomy would be false without this step.
3. Equality cases at `qm_j=3e` and `qm_j=6e` belong to the large and huge
   classes, respectively, as required by the ceiling function.
4. The rounding correction is below `3/2` because `m_j<e`; therefore
   `(FDT4)` lists every possible value of `chi_j`.
5. The parameter-degree restrictions rule out three large factors, two
   huge factors, and a simultaneous large and huge factor for both
   `q=9` and `q=7`.
6. The exact deficit equation forces `E=0`; this is stronger than merely
   finding one macroscopic factor.
7. The independent verifier exhausts all integer partitions for every
   odd `7<=e<=31` and both values of `d_A`, including all three profiles.
