# Provenance

Derived from the factorwise incidence inequality by retaining the exact
one-unit identity `2N-3M=-1`. The content-removal argument uses a clean
dual-MDS fiber and the nonzero source-row representation on `U_0`.

No external theorem or large computation is used. The independent audit
is a finite replay of the integer-partition consequences, not evidence
substituted for the proof.
