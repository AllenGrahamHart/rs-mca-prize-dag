# Claim contract

- Exact row only: `K'=76`, `q=66`, `m=67548`, `n=1048652`.
- Exactly the 3,800 above-ceiling tuples represented by the canonical digest
  are rerouted through the complete pairwise atlas.
- Every high-support terminal/defect branch remains in the conservative and
  geometry streams.
- Eligible fixed unions use the flat-coupled support-4/support-5 charge.
- Counts and comparisons are exact integers; no sample is used.
- This node closes `K'=76`; it makes no claim about `K'=77`.
