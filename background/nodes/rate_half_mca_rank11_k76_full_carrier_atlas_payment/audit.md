# Audit

1. Conservative and geometry lanes are exhaustive and disjoint.
2. Above-ceiling filtering uses exact integers and strict `>`.
3. The 3,800-tuple frontier is pinned by canonical digest.
4. Full-completion and high-support defect branches are retained.
5. Every reroute and geometry maximum is below the safe plain maximum.
6. Floor placement, record coefficient, and payment are replayed exactly.
7. Heavy reconstruction is Modal-only; local verification uses the shared
   compact-contract validator.
