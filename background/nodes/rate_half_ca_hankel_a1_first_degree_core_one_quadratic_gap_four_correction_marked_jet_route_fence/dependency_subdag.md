# Dependency sub-DAG

```text
Pade regular-factor identity ----+
marked Hankel determinant gate --+--> correction marked-jet route fence
two-simple marked factorization -+
```

The output removes determinant multiplicity as a standalone exclusion
route while retaining Hankel/source realizability as the live target.
