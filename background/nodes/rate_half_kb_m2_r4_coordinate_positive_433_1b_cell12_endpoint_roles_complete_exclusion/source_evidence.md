# Source Evidence

The source cut and deployed-root replay are separate programs.  The target
census uses Singular guarded ideals; its independent audit rebuilds every
system in SymPy and computes unrestricted lex bases.  Scripts, compact source
packets, complete result packets, source hashes, and program hashes are
retained in `experiments/prize_resolution`.
