# Audit

1. The second and third coordinates are selected from root sets after
   removing all previously selected coordinates, so they are distinct.
2. The denominators are the remaining universe sizes `1116047` and
   `1116046`, not the original universe size twice.
3. Slope mass, rather than class count, is retained at each nesting step.
4. Rank three gives a two-dimensional kernel in `B`; multiplication by the
   two-space `P` gives dimension at most four, without assuming tensor-map
   injectivity.
5. The contradiction uses the chronology-safe `R_4` slope cap on the final
   first-owned subfamily.
