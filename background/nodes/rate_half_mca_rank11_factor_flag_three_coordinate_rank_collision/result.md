# Result

`PROVED`.

The high-mass local-core bucket cannot have three independent residual
evaluation columns. It forces a rank-at-most-two three-coordinate collision
carrying over `3.47e11` slopes. The next branch is therefore a concrete
rank-two evaluation-flat or proportional-column classification, not a
generic dimension-eight list bound.
