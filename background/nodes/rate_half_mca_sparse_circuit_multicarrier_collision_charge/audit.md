# Audit

1. `W` and `D` are fixed before the target deletion is chosen.
2. Grassmann is applied in dimension ten and gives `g+1-d`.
3. The common-root bound uses the actual union `D union U_A`.
4. A circuit with `j` outside points has exactly `j` outside-deletion
   exposures.
5. Dependent deletion choices are retained as harmless overcounting.
6. Negative outside-completion budgets are replaced by zero.
7. The selected-set multiplier is `C(m-d,11-d)`.
