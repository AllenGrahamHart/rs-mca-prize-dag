# Result

A fixed union carrying a fixed vanishing space gives a branch-safe target
circuit cap through exact outside-deletion exposure.  The lemma separates
the reusable counting step from the geometry needed to construct the
union.
