# `A=1` quadratic center-disjoint heavy-row nonzero scalar

- **status:** PROVED
- **closure:** exclusion of the zero heavy row on the separated center-disjoint locus
- **consumer:** `rate_half_band_crossing_location`

Retain the separated double-root extremal profile:

```text
S_B squarefree,       gcd(g_*,S_B)=1,
Q(t,x_*)=a_Q g_*(t)S_B(t)^3,
E_4=c_E S_B^2,       c_E!=0.                       (HNS1)
```

Assume the correction/support divisor is disjoint from the three center
lines:

```text
gcd(Lambda,g_*S_B^2)=1.                            (HNS2)
```

Then the scalar in the heavy-row center-overlap theorem is nonzero:

```text
G(t,x_*)=c g_*(t)S_B(t)^2,       c!=0.             (HNS3)
```

Consequently the barycentric extrapolation of the unique connected-weld
candidate, when that candidate passes the row coefficient gate, must satisfy

```text
R_lambda(t)=c g_*(t)S_B(t)^2,       c!=0.          (HNS4)
```

It is not enough for the extrapolated row merely to vanish.

## Scope

The proof uses center-disjointness. If a correction root is also a center
root, the exact center-factor cancellation changes the local resultant
ledger and this theorem makes no assertion. Nonreduced or
supported/correction-collision loci remain outside the inherited separated
hypotheses.
