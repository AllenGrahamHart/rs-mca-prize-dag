# Result

The center-disjoint separated double-root survivor has an exact nonzero
heavy row:

```text
G(t,x_*)=c g_*S_B^2,       c!=0.
```

Thus the next rank test must hit this one projective coefficient vector; the
zero row is no longer a survivor. Remaining overlap cases are indexed by
`1<=deg gcd(Lambda,g_*S_B^2)<=3`.
