# Dependency sub-DAG

```text
x4_general_star_minimal_trade_route_cut [PROVED]
    --ev--> x4_primitive_star_u1_coverage [TARGET]
```

The edge records a killed proof route.  It is evidence, not a requirement:
the official-row coverage target remains logically possible through a
strip-aware theorem or a broadened `u1` ledger.
