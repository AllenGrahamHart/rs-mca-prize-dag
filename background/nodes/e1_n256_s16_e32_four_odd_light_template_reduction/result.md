# Result

`PROVED`: the two live `V=64` profiles share exactly 148 affine light-support
templates. Every template has zero light-light diameters and one repeated
light-chord wedge, hence a light three-term progression. The diameter ledger
is `D_64 in {0,4,8,12,16,20}`.

Profiles `(4,7)` and `(3,5,1)` remain open.
