# E1 N=256 E=32 four-odd light-template reduction

- **status:** PROVED
- **closure:** proof plus exact classification

At `N=256`, folded coefficient profile `(3,4,0)`, and autocorrelation
variance `V=64`, either surviving autocorrelation magnitude profile

```text
(4,7), (3,5,1)
```

has four light coefficient positions with the following exact geometry:

- there is no light-light diameter;
- among the six light-light chords, one distance class occurs twice and four
  classes occur once;
- the two equal-distance edges share a light vertex, so the light support
  contains a three-term progression.

There are exactly 28,800 normalized light supports and 148 affine odd-unit
orbits. Their normalized-orbit size histogram is

```text
size 32: 4,       size 64: 16,
size 128: 40,     size 256: 88.                       (1)
```

Consequently the complete diameter ledger sharpens to

```text
D_64 in {0,4,8,12,16,20},       C=(D_64-70)/2.        (2)
```

This theorem is a common light-support router, not an exclusion of either
profile.
