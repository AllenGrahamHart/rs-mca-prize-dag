# Audit

1. `K subset I` and `|K|=5` are load-bearing.
2. Equation `(KBF-2)` concerns the whole outgoing factor, so roots of each
   irreducible outgoing component are contained in that six-set.
3. The conclusion uses the graph-free `s=6` branch only.
4. No parameter-to-carrier identification or owner is used.
