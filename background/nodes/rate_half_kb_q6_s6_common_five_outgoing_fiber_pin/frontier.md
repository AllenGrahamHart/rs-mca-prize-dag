# Frontier

Intersect the exact six-label horizontal fibers with the source-star
neighborhoods of each residual irreducible component. A neighborhood not
contained in `I^c` deletes that component without active-root enumeration.
