# KoalaBear Q6 s6 common-five outgoing-fiber pin

- **status:** PROVED
- **scope:** actual graph-free `Q=6,s=6` equality-wall source packet
- **consumer:** residual KoalaBear source components

Let `F_out(T,X)` be the graph-free outgoing factor. The proved source-facet
theorem supplies a six-label invariant-coordinate set `I` and a five-label
common set

```text
K subset I,       |I|=6,       |K|=5.               (KBF-1)
```

For every `k in K` and every point `pi` in the complete coordinate fiber
`psi^(-1)(alpha_k)`, the horizontal source roots are exactly

```text
Root_T F_out(T,pi)={alpha_j:j notin I}.              (KBF-2)
```

In particular, every irreducible outgoing component has all of its
horizontal roots at such a point in `I^c`.

This is a source-fiber identity, not a component exclusion or owner. It
moves no ledger quantity and closes no decomposition type, endpoint row,
KoalaBear row, or Prize problem.

## Falsifier

An actual `Q=6,s=6` graph-free packet in which `K` is not a five-subset
of `I`, or a point over `alpha_k`, `k in K`, whose outgoing root set
differs from `I^c`.
