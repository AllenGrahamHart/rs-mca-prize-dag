# Proof

The E16 router proves that every `V=32` candidate lies in one of four profiles
on 154 templates. The four-profile exclusion proves that two routed classes
are exactly empty, every proper-conductor vector is removed by the conductor
theorem, and every full-conductor norm has positive odd part below `2^250`.
These branches exhaust the router, so the entire `V=32` chamber is empty.
Variance is even, hence the next live positive endpoint is at most 30.
