# Audit - L1 partial-pullback Johnson router

1. `z` counts agreement points, not merely the number of partial fibers.
2. Agreements in incomplete domain fibers are included in `z`.
3. The identity `|Agr|=s|E|+z` is exact and disjoint.
4. The quotient threshold rounds upward after subtracting `Z`.
5. Residue-dependent component dimensions determine `kappa` exactly.
6. The direct Johnson count applies to interleaved tuples without a field-size
   or smooth-label assumption.
7. The kernel factor is retained after the Johnson bound.
8. Map aggregation uses the tame whole-petal census and no broader class.
9. Polynomiality requires fixed `K` and `q<=n^gamma`.
10. The `F_17` obstruction is checked on the nonpositive side.

The next theorem must force `(PJ5)` or bound the nonpositive-gate cells by a
different structural payment.
