# Frontier

Cells `0,1,2` are now closed.  Principal cells `3..14` remain.  Cell `3`
is the next natural chart census; its chart-0 rows were positive-dimensional,
so expect component decomposition rather than immediate unit ideals.
