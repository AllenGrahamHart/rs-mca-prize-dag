# Result

The signed-8-core proposal exceeds `2^89` only by counting zero-sum padding
copies with identical `e_1`. Its distinct-center count is at most
`2^36.292`, or `2^40.044` even with every sign pattern. The proposed
size-budget discharge is refuted and `generator_economy` remains open.
