# Lineage

This uses the missing-record eliminant and finite-root replay pattern from
the closed `O0a` workboard, rebuilt on the explicit repeated-BC torus rather
than transplanting an `O0a` factorization.
