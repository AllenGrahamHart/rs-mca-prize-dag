# Claim contract - PMA sigma-one first-layout domination

## Claim

All finite first-match Post mass after the first paired core is contained in
the first core's `M` planted anchors. Hence known per-sunflower payments are
global after applying them once to the first layout, and only that layout's
unpaid residual remains.

## Consumes

- `pma_aux_list_reduction`: universal core-defect carriage of every
  non-planted listed codeword in a fixed source layout;
- `pma_sigma_one_paired_core_normalization`: unique layouts, anchor sets, and
  the exact first-match decomposition;
- `pma_sigma_one_post_top_allowance`: the finite Post object and `B_post`;
- the three proved finite source payments for the combined arithmetic.

## Serves

- retired `pma_wide_residual`: replaced a weighted paired-core census by one
  first-layout residual plus `M`, before that endpoint was refuted;
- `petal_mixed_amplification`: prevents a hidden chart multiplier in any
  corrected polynomial/profile ledger.

## Nonclaims

No payment of the first-layout residual, asymptotic chart collapse, unsafe
witness, or transfer between profile columns is claimed.
