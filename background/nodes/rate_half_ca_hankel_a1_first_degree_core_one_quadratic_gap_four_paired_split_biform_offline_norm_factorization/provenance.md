# Provenance

Derived in Codex Cycle 146 from the two paired split-biform reductions after
an own-repo subtraction showed that the newly merged `Rout` prompt belongs
to the already closed strict `A=3` branch. The construction is the
parameter-slope norm of the live `A=1` carrier and uses no numerical
inference.
