# Result

`PROVED`. The extremal center line has only three source-root classes, of
sizes `p-1+r_gamma`, plus at most one exceptional coordinate. This converts
the previously aggregate missing-incidence statement into a field-valued
source partition available to every off-line interpolation argument.
