# f3_h3_factorial_collision_69

- **status:** see `dag.json` (single source of truth)
- **role:** stronger background sufficient route

For every official row,

```text
M_21=sum_(t!=1) P(t)(P(t)-1)R(t) <= 69n^2.       (FM69)
```

The summand counts two distinct ordered product representations and one
quotient representation of the same parameter.
