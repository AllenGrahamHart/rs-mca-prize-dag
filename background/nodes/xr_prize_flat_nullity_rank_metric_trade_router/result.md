# Result

Large flat-nullity mass now forces low trade rank. Every core carries a trade
of rank at most

```text
t-1-ceil(((u+v)(t-2)+e+1)/|V|).
```

At maximal flat nullity this sends small official cores directly to the
rank-one circuit-owner or rank-two lanes. The remaining unrestricted-rank
region is confined by `(RM4)--(RM5)` rather than being the whole nonuniform
cell.
