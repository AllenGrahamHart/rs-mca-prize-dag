# Dependency sub-DAG

```text
upstream_sp_coefficient_scale_quotient_sieve [PROVED]
  --ev--> x4_primitive_star_u1_coverage [TARGET]
```

The edge is evidence because the theorem defines and removes the quotient
part but does not bound the remaining primitive local degree.
