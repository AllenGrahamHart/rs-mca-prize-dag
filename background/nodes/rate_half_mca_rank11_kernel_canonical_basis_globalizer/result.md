# Result

Every fixed kernel chart is now an aggregate object rather than a collection
of unrelated irreducible components. Its rank stratum has one common
explanation space, one exact record cap, and one generalized-MDS extension
cap. This supplies the summable capacity formula used by the adjacent
finite shortening cut.
