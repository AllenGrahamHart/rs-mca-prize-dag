# G1/K4 intrinsic-scale reserve compiler

- **status:** PROVED
- **consumer:** `petal_small_scale_staircase_census`

Assume the quotient-closed G1 paid-atlas theorem on every official row and
every rate-preserving intrinsic quotient descendant of dyadic length
`n'>=256`, with weighted census

```text
sum_(chart chi) (m_chi+1) <= (121/128)(n')^6.
```

Then K4's proved per-chart bound gives a primitive payment at most the same
quantity.

at every rate `rho in {1/2,1/4,1/8,1/16}`. Combining the current row and all
descended intrinsic scales with `intrinsic_scale_geometric_ledger` costs less
than

```text
(64/63)(121/128)n^6 = (121/126)n^6 < n^6.
```

Together with the proved quotient descent and exact-support projection, this
discharges `petal_small_scale_staircase_census` conditional only on the
quotient-closed G1 atlas.
