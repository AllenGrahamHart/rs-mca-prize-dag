# Provenance

This node specializes three existing proved theorems:

- `l1_fixed_support_defect_johnson_bound`;
- `l1_background_overlap_singleton_payment`; and
- `l1_joint_core_background_johnson_bound`.

The missing EMPTY bookkeeping was identified and independently replayed in
`notes/pilots_20260809/m7_falsifier_hunt/`. This node banks only the exact
symbolic reduction; it does not adopt that pilot's sampled density figures.
