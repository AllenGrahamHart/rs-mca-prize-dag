# Audit

1. The loop-pair exchange acts on labels, common products, and outside
   products; it is not inferred from similar-looking row equations alone.
2. The minus sign in `c'=-c/b` is required and preserves `tau=-1`.
3. The forced `H8-M` value is `b^2`, verified homogeneously from its own
   numerator and protected denominator.
4. One common scaling `b^-2` is used for all twelve product values.
5. The horizontal transformation preserves `sigma` and every forced-type
   orbit.
6. Involutivity and `b!=0` cover the inverse chart; the conclusion is a row
   deletion only at complete products.
