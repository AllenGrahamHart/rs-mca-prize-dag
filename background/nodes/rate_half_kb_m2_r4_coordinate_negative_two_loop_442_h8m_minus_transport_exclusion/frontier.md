# Frontier

Analyze the two `tau=+1` eighth-root rows.  First test the same loop exchange:
derive its sign and `c` transport exactly rather than assuming it matches the
negative-sign formula.  If it preserves the cells, eliminate only one of
the two rows and transport the result.
