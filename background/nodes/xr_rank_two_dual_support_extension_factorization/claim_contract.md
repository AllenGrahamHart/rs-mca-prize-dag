# Claim contract

- **claim id:** `xr_rank_two_dual_support_extension_factorization`
- **mathematical statement:** every active rank-two row has the unique common
  cofactor factorization `(SE3)--(SE6)` between its active-union and selected-
  block dual-GRS representations, with an exact dual-codeword converse
- **scope:** every row of every uniform rank-two split-pencil trade on every
  active-union shell
- **consumer:** `xr_highcore_collision_count`
- **status:** `PROVED`
- **proved dependency:** the uniform split-pencil and dual-GRS row
  normalization
- **new open content:** actual agreement-block realization of `T_i`, count and
  ownership of compatible extension families, first-core selection, and
  cross-core payment
- **falsifier:** an active row whose two normalized locator representations
  do not share one cofactor, violate `(SE3)`, or disagree under the converse
- **nonclaims:** no received-pair realization theorem and no extension count
- **replay:**
  `tools/ramguard tiny -- python3 background/nodes/xr_rank_two_dual_support_extension_factorization/verify.py`
- **upstream mapping:** witness-exhaustive first-match atlas / exact extension
  payment
