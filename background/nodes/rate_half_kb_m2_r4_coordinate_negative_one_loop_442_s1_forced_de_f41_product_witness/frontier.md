# Frontier

Do not seek a characteristic-independent product-only contradiction for
this cell.  Over the deployed rank-six quotients, test the same forced-mate
plus `E_0,E_1,E_2` system; any survivor must then be carried into an explicit
assignment of the seven outside source fibers and their `q` rows.
