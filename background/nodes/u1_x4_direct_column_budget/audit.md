# Currency audit

The four assembly inputs were checked against their stated objects:

- `f3_h1_singleton_injectivity` supplies the zero padding term;
- `f3_h2_stratum_theorem` counts width-2 equal-`e_1` records, exactly the
  width-2 minimal condition;
- `f3_h3_direct_floor_conditional_close` bounds `T_3`, defined as disjoint
  width-3 pairs with equal `(e_1,e_2)`;
- `f3_hge4_aggregate_budget` explicitly pins every `R_h` to order-`(h-1)`
  F-4 minimal records.

No required input states a general order-`t` to minimal-record coverage
theorem.  The exact census in `x4_general_star_minimal_trade_route_cut`
proves that no universal strip-free multiplicity-one bridge exists.  The old
headline therefore exceeded the proved implication; this re-pose narrows the
node to the common currency of its inputs without changing its amber status
or any open mathematical premise.
