# Claim contract

| field | content |
|---|---|
| status | PROVED |
| claim | The common base has rank six globally, every nonloop quotient image is nonzero, the `Z` branch is empty, and every common survivor has rank seven with the explicit unique full kernel `(KBPCU-4)`. |
| assumptions | The deployed-field global base-rank certificate and the exact common Vieta/pivot row shapes, with nonzero distinct common labels. |
| retained difficulty | Solving the nonzero pivot charts, leading support, all outside rows, and route deletion. |
| nonclaim | No common chart is asserted empty and no Prize node is closed. |
