# Audit

1. Nonvanishing of `(KBPCU-2)` uses both nonzero source labels and pairwise
   source-label distinctness.
2. The projection argument uses the exact coefficient order: all product
   rows vanish in the final two columns and the loop sum row does not.
3. Kernel uniqueness is asserted only after imposing rank at most seven;
   a generic matching-cell point may have common rank eight.
4. Leading support is automatic for an actual packet by the complete-fiber
   theorem, but is not inferred for every algebraic minor-system point.
5. The earlier `F_13/F_17/F_29` observation that all four images were
   nonzero is now an exact theorem and is not proof input.
