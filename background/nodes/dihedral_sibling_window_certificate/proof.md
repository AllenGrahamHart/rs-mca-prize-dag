# proof: dihedral_sibling_window_certificate — REFUTED (Pro T8, verified)

The P6 dihedral mechanism FAILS a degree audit. NO positive certificate; rate-1/2
is BRACKET-GRADE unless a genuinely new theorem supplies valid witnesses.

## The degree audit (VERIFIED)
"Scale d" is a PACKET SIZE, not a Chebyshev top-degree drop. A twin-pair packet
prod(X^2-y_i X+1) has X^{2l-1} coeff = -sum y_i != 0 (NO top gap, VERIFIED). The only
uniform top-gap identity is Dickson D_r(X+X^-1)=X^r+X^-r: X^r(D_r-c)=X^{2r}-cX^r+1,
so a size-d=2r packet gives drop r=d/2, NOT d. First band sigma0=2^33+1 needs
sigma0<drop but drop=d0/2=2^32+... => OVERFLOW by 2^32 (VERIFIED). Endpoint sibling
also overflows; the fixed singleton adds an X^{d-1} term, not half the drop.

## Fiber collapse (VERIFIED)
True Dickson fibers x^r+x^-r=c on mu_{2^41} have <= 2 gcd(r,2^41) points: first band
r0=2^33+2 => <=4 points, endpoint r*=sigma*+1 (odd) => <=2 points (<< sigma). Large
fibers occur only at large 2-power r = the OLD quotient-core regime. And the next old
scale M=2^34 is UNPRICED: log2 C(127,64)=123.17 < 128 (VERIFIED).

## Consequence
The advertised 4.05e10 packet entropy is real as a COUNT but is NOT a count of valid
degree-<k bad witnesses. rate-1/2 remains BRACKET-GRADE unless a new theorem supplies
many monic degree-k locators with the first sigma high coeffs cancelled, k roots in D,
disjoint sigma-tails, provably NOT old 2-power quotient-core.
