# Result

The coherent split-pencil obstruction no longer requires classifying an
arbitrarily large block family directly. Every counterexample contains a
bounded Maxwell core with at most `308,359,640` blocks in the three high-core
base rows, or `256,299,480` blocks in the first low-core defect.

Every proper subfamily is below the dimension threshold. The whole core has
an extra left-kernel relation beyond ordinary overdetermination, and each
block has at most `2,2,1` private coordinates on the three rates. The next
theorem can therefore target sparse-tight product-code circuits and their
quotient/tangent degenerations rather than a billion-member packing.

This is a finite circuit extraction, not a circuit classification or an XR
payment. The complete Modal verifier fleet passed `164/164` in
`ap-Drs4UGDXJ8cFnCcvF3qNLt`; all five integration gates passed in
`ap-dhz8ocKmyIReCCOaXZLebo`.
