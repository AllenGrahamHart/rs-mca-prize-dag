# Attack

The node is proved.  Regression attacks should search finite matroids with
loops, parallel classes, several intersecting rich rank-three flats, or
rank-four flats attaining `B+1`, and should mutate either coefficient in
`(FC)`.
