# Audit

1. The equality is stronger than the preceding cap `j<=1`: it uses both
   directions of the exact padded-root/deficit identification.
2. A correction center contributes no new gcd root because the source
   partition forces it to be a padded-heavy center already counted by
   `g_*`.
3. Multiplicities in `S_B^2` do not affect the gcd because `Lambda` is
   squarefree.
4. The conclusion is only `deg T_1<=1`; a constant quotient remains
   possible in the `d_A=1` profile.
5. No statement is made for nonreduced `S_B` or the two-simple arm.
