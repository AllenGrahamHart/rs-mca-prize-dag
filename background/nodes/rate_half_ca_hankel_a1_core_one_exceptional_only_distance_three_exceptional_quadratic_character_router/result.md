# Result

Every distance-three survivor must pass one Legendre-symbol test involving
only the exceptional locator and the stripped/omitted rows:

```text
-A(0)A(s)A(x_0) is a square.
```

The test precedes all matching and slope variables. The proof and replay use
no Modal resource. Passing it does not close or certify the distance-three
chart.
