# Claim contract

- **Proved:** full-prefix fibers inject into fixed-weight `p`-free power-sum
  syndrome fibers.
- **Proved:** the same linear map on the full cube has a fiber of size at least
  `2^129` on every official row.
- **Route cut:** a full-cube maximum bounded by `N^3` is false; fixed-weight
  information cannot be discarded.
- **Not claimed:** the fixed-slice maximum exceeds `N^3`; a counterexample to
  the prize target; failure of weight-resolved F2 or list-recovery methods.
