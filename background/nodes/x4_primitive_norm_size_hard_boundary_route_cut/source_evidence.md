# Source evidence

The proof consumes the official corridor upper bound and the complete local
active/zero norm product.  Its verifier checks every integer bound and a
finite truncation of the dyadic pair-sum identity; no experimental claim is
used.
