# Frontier

Do not add another uniform archimedean ceiling or generic residue-degree
lower bound to attack the whole wedge.  At `T=t_XR`, use one of:

1. a shape-specific common ideal or exact factor sieve;
2. a split-locator incidence theorem at the fixed base support;
3. an operational owner that removes or separately pays the hard boundary.

Any proposed norm continuation must print the extra information that exceeds
the complete factor package already included in `(RC-1)`.
