# Cell-3 `BC+` uncolored `DF` exclusion

- **status:** PROVED
- **field:** `F_2130706433`
- **scope:** missing `DF+/-` records in repeated-`BC` cell 3

All 120 cell-3 `BC+` systems represented by missing `DF+` are empty. Direct
replay at the complete exceptional base atlas yields 2,248 torus fibers:
1,560 common-guard boundaries, 240 inconsistent missing products, 240
zero-product boundaries, and 208 endpoint-bearing fibers. Every one of the
832 endpoint rows has residual gcd one. The exact `d -> -d` quotient closes
the 120 missing-`DF-` systems, for 240 formal systems in cell 3.
