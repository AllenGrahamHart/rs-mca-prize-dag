# Proof

The generic-rank node excludes every system away from its five case guards.
At each exact guard root, lift the torus equation and replay the original
common guard and cofactor kernel. Guard-boundary lifts are outside the open
locus; a vanishing missing coefficient always has nonzero constant; and
`q=0` violates the nonzero-coordinate guard.

For every remaining lift, exact `gcd(P,x^p-x)` certifies all roots of the
missing-endpoint quartic. Reconstructing the three residual paired-product
polynomials at all 832 endpoint roots gives gcd one. This excludes every one
of the 120 `DF+` representatives. The proved `d -> -d` involution supplies a
bijection to the 120 `DF-` systems. QED.
