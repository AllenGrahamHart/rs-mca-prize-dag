# Lineage

The antipodal-label classifier reduced the `(4,4,2)` two-loop skeleton to
three roots-of-unity `K` loci.  This node substitutes each locus into the
rank-three Mobius product matrix and eliminates the two normalized `J`
parameters exactly.
