# Frontier

Treat the six quadratic-linear rows as the complete common-`K` product
ledger.  First test the two q-weld signs exactly.  For each survivor,
reconstruct `B_0/B_2`, then compile the six `L^c` and one `eta` product
records from the source-facet completion.  Apply the paired source-label
involution before any full resultant.  Do not reopen generic `b,c` search.
