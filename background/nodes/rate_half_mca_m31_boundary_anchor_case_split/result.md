# Result

`PROVED`. A two-anchor synchronization case split absorbs the exact
Mersenne boundary layer at `e=98230`. The resulting full-lift bound is
`16487313`, below the `16777215` MCA budget by `289902`.

The first residual support is now `e=98231`; the same compiler gives
`17492173` there and does not pay it.
