# Dependency sub-DAG

```text
full-lift mean-centered global-line profile (PROVED)
top-third global-line payment (PROVED)
        |
        v
boundary-anchor case split (PROVED)
        |
        +--> global-core/rank/support-distance router (evidence)
        +--> rate-half band crossing location (evidence)
```

The child reuses the prefix cap only at its proved scope. The new content is
the exhaustive `|A|<=1` versus `|A|>=2` split and the mixed
`(s,s,s+1)` triple synchronization.
