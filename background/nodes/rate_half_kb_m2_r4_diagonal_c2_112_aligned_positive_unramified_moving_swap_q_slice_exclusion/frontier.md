# Frontier

Closed here: moving-moving `swap`, including both divisorial components,
all exceptional norm factors, and every finite off-common projection
intersection.

Still open in the same aligned-positive unramified six-cell packet:

- fixed-moving `same`, `swap`, and `mixed`;
- moving-moving `same` and `mixed`.

Those five cells already have generic determinant/conic reductions but still
need finite-specialization and off-common replay.
