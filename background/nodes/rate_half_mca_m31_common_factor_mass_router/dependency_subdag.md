# Dependency sub-DAG

```text
size-two capped bank --> 7583 distinct pair cores --------+
                                                       +--> >=126188 factor points
common interpolation gcd --> cofactor Bezout capture --+
```

The node converts existence of a common factor into near-total received-point
concentration.  Factor classification remains the next algebraic dependency.
