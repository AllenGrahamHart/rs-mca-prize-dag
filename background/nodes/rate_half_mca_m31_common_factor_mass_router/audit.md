# Audit

- Line 7,583 is forced from the charge after 7,582 removals; the threshold-one
  record after 7,583 is not used to force another line.
- `P` is the full gcd, so the divided cofactor family has gcd one.
- Clearing denominators uses a primitive polynomial representative; a pure
  `X` factor is a unit over `F(X)` and is excluded from `d`.
- A selected pair outside `P` must be a common zero of every cofactor, so the
  cofactor Bezout cap applies exactly to the off-factor population.
- Pairwise core intersections are at most five because at least one of the
  two degree-at-most-five polynomial differences is nonzero.
- The Cauchy lower bound uses total incidence at least `t*807`; its rational
  expression is increasing in both incidence and `t`.
- `126188` is a lower bound on factor points, not a common core shared by all
  captured pairs.
