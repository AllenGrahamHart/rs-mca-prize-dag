# Result

`PROVED`: every unsafe common-factor survivor at `e=130237` has a primitive
factor containing at least 4,982 selected polynomial-pair sections and the
received pair on at least 126,188 inside coordinates.

The support residual remains open, but only 4,049 inside coordinates can now
escape the factor relation.
