# Lineage

This terminal aggregate follows the `[12,13]` orbit pattern but depends on the separately refactored cell-5 active, endpoint, complete, and transport nodes. It contains no large proof ledger itself.
