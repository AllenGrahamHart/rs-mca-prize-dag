# Frontier

Do not revisit cells with `4e^2>N(e+d)`.  Attack the explicit residual wedge

```text
4e^2<=N(e+d),       e>=t_XR+d+1,       d>=1,
```

using quotient-scale primitivity and the `1024` projection codegree on its
`d=1,e<=N/4` edge.  A sufficient combined budget for this wedge plus `D_0`
is `(251/16)N^3-1`.
