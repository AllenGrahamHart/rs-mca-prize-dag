# E1 prize N=256 profile-(3,6) cofactor-16 twice-divided exclusion

- **status:** PROVED
- **closure:** exact product ledger, dual complete radius census, and certified norm intervals
- **scope:** prize-envelope `N=256`, profile `(3,6,S=18)`, twice-divided singleton supports
- **dependency:** energy-adaptive product windows

There is no profile-`(3,6,S=18)` prize collision with norm cofactor `m=16`
whose six-singleton binary support lies in one residue class modulo four.

After translation such a support is `4T` in `Z/128`, where the six-term
support polynomial of `T` in `Z/32` has exact multiplicity one at `X=1`.
A complete atlas contains 903 affine support orbits in this branch.

Independent forward and reverse complete radius engines find 205,513,652
product-live signed vectors. Certified 48-bit root intervals put 205,486,644
strictly below the allowable `16p` interval and 27,008 strictly above it; no
interval is unresolved.

This proves only the twice-divided m16 branch. The primitive multiplicity-four
and once-divided multiplicity-two branches remain open.
