# Result

The `u=v=0` rank-five branch now has an exact local form.  P-B is paid by
unique four-basis packing.  Any P-A counterexample contains a nonempty
subfamily whose minimum number of distinct collision lines through each
member is

```text
125 of 126,       123 of 126,       31 of 35
```

on the three RowC rows.  Individual line populations remain bounded by
`153`, `179`, and `320`.

This is a structural reduction, not a count of the resulting line-rich core.
The next P-A argument may therefore assume near-complete local line reuse in
the hardest base branch; a diffuse family with many unique kernel bases is
already below the allocation.

Focused Modal replay: `ap-RcA2utFuQPHtXSmXQpxbxF` (`57 MB`). The complete
fleet passed `152/152` in `ap-Bb1t2iwL5BiatZ3CB5NVqy`; all five integration
gates passed in `ap-OpUzoLttRAbbmwWKAO5T6K`.
