# Frontier

Eight affine positive charts remain, all in the moving-moving template:
the `xi=a` square-ell and mixed allocations, and all three allocations for
each of `xi=tau(a)` and the other reciprocal orbit. The projective `w=0`
boundary and negative factor loci also remain. This node gives no automatic
symmetry deletion beyond its printed scope.
