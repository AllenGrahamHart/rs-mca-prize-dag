# KoalaBear c2 (1,1,2) near positive moving-moving a-xi square exclusion

- **status:** PROVED
- **scope:** the deployed KoalaBear field `F_(2130706433^6)`; positive
  near-aligned source-line `(1,1,2)` packets; moving-moving internal template;
  representative `xi=a`; square allocation `c->1/2`, `d->1/d`
- **dependencies:**
  `rate_half_kb_m2_r4_diagonal_c2_112_source_line_internal_star_reconstruction`
  and
  `rate_half_kb_m2_r4_diagonal_c2_112_source_line_q_slice_resultant_gate`
- **consumer:** `rate_half_band_closure` (evidence)

Normalize `a=xi=2`, orient `(eta,ell)=(c,d)`, put `w=1/c`, and use
the moving-moving source edges `{2,b}` and `{2,1/b}`. Require the residual
over `c` to have the double root `1/2` and the residual over `d` to have the
double root `1/d`. After removing the finite-incidence square from the two
product equations, the four primitive conditions have degrees

```text
c product (4,8,6),  c sum (4,12,8),
d product (4,6,8),  d sum (4,10,9)                 (KBMMAS-1)
```

in `(b,c,d)`. They are reciprocal in `b`. With `s=b+1/b`, their exact trace
forms have degrees

```text
c product (2,8,6),  c sum (2,12,8),
d product (2,6,8),  d sum (2,10,9)                 (KBMMAS-2)
```

in `(s,c,d)`. Eliminating `s` within each q-root leaves three nonstandard
components on each side, of bidegrees

```text
c: (2,2), (2,2), (4,4);     d: (1,2), (3,2), (4,4).
```

The nine component pairs are exhaustive. Their complete factorization
modulo `p=2130706433`, after standard forbidden linear support is removed,
has exactly 15 distinct irreducible factors whose degrees divide six:

```text
6 linear, 5 quadratic, 3 cubic, 1 sextic.           (KBMMAS-3)
```

For each factor, the ideal generated by `(KBMMAS-2)` and that factor becomes
the unit ideal after saturation by the product of the collision,
inversion-fixed, reciprocal, `z=1`, and finite-incidence loci. Factors of
other irreducible degrees have no root in `F_(p^6)`. A direct/resultant
primary and a no-import fraction-free/subresultant audit reproduce the full
census and all 15 saturations. Hence this chart is empty over the deployed
field.

Together with the nine fixed-moving charts, 10 of the 18 affine positive
charts are closed. This node does not delete the other eight moving-moving
charts, the projective boundary, a negative locus, a packet, or the
rate-half target.

## Falsifier

An admissible deployed-field solution of `(KBMMAS-1)`, a failure of the
reciprocal lift `(KBMMAS-2)`, an omitted residue-degree-dividing-six factor,
or a nonforbidden point in either certificate path.
