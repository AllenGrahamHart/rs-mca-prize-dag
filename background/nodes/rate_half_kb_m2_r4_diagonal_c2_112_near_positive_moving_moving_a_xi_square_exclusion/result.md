# Result

**PROVED.** The deployed-field positive near-aligned moving-moving `xi=a`
square allocation `c->1/2,d->1/d` is empty. This closes affine positive chart
10 of 18 and supplies one evidence edge to `rate_half_band_closure`.
