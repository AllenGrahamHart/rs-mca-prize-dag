# Frontier

Enumerate or classify only squarefree four-edge divisors `C_H` in the
two-regular pole graph. For each candidate, solve the two partial-resultant
identities together with the universal `45 x 12` source-row gate. In the
coordinate and lifted-diagonal branches also impose their 8/7-dimensional
coefficient forms before any generic endpoint-biform search.
