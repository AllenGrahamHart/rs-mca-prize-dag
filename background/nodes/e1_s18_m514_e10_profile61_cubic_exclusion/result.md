# Result

`PROVED`. The cofactor-`514` magnitude profile `(E;n_1,n_2,n_3)=(10;6,1,0)`
has `M_3<=264`, and an exact cubic Hermite majorant puts its norm below the
prize field floor. The live profile-`(0,18)` ledger contracts from 13 rows to
12 without enumeration.
