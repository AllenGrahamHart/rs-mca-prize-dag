# Audit

- The layer sizes `14,2` reproduce the profile energy ten and `L1` mass eight.
- The relaxed layer cap is `268`; divisibility by six is applied only after
  the signed relation index is identified as an integer orbit sum.
- The cubic Hermite remainder has a nonpositive sign on all of `y>0`, not
  merely between the two contacts.
- The leading coefficient is certified positive before the upper bound
  `M_3<=264` is substituted.
- The verifier uses exact rational logarithm intervals and pins the strict
  margin's bit lengths and hashes.
- No autocorrelation target is promoted to a coefficient realization.
