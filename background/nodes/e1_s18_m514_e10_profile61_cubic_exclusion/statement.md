# E1 square-mass-18 cofactor-514 energy-ten profile-(6,1) cubic exclusion

- **status:** PROVED
- **closure:** signed-relation divisibility and cubic Hermite norm majorant
- **scope:** every conductor-256 square-mass-18 collision of cofactor 514

No cofactor-`514` collision on a prize-envelope row can have positive-half
autocorrelation magnitude profile

```text
(E;n_1,n_2,n_3)=(10;6,1,0).
```

For this profile the nested-layer relation bound gives `|M_3|<=268`. The
signed cubic relation index is divisible by six, so in fact `M_3<=264`. The
cubic Hermite interpolant to `log y` with double contacts at `33/2` and
`65/2` is a global majorant on `y>0`; exact logarithm intervals then put the
norm strictly below `514*p_min`.

Consequently the profile-`(0,18)` cofactor-`514` frontier contracts from
thirteen magnitude profiles to twelve.
