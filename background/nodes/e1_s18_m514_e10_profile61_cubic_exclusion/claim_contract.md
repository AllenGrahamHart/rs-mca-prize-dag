# Claim contract

## Consumed content

- The refined cofactor-`514` ledger retains profile `(10;6,1,0)`.
- The conductor-256 moment dictionary supplies the three raw moments in
  `(1)` of `proof.md`.
- The prize field floor supplies `p>=p_min`.

## New proved content

1. The nested-layer bound gives `|M_3|<=268` for profile `(6,1,0)`.
2. The signed relation index is divisible by six, hence `M_3<=264`.
3. A global cubic Hermite majorant puts the profile below `514*p_min`.
4. Twelve cofactor-`514` magnitude profiles remain.

## Guards

- No coefficient realization, fixed-root equation, or support enumeration is
  assumed.
- The theorem removes one magnitude profile; it does not bound occupied
  split ideals in any remaining profile.
- Positivity is used only through `y_u>0`, which holds for a nonzero collision
  norm.
