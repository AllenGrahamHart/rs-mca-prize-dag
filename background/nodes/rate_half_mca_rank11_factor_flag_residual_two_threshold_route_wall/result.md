# Result

Serial ordered-basis threshold refinement is exhausted inside the exact
`2 x 5` factor model. Improving the `18166` residual output requires a new
coupling theorem rather than an intermediate rich-flat band.
