# Dependency sub-DAG

```text
primitive coordinate map
          |
          v
generic guard-unit ledger --ev--> rate-half band closure
```

The parent supplies exact residue factors and coordinate maps.  This node
proves that no whole generic residue component is lost to the named guards.
