# Lineage

- The global quadratic quotient and compact kernel supply the exact source
  algebra and direct-lift maps.
- The `xi=3,pairing=0` theorem supplied the exhaustive factorization of the
  common first pair `paired(de,de)` into three rational `q` branches.
- Pairings 1 and 2 replace the reciprocal-square variable by `z=1/d`, making
  the missing relation quartic and the next paired relation quadratic.
- Reducing the quartic modulo that quadratic lowers the source elimination to
  one linear-remainder cut before taking the norm.
- Branch and polynomial-division exceptional strata are replayed directly
  rather than removed by an informal genericity assumption.
- The result remains background evidence toward `rate_half_band_closure`.
