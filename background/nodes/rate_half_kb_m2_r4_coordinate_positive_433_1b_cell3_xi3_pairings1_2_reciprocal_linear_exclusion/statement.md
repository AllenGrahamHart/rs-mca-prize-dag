# Statement

## Claim (KBP1B3-XI3P12-RL-1)

On the guarded positive deployed 433-1b role-cell-3 branch, every atlas case
with missing record `xi=3` (the `df` record), canonical matching index in
`{1,2}`, any source sign `epsilon in {+-1}^2`, and any target lane
`sigma in {+-1}^2` is empty.

For each source point, the first residual pair `paired(de,de)` has exactly
the three rational branch types `q=B_i/A_i`, `i in {0,1,2}`, subject to
explicit denominator replay. Pairing 1 uses twelve exact branch rows, each
checking all four target lanes. Pairing 2 uses twenty-four exact branch rows,
each fixing `sigma_c` and checking both `sigma_o` lanes. These 36 internal
branch rows pay 32 raw target-lane cases, not 96.

No other matching, missing role, complete cell-3 exclusion, rate-half band,
LIST, MCA, or Prize result is claimed.
