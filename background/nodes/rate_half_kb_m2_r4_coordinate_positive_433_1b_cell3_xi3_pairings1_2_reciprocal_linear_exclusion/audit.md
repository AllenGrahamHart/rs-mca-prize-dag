# Audit

- Pairing 1 has four source signs times three `q` branches: 12 internal rows,
  each covering all four target lanes.
- Pairing 2 has four source signs times three branches times two `sigma_c`
  values: 24 internal rows, each covering both `sigma_o` lanes.
- The identity `paired(q,q)=4(A_0q-B_0)(A_1q-B_1)^2(A_2q-B_2)` proves branch
  completeness; branches are not counted as separate raw target lanes.
- Every row is `COMPLETE`, has quartic and quadratic degrees `(4,2)`, a
  linear remainder, and matching direct six-basis and tower norms.
- Candidate parameters are the union of all norm and exceptional-guard roots.
- All 448 source points are explicitly listed and unique within each row.
- All 56 finite `z` candidates replay the branch, missing-product,
  missing-sum, and second-pair equations.
- All 128 final colored-pair evaluations are nonzero.
- Witness, target-boundary, free-branch, and unresolved ledgers are empty.
- The 36 internal rows pay 32 raw cases because each raw lane is tested
  through all three exhaustive `q` branches.
- Primary and independent verifiers pin the compiler and census hashes.
