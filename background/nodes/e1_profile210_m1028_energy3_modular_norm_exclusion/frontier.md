# Frontier

The cofactor-`1028` branch is now concentrated at one energy:

```text
E=4.
```

The finite-field screen has 8,385 compatible signed four-lag types. Their
diagnostic norms are all below the field floor. Reuse the exact modular
resultant engine from this node to replace the queued Bareiss fleet with a
single low-memory certificate. Cofactor `514` remains untouched.
