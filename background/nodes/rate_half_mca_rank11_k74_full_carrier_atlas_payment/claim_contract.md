# Claim contract

- Exact row only: `K'=74`, `q=64`, `m=67546`, `n=1048650`.
- Inherited marks, kernel capacity, record floor, and higher-support caps are
  unchanged from the proved parent chain.
- Exactly the 729 above-ceiling conservative tuples represented by the
  canonical digest are rerouted.
- The pairwise carrier router is exhaustive for every represented positive
  support-three offset.
- Eligible fixed unions use the joint support-4/support-5 charge on the
  original outside evaluation matroid.
- All counts and comparisons use exact integers; no sample is used.
- The tied reroute maximum is certified without claiming a unique maximizer.
- This node closes `K'=74`; it makes no claim about `K'=75`.
