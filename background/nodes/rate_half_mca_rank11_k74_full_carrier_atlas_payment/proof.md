# Proof

Instantiate the proved carrier-atlas and flat-coupled charge theorems at
`K'=74`, `q=64`, `m=67546`, and `n=1048650`. The exact position partition
removes 1,056 impossible support-two/support-three pairs and leaves 723
maximal ordinary vectors, plus every full-completion carrier lane.

The conservative stream has 8,869,588 leaves. Exactly 729 distinct defect
tuples lie strictly above the exact safe ceiling. Their lexicographically
ordered list has SHA-256

```text
e036791483f8f10702731c02172071ce6106a1587f1996439640f08122162cf4.
```

The largest conservative leaf already below the ceiling is

```text
s2=32/s3=31/s4=32/s5=30/c6F/c7F/c8F/c9F/offset1_plain,
```

with premium `(P74)` and margin `(M74)`.

Apply the exhaustive pairwise carrier atlas to each exceptional tuple and
intersect every resulting charge with the inherited same-support,
cross-support, and flat-coupled support-4/5 caps. The resulting 338,149
exact evaluations are all safe. Their maximum is

```text
32837046420997427924790365894433685328482435320,
```

below the ceiling by
`8311385718822403572953948792153250318018275877`. Two tuples tie at this
maximum, so no unique active representative is claimed.

The seven disjoint geometry lanes contain 124,851,888 exact evaluations.
Their largest premium is the one-step value

```text
36187398164184684606907419896584782044225756904,
```

which is also below `(P74)`. Hence the global premium is `(P74)`.

With inherited record floor `R`, rank-nine marks `G`, and kernel capacity
`Kern`, exact integer arithmetic gives

```text
Cap_full=floor((G+R P_74)/55),
Cap_total=Kern+Cap_full,
Demand=R C(67546,11)-C(1048650,11).
```

Substitution gives `(G74)`, so demand exceeds complete capacity. QED.
