# Upstream crosswalk - nu=2 prime-field Belyi normal form

For the upstream split-pencil census, the surviving `(2,1)` tangent case is
one of a discrete family of prime-field two-critical-value polynomials. The
multiplicity triple determines the tangent-root ratios explicitly; only one
scalar conjugacy and the regular split-fiber test remain.

This is suitable for an `(A)` / split-pencil theorem packet and a sharply
compressed contributor request. It is not an h=3 exclusion, exact SPI count,
or finite MCA payment.
