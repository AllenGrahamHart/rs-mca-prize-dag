# Proof

The pinned cell-4 pairing-3 elimination is imported by AST at its
`evaluate_case` boundary and supplied with the exact cell-5 tower and kernel.
All 32 sign/lane/xi rows terminate with no witness and no unresolved exit.
The exact totals are 304 target roots, 496 candidate roots, 704 source-route
points, and 96 compatible `(u,v)` lifts.

Independent Frobenius-gcd reconstruction recovers every base-field root from
the 53 printed profiles. Direct audit then rebuilds each root union, candidate
partition, quadratic-pair equation, missing-sum equation, `f` recovery, and
colored-pair cut. The final 144 `f` rows split into 96 nonzero colored cuts
and 48 target-boundary rows. The universal quotient computes the two claimed
label orbits exactly.
