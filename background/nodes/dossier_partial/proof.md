# Proof Of Assembly

The dossier's only positive mathematical claim is `pinned_row`, which is
`PROVED`. Its endpoint and integer arithmetic are replayed by the proved
compiler and the vendored Tier-1 Lean package. `s0_zero_open` supplies the
convention freeze, while `bridge_ledger` prevents silent object crossings.
The proved harness supplies the complete verifier replay.

The envelope paragraph prints the high-agreement theorem's range hypothesis
and explicitly excludes the capacity band. The non-claims section is backed by
a JSON manifest whose `open_truth_leaves` set is checked against the generated
critical orbit. Thus the artifact cannot silently imply that current red leaves
are closed.

`audit.py` reconstructs the field threshold and multiplicative order, checks
all required artifact statuses, compares the non-claims set exactly to the
critical orbit, and rejects full-prize language.

