# Result

The collapsed minimum-face branch is empty on the post-strip XR interface.
Its degree-`<a` interpolants force every selected codeword to extend from its
`a+1` facet to the complete `a+2` active union, so two selected errors would
share `k+2` zeros against the proved cap `k`.

Thus minimum-union rank-two trades consist only of the known regular Plucker
face syzygies. After their quotient, the rank-two frontier starts at active
union `a+3`, with no near-tangent exception. Larger-union rank-two trades,
trade rank at least three, and the aggregate slope payment remain open.
