# Audit - decorated shift-pair gcd descent and sharpness

## Checked axes

1. The gcd is monic, so reduced leading coefficients remain normalized.
2. The common gcd divides the residual before degree subtraction.
3. Effective depth increases by `c`, while cofactor degree decreases by `c`.
4. The uniqueness inequality becomes `e<=w+2c`.
5. Cross-coprimality places every domain gcd root in `G`.
6. The sharpness tails `A,B` are disjoint and split.
7. The quadratic gcd test is uniform in `t`.
8. Parameters `0,4,12` are removed for three different exact reasons.
9. Every retained witness has `deg U=5<n=6` and two degree-below-two
   codewords.
10. The family varies `U`; no fixed-word multiplicity claim is made.

## Route effect

The common-factor branch now recursively enters a deeper primitive
shift-pair problem. The only decoration regime not compressible to ordered
support geometry is the canonical high-cofactor residual `e'>w'`, and the
printed family proves that this distinction is necessary.
