# Upstream crosswalk - decorated gcd descent

The scalar branch remains upstream locator-Q shift-pair control. Positive
common cofactor degree is a tangent/common-factor refinement: its domain roots
are forced into the common agreement core, and division raises the effective
prefix depth. This is compatible with Grande Finale v3's rule that tangent and
common-factor cells precede primitive Q/list-interior cells.

The `F_13` family shows why upstream's arbitrary-word list-interior term must
retain a received-word or Toeplitz owner after common-factor removal whenever
`e'>w'`. A support-only extension of the locator-Q shift-pair ledger is false
at the first omitted degree.
