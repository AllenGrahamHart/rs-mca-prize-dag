# Result

The original fixed source and the compressed recurrence support are not
interchangeable. A heavy point outside `U_0` can still be a root of the
minimal recurrence, in both regular corank one and two. The nonreduced
collision must retain the resulting corank-one `[4]` profile, while the
old corank-one vanishing argument remains valid only under explicit
compressed-recurrence separation.
