# Claim contract

- **Refuted claim:** `x_* notin U_0` forces the compressed minimal
  recurrence to be nonzero at `x_*`.
- **Quantifier:** already false over `F_101` at `(e,p,d,n_0)=(5,7,13,19)`.
- **Certificate:** two all-nonzero moment-transfer vectors, exact Hankel
  ranks 12 and 11, disjoint compressed supports, and exact double-root
  kernel polynomials.
- **Output:** original-source separation must be an explicit hypothesis or
  proved from additional global structure.
- **Nonclaim:** no full global Lane-T counterexample.
- **Replay:** `tools/ramguard tiny -- python3
  background/nodes/rate_half_ca_hankel_a1_first_degree_core_one_quadratic_gap_four_truncated_source_minimal_recurrence_separation_fence/verify.py`.
