# Result

Both live `A=1` core ranges now begin at `e=183251937963`. The previous
core-free threshold `149933403787`, core-one threshold `169155635042`, and
the cubic-residual corner at that earlier degree are no longer live.
