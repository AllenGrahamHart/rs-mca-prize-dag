# Lineage - L1 full-pullback divisibility/Johnson closure

The general pullback descent replaced arbitrary polynomial fibers by a
quotient interleaved list but left its ordinary-list payment open. In the full
partition case, official dyadic divisibility moves every fully fiberwise
contributor off the exact source shell. A direct Johnson intersection count
then pays the positive-gate region without assuming that the quotient labels
form a smooth multiplicative domain.
