# Frontier

Reduce the unique common coefficient kernel modulo the four-polynomial lex
basis.  Express `A_2`, `A_0`, and `B_1` on the two `b` branches as rational
functions of `t`, then append the universal target-free
`DE+/DE-/BE` signed-family cut.  Use the two explicit `t=2` points as exact
specialization audits.  Do not enumerate 267 outside cases.
