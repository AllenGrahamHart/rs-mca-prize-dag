# Audit

The production derivation ran on Modal as `ap-BHdqdt3NzeCkN5oPfwNwAo` with
one 256 MB worker and a 60-second timeout.  Two earlier runs are explicitly
non-evidence: `ap-BllzyqghssXfECvbtq14LT` failed during remote import and
`ap-fs6qDeT2zhk2cNUY23gOpQ` exposed an out-of-range diagnostic trace before
writing a result packet.

The successful packet is complete and source-pinned.  The independent checker
uses a distinct signed-sum enumeration, a separate dynamic program, recursive
profile generation, and two hostile mutations.  It reproduces `L<=14`, all
nine profiles, the six parity survivors, both atlas counts, 154 templates, and
the exact 3,056,582,144-vector floor.

The fixed cubic-Hermite majorant is neither evaluated nor silently treated as
a filter.  The route-boundary theorem is used only to explain that choice.
