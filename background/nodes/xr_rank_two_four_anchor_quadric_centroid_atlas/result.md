# Result

The four-anchor branch of every uniform rank-two trade is now an exact
quadric-centroid configuration. Every non-anchor is a support-three/four point
on one explicit smooth split quadric in anchor coordinates, its fundamental
owner is read directly from those coordinates, and all points sum to
`(-1,-1,-1,-1)`.

Together with the three-anchor dual-`GRS_3` factorization, this completes the
rank-two coefficient atlas. Realized support embeddings, first-core selection,
and cross-core payment remain open.
