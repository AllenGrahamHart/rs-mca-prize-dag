# Result

All three fiber-two cycle quartic pencils now inherit the complete
doubled-order boundary compiler through the canonical span identity. The
generic and intermediate minimum-degree boundaries both have a linear
differential residual and the same primary gap at indices
`2^38,2^38+1,2^38+2`; the generic secondary gaps occur at
`2^37-1,2^37`.

The canonical outer quartic must match the cycle completion roots, not
arbitrary square-root lifts of the denominator. This is automatic in the
matched `c=0` stratum and is the precise new coupling problem in `c=1,2`.
The transfer narrows the theorem frontier but does not prove any boundary
empty.
