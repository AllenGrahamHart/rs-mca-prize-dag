# Dependency sub-DAG

```text
fiber-two cycle quartic-pencil router ------------------+
reverse residual / pure quartic / fourth-root gap ------+
secondary gap / two-window square / parity -------------+--> cycle boundary transfer [PROVED]
canonical-span criterion -------------------------------+       |
                                                               +--ev--> rate_half_list_adjacent_crossing [TARGET]
```

The transfer stops at the canonical certifier. For `c=1,2`, the later
one-parameter Mobius-ratio and norm gates use the wrong source matching and
require a new theorem before any contributor computation is complete.
