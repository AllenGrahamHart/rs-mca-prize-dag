# Claim contract

## Proved

1. Every outer zero is simple and `h` is unramified over it.
2. The 60 active roots are exactly `n=60/m` complete `m`-point fibers of
   `h`, all supported on the deployed field.
3. Every source point lies over an outer pole of order one or five.
4. The source divisor has exactly the eight printed fiber profiles.

## Imported proved inputs

- the endpoint identity `f=V_act/A^5` with reduced, disjoint active and
  source divisors;
- geometric decomposability and the exhaustive eight-row degree ladder from
  `rate_half_kb_q6_u2_primitive_subdegree4_route_cut`.

## Not proved

No model of `h` over the deployed field, full evaluation-domain quotient,
received-data descent, explaining-polynomial descent, slope projection,
same-record owner, charge, cap `68`, `u=2` close, or row close is asserted.
