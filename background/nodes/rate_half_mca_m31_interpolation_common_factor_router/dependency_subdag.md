# Dependency sub-DAG

```text
exact-layer line cores ----+
                           +--> 2705 polynomial-pair cores --+
capped-core line peeling --+                                 |
                                                             +--> common factor
weight-264 interpolation kernel --> coprime Bezout cap 2704 -+
```

The proved branch replaces the first-order size-two packing wall by one
algebraic exception.  The common-factor census remains evidence-level input
to the existing MCA routers; no critical status changes here.
