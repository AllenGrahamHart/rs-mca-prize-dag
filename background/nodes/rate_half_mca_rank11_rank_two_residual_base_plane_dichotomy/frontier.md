# Frontier

The two next exact adapters are:

1. iterate the common-base cancellation while recording the cumulative
   deleted locator and preserving the one first-match bucket; or
2. classify the heavy rank-four `2 x 2` product plane with its
   27,733-coordinate nonbase clone.

The second output is now close to the base-field-normalized split-pencil
census, but equality with that upstream object is not yet proved.
