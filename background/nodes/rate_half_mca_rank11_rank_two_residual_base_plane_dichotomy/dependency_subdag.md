# Dependency sub-DAG

```text
shortened rank-two 2 x 3 bucket
  -> residual base H, dim H=3, roots per class >=37733
  -> |Z(H)|>=10001: common-core shortening of complete mass
  -> |Z(H)|<=10000: every class is a plane
     -> disjoint nonbase clone classes, at most 39 planes
     -> one plane mass >=9965407986
chronology-safe R_3 cap
  -> heavy plane correction rank exactly 4
```
