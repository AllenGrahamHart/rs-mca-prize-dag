# Attack

1. Vendor the upstream 36-cell semantic registry and pin every literal
   source pair and root distribution to local notation.
2. Replay only the bounded exact verifiers for already claimed empty cells.
3. Import a cell as PROVED only when its named open and full-quotient scope
   match the local branch.
4. For remaining cells, use the upstream quadratic `U^2-VZ` split and retain
   the `V=0` rank-drop branch.
5. Audit aligned-negative and near-aligned normalization separately; do not
   infer their coverage from the positive atlas.
6. Promote the parent only after a machine-checked literal coverage table has
   no unclassified row.
