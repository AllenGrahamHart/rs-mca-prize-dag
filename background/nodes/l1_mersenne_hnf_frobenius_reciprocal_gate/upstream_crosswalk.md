# Upstream crosswalk

For the finite base-field-normalized split-pencil census, this replaces the
first order-zero degree-`n` test by a bounded-degree Frobenius reciprocal
system and an exact off-diagonal saturation. It is compatible with the
resultant/norm certificate style already used in the upstream finite-track
packets. It is a necessary gate, not a split-pencil census closure.
