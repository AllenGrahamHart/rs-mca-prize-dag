# Statement

The proved matching-7 exclusion and the exact parallel-`DE` involution exclude

```text
(0,7), (1,7), (2,7), (2,10)
```

in positive `433-1b` role cell 4. These are four labels in two quotient
orbits, hence

```text
4 labels * 4 source signs * 4 target lanes = 64 raw cases.
```

Together with the already-proved 27-label pairing-5/12 block, 31 of 105
labels in 17 quotient orbits are paid. The live ledger is therefore 74
labels in 43 quotient orbits.

The involution does **not** send `(0,7)` or `(1,7)` to matching 10. The
positive-`DE` labels `(0,10),(1,10)` remain open.
