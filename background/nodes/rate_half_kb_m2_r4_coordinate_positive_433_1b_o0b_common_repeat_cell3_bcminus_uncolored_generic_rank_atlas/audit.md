# Audit

The verifier pins launcher and result custody, reconstructs the exact
360-case label set, checks every full-rank 16-by-16 witness, validates every
guard digest, and checks the selected-pair and equation-degree histograms.
The hostile verifier mutates coverage, rank, pair choice, guard custody, and
completion.
