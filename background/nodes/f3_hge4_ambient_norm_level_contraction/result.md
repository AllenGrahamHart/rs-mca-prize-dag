# Result

Keeping the official ambient inequality `p>n^2` in the exact-ratio tower
strictly contracts every proper HGE4 level. The surviving width range at
`m=2^r|n=2^s` is now

```text
4<=h<2 ceil(mr/(8s)).
```

The closed-form top-level cutoff is unchanged. Across all official ambient
rows that cutoff deletes `55,050,457,488` additional `(n,m,h)` cells without
computation.
The stronger exact defect/swap predicate
`n^(2 floor(h/2)) >= (4h-floor((h-1)/2)-2)^(m/4)` removes additional
parity-sensitive widths, including some at the top level. The surviving orbit
aggregate remains open.
