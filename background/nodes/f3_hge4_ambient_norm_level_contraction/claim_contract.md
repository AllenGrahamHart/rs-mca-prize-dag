# Claim contract

- **claim id:** `f3_hge4_ambient_norm_level_contraction`
- **status:** PROVED
- **scope:** official `n=2^s`, exact level `m=2^r|n`, split prime
  `p>=n^2`, and primitive exact-level top shift pairs
- **cutoff:** `c_(n,m)=2 ceil(mr/(8s))`
- **stronger exact gate:**
  `n^(2 floor(h/2)) >= (4h-floor((h-1)/2)-2)^(m/4)` below one quarter
- **claim:** `E_h^prim(m,p)=0` for `max(4,c_(n,m))<=h<m/3`
- **currency:** exact-level primitive scaling orbits
- **consumer:** `f3_hge4_norm_gate_count`
- **nonclaim:** no estimate below the cutoff, no level aggregate, no ERT4
  payment, and no HGE4 closure
- **falsifier:** an official primitive exact-level pair in the printed
  ambient-deleted interval
