# Provenance

This node is the norm consequence of the new clean `A=3` weld. It mirrors
the role of unit-resultant gates in the repository's `A=1` core-one route,
but its `1+(m-1)` exceptional split and infinity-factor accounting are
derived independently from the clean endpoint identities.

No computation is load-bearing. The primary verifier computes resultants by
Sylvester determinants on a nontrivial exact control family; the independent
audit checks the closed product formulas and hostile perturbations.
