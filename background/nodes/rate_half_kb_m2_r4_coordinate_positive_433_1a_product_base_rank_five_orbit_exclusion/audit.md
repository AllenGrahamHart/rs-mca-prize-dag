# Audit

1. The duplicate-role action, not an unproved target symmetry, transports
   cell 4 to 7 and cell 5 to 8.
2. The cell-4 determinant uses `R+T!=0`, exactly the source guard
   `r^2+t^2!=0`.
3. The cell-5 elimination does not divide by `S`, `b`, or another residual.
4. `B` and `C` are nonzero because both `R` and `T` differ from `+/-1`.
5. The Modal dumps `ap-Q44CKyZwfaZG9ehYgPTwwU` and
   `ap-XXOEEJUIdsi3UbCP4l7uX4` print the four low-degree minors directly.
