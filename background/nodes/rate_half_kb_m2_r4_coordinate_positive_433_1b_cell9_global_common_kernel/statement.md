# Statement

## Claim `(KBP1B9-KERNEL-1)`

On the complete guarded product-rank-five common locus of deployed positive
`433-1b` role cell `9`, there is a primitive eight-coordinate polynomial
coefficient kernel in every source-sign row.  Its first six coordinates are
independent of both signs.  Its final two coordinates are independent of the
second sign and are exchanged when the first sign changes.

The kernel annihilates all five product rows, the `LA` sum row, and the `AB`
pivot sum row identically.  Its pairings with the `AC`, `BC+`, and `BC-` sum
rows reduce to zero modulo the exact global common ideal in all four sign
rows.

This is a common-kernel theorem only.  It excludes no outside record or cell
and proves no rate-half, K3, LIST, MCA, or Prize conclusion.
