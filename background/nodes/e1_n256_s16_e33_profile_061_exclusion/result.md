# Result

`PROVED`: profile `(0,6,1)` is impossible at `V=66`. The exact symmetric
target-fiber theorem gives the sharp abstract cap

```text
M_3<=1644<1732.
```

The V=66 residual is now `(5,7)`, `(1,8)`, or `(4,5,1)`.
