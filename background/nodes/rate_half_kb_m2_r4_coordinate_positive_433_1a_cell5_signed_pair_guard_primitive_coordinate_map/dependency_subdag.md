# Dependency sub-DAG

```text
primitive residue ledger
          |
          v
primitive coordinate map --ev--> rate-half band closure
```

The parent supplies the primitive presentation and residue fields.  This
node expresses the three squared source coordinates in that presentation.
