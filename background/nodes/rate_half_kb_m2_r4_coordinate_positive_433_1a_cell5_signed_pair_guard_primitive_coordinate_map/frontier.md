# Frontier

Use the exact maps in the five fields `K[s]/(phi_j)`:

1. use the now-PROVED generic guard-unit ledger: no residue factor vanishes
   identically on any of its 30 named guards;
2. compute guard resultants/norms only when the finite exceptional-`t`
   locus is needed;
3. keep source-square character, unsquared signs, and source-slot
   distinctness separate from squared generic nonvanishing;
4. append the residual colored `BE` cubic and unsquared sum only on
   surviving components;
5. return a unit certificate or an exact surviving residue factor.

Do not recompute the pair quotient, stable basis, primitive polynomial,
factorization, coordinate maps, or generic guard-unit ledger.
