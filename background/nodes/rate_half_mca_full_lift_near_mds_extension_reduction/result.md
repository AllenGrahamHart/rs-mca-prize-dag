# Result

`PROVED`: the full-lift branch is a full-affine-rank sparse-list problem in
the codimension-one extension `W=C+span{r_1}`.  Its generalized weights are
MDS-sharp from rank two onward; only `d_1(W)=e` is defective.

The generic corrected affine-list/occupancy ceiling still exceeds both
budgets at the MDS endpoint.  The remaining target is therefore a
row-sharp theorem using additional RS-extension structure, not another
replay of the same weight hierarchy.
