# Audit

1. `U_0` has `rho+j-1` points because the fixed core is removed once.
2. Every point of `U_0` is light; padded heavy rows are absent from actual
   supports and do not enter `(3)`.
3. For an off-line slope, the intersection bound is `j-r_delta-2` on
   `U_0`: one further unit belongs to the fixed core.
4. The line deficit cancels from `(8)`, but the packet-wide deficit `e-6`
   remains.
5. Replacing integer `h` by the real upper bound `u/j` weakens the
   contradiction and is therefore valid.
6. Concavity means the minimum on `(11)` is at an endpoint; no large scan
   over the official interval is used.
7. The final expansion count uses full locators only in the implication
   `full union<=2rho => actual union<=2rho`.
