# Result

`PROVED`. Every pair in either core-one quadratic `u=4` packet has actual
support union at least `3rho/2-1`; on the official row the floor is
`824633720831`. Every endpoint codeword line contains at most three
supported centers, and every pair has at least `rho+1` expanding thirds.
