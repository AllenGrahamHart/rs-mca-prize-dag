# Source evidence

This file records evidence for the optional full-coordinate route. The proved
two-involution route does not depend on it.

## Primary published evidence

Daniel J. Bernstein, *S-unit attacks*, invited plenary talk, SIAM Conference
on Applied Algebraic Geometry, 20 August 2021:

```text
https://cr.yp.to/talks/2021.08.20/
slides: https://cr.yp.to/talks/2021.08.20/slides-djb-20210820-sunitattacks-4x3.pdf
sha256: e0d441b6207999a110a921a4792d78dee1bfb1d9fa6cbb675c78fc370905a40e
```

PDF page 72, displayed slide 45, states that ideal classes for
`Z[x]/(x^64+1)` are indexed by `Z/359057`, with the prime denoted
`P_(257;1)` assigned class one, and that the automorphisms indexed by `-1`
and `3` act by multiplication by `-1` and `29301`. PDF page 65, displayed
slide 39, defines the prime-label/Galois convention.

## Class-number corroboration

Daniel J. Bernstein and Tanja Lange, *Non-randomness of S-unit lattices*,
2021:

```text
https://cr.yp.to/papers/spherical-20211023.pdf
sha256: 162c0d392e77b07e61271e41d7362e4b44c5791c0e2145046e7d4c0963fa45ee
```

The paper records the factor `359057` for `Q[x]/(x^64+1)` and surveys the
exact class-number calculation. It does not print the prime class coordinate
or the multiplier `29301`; those load-bearing facts currently come only from
the talk and are not treated as independently replayed coordinates.

Appendix C.1 records Weber's theorem that the class number `h_m` is odd for
power-of-two conductor, and explicitly notes that both its minus and plus
factors are odd. This published theorem is used by the two-involution
proved two-involution proof. Appendix C.2 separately surveys unconditional real
class-number-one results through conductor 256; the DAG imports the stronger
Miller theorem through `e1_conductor256_full_unit_circular_basis`.

## Tiny derived arithmetic

Exact integer arithmetic gives

```text
ord_359057(29301) = 32,
#{ +/-29301^j mod 359057 : 0<=j<32 } = 64.
```

Also `(Z/128Z)^x` is generated by `-1` and `3`, with `3` of order 32.
Consequently the published coordinates imply the class orbit immediately.
This last implication is replayed by the consumer's verifier; it does
not certify the published coordinates themselves.

The same coordinates predict the new minimum certificate. Since

```text
3^16=65 mod 128,
29301^16=211209 mod 359057,
-211209=147848 mod 359057,
1+211209=211210,
1+147848=147849,
```

the classes of `q_1 q_65` and `q_1 q_63` are predicted to be nonzero and to
lie respectively in the 17-primary and 21121-primary components. The first
prediction is now proved independently by the Harbater/Hilbert-class-field
node. The second historically motivated the fixed-field test, which is now
proved independently by the residue obstruction.
