# Frontier

Closed. The 64 primes above 257 have pairwise distinct ideal classes. The
downstream profile-(0,18) class descent and exact payment are also proved;
the live E1 frontier advances to profile `(4,4,S=20)` with allowance seven
complete shift/sign orbits.
