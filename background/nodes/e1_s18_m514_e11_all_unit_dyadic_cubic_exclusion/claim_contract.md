# Claim contract

## Consumed content

- The parity/trace router supplies exact local parity multiplicity two.
- The conductor-256 moment dictionary supplies the three moments in `(3)` of
  `proof.md`.
- The prize field floor supplies `p>=p_min`.

## New proved content

1. The dyadic relation recursion `(1)` is valid for every symmetric support
   avoiding zero and the involution.
2. Exact local parity specializes it to `M_3<=384` at the all-unit
   energy-eleven profile.
3. A global cubic Hermite majorant puts that profile below `514*p_min`.
4. Ten cofactor-`514` magnitude profiles remain, all at energies 5 through 10.

## Guards

- `U_k(s)` is a proved upper bound, not asserted to be an attained maximum.
- No lag-set enumeration, coefficient realization, or fixed-root equation is
  used.
- The theorem removes one magnitude profile and does not bound occupied ideals
  in the ten survivors.
