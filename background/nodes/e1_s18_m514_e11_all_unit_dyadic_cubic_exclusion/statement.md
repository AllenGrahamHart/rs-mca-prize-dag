# E1 square-mass-18 cofactor-514 energy-eleven all-unit exclusion

- **status:** PROVED
- **closure:** dyadic relation recursion and cubic Hermite norm majorant
- **scope:** every conductor-256 square-mass-18 collision of cofactor 514

No cofactor-`514` collision on a prize-envelope row can have positive-half
autocorrelation magnitude profile

```text
(E;n_1,n_2,n_3)=(11;11,0,0).
```

Recursing the even part of the signed relation support through the
`2`-power filtration, while retaining the exact local parity condition, gives
`M_3<=384`. The cubic Hermite interpolant to `log y` with double contacts at
`17` and `37` is a global majorant and places the norm below `514*p_min`.

Consequently the profile-`(0,18)` cofactor-`514` frontier contracts from
eleven magnitude profiles to ten, all at energies 5 through 10.
