# Lineage

The J-zero role/`P_4` compiler reduces one official role to an eight-filter
univariate endpoint and reconstructs `d` from a role root. The degree-eight
router places all reconstructed coefficients in one explicit finite field.

This node returns to the original cubic factor model. It reconstructs the
normalized color polynomial and proves that a pointwise `p+1` congruence,
together with the norm of `d`, already implies the full outer cyclotomic
divisibility. This replaces several vague downstream lift labels by one
finite replay contract while preserving the independent inner-lift gap.
