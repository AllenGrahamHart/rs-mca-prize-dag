# Claim contract

| field | value |
|---|---|
| claim | The official `nu=0`, `b!=0`, `deg H=3` endpoint is empty. |
| status | PROVED |
| exact input | The Euler quotient identity and the two-or-three-root tangent-radical passport. |
| exact output | At a repeated tangent root, `ord(H-kappa)=ord(T)`; hence `p<=9`. |
| excluded scope | `b=0`, positive valuation, `h=2`, `m=8,16`, and wider exchanges. |
| consumer | `l1_mixed_petal_amplification` |
