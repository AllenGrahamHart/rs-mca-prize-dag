# Lineage

This node closes the cubic tangent passport left by
`l1_m4_h3_nu0_nonzero_b_tangent_exclusion`. It uses the exact Euler quotient
identity rather than a coefficient search: the rational tangent map is
etale at `y_0`, so large multiplicity cannot be hidden inside a cubic
eliminant.
