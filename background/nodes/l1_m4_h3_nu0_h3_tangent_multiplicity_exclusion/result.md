# Result

The `nu=0`, `b!=0`, cubic-eliminant endpoint is empty on every official
`m=4` row. A repeated tangent root of multiplicity `e` forces the cubic
`H-kappa` to have the same local order, while the tangent fiber has only two
or three distinct roots. This would require `p<=9`.
