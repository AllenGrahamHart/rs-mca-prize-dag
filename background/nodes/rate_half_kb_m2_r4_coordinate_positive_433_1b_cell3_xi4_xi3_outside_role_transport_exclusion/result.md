# Result

`PROVED`: the signed outside-role involution
`(d,e,f) -> (sigma_o e,sigma_o d,f)` transports all 240 guarded principal
cell-3 cases with missing `sigma_o ef` record `xi=4` bijectively to the
already excluded `xi=3` slice.  The transport fixes the source role cell,
target lane, target guards, compact record order, and every matching index.
