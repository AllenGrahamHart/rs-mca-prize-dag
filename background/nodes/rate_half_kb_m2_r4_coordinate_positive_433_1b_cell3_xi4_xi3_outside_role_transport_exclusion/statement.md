# Statement

## Claim `(KBP1B3-XI4-TRANSPORT-1)`

On the guarded positive deployed `433-1b -> O0a` role-cell-3 principal
branch, every atlas case with missing record `xi=4` (the
`sigma_o ef` record), any canonical matching, any source sign, and any
target lane is empty.

For a fixed outside-cycle sign `sigma_o`, set

```text
D = sigma_o e,       E = sigma_o d,       F = f.  (KBP1B3-XI4-T-1)
```

This signed outside-role involution maps the complete `xi=4` product and
squared-sum system bijectively to the already excluded `xi=3` system, with
the same common source role cell, source signs, target lane, compact residual
record order, canonical matching index, and target guard divisor.  Thus the
six proved `xi=3` matching blocks transport to all

```text
4 source signs * 4 target lanes * 15 matchings = 240
```

raw `xi=4` cases.

This is not the rejected `B,C` exchange: that common-target exchange sends
source role cell 3 to cell 6.  The map `(KBP1B3-XI4-T-1)` acts only on the
outside roles `D,E` and fixes source role cell 3.

The claim does not by itself exclude another missing record, the product-rank
drop branch, another role cell, K3, LIST, MCA, or either Prize problem.

## Falsifier

A target guard, product record, squared-sum record, target-sign lane, compact
matching index, or source role cell not preserved by `(KBP1B3-XI4-T-1)`, or
an `xi=4` packet whose transported target is not an `xi=3` packet.
