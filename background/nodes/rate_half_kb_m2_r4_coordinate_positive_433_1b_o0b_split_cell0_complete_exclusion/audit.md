# Audit

The primary verifier pins the launcher, component and representative inputs,
full result, checker, outside core, and independent audit. It validates the
complete ordered 708-row result and the DAG dependencies.

The independently written audit parses the launcher, checks every
load-bearing construction token, reconstructs the six signed O0b lane rows,
and parses all 708 Singular transcripts. It requires 708 distinct program
hashes, contiguous saturation stages, stable unit tails, no stderr, and the
exact `30:354,31:354` guard profile. Three hostile result mutations are
rejected.
