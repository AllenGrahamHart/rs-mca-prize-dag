# Proof

The common-classification parent excludes both mixed source-sign rows in all
six split lanes, paying 1,260 raw labels. For equal signs, it proves that every
guarded common point lies on at least one of `A_s` or `B_s`; overlap between
the two is harmless because the outside ledger is a covering superset.

For each component/sign/lane state, every possible outside placement chooses
the outside record at the missing singleton mate and one of the fifteen
perfect matchings of the residual six records. This gives

```text
2 components * 2 signs * 6 lanes * 7 records * 15 matchings = 2,520 cases.
```

The component quotient parent proves covariance of the component relations,
complete source/target systems, labels, and guards. Its commuting actions
reduce those cases to exactly 708 representatives without deleting a case.

For each representative, the outside compiler imposes the component
relation, missing-product equation, three paired-product resultants, and the
missing squared-sum equation. It clears denominators and sequentially
saturates by all unique nonconstant source, denominator, leading-support, and
target-distinctness factors. The complete Modal certificate has 708/708
`COMPLETE` rows and every final basis is `1`, dimension `-1`, basis size one.

The independent audit pins all inputs, reconstructs the exact O0b signed-edge
table, checks the ordered representative cover, verifies 708 distinct program
hashes and every contiguous saturation transcript, and finds the exact guard
profile `30:354,31:354`. Hence no equal-sign outside point exists. Together
with the mixed-sign exclusion, all 2,520 cell-`0` labels are empty. QED.
