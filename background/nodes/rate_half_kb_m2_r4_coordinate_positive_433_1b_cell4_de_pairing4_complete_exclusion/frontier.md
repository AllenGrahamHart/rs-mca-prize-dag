# Frontier

The theorem pays `(xi,pairing)=(0,4),(1,4),(2,4)`. The parallel-`DE`
matching involution separately transports `(2,4)` to `(2,9)`, but it does
not pay `(0,9)` or `(1,9)`. Those positive-`DE` matching-9 cases are the
next exact target in this orbit.
