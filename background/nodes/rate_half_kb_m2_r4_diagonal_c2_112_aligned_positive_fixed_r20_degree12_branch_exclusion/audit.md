# Audit

The composition uses a field-point dichotomy only. `s=0` versus `s!=0`,
then `L6=0` versus `L6!=0`, then `K10=0` versus `K10!=0` are pairwise
disjoint and exhaustive. Every leaf supplier is independently PROVED.
