# Result

Future order-one elimination should generate the first-three necessary
equations from the original degree-`h` polynomial `P`, not from the divided
degree-`h-1` polynomial `L` and not from `Qtilde`. The automatic root cancels
exactly from every trace comparison.

This lowers representation and denominator risk. It closes no order-one
chamber or critical node by itself.
