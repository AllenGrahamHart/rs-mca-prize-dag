# Repeated-BC cell-11 outside-label sign transport

- **status:** PROVED
- **scope:** all cell-11 common rows, both outside-cycle signs, and all 105
  missing-record/matching labels

The target involution `d -> -d` fixes `BE`, `CF`, and `EF`, exchanges
`DE+ <-> DE-` and `DF+ <-> DF-`, and transports every perfect matching of
the six residual records bijectively. It fixes the cell-11 common packet.
Thus complete exclusions for missing `DE+` and `DF+` transport to missing
`DE-` and `DF-` respectively.

## Falsifier

A changed common record, product or squared-sum mismatch, non-involutive
matching transport, or omitted label.
