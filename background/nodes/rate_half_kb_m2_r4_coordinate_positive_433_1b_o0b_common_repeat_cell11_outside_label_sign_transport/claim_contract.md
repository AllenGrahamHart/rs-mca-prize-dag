# Claim contract

The node proves an exact label-level bijection, not an emptiness result by
itself. It applies to every cell-11 row because the common packet is
independent of `d`. It transports all residual matchings and both outside-
cycle signs. It does not transport cell 11 to cell 14 or pay K3 slopes.
