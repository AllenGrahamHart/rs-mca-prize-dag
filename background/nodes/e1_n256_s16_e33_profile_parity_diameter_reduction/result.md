# Result

`PROVED`: the `V=66` endpoint has `L<=19`, exact cubic threshold `M_3=1732`,
and only four surviving magnitude profiles:

```text
(5,7), (1,8), (4,5,1), (0,6,1).
```

Odd energy forces exactly one light-light diameter and
`D_64 in {1,5,9,17,21}`. The two five-odd profiles have diameter-Sidon light
support. The four residual profiles remain open.
