# Result

Every allowed `Q_(a,b)` is geometrically irreducible and rational. Its
singularities are three nodes for `b!=a`, or one node and one tacnode for
`b=a`. The coefficient-curve gate deletes no parameter; the six-pole and
complete-source locator equations are the remaining residual frontier.
