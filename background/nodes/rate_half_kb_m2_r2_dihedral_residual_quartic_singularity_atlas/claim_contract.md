# Claim contract

- **Claim ID:** `rate_half_kb_m2_r2_dihedral_residual_quartic_singularity_atlas`
- **Input:** the residual normal form `Q_(a,b)` with `a=-1` or `1` and
  `b notin {-2,2}`.
- **Output:** geometric irreducibility and genus zero for every allowed
  parameter, with three nodes for `b!=a` and one node plus one tacnode for
  `b=a`.
- **Dependencies used:** the exact one-parameter coefficient formulas and
  standard plane-curve genus and quadratic-extension criteria.
- **Not claimed:** realization of any allowed parameter by the six poles or
  source locators, profile deletion, an owner, a payment, or a row close.
