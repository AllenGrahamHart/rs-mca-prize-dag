# Audit

## Construction semantics

The carrier remains the deployed smooth subgroup in the base field, while
received values and slopes may use the deployed sextic line field. The
polynomials `a_e` and `b_e` are valid degree-below-`K` codewords over that
field. No large vector or degree-million polynomial is materialized: the
proof uses only root counts, dimensions, and exact cardinalities.

## Exact-support and first-match scope

Every selected record has complete agreement set `H_e union {x}`, not merely
a chosen subset. The pair-noncontainment and post-near arguments use this
exactness. The construction supplies no Q, BC, S, A, or E chronology label;
that omission is the point of the fence. It falsifies owner uniqueness from
the local pair-core data, not an owner theorem with an additional executable
classification premise.

## Rank scope

The official family has explanation affine rank exactly `10` and error rank
exactly `11`, so it is not merely the small constant-code star already
shipped with `#1168`. The independent verifier checks the dimension ledger
symbolically and replays a complete `GF(29)` analogue with four pair lines,
exact supports, interpolation-based pair noncontainment, margin one, and
error/explanation ranks `4/3`.

## Budget scope

The total is `2865900`, many orders of magnitude below `B_*`. The node is a
route fence and adversarial control. It cannot be cited as an MCA safety
counterexample or as evidence that the full abstract pair/core ceiling is
realizable.
