# Provenance

Derived by combining the external-heavy-row fact used in the separated
quotient with the exact three-center deficit ledger. It converts the
endpoint ordering `U=S_alpha union S_beta` into a restriction on which
center factors can occur in the correction quadratic.

No external theorem or computation is used.
