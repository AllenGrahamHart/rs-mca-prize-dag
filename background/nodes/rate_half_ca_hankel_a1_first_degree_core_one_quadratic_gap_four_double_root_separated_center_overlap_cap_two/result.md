# Result

On the separated double-root locus,

```text
deg gcd(S_B,Lambda)<=1,
deg gcd(g_*,Lambda)<=1,
j=deg gcd(Lambda,g_*S_B^2)<=2.
```

The four-scalar `j=3` heavy-row case is impossible. Every survivor has one
nonzero correction-coprime form `T_j` with at most three coefficients.
