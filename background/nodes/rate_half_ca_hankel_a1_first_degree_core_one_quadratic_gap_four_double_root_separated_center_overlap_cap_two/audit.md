# Audit

1. `x_*` is outside `U`, not merely outside the selected classified row set:
   it is a padding root at every root of nonempty `g_*`, and padding lies
   outside `U_0`; also `x_*!=s_0`.
2. If `alpha` or `beta` is a correction root, `Q(tau,x_*)=0` makes `x_*`
   a locator root. Since it is outside the actual endpoint support, it must
   be padded and hence a root of `g_*`.
3. This contradicts the separated condition rather than a numerical count.
4. The remaining center `theta` may be a correction root; it is not silently
   excluded.
5. Center roots of `g_*` are counted by the exact indicators `r_gamma`, whose
   total is at most one.
6. `Lambda` is squarefree and `g_*` is coprime to `S_B`, so the two overlap
   caps add without multiplicity ambiguity.
7. The result removes only `j=3`; it does not infer rank from the remaining
   three scalar unknowns.
