# Result

The packet-local qualifier can be removed from the rational dense-anchor
frontier. A one-swap star either finds a high-complexity packet or forces all
5524 anchor exception locators into one fixed coprime degree-`1..11` pencil.
This makes every valid fixed-pencil census directly usable on the dense
anchor, without multiplying local charges across unrelated packet choices.
