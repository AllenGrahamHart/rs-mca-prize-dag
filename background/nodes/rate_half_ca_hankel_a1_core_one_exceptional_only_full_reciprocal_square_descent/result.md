# Result

The corrected exceptional-only square now has a triangular reciprocal
normal form. Four blocks `F,U,W_vee,S` determine all other complement forms
through three exact quotient checks and one linear formula; independent
searches for `L,G,K_vee,V_vee` are obsolete.
