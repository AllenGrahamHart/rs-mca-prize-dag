# Audit

- Reversed `WB_1-1=QK_1` at `r+n_X`; the constant one therefore becomes
  exactly `Y^(r+n_X)`.
- Used the possibly zero infinity coefficient `v_inf` only to prove
  cancellation in `j_infW_vee+EK_vee`; no nonvanishing was introduced.
- Cancelled one `Y` after defining `S`, producing the exponent
  `N=r+n_X-1` shared by the reversed first complement.
- Kept all three coefficientwise divisions, one by `P_cl` and two by `E`,
  as explicit reconstruction gates.
- The second complement is removed only because its algebraic derivation
  from the other corrected identities is already proved; Hankel and
  splitting obligations remain.
