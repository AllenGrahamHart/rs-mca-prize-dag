# Statement

The proved matching-8 exclusion and the exact parallel-`DE` involution exclude

```text
(0,8), (1,8), (2,8), (2,13)
```

in positive `433-1b` role cell 4. These are four labels in two quotient
orbits, hence

```text
4 labels * 4 source signs * 4 target lanes = 64 raw cases.
```

Together with the already-proved 33-label pairing-7/10 block, 37 of 105
labels in 20 quotient orbits are paid. The live ledger is therefore 68
labels in 40 quotient orbits.

The involution does **not** send `(0,8)` or `(1,8)` to matching 13. The
positive-`DE` labels `(0,13),(1,13)` remain open.
