# Proof

The generic-rank node excludes every system away from its exact case guards.
The guard-lift atlas supplies every guarded `(q,y,r)` common point above the
deployed-field guard roots. At these points a vanishing missing coefficient
always has nonzero constant, and `q=0` violates the nonzero-coordinate guard.

For every remaining lift, exact `gcd(P,x^p-x)` certifies all roots of the
missing-endpoint quartic. Reconstructing the three residual paired-product
polynomials at all 1,408 endpoint roots gives gcd one. This excludes every one
of the 120 `DF+` representatives. The proved `d -> -d` involution supplies a
bijection to the 120 `DF-` systems. QED.
