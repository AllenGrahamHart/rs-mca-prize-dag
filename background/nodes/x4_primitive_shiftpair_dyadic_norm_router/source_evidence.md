# Source evidence

The proof is local.  It consumes the exact prefix/difference object from
`x4_general_shiftpair_difference_degree_partition`, the maximal-scale owner
from `upstream_sp_coefficient_scale_quotient_sieve`, and the arbitrary
integer-vector cyclotomic norm ceiling from `dli_norm_gate_energy_ceiling`.
The finite verifier covers both a split-prime field and a generated extension
field to audit the orbit accounting.
