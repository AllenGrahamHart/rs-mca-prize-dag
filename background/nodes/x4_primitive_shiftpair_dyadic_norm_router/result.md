# Result

Every coefficient-primitive X4 shift pair now enters the DLI arithmetic
ledger through a canonical nonzero root fold.  Additional dyadic folds add
residue-degree-weighted norm divisors whenever they are nonzero.  The next
step is to count primitive signed vectors satisfying these gates together
with the exact locator and first-owner constraints.
