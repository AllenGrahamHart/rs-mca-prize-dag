# Upstream crosswalk - nu=0 h=0 projective quarter certificate

This is a base-field-normalized finite split-pencil preprocessing result for
the `(A)` / exact SPI lane. It replaces an unrestricted constant-eliminant
outer-coefficient search by one packet on three official characteristics and
two packets on the largest.

An upstream packet should vendor the exact checker and preserve the
nonclaim: the projective classification neither constructs nor excludes an
inner polynomial. Any larger contributor run must start from the printed
packets and request a lift/exclusion certificate, not repeat the outer
parameter search.
