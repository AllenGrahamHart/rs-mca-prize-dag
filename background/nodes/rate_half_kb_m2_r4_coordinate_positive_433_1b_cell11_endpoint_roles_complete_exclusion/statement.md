# Statement

Let `p=2130706433`. On the deployed guarded common locus of positive
`433-1b` cell `11`, the endpoint roles `xi=5` and `xi=6` are
impossible for every one of the 15 residual pairings.

Equivalently, all 30 raw endpoint labels

```text
{5,6} x {0,...,14}
```

are empty.
