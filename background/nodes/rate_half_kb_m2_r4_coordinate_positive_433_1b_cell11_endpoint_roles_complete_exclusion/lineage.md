# Lineage

This node adapts the cell-12 endpoint compatibility architecture to cell
`11`'s genus-three tower. Cell 11 has one base-field eliminant root per row,
but complete tower replay kills every lift at the source equation, so the
downstream 2,400-system residual architecture is still unnecessary.
