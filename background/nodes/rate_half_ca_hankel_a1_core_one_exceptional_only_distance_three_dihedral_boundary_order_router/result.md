# Result

The two surviving dihedral matchings are no longer unrestricted. Both require
`gcd(e,p-1)` to be `e/3` or `e`. The apparent reciprocal exception has exact
normal form `c=sx_0`, `T={u,t,c/t}`, `u^2=c`, but its triple boundary ratio
is `(u/t)^3`; the required power equality would force `u=t`. No rank-two
search is warranted outside the two high-order field strata.
