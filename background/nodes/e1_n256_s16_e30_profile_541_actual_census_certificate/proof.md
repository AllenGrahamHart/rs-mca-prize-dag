# Proof

For every exceptional light orbit, choose three heavy positions among the
remaining 124 coordinates and normalize one global sign.  The remaining
relative signs give 64 cases, producing the displayed exhaustive count.

The production engine updates folded signed differences.  The audit instead
multiplies the sparse vector in the negacyclic quotient.  The verifier checks
their equality on all 321 template rows and replays every one of the 440
exceptional witnesses directly.  It then recomputes each conductor and finds
exactly 86 primitive witnesses.  QED.

