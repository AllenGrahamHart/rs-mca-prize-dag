# Audit

Modal app `ap-YxeY4JsxalEEMItNl8DQOC` ran one 256 MB worker with a 60-second
timeout. The source-pinned result is complete. The independent checker uses a
distinct signed-sum enumeration, dynamic program, recursive profile generator,
matching enumeration, and hostile parity mutation. It reproduces `L<=12`, all
seven profiles, the six survivors, both relevant atlases, the diameter
ledgers, and the exact 26,219,123,456-vector floor.

The majorant is absent by contract; no failed inequality is treated as an
exclusion.
