# Audit

- The seven cofactor classes are imported only from hash-pinned proved
  statements and are explicit `req` dependencies.
- Each exhaustive child has its own independent census and exact-resultant
  replay; this node does not reinterpret their normalized counts as edges.
- `verify.py` independently recomputes all 271 weighted profiles, deletes only
  `(4,2,18)`, checks the 270-profile residual, and proves the maximum and cap
  with exact integers.
- It also checks that 93,963 is not certified by the uniform inequality.
- RowC and all other prize profiles are explicit nonclaims.
