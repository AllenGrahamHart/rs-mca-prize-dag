# Dependency sub-DAG

```text
colored quotient compiler + literal-cell crosswalk
                         |
                         v
          ten moving cells (PROVED)
                         |
                         v
          literal coverage TARGET (evidence)
```

The two balanced `M01/M02-R11` cells are deliberately absent and remain in
the sibling review gate.
