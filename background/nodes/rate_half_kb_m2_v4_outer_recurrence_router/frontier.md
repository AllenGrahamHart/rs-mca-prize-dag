# Frontier

The independent type count remains three:

```text
(r,delta)=(2,4),(4,2),(8,1).
```

There is no primitive outer branch left. Every survivor lies in a proper
outer decomposition tower that returns to an inner degree-two endpoint
decomposition. The `(2,4)` row additionally has full V4 symmetry and the
equivariant no-triple source-star normal form. In `(4,2)`, apply that normal
form to the `Stab(Gamma)=<tau x 1>` orientation; the other two orientations
remain explicit. The `(8,1)` component has trivial V4 stabilizer.

Next, write the source-coupled equations in these stabilizer normal forms.
The route-deciding output must either prove an actual-producer
contradiction, force a distinct degree-two decomposition whose joint deck
group terminates, or emit a same-record carrier/data/slope owner. Repeating
the abstract decomposition cycle is not progress.
