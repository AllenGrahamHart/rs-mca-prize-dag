# Result

PROVED. The three residual `m=2` rows are exactly the V4 stabilizer orders
four, two, and one. Their outer degree-30 maps are all decomposable, and the
complete destination ledger is impossible or recurrent to `m=2`.

For rows stabilized by `tau x 1`, including all of `(r,delta)=(2,4)`, the
actual source component has the diagonal endpoint/source-deck symmetry,
avoids both locators in every paired source row, and has no weight-three
defect. No `m=2` row is closed or paid.
