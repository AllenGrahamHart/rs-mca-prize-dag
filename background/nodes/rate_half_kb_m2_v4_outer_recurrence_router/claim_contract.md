# Claim contract

- **Claim ID:** `rate_half_kb_m2_v4_outer_recurrence_router`
- **Status:** PROVED
- **Input:** an actual residual KoalaBear `u=2` component with terminal
  inner degree two and transverse type `(2,4)`, `(4,2)`, or `(8,1)`.
- **Output:** the component degree is its V4 stabilizer order; the outer
  degree-30 map is decomposable; all coarser destinations are impossible or
  recurrent to `m=2`; and coordinate-stabilized rows obey the printed
  source-star symmetry and defect parity.
- **Dependencies used:** actual bidegree-`(4,4)` birational lift and defect
  theorem, source-pencil/divisor adapter, complete primitive degree-30
  catalogue, and the proved degree `3,4,6,10,12,20,30` routes.
- **Consumer:** `rate_half_band_closure`.

## Explicit nonclaims

- No one of the three `m=2` types is deleted.
- Recurrence to a degree-two decomposition is not endpoint nonexistence.
- The order-two stabilizer in the `(4,2)` row is not asserted to have one
  preferred orientation.
- The source-star refinement is asserted only when `tau x 1` belongs to the
  component stabilizer; it is automatic for `(2,4)`.
- No endpoint-to-carrier, received-data, explaining-polynomial, slope,
  owner, payment, `u=2`, adjacent certificate, or Prize row is proved.
