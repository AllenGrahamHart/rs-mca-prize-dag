# Statement

## Claim `(KBP1B4-X2P0-1)`

On the guarded principal branch of deployed positive `433-1b -> O0a` role
cell `4`, every outside case with missing record `xi=2` and canonical
matching `0` is empty over `F_2130706433`, across all four source-sign rows
and all four target-sign lanes.

If `m=B(-t^2)/A(-t^2)` is the source-forced value of the omitted record, then
that record is `-DE`, so both surviving parallel records equal `DE=-m`.
Matching `0` therefore forces the target-free identity

```text
P(-m,-m)=0,
```

where `P` is the exact paired-product eliminant. In the proved four-basis
common algebra, its multiplication determinant has numerator degree `308`
with `281` terms and denominator degree `160` with `161` terms. For each
source-sign row the numerator has exactly eight deployed-field roots. The
union with every inverse-numerator, inverse-denominator, and norm-denominator
root has exactly ten candidate `r` values.

The complete lift ledger in each sign row has:

```text
5  r-guard terminals,
2  t-guard terminals,
6  base points with no F_p-valued b lift,
4  guarded common points where P(-m,-m) is nonzero.
```

There is no target-free witness, free fiber, or unresolved boundary. The cut
is independent of the two target signs, so four source computations pay
`4*4=16` raw cases. Together with the separate `xi=0` exclusion and the
`xi=0 -> xi=1` parallel-edge transport, all three parallel-`DE` omissions
are closed at matching `0`; the other `102` missing/matching slices in cell
`4` remain open.

This is a deployed-row exclusion only. It does not close cell `4`, orbit
`[4,7]`, `433-1b -> O0a`, K3, LIST, MCA, or either Prize problem.

## Falsifier

A guarded source point satisfying `P(-m,-m)=0`, an omitted candidate root,
an inverse boundary with a live original-equation lift, a false no-`b`-root
terminal, or a mismatch between the norm and direct paired-product replay.
