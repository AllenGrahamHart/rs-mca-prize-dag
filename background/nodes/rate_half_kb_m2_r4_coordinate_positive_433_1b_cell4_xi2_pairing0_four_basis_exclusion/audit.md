# Audit

- The sign is load-bearing: the omitted record is `-DE=m`, so matching `0`
  imposes `P(-m,-m)`, not the `P(m,-m)` equation used at `xi=0`.
- The norm is used only in the sound direction `target-free element zero`
  implies `multiplication determinant zero`; no field or irreducibility
  assumption is made for the quadratic tower.
- Every inverse introduced by base normalization, `b` normalization, linear
  `c` recovery, and missing-record evaluation is in the candidate-root union.
- Empty `b` fibers are explicit payload rows and are independently checked by
  nonsquare discriminants; they are not inferred from an empty loop.
- All four finite common points per sign are replayed in the original tower,
  and their direct paired-product values are nonzero.
- Target-lane multiplication is valid because neither `sigma_c` nor
  `sigma_o` occurs in `P(-m,-m)`.
- This result pays one of `7*15=105` missing/matching slices. Together with
  the two prior matching-zero slices, `102` cell-4 slices remain.
