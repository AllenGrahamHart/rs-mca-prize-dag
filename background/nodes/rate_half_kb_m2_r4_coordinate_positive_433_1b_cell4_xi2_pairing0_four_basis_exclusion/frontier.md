# Frontier

The three parallel-`DE` omissions `xi in {0,1,2}` are closed at canonical
matching `0`. Before launching independent norms, test the exact residual
matching permutations used in cell `3`: matchings `1` and `2` may reduce to
the same missing-sum quartic or transport under exchange of the parallel
positive `DE` records.

The four-basis tower remains the correct common representation. Do not retry
ambient denominator-cleared standard bases without a new reduction.
