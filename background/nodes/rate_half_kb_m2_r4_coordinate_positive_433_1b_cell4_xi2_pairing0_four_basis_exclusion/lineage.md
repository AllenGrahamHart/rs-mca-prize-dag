# Lineage

- The cell-4 tower theorem supplies the exact four-basis common algebra and
  globally normalized coefficient kernel.
- The signed-edge atlas supplies the outside record order and four target
  lanes.
- The separate `xi=0` theorem and parallel-edge transport close the other
  two parallel-`DE` omissions at matching `0`.
- This node introduces the genuinely different equal-record equation
  `P(-m,-m)` and closes the third matching-zero slice.
- The eventual consumer remains `rate_half_band_closure`.
