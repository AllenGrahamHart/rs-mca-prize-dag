# Frontier

In the generic weld geometry, every large-type record now lies in one fixed
pole-simple scalar-locator atom unless high complexity occurs. The next
theorem must bound or first-own the complete record set of that one atom.
The alternative rational-rank-two pair pencil remains a separate census.
