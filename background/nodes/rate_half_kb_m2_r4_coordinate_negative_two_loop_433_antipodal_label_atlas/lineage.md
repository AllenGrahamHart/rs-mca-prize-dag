# Lineage

The two-loop weld applied to the doubled signed `AB` cross-pair gives one
monomial identity among its two fiber labels and the `C` loop.  This node
classifies that identity against the exact antipodal structure of `K`.
