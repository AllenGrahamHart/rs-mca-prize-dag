# Audit

1. The `+/-` labels refer to the two signed `AB` edge-product types, not to
   an antipodal pair of `K` labels.
2. The edge orbits are oriented by their shared `A` endpoint before the
   exact weld is squared.
3. Normalizing by `k_+` preserves antipodal pairs because `k_+!=0`.
4. The table classifies role placements, not merely unlabeled five-sets.
5. Free parameters in `M1,L1,Z1` must avoid all five-label collisions.
6. The single identity `(KB43-1)` is only one of the two q constraints; the
   product map and the connection to the `BC` edge remain open.
