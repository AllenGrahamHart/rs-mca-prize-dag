# Claim contract

- Exact low-support defects: `c=2,3,4,5`, `0<=s_c<=q`.
- Cross-support collision targets: `2<=d<=9`, `c+d<=11`, only when
  `s_c<q`.
- Joint support-four/support-five charge: only when `s_4+s_5<q`.
- High-support branch order: `6,7,8,9`, all terminal/fallback choices.
- Pareto groups: `(2,3)`, `(4,5)`, `(6,7,8,9)`.
- Closed rows: `K'=60..70`.
- Adjacent wall: `K'=71`, retained as a nonclaim.
- Every kernel corank, rank-nine mark, and floor-record cross is retained.
