# Attack

At `K'=71`, all low-support defects are `31` and supports `6..9` remain on
fallback.  A continuation should couple source support two to target nine
more strongly, couple several source carriers simultaneously, or price the
high-support fallback through another shared resource.  Do not extend the
collision theorem to `c+d=12`.
