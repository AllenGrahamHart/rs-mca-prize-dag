# Audit

1. Exact source defects include the empty value `s=q`, but no cross-support
   cap is applied on that value.
2. Every cross-support target satisfies `c+d<=11`.
3. Existing same-support and joint support-four caps are intersected, not
   replaced.
4. All `120` high-support terminal/fallback choices remain present.
5. Only componentwise dominated vectors are discarded.
6. Every kernel corank and rank-nine mark is unchanged.
7. The floor-record coefficient and raw cross are positive on safe rows.
8. `K'=71` is recorded as a method wall, not a counterexample.
