# Audit

1. The lane is selected before record/subset deduplication.
2. The divisor `C(m'-9,2)` is the exact maximum number of eleven-subsets of
   one size-`m'` support which extend a fixed nine-subset.
3. The identity converting eleven-subset incidence mass to nine-subset mass
   is exact; no independence assumption is used.
4. The output counts distinct records for one fixed `B`, not incidences.
5. Every factor in the endpoint product increases with `K'`, so the
   uniform endpoint is `K'=10`.
6. The statement preserves affine-owner versus kernel lane typing.
7. No first-match ownership or aggregate row payment is claimed.

No Modal computation is used.
