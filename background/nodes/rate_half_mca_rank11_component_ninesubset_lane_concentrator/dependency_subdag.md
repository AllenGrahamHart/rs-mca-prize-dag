# Dependency sub-DAG

```text
dense-locator component-incidence dichotomy
                 |
       choose the heavier lane
                 |
 mark all nine-subsets of each eleven-subset
                 |
 divide by C(m'-9,2), then average over C(n',9)
                 |
 fixed B + at least 2578110 records in one lane
```

The next node classifies the fixed target. No component identity or payment
is inferred here.
