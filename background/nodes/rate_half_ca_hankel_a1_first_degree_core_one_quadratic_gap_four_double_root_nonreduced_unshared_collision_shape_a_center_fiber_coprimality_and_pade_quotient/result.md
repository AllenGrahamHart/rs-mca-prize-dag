# Result

`PROVED`. No center fiber of `G` meets the corresponding center locator.
At the large center this forces both `G(gamma_0,x_*)` and
`T_3(gamma_0)` to be nonzero. The center-specialized Pade identity also
has the exact quotient

```text
B_src(gamma)=L_Mgamma C_gamma,
P_F(gamma)=chi_gamma R_gamma C_gamma.
```

This is the next interface for coupling the three canonical center defects.
