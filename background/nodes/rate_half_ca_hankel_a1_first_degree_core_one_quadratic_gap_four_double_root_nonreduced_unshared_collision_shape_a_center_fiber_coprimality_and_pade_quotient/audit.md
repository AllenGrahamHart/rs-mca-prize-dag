# Audit

1. The row-root theorem applies to every point of `U_0` because Shape A
   has no exceptional source row.
2. The large locator has one root outside `U_0`, namely `x_*`; it requires
   the residual-cycle support argument rather than the row-root theorem.
3. Coprimality is over the complete center locator: its displayed factors
   list all roots with multiplicity one.
4. In the Pade specialization, cancellation is by the nonzero polynomial
   `L_rest,gamma`, not pointwise division.
5. `gcd(R_gamma,L_Mgamma)=1` uses `x_* notin U_0` in the large class.
6. Nonzeroness of `C_gamma` is checked at a root of `L_rest,gamma`, where
   the source numerator has the nonzero value in `(7)`.
7. No critical status changes.
