# Audit

1. The base candidate is guarded, so `Q_0` is invertible modulo `D_0`.
2. The inverse is proved by exact divisibility, not a dimension count.
3. The chart covers the unguarded monic slice; splitness and primitivity are
   reimposed explicitly and are not silently discarded.
4. The derivative clause in `(DC4)` uses squarefreeness only at common roots.
5. The collective route fence concerns formal determinant identities only;
   split-root incidence and owner structure remain live.
6. No field-sized chart cardinality is presented as a prize payment.
