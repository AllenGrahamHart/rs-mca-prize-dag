# Frontier

Define the active balanced-core certificate as source-bound mathematical data
and prove exact projection to `Z_BC`. Then either realize those certificates
as K3 endpoint records with exact fibers or route them through the current
upstream v4 same-owner `(S)/(A)/(E)` endgame.
