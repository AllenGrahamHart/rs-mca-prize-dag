# Attack record

The `K'=24` ledger was blocked because the old baseline treated every
support at least six as having only 45 rank-nine shadows.  Raising that
baseline requires finite caps for supports six through nine.

The deletion argument already used below saturation does not require a
carrier if the completion count is allowed to reach `q`.  Keeping the full
integer maximum over `0<=b<=q` supplies exactly the missing caps without a
new premise.
