# Audit - L1 tangent Hasse root-pinning census

## Checked axes

1. `D|L` is retained in the converse direction of the double-root test.
2. Squarefreeness is used to make `gcd(D,L/D)=1`.
3. First Hasse derivatives make the Hermite statement characteristic-safe.
4. The confluent map has rank `min(k,2r)`, not automatically `2r`.
5. An inconsistent affine system is allowed to have zero solutions.
6. A codeword is counted by its complete agreement set, not its subsets.
7. Tangent members are owned by the exact gcd, not every divisor of it.
8. The tangent degree is capped by both `a` and the cofactor degree `e`.
9. Full remainder rank `a-r` is distinguished from projected Pade rank.
10. The raw binomial sum is explicitly not promoted to a row-sharp payment.

## Route effect

The tangent resultant now has an exact degree/root-set ledger.  Large
tangent searches should test collective feasibility or owner absorption;
fixed-root Hermite rank and raw divisor enumeration are settled analytically.
