# Result

The `FFI` and `FIF` leaves reduce exactly to systems with linear `A2` and
`A0`. Their two finite common-root pairs are therefore linear in the auxiliary
root variables. `FFF` and `IFF` remain structurally unreduced.
