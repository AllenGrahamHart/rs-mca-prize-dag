# Audit

The verifier pins the exact S0 record order, the `xi=2,pairing=0` matching,
both subtraction identities, and all four guards used. The hostile audit
checks a wrong subtraction sign and explicit nonzero-`z2` witnesses that
appear if either square-distinct guard is removed.
