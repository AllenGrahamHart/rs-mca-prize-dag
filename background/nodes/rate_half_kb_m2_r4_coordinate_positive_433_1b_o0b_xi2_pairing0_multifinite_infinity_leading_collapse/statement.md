# Statement

In the canonical `xi=2,pairing=0` S0 case, every admissible solution in chart
`FFI` or `FIF` has

```text
z2 = z5 = 0.
```

Here `z0,z1,z2` and `z3,z4,z5` are the coefficients of the two lifted
quadratics `A2` and `A0`.
