# Lineage

The msolve F4 pilot retained an explicit 38-polynomial `FFI` system. Its last
two chart equations expose the common factor `f(d-e)z2`, yielding the first
non-computational simplification of the multi-finite frontier.
