# Frontier

Compile a reduced `FFI` system with `z2=z5=0`, omit those two lift variables,
and invert only `f`, `d^2-e^2`, and `b+1`. A checked unit result closes `FFI`.
The same architecture then applies to `FIF` with guards `d` and
`e^2-f^2`.
