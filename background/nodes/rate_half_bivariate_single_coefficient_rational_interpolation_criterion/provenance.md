# Provenance

The criterion is derived from the interpolation-defect formula and the
standard dual description of a shortened Reed-Solomon parity-check kernel.
It explains the coefficient-block profiles measured by the bounded `m=2`
Modal campaign.

No numerical result is used in the proof. The campaign is retained only as
falsification evidence. The later proved node
`rate_half_bivariate_incidence_only_rational_trace_route_fence` supplies an
adversarial rational certificate and limits that evidence to its sampled
distribution.
