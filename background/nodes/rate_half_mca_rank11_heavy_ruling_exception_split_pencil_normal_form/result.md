# Result

The live heavy-ruling rational profile is no longer an arbitrary
scalar-locator certificate. It contains an exact split-pencil instance

```text
u+gamma v=s_gamma L_(E_gamma),
1<=deg L_(E_gamma)<=11,
```

with at least 20 pairwise-disjoint split fibers, `gcd(u,v)=1`, no scalar-zero
anchor, and a denominator coprime to the large anchor-core locator.
