# Result

Before selecting the internal fiber scalars, every support/pair/internal-
slope packet now presents an explicit lambda-cube kernel matrix. Full rank
or one coloop rejects the whole packet. The first official-shaped
`e=7,F_193` control is a `49x7` full-rank matrix, whereas `e=3` has no rows
and is formally incapable of this exclusion.

The strongest current theorem target is to prove full rank or a coloop for
every official admissible matrix `U`, then classify only the
deletion-stable deficient exceptions. This node supplies the reduction but
does not prove that target.
