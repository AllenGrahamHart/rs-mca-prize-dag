# Dependency sub-DAG

```text
x24_char0_dyadic_descent -------------------------------req--+
f3_hge4_primitive_shift_pair_near_square_union_router ---req--+
                                                              |
                                                              v
                         this node --ev--> f3_hge4_norm_gate_count
```

The node supplies a fixed-cell characteristic-sieve architecture for the
whole non-full near-square locus. The target still needs a uniform aggregate.
