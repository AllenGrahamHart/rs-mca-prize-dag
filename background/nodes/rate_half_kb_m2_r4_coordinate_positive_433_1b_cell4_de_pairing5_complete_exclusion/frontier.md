# Frontier

The theorem pays `(xi,pairing)=(0,5),(1,5),(2,5)`. The parallel-`DE`
matching involution separately transports `(2,5)` to `(2,12)`, but it does
not pay `(0,12)` or `(1,12)`. Those positive-`DE` matching-12 cases are the
next exact target in this orbit.
