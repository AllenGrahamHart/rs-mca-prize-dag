# Proof

Build the four literal q-slice rows for `F06-R20` and reconstruct on `V!=0`.
After named factors are removed, the first-two-row resultant has two cubic
factors and one degree-12 factor. Adjoin each cubic to the two remaining
essential q-slice cores over `F_2130706433`.

Factor 0 gives a dimension-one basis of size 45. Sequential reduction of the
24-factor square-free transported localizer reaches zero at factor 15, so
the complete named-open branch is empty over the base field and every
extension.

Factor 1 gives a dimension-one basis of size 46. All 24 localizer factors and
the first four powers of their product remain nonzero; this is the surviving
route, not an emptiness claim. Exact factorization makes it the only cubic
route left. QED.
