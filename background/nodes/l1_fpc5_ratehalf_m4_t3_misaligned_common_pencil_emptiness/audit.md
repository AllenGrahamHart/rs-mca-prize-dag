# Audit

1. Misalignment is exactly `A!=0`; the aligned case is handled by the sibling
   constant-multiplier theorem.
2. Unit status of `Etilde` ensures `m_0!=0`.
3. The key zero conclusion uses both `deg Q<=ell-a` and
   `deg V<=ell-a`, strictly below `deg(P-z_0)=ell`.
4. Degree comparison forces `deg Q=ell-a`; it is not merely an upper bound.
5. The contradiction uses the candidate-wise exact gcd guard, not flat-wide
   primitivity.
