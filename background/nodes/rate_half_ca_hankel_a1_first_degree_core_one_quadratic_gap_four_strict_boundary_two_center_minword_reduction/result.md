# Result

`PROVED`. The first strict pair boundary has a two-center source partition
and at least `p+2` exact RS minimum words, including at least
`(e+15)/2+r_A` unpadded instances. It is therefore no longer an unstructured
support-design boundary.
