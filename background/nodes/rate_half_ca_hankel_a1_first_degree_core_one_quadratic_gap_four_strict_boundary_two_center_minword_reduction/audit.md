# Audit

1. The endpoint line has exactly two centers because Cycle 127 excludes a
   third center throughout the strict branch.
2. The noncore joint support has size `3p-1`; this shifts each endpoint
   missing class to `p+r_gamma`.
3. The off-line population is `T-2=3e+1`, not the `3e` of the extremal
   three-center branch.
4. Heavy deficits cancel from the excess sum only after both line and
   off-line incidence contributions are included.
5. The center difference is nonzero because every off-line center is off
   the endpoint codeword line.
6. The clean count removes all positive-deficit slopes adversarially; it
   does not assume deficit one.
