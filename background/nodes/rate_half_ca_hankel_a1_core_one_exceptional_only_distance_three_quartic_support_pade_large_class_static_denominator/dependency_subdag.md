# Dependency sub-DAG

```text
pade_relation_class_reduction [PROVED]
             |
             +--req--> pade_large_class_static_denominator [PROVED]
                                      |
                                      +--ev--> rate_half_band_closure [UNPROVED]
```

The open leaf is the common-gcd bound of `P_Z` and the three residuals
`(QPSD3)`.
