# Result

The remaining boundary-saturated curve is no longer posed as an arbitrary
biform. Its coefficient space is an `(m+1)`-plane simultaneously isotropic
for four explicit adjacent Hankel forms, and the entire resultant boundary
polynomial `q_inf` spans both infinity radicals.

The next classifier must therefore act on the joint object

```text
(W_Q; H_(0,0),H_(0,1),H_(1,0),H_(1,1); B,W),
```

subject to the exact rank-one frame cancellations and saturated resultants.
