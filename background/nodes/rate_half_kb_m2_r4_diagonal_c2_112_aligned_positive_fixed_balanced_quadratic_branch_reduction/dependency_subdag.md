# Dependency sub-DAG

```text
aligned-positive colored quotient compiler
  -> fixed quadratic orbit compression (PROVED)
       -> generic V!=0 direct four-cell localization (PROVED here)
       -> rank-drop V=0 eight-factor localization (PROVED here)
            -> fixed balanced four-cell exclusion (PROVED)
                 -> literal-assignment coverage (TARGET)
```
