# ATTACK - e1_official_prime_exception_control

Status: target. This is a route-uniform statement; use light local work and
Modal only for pre-registered route-deciding experiments.

## Proved reduction

`e1_exceptional_set_reduction` identifies every residual collision with an
explicit nonzero cyclotomic norm divisor, and `are_exceptional_density` gives
an almost-all-primes average. `official_row_primes_pinning` proves that there
is no hidden finite list of official primes. `axis8_generating` and
`v13_base_field_normalization_guard` show why no direct-E1 theorem may silently
spend a larger ambient field. This node is uniform only over rows explicitly
assigned to the direct E1 route. It does not prove that this route covers every
admissible row.

The named-field folded-certificate machinery is sound at its printed scope and
is retained in `background/nodes/`. It can calibrate algorithms and certify an
exhibit, but two named rows cannot prove this route-wide target.

## Route-deciding attacks

1. Try to construct a prize-admissible direct-E1 row, especially a split prime,
   with collision mass above the required budget. An exact family kills
   `e1_fullness` and requires an input-sensitive correction downstream.
2. If that fails, strengthen the norm-divisor ledger from almost-all-primes to
   the exact uniform budget needed for every assigned row.
3. A certifier route is legitimate only if a theorem proves termination,
   soundness, and coverage for every row reaching this branch. Per-exhibit
   transcripts alone are evidence.

Do not promote from birthday scans, lattice-reduction summaries, or finitely
many successful exhibits. The smallest useful falsifier is one assigned row
whose exact folded collision count exceeds the budget. A row outside the
direct route tests `unsafe_crossing_family_instantiation`, not this target.
