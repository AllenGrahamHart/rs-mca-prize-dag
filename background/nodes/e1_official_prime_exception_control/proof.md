# Proof status: open

There is no route-uniform proof of this target. The former automatic discharge
combined a valid folded-certificate implication with named-exhibit premises,
then incorrectly promoted it across every admissible row. The scope failure is
recorded in `status_ruling.md`; `conditional.md` is retained only as the
retired finite-exhibit implication.

The current proved reductions force the live pair-feasible branch to the
prime field, remove the first four `N=256` swap bands and first two `N=512`
bands, and reduce one first-band profile to three exact `V=64` chambers. They
do not prove the required row-uniform collision-pair allowance. Promotion
still requires the exact every-row bound or a sound and complete per-input
certifier stated in `statement.md`.
