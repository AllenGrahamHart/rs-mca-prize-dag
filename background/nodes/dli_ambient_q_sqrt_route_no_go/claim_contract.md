# Claim contract

## Proved

- The scale-free ambient strengthening `K_amb<=sqrt(2n)` is false.
- The counterexample uses the exact DLI moment prefix, cyclic domain,
  one-sided frequencies, and antipodal first-owner subtraction.
- On the same row, the true Haar ratio remains below `sqrt(2n)`.

## Evidence

- A complete 189-prime high-cap scan has 56 ambient failures and no Haar
  square-root failure.

## Not proved

- `J_prim<=sqrt(2n)` is not proved.
- The official fixed bound `K_amb<=2^21` is not formally refuted by this
  `n=32` row, although the scale-free transport to it is dead.
- C2'', LIST, MCA, and either prize terminal are unchanged.

