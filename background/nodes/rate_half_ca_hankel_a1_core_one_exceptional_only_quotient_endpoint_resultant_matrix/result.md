# Result

The sharp endpoint is reduced to two exact `2 x 2` resultant matrices: a
flat pure-power matrix and a one-factor checkerboard swap. All row and column
products reproduce the existing Bezout and two-sided saturation identities.

The remaining endpoint task is algebraic compatibility with the corrected
square beyond these resultants, not incidence enumeration.
