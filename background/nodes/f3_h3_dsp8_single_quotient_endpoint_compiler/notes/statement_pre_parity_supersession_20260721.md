# Provenance snapshot (2026-07-21): statement.md immediately before the
# w18-C1 DSP8 parity supersession, maintainer-ratified this date.
# Preserved per custody #104 (merge-not-replace). The parity package
# strictly sharpens the constants; see WAVE18_AUDIT_FINDINGS.md.

# DSP8 single-quotient endpoint compiler

- **status:** PROVED
- **closure:** proof
- **consumers:** `f3_h3_dsp8_correlation_bound`,
  `f3_h3_official_order_template_survivor`
- **dependencies:** `f3_h3_dsp8_accident_depth_compiler`,
  `f3_h3_dsp8_joint_star_depth_pareto_compiler`,
  `f3_h3_double_accident_nonzero_coupling_ideal_router`

Put

```text
e(t)=(P(t)-18)_+,       rho_0(t)=R(t),
B_(0,17)(n)=300n^2-289(n-1)(n-2)
            =11n^2+867n-578>11n^2.                 (SQE1)
```

Then the C36' target follows from the twelfth Pareto endpoint

```text
17 sum_(t!=1,P(t)>=36)(P(t)-18)R(t)<=B_(0,17)(n).  (SQE2)
```

Let

```text
Dbar_17^c=sum_(t in class c,P(t)>=36)
                   N_6^disj(t)R(t).                (SQE3)
```

The class-sensitive disjoint-six ratios are

```text
P(t)-18<=(9/29)N_6^disj(t)    in class 0,
P(t)-18<=(9/22)N_6^disj(t)    in class A.          (SQE4)
```

Consequently the exact moment inequality

```text
Dbar_17^0+(29/22)Dbar_17^A<=(319/153)n^2           (SQE5)
```

is sufficient for C36'. It has the same normalized allowance as the
`(P,R)>=(35,2)` endpoint, but its retained rectangle is

```text
(P,R)>=(36,1).                                     (SQE6)
```

Every positive summand has a pure disjoint-distance-six product star of
degree at least eight in class `0` or six in class `A`, and one ordered
quotient lift `Q=(z,w)`. For this fixed `Q`, at most two unordered product
representations `E` have zero normalized coupling `lambda_(E,Q)`. Hence one
of the at least nine or seven vertices in the product star has

```text
lambda_(E_*,Q)!=0.                                 (SQE7)
```

If `J_star` is the ideal generated by the normalized differences on that
star, the single-quotient packet is

```text
I_17=(J_star,lambda_(E_*,Q)).                       (SQE8)
```

At every surviving official row prime, `I_17` lies in the degree-one row
prime ideal, so that rational prime divides `Norm(I_17)`. No second quotient
lift and no quotient-collision spoke is needed. The quotient lift itself is
retained and certifies `R(t)>=1`.

This theorem adds an alternative compiler row. It does not prove `(SQE2)` or
`(SQE5)`, generate the official candidate ideals efficiently, bound their
survivors, promote DSP8, or close C36'.
