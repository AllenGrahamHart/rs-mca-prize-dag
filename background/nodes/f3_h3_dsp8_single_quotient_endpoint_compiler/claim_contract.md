# Claim contract

- **claim:** the C36' Pareto diagonal has a valid `(P,R)>=(36,1)` endpoint
  with the printed class-sensitive moment allowance and a one-lift fixed-order
  packet containing a provably nonzero coupling.
- **scope:** every official H3 row in the remaining C36' corridor.
- **dependencies:** the accident-depth identities, endpoint disjoint-six
  ratios and star degrees, and the exact binary-telescoping zero-coupling
  classification.
- **falsifier:** a row where `(SQE2)` does not imply C36', a retained profile
  violating `(SQE4)`, or a fixed quotient lift with three distinct
  zero-coupling product centers.
- **nonclaims:** neither endpoint moment is bounded; no candidate generator,
  survivor theorem, DSP8 promotion, or C36' close is asserted.
