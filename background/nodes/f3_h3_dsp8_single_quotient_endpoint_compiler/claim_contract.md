# Claim contract

- **claim:** swap parity and the diagonal-target affine payment strengthen the
  valid `(P,R)>=(36,1)` endpoint to the printed row-sensitive
  `(29/153)B_par` allowance and its uniform `234697/48960` corollary; its
  one-lift fixed-order packet contains a provably nonzero coupling.
- **scope:** every official H3 row in the remaining C36' corridor.
- **dependencies:** the accident-depth identities, endpoint disjoint-six
  ratios and star degrees, the exact binary-telescoping zero-coupling
  classification, and the constants-explicit affine coset-pair Stepanov
  bound.
- **falsifier:** a low profile violating the parity payment, a diagonal-target
  mass violating `(SQE2)`, a retained profile violating `(SQE5)`, or a fixed
  quotient lift with three distinct zero-coupling product centers.
- **nonclaims:** neither endpoint moment is bounded; no candidate generator,
  survivor theorem, DSP8 promotion, or C36' close is asserted.
