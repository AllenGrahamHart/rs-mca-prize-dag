# Result

`PROVED`. Swap parity confines the expensive seventeenth low-excess unit to
diagonal product targets. Their quotient-weighted mass is lower order by the
proved affine subgroup-line bound, so the `(36,1)` endpoint now has

```text
Dbar_17^0+(29/22)Dbar_17^A
 <=(29/153)B_par(n)
```

as its exact row-sensitive sufficient moment. Uniformly,
`B_par>(8093/320)n^2` gives the constant `234697/48960`, versus the former
`319/153`. Its product packet still has degree-eight/six stars and needs only
one quotient lift with one nonzero coupling anchor; no quotient-collision
spoke remains.
