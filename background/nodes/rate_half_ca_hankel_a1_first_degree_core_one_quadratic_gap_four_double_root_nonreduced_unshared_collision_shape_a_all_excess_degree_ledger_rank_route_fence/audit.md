# Audit

1. The extra slope is `0`; it is not contained in any nonzero fifth-root
   fiber and its `X`-fiber has no root on `X`.
2. The twenty active fibers have monic known factor `X^7-delta^5`; the
   residual scalar is `-1`.
3. The zero fiber has `a_0=7`, known factor `1`, and residual `-X^7`.
4. Every block is nonzero, although most of its individual coefficients
   may vanish.
5. Rank `27` is computed over `F_211`, not over floating point or the
   integers.
6. The example refutes degree-ledger-only full rank. It does not refute an
   official theorem using the omitted Shape-A constraints.
