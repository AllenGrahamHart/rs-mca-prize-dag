# Audit

## Checked

- the integer partitions of energies one through four;
- all four spectrum counts;
- the Laurent-to-polynomial resultant normalization;
- exact official interval endpoints and all `1133` legal cofactors;
- complete return of `16` energy-three and `128` energy-four shards;
- empty viable lists and the stronger zero integer-interval count at energy
  four;
- source hashes and mutation controls in both local verifiers.

## Falsification record

A `55`-second annealing probe found realizable energy-`2` vectors after
`5,316,838` trials, refuting any universal coefficient-level floor above
two. A complete four-singleton parity census also found `264` normalized
one-lag masks. The theorem therefore excludes low energy by exact official
norm arithmetic, not by a false combinatorial nonexistence claim.

## Residual risk

The complete norm census is computational. Independent local auditing checks
the enumeration grammar, norm identity on pinned controls, counts, hashes,
and hostile certificate mutations; full recomputation uses the pinned Modal
programs.
