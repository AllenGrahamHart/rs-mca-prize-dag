# Audit

1. Product factorization is checked against the original first determinant.
2. Cell `12` retains both reciprocal product branches.
3. Cells `13/14` retain both `(r-1)/(r+1)` orientations.
4. Every solve denominator has an explicit lost-branch audit.
5. Opposite-sign deletion uses the original label guard `r^2+1!=0`.
6. Every admitted tuple replays all four original equations and the full guard.
7. The 64 count combines disjoint matching cells; it is not an outside-packet count.
