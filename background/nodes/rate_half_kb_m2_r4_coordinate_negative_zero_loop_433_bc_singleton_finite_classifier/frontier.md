# Frontier

Compile the five outside skeletons over the 32 new packets.  The existing
product-skeleton router applies structurally, but its involution constants
must be recomputed from each cell's two common pairs.  Delete product types
before assigning `eta` and `L^c` q rows.
