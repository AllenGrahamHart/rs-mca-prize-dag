# Result

The exceptional `x=0` cubic `2+2+2` branch reduces to one degree-five
polynomial in `d`. Three additional rational values are already excluded by
the official norm-color condition; only 32 tiny norm gcds remain for this
branch.
