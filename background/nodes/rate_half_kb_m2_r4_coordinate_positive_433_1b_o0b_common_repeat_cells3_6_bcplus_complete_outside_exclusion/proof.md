# Proof

The colored missing-edge atlas excludes all 120 cell-3 missing-`BE` systems.
The missing-`CF` residual node excludes all 120 cell-3 missing-`CF` systems.
The three uncolored leaves exclude 240 missing-`DE+/-`, 240
missing-`DF+/-`, and 120 missing-`EF` systems. These seven record classes are
disjoint and exhaustive, so

```text
120 + 120 + 240 + 240 + 120 = 840
                         = 4 root signs * 2 outside signs
                           * 7 missing records * 15 matchings.
```

The full-system transport is an exact involutive bijection from cell 3 to
cell 6 preserving `BC+`, outside sign, root signs, and the complete
missing-record/matching label set. It therefore transports emptiness of all
840 systems, proving the cell-6 census as well. QED.
