# Cells 3 and 6 complete repeated-`BC` `BC+` outside exclusion

- **status:** PROVED
- **field:** `F_2130706433`
- **scope:** all seven missing records in repeated-`BC` owner cells 3 and 6

Every cell-3 repeated-`BC` `BC+` outside system is empty: 120 systems for
each of `BE` and `CF`, 240 for each signed orbit `DE` and `DF`, and 120 for
the fixed record `EF`. This is the complete census
`4*2*7*15=840`. The exact full-system transport bijects this workload with
cell 6, so all 840 cell-6 systems are empty as well.

This closes only the `BC+` branch of these two owner cells. The `BC-` branch,
other owner blocks, split lanes, and the complete `433-1b -> O0b` route remain
open.
