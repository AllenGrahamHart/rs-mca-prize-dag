# Audit

The factors `M` and `D` have different roles: `M` contains each missing
color once, while `D` records multiplicity beyond the first occurrence.
They must be coprime, and `M` must be squarefree.

Cyclic scaling is used only for the necessary value system. A retained
scaled polynomial must be returned to the actual pointwise Frobenius
assignment before any outer conclusion.

The 54 count uses Burnside's half-turn fixed configurations; naïvely
dividing 420 by eight is invalid. Exact multiplicities still require
subresultant saturation rather than the value identity alone.
