# Result

The official checkpoint regime is a 59-pair finite atlas. Thirty-three pairs
have `floor(n/p)=1` and no minimum-width split collision. Ten have
`floor(n/p)=2` and reduce exactly to square-quotient complements. Only sixteen
pairs retain three or more possible split values and require the general
Frobenius-pencil eliminant route.
