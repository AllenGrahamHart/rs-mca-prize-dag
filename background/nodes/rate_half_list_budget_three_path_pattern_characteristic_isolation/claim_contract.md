# Claim contract

- **claim id:** `rate_half_list_budget_three_path_pattern_characteristic_isolation`
- **mathematical statement:** the exact exponent-support pattern of the known
  order-sixteen path witness is singular only possibly in characteristics
  dividing `2^170 17^4`, and hence only in characteristic `17` when the domain
  has order sixteen in odd characteristic
- **scope:** the fixed four supports `(PCI2)` and their Galois reductions;
  official-row consequence only at the prize-max order `2^41`
- **consumer:** `rate_half_list_adjacent_crossing`
- **status:** `PROVED`
- **proved dependency:** the exact `F_17` path witness
- **falsifier:** a realization of `(PCI2)` with deficient intersection-matrix
  rank in an odd characteristic other than `17`, or a wrong determinant/norm
- **nonclaims:** no exclusion of other path assignments, no exclusion of the
  other five incidence types, and no determination of the `B*=3` crossing
- **replay:**
  `tools/ramguard tiny -- python3 background/nodes/rate_half_list_budget_three_path_pattern_characteristic_isolation/verify.py`
- **upstream mapping:** finite adjacent-row split-pencil arithmetic / exact
  exceptional-characteristic certificate
