# Attack

The open rank-nine interval is now exact: `10<=K'<=20617`.

1. Work in residual units. Any common-core or support-intersection theorem
   must subtract the deterministic deleted locator core before comparison
   with `m'`.
2. Retain the fixed-chart marked extension weight. A successful cap must
   improve `981105*(m'-10)n'` by a factor of about twelve at the shortest
   row.
3. Use the rank-nine owner plane's line design together with the ancestor
   normalized-deviation span or a chronology-correct cross-chart charge.
4. The existing fixed-chart rank-nine construction is a mandatory hostile
   control; chart-local population alone cannot close the branch.
