# Attack

The fixed target has two branches left.

1. For the kernel chart, retain marked extension weight and charge every
   rank-deficient extension to a fixed kernel word, root set, or quotient.
2. For the rank-eight owner flat, use its two-dimensional kernel and
   affine-error-rank-three form without discarding the extension weight.

Any local cap must be tested against the official weighted-plane fence, but
that fence no longer realizes the required marked weight.
