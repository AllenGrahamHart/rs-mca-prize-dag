# Audit

1. Both sides of the surviving proof count `(record,T)` incidences.
2. The first strict weighted crossing is `K'=20618`, not `67473`.
3. The monotone ratio has nine binomial factors and one final factor.
4. The `134944` pair-core floor belongs to original-row supports of size
   `1116048` in length `2097152`.
5. Reverse shortening inserts `1048576-K'` coordinates into every lifted
   core; these must be removed before a residual comparison.
6. The former inequality `134944<=|J|<m'` mixed original and residual rows
   and is retracted.
7. The repaired node is still PROVED, but only on `K'>=20618`.

No Modal computation is used.
