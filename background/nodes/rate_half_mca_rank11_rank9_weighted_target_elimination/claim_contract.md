# Claim contract

## Input

The fixed typed rank-nine target, its marked extension demand, and the
rank-nine weighted component cap in the same `(record,T)` unit.

## Output

The rank-nine affine-owner alternative is empty on every official residual
dimension `20618<=K'<=1048576`.

## Reopened scope

Rows `10<=K'<=20617` are not closed. The original-row pair-core dichotomy
cannot be compared directly with residual support size after the reverse
shortening inserts its deleted common core.

## Nonclaim

The low rank-nine interval, kernel lane, and rank-eight lane remain live.
No active-v4 atom, full rank-eleven payment, or prize closure is asserted.
