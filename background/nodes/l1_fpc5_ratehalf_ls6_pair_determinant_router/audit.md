# Audit

1. All candidates belong to one fixed atom and therefore share `E` and `M`.
2. The quotient degree uses the already proved gate `deg E>=a`.
3. Primitive quotient coprimality uses core/petal disjointness.
4. Nonzero determinant uses both primitive guards and monic equal degrees.
5. Gcd degree becomes root intersection only for the guarded split,
   squarefree locators.
6. `(PD7)` is field-sized and explicitly not banked as a payment.
7. `J<=0` fences distance-only Johnson closure; it does not refute a
   collective algebraic or split-incidence bound.
