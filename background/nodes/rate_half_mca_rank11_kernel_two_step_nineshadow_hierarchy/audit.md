# Audit

The primary verifier reconstructs every printed constant and checks the
double-counting algebra.  The independent audit scans every admissible
closure size on representative boundary rows and separately enumerates the
possible coloop counts.  It does not import the primary coefficient builder.
