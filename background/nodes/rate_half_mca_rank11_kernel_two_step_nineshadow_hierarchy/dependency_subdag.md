# Dependency sub-DAG

```text
kernel_nine_shadow_coupling
                 \
                  -> two_step_nineshadow_hierarchy
                 /
rank8_nineshadow_extension_deficit
```

The first parent supplies the spanning-shadow multiplicity and same-rank
extension cap.  The second supplies the rank-two-contraction counting pattern
that is generalized here.
