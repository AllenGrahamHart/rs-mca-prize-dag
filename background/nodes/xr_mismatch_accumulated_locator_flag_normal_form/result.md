# Result

Every canonical nongeneric mismatch branch now has a global RS lift and one
disjoint accumulated-locator flag of total degree below `K`. Descendant
explanations share the appropriate prefix divisor, and puncturing a common
prefix preserves the exact residual low-core cap.

The tangent bridge remains open. Its remaining compiler must count these
flags and their terminal generic slope fibers across lifted explanations;
the theorem forbids arbitrary overlapping-zero histories but supplies no
aggregate payment.
