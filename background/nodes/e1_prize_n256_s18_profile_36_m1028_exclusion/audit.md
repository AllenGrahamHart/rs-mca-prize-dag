# Audit

- The exact `mu=2` Hasse conditions force a pair at separation `2 mod 4`,
  proving the `{0,2}` normal form.
- The normalized classifier examines exactly 10009125 singleton supports;
  2503715 have multiplicity two and 20167 pass the necessary chord gate.
- Those supports canonicalize to 1603 affine orbits.
- The two heavy-position engines agree on every aggregate count, including
  exactly 16 energy-five vectors and no other energy in `{2,...,6}`.
- Every geometry hit is tested at all 128 primitive roots modulo 257. This is
  stronger operationally than selecting a root after Galois normalization.
- Primary Modal app: `ap-9YPE5CXuy9YJlmbb8kOTTL`.
- Audit Modal app: `ap-vPJTFiYL24oRxWpxi0C7f1`.
- The earlier congruence-preserving heuristic app
  `ap-gdrFAwJGaboYJWVNVr0Tuu` made 81430152 mutations without reaching the
  live window; it is route evidence only and is not used in the proof.
