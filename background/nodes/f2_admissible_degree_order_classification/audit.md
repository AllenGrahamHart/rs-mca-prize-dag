# Audit

- `k|e` is retained; generation is not assumed.
- The `e=6,k=2` exclusion checks both residue branches and the exact field
  cap before discarding the type.
- Every compositeness claim has a printed divisor and exact replay.
- Nonemptiness checks the largest extension degree used with each witness.
- The seven non-generating types are obtained only after the 12-type table
  is complete.
