# Audit

- Re-expanded all three identities `E_(j,s)=A_j+xB_j`; the coefficients
  `6,-7,-2` are exact.
- Kept each sign `s` separate. The quadratic norm eliminates the sign of `x`,
  not the two choices of `s`.
- Checked that `x=0` and `x^2=1` cannot occur on the primary common-root
  locus in good characteristic, so the converse loses no branch.
- Kept the ordinary Legendre polynomial distinct from the Jacobi notation.
- The output degree is `M=2^35`, not a claim that a dense computation at that
  degree is affordable.
- No Modal or other remote computation was used.
