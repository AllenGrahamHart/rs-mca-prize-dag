# Result

The preferred fixed-order correlation now pays three quotient layers:

```text
rho_3(t)=(R(t)-3)_+,
Mbar_(3),6,33^0+(53/42)Mbar_(3),6,33^A <2.144n^2.
```

The wider `rho_1` analytic endpoint remains available with constant
`2385/272<8.769`. More generally, absence of any one rectangle

```text
(P,R)>=(19,18),(21,16),(25,12),(29,8),(33,4)
```

proves C36'. The direct cutoff-25 family permits every depth `1<=L<=11`; at
`L=11`, its primitive-SP allowance is `1799/160=11.24375` and only `R>=12`
survives.

For fixed-order work this fuses the two strongest routers: every relevant
`P>=33,R>=4` target has a degree-seven or degree-five pure
disjoint-distance-six star and a four-lift quotient packet with at least three
nonzero couplings. At official odd primes, its full four-coupling batch is
equivalent to one nonzero coupling anchor and a three-spoke quotient-collision
star. CR-001 may use this separated joint-star normal form and analytically
discard every row outside that rectangle.

The reduced moment and survivor bound remain open.
