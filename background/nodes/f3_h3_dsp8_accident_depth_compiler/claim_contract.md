# Claim contract

- **claim id:** `f3_h3_dsp8_accident_depth_compiler`
- **mathematical statement:** paying `L` quotient layers and product excess
  through `E` reduces C36' to `rho_L=(R-L)_+`; the diagonal `L+E=17` gives
  five sufficient rectangle exclusions, and the selected `(L,E)=(3,14)`
  survivor carries a pure distance-six star and a four-lift quotient packet
  with at least three nonzero couplings, equivalently one nonzero coupling
  anchor and a three-spoke quotient-collision star after inverting two
- **scope:** every official H3 row
- **consumers:** DSP8 correlation and the CR-001 official-order survivor route
- **status:** `PROVED`
- **proved dependencies:** double-accident base payment, class-sensitive
  distance-six ratios, global overlap payment, primitive-SP normalization,
  pure distance-six multiplicity gate, nonzero-coupling ideal router, and
  coupling-batch odd saturation
- **new open content:** prove `(ADC6)` or `(ADC8)`, or generate and bound the
  class-sensitive joint star/coupling ideals at official orders
- **falsifier:** a quotient ratio set violating `(ADC1)`, a failure of a
  displayed diagonal or moment condition to imply C36', or a selected
  `P>=33,R>=4` target without the asserted four-lift anchored joint ideal or
  odd-local equivalence `(ADC10)`
- **nonclaims:** no correlation estimate, no candidate enumeration, no
  survivor bound, and no DSP8 or C36' promotion
- **replay:**
  `tools/ramguard tiny -- python3 background/nodes/f3_h3_dsp8_accident_depth_compiler/verify.py`
- **independent audit:**
  `tools/ramguard tiny -- python3 background/nodes/f3_h3_dsp8_accident_depth_compiler/verify_audit.py`
- **upstream mapping:** primitive shift-pair control / target-local excess
  quotient-depth ledger
