# DSP8 accident-depth compiler

- **status:** PROVED
- **closure:** proof
- **consumers:** `f3_h3_dsp8_correlation_bound`,
  `f3_h3_official_order_template_survivor`
- **dependencies:** `f3_h3_cutoff18_double_accident_reduction`,
  `f3_h3_antipodal_tail_distance_six_split`,
  `f3_h3_dsp8_global_overlap_cover_payment`,
  `f3_h3_dsp8_primitive_shift_pair_adapter`,
  `f3_h3_dsp8_disjoint_six_multiplicity_gate`,
  `f3_h3_double_accident_nonzero_coupling_ideal_router`,
  `f3_h3_double_accident_coupling_batch_odd_saturation`

For an integer `L>=1`, put

```text
rho_L(t)=max(R(t)-L,0),
T_L=sum_(t!=1)rho_L(t).
```

The quotient ratio set contains at least `n-2` nonidentity values, so

```text
T_L<=T_1<=(n-2)^2.                                 (ADC1)
```

After paying the first `L` quotient layers by the base ledger, define

```text
B_(L,E)(n)=300n^2-17L(n-1)^2-17E(n-2)^2.          (ADC2)
```

Then C36' follows from

```text
17 sum_(t!=1,P(t)>=19+E)(P(t)-18)rho_L(t)
 <=B_(L,E)(n).                                     (ADC3)
```

On the Pareto diagonal `L+E=17`, the useful cutoffs are

```text
E     L     retained rectangle       product certificate
0     17    P>=19, R>=18             mixed weight 4
2     15    P>=21, R>=16             mixed weight 6
6     11    P>=25, R>=12             mixed weight 8
10    7     P>=29, R>=8              mixed weight 10
14    3     P>=33, R>=4              pure D6 stars 7 / 5.   (ADC4)
```

For every row and every displayed pair,

```text
B_(17-E,E)(n)
 =11n^2+(578+34E)n-(289+51E)>11n^2.               (ADC5)
```

Thus exclusion of any one retained rectangle in `(ADC4)` proves C36'.

For `E=14`, let

```text
Mbar_(L),6,33^c
 =sum_(t in class c,P(t)>=33)N_6(t)rho_L(t).
```

The selected fixed-order endpoint is

```text
136(42Mbar_(3),6,33^0+53Mbar_(3),6,33^A)
 <=1113B_(3,14)(n),                                (ADC6)
```

and the simpler condition

```text
Mbar_(3),6,33^0+(53/42)Mbar_(3),6,33^A
 <=(583/272)n^2 <2.144n^2                          (ADC7)
```

is sufficient. The wider `L=1` analytic alternative retains the former
constant `2385/272<8.769` with `rho_1=(R-1)_+`.

There is also a direct DSP8 form. Put

```text
Kbar_(L),25^c=2 sum_(t in class c,P(t)>=25)
                       N_6^disj(t)rho_L(t),
Sbar_(L),A=sum_(t antipodal,P(t)>=25)rho_L(t).
```

Then C36' follows from

```text
10Kbar_(L),25^0+17Kbar_(L),25^A
 +68Sbar_(L),A+(867/4)n^(5/3)
 <=750n^2-(85L/2)(n-1)^2-255(n-2)^2.              (ADC8)
```

For every `1<=L<=11`, the target-independent condition

```text
160(10Kbar_(L),25^0+17Kbar_(L),25^A)
 <=(76599-6800L)n^2                                (ADC9)
```

is sufficient. At the diagonal choice `(L,E)=(11,6)`, its right side is
`1799n^2` and every retained target has `P>=25,R>=12`.

Every positive selected summand in `(ADC6)` has `P>=33,R>=4`. It supplies a
pure disjoint-distance-six star with seven leaves in the antipodal-free class
or five leaves in the antipodal class. Let `J_E` be the ideal generated by its
normalized star differences, choose four distinct quotient lifts
`Q_0,...,Q_3`, and relabel them so that `lambda_0` is nonzero. At least three
of the four normalized product-to-quotient couplings are nonzero. If
`theta_(0,i)` is the normalized quotient collision between `Q_0` and `Q_i`,
then

```text
I_E^batch=(J_E,lambda_0,lambda_1,lambda_2,lambda_3),
I_E^anchor=(J_E,lambda_0,theta_(0,1),theta_(0,2),theta_(0,3)),
I_E^batch O[1/2]=I_E^anchor O[1/2].               (ADC10)
```

Both forms retain all four quotient lifts, have the same valuations at every
official odd row prime, and have norm divisible by the selected row prime.
Thus the fixed-order generator may use either the symmetric coupling batch or
the separated product-star / coupling-anchor / quotient-star normal form.

This theorem supplies no estimate for `(ADC6)` or `(ADC8)` and no
official-order template generator.
