# Upstream crosswalk - positive value-coset certificate

For the upstream base-field-normalized split-pencil census, this is an exact
finite value-side certificate. It uses only root products, the order
`4(p+1)` subgroup, and Frobenius quarter classes. Two official
characteristics lose every positive-valuation cubic; the other two acquire
the printed invariant `a^3+8b^2=0`.

This is suitable for an `(A)` / split-pencil theorem packet with both exact
replayers. It is not a `nu=0` result, full h=3 exclusion, exact SPI count, or
finite MCA payment.
