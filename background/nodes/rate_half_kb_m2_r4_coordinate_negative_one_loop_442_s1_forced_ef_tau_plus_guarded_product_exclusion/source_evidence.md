# Source evidence

The forced-colored node supplies the checked cubic-field solver.  The sign
classifier supplies the two forced `EF` record orbits, and the packet guards
require every target representative to be nonzero.
