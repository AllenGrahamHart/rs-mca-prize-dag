# Lineage

This node extends the sparse solver to the forced full-pair record.  It is
the first deployed cell deletion here whose exact certificate is a forbidden
coordinate hyperplane rather than the raw unit ideal.
