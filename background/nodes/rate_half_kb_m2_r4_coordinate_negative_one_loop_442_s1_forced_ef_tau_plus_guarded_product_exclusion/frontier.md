# Frontier

Replay the two opposite-parity forced-`EF` cells, then treat the two
forced-loop cells using a quadratic extension of the common algebra.  Keep
raw-unit and guard-saturated deletions distinct.
