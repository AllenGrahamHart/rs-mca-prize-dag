# Audit

1. Both choices of forced `EF` sign and both cubic components are replayed.
2. The conclusion is guard saturation, not a raw unit ideal.
3. The one-term polynomial is checked exactly as `e`, whose exponent is
   `(0,1)` in the solver's `(d,e)` coordinates.
4. Each verifier handles one cubic component and remains below 60 seconds.
5. No opposite-parity or common-sign transport is counted.
