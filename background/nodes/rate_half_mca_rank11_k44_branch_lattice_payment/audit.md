# Audit

1. The 27 parent leaves are reconstructed exactly.
2. Exactly two parent leaves are removed and each is replaced by five
   support-six children, giving 35 leaves.
3. Every child retains all caps from its support-five parent.
4. Every terminal support-six source ceiling and valid carrier cap is
   intersected with the inherited caps.
5. All support deficits `C(11-d,2)` for `2<=d<=9` are retained.
6. The maximum premium is independently identified as nested defects two/two.
7. Every kernel corank and rank-nine core offset is retained.
8. Demand subtracts `C(n,11)` once and both endpoint signs are positive.
9. `K'=45` is independently replayed as a negative control.
10. No rank-eight or chronology target is declared paid.
