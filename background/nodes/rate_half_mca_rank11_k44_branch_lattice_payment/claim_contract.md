# Claim contract

This node changes only the weighted sparse completion premium at `K'=44`.
It reuses all kernel coranks, the all-core rank-nine chart, all 55-shadow
deficits, and the sharp isolated-incidence lower bound from the preceding
payment.

Only two parent leaves are refined.  Each is replaced by five exhaustive
support-six children, all of which retain their support-five cap.  Every new
cap is intersected with the preceding supportwise cap before weighting.

The node closes only the rank-nine component target at `K'=44`.  It does not
pay a rank-eight alternative, close rank eleven, move active-v4 mass, or
assert closure at `K'=45` or later.
