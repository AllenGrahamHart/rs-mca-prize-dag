# Probe results

At `e=7,d_A=1`, the smooth cyclic `28 x 21` incidence ledger has weld rank
`28` over both `F_337` and `F_421`, so the weld stage alone excludes it. One
hundred deterministic degree-preserving switch trials per field also all had
rank `28`.

This is falsification calibration only. The trials are not exhaustive and do
not prove the official rank outcome.
