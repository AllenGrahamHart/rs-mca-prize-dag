# Frontier

Start with 433.  Substitute the two colored values from `(KBP3N-5)` into
the three equations `(KBP3N-6)`, clear `N_den`, and reduce modulo the
corresponding common residual.  A unit ideal closes that lane; a survivor
must be back-substituted into all seven original quotient-label equations.

For 442, impose `(KBP3N-7)` before the three remaining norm rows.  Do not
replace the two final rows by only their product unless the lost ratio is
separately recovered.
