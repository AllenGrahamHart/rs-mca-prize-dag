# Claim contract

## Proved

1. The equal-block near-Johnson Gram-rank cap `(GR1)` over every field,
   because its proof is real incidence linear algebra.
2. The two-threshold sparse-direction compiler `(GR2)`.
3. Every support in the KoalaBear strip `63909<=e<=64037` and Mersenne
   strip `65237<=e<=65418` is paid.
4. The exact endpoint and adjacent denominator or budget outcomes.

## Not proved

1. A payment at `e=64038` or `e=65419`.
2. Sharpness of the Gram-rank cap.
3. Any theorem for the remaining middle-support cells.
4. A complete MCA or prize result.

## Promotion rule

Keep `PROVED` only while both verifiers accept the source pins, matrix-rank
argument, complete finite-strip scans, exact adjacent records, and hostile
mutations.
