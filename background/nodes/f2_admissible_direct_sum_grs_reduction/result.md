# Result

The old 16-rung F2 tower is replaced on every official admissible row by at
most four explicit prime-field GRS kernels. At the banked maximal witness,
one class has

```text
[S,S-R,R+1]_p,
S=2^38,  R=4,294,967,340,
```

and the full ternary mass is `Z_1^C` with `C<=4`.

Modal app `ap-gc4EOdiUFEghRR4qkIjUfX` replayed the canonical verifier with
`373 PASS`, `0 FAIL`, digest `F2_ADM_ALL_PASS`. No consumer status follows:
the explicit `Z_1` bound is still open, and `(O1)` is false on admissible
non-generating rows.
