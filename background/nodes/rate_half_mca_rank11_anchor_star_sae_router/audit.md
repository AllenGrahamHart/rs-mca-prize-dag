# Audit

## Semantic checks

1. The 31 anchors use fixed original supports and locators. Tuple-dependent
   maximal-support witnesses are not used in the coherence step.
2. For `t<=6`, only filler records are omitted from the core-forcing deck.
   For `t>=7`, one doubled basis pair becomes single; no basis pair vanishes.
3. The resulting worst case has seven single cores, giving the strict
   `K-2214` bound without assuming cross-pair exception disjointness.
4. Every star tuple retains all eighteen dense-pair slopes and the fixed
   off-line explanation, so non-affinity and degree `18..31` are uniform.
5. Exact-support cancellation lifts certificates back to the same fixed
   anchor locators, which is the hypothesis needed by collision rigidity.
6. Maximal common support is checked before the v4 S/A/E partition. A
   punctured-domain output is retained as `(C)` and is not called a deployed
   primitive spread core.
7. The v4 taxonomy is used only as a route partition. No terminal is treated
   as paid.
8. The near-rational set remains disjoint and separately priced.

## Executable controls

The primary verifier checks all ten anchor schedules, the strict common-core
bound, star coverage, and the official 31-overlap near-sunflower threshold.
The independent audit reconstructs the schedules from the slot rule and
checks projective-certificate normalization on a finite-field control.
