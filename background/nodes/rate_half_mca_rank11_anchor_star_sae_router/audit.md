# Audit

## Semantic checks

1. The maximal common core is intersected over the complete selected
   post-near family and canceled once. No record-local core forest is summed.
2. S/A/E is used only when that line-global core is empty, so every star
   tuple remains on the original deployed domain.
3. Deviations from the dense pair line lie in the ten-dimensional
   explanation direction code. Actual records, not abstract vectors, are
   chosen as a deviation basis.
4. The maximal-support intersection of the dense records and deviation basis
   is proved equal to the complete line-global core. Fillers only shrink it.
5. Every star tuple retains eighteen dense-pair slopes and a nonzero basis
   deviation, giving uniform non-affinity and degree `18..31`.
6. Fixed exact supports and locators are used throughout the zero-core star,
   which is the hypothesis needed by collision rigidity.
7. The v4 taxonomy is used only as a route partition. No terminal is treated
   as paid, and `(C)` is not folded into `(E)`.
8. The near-rational set remains disjoint and separately priced.

## Executable controls

The primary verifier checks every deviation dimension `1..10`, the 31-slot
deck, the line-global route labels, and the official 31-overlap
near-sunflower threshold. The independent audit reconstructs the deck and
checks projective-certificate normalization over GF(17).
