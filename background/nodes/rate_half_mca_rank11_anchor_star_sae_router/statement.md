# Rank-eleven 31-anchor C/S/A/E globalization router

- **status:** PROVED
- **scope:** the complete deployed KoalaBear post-near error-rank-eleven
  family on one received line
- **output vocabulary:** line-global common core `(C)` followed by Grande
  Finale v4 `(S)`, `(A)`, and `(E)`

Apply the proved whole-line global-core router once to the complete selected
post-near family. The family is not globally affine. Hence its complete
maximal common support `C_*` has size below `K`.

- If `C_*` is nonempty, the entire slope set enters one identity-fiber
  shortened family. This is the explicit line-global residual **`(C)`**.
- If `C_*` is empty, there is a fixed set `A_*` of `31` actual records, with
  their original explanations and exact support locators, such that every
  tuple `A_* union {z}` has empty maximal common agreement and slope degree
  `18..31`.

The fixed deck contains eighteen slopes owned by one minimizing pair and
actual records whose deviations from that pair line form a basis of the
at-most-ten-dimensional explanation direction space. Its maximal support
intersection is exactly `C_*=empty`; fillers bring the deck to 31 records.

Consequently, in the zero-global-core branch, the complete post-near family
routes with one received-line owner and original slope/support labels into:

- **`(S)`**: one anchor-star tuple contains a first-match primitive spread
  core on the deployed domain;
- **`(A)`**: outside named exceptions, all root-free rational certificates
  are projectively identical and form one coherent rational atom;
- **`(E)`**: a pure-locator, denominator-root, extension, quotient, clone,
  field-drop, nonprimitive collision, or quantified near-sunflower branch is
  present.

The intrinsic near set of size at most `2w=134944` remains a disjoint charge.
This theorem proves the exact route reduction `(C)` or `(S)/(A)/(E)`. It
does not pay the line-global shortened family or any S/A/E terminal.

## Falsifier

A non-global-affine family with global common support at least `K`; a
deviation outside the selected basis span; a zero-global-core anchor deck
whose maximal supports intersect; a target slope not covered by an
anchor-star tuple; loss of a fixed support locator; a degree-below-18 target
tuple; two distinct primitive root-free certificates sharing the 31 fixed
records without a collision/near-sunflower exception; or deployment of S/A/E
inside the nonempty global-core branch.
