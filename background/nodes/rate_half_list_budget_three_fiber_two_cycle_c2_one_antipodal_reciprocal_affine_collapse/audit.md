# Audit

1. The Frobenius sign is proved from `p=7 mod 8`; it is not an assumed
   complex-conjugation analogy.
2. Both affine images and `y` must be in `mu_N`. Using only one image does
   not yield `uv=y`.
3. The excluded solution `y=1` is exactly the forbidden repeated infinity
   root, not a discarded torsion candidate.
4. `R_40=2` is equivalent to `r^N=1` in a field; it is not merely a trace
   necessary condition.
5. A small passing control exists: for `p=31`, `N=32`, the same recurrence
   reaches two. The official interval therefore remains an exact finite
   sieve rather than an analytic emptiness claim.
6. The official screen found no modular hit, including composites. Thus no
   primality test or downstream canonical replay is needed for this shard.
7. The certificate excludes only the exact scope in the claim contract; it
   is not evidence against the fixed-field or degree-deficient chambers.
