# Claim contract

- **Claim:** every pole scheme of `G/H` has length at most the omission
  budget and can be cancelled by one `(1,m/2-1)` biform; four universal
  Forney sections then contradict surface cohomology.
- **Dependencies:** the universal Forney contact section and component-defect
  localization.
- **Output:** exclusion of every strict `A=3`, `e=m` endpoint defect profile
  on the official row.
- **Consumer:** `rate_half_band_crossing_location`.
- **Nonclaim:** no `e>m`, `A=1`, or adjacent-unsafe obligation is closed.
- **Falsifier:** pole-ideal colength above `O`, fewer than `m` interpolating
  sections, an `(1,ell)` biform containing a component, a zero tensor
  product on the reduced curve, or nonvanishing of `(RPI7)`.
- **Replay:** `tools/ramguard tiny -- python3
  background/nodes/rate_half_ca_hankel_endpoint_residual_pole_interpolation_exclusion/verify.py`
  and `verify_audit.py`.
