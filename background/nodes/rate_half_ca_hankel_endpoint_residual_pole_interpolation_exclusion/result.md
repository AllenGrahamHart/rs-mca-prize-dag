# Result

All omission-defect strata at the first strict `A=3` endpoint are closed.
The omission budget is exactly enough to bound the pole scheme of the grid
ratio, but too small to prevent a degree-`(1,m/2-1)` interpolant from clearing
it. The cleared grid section and four universal Forney sections land in a
line bundle with no global sections.

The live strict frontier now starts at `e>m`; no `e=m` endpoint computation
or component classification remains necessary.
