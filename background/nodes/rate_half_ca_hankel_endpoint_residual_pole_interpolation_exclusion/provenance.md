# Provenance

Derived in the Codex resolution worktree on 2026-08-11 by replacing the
clean effective Picard point with the pole ideal of the general grid ratio
`G/H`. The key interpolation dimension is exact on the official even row:
`h^0(O(1,m/2-1))=m>O`.
