# Frontier

Do not formulate the next theorem as "twenty full fibers imply a cyclic or
dihedral domain quotient." The official base field already has at least
`1032` fibers in a non-domain-preserving affine-reflection pencil.

The useful next step must instead exploit one of:

1. the factor-pair ownership and first-match chronology retained before the
   exception-SPI projection;
2. an aggregate bound on all partial pullback pencils attached to the heavy
   ruling; or
3. a larger synchronized packet whose fiber count crosses a genuine
   subgroup-incidence classification threshold.
