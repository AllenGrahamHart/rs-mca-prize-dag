# Audit

1. The average excludes `c=0`; the antipodal reflection contributes exactly
   `N` and is subtracted before pigeonholing.
2. The printed average is exact:
   `(N^2-N)/(p-1)=2064+127/1016`.
3. A fixed point is explicitly removed, leaving at least `1032` squarefree
   two-point fibers.
4. Equal sum and product identify an unordered pair, so distinct reflection
   orbits cannot share a slope.
5. The nonzero reflection is proved not to stabilize the whole domain.
6. The independent checker reconstructs six fibers for `c=-1` in the
   order-32 subgroup of `GF(97)`.
7. No critical status changes: the theorem is a route fence, not a payment.
