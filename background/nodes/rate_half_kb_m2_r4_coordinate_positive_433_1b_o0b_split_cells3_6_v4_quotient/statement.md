# O0b split cells 3 and 6 outside quotient

- **status:** PROVED
- **scope:** all six split-principal lanes, four source-sign rows, and 105
  outside labels in each of role cells 3 and 6

The proved simultaneous `B<->C`, `E<->F` involution exchanges cells `3` and
`6`, fixes the first source-root sign, flips the second, and transports every
outside label. It commutes with the proved `d->-d` action in `S0` and with
the identical-record action in `SDE/SDF`.

On the complete `2*6*4*7*15=5,040` systems, the exact orbit profiles are

```text
S0:       72 size-two + 384 size-four orbits =   456 representatives
SDE/SDF: 240 size-two + 720 size-four orbits =   960 representatives
total:                                         1,416 representatives.
```

A committed set of 24 representatives covers all 56 coarse
lane-orbit/outside-sign/first-source-sign/missing-record pilot strata.

This theorem excludes no outside system and does not close cells `3/6`,
O0b, K3, MCA, LIST, or either Prize problem.

## Falsifier

A failed full-system transport, noncommuting action, uncovered raw tuple,
incorrect orbit profile, or mismatch with the committed representative list.
