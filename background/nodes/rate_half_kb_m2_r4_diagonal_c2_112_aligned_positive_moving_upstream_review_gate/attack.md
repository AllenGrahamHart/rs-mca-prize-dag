# Attack

1. Retain the exact commit and certificate pins for PR #1144.
2. Isolate the `M01-R11` q-slice basis and both `J/I` augmentation stages.
3. Avoid Sage's external Singular string-return path: emit a direct Singular
   certificate, use a bounded in-process backend with progress checkpoints,
   or provide a smaller elimination proof of the same named-open emptiness.
4. Bind every alternate result to the upstream generators, named units,
   basis/localizer metrics, and exact index-two nilpotence terminal.
5. Replay the existing complete `M01 -> M02` literal inversion.
6. Promote only after an independent reviewer reproduces the two-cell exact
   conclusion without an exception or timeout.
