# Attack

1. Retain the exact commit, source, certificate, and successful output pins.
2. Replay the direct Singular bridge with the exact `interred` same-ideal
   transformation and all 148 deterministic `I` blocks.
3. Check every source polynomial fingerprint, q-slice/`J` dimension and size,
   final `I` metrics, all 20 named units, and localizer-square-zero terminal.
4. Retain the failed monolithic routes as performance fences, not evidence.
5. Replay the existing complete `M01 -> M02` literal inversion.
6. Reopen the node on a missing block, changed pinned source, failed exact
   terminal, or a flaw in the same-ideal remainder induction.
