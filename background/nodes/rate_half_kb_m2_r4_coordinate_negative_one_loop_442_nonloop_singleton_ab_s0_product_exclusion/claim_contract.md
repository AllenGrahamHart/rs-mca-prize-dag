# Claim contract

| field | content |
|---|---|
| claim | All six canonical `S0` product cells are empty in both `b` rows and both `c` signs. |
| assumptions | The cells `[3,6]` product involution, outside sign census, sparse sextic equations, and nonzero outside guards. |
| exact interface | Complete `S0` and preceding `S2` product deletion; only `S1` remains. |
| nonclaim | `S1`, outside q/interpolation, another common orbit, coordinate or row closure, or either Prize theorem. |
