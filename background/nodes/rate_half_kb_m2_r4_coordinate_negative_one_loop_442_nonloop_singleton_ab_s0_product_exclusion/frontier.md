# Frontier

Evaluate the eight rational `S1` forced-record cells, then handle the two
forced-loop cells using the deployed square roots of `-m`.  A complete
`S1` deletion closes this common orbit at product level.
