# Result

PROVED. Profile `(3,4,0)` at `N=256,s=5` can collide only in the positive
even autocorrelation-variance range `V<=134`. The zero-variance and all
`V>=136` cases are excluded.

The low-variance residual and the parent pointwise collision target remain
open.
