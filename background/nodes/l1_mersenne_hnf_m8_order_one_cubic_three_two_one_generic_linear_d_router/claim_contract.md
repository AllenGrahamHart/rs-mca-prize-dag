# Claim contract

| field | value |
|---|---|
| claim | On the generic determinant chart, `E_6` makes the remaining coefficient equations a quadratic pair in `d` whose exact combination is linear; the nonzero coefficient chart eliminates `d`, and the zero coefficient chart gains two parameter equations. |
| status | PROVED |
| dependency | `l1_mersenne_hnf_m8_order_one_cubic_three_two_one_coefficient_matrix_router` |
| consumer | `l1_mixed_petal_amplification` |
| equivalence guards | Inherited `q*d*(q-d)*D*K_6*R*S!=0`; generic `Delta!=0`, which also forces `W!=0`; `C_1!=0` only on the rational chart. |
| open residue | Unit or retained-point verdict for both `C_1` charts and all 21 role alternatives; every norm, exact-fiber, Frobenius-converse, cyclotomic, and inner-lift filter. |
| nonclaim | Neither chart is empty, and the vanishing `C_1` locus is not discarded as a denominator component. |
