# Upstream crosswalk

This is a finite-track primitive shift-pair coefficient ledger for the
official `h=7`, cubic `3+2+1` packet. It replaces the generic four-variable
elimination request by a three-variable chart plus an explicitly retained
exceptional coefficient locus. It proves no asymptotic quotient-flatness or
split-pencil census.
