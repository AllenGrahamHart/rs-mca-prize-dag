# Proof

The matching-resultant theorem says the original conjunction
`q4=q5=q6=0` is exactly the union of eight charts. Existing nodes exclude
`III`; the three one-finite charts `FII,IFI,IIF`; the two collapsed charts
`FFI,FIF`; and `IFF`. The new complete chart node excludes `FFF`. The union
is therefore empty.

The quotient script proves that the canonical tuple is a representative of a
size-four orbit:

```text
(3,S0,-1,-1,-1,2,0)  (3,S0,-1,-1,-1,3,1)
(6,S0,-1,-1, 1,4,1)  (6,S0,-1,-1, 1,5,2).
```

Equivariance transports emptiness to all four cases. The earlier basis-case-0
theorem closes a different representative, so exactly two of 1416 are now
closed. QED.
