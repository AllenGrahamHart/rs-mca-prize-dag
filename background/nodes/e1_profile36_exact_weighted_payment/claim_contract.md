# Claim contract

## Consumed content

- The weighted-kernel dictionary supplies the exact profile multiplicities
  and oriented-to-unordered factor `1/2`.
- The complete `(4,2,S=18)` exclusion removes the former heaviest profile.
- The high-cofactor and cofactor-`2` height collapses jointly supply
  `T_36(p,r)<=4`.

## New proved content

1. The exact maximum charge for profile `(3,6,S=18)`.
2. The exact residual binding-row edge budget.
3. The identification of `(2,10,S=18)` as the largest remaining profile.
4. The tight residual uniform sufficient cap `|D_res|<=104955`.

## Guards

- The payment is for one fixed official row and its quotient root.
- It restores all 256 oriented vectors per shift/sign orbit and the
  dictionary's division by two.
- It does not assert that four profile-`(3,6)` orbits exist.
- The uniform residual cap is sufficient, not necessary; the exact weighted
  sum may close with more vectors.
- No lower profile is counted or excluded here.

