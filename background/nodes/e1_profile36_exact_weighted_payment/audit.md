# Audit

- The orbit size is restored as `256`; no normalized orbit is counted as one
  vector.
- The dictionary's final division by two is retained, giving the factor
  `128` in the profile charge.
- The old maximum profile `(4,2)` is removed only through its proved complete
  exclusion.
- The profile `(3,6)` is paid at its upper bound before the residual budget is
  formed.
- All 271 eligible profiles are recomputed from the dictionary formula; no
  hand-maintained ranking is consumed.
- Exact integer inequalities certify both sides of the tight cap boundary.
- The node changes no critical status and does not claim E1 closure.

