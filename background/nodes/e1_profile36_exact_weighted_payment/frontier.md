# Frontier

The maximum-weight profile is fully paid and must not be revisited. The next
profile is

```text
(a,b,S)=(2,10,18),
M_33=1227527050040565145269313275179180544.
```

Its ten singleton coefficients allow the previously absent local valuation
`mu=7`; the pure cofactor list can therefore include `128` in addition to
the powers already seen. The immediate structural task is to derive the
exact cofactor/prime-ideal router for this profile and separate:

1. pure powers of two, where the proved entropy-height collapse is expected
   to bound each fixed cofactor by one orbit; and
2. cofactors with an extra split prime above `257` or `769`, where distinct
   prime ideals must not be merged into one unit-associate family.

No broad support-12 enumeration is authorized before that ideal partition is
proved and priced.

