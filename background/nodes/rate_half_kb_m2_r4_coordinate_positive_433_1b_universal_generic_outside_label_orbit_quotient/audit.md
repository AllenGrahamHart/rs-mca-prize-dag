# Audit

- The theorem is universal within `433-1b -> O0a` because both record
  involutions fix all O0a common role cells; no cell-12 geometry is used.
- The route qualifier is load-bearing: neither generator is universal on
  `433-1b -> O0b`.
- Matching indices are recomputed after deletion and compact relabeling.
- Endpoint and non-endpoint subsets are unions of whole orbits.
- The quotient is not an exclusion theorem.
