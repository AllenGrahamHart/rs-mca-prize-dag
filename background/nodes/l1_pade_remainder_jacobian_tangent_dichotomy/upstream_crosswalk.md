# Upstream crosswalk - Pade Jacobian/tangent dichotomy

## Base-field-normalized split census

The arbitrary-word list-interior section is the low-remainder locus
`deg(U mod L)<k`.  Its differential is multiplication by the recovered
cofactor modulo `L`.  On the primitive locus `gcd(L,Q)=1`, this is an
automorphism, proving the expected codimension without a genericity
assumption or a field-size exception.

## Tangent owner

All possible rank collapse lies on `Res(L,Q)=0`.  For exact shells the common
factor has domain roots only inside the agreement support because the
complement gcd guard is already enforced.  This is a literal tangent/common-
factor first-match owner in upstream terminology, not a new amorphous
effective-image-collapse class.

## Remaining statement

After tangent and quotient payments, the upstream-compatible open target is a
row-sharp split-point bound on a smooth codimension-`w` low-remainder section.
Smoothness alone is not a point-count theorem, so this packet should be
vendored as a reduction and scope guard, not as closure of `U_list-int`.
