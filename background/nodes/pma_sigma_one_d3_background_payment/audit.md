# Audit - PMA sigma-one defect-three background payment

1. The exact background state is load-bearing. Dropping `r=1` changes
   `kappa` from three to four and the ambient aggregate from order `n^6` to
   order `n^7`.
2. The factor `binom(k-1,3)` restores every defect set exactly once.
3. The theorem is per maximal sunflower. Its `n^6` scale leaves only a
   constant chart multiplicity unless a stronger global owner deduplicates
   the cell.
4. The result counts periodic members and therefore remains an upper bound
   after the global periodic owner.
5. Failure of the analogous `r=0` paving census to fit is a route cut only;
   it is not a lower bound on that realized source class.
