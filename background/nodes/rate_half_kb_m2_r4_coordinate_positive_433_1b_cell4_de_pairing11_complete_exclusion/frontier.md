# Frontier

The theorem pays `(xi,pairing)=(0,11),(1,11),(2,11)`. The parallel-`DE`
matching involution separately transports `(2,11)` to `(2,14)`, but it does
not pay `(0,14)` or `(1,14)`. Those positive-`DE` matching-14 cases are the
next exact target in this orbit.
