# Result

The core-one `A=1` prefix is unconditionally empty through
`e=floor(16m/13)-1`. The first surviving official degree moves from
`164926744166` to `169155635042`, and every later survivor satisfies
`ell>=e-3-floor(Delta/5)`.
