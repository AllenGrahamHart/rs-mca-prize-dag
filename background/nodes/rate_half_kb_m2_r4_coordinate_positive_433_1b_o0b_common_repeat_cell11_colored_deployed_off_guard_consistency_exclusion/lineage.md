# Lineage

The common-kernel reconstruction left colored missing records outside the
quartic-plus-one-free-endpoint model used for `DE+`, `DF+`, and `EF`.  This
node uses the known common endpoint directly: product reconstruction fixes
the colored endpoint, and squared-sum reconstruction supplies a single
source-algebra consistency element.

