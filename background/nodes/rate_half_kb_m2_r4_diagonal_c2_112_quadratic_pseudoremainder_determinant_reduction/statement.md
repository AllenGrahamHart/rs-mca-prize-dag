# Statement

Let `D(w)=a w^2+b w+c`, let `P(w)` have degree four, and let three
pseudo-division steps give

```text
a^3 P = Q D + H,                 H=r1 w+r0.                 (QPR-1)
```

On a quadratic reconstruction chart with `Vw+U=0`, define

```text
Delta_D(P) = V r0-U r1.                                  (QPR-2)
```

On `aV!=0` and `D(w)=0`, one has

```text
P(w)=0  <=>  Delta_D(P)=0.                                (QPR-3)
```

For the literal `F04-R02` degree-12 branch, applying this construction to
`A1,B1` with divisor `B0` factors both determinants as transported named
units, a common irreducible degree-8 leading factor `K8`, and irreducible
cores of degree 34 with `3201` and `2937` terms. Thus on the named-open
`V K8!=0` chart the former essential rows may be replaced exactly by those
two degree-34 cores.

The ideal generated over `F_2130706433` by the selected degree-12 resultant
factor and the two cores has a 142-element, dimension-one Groebner basis.
It is not an algebraic-closure emptiness certificate.
