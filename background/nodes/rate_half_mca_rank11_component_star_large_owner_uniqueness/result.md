# Result

The full-rank component-star route now has a canonical recordwise owner key.
The choice of ten-subset cannot create two competing owners below deficiency
`22320`.

The unresolved step is aggregate: count or chronology-route the distinct
keys across records while retaining their component-incidence weights.
