# Dependency sub-DAG - M31 quotient-rotation product spectrum

```text
rate_half_cyclic_rotated_prefix_floor
  --req--> rate_half_m31_adjacent_quotient_rotation_product_spectrum

rate_half_m31_adjacent_quotient_rotation_product_spectrum
  --ev--> rate_half_list_adjacent_crossing
  --ev--> l1_mixed_petal_amplification
```

The evidence edges record a sharp lower floor and route calibration only.
