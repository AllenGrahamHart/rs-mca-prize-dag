# Claim contract - M31 adjacent quotient-rotation product spectrum

## Proved

- exact specialization of the cyclic quotient-rotation construction to the
  M31 adjacent list agreement;
- exact spectrum of all 32 normalized product classes;
- exact structured lower floor `8,287,155`;
- exact comparison with the `2^-100` stress-row budget `16,777,215`.

## Not claimed

- no upper bound on the complete prefix fiber or ordinary list;
- no proof that the displayed construction exhausts any first-match atom;
- no `U_Q`, active-partition payment, MCA slope count, adjacent safety, or
  Prize endpoint movement;
- no use of this auxiliary `2^-100` row as a `2^-128` Prize row.
