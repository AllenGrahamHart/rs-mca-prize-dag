# Result

The `m=16` branch of the leading prize profile is reduced from 22 variance
chambers to the 13 chambers

```text
V in {10,18,26,...,106}.
```

This is a strict analytic narrowing. At this stage the `m=16` branch, the
other live cofactors `m=2,4`, and the aggregate E1 weighted edge budget remain
open. The proved exhaustive child subsequently excludes the bounded `m=16`
residual.
