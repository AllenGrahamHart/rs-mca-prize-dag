# Attack record

The `K'=42` full-deficit ledger was only `1.3251` percent above the needed
weighted premium.  Its support strata were capped independently, although a
near-saturated completion family spans most of the single global
annihilator.

The cross-support carrier retains that shared span.  Source support five is
the useful specialization: defects zero through four force progressively
smaller target-support carriers, while the complementary branch lowers the
support-five completion ceiling to `q-5`.

The invalid shortcut of subtracting received-word equations from the
quotient dimension is not used.  Only sparse label representations inside
the exact Vandermonde independence range are compared.
