# Result

The quotient-lift index in the derivative-ideal route is no longer
mathematically essential for evaluating one row. Once the two global
resultant polynomials are available modulo `p`, layered gcd degrees recover
the complete `X_18` and the target-sensitive double-accident term `Y_18`
exactly. The next algorithmic target is a succinct way to compute or bound
those degree sums without materializing degree-`(n-1)^2` polynomials. Only
the first `B_n=O(n^(2/3))` layers can be live on official rows, and each can
be certified independently by exact division and a Bezout identity.
