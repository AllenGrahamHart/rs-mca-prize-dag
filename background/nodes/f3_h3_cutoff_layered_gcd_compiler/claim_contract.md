# Claim contract

- **claim:** exact layered polynomial-gcd formulas recover `X_c` and `Y_c`
  from the global product and quotient polynomials;
- **scope:** prime-field dyadic rows with `p>=n^2`, and every cutoff
  `0<=c<n-1`; the critical specialization is `c=18`;
- **consumer:** the fixed-order and double-accident routes supporting
  `f3_h3_mobius_excess_half`;
- **dependencies:** the exact global resultant representation and the
  definition of the cutoff-18 double-accident split, plus the official
  uniform product-fiber bound for the `B_n` layer cap;
- **falsifier:** a row/cutoff for which either degree sum in `(LGC3)` differs
  from its direct weighted multiplicity moment;
- **nonclaims:** no upper bound on `X_18` or `Y_18`, no universal C36' close,
  and no official-scale complexity claim;
- **upstream mapping:** exact residual-ray/shift-pair multiplicity compiler,
  retaining the product-richness and quotient-depth correlation.
