# Frontier

The cofactor-`514` branch now has the middle energy window

```text
E=5,...,13.
```

The next useful split is by integral autocorrelation shape and local
multiplicity two. Before any coefficient-support census, screen the signed
autocorrelation targets at a fixed primitive root modulo `257`; then use
modular resultants only on the compatible Galois types. The cofactor-`1028`
branch separately remains at energy four.
