# Claim contract

- **Claim:** the exceptional CRT degree collapse is equivalent to the
  `2e-5` explicit dual-RS moments `(QBM2)`, or to their base-field-normalized
  pair-trace form `(QBM3)`.
- **Scope:** the official `e>=3` pair-Lagrange distance-three chart after
  construction of the first-jet polynomials `N_i`.
- **Dependency:** quartic boundary CRT reconstruction.
- **Consumer:** `rate_half_band_closure` and `CR-003-CLIFT`; streaming exact
  certificate interface for the live generic saturated branch.
- **Falsifier:** a valid exact design with a nonzero moment `M_j`, or a
  boundary vector passing every moment but having interpolation degree above
  four.
- **Nonclaims:** no specialized algebraic independence of the moments, no
  coefficient-rank bound, no line decomposition, and no external-quartic
  splitting.
- **Verification:**
  `tools/ramguard tiny -- python3 background/nodes/rate_half_ca_hankel_a1_core_one_exceptional_only_distance_three_quartic_boundary_dual_moment_gate/verify.py`
