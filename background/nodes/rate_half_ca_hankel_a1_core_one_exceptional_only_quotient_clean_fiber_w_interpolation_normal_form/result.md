# Result

The high-distance corrected square no longer needs a freely allocated
`W_vee` biform. Clean first jets determine its residue modulo `P_cl`; the
remaining endpoint classification has only two univariate correction
polynomials before the exceptional and Hankel gates are imposed.
