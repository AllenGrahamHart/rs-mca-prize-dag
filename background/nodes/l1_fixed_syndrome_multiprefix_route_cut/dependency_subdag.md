# Dependency sub-DAG

```text
l1_fixed_syndrome_multiprefix_route_cut [PROVED]
                       |
                       ev
                       v
l1_mixed_petal_amplification [TARGET]
```

The theorem is proved directly. Upstream PR `#1041` is provenance and a
broader deployed census, not an unproved DAG premise.
