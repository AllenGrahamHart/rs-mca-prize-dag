# Result

Every shape-A off-diagonal component is arithmetically visible over the
base field, and the subgroup geometry rules out all image degrees below
`39,768,216`. The remaining component route is necessarily macroscopic;
closure now requires a source/scalar-weld restriction on those high-degree
components rather than another low-degree torus classification.
