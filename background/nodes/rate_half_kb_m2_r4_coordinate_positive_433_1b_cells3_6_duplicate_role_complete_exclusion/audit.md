# Audit

- The common product, weighted-sum, and quotient-label rows are compared as
  exact sparse integer polynomials for all four source signs.
- The second source sign flips because the `BC-` target sum changes sign; the
  first source sign is fixed.
- The target gauge on `D,E,F` restores all seven canonical active-edge signs
  and fixes both cycle invariants.
- The outside atlas swaps exactly `bf` and `sigma_c cf`, in both product and
  squared-sum rows.
- The full target guard divisor is fixed up to units.
- Exact canonical matching enumeration proves a bijection on all 105
  missing-record/matching cases and all 1,680 signed principal cases.
- Cell 6 is separately present in the rank-drop classifier's unit-ideal list.
- The proof explicitly distinguishes complete-cell transport from the earlier
  invalid attempt to use `B,C` exchange as a within-cell matching shortcut.
