# Claim contract

- Exact row only: `K'=79`, `q=69`, `m=67551`, `n=1048655`.
- Exactly the 19,406 above-ceiling tuples represented by the canonical digest
  are rerouted through the complete pairwise atlas.
- Every carrier, high-support, and geometry branch is retained.
- Counts and comparisons are exact integers; no sample is used.
- This node closes `K'=79`; it makes no claim about `K'=80`.
