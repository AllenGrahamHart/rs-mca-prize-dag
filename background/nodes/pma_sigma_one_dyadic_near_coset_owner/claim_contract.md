# Claim contract - PMA sigma-one dyadic near-coset owner

## Inputs

1. The chart-free interpolation owner proved at the index-two scale.
2. Cyclic uniqueness of every subgroup of order `L|n`.
3. The official dyadic `sigma=1` row range and `Q_2(k+2)` profile.
4. The sharper exact-periodic global count.

## Output

One global first-match owner for every listed codeword missing at most
`s_n=floor(n/(32 log_2 n))` points of any dyadic coset of size `L>=k-s_n`,
with total count below `Q_2(k+2)`. Together with exact periodicity, the owner
uses less than eight of the available 719 first-scale units.

## Consumer effect

The finite diffuse theorem may restrict to the complement of every dyadic
quotient core in the growing miss window, not merely the explicit index-two
construction. This pays near-quotient portions through `d=s_n-1` in the worst
background state; the far-from-coset residual, larger defects, and all unowned
low-boundary closures remain open.

## Falsifier

An owner collision, an eligible official row or scale violating the printed
size conditions, or an official all-scale count reaching `Q_2(k+2)`.
