# Dependency sub-DAG - PMA dyadic near-coset owner

```text
pma_sigma_one_index_two_core_owner [PROVED] --------+
                                                       v
pma_sigma_one_dyadic_near_coset_owner [PROVED]
                              | req                    | ev
                              v                        v
              petal_mixed_amplification          pma_wide_residual [REFUTED]
```

The theorem extends the paid source predicate from the four-defect index-two
core to every eligible dyadic quotient coset with at most
`floor(n/(32 log_2 n))` misses. It remains one global first-match class and
does not create a chart-local payment.
