# Result

Arbitrary-`W` rank-two dual planes now split into:

```text
five blocks: augmented-orthogonal;
four blocks, eta=0: augmented-orthogonal;
four blocks, eta!=0: perfect received-pair interaction.
```

The remaining dual-plane census must count only these two interaction classes
and retain the full selected-block certificates.
