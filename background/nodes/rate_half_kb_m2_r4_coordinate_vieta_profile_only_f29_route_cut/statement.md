# KoalaBear coordinate Vieta profile-only F29 route cut

- **status:** PROVED
- **scope:** the abstract aligned coordinate source-facet and positive
  common-`K` Vieta interfaces over `F_29`
- **dependencies:**
  `rate_half_kb_m2_r4_order2_coordinate_source_facet_signature`,
  `rate_half_kb_m2_r4_coordinate_k_fiber_vieta_rank_compiler`, and
  `rate_half_kb_m2_r4_coordinate_colored_quotient_resultant_compiler`
- **consumer:** `rate_half_band_closure` as route evidence only

There is an exact aligned abstract packet over `F_29` with

```text
I={+/-1,+/-4,+/-9},       J={+/-2,+/-3,+/-5},
K={1,-1,4,-4,9},          xi=-9,
```

such that:

1. its 24 component stars obey coordinate deck transport, the exact
   `(J-J,I-I,I-J)=(10,10,4)` census, source degree four at every label,
   one diagonal-free two-regular pole graph, all six one-exchange facets,
   four component-colored edges, and complete-source defect two;
2. its five common-`K` edge orbits have paired degree profile
   `(2,2),(4,4),(4,4)`;
3. the geometrically irreducible positive source form

   ```text
   H(T,X)=A_2(W)T^2+A_0(W)+XT B_1(W),       W=X^2,
   A_2=17+9W+18W^2,
   A_0=28+20W+3W^2,
   B_1=24+W
   ```

   has exactly those five edge orbits, has nonzero leading value on every
   point of `K`, and makes the exact `10 x 8` Vieta matrix have rank seven.

Thus the source-facet ledger, allowed profile, defect budget, geometric
irreducibility, and Vieta determinant do **not** by themselves exclude the
coordinate branch.

The full colored quotient gate does exclude this packet. Its `J`-resultant
forces

```text
R_J=K_5^2 c_0,       c_0(W)=W^2-7W+9=(W-13)(W-23).
```

Neither root of `c_0` is a `J` label, while the two colored right vertices of
the packet are exactly `3,26` and prescribe `c_col=(W-3)(W-26)=W^2+20`.
Thus `c_0` is not an admissible colored quadratic. The companion `I` identity
also fails, already at `W=xi=20`.

This is not a deployed-field component, a characteristic-zero construction,
an actual endpoint packet, an orientation deletion, an owner/payment, a row
close, or a Prize result.

## Falsifier

Failure of any printed field equation, source-facet count, defect, rank,
irreducibility test, forced quotient, or support obstruction.
