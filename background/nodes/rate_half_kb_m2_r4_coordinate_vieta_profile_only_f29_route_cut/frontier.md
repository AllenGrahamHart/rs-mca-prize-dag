# Frontier

For the deployed coordinate branch, classify the joint

```text
source-facet packet + defect <=3 + Vieta kernel + colored quotient support
```

system. Apply the small rank gates first, but retain every surviving packet
until the forced quadratic `R_J/K_5^2` is tested for squarefree allowed
support and the companion `I` identity is checked. A profile-only determinant
theorem is now known to be false in a good odd characteristic.

The bounded all-assignment `F_29` census remains deferred because Modal did
not start under the workspace spend limit. It is evidence work, not a
deployed-field closure substitute.
