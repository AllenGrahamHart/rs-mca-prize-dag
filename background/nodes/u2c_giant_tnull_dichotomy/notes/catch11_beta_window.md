# Catch #11: the β-normalized window (node-local, 2026-07-07)

Full derivation: `prize/notes/correspondence/MB_VS_F1_LEDGER.md` §6;
map: `prize/notes/correspondence/REGIME_MAP.md` + regime_map.svg (the
β-jump arrow). The dag statement of this node carries the correction;
this note is the node-local pointer + the arithmetic in one place.

- Frozen wording read the sub-balance window at the ambient field:
  q^t ≥ 2^n.
- At base-domain official extension rows the t-null value space is the
  GENERATED field's: for D = h·H with H ⊆ F_p*, power sums obey
  p_j(S) = h^j·σ_j, σ_j ∈ F_p (proved, one line; scaling conjugacy).
- KoalaBear row (n = 2^21, q = p^6, official-shaped): q-window admits
  at t = 11280 (margin +162 bits) while t·log2 p = 349,552 — above true
  balance by 2^1,747,600; pigeonhole gives a depth-t prefix fiber
  ≥ 2^1,747,589 vs coset unions ≤ 2^182 and budget n³ = 2^63.
- Correction: window = |F_p(D)|^t ≥ 2^n. All hardening evidence
  (F2-A1/A2/A3b + Pro window 2) ran at prime rows where B0 = F —
  untouched. Consumer rule: x4/b2_modp_giant_extras consumes this floor
  only at generated-field-window rows; base-domain extension rows are
  priced by the f1/ext descent (s6 imported window).
- Honest grading: the pigeonhole fiber is at SOME prefix z; the z = 0
  instantiation is supported by the verified above-balance witness
  family (p = 97) and upstream's mode-at-null calibration, and the
  window's justification is proved false regardless — hence a KILL of
  the wording, not of the floor at its corrected scope.
