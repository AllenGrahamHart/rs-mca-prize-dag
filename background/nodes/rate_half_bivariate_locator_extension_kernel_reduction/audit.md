# Audit

1. The number of parity moments is `a-rho-1`, not `a-rho`.
2. The extension rows use `A_x R_x=Q_Y(x)`, so they do not contain the
   representation factor `L_x` present in `M_W`.
3. The denominator `sigma'_W(x)` is nonzero because the points of `W` are
   distinct.
4. The criterion is coefficientwise and remains linear in every deficiency
   clone variable.
5. The genuine `m=1` witness is retained: on all ten canonical pair unions,
   `rank(M_W)=rank(C_W)=5<6` and its all-nonzero kernel passes every new row.
6. The verifier independently checks the generalized Reed-Solomon parity
   rank and the `m=1` control.
