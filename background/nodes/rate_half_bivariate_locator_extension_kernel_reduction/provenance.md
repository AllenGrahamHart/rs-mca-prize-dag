# Provenance

This node arose by adversarially constructing a rational-trace kernel for
`M_W` at `m=2` and then checking the next official locator-curve condition.
The witness failed coefficientwise degree-`rho` interpolation, revealing the
omitted linear layer.

The proof is the standard dual Reed-Solomon evaluation criterion. Numerical
evidence is not used to establish the theorem.
