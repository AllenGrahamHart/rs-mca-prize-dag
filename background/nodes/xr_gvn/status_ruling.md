# Status ruling

The former conditional packet treated the exact `k=2` fixed-slope moment as
though it supplied a defined multi-exchange Cauchy--Schwarz chain and endpoint
inequality. It does not. The edge from `averaged_xr` is evidence-only, and the
broader generalized von Neumann statement is restored to `TARGET`.
