# Claim contract

- **Scope:** official rate half, `M=4,t=2`, and unguarded petal-equation
  codimension exactly two.
- **Forced arithmetic:** `5ell=k+4` and `b=r=s=ell-3`.
- **Input:** two full-petal divisibilities plus the background roots supplied
  by the core-defect normal form.
- **Output:** an exact guarded linear slice of pair and locator dimension
  `ell-1`, hence locator codimension `ell-1`.
- **Retained guards:** monic split-on-core `F`, primitivity, exact
  nonagreements, and first ownership.
- **Nonclaim:** a split-locator count or payment of the aggregate target.
