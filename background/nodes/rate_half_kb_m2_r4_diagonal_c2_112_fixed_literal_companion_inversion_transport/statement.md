# Statement

For each `Rxx` in `R02,R11,R20`, substitution `b -> b^-1` gives exact
isomorphisms of complete named-open systems

```text
F04-Rxx <-> F05-Rxx,       F06-Rxx <-> F07-Rxx.             (KBLIT-INV)
```

The transport includes the reconstructed source, all q-slice equations, both
full colored quotient identities, and every required open factor. Therefore
the two cells in either pair are simultaneously empty. This node does not
assert that any one of them is empty.
