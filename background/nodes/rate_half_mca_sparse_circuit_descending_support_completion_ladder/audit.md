# Audit

1. The source order is fixed as `5,4,3,2`.
2. At source `c`, defects `0..9-c` and fallback `M_c<=q-(10-c)` partition
   every integer maximum in `0..q`.
3. The four terminal counts are `5,6,7,8`.
4. Exactly one all-fallback leaf remains, giving 27 leaves total.
5. Every terminal branch retains all preceding fallback ceilings.
6. Cross-support caps are used only under the Vandermonde union condition.
7. The statement has no finite-row payment or closure claim.
