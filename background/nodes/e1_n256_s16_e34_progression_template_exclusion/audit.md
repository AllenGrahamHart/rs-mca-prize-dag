# Audit

Primary Modal app `ap-i5ZUL3DXjsMVeoSd2KwzT4` completed all five shards with
29.943997 aggregate worker-seconds. Independent app
`ap-x6NGO4WBkgu0GbaGBpeQim` completed the same partition with 50.977832
worker-seconds.

The implementations use unordered signed chords versus ordered negacyclic
multiplication, and explicit four-position membership versus direct circular
distance for the weld. The checker validates source hashes, exact shard and
support coverage, all count/max fields, retained witnesses, and the exact
`32,16,8,4,2` orbit weighting.

Both apps used one CPU, 256 MiB, 60-second function caps, and at most five
containers. The combined 80.921829 worker-seconds remained below the declared
conservative `$0.20` ceiling. No rerun is authorized.
