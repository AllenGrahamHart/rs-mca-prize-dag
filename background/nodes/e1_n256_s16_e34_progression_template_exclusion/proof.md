# Proof

The progression-weld reduction proves opposite outer heavy signs, an exact
four-position weld chamber, and odd-unit transport from all 62 steps to the
five representatives `1,2,4,8,16`. The representative chambers contain

```text
5 * 1,195,965 * 2 * 16 = 191,354,400
```

signed vectors. The orbit multiplicities are `32,16,8,4,2`.

The primary census constructs positive-half autocorrelation coefficients from
the 21 unordered support chords. The audit independently constructs
`F(X)F(X^-1)` in `Z[X]/(X^128+1)` from 49 ordered products and recognizes the
outer-class weld by direct circular distances. The checker proves exact
five-shard coverage and 1,195,965 supports per shard, compares every count and
maximum, replays retained witnesses, and applies the proved orbit weights.

The packets agree on 329,776 representative full-conductor profile vectors,
3,131,008 after exact orbit weighting, and maximum `M_3=1722`. By odd-unit
invariance, this maximum applies to all 62 forms. Every pair-feasible residual
has full conductor by `e1_n256_proper_conductor_collision_exclusion`. The
rational cubic-Hermite certificate inherited from the E34 three-profile
reduction has positive norm margin at `M_3=1947`; hence the progression
collision norm is strictly below `2^250`. The collision-norm criterion and row
threshold exclude every vector in the branch. QED.
