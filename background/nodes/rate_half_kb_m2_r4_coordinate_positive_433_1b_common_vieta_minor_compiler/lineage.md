# Lineage

This node reuses the exact common-minor architecture developed for the
closed `433-1a -> O0b` route.  The new roles have no assumed duplicate-role
quotient, so all fifteen matching cells and four root-sign rows are retained
explicitly.
