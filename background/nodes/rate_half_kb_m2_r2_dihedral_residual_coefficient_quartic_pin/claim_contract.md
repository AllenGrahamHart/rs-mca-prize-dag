# Claim contract

- **Claim ID:** `rate_half_kb_m2_r2_dihedral_residual_coefficient_quartic_pin`
- **Input:** an actual residual full-V4 profile with `n=3` or `n=6`.
- **Output:** after normalizing `h(t)=t^2`, the birational coefficient image
  is exactly the canonical quartic `k(S^2-2P,P^2)=0` pulled back from the
  symmetric sibling `(2,2)` correspondence.
- **Dependencies used:** distinct dihedral reflection involutions, the
  transverse source component, and the residual birational-quartic branch.
- **Not claimed:** that the canonical quartic exists with the required
  singularities, that either residual profile is empty, or that any owner,
  payment, row, or Prize problem is closed.
