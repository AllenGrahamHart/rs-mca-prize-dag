# Audit

- The sibling correspondence uses the two `Y` values over one `Z` value;
  it is not the original ordered `(Y,Z)` outer component.
- Symmetry follows by precomposition with the `Z` deck involution.
- Birationality fails only if that involution normalizes the other
  reflection subgroup, which would reduce to `D_2` and is excluded here.
- The substitution is exact:
  `y_0+y_1=S^2-2P` and `y_0y_1=P^2` under `h(t)=t^2`.
- Equality with the coefficient image uses its already-proved irreducible
  degree four, not a numerical interpolation rank.
- No existence, profile deletion, owner, or payment is inferred.
