# Result

Both residual source coefficient images are pinned to the canonical plane
quartic `k(S^2-2P,P^2)=0` obtained from the symmetric sibling `(2,2)`
correspondence. The remaining task is its rational singularity and V4
source-cover realization; neither `n=3` nor `n=6` is deleted here.
