# Result

PROVED. The four-singleton folded profile `(0,4,0)` cannot collide modulo any
named pair-feasible `N=512` row prime.

The first surviving `N=512,s=2` band is therefore reduced from two profiles to
the single profile `(1,2,0)`. The parent pointwise E1 collision target remains
open.
