# Dependency sub-DAG

```text
saturated source-line census
        |
        v
aligned/near colored quotient partition
        |
        +--> aligned printed cells ---------------+
        +--> near printed cells ------------------+--> source-line exclusion PROVED
        +--> literal-assignment coverage PROVED --+
```

The DAG records the 32 theorem/compiler prerequisites and the proved
literal-assignment coverage theorem as `req` edges. The outgoing edge to
`rate_half_band_closure` remains evidence only.
