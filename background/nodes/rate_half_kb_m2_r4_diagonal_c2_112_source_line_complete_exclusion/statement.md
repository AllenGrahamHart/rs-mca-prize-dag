# KoalaBear m2 r4 diagonal c2 (1,1,2) complete source-line exclusion

- **status:** CONDITIONAL
- **scope:** saturated diagonal `c2(1,1,2)` source-line packets over
  `F_(2130706433^6)`
- **requires:** the exhaustive defect/quotient/reconstruction compilers and
  all aligned and near-aligned sign, allocation, ramification, and boundary
  exclusions listed as DAG requirements, plus
  `rate_half_kb_m2_r4_diagonal_c2_112_source_line_literal_assignment_coverage`
- **consumer:** `rate_half_band_closure` (evidence edge)

The saturated defect classifier leaves exactly 96 labeled source-line
packets in 12 matching-preserving combinatorial orbits. The colored quotient
compiler partitions every packet into the aligned target `Omega=J_1` or the
near-aligned target `Omega={xi,ell}`.

The existing algebraic leaves prove the canonical aligned cells and the
printed near-aligned chart systems. They do not by themselves transport
through every matching-centralizer orbit: the full q-slice target is not
covariant under a generic endpoint normalization with the quotient
coordinate fixed. The aligned-positive literal registry alone contains
`12 x 3 = 36` semantic cells, whereas the local unramified leaves compile
the canonical `F00/M00` systems.

Conditional on the literal-assignment coverage target, the aligned negative,
ramified positive, six canonical unramified leaves, near negative, 18
affine-positive chart theorems, and seven projective-boundary shards compose
to exclude every literal packet.

Hence the complete saturated source-line `c2(1,1,2)` branch is excluded only
conditional on the missing literal-assignment coverage theorem.
Coordinate/source-cover branches and later packet/source-row assembly also
remain open.
