# KoalaBear m2 r4 diagonal c2 (1,1,2) complete source-line exclusion

- **status:** PROVED
- **scope:** saturated diagonal `c2(1,1,2)` source-line packets over
  `F_(2130706433^6)`
- **requires:** the exhaustive defect/quotient/reconstruction compilers and
  all aligned and near-aligned sign, allocation, ramification, and boundary
  exclusions listed as DAG requirements
- **consumer:** `rate_half_band_closure` (evidence edge)

The saturated defect classifier leaves exactly 96 labeled source-line
packets in 12 matching-preserving orbits. The colored quotient compiler
partitions every orbit into the aligned target `Omega=J_1` or the
near-aligned target `Omega={xi,ell}`.

In the aligned branch, the negative sign is excluded universally. Positive
forms split into the forced-ramified cell and six unramified cells given by
two internal templates times three UFD allocations. All seven are PROVED
empty for the full quotient system.

In the near-aligned branch, the negative theorem includes `w=0`. The 18
affine-positive charts are all excluded, and the projective-boundary theorem
exhausts the remaining seven homogeneous shards. Therefore no aligned or
near-aligned candidate exists, and these cases exhaust all 12 source-line
orbits.

Hence the complete saturated source-line `c2(1,1,2)` branch is excluded.
Coordinate/source-cover branches and later packet/source-row assembly remain
open.
