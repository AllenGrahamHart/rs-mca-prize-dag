# Lineage

This node formerly promoted a long normalized cell ledger to an unconditional
branch theorem. The literal 36-cell comparison showed that combinatorial
matching orbits are not full q-slice covariance orbits. The node is now a
a conditional composition with the missing literal-assignment coverage premise
made explicit. That premise is now PROVED by direct literal certificates, so
the branch theorem returns to PROVED. Existing cell proofs remain unchanged
at their exact scopes.
