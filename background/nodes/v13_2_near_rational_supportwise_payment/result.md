# Result

`REFUTED`. Pair proximity does not pay support-wise MCA witnesses. The exact
rate-half `mu_8 subset F_17` counterexample invalidates the support step, and
the `mu_16=F_17^*` counterexample has two bad slopes with no far-from-code
slope at all, falsifying the former `+1` inequality. The corrected proved
charge is `2w`, in `v13_2_near_rational_supportwise_two_anchor_payment`.
