# Claim Contract

## In scope

- Cell `12`, product rank five, missing roles `DF/sigma_o EF`.
- Residual matchings `1` and `2`, all source and target signs.
- Generic leading-open and rational leading-boundary pieces.

## Out of scope

- Matchings `0` or `3..14`, other missing roles, cell `13`, or cell closure.
- Any claim over a nondeployed field.

## Promotion rule

`PROVED` requires all three `q` branches, every norm/inverse root, every
direct `z` lift, and every final sign lane to terminate without a witness or
unresolved branch.
