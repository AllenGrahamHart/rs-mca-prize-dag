# Statement

Over `F_p`, `p=2130706433`, the guarded cell-12 positive `433-1b` route is
empty for missing `DF`, residual matchings `1` and `2`, and every source and
target sign lane. Exact `D/E` outside transport gives the same conclusion for
the two partner labels with missing `sigma_o EF`.

This closes four labels forming two generic orbits. No other matching is
decided.
