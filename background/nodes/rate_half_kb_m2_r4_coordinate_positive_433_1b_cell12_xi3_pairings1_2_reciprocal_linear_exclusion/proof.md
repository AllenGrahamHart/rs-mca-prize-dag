# Proof

After deleting `DF`, the residual product list is

```text
de, de, -de, sigma_o ef, bf, sigma_c cf.
```

Canonical matchings `1` and `2` are

```text
(de,de), (-de,bf),         (sigma_o ef,sigma_c cf),
(de,de), (-de,sigma_c cf), (sigma_o ef,bf).
```

Write `L_i(q)=B_i-qA_i`. Direct expansion gives

```text
paired(q,q)=4 L_0(q)L_1(q)^2L_2(q).              (RS12-3)
```

Since the characteristic is odd, every finite `q=de` solution lies on one
of the three branches `q=B_i/A_i`. Every zero denominator is retained in the
exceptional census.

Let `m=df`, `S=(d+f)^2`, and `z=1/d`. The missing Vieta equation becomes

```text
M(z)=1+(2m-S)z^2+m^2z^4=0,   e=qz,   f=mz.       (RS12-4)
```

For matching `1`, the second equation is `P(z)=paired(-q,bmz)=0`. For
matching `2`, it is `P(z)=paired(-q,sigma_c cmz)=0`. In either case `P` is
quadratic. Division of `M` by `P` in the exact four-dimensional source
algebra has linear remainder `R(z)=r_0+r_1z`. Writing
`P(z)=p_0+p_1z+p_2z^2`, every common root satisfies the division-free cut

```text
r_1^2 p_0-r_1 r_0 p_1+p_2 r_0^2=0.              (RS12-5)
```

The previously audited reciprocal-linear compiler norms this cut through
the exact basis `1,t,b,bt`. A pinned AST adapter supplies the cell-12 tower
and compact kernel without modifying the compiler function. Thirty-six rows
cover four source signs and three branches, with one anchor for matching `1`
and two `sigma_c` anchors for matching `2`.

The exact candidate union contains every deployed root of all norm and
inverse numerators and denominators. Its 620 case-labeled roots replay
through the original tower, producing 1,040 guarded source/route points.
The boundary ledger contains 180 `r`-guard exits, 144 `t`-guard exits, and
36 cell-12 `b`-leading exits. The last are exactly in the leading complement
paid by the rational-boundary parent.

At each guarded point, direct scalar arithmetic intersects every root of
`M` and `P`. There are 80 common nonzero `z` candidates. Recovering
`d=1/z,e=qz,f=mz` and evaluating the final pair in every remaining sign lane
gives 192 nonzero values. All 36 rows are complete, with no witness, final
solution, or unresolved branch. Thus missing `DF`, matchings `1` and `2`, is
empty. Exact `D/E` transport pays the two missing-`EF` partner labels. QED.
