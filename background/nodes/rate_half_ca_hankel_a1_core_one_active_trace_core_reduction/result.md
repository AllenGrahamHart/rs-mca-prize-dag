# Result

All zero traces can be cancelled canonically, leaving the active-core square
`(ACR4)`. Capacity proves that every bad-domain trace is active except in two
explicit `b=0,D_*=1` boundary profiles with one zero row.

The remaining official obstruction is therefore a genuinely active local
trace system, not a mixture with an uncontrolled trace-free bulk. No Modal
computation was used.
