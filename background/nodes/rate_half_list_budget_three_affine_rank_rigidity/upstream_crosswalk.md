# Upstream crosswalk

The input pencil cap is upstream `thm:affine-span-list` at `b13de811`.
The rank-two equality exclusion is a new local corollary and is a candidate
for an upstream Lane-L packet.

Upstream terminology should be "affine codeword flat" or "affine-span list
compiler". It must not be called a consequence of the locator Grassmann
line without an additional bridge theorem.
