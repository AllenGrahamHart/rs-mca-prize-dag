# Frontier

The dimension-two branch is now a shortened near-partition: at least 1349
pair-core petals are disjoint and, at the minimum common-core size, miss only
872 shortened coordinates. The next theorem should use endpoint incidence,
first ownership, or the two-point quotient exceptions to show that this
packing cannot support the retained record mass.
