# Audit

1. The count is of affine secant lines, not projective directions.
2. Parallel affine lines are handled by their offset difference; their
   actual core intersections still meet only in `J`.
3. Every selected point needs 38 incident lines because one line supplies at
   most 14 of its 519 partners.
4. Cauchy uses the exact ordered-pair identity `sum r(r-1)=269880`.
5. The 872 slack is asserted only at the minimum common-core floor.
