# Proof - L1 identity-pullback role-payment fence

Put `P=X` and `s=1`. For every `x in D`, the level fiber of label `x` is the
singleton `{x}`, so the complete-label set is `B=D` and `b=n`.

The decomposition in `l1_general_pullback_interleaving_descent` has one
residue class:

```text
f(X)=g_0(P(X))=g_0(X),       deg g_0<k.
```

The Vandermonde matrix on a singleton is `[1]`. Its quotient receiver is
therefore `u_0(x)=U(x)`, and the evaluation-kernel exponent is

```text
kappa=max(0,k_0-b)=max(0,k-n)=0
```

because `k<n` on every Reed--Solomon row in scope.

A singleton fiber is fully agreed precisely when its point is an agreement.
Thus

```text
E_f=Agr(f,U),
disjoint_union_(x in E_f){x}=Agr(f,U),
z(f)=0.
```

The quotient tuple is the one polynomial `g_0=f`, evaluated on the same
domain against the same receiver. Membership in its common-support list at
agreement `a` is consequently identical to `|Agr(f,U)|>=a`, proving `(IP4)`.
The exact-shell bridge then partitions this threshold list by the unique
complete agreement size.

Finally, every source petal is a disjoint union of singleton fibers. The
official-reserve router gives `ell<char(F)`, so `char(F)` does not divide the
outer degree `ell`. This puts `s=1` inside the official anchored tame
refinement scope. Any uniform payment over that scope includes `(IP4)`, which
proves the route-fence conclusion.
