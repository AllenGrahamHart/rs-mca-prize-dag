# L1 identity-pullback role-payment fence

- **status:** PROVED
- **role:** show that uniform tame fiber-role payment already contains the
  original L1 list problem at degree one
- **consumer:** `l1_mixed_petal_amplification`

## Identity endpoint

Use the setup of `l1_general_pullback_interleaving_descent` with

```text
P(X)=X,       s=1,       B=D.                          (IP1)
```

Every domain point is one complete fiber. The pullback decomposition,
quotient receiver, and kernel become

```text
f(X)=g_0(X),       u_0(x)=U(x),
k_0=k,             kappa=max(0,k-n)=0.                (IP2)
```

For every codeword `f`, its fully agreed labels are exactly
`Agr(f,U)`, and the partial loss is

```text
z(f)=0.                                                   (IP3)
```

Consequently the common-support one-interleaved quotient list at agreement
`a` is exactly the ordinary Reed--Solomon list:

```text
L_1(D,k,a;U)=#{f in F[X]_<k: |Agr(f,U)|>=a}
            =#ImgFib_U(a).                             (IP4)
```

At `a=k+ell-1`, the exact shells of
`l1_exact_shell_prefix_hankel_bridge` partition this same set.

## Official anchored consequence

Every fixed-source petal is a union of singleton `P`-fibers, so the
whole-petal anchor is automatic. At the canonical official cutoff,
`l1_official_reserve_tame_refinement_router` gives `ell<char(F)`. Hence the
outer degree `ell/1` is tame, and the degree-one endpoint belongs to the
anchored tame family whose map supply has already been counted.

Therefore a theorem paying **all** anchored tame fiber-role assignments must
either prove the ordinary L1 bound in `(IP4)` or explicitly exclude `s=1`
and discharge it through a separate global exact-shell/Toeplitz theorem.
Classifying refinement maps or role vectors at `s>=2` cannot by itself close
L1.

## Scope

This is an exact route equivalence, not a list-size upper bound. It does not
refute the internal pullback route or say that `s>=2` is uninteresting. It
prevents degree-one L1 from being hidden inside a purportedly simpler
uniform role-payment lemma or compute campaign.
