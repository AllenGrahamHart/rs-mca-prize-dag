# Audit

1. The resultants in `(3)` are used only after the listed collision factors
   are excluded.
2. The squared second weld is sufficient only because the `BC` deck-orbit
   orientation remains available; it does not certify a full source facet.
3. `c^2+c+1=0` is removed in `L1` because it forces `b=-c^3=-1`.
4. The degree-24 count is before Galois identification and after algebraic
   equations, but before replaying every guard at every root.
5. Finite-field examples prove common-`K` nonemptiness only.  Their fields
   are not the deployed KoalaBear field.
6. The other seven fibers and paired source-label involution are mandatory
   next gates.
