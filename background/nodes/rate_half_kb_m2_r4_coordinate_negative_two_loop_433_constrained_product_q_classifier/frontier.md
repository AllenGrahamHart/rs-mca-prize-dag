# Frontier

Do not re-eliminate `X2,N1,L1`.  Replay their at most 24 candidates only
when compiling complete source-facet records.  Continue the common-`K`
classification on `M1,M2,M3`; after that, move the entire `(4,3,3)` ledger
to `eta`, six `L^c` fibers, and the paired-product involution gate.
