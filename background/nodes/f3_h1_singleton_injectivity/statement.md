# f3_h1_singleton_injectivity

- **status:** see dag.json (single source of truth)

R_1 = 0: distinct width-1 supports {a}, {b} with e1({a}) = e1({b}) force
a = b; no width-1 collision pair exists, so the fully stripped width-1
direct-column residue is zero. Minted at the U16 surgery (catch #73 — the
proposal labeled it PROVED with no artifact anywhere).
