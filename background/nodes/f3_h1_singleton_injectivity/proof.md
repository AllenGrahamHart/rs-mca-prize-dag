# Proof (one paragraph)

For a width-1 support {a} in H, e1({a}) = a — the first power sum of a
singleton IS the element. So e1({a}) = e1({b}) gives a = b directly: there
is no pair of DISTINCT width-1 supports sharing e1, hence no width-1 trade,
hence no width-1 stripped record: R_1 = 0 at every row unconditionally.
Consistency: the proved band pin (star_pte_support_bound, KB #27) already
places the obligation at 2 <= h <= H_max — full trades have h > t >= 1;
this note discharges the h=1 stratum of the U-PART partition explicitly so
the partition has no silent gap. Falsifier: a certified width-1 stripped
record at any official row (cannot exist by the display above).
