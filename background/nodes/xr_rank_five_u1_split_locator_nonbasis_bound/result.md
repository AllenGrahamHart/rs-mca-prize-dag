# Result

The one-defect quartic kernel now has an exact source-to-flat theorem. Its
nonbasis four-sets are split quartic locators in the kernel hyperplane, and a
triple-completion count bounds them by

```text
floor((C(N,3)+(N-2)(N-4))/4).
```

This improves the official per-block basis floors from the generic
flat-nullity values to `168,168,50`. The rate-`1/4` `u=1` branch still has
the independent five-set packing ceiling `9,009,204,611`, above
`8*1024^3`; the theorem is therefore a real structural refinement but not a
critical payment.

The complete Modal verifier fleet passed `161/161` in
`ap-X0EeIjMd6ViaYH5PCw6rSZ`. All five integration gates passed in
`ap-LyWp0qsSzOuclG0D8L2ULR`.
