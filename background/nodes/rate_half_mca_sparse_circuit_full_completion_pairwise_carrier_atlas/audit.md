# Audit

1. The full parallel class property follows from support-two completion.
2. Proper-subspan positions do not get mislabelled as full completion.
3. Dropping one union point in the no-anchor proper case is conservative.
4. The `d-2` residual points are anchors outside the rank-two span.
5. Positive overlap uses a point outside `B_2`, so it is nonparallel to `P`.
6. Grassmann is taken inside the nine-dimensional `H_2`.
7. Support-four and support-five alternatives are combined by Cartesian
   product; no unproved relation between their residuals is used.
