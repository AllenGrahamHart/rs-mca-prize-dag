# Result

The full-completion pairwise carrier atlas is `PROVED`.  It gives a finite,
exact carrier-position router for supports two through five and supplies the
fixed `(u,g)` unions used by the complete `K'=72` replay.
