# Claim contract

- The ambient correction space has dimension ten and empty common zero set.
- Completion maxima are exact and positive; every named carrier is attached
  to one attaining independent deletion.
- `B_2` is the complete ground-set parallel class, not a chosen subset.
- `T_c/A_c/F_c` are exhaustive conservative routes for `c=3,4,5`.
- The overlap formula is claimed only when both support three and support
  `d` are in full-completion position relative to `B_2`.
- A carrier is not asserted to be the full ground-set intersection with its
  projective flat.
- No numerical row or prize closure is claimed here.
