# KoalaBear near-positive F02 square-orbit exclusions

- **status:** PROVED
- **scope:** four affine positive near-aligned `F02` square-allocation orbit
  representatives over characteristic `p=2130706433`
- **dependency:** the exact 30-cell direct residual registry
- **consumer:** source-line literal-assignment coverage

The complete named-open q-slice ideals are unit for

```text
F02-A-RX,       F02-A-RL,
F02-OB-RX,      F02-OB-RL.                        (KBF2S-1)
```

Here `A` and `OB` represent the two target-root transport orbits, while
`RX/RL` are the two square allocations. Unit ideal holds after saturation by
all ten radical localizer factors. Thus all four cells are empty over the
algebraic closure of `F_p`, and therefore over the deployed `F_(p^6)`.

The transport theorem makes each cell in `(KBF2S-1)` one full semantic orbit,
including its `F03` companion and the alternate target orientation. The
direct affine frontier consequently decreases from 30 to 26 representatives.

Both mixed representatives

```text
F02-A-RM,       F02-OB-RM                         (KBF2S-2)
```

have nonunit ideals after the same complete saturation. They remain open;
nonunit is not asserted to give a deployed-field survivor.

## Falsifier

A named-open algebraic-closure point in a cell of `(KBF2S-1)`, disagreement
between sequential saturation and the Rabinowitsch audit, or deletion of a
mixed cell in `(KBF2S-2)` by this theorem.
