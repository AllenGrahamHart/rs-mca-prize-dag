# Result

The surviving rank-two branch is reduced to a finite split-divisor problem:
one squarefree degree-`3e` polynomial must have at least `3e-3` distinct
degree-`e` divisors whose projective classes lie on an explicit quadric in a
fixed `P^3`. This is the next theorem interface; neither root enumeration nor
the vacuous dual-residue gate addresses it.
