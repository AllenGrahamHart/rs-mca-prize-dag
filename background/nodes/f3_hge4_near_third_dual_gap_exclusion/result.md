# Result

Every primitive exact-level width above the `2m/7` line is empty:

```text
E_h^prim(m,p)=0       when 7h>=2m+1.
```

The live HGE4 exact-level aggregate therefore stops at `floor(2m/7)`, rather
than `floor((m-1)/3)`. This removes the linear and quadratic boundaries and
four previously paid necklace cells at zero budget cost.
