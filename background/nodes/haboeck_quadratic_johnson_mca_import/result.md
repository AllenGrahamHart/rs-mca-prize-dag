# Result

The DAG now has a typed unconditional import of the quadratic Johnson-range
MCA theorem. It may be specialized directly to prize bad-slope budgets. The
unproved BCHKS25 linear refinement remains visibly excluded, preventing the
large numerical gain associated with that refinement from being
misclassified as unconditional.
