# Audit

1. The proof restricts `W`, not the full deletion vanishing space.
2. A deletion with `j-1` outside points cuts `W` by at most `j-1`.
3. The common-root set is the actual union of `D` and the complete carrier.
4. The `j-1` already-selected outside points are subtracted before counting
   completions.
5. For `j>g`, the proof falls back to the supplied global maximum.
6. Floors occur after multiplying each exact stratum and before summation.
7. The parallel-class corollary excludes support two, where two parallel
   points are themselves the desired circuit.
8. Primary and independent scripts implement the formula separately.
