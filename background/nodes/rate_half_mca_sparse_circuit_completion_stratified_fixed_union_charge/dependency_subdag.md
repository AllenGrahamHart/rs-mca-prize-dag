# Dependency sub-DAG

```text
fixed D and g-space W vanishing on D
  + global completion maximum M_d
  -> inside points do not cut W
  -> stratum-j intersection dimension at least g-j+1
  -> outside completion budget min(M_d,K-g-u) for j<=g
  -> exact outside-deletion double count
  -> completion-stratified fixed-union charge [PROVED]
```

The optional parallel-class refinement changes only the count of eligible
inside subsets.
