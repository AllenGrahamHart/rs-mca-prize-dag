# Attack

Try a deletion with many points in `D` and verify directly that only its
outside points restrict `W`. The sharp boundary is a stratum `j<=g` whose
carrier has more than `K-g-u` new outside completions. For the structured
corollary, search for a minimal support at least three containing a parallel
pair; such a support would immediately contain a smaller circuit.
