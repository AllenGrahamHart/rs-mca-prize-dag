# Result

The fixed-union count now uses the actual number of outside deletion points.
Its completion budget is constant across every controlled stratum:

```text
K-g-u.
```

The theorem also continues through target supports above `g` by reverting
only the uncontrolled strata to the exact global completion maximum.
