# Result

The external split design has no remaining independent combinatorial data.
It exists exactly when all active-row parameter polynomials are squarefree
of degree `e` and their resultant norm is an exact `(2e+1)`st power of a
squarefree split degree-`3e` polynomial. The latter polynomial is the unique
squarefree radical of the resultant.

All external slopes, blocks, locators, and incidences are reconstructed from
that radical and `Q`. CR-003 should classify the compact resultant criterion,
not allocate or enumerate a biregular design.
