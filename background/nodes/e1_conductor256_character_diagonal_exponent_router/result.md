# Result

The conductor-256 unit search is no longer an unstructured rank-63 lattice
problem.  In the cyclic coordinate generated by `5`, the complete unit log
map is diagonal, with 63 explicit nonzero Fourier eigenvalues.  The existing
AM-GM body now prints a finite integer ellipsoid and coordinate box for every
same-cofactor associate family.

The remaining maximum-weight task is exact and finite: certify the
eigenvalue intervals, enumerate only the resulting exponent region, and
retain vectors by exact inverse-pair coefficient and sparse-profile tests.
The aggregate target is still at most 367 torsion orbits across the four
cofactors. No E1 or Prize status changes.
