# Dependency sub-DAG

```text
weighted fixed-nine-set demand
  + rank-eight owner-pair marked cap
  -> exact crossing at K'=37996
  -> monotone C(m',11)/C(n',11) ratio
  -> rank eight impossible for K'>=37996             [PROVED]

kernel lane for K'>=4599                             [OPEN]
rank eight for K'<=37995                             [OPEN]
```
