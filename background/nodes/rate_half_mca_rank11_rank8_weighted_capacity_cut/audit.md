# Audit

1. Demand and cap use the same `(record,T)` incidence unit.
2. The binomial identity cancels both the factor `55` and the extension
   pair count exactly.
3. `ceil(L)>U` with integer `U` implies the strict unrounded inequality
   needed for monotone propagation.
4. All eleven factors `(m'-i)/(n'-i)` increase because the complement
   `n'-m'=981104` is fixed.
5. The failed row `K'=37995` is printed, preventing accidental extension
   of the claimed interval.
