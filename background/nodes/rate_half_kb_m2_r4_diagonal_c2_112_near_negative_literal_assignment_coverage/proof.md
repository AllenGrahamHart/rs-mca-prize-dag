# Proof

The constant-term reduction `(KBNN-2)` is inherited from the exact
characteristic-zero aligned-negative component identities, not inferred from
finite-field sampling. Its plus branch is a literal reciprocal-label
collision. Its minus branch fixes `d` as `(KBNN-3)`, reducing every remaining
cell to the variables `b,c,w`.

For each assignment and target root, the compiler reconstructs the complete
negative source form from two forced membership rows and three internal-star
rows. It solves the generic four-row minor and factors the unused consistency
row. Determinant factors are localized; the remaining one or two factors are
handled as separate component equations.

For every component, evaluate `G=U^2-WV^2` at `c,d`, divide the two exact
`(W-w)^2` factors, and compare the resulting monic quartic with

```text
((W-xi^-1)(W-d^-1))^2.
```

After substituting `(KBNN-3)`, the constant equation reduces identically on
the component. The component relation and all four coefficient equations
are then localized over `F_2130706433` by the complete named open. Every
localized ideal is `[1]`.

The primary certificate uses one Rabinowitsch variable for the product of
the 23 or 24 irreducible localizers. The audit independently saturates the
three-variable ideal one factor at a time. Both compile 48 identical cells
and 64 identical component equation tuples; all results agree and are unit.
