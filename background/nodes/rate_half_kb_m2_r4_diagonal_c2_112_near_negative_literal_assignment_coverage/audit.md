# Audit

Two representatives were tested before the census: fixed `F00-A` and
two-component moving `M01-OB`. Both closed in under eight seconds. The full
primary run then closed all 48 cells in 4.76--9.56 seconds each. Independent
sequential saturation closed the same cells in 4.63--11.88 seconds each.

The merged census has 64 distinct equation fingerprints and 48 distinct
localizer fingerprints. Every basis is exactly `[1]`. Primary peak use was
405,168 KiB and audit peak use was 404,512 KiB. No timeout or partial result
is promoted.
