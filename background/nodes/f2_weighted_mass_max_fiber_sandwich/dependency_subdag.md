# Dependency sub-DAG

```text
f2_weighted_kernel_collision_floor [PROVED]
  --req--> f2_weighted_mass_max_fiber_sandwich [PROVED]
              --ev--> f2_conditional_close [TARGET]
```

The missing leaf is the uniform max-fiber estimate `(MF-2)` on the explicit
official dyadic maps. This node proves only that such an estimate transfers
to weighted mass without an additional Fourier or normalization loss.
Upstream `def:q-row-atom` is not this leaf without a proved map-and-owner
transport from its first-match residual family to these syndrome fibers.
