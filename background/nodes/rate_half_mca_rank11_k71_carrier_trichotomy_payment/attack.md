# Attack

The first open rank-nine row is `K'=72`.  Its active branch has
`M_3=M_4=M_5=M_2+2`, so the one-step carrier trichotomy does not fire.
A continuation should classify the two-extra-completion position, couple a
second projective class, or price the support-six fallback through another
shared fixed union.  Do not extrapolate the one-step nested formulas to a
two-step carrier without accounting for the extra completion points.
