# Claim contract

- Exact row arithmetic: `K'=71`, with `K'=72` as the adjacent wall.
- Exact support defects: every `s_c in 0..q`, `c=2,3,4,5`.
- Support-two/support-three pruning applies only when both strata are
  nonempty and `M_3<=M_2`.
- Both transverse and anchor fixed-union cases are retained.
- One-step provenance `M_3=M_2+1` is preserved until support four is known.
- If `M_3=M_4=M_2+1`, all six carrier-position cases are retained.
- Same-support and cross-support caps, the support-four/support-five joint
  charge, and every support-six through support-nine branch remain active.
- Pareto compression removes only duplicate or componentwise dominated cap
  vectors.
- Every kernel corank, rank-nine mark, floor, and exact integer division is
  inherited unchanged from the `K'=60..70` payment.
- No claim is made at `K'=72`.
