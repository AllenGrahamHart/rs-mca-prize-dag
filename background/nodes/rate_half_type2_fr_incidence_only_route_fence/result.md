# Result

`WITNESS`. At `m=64`, the exact saturated incidence ledger and every
pairwise/individual support inequality used by `(NEWCAP)` coexist with

```text
max |S_gamma intersect W|=189>128=2m.
```

The arbitrary-`W` incidence-only continuation is fenced. Round 32 showed that
the witness `W` is not a pair union and extracted the separately proved
canonical bound `rate_half_fr_canonical_min_pair_union_bound`. The official
type-2 residual remains open beyond that corrected bound.
