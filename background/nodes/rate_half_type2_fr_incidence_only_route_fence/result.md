# Result

`WITNESS`. At `m=64`, the exact saturated incidence ledger and every
pairwise/individual support inequality used by `(NEWCAP)` coexist with

```text
max |S_gamma intersect W|=189>128=2m.
```

The incidence-only continuation is fenced. The official type-2 residual
remains open and must be attacked through the locator polynomials or the
Hankel pencil.
