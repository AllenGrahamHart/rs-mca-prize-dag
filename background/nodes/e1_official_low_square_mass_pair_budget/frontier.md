# Frontier

The binding finite statement is

```text
E_low <= 62622678770648913918718317914905517790930
```

for every pair-feasible prize rate-`1/8` row. Since
`K=38001322036274275320505631960233903602944`, a maximum-degree-three theorem
gives `E_low<=3K/2` and closes this entry with room.

The proved weighted-kernel dictionary gives the exact alternative form

```text
E_low=(1/2) sum_{d in D_p(33)} M_33(a(d),b(d)).
```

The largest eligible weight is the `(3,4,S=16)` value
`4550972295647251657752808370587724056`. Therefore the coarse oriented-vector
cap `|D_p(33)|<=27520` is sufficient. This is now the clean finite attack
interface, although the profile-weighted inequality is weaker and preferred.

The existing `(3,4)` variance campaign removes many relations in the
maximal-weight profile but does not yet control its `V<=24` residual or the
other 274 norm-eligible profiles through `S=66`.

The coloring target is an independent alternative. Degree two pays both;
degree three pays this pair-budget route even when the graph is not
three-colorable.
