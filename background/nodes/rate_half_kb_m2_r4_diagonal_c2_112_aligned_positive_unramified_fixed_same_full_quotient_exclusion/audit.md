# Audit

1. The direct router uses the affine minor-conic resultant, retaining all
   function-field denominator specializations automatically.
2. It regenerates all 28 norm factors, filters extensions only by the exact
   `d|6` criterion, and replays every deployed `p,w,b` candidate in the four
   original equations and full forbidden set.
3. The four-point certificate is regenerated byte-for-byte.
4. The quotient probe first reproduces the q-slice target, then rejects all
   four points in both full quotient norms.
5. The off-common checker classifies every projection factor and evaluates
   both `2 x 1 x 1` residual combinations. All seven endpoints are boundary.
6. The claim is a full-quotient exclusion, not q-slice emptiness.
