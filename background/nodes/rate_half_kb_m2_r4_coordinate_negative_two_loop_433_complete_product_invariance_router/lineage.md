# Lineage

The common-`K` classifier, forced outside product, and unique edge skeleton
left a signed placement-and-matching problem.  Sign gauge and invariant
binary forms reduce its 300 naive signed perfect matchings to 20 exact
algebraic cells without conflating the independent source and endpoint
coordinates.
