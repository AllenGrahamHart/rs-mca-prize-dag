# Source evidence

- The final common-`K` classifier supplies `epsilon` and its three exact
  equations.
- The outside-product compiler supplies `xi`, its forced value, and the
  nonsingular product involution.
- The complete-edge skeleton supplies the seven signed product types.
- The coordinate coefficient normal form proves that endpoint `T` and
  source/quotient coordinates are normalized independently.
- Primary and audit verifiers replay the sign quotient, involution matrix,
  binary-form cell count, and naive-to-invariant case reduction.
