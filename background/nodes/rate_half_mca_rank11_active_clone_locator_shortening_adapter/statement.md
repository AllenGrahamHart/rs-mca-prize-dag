# Rank-eleven active-clone locator shortening adapter

- **status:** PROVED
- **scope:** the active large-clone horn of the base-free `2 x 5` factor branch

Let `D` be the active projective clone emitted by the clone/line dichotomy,
with

```text
c=|D|>=10001,            mu_D>=388650911452.
```

All nonzero evaluation functionals on the residual five-space `B` indexed
by `D` are proportional. Their common kernel

```text
B_D={b in B:b(x)=0 for every x in D}
```

has dimension four. If

```text
L_D(X)=prod_(x in D)(X-x),
```

then exact polynomial division gives

```text
B_D=L_D bar(B)_D,             dim bar(B)_D=4.           (AS1)
```

Put `s=K-c`. The shortened correction space satisfies

```text
span(P bar(B)_D) <= F[X]_{<s},
dim span(P bar(B)_D)>=5,
```

so

```text
5<=s<=1038575,
10001<=c<=1048571.                                      (AS2)
```

Every residual class counted by `mu_D` has `B_i<=B_D`; division sends it
to `bar(B)_i=L_D^{-1}B_i` of the same dimension two or three. The
same-record common-core adapter therefore transports the complete bucket
reversibly to

```text
(n',k',m')=(2097152-c,1048576-c,1116048-c),            (AS3)
```

preserving all first-owned slopes, pair noncontainment, the field, and the
three differences

```text
n'-k'=1048576,   m'-k'=67472,   n'-m'=981104.
```

Thus the large-clone horn is not an untyped ambient-code feature: it is an
active shortened `2 x 4` factor bucket of mass at least `388650911452`.

## Nonclaim

The shortened bucket is not paid here. This theorem does not synchronize
different active clones, identify the bucket with an upstream split pencil,
or handle the genuine rank-two-triple horn.
