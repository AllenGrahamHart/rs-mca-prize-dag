# Result

`PROVED`.

The large-clone alternative now has an exact destination. Its active mass
survives reversible locator cancellation in a shortened `2 x 4` factor
bucket, and the clone cannot exceed `K-5` coordinates. The remaining task
is to pay or recursively classify this shortened bucket, in parallel with
the genuine rank-two-triple horn.
