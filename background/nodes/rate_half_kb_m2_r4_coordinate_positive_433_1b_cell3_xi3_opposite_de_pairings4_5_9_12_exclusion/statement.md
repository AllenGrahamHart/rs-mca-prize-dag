# Statement

## Claim (KBP1B3-XI3-P45912-OPP-1)

On the guarded positive deployed 433-1b role-cell-3 branch, every atlas case
with missing record `xi=3` (the `df` record), canonical matching index in
`{4,5,9,12}`, any source sign `epsilon in {+-1}^2`, and any target lane
`sigma in {+-1}^2` is empty.

Twelve exact source rows compute matchings 4 and 5. Four pairing-4 rows each
cover all four target lanes; eight pairing-5 rows each cover both `sigma_o`
lanes for one `sigma_c` anchor. Exchanging the two identical positive `de`
records maps matching 4 to 9 and matching 5 to 12. Thus 32 computed raw cases
transport value-for-value to 32 further raw cases, and all 64 raw cases in the
stated scope are empty.

Together with the preceding `xi=3` theorems, this completes all 240 cases in
the `xi=3` slice. No case with `xi in {4,5,6}`, complete cell-3 exclusion,
rate-half band, LIST, MCA, or Prize result is claimed.
