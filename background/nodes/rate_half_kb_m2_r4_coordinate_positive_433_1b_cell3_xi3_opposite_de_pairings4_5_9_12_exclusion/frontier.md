# Frontier

The paid cell-3 ledger is now 960 raw cases: 720 parallel-`DE` cases at
`xi in {0,1,2}` and all 240 cases at `xi=3`.

No `xi=3` pairing remains. Every pairing at `xi in {4,5,6}` remains, for 720
raw cases.

Do not infer complete cell-3, K3, LIST, MCA, or Prize closure.
