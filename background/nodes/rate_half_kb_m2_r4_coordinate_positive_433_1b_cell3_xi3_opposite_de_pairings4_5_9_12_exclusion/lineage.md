# Lineage

- The global quadratic quotient and compact kernel supply the exact source
  algebra and direct-lift maps.
- The first pairing-4 route reduced the missing quartic inside the opposite-DE
  `q` quotient. Both its original and cached-product implementations crossed
  300 seconds without reaching a useful checkpoint.
- Passing to `x=q^2` moves the expensive step out of the quotient: one exact
  quadratic resultant in `x`, followed by the monic missing-`f` reduction,
  gives the same bounded 4-by-4 Bezout endpoint as the fully mixed theorem.
- The exact rows completed in 215-419 seconds on Modal. No local heavy
  computation was used.
- This theorem closes the last two `xi=3` transport orbits. The cell-3
  frontier moves to missing indices 4 through 6.
- The result remains background evidence toward `rate_half_band_closure`.
