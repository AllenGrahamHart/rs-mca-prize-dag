# Result

`KBP1B3-XI3-P45912-OPP-1` is **PROVED**. All 64 guarded positive deployed
433-1b role-cell-3 cases with missing `df` record `xi=3` and canonical
matching index in `{4,5,9,12}` are empty. Consequently every one of the 240
raw `xi=3` cases is now paid.
