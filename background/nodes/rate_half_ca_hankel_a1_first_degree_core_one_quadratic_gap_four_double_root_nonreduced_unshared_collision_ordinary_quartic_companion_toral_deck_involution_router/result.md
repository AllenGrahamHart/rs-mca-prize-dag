# Result

The remaining low-degree companion is no longer an arbitrary `(4,6)`
curve. It must carry a deck involution visible directly on the row
coordinate, either antipodal or reciprocal, and therefore descends to a
cubic row-coordinate quotient.

Shape C remains open. The next proof should split into the `X^2` and
`X+k/X` quotient arms and transport the heavy-row collision and source
locator identities to the quotient biform.
