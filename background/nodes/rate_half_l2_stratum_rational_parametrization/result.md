# Result

The `e = m = 2` (L2) stratum is rationally parametrized: five polynomials
and one linear factor generate every member at hit rate 1, replacing the
round-35 inversion (hit rate `1/q`) as the construction instrument. The
stratum has dimension exactly 18, the parametrization is determinantal,
and its syzygies pin the third boundary condition's exact scope.

This is the instrument the (SAT3)-on-(L2) gate is attacked through — the
round-38 `T = 3` achievement (scale elimination) runs inside it. The
membership criterion (RES) is carried honestly split: proved forward,
measured backward. Everything is `m = 2` specific.
