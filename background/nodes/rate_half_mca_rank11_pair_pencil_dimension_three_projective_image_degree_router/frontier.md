# Frontier

Attack the branches separately.

1. On `c=2`, classify degree-`1021..2490` rational maps `A/B` for which at
   least 41,746 distinct binary quadratics in `A,B` each have at least 2,041
   official-domain roots after the common-gcd charge. This is now a
   one-function quotient-periodic or split-fiber problem.
2. On `c>=3`, combine the 597-or-more distinct full evaluation normals with
   the 218-point owner-plane cap. A useful next bound must use distinct
   normal fibers or curve incidence; repeating aggregate direction-root
   counting cannot improve the current certificate.
