# Result

The collision's four-root complement substantially narrows the global
factorization. At `d_A=1`, the two-large-odd and huge-even profiles are
impossible, leaving one large odd factor of official parameter degree at
least `109951162777` and only ordinary-even companions. At `d_A=0`, all
three profiles remain but their large and huge thresholds rise to
`78536544841` and `157073089682`.
