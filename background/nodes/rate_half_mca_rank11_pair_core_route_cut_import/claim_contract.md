# Claim contract

**Claimed (PROVED, per the declared certificate class):** the summed
nonuniform margin resource; the fixed-minimizing-pair terminals of the
statement table; the greedy/Abel certificate-class wall
`U_total = 813929118931913384 > B_*` at the unique optimum `J = 19737`;
the `GF(7)` sharpness star.

**Not claimed:** any rank-eleven payment; any active-v4 ledger
movement; KoalaBear or prize closure; that NO theorem can pay rank
eleven (the pre-registered coupling/owner escape is open).

**Falsifier:** an arithmetic error in any printed constant (the wall is
independently recomputed here); a legal cutoff beating `J = 19737`; or
a declared-class certificate paying rank eleven under `B_*`.
