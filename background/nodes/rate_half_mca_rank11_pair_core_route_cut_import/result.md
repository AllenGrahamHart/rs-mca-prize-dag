# Result

- Error rank eleven of the post-near KoalaBear stratification remains
  UNPAID: the complete declared pair/core certificate class bottoms out
  at `U_total = 813929118931913384 > B_*` (excess
  `538948390820518297`, factor `> 2.9`), with the nonuniform
  relaxation's optimizer also over budget.
- Every over-budget rank-eleven line forces exact dense-pair-core
  terminals: a `delta <= 8` pair of weight `>= 743449148` (cutoff
  6486) and a `delta <= 4` pair owning `>= 200632` slopes (cutoff
  1795).
- The `GF(7)` star kills distinct-neighbor / per-pair-multiplicity
  improvements from local hypotheses.
- Wall independently reproduced here: `L(19737)` exact match.
- The first grand-challenge row's post-near frontier is now: ranks
  `<= 10` paid, rank 11 = this wall, rank `>= 12` behind it.
