# Dependency subdag

- **Ingredients (textual):** `#1166`'s intrinsic near deletion and
  support-local margin (banked:
  `rate_half_mca_support_local_transversality_compiler`); `#1167`'s
  sub-square interleaving collapse (banked equivalent:
  `rate_half_mca_rank10_margin_interleaving_split_payment`); the
  reversible error-rank gauge. No requires edges: the wall arithmetic
  is replayed locally and the theorem clauses are upstream-reviewed.
- **Evidence for:** `rate_half_band_crossing_location` — this is the
  exact first unpaid stratum of the post-near KoalaBear ledger, i.e.
  the current frontier object for the first grand-challenge row.
