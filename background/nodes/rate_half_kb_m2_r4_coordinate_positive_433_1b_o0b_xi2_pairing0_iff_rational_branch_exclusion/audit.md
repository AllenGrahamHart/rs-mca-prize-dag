# Audit

The proof distinguishes logical weakening from exact algebra:

- The infinity split and rational formulas are exact under printed guards.
- The finite common-root equations imply the scaled resultants, but the
  converse is not claimed because infinity roots are retained.
- Unit ideal for that larger resultant locus is therefore sufficient.

The hostile audit removes `q6`, removes the `k5` denominator guard, and
marks the collection incomplete; all mutations are rejected.
