# Claim contract

- The result applies exactly to the canonical `IFF` chart.
- The `k2=0` branch uses the proved admissible
  `k2=k5=0` exclusion.
- The `be=cf` branch uses `k2,k5,a2m,b,c != 0` to justify every cleared
  denominator.
- The finite equations are weakened to scaled resultant vanishing; proving
  this larger locus empty is sufficient.
- It does not address `FFF`, where no infinity equation forces a rational
  outside-variable normalization.
