# Lineage

The initial direct `IFF` observation produced only
`(be-cf)k2=0`. The collapsed-common unit certificate closes one factor,
while exact rational solution of `be=cf` removes all outside variables
from the other.
