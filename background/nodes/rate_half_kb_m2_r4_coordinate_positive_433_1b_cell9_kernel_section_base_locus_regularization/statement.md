# Statement

## Theorem (KBP1B9-BASE-REG-1)

At every one of the eight distinct deployed section-base points supplied by
`(KBP1B9-EP-DECOMP-1)`, the common `10 x 8` Vieta matrix has rank seven.
Its unique pointwise kernel `k` has `A_k(-t^2)!=0`.  Consequently every
missing outside row at such a point has the ordinary necessary values

```text
m = B_k(-t^2)/A_k(-t^2),
S = (-t^2) beta_k(-t^2)^2/A_k(-t^2)^2.
```

The zero value of the global polynomial section is therefore a base-locus
artifact, not an extra kernel dimension or an unconstrained outside row.
