# Source Evidence

Modal app `ap-5AdOkV7s8XMpFQxWzAcvRF` independently reconstructed the common
matrix and pointwise kernel in every source-point/target-lane row.  All 32
rows report rank seven, zero stored section, and nonzero regularized
`A(-t^2)`.  The independent audit rebuilds the matrices and kernels from the
source points rather than trusting those reported fields.
