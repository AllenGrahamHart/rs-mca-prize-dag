# Frontier

The parallel-`DE` block is now proved for matching indices
`0,1,2,3,4,5,6,7`, paying `384` raw cases.

Matching 8 keeps the first pair `(de,sigma_o ef)` but replaces the other
two pairs by `(second_de,sigma_c cf)` and `(df,bf)`.  Derive its lowest
elimination degree before another norm campaign.  The competing
`xi=3,pairing=0` branch still needs a lower-degree shared-`f` elimination.

Do not infer complete cell-3 closure from the eight paid matching indices.
