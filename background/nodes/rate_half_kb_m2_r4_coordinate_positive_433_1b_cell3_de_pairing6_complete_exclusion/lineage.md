# Lineage

- The pairing-3 FLINT function-field backend supplied the exact quartic
  nested cut and exceptional-root replay.
- Matching 6 exchanges the `df,ef` partners of the two residual `DE`
  records.  Using `u=ef`, `v=df` moves `sigma_o` into the first cut while
  preserving the missing-sum quartic.
- The resulting census is cheaper than the degree-eight pairing-4 and
  pairing-5 routes; every row completes below 90 seconds.
- No target-label transport is assumed.  Every source sign and lane is
  computed for `xi in {0,2}`.
- Exact parallel-edge transport adds the second positive `DE` deletion.
