# Statement

## Claim `(KBP1B3-DE-P6-1)`

On the guarded product-rank-five branch of the deployed positive
`433-1b -> O0a` role-cell-3 atlas, every outside case with

```text
xi in {0,1,2},  pairing = 6
```

is empty for all four source-sign pairs and all four target-sign lanes.
Consequently all

```text
3 parallel DE missing copies * 1 matching
* 4 source signs * 4 target lanes = 48 raw cases
```

are excluded.

For `xi in {0,2}`, 32 exact computed rows pay 32 nonidentical raw cases.
A quartic nested-quadratic norm and direct exceptional-root replay gives
zero colored-pair solutions, zero witnesses, and zero unresolved branches.
The `xi=1` rows transport from `xi=0` because deleting either positive
parallel `DE` copy leaves identical product and squared-sum lists.

The claim does not cover another matching, `xi=3,...,6`, complete cell 3,
the rate-half band, K3, LIST, MCA, or either Prize result.
