# Result

The nodal pair search now has an exact constant-cost front end. Positive root
overlap is one six-value Mobius orbit, the complete zero-target divisor lies
inside that orbit, and the identity target is the single symmetric equation

```text
q(a)+q(b)+3q(a)q(b)=0.
```

Together with the nine explicit negative-root comparisons, these rules prune
and canonicalize a finite packet before any richness or quotient-weight work.
The remaining task is still the weighted correlation bound on the surviving
pairs.
