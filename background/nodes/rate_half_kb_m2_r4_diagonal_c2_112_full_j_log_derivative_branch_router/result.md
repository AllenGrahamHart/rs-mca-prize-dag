# Result

The full-`J` coefficient-one condition has an exact low-order router. Its
four previously hidden denominator branches and its guarded irreducible
degree-67 numerator branch are closed by dedicated children on both `F04`
targets. Their composition closes the two `F04` cubic/full-`J` routes. The
router prevents quotient arguments from silently assuming the individual
constant factors are nonzero.
