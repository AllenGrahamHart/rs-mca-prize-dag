# Proof

If `s=0`, apply the literal degree-12 `s`-zero exclusion. Otherwise split on
the irreducible degree-6 `x`-leading factor `L6`. Its zero branch is empty by
the literal degree-6 leading-curve exclusion.

On `s L6!=0`, split on the `B0` leading factor `K8`. The `K8!=0` chart is
empty over `F_(p^6)` by the global saturated `s`-elimination and its five
terminal fibers. The complementary `K8=0` branch is empty over the algebraic
closure by the exhaustive `K10` dichotomy.

These cases partition every field point of the degree-12 branch. QED.
