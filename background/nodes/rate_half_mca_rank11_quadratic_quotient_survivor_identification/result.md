# Result

The two quadratic quotient classes are now exact named DAG branches rather
than an undifferentiated residual. Both are large real pencils, so future work
must charge their record currency rather than attempt to falsify them by a
small-fiber bound.
