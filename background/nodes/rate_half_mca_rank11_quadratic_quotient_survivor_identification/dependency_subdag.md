# Dependency sub-DAG

```text
quadratic Mobius router --------------------------+
cyclic quotient-periodic fence ------------------+--> quadratic quotient
dihedral quotient fence -------------------------+    identification [PROVED]
reciprocal-affine lambda=1 elimination -----------+             |
                                                                +--ev--> rate-half crossing
```

The node identifies and counts the two quotient outputs. It does not consume
or pay their first-owned record currency.
