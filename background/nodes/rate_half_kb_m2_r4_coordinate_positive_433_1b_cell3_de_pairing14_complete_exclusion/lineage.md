# Lineage

- The global quadratic quotient and compact kernel supply the exact source
  algebra and direct-lift maps.
- Pairing 14 reuses pairing 11's common-`f` geometry with the two residual
  `DE` records assigned to the `bf` and `sigma_c cf` cuts in the opposite
  order. The explicit division-free resultant remains exact.
- The positive parallel-copy transport is the exact residual-record deletion
  used in pairings 0 through 13.
- No target-label exchange is used: that map sends source role cell 3 to
  duplicate cell 6 and does not close a within-cell matching.
- This result finishes all fifteen parallel-`DE` matchings but remains
  background evidence toward `rate_half_band_closure` because missing roles
  `xi in {3,4,5,6}` are still open.
