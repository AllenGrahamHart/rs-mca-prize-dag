# Audit

1. The 38-value list is exactly the map-only remainder of the proved router.
2. The rational matrices are computed as `p_u(L)`, not fitted from samples.
3. All 1,728 matrix entries are present and reduced; no routed denominator
   vanishes.
4. The checker ties the matrices to the old exact operator both symbolically
   and at a regular fiber, then checks commutation at all 38 fibers.
5. Every dynamic coordinate reconstructs the full multiplication operator,
   not merely its action on the cyclic vector.
6. Every degree-24 minimal polynomial is squarefree and completely factored.
7. All 804 finite rows have gcd exactly `1` or `e^2-1`.
8. Hostile mutations of a coordinate operator and a finite-row gcd are
   rejected.
9. The eight basis-specialization hazards remain open.
