# Claim Contract

## In scope

- Cell `12`, product rank five, parallel-`DE` roles `xi in {0,1,2}`.
- The two generic label orbits represented by `(0,5)` and `(2,5)`.
- All source signs, target lanes, and the proved rational leading boundary.

## Out of scope

- The other five generic representatives, cell `13`, or cell closure.
- Any claim over a nondeployed field.

## Promotion rule

`PROVED` requires complete Frobenius root unions, direct replay of every
`(u,f)` row, exact orbit reconstruction, and no witness or unresolved branch.
