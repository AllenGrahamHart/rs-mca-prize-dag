# Result

The calibrated trace and the exact external incidence ledger exclude the
remaining dihedral distance-three packet on the official row. The abstract
quadric has many divisors, but their trace quadratics cannot sustain the
required `e-4`-fold root incidence without collapsing to three repeated
root classes, contradicting complement injectivity.

The deferred calibrated-classification computation is therefore retired;
this branch closes by a short collision argument.
