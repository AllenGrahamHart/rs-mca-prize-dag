# Frontier

Do not recompute the generic `DE+/DE-/BE` gcd or repeat it on atlas charts
3--5: `gcd(P,L_2)=1` proves that chart 2 covers the entire generic common
locus.  The remaining cell-5 work is now:

1. compute the finite exceptional-`t` locus from the primitive, coordinate,
   atlas, and guard denominators and treat those deployed-field fibers;
2. treat the other common sign rows and matching cells needed for a cell
   deletion.

There is no declared `B <-> C` symmetry: their common multiplicities differ.
Nevertheless the `DF+/DF-/CF` analogue is not needed for this sign row,
because every packet in the row already contains the excluded
`DE+/DE-/BE` triple.
