# Claim contract

| field | value |
|---|---|
| assumptions | Deployed characteristic; generic `t`; cell 5 and signs `(-1,-1)`; reciprocal common projection; globally invertible chart-2 coefficient `L_2`; the proved guard-localized squared `DE+/DE-` algebra; one colored `BE` record; target and source guards. |
| claim | The exact identity `gcd(P,L_2)=1` makes chart 2 global on the generic common locus. On every one of the five primitive signed-pair residue fields, the `DE+` necessary polynomial and colored `BE` resultant have common factor only on `e^2=1`, and on the octic component are coprime. Hence the entire admissible generic cell-5 sign row `(-1,-1)` is empty. |
| dependencies | Reciprocal projection and rational lift atlas; signed-family target-free interface; outside-edge eliminant; primitive residue and coordinate-map ledgers; generic guard-unit ledger. |
| excluded | Finite exceptional `t` fibers; other signs or matching cells; cell, route, K3, row, LIST, or MCA closure. The `DF+/DF-/CF` family is unnecessary for this already-excluded sign row. |
| falsifier | A guarded generic realization or a failed exact Bezout/source-normalization audit. |
