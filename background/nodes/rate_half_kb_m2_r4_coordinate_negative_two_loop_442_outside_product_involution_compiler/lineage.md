# Lineage

The six common-`K` rows and their q orientations were complete, but their
seven outside records were unconstrained.  This node uses the two antipodal
pairs already in `K` to force the singleton mate and reduce the remaining
paired-product condition to three copies of one explicit bilinear equation.
