# Frontier

Combine the forced `xi` product with the unique outside edge multiset

```text
C-D,C-E,D-E,D-F(+/-),E-F(+/-).
```

After sign gauge, remove each possible `xi` edge product and impose
invariance of the residual binary sextic under `(KB44O-3)`.  Saturate by
product distinctness and fixed-point avoidance before full twelve-row
Mobius interpolation.  Keep horizontal `D,E,F` coordinates independent of
the quotient parameter `l`.
