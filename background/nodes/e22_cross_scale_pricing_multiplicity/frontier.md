# frontier: e22_cross_scale_pricing_multiplicity

Closed.

Support-level canonicalization, the dyadic minimal-scale selector, and the
tail-minimality criterion are proved. The minimal-scale column evaluation and
pricing compatibility are now proved, so the quotient staircase column counts
canonical cross-scale classes with the declared multiplicity.
