# Statement

Fresh independent review must cover the complete final load-bearing K3
sub-DAG at pinned commits. This includes the active bad-slope-to-component
bridge, full-V4 and negative-coordinate exclusions, every positive raw
workboard and route payment, all source-line/source-cover/trivial-stabilizer
payments, orientation composition, allocation definition, and final
distinct-slope ledger. It must replay certificate custody checks, inspect the
mathematical implications rather than only script exits, verify exact
multiplicities and the active row/partition digest, and confirm same-owner
distinct-slope chronology.

The reviewer must be outside every producing implementation. This node
remains `TARGET` until the review record names the reviewer,
commits, commands, verdicts, and any resolved objections.
