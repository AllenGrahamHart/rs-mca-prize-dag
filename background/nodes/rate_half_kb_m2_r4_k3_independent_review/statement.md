# Statement

Fresh independent review must cover the complete final load-bearing K3
sub-DAG at pinned commits. This includes the active bad-slope-to-component
bridge, full-V4 and negative-coordinate exclusions, every positive raw
workboard and route payment, all source-line/source-cover/trivial-stabilizer
payments, orientation composition, allocation definition, and final
distinct-slope ledger. It must replay certificate custody checks, inspect the
mathematical implications rather than only script exits, verify exact
multiplicities and the active row/partition digest, and confirm same-owner
distinct-slope chronology.

The reviewer must be outside every producing implementation. This node
remains `TARGET` until the review record names the reviewer,
commits, commands, verdicts, and any resolved objections.

## Partial external replay record (2026-08-12 addendum)

Two outside replays now exist, both by scottdhughes on upstream
przchojecki/rs-mca, both matching our banked verdicts exactly:

- PR `#1153` (2026-08-09): the six residual `xi=3` cell-5 pairing
  representatives, zero witnesses, exact over `F_2130706433`, public-DAG
  pin `28b3bc8a`.
- PR `#1157` (2026-08-10): the raw `433-1b -> O0a` exclusion, 15 cells,
  1,575 labels, 25,200 signed systems, zero survivors, public-DAG pin
  `8df0903391a2`.

The upstream K3 boundary after these packets (route cuts only, zero
ledger movement upstream): `#1155` proves the `433-1a` guard product
does not transplant to the `433-1b` tower (a quadratic cover is the
correct next obligation); `#1158` refutes direct endpoint-coordinate /
evaluation-carrier identification (`tau(T)=1-T` does not preserve the
carrier; conjugated folds remain open); `#1159` proves the actual-record
dimension shift `d1^(k) = d1^(k+1) = 67473` and isolates `SEM-QBC` (the
source theorem connecting deployed actual-slope chronology to the Q/BC
priority map) as the first missing bridge before `Rec_2_4`.

These are PARTIAL: they do not cover the complete load-bearing K3
sub-DAG, so this node stays `TARGET`. They accumulate as review
evidence toward it.
