# Frontier

Do not rerun the broad standard bases or extend their timeouts.  The
exceptional scale ledger is closed by the proved child node.  On the sole
remaining main genus-three chart, use the sealed four target-free equations as
a sequential resultant problem in `z2`, then `z1`, rather than one global
Groebner basis.  Exploit `B_1(W)=b10(1-W)` before expansion and pseudo-reduce
by `P` after every elimination step.

A unit resultant chain closes `[3,6]`; a surviving zero-dimensional algebra
must be tested for deployed-field points and the omitted target/source guards.
