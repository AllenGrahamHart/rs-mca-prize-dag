# Frontier

Do not rerun the broad standard bases, the squared-`w2` formulation, or the
direct unsquared `z2` resultant.  The exceptional scale ledger is closed by
the proved child node.  On the sole remaining main genus-three chart, first
rewrite the signed `DE+/DE-` pair in squared variables `w0,w1` and factor its
component resultant.  Append the colored `BE` equations only on the resulting
components.  Exploit `B_1(W)=b10(1-W)` before expansion and pseudo-reduce by
`P` after every multiplication.

A unit resultant chain closes `[3,6]`; a surviving zero-dimensional algebra
must be tested for deployed-field points and the omitted target/source guards.
