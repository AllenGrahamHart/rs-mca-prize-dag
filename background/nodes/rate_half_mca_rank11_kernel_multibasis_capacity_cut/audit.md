# Audit

1. The divisor is applied separately to each corank stratum before the
   capacities are summed.
2. Integer floors are retained, and the lane demand uses an integer
   ceiling.
3. The replay includes every row from `10` through `11641`.
4. The support-local cap uses the special proved rank-nine value at `d=9`.
5. The independent verifier does not import the primary verifier.
6. The first reversal at `11642` is an explicit nonclaim boundary.
