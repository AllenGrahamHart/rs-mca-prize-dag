# Result

All-bases decoration removes another 7,043 residual dimensions from the
kernel frontier:

```text
old closed interval: 10 <= K' <= 4598
new closed interval: 10 <= K' <= 11641
first open row:       K' = 11642
```

The rank-eight affine-owner lane is already impossible above `K'=37995`.
Thus this result narrows, but does not close, the rank-eleven residual
problem.
