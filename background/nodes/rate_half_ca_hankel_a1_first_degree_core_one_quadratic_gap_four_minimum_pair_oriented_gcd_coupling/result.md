# Result

`PROVED`. Every `rho+3` quadratic pair carries a center-owned common
parameter divisor of degree linear in `e`. The formerly weak zero-deficit
case is strongest: forward/reverse coupling forces degree at least
`(3e-2)/5`. The next gate is compatibility of this large divisor with the
heavy-row cube/Forney factors.
