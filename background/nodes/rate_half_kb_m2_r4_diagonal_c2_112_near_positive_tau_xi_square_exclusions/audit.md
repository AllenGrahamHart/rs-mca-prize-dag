# Audit

1. All eight generic endpoint-line/allocation pairs are separate tasks.
2. Full leading-zero loci are classified in both elimination directions.
3. Primary and audit use direct and fraction-free source solves respectively.
4. The exact-monomial coefficient mutation remains an active regression test.
5. Every certificate is recomputed modulo the deployed characteristic.
6. Each task runs under `timeout 60s` and `ramguard tiny`.
7. The swapped `(1,0)` expectation is reciprocal-orbit-specific:
   `(d-2)(d-1)(d+1)`. Substituting the fixed-`xi` factor is rejected.
