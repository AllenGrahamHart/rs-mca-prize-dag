# Result

`PROVED`. At `n=8192`, rate `1/8` is paid through source scale `32` and rate
`1/16` through source scale `67` on explicit nonempty upper field slices.
The support-shortening compiler stops at `M=33` and `M=68`, respectively.
