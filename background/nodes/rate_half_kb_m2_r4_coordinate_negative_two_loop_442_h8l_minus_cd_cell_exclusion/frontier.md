# Frontier

Apply the same one-parameter descent to forced types `sigma DE` and `DF` on
`H8-L,tau=-1`.  Then test whether the `H8-M` rows are transported versions
of these cells before launching new elimination.  Keep factor norms and
deployed-characteristic residues explicit.
