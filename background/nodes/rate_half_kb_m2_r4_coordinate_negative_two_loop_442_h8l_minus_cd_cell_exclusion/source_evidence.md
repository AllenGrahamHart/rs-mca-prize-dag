# Source evidence

- The common-row classifier supplies `(1)` and product guards.
- The outside compiler supplies the protected forced product and exact
  cross-product involution.
- The invariant router supplies the 15-matching complete interface.
- The signed-pair theorem deletes the three closed templates.
- Primary and audit verifiers reconstruct the univariate descent and all
  mixed-template factor norms using different resultant chains.
- Small-field no-hit reconnaissance motivated the cell choice but is not
  used in the proof.
