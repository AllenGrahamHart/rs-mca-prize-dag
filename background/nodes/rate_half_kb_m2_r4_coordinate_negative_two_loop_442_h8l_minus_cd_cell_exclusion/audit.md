# Audit

1. The descent to `P_4(b)` is necessary for every original `(l,b)` root;
   no converse is used.
2. The forced value `1` and identity `Beta=-Gamma` are polynomial reductions,
   not numerical observations or divisions by a possibly zero coefficient.
3. The intrinsic coordinates retain both free horizontal parameters and all
   six residual products.
4. The three closed templates come from the parent theorem; the remaining
   twelve are covered by six explicit `F`-sign orbits.
5. Each resultant factor is normed separately.  A product norm or sampled
   root check is not substituted for factor coverage.
6. The audit changes the shared equation in the resultant chain.
7. The conclusion deletes two cells, not the common row or either Prize
   theorem.
