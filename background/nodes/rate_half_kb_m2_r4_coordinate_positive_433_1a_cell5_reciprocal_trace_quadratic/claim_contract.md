# Claim contract

| field | value |
|---|---|
| assumptions | Deployed field, cell 5, sign row `(-1,-1)`, three common minors, and every printed common/source guard. |
| claim | The saturated projection to `(b,t)` is the principal reciprocal quartic `(KBRT-1)--(KBRT-2)`, equivalent on `b!=0` to the trace quadratic `(KBRT-3)` with discriminant `(KBRT-4)`. |
| exact engine | Ordinary deployed-characteristic polynomial rings; no function-field backend is used. |
| output | One 19-term elimination generator and its guarded quadratic trace descent. |
| excluded | Reconstruction of `r,c`, signed-pair or colored-edge emptiness, cell/route/K3/Prize closure. |
