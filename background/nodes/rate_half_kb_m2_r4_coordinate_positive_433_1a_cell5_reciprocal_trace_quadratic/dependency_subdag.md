# Dependency sub-DAG

```text
cell-5 ratio exceptional-branch exclusion
                 |
                 v
deployed reciprocal trace quadratic --ev--> rate-half band closure
```

The evidence edge supplies a smaller exact common-curve interface.  It does
not discharge the band-closure target.
