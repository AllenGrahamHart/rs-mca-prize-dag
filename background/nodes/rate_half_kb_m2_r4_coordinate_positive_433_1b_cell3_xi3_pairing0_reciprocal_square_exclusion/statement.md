# Statement

## Claim (KBP1B3-XI3P0-RS-1)

On the guarded positive deployed 433-1b role-cell-3 branch, every atlas case
with missing record `xi=3` (the `df` record), canonical matching index
`0`, any source sign `epsilon in {+-1}^2`, and any target lane
`sigma in {+-1}^2` is empty.

For each source point, the first residual pair `paired(de,de)` has exactly
the three rational branch types `q=B_i/A_i`, `i in {0,1,2}`, subject to
explicit denominator replay. Twenty-four exact rows cover all source signs,
three branches, and both `sigma_o` values; each row checks both `sigma_c`
lanes. These branch rows pay 16 raw target-lane cases, not 48.

No other matching, missing role, complete cell-3 exclusion, rate-half band,
LIST, MCA, or Prize result is claimed.
