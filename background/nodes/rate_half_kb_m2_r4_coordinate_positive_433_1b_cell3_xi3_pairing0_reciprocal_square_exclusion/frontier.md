# Frontier

The paid cell-3 ledger is 832 raw cases: 720 parallel-`DE` cases and 112 cases
at `xi=3,pairing in {0,1,2,3,6,11,14}`.

The remaining `xi=3` pairings are `{4,5,7,8,9,10,12,13}`, grouped under
positive-`de` exchange as `{4,9}`, `{5,12}`, `{7,10}`, and `{8,13}`. Every
pairing at `xi in {4,5,6}` also remains.

Do not infer complete cell-3, K3, LIST, MCA, or Prize closure.
