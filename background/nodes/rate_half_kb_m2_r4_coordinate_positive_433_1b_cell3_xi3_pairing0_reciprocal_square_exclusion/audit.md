# Audit

- The source census is the Cartesian product of four source signs, three
  `q` branches, and both `sigma_o` values: 24 internal rows.
- The identity `paired(q,q)=4(A_0q-B_0)(A_1q-B_1)^2(A_2q-B_2)` proves branch
  completeness; branches are not counted as separate raw target lanes.
- Every row is `COMPLETE`, has two quadratic `y` cuts, and has matching
  direct six-basis and tower norms.
- Candidate parameters are the union of all norm and exceptional-guard roots.
- All 216 source points are explicitly listed and unique within each row.
- All 112 `(y,d)` candidates replay the branch, missing-product,
  missing-sum, and second-pair equations.
- All 224 final colored-pair evaluations are nonzero.
- Witness, target-boundary, free-branch, and unresolved ledgers are empty.
- The 24 branch rows pay 16 raw cases because each raw lane is tested through
  all three exhaustive `q` branches.
- Primary and independent verifiers pin the compiler and census hashes.
