# Lineage

- The global quadratic quotient and compact kernel supply the exact source
  algebra and direct-lift maps.
- The earlier `xi=3,pairing=0` quartic-resultant pilots timed out because they
  eliminated two quartics directly.
- The replacement route first factors `paired(q,q)`, then uses
  `y=1/d^2`; this lowers the source elimination to a quadratic resultant.
- The branch-denominator strata are replayed directly rather than removed by
  an informal genericity assumption.
- The result remains background evidence toward `rate_half_band_closure`.
