# Lineage

The `M2/M3` forced-mate method extends to the three constrained rows only
after respecting their distinct singleton placements.  Candidate-row
minors give one exact finite compiler for the remaining 433 frontier.
