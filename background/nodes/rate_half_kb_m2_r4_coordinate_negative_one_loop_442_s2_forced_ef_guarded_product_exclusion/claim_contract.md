# Claim contract

| field | content |
|---|---|
| claim | The forced-`EF` `S2` product cell is empty on the nonzero outside-representative guard in every common sign row. |
| assumptions | The exact sparse equations, the guard `e!=0`, and all-row identity of the common product data. |
| exact interface | Delete four cells and reduce the accepted invariant-cell frontier from 36 to 32. |
| nonclaim | A raw unit ideal, another `S2` or `S0` deletion, outside `q`, interpolation, coordinate or row closure, or either Prize theorem. |
