# Claim contract

## Direct claim

The auxiliary residue character `Psi` kills all global units and all
`21121`st powers but does not kill the explicit Jacobi element `alpha`.

## Dependencies

- `e1_conductor128_full_unit_circular_basis` supplies a complete list of
  unit generators.
- `e1_qzeta128_p257_j63_stickelberger_relation` supplies
  `(alpha)=I^(2*21121)`.

## Replayed finite claim

Primality, element orders, all 31 unit evaluations, all 32 Jacobi sums at
all 32 embeddings, and `Psi(alpha)=500235` are recomputed exactly.

## Consumer

`e1_qzeta128_p257_j63_fixed_field_nonprincipality_certificate`.
