# Claim contract

- **claim id:** `l1_mersenne_checkpoint_embedded_m2_family`
- **status:** PROVED
- **scope:** all nine rows `n=m(p+1)`, `m in {4,8,16}`
- **proved content:** an injectively indexed family of exactly `n/2`
  unordered two-fiber pairs, all valid at depths `p,p+1`; every corresponding
  normalized pencil has even split-value degree, and exactly `h=2` when
  `m=4`
- **dependencies used:** the exact antipodal `m=2` classification and the
  nine-row Mersenne router
- **nonclaim:** no exhaustivity among `h=2` pencils, no exclusion of
  nonembedded `h>=3`, no existence claim for higher even degrees, no
  row-sharp L1 payment, and no higher-width result
- **evidence:** the complete `(32,7,4)` analogue independently reports that
  all 16 observed pairs have this embedded form; the proof does not use it
