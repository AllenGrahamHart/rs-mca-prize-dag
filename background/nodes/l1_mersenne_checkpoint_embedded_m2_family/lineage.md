# Lineage

- The exact family is inherited from the polynomial-abc classification of
  the `n=2p+2` endpoint.
- The Mersenne normal form identifies all remaining ambient rows as multiples
  of that exact domain order.
- The `(32,7,4)` pilot first exposed the embedding empirically; the proof is
  the subgroup-coset construction and is independent of the census.
