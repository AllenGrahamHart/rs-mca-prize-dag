# Upstream crosswalk

This is a finite exact split-pencil payload for the base-field-normalized
census. It should be treated as a necessary paid family at depths `p,p+1`,
analogous to an explicit planted or subgroup-periodic contribution. It is
not an upper bound on the complete lower-`h` census.
