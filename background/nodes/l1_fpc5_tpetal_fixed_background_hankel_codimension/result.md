# Result

`PROVED`. Every fixed required background set in the `u>=0` arbitrary-`t`
residual produces a full-rank rational Padé-Hankel split-divisor cell of
monic codimension exactly `ell-1`, with exact overlap accounting across the
possible background sets.
