# Upstream crosswalk

- **upstream target:** base-field-normalized interior Padé/Hankel census in
  CAP25 v13.2;
- **local export:** `przchojecki/rs-mca` PR #1151, `LIST: add FPC5 LS6 and
  general t-petal reductions`;
- **pinned head:** `7d4edabf1430779a042c75a1812771cee0bab5b1`;
- **upstream status at pin:** open draft, mergeable;
- **relation:** arbitrary-`t` FPC5 fixed-background codimension
  normalization and exact support-incidence ledger, exported as Section 10
  of `experimental/notes/l1/list_tpetal_joint_anchor_owner_v1.md`.

The `ell-1` Hankel row count exposes the correct base-field normalization
for every fixed required background set. It does not prove an upstream
split-pencil bound or coalesce the background subsets.
