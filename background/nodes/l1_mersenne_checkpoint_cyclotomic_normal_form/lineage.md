# Lineage

- `l1_official_broad_checkpoint_frobenius_periodicity_exclusion` used only
  the gcd of the residual frequencies and left the nine gcd-one rows open.
- This node keeps the full quotient/remainder coordinates of those rows and
  computes every cyclotomic chamber exactly.
- The `(32,7,4)` Modal census was run after the formula was derived and serves
  only to choose between the `h=2` and `h=3` successor routes.
