# Upstream crosswalk

The statement belongs to the base-field-normalized split-pencil census. It
turns the nine residual first-checkpoint rows into explicit prime-field
cyclic-code chambers before any shifted-lattice or split-pencil computation.
An upstream packet may consume `(MCN2)--(MCN6)` as preprocessing, but should
not describe it as a finite-row exclusion or a row-sharp census.
