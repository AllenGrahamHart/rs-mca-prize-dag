# Frontier

The `(r,delta)=(2,4)` row is reduced to:

```text
source genus 0, #Fix(tau x b)=2;
source genus 1, #Fix(tau x b)=0.
```

The next route-deciding attack is to obtain a source-genus lower bound from
the actual complete-source/active equations, or classify the rational and
elliptic bidegree-`(2,2)` outer correspondences and route their right
factors. The `(4,2)` and `(8,1)` rows are unchanged.
