# Repeated-BC cell-3 `BC+` missing-`CF` residual exclusion

- **status:** PROVED
- **field:** `F_2130706433`
- **scope:** residual paired-product systems over the finite missing-`CF` cut

For each of the four source-root sign rows, the colored-missing atlas leaves
four guarded necessary points with target coordinates `(b,c,f)`. For each
point, each outside-cycle sign, and each of the fifteen matchings of

```text
(BE, DE+, DE-, DF+, DF-, EF)
```

into three source deck pairs, impose the three exact paired-product equations
in the remaining target coordinates `(d,e)`. All
`4*4*2*15=480` point/sign/matching systems are empty over the deployed field.
Hence all `4*2*15=120` formal cell-3 `BC+` systems with missing record `CF`
are empty. Full-system transport proves the corresponding 120 cell-6 systems
with missing record `BE` empty.

This does not address missing `DE+/-`, `DF+/-`, or `EF`, the `BC-` branch,
another owner block, a split-BC lane, or the complete route.
