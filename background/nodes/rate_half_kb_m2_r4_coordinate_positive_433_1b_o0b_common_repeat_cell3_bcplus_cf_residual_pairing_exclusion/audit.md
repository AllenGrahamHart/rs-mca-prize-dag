# Audit

The primary verifier pins the launcher, result, and finite missing-cut source;
reconstructs all common kernels and residual record systems; checks each
selected resultant by bounded interpolation; proves every listed projected
root complete using `gcd(R,x^p-x)`; and replays all specialized unit gcds.

Hostile controls delete a case, corrupt a kernel, alter a resultant
coefficient, add a false projected root, and replace a unit fiber gcd.
