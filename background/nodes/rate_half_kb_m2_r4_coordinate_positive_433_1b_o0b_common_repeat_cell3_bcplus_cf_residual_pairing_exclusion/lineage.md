# Lineage

This node consumes the exact sixteen-point missing-`CF` frontier isolated by
the colored-missing atlas. It is deliberately separate from the common torus
and missing-edge cut so that its 480 bivariate residual systems remain a
small, independently replayable proof leaf.
