# Frontier

Together with the colored-missing atlas, both colored missing records `BE`
and `CF` are now closed in cells 3 and 6 for `BC+`. The remaining cell-3
missing records are `DE+`, `DE-`, `DF+`, `DF-`, and `EF`; target-sign and
cell transport should reduce these before new elimination work. The `BC-`
curve and the other surviving owner blocks remain separate.
