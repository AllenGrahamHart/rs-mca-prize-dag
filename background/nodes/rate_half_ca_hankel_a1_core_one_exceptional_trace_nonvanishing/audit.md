# Audit

- Exact degree `r-1` of `q_0` comes from the proved zero-exceptional
  allocation; it is not asserted for an active exceptional trace.
- The quotient identity uses the full product `P`, so its second term vanishes
  at `gamma_0`.
- `X_0=1` is load-bearing: it prevents a cancelled bad-row factor from
  lowering the global degree of `A_a`.
- The contradiction is one degree. Replacing `r-1` by `r` removes it.
- Specialization may lower but cannot raise `X`-degree.
- The two systems in `(ETN6)` remain open.
