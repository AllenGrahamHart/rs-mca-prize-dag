# Claim contract - L1 bounded-polarity marked full-pencil reduction

## Inputs

- `l1_polarized_petal_entropy_ledger` for the sparse/dense coordinate;
- `petal_reserve_rich_fiber_reduction` for `h>=ell`, the exact threshold,
  and maximality;
- `pma_saturated_mixed_support_kernel` for exact-defect coprimality.

## Output

The bounded-polarity, genuinely growing-cofactor branch injects into a
polynomial number of bounded-mark deformations of the full-petal
split-pencil identity.

## Consumer Rule

Future work may target uniform bounded-mark stability of the existing
full-petal payment. Do not cite this reduction as that payment.

## Falsifier

A word satisfying `(MP1)` with fewer than two dense petals, failure of the
cleared identity or degree bounds, or non-injectivity of the canonical data.
