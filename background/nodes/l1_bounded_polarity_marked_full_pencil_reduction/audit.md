# Audit - L1 bounded-polarity marked full-pencil reduction

## Checked axes

1. The two-dense conclusion needs both `p<ell` and strict
   `d>ell+p-1`.
2. The selected deficits are charged inside `p`; no total-deficit `u`
   estimate is used.
3. Both missing-set locators are required when clearing denominators in
   `(MP3)`.
4. The enlarged locator `FJ` has marked roots outside the core; it is not
   silently passed to a theorem requiring all roots in `C`.
5. Exact-defect coprimality is retained after dividing by support locators.
6. The mark census is polynomial only for fixed `P`.

## Remaining attack

Prove that the full-petal split-pencil payment is stable under the bounded
mark factor `J` and the bounded cofactor twists in `(MP3)`, uniformly in the
growing excess `c`.
