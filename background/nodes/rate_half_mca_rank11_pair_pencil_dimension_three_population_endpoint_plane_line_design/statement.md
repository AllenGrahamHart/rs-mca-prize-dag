# Population-endpoint plane-line design

- **status:** PROVED
- **scope:** the scalar-dimension-three branch with the extremal retained
  quotient-type population `q=3170`

For every residual dimension `4960<=K'<=4982`, select the minimum number
`B` of distinct full 218-owner planes needed to carry the proved full-owner
coordinate floor. Then

```text
339<=B<=358.                                        (PL1)
```

Among those `B` planes, at least 22,752 unordered plane pairs intersect in
exactly 15 selected scalar points. Consequently at least 217 distinct affine
scalar lines contain exactly 15 selected types. Every one of these saturated
lines occurs in the residual common cores at least

```text
K'-2609>=2351                                      (PL2)
```

times.

This is a saturated plane-line design router. It does not exclude `q=3170`,
pay a record family, or close rank eleven.
