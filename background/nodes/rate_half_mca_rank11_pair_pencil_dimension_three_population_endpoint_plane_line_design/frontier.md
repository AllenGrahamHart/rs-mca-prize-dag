# Frontier

The `q=3170` endpoint is no longer merely a near-constant coordinate
multiplicity profile. It contains hundreds of endpoint 218-planes and at
least 217 saturated 15-lines, each recurring on thousands of residual
coordinates.

The next exact attack should couple the plane endpoint's internal
210-direction bank across the 22,752 saturated plane intersections, or use
the 217 recurrent lines to construct a chronology-safe owner. A theorem on
one isolated 218-plane still does not pay the complete endpoint.
