# Dependency sub-DAG

```text
dimension_three_type_population_ceiling
                    |
plane218_projective_direction_bank
                    |
                    v
population_endpoint_plane_line_design
                    |
                    v
rate_half_band_crossing_location (evidence only)
```

The endpoint design is conditional on `q=3170`; it does not become a payment
edge.
