# Audit

1. The near target is `tau^*chi_Omega`, with `Omega={xi,d}`. No aligned
   `tau^*q` identity is reused.
2. The moving `C=0` locus is covered by the exact `b->1/b` symmetry of its
   unordered edge pair and target orbit.
3. The factorization of the reconstruction minor is replayed before any
   rational substitution. Its sole nonstandard zero `cd=-1` is a collision
   on every branch forced by the constant gate.
4. The `xi=b` line denominator is checked separately; no vertical component
   is lost in `(7)`.
5. Characteristic-zero resultants identify the complete projected support.
   Independent deployed-prime saturation rules out modular factor growth.
6. The `w=0` branch is retained throughout. It appears only over forbidden
   `d=0` in `(9)`, so this node includes negative forced ramification but
   says nothing about positive forced ramification.
7. The standard-library audit reconstructs the full five-row negative
   system on exact admissible fixtures and checks the near constant identity
   and equality of the two template q-slices without importing the primary.
