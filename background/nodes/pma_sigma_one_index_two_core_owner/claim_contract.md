# Claim contract - PMA sigma-one index-two near-core owner

## Inputs

1. Reed-Solomon uniqueness from `k` agreement points.
2. The exact-periodic global owner and its sharper official finite count.
3. The reciprocal-quadratic construction's four-miss index-two core.
4. The official dyadic `sigma=1` parameters and first-scale profile
   `Q_2(k+2)`.

## Output

A chart-independent owner for every listed codeword missing at most four
points of one index-two coset, with global count below `Q_2(k+2)` and combined
periodic/near-core count below the existing `719(1+2^-690)Q_2(k+2)` line.

## Consumer effect

The reciprocal-quadratic `Omega(n^7)` per-sunflower family is removed before
the mixed diffuse chart ledger. The finite red remainder becomes the diffuse
`(3,0)` complement of `QOWN_per union QOWN_core4`, followed by `d>=4` and
chart composition. The asymptotic remainder is unchanged.

## Falsifier

Two distinct degree-`<k` listed codewords with the same printed `(B,E,J)`
owner, an official row where the owner count exceeds `Q_2(k+2)`, or a counted
reciprocal-quadratic codeword whose agreement set misses more than four points
of both index-two cosets.
