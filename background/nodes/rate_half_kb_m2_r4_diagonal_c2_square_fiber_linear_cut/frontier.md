# Frontier

Treat the ramified orbit first: test whether exact source-facet support,
outer-factor separability, or the complete-source product excludes a common-
`K` label at a branch value of `W=X^2`. For the unramified branch, substitute
the `4/3`-dimensional linear parametrization into the four-fiber identity
of `(KBDM-8)` and the `45 x 12` source interpolation gate. Keep the
exceptional unsaturated `(1,1,2)` orbit and the biquadratic branch separate.
