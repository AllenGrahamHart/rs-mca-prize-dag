# Result

`PROVED`: cofactor `64` is impossible in the prize profile `(3,6,S=18)`.
Five profile cofactors remain:

```text
2, 4, 8, 16, 32.
```

The next branch is `m=32`, with multiplicity five and the bounded parent
window `E<=85` before parity-adaptive contraction.
