# Cell-3 `BC-` uncolored `EF` exclusion

- **status:** PROVED
- **field:** `F_2130706433`
- **scope:** missing `EF` records in repeated-`BC` cell 3

All 120 cell-3 `BC-` systems with missing record `EF` are empty. The
guard-lift atlas leaves 832 relevant common fibers: 240 inconsistent missing
products, 240 zero-product boundaries, and 352 endpoint-bearing fibers.
Every one of the 1,152 endpoint rows has residual gcd one. The record is
fixed by the `d -> -d` quotient, so no second signed orbit is present.
