# Upstream crosswalk - tangent-radical exclusion

For the upstream split-pencil census, this is an exact case exclusion inside
the finite Davenport-Mason ledger. The outer tangent fiber has radical degree
at most `nu+deg H`, while its derivative multiplicity factor has exact degree
`p+deg H-4`. The resulting inequality kills `nu=3` and leaves only two
positive-valuation strata.

This is suitable for an `(A)` / split-pencil theorem packet. It is not full
h=3 emptiness, an exact SPI count, or a finite MCA payment.
