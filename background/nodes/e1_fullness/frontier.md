# Frontier: e1_fullness

The route is now a finite implication rather than an asymptotic density slogan.

## Proved inputs

- `e1_exceptional_set_reduction`: every non-quotient class collision is an
  explicit norm-divisor/folded-kernel incidence.
- `e1_clean_anchor_exact_collision_allowance`: exact class counts, endpoint
  orders, and the conversion `P<=K-B*-1 => |image|>B*`.
- `graded_collision_radius`: small swap-distance collision pairs are absent.
- `are_exceptional_density`: almost-all-primes evidence only.

## Remaining theorem

`e1_official_prime_exception_control` must prove the exact pointwise pair
bound for every row with `|F_p(Q)|>B*`. The current quotient orders are
`256` and `512`; `128` and `256` are their folded dimensions.

The complementary generated-field branch is a proved E1 route cut and must be
paid by another unsafe supplier.

The historical no-vector certificates are stronger than necessary at one
exhibit and insufficient for the family quantifier. The preferred next action
is a pointwise pair-incidence theorem or an exact counterexample above one
printed allowance, not another broad birthday or BKZ campaign.
