# Lineage

The complete-fiber compiler supplies one injective Mobius product map.  The
loop-stratified q compiler independently presents a `3 x 3` two-loop gate.
This node welds those two halves and replaces the determinant by two
coefficient-free scalar identities.
