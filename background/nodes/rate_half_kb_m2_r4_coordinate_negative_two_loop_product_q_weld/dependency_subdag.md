# Dependency sub-DAG

```text
complete-fiber Vieta compiler ---------\
                                        -> two-loop product-q weld --ev--> band closure
loop-stratified negative q compiler ---/
```

Both incoming edges are requirements.  The outgoing edge is narrowing
evidence until the signed two-loop atlas is classified.
