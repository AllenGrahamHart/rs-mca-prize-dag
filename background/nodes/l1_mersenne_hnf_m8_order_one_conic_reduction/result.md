# Result

The smallest live order-one curve has an explicit low-degree model. Future
`h=7` elimination should use (OCR2) directly or the conic plus its quadratic
pullback, instead of reconstructing the ten-term residual from a hash.

No order-one row or critical node is closed by this reduction alone.
