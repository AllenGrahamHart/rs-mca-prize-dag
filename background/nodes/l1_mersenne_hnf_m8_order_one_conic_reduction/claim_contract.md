# Claim contract

| field | value |
|---|---|
| claim | The ten-term `h=7` residual curve is exactly a quadratic in `u=rho*c*(c-1)`, equivalently a reciprocal-quartic square condition and a conic after `z=c+c^-1`. |
| status | PROVED |
| dependency | `l1_mersenne_hnf_order_one_involution_component_exclusion` |
| exact rows | The four official `(m,h)=(8,7)` rows. |
| excluded characteristics | `2,3,5,7`; none is official. |
| chart exceptions | The `t=infinity` point `(-1,-6)` and `247-7t^2=0` projective chart are retained separately; the base point occurs at `t=23/7`. |
| consumer | `l1_mixed_petal_amplification` |
| open residue | Intersect this curve with full traces, pointwise Frobenius, torsion, cyclotomic divisibility, and inner lifts. |

## Falsifier

An order-one point in an official characteristic at which the printed
`Psi_7` vanishes but (OCR2), (OCR4), or (OCR6) fails, or a point lost by the
declared chart decomposition.
