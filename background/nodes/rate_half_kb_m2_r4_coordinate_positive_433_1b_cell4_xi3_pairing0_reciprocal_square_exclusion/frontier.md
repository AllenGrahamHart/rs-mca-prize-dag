# Frontier

This node closes only `xi=3`, matching 0. The immediate exact consequence is
the corresponding `xi=4`, matching-0 exclusion through the already-PROVED
universal outside-role transport.

After that transport payment, cell 4 retains 28 live labels: `xi in {3,4}`
at matching indices 1 through 14. The next useful task is to classify which
matching indices are equivalent under certified residual-row permutations,
then run one reciprocal-square or eliminant proof per surviving orbit.
