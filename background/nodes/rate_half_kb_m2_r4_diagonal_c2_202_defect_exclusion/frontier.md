# Frontier

Remove `(2,0,2)` from every diagonal branch packet. Attack `(1,1,2)` next:
its saturated cases also have two reciprocal doubled square vertices, leaving
only one defect unit for the four `J_0-J_0` and four `J_0-J_1` stars. Classify
the reciprocal occupancy patterns that meet this remaining budget, while
retaining the printed exceptional unsaturated orbit.
