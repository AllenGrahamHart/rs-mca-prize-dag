# Result

`PROVED`. Every live Shape-A coefficient series has only linear and
quadratic minimal syzygies:

```text
E=O(-1)^(2r-e) direct_sum O(-2)^(e-r-1).
```

Each of the two small source classes also determines a unique projective
domain coefficient form with the explicit nonzero barycentric values in
`(LQD8)`. These two forms are the next exact exclusion target.
