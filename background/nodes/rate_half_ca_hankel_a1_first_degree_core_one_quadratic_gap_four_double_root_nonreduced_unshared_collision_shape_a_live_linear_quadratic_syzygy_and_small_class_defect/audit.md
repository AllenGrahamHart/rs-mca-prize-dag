# Audit

1. A small class is essential: its interpolation projection has the
   `r-1` lower bound. Shape A has exactly two such classes.
2. The injection `J -> ker pi_gamma` uses the Koszul relation between the
   other two quadratic generators, so its chosen coordinate is exactly zero.
3. The lower bound on `c_1` is nonnegativity of `c_3`; combining it with the
   coordinate-kernel upper bound is what removes all cubic summands.
4. Exact rank `r-1` makes the left kernel of `(11)` one-dimensional because
   `E_B` has `r` independent rows.
5. The dual-RS vector in `(13)` is unique only projectively, matching the
   scalar `kappa_gamma`.
6. No relation between the two small-class scalars or defect forms is
   asserted.
7. No critical status changes.
