# Audit

The primary verifier pins the launcher and result, checks all 360 semantic
case labels and residual matchings, recomputes every guard digest, checks the
54-guard cover, and verifies the exact degree and full-rank censuses.

Hostile controls delete a case, lower a rank, alter a matching, corrupt a
guard polynomial, and remove a case guard.
