# Result

The forced background factor has been removed from the primitive cofactor
cross determinant. The quotient `E` is an exact affine coordinate on the
sharp guarded monic slice, and one gcd `gcd(E,F_0)` recovers the balanced
determinant owner. The remaining sharp problem is a single split-point count
over this gcd-stratified coordinate body.
