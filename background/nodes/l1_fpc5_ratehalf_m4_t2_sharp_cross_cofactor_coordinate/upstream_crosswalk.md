# Upstream crosswalk

Upstream PR #1125 supplies the balanced exact-shell determinant coordinate
and identifies its gcd with the anchor error locator as the
common-complement owner. This node proves the direct FPC5 realization:

```text
upstream determinant coordinate
  <-> E=(A_1A'_2-A'_1A_2)/L_0
```

at the level needed for ownership: both coordinates recover exactly
`gcd(F_0,F)` and determine the neighbor injectively. No assertion that the
two polynomial coordinates are literally equal under a fixed basis is
needed or made.

The result remains local while PR #1125 is open. It strengthens the local
critical-DAG interface but claims no upstream acceptance, aggregate owner
bound, or row payment.
