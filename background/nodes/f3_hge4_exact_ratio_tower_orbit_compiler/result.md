# Result

`PROVED`. Every primitive HGE4 shift-pair orbit occurs at one exact dyadic
ratio level. A `(21/2)m^2` orbit estimate at each level sums to strictly less
than the required `14n^2` ambient budget. Exact levels have a direct anchored
near-square selector implementation.

The levelwise count remains open.
