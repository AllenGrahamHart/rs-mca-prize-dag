# Provenance

Derived on 2026-08-13 from the fixed-cutoff direction-class theorem by
retaining every direction class as an affine-line slot instead of closing it
with an outside-core cap.  The unsafe-core absorption parent then converts
the line-bank pigeonhole into a contradiction.

The official interval was located by a constant-memory C scan under
RAMguard.  No Modal computation was needed.
