# Dependency sub-DAG

```text
fixed-cutoff boundary-stack theorem ----+
                                        +--> boundary line bank --> MCA router
common-core absorption theorem ---------+
q2 wall repair (interval continuity) ---+
```

All three parents are `PROVED`.  The first supplies the normalized direction
classes and prefix; the second supplies total-core absorption; the third
closes the immediately preceding support.
