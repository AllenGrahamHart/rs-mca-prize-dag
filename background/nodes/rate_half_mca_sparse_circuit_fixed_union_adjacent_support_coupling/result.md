# Result

The fixed-union coupling is no longer support-5/6 specific. At `K'=83`,
the ordinary `(u,g)=(39,7)` and `(38,8)` support-2/3 carriers also control
support 6/7 (and, in the latter case, support 7/8). This pays the diagnosed
ordinary wall through the disjoint pairs 4/5 and 6/7.
