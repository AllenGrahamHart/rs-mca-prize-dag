# Audit

1. Every relevant `S` is a proper subset of a circuit and hence independent.
2. The flat cap uses the actual fixed union, not a circuit-dependent union.
3. `d<=g-1` supplies both adjacent flat ranks.
4. The completion budget after deleting outside points is exactly `R`.
5. The weighted optimization treats both signs of `lambda_i`.
6. Only support-disjoint pair bounds are composed.
