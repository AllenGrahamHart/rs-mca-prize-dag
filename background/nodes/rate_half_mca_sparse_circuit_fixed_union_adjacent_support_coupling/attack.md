# Attack

Audit contraction when `D` contains dependent subsets, the endpoint strata,
the sign of `lambda_i`, and the prohibition on overlapping pair payments.
The fastest falsifier is a small represented matroid violating `(FAS1)`.
