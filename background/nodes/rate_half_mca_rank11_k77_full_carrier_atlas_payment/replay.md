# Replay

Run the parameterized compact audit with argument `77` for the frontier,
digest, 6,623,568 reroute evaluations, and payment. Run six geometry lanes
with the default runner and `five_geom` with `tools/modal_run_script_long.py`.
The geometry total is 143,928,183 leaves; observed peak RSS was 58--62 MB.
