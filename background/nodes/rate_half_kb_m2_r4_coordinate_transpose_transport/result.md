# Result

PROVED. The two coordinate order-two subgroups are one geometric route:
every `<1 x tau>` component transposes to an actual `<tau x 1>` component
with a fresh source presentation satisfying the full coordinate compiler
chain. The coordinate and diagonal routes remain open.
