# Frontier

Do not attack `<1 x tau>` separately. A universal close of the coordinate
source systems closes both coordinate subgroups by transposition. Continue
the order-two program with exactly two geometric routes: coordinate and
diagonal. Packet-specific tests on a transposed component must first build
its primed source record.
