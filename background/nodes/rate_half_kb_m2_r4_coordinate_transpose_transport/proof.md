# Proof

The residual endpoint component lies in the non-diagonal self-
correspondence

```text
f(T)-f(W)=0.                                       (1)
```

Equation `(1)` is unchanged after exchanging `T,W`. Transposition is an
automorphism of the ambient product, so it preserves irreducibility,
bidegree, non-diagonality, projection degrees, and actuality in the same
endpoint record. It is an involution on components.

Conjugation by `theta` exchanges the two generators of

```text
V4=<tau x 1,1 x tau>
```

and fixes their product. Setwise stabilizers therefore obey `(KBTT-1)`,
which gives `(KBTT-2)` and preserves the exact stabilizer order two.

It remains to transport the source interface without reusing data tied to
the old second projection. Rename the coordinates on `Gamma^tr` by
`T'=W,W'=T`. The degree-two decomposition has the same inner map and the
same symmetric endpoint hypotheses, so the source reduction applies to
the second projection `W'`. It supplies a new quadratic base change
`W'=psi'(X')`, deck involution `b'`, and actual bidegree-`(2,4)` source
component `H'`. The equality-wall source-facet theorem and complete-source
saturation are statements for that reoriented actual record, hence supply
the primed sets in `(KBTT-3)`.

Now `Gamma^tr` has stabilizer `<tau x 1>`, so every hypothesis in the
coordinate source-facet, coefficient, colored-resultant, and Vieta-rank
compilers holds for the primed record. Applying those universal theorems
proves the printed chain. Transposing back proves the equivalence of the
two coordinate existence routes. At no point was `H'` identified with a
transpose of a source form based on the old second projection. QED.
