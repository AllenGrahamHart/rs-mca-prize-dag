# Audit

- Outer zeros are challenge-field values because the descended inner map is
  evaluated at challenge-field active roots.
- The unique outer pole and the unique second cyclic branch point are
  separately Galois fixed.
- The cyclic normal form is derived from a divisor over `K`; the order-five
  automorphism itself need not be defined over `K`.
- Both `b=0` and `b!=0` fifth-power zero fibers are checked.
- The node removes `r=1,3` only; `r=2,4` remain live.
