# Frontier

Attack the `r=2` dihedral and `r=4` one-orbit outer components using the
canonical degree-12 pencil and the 21-distinct-source-star-vertex bound.
Do not revisit cyclic `r=1` or primitive-impossible `r=3`.
