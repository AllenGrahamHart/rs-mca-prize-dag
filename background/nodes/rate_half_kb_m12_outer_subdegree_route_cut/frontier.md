# Frontier

The diagonal-socle child deletes `r=2`, and its secondary-degree-five child
deletes the five remaining `r=4` families. The complete `m=12` branch is
closed. Do not revisit cyclic `r=1`, Dickson `r=2`, primitive-impossible
`r=3`, the `r=4` normal forms, tame `AGL(1,5)`, or the two excluded
higher-genus profiles.
