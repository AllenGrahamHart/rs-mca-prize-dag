# Frontier

The diagonal-socle child deletes `r=2`: Scott strips and the actual
size-four suborbit force the block projection to have size four. The live
frontier is therefore the five `r=4` normal-form families only. Compile the
canonical degree-12 pencil against their synchronized one-point-per-block
diagonal correspondence, then impose the source-star divisor. Do not revisit
cyclic `r=1`, Dickson `r=2`, primitive-impossible `r=3`, tame `AGL(1,5)`, or
the two excluded higher-genus profiles.
