# Claim contract

| field | value |
|---|---|
| claim | On the exceptional `E_G=0` coefficient chart, cancellation with the quadratic `F_b` gives one affine equation in `q`; off its singular charts, the retained `X_*` equation and conic endpoint become two explicit univariate polynomials of degrees at most 23 and 16. |
| status | PROVED |
| dependency | `l1_mersenne_hnf_m8_order_one_cubic_three_two_one_generic_fully_proportional_coefficient_bivariate_compiler` |
| consumer | `l1_mixed_petal_amplification` |
| equivalence guards | The printed `a_2` and `S_1` chart guards, plus inherited `b*D_*!=0`. |
| open residue | A gcd/root or retained-component verdict for `(V,X_E)`, the `S_1=S_0=0` chart, the fixed `a_2=0` chart, and all structural, role, `P_4`, and arithmetic-lift equations. |
| nonclaim | No displayed univariate polynomial is asserted nonzero and no chart is asserted empty. |
