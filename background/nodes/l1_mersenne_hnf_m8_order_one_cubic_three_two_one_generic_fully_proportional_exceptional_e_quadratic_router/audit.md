# Audit

The leading-coefficient chart `a_2=0` is retained separately. The affine
eliminant is divided only on `S_1!=0`; its simultaneous `S_1=S_0=0` locus
is retained with `F_b` and `X_*`.

The equations `V=X_E=0` describe only the exceptional coefficient portion.
Every structural, role, `P_4`, saturation, and arithmetic-lift equation
remains mandatory after reconstructing `q`.

The degree ledger is an upper bound before cancellation. It is not a gcd,
resultant, or emptiness certificate.
