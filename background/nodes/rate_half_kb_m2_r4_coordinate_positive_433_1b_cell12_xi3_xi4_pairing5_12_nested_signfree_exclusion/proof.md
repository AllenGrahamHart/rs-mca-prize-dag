# Proof

After deleting `DF`, matching `5` pairs the residual products as

```text
(de,-de), (de,sigma_c cf), (sigma_o ef,bf).
```

Write `m=df`, `S=(d+f)^2`, `z=1/d`, `y=z^2`, and `q=de`. The missing Vieta
equation is

```text
M(y)=1+(2m-S)y+m^2y^2=0.                         (RS12-10)
```

The first pair `A(q)=paired(q,-q)` is quadratic in `u=q^2`. Multiply the
second pair `B(q,z)=paired(q,sigma_c cmz)` by `B(-q,z)`, rewrite in `u,z`,
and reduce modulo `A`. The linear-in-`u` remainder gives a division-free
polynomial in `z`. Reducing modulo `M(z^2)`, separating even and odd powers,
and multiplying the two signs leaves a polynomial in `y`. Its remainder
modulo `M` is linear in every source-sign/`sigma_c` row. The resulting cut
is normed through the exact basis `1,t,b,bt`.

A pinned AST adapter applies the audited nested sign-free compiler to the
cell-12 tower and compact kernel unchanged. Eight rows cover all source
signs and both `sigma_c` anchors. Every deployed root of all norm and inverse
numerators and denominators is included. The 168 case-labeled roots replay
to 256 guarded source points. The boundary ledger has 40 `r`-guard exits,
32 `t`-guard exits, and eight cell-12 `b`-leading exits; the last lie in the
complement paid by the rational-boundary parent.

Direct replay retains 16 compatible `(z,q)` lifts. At each lift both
remaining `sigma_o` lanes evaluate

```text
paired(sigma_o ef,bf).
```

A separate SymPy/Galois-tools audit reconstructs all norm and inverse roots.
The local audit rebuilds the source kernel and all three paired equations at
every lift. All 32 final values are nonzero. There is no witness, final
boundary, or unresolved branch.

Exchanging the identical positive `DE` records maps matching `5` to
matching `12`; outside `D/E` transport maps missing `DF` to missing `EF`.
The exact generic orbit quotient proves both transports and closes all four
labels. QED.
