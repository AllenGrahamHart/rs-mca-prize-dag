# Statement

Over `F_p`, `p=2130706433`, the guarded cell-12 positive `433-1b` route is
empty for missing `DF` and residual matching `5`. Duplicate-positive-`DE`
exchange and exact outside `D/E` transport give the same conclusion for
matching `12` and for the two partner labels with missing `sigma_o EF`.

This closes four labels forming one generic orbit. No other matching is
decided.
