# Audit

The equivalence in (QQW7) is restricted to `alpha!=0`. Since
`alpha=-(q-d)x(x^2+q/6)`, its deleted loci are exactly the inherited
`q=d` saturation and the two separately owned exceptional slopes. Do not
use the weld on either exceptional branch.

The recurrence must be applied to the homogeneous factors (AIF6), not to a
quotient `Q^2/P^3`. Clear denominators only after forming each remainder,
and retain the contents and leading coefficients in any factor audit.

The coefficients `c_(i,j)` are deterministic compiler output, not four new
conjectures. A nonunit system remains only an affine-color-compatible HNF
candidate and owes every outer/inner filter listed in the claim contract.
