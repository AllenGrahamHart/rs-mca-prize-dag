# Result

The generic cubic `2+2+2` chamber is now four denominator-free, equivalent
systems of four equations in `(x,q,d)`. The quadratic fiber variable `b` is
eliminated without a resultant, and no color-role or individual-root
variable remains.
