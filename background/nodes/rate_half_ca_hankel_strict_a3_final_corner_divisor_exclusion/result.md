# Result

The strict `A=3` program is closed. Contact/pole cohomology eliminates every
slope-slack stratum except one, and the final integral corner demands one
more common-divisor multiplicity than its entire omission budget permits.
The rate-half frontier now moves to the residual `A=1` profiles and the
independent crossing/unsafe tasks.
