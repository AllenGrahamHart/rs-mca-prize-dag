# Final strict `A=3` corner divisor exclusion

- **status:** PROVED
- **closure:** complete-fibre divisor and excess-multiplicity contradiction
- **consumer:** `rate_half_band_crossing_location`

The final corner

```text
rho=3e+1,       T=rho+2,       delta=1,       O<=1,
e>3                                                   (FDE1)
```

does not exist. Consequently every strict `A=3` moving-kernel failure on the
official rate-half row is excluded.

## Scope

This closes the strict `A=3` branch only. The residual `A=1` profiles, the
complete adjacent MCA crossing, and the adjacent unsafe witness remain open.
