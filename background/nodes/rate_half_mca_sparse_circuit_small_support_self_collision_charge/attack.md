# Attack

Stress the three boundary cases separately: `s=0` uses annihilator-span
containment, `0<s<q` uses collision counting, and `s=q` is exactly the empty
stratum.  Do not evaluate the collision sum at `s=q`.
