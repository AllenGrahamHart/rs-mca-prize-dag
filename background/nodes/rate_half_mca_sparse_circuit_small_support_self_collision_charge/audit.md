# Audit

1. The support range stops at five, where `12-2c` is still positive.
2. The empty stratum is handled before the collision formula.
3. The `s=0` containment argument uses at most `K` evaluation points.
4. The outside budget includes the `j-1` points already in the deletion.
5. Every outside circuit is charged exactly `j` times.
6. Integer flooring occurs only after the exact incidence division.
7. The selected-set multiplier is support-specific.
