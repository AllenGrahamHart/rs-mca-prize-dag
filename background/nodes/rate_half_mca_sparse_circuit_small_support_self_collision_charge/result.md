# Result

The same-source collision charge is proved for every support `2..5` and
every exact completion defect.  It introduces no premise and is strictly
stronger than a deletion-only fallback on the balanced deep rows.
