# Frontier

For each of the twenty canonical forced-record cells in a common sign row,
form the residual sextic coefficients and reduce any three independent rows
of `(KB41EV-3)` in the rank-six common quotient.  Retain all seven equations
as an audit.  Do not introduce `lambda`, coefficient minors, or residual
matching variables.
