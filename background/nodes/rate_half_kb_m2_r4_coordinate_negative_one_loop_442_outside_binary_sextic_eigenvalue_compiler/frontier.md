# Frontier

The PROVED uniform-row selector identifies `E_0,E_1,E_2` as a basis in all
four rank-six common quotients.  Form those three equations for each
canonical forced-record cell and retain the other four only as an audit.  Do
not introduce `lambda`, coefficient minors, or residual matching variables.
