# Result

The common-divisor seam in the selector transport is now explicit rather
than implicit. Fixed bits are exactly fixed selector roots; factoring them
leaves a gcd-trivial, aperiodic punctured prefix instance. At the saturated
F2 windows the full locator average costs only the rounding imbalance
`2^(2 Delta)`, so a polynomial primitive split-locator theorem would give
the required `2^o(m)` mass bound.

The remaining mathematical supplier is primitive master flatness uniform on
the printed punctured domains. The separate finite first-match and PP5.0
accounting needed for the direct `n^3` target is not closed.
