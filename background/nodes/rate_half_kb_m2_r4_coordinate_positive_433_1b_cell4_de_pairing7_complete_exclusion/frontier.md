# Frontier

The theorem pays `(xi,pairing)=(0,7),(1,7),(2,7)`. The parallel-`DE`
matching involution separately transports `(2,7)` to `(2,10)`, but it does
not pay `(0,10)` or `(1,10)`. Those positive-`DE` matching-10 cases are the
next exact target in this orbit.
