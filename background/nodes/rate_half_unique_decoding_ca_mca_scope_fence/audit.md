# Audit

1. Agreement and radius use `r=n-a`; no real-valued rounding is hidden.
2. The endpoint is closed: equality at `a=3n/4` is retained and agrees with
   `rate_half_half_distance_safe_bracket`.
3. The live interval is open at `3n/4`; even its nearest integer point misses
   the transfer hypothesis by two.
4. The result fences the named unique-decoding theorem only. It must not be
   quoted as evidence that every possible CA/MCA comparison fails beyond half
   distance.
5. No claim is made that ordinary LIST supply controls `B_ca^far`.
