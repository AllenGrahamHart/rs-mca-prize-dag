# Provenance

The scope gate is printed in Przemek's `tex/towards-prize.tex`, theorem
`thm:half` and corollary `cor:import`, and in `tex/cs25_cap_v13_2.tex`, theorem
`thm:mca-from-ca`. The local DAG already imports that theorem as
`mca_from_ca_reduction`. This node adds only the exact specialization to the
Round-31 live rate-half agreement interval.
