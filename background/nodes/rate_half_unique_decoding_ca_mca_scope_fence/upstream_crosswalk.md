# Upstream crosswalk

- **upstream source:** `tex/towards-prize.tex` at
  `93fba1be3f3299b0ba4708d88715377bbb656e45`;
- **source blob:** `14acce1330f5bea76fa7b4b485ba68e9ca0a5b2e`;
- **upstream theorem:** `thm:half` and `cor:import`, with exact hypothesis
  `2 floor(delta*n)<=n-k` for Reed-Solomon codes;
- **relation:** exact official-row scope specialization and route fence.

The source theorem is already upstream. This packet does not propose an
upstream theorem change; it prevents an invalid downstream use below the
printed half-distance boundary.
