# Claim contract

- **Claim:** the exact agreement scope of the named unique-decoding CA/MCA
  transfer at the official maximal rate-half row.
- **Dependency:** `mca_from_ca_reduction`, including its closed integer gate
  `2r<=n-k` and the already-tracked BCIKS CA import.
- **Consumer:** route evidence for `rate_half_band_crossing_location`.
- **Nonclaims:** no beyond-half-distance CA/MCA impossibility theorem, no
  upper bound on `B_ca^far` inside the live interval, and no crossing close.
- **Falsifier:** a live interior agreement satisfying `2(n-a)<=n-k`, or a
  source theorem whose printed hypotheses cover the interior without that
  unique-decoding gate.
- **Replay:** `tools/ramguard tiny -- python3
  background/nodes/rate_half_unique_decoding_ca_mca_scope_fence/verify.py`.
