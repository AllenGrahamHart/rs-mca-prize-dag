# Claim contract

- **claim id:**
  `rate_half_list_budget_three_fiber_two_cycle_mismatch_invariant_coupling_router`
- **mathematical statement:** the `c=1,2` completion-root PGL coupling is
  equivalent to 24 and six explicit binary-quartic invariant tests,
  respectively
- **scope:** generic canonical fiber-two cycle denominator-mismatch strata
  after the exact `D_*` root inventory
- **consumer:** `rate_half_list_adjacent_crossing`
- **status:** `PROVED`
- **proved dependency:** doubled-order fiber-two cycle boundary transfer
- **new open content:** exclude the 30 indexed scalar candidates jointly with
  the canonical coefficient gaps, or derive branch-specific elimination
  gates for their survivors
- **falsifier:** a valid `c=1` or `c=2` completion-root matching absent from
  its finite packet, a packet hit with no PGL matching, or an invariant/count
  mismatch
- **nonclaims:** no candidate is excluded, no matched norm gate is imported,
  and no official-order computation is authorized
- **replay:**
  `tools/ramguard tiny -- python3 background/nodes/rate_half_list_budget_three_fiber_two_cycle_mismatch_invariant_coupling_router/verify.py`
- **upstream mapping:** base-field-normalized split-pencil census; exact
  finite SPI/exchange-degree ledger
