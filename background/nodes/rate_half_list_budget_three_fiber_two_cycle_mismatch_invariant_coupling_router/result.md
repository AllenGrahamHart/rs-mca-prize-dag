# Result

The two denominator-mismatch couplings no longer require a free Mobius map or
a permutation search. The `c=1` stratum is exactly a 24-test binary-quartic
invariant packet, and `c=2` is a six-test packet. The indexed tests may
coincide on special denominators. Each is one printed scalar equality
involving the canonical outer coefficients and the four roots of `D_*`.

This closes the coupling derivation but not either mismatch census. The next
mathematical task is to combine these 30 candidates with the doubled-order
coefficient gaps and canonical span equations, seeking low-degree
eliminations before proposing any computation.
