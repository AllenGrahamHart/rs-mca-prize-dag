# Result

**PROVED.**  Every loopless rank-three matroid with maximum parallel-class
size `a` has at least `(m-1)(m-1-a)/2` bases.  The floor is sharp on the
divisible one-coloop rank-two partition family whenever `m-1>=2a`.
