# Audit

The primary verifier checks the coloop inequality, the smallest-class
contraction estimate, the induction identity, and equality constructions
over a finite parameter grid, then applies hostile contract mutations.

The independent audit computes rank-two partition minima by dynamic
programming rather than the square-sum inequality and separately enumerates
the contraction group-size recurrence.  The textual proof carries the
universal quantifier.
