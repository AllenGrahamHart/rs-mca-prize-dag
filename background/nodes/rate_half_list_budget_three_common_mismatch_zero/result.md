# Result

The common-mismatch parameter in the harvested rank-flat compiler is now
exactly paid:

```text
b=0
```

for every budget-three chamber. Together with the proved affine-rank theorem,
the chamber bridge is reduced from `(s,d_1,d_2,d_3,b)` to the three
generalized weights `(d_1,d_2,d_3)`. Since `b=0` minimizes the rank-flat
denominator, this is a route narrowing rather than a chamber exclusion.
