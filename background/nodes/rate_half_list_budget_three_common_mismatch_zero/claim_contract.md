# Claim contract

- **Claim:** every common zero of the four-word direction code is a common
  agreement at the budget-three predecessor; equivalently `b=0`.
- **Scope:** every field, every `4d`-point RS domain, and every four-word
  witness at agreement at least `3d-1` to which the selected-support
  intersection reduction applies.
- **Dependency:** `rate_half_list_budget_three_intersection_reduction`, used
  only for the proved coverage statement `n_0=0`.
- **Consumer effect:** pays the mismatch coordinate of the chamber-to-rank-
  flat transport. The remaining bridge variables are `d_1,d_2,d_3`.
- **Nonclaims:** no chamber exclusion, generalized-weight formula, list cap,
  official adjacent crossing, MCA statement, or field-specific assertion.
- **Falsifier:** a witness and selected support pattern with `n_0=0` but a
  point where all four codewords coincide away from the received value.
