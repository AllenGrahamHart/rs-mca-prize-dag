# Upstream crosswalk

This theorem supplies the exact `b=0` specialization for Przemek's
generalized-weight rank-flat list compiler (`thm:rank-flat-list` in
`experimental/grande_finale.tex`) on the local budget-three four-word
chambers. It does not alter an upstream Q, BC, SP, K, M, or L terminal without
the remaining generalized-weight transport and a cap of at most three.
