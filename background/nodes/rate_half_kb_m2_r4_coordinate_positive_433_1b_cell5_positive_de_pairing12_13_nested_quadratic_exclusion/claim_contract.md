# Claim Contract

## In scope

- Cell `5`, product rank five, the positive parallel-`DE` roles
  `xi in {0,1}`.
- The generic label orbit represented by `(0,12)`.
- All source signs and target lanes; the row-6 tower has no leading remainder.

## Out of scope

- The 32 labels in 10 active orbits that remain, K3, LIST, MCA, or Prize
  closure.
- Any claim over a nondeployed field.

## Promotion rule

`PROVED` requires complete Frobenius root unions, direct replay of every
`(u,f)` row, exact orbit reconstruction, and no witness or unresolved branch.
