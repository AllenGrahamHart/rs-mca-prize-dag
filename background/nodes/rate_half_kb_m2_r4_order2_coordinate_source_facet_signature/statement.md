# KoalaBear m2 r4 coordinate-order-two source-facet signature

- **status:** PROVED
- **scope:** actual `(m,r,delta)=(2,4,2)` component with stabilizer
  `S=<tau x 1>`
- **dependencies:** `rate_half_kb_m2_v4_outer_recurrence_router` and
  `rate_half_kb_q6_s6_common_five_outgoing_fiber_pin`
- **consumer:** `rate_half_band_closure`

Let `I` be the invariant-coordinate six-set, `L` the invariant-fiber image
six-set, `J=I^c`, and `K subset I intersect L` the common five-set. Put
`eta=L minus K`. Let `bar` be the fixed-point-free involution on source
labels induced by the endpoint quadratic deck involution. The preserving
source lift gives

```text
star(bx)=bar(star(x)).                               (KBO2-1)
```

For the degree-two outgoing component, its 24 complete-source stars have
the exact category census

```text
J-J: 10,       I-I: 10,       I-J: 4.              (KBO2-2)
```

Moreover `bar(I)=I` and `bar(J)=J`. The ten `J-J` stars over the five
`K`-fibers have, up to permutation of the three `bar`-pairs of `J`, one
of exactly two degree profiles:

```text
(4,4),(4,4),(2,2),       or
(4,4),(3,3),(3,3).                                  (KBO2-3)
```

These consequences do not delete the coordinate-order-two orientation.
There is an explicit aligned (`L=I`) abstract source-facet assignment satisfying every
horizontal facet containment, the two-regular pole graph, four
component-color edges,
`(KBO2-1)--(KBO2-3)`, source degree four at every label, and defect zero.

No actual algebraic component is constructed. The diagonal order-two
orientation `S=<tau x tau>` and the trivial-stabilizer type remain open,
and no owner, payment, K3, KoalaBear row, or Prize result is proved.

## Falsifier

An actual coordinate-order-two component with a star outside the category
census, an endpoint deck involution mixing `I` and `J`, a `K`-degree
profile outside `(KBO2-3)`, or failure of the printed abstract fixture.
