# Audit

1. Category counts use divisor multiplicity, not an unramified shortcut.
2. The involution-preservation proof uses the four-incidence complement
   and parity of cross-pairs.
3. The two degree profiles are exhaustive partitions of two across three
   involution pairs.
4. The fixture checks full one-exchange facets and pole-graph degrees, not
   only an unlabeled 4-regular graph.
5. The fixture is explicitly nonalgebraic and proves only insufficiency of
   the named source-facet/star ledgers.
6. The general count uses `eta in L minus K`. Only the route-fence fixture
   specializes to the allowed aligned case `L=I`.
