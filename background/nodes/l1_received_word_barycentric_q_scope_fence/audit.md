# Audit - L1 received-word barycentric Q scope fence

## Checked axes

1. The moment range is `0<=j<a-k`, exactly `a-k` equations.
2. The derivative is the complete moving locator derivative `L_A'(x)`.
3. The zero-moment equivalence is triangular, not coordinatewise equality at
   depths above one.
4. The comparison is with locator coefficients, not interpolant coefficients.
5. Both witness triples have the same root sum modulo seven.
6. The four exchange supports contain each moving point with equal
   multiplicity on both sides.
7. The exchange defect is nonzero modulo seven.
8. The zero-prefix witness has no extra agreement inside the printed domain.
9. The counterexample excludes only a literal affine fixed-column map.
10. Nonlinear and chart-specific transports remain explicitly open.

## Route effect

This theorem prevents `def:q-row-atom` from being imported into L1 by name
alone. It replaces that shortcut with one exact target: normalize the moving
barycentric weights on all primitive exact shells, or prove their row-sharp
zero-fiber bound directly.
