# Claim contract - L1 received-word barycentric Q scope fence

## Inputs

- the exact-shell interpolation-prefix map;
- Lagrange interpolation on distinct field points;
- upstream's printed primitive-Q requirement of a fixed-column linear map.

## Outputs

- the exact barycentric moment form `(BQ1)--(BQ2)`;
- a same-locator-prefix/different-received-prefix witness;
- an exchange witness excluding a literal affine fixed-column representation.

## Consumer rule

Call the global L1 route "received-word barycentric prefix flatness" unless a
map-and-owner transport to upstream locator Q has been proved. Do not consume
locator-prefix, toy power-sum, or F2 Myerson results directly.

## Nonclaims

No max-fiber statement is refuted. No nonlinear target transformation is
excluded. No chart-specific normalization is excluded. The theorem fences
only direct reuse of the current fixed-column locator-Q interface.

## Falsifier

A failure of `(BQ2)`, a zero exchange defect in the printed `F_7` witness, or
a fixed-column formula reproducing all four printed received-word prefixes.
