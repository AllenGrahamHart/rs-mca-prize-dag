# Upstream crosswalk - received-word barycentric Q scope fence

## Upstream locator Q

Grande Finale v3 defines a primitive prefix syndrome as a linear map
`Phi:Z^T->B^R`. In the unprofiled locator chart, Newton identities identify
its fibers with those of the fixed power-sum columns. The finite row atom
`def:q-row-atom` counts a first-match residual inside one such locator-prefix
fiber.

## Local exact-shell map

The L1 exact-shell map sends a support to the high coefficients of the
received-word interpolant. Its dual columns have weights
`U(x)/L_A'(x)`, which move with `A`. The printed `F_7` witness proves that the
depth-one map is not determined by the depth-one locator prefix and is not
literally additive on the fixed-size slice.

## Portable conclusion

The upstream proof architecture remains relevant, especially its demand for
first-match residuals, effective-image normalization, and separate quotient
payment. The existing theorem statements do not yet cover this arbitrary-word
barycentric map. A portable future result must either extend primitive Q to
these moving weights or prove a complete chart transport before applying the
finite row constants.
