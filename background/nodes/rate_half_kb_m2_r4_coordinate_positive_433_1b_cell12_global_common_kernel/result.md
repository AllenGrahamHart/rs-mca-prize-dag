# Result

Cell `12` now has a compact global common-kernel certificate.  Later outside
attacks can contract each missing record directly against one fixed
eight-coordinate vector instead of repeatedly solving the full ten-row
Vieta system.

No outside family has yet been excluded by this node.
