# Statement

On the complete guarded product-rank-five common locus of deployed positive
`433-1b -> O0a` source role cell `12`, there is one primitive polynomial
kernel

```text
K = (K_0,...,K_7) in F_p[t,r,c,b]^8
```

with these properties:

1. `K` is independent of the two source signs and of the six
   product-cofactor charts.
2. Its scalar product with each of the five product rows, the `LA` sum row,
   and the `AC` pivot sum row is the zero polynomial.
3. Its scalar products with the `AB`, `BC+`, and `BC-` sum rows reduce to
   zero modulo the exact guarded common ideal for all four source-sign rows.
4. The same kernel vanishes on all eight deployed rational points in the
   leading-boundary complement of the elliptic four-basis chart.

The result provides a global coefficient vector for subsequent
outside-record compatibility tests.  It does not itself impose or exclude an
outside row and does not close cell `12`.
