# Lineage

This node is deliberately separated from the elliptic common-locus theorem.
The parent establishes where the common points live; this node establishes a
single coefficient vector on them.  The split keeps future outside-family
nodes dependent on the precise algebraic input they use and avoids repeating
the large common-locus packet.
