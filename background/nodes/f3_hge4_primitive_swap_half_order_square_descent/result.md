# Result

`PROVED`. The odd swap branch is a deterministic perfect-square locator test
on the half-order subgroup. Its orbit count is unchanged, while its sign
enumeration factor `2^(h-1)` is removed exactly.
