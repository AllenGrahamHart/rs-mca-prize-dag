# Proof

The universal transport parent proves that

```text
(d,e,f) -> (D,E,F) = (sigma_o e, sigma_o d, f)
```

is a guarded involution on every positive deployed 433-1b common role cell.
It transposes exactly atlas rows `xi=3` and `xi=4`, fixes every other product
and squared-sum row, and preserves the compact residual order. Hence it maps
a cell-4 system with missing `xi=4` to a cell-4 system with missing `xi=3`
at the same canonical matching index.

The map also fixes source signs, target lane, target guards, and role cell 4.
In particular, matching 1 maps to matching 1 and matching 2 maps to matching
2. The other required parent proves both target `xi=3` blocks empty. The
bijective preimages are therefore empty.

Four source signs, four target lanes, and two matchings give 32 transported
raw cases. QED.
