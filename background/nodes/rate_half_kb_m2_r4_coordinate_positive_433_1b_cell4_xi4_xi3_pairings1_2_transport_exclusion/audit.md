# Audit

This is a two-parent logical corollary, not a second census. The verifier
reexecutes the pinned universal transport certificate and checks both parent
statuses and both required DAG edges.

Scope alignment is exact at each of matchings 1 and 2. The 32-case payment
counts source signs, target lanes, and matching indices once; it does not
multiply by the direct parent's three internal rational branches.
