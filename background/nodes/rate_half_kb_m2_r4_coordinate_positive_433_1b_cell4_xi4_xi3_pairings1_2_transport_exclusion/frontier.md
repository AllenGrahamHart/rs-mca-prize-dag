# Frontier

Cell 4 retains `xi in {3,4}` at the six matching-exchange pairs
`{3,6}`, `{4,9}`, `{5,12}`, `{7,10}`, `{8,13}`, and `{11,14}`. By the
universal transport, only the 12 `xi=3` labels need independent proof.

The next target should be one representative of the lowest-degree surviving
pair, followed by the proved parallel-`DE` matching exchange and `xi4 -> xi3`
transport compositions.
