# Result

The clean tensor dependence now has an exact cofactor ledger. Its degree-
`2m-1` scalar factors as the regular determinant times the marked fibre
polynomial, while at least `2m+3` supported slopes turn the complete
`rho`-subset Cauchy-Binet sum into the outer square of a fully split locator.
The next proof can target this printed subset identity rather than an
unspecified Hankel incompatibility.
