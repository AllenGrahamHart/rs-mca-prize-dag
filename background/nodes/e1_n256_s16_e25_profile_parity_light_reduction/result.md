# Result

`PROVED`: every pair-feasible `N=256`, folded-profile `(3,4,0)`, `V=50`
collision lies in one of nine exact profiles over one of 111 affine light
templates. The exact cubic cutoff is `M_3=13`; this node is a router, not an
exclusion.
