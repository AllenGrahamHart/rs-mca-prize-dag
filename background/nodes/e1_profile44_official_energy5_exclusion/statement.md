# E1 profile-(4,4) official energy-five exclusion

- **status:** PROVED
- **closure:** complete realized-parity resultant census and independent replay
- **scope:** binding prize rate-`1/8` row, profile `(4,4,S=20)`

No official profile-`(4,4)` collision has autocorrelation energy `E=5`.
Together with the proved exclusion of `E<=4`, every collision satisfies

```text
E>=6,                 V=2E>=12.                       (P44-E6)
```

Consequently every collision cofactor satisfies

```text
m<=853574.                                             (P44-M6)
```

The exact current cofactor frontier contracts from `645` to `608` values.
All thirteen formerly surviving pure powers remain, so this is not an orbit
count or a payment of the seven-orbit target.

The exhaustive computation uses only parity masks realized by four
singleton positions. There are exactly `1785` five-lag masks and `31`
one-lag masks, giving

```text
1785*2^5 + 31*62*4 = 64808
```

signed energy-five spectra. Two independent eight-shard exact resultant
censuses found no integer cofactor interval and no official hit.

## Falsifier

An energy-five collision on the official row, a realizable parity mask
missing from the `1785/31` census, or an exact replay with a nonempty
integer cofactor interval.
