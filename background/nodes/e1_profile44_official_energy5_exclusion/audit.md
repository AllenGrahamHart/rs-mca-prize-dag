# Audit

The audit checks:

- both integer partitions of energy five;
- all `333375` normalized singleton supports and exact unique-mask counts;
- the `64808` signed-spectrum count;
- source hashes and three Modal run identities;
- complete return of both eight-shard norm censuses;
- empty primary hits and the independent stronger zero-integer-interval
  result;
- exact reconstruction of the `608` retained cofactors;
- hostile mutations of masks, spectra, hits, source hashes, and survivor
  count.

The primary and independent Modal scripts use different support loops, mask
representations, spectrum orders, and interval tests. Both use FLINT for the
final exact resultant.
