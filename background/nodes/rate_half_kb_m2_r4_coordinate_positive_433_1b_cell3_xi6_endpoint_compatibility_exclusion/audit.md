# Audit

- The cleared endpoint identity is checked formally and replayed at every
  eligible lifted source point.
- Each exact row uses the quadratic-over-cubic tower norm in the six-element
  quotient basis.
- Candidate parameters are the union of norm roots and every exceptional
  inverse-guard or base-leading root.
- The four `xi=6` rows contain 32 candidate parameters and 32 recovered
  source points: 8 missing-product boundaries and 24 nonzero compatibility
  evaluations.
- All 24 eligible `xi=6` compatibility evaluations are nonzero.
- Compatible-source and unresolved ledgers are empty for `xi=6`.
- The condition is source-only, so four computed rows cover all 15 matchings
  and all four target lanes: `4*15*4=240` raw cases.
- The 24 compatible `xi=5` sources are explicitly retained and are not called
  target witnesses or exclusions.
- Primary and independent verifiers pin the compiler and census hashes.
