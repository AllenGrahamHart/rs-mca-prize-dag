# Result

The formerly undifferentiated quotient-distance-at-least-four branch begins
only at distance `183251937965`. Every distance from four through
`183251937964` is impossible by an exact incidence contradiction.

The exceptional-only profile now splits into one completely normalized
distance-three resultant-power chart and a genuinely high-distance tail.
Future classifiers must not allocate or enumerate small higher-distance
supports.
