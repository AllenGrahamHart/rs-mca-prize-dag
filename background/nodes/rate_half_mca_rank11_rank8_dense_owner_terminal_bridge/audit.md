# Audit

1. `q_p` weights coordinate pairs and `t_p` weights records; their product
   is in the marked `(record,T)` unit.
2. Comparing with `200631*Q` forces the integer floor `200632`.
3. The unrounded ratio, not a sampled rounded sequence, propagates the
   boundary inequality.
4. The deficiency formula uses the invariant support complement `981104`.
5. `delta>=5` gives at most `196221` records, strictly below `200632`.
6. The explicit multi-owner fence remains compatible with the theorem.
