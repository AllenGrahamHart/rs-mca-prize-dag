# Upstream crosswalk - L1 exact-shell prefix/Hankel bridge

## Exact match

Upstream `l1_arbitrary_fiber_repair.md` proves that `ImgFib`, maximal support,
and canonical selector are the correct list objects and that the raw support
fiber overcounts higher-agreement codewords. Equations `(EH1)--(EH3)` sharpen
that repair into disjoint exact first-match shells.

Upstream Grande Finale v3 defines primitive Q as a first-match residual
max-fiber problem for a proved prefix map. The sets `E_a(U)` are the exact
received-word prefix zero shells that an L1 use of this input must bound.

## Scope mismatch retained

The current upstream toy packets enumerate the unprofiled power-sum map on
subsets of a multiplicative subgroup. The L1 sunflower chart is the Hankel map
`T_(X,d)` on split defect locators. Equation `(EH6)` proves that its **zero
condition** is a chart of the global received-word interpolation prefix, but
does not turn it into the toy power-sum distribution or the special F2
zero-prefix map.

Therefore a future upstream import may close L1 only if it states row-sharp Q
for the received-word exact shells, or proves a chart transport covering these
Hankel cells after first-match and quotient payment.
