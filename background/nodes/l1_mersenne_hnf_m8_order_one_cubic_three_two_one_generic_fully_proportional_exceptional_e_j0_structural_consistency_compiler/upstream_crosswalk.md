# Upstream crosswalk

This extends the exact primitive shift-pair/second-moment ledger for the
official `h=7`, cubic `3+2+1`, fully proportional exceptional coefficient
residue. In upstream notation it follows the `J_*=L_*=0` affine router and
uses `Q_0=q^2/3` to reconstruct the retained `G_2`, leaving two bounded
univariate structural filters.

It belongs in the existing draft h=7 cubic profile-reductions PR. It is not
an emptiness claim and does not consume the role, `P_4`, or lift layers.
