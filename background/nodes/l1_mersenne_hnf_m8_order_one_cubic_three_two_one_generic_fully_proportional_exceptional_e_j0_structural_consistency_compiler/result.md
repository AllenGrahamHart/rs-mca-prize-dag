# Result

`PROVED`: the exceptional `J_*=L_*=0` chart now reconstructs `G_2` and all
other structural variables. Two additional univariate filters of degrees at
most 24 and 16 complete its coefficient-and-structural endpoint.

The six-filter common roots, role packet, `P_4`, saturations, arithmetic
lifts, and the critical consumer remain open.
