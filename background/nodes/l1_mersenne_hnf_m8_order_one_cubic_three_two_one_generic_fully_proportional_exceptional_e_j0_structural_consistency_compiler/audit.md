# Audit

- `G_2` is reconstructed from the original `Q_0` definition only after
  `X_*=0` has supplied `Q_0=q^2/3`; no coefficient equation is reused as a
  structural verdict without this bridge.
- The division by `A=-(b+3)/2` uses the inherited `A!=0` saturation.
- `Z_R^j` is jointly reversible with `Z_D^j`: before `D=YV`, the difference
  between the original and simplified `R_0` residuals is explicitly
  `(A+x)(YV-D)`.
- The univariate degree bounds use uncancelled numerator representatives and
  the proved `T!=0` guard. Cancellation can only lower them.
- Role, `P_4`, saturation, and arithmetic-lift equations are not consumed.
- The verifier sources are complete but intentionally unexecuted locally;
  only syntax, manifest, DAG, and hash checks are authorized without Modal.
