# Proof

Pairing the two degree-7 and the two degree-11 denominator factors under
`c<->d` gives two symmetric equations. For every literal assignment, descend
these equations to symmetric coordinates and substitute the exact
`V`-nonzero reconstruction `w=-U/V`. Remove only factors belonging to the
transported named open.

The resulting union equations have degrees `25,60` on `R02` and `28,62` on
`R20`. Intersect each with the selected cubic resultant factor, the two
remaining q-slice cores, and the literal `J11` factor. Exact deployed-prime
Groebner bases have sizes `17,36` on `R02` and `22,45` on `R20`, independently
of assignment. They are dimension one and nonunit, but sequential reduction
of the complete square-free transported named localizer reaches zero in each
of the twelve ideals. Thus every named-open intersection is empty over the
base field and every extension. QED.
