# Audit

The primary packet recomputes the slack dynamic program, all 13 energy
profiles, the exact rational cubic boundary, the parity split, and both atlas
hashes. The first Modal launch `ap-7jev6lqubs6DibhWhq1kBo` completed the remote
algebra but failed before writing a packet because of a local path error; it is
not evidence. The corrected complete packet is from
`ap-6FCOpigQ0DnR00JZXFRPB5`.

The independent checker uses a recursive profile enumerator, Gaussian
elimination for the Hermite basis, and direct affine-orbit expansion. It
expands the 87 and 1,234 representatives disjointly to all 8,168 and 280,720
required supports and recomputes the direct-vector floor.

All replay is verifier-scale; no large census is rerun locally.
