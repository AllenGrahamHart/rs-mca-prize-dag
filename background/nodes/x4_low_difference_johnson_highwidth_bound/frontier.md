# Frontier

Restrict the next `d=1` attack to

```text
t_XR+2<=e<=N/4.
```

Use the proved `1024` fixed-projection codegree there.  The missing theorem is
a first-owner-compatible bound on one distinct-locator projection in this
interval.  Do not recount the paid high-width band.
