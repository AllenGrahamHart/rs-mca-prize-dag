# W3 attack disposition

The exact packet
`background/nodes/ww_spending_cell_fiber_layout_counterexample/` fires against
an all-cell extension but not against W3's literal safe-side quantifier.

No further W3 computation is authorized under the project budget because the
node is no longer consumed. The next mathematical target on the live list
path is the explicit `l1_mixed_petal_amplification` input to `list_safe`,
together with the separate rate-half adjacent-crossing target.

Any proposal to restore W3 to the prize path must first show that its upper
bound is actually consumed by the repaired list-crossing assembly. Any
proposal to extend it beyond the safe side must state how the fiber-layout
counterexample is owned.
