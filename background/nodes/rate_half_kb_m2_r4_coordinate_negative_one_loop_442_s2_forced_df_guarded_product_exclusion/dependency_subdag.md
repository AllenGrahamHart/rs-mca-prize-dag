# Dependency sub-DAG

```text
preceding all-row S2 exclusions --+
all-row common product data ------+--> S2 forced-DF guarded exclusion
```

The exclusion supplies product-level evidence to `rate_half_band_closure`.
