# Claim contract

| field | content |
|---|---|
| claim | The forced-`DF` `S2` product cell is empty on the nonzero outside-representative guard in every common sign row. |
| assumptions | The exact sparse equations, the guard `d!=0`, and all-row identity of the common product data. |
| exact interface | Delete four cells and reduce the accepted invariant-cell frontier from 32 to 28. |
| nonclaim | A raw unit ideal, the forced-loop `S2` or an `S0` deletion, outside `q`, interpolation, coordinate or row closure, or either Prize theorem. |
