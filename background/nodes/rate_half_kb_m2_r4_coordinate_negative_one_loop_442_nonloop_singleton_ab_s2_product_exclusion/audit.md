# Audit

1. All four forced-record orbits are represented.
2. Both deployed `b` roots are replayed separately.
3. Raw-unit and guard-saturated deletions are distinguished.
4. The `c -> -c` transport is coefficient-level, not inferred from a
   numerical sample.
5. No q or interpolation conclusion is drawn.
