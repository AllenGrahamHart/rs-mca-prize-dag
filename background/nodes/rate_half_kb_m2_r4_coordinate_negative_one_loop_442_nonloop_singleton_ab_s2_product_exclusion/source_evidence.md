# Source evidence

- Sparse builder and Buchberger engine:
  `experiments/prize_resolution/rate_half_kb_one_loop_442_cell36_outside_buchberger.py`.
- Primary verifier replays all four cells in deployed `b` row zero.
- Audit verifier replays all four cells in deployed `b` row one.
