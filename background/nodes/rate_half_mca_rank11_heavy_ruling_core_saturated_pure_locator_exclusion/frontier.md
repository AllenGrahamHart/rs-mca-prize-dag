# Frontier

Two outputs remain.

1. **Rational profile:** exploit the 24-record anchor line and two large
   saturated pair cores to constrain `Q`, its domain roots, and the affine
   locator scalar. Pole cancellation should be applied before any root-free
   atom claim.
2. **High complexity:** retain the exact decomposition into one 24-record
   pair block and at most four two-record pair blocks when translating
   `chi>=2299571` into a spread or support-alignment payment.

The pure-locator exception is closed for this packet and should not be
reintroduced downstream.
