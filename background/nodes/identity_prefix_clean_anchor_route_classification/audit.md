# Audit

Date: 2026-07-26.

The candidate table is pinned to
`critical/nodes/xr_smallcore_spread_count/notes/audit_consumption_replay_20260710.py`
with SHA-256
`c39442d16fcbe86bbfd97f245de970dc729d0e257514c6d4f9f74c9a8c7fac56`.

The five negative verdicts use only the exact numerator budget and dimension;
they do not require a characteristic, field order, or generated-subfield
guess. The positive RowC rate-`1/16` branch is deliberately conditional on a
proved domain field because the repository explicitly records the RowC prime
as unpinned. Using an invented prime or silently setting `b=q` would violate
the claim contract.

The verifier exhausts small floor intervals for both router implications,
recomputes all six anchor verdicts, derives the base-field cutoff from
`binom(1024,66)`, and checks boundary mutations.
