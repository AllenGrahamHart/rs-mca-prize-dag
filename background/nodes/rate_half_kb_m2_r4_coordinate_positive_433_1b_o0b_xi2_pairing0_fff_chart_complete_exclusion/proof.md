# Proof

Every field element `t` is either outside the exhaustive fourteen-root set or
inside it. The generic-root dichotomy excludes the first class. The
exceptional-fiber theorem excludes all fourteen members of the second class
using denominator-free equations. These classes partition the base field, so
the `FFF` chart is empty. QED.
