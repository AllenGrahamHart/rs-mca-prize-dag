# Audit

The two required nodes use the same exhaustive root ledger. The aggregate
asserts only the set partition `F_p = (F_p minus roots) union roots`; there is
no uncovered boundary or overlap assumption.
