# Upstream crosswalk - boundary-Q planted-root descent

## Polynomial-led case

Upstream Proposition `prop:q-boundary-divisor` uses `g_1=(1,P_z)`.  Then
`D=1`, and `(PR1)` is the standard prescribed-top-coefficient locator-Q
fiber.

## Rational case

For nonconstant `W_1`, this node first removes the canonical planted owner
`gcd(W_1,Omega)`.  The residual multiplier is invertible on the punctured
domain, but the split family remains the projective rational cell `(PR5)`.
This is the list-side object to be handled by upstream's quotient/prefix
pullback priority map; the existing fixed-column Q statement does not cover
it without a transport theorem.

## Remaining input

Prove a row-sharp root-free rational-Q estimate or a numerator-preserving
first-match transport to the active polynomial-led Q atom.  Any transport
must state whether it survives arbitrary canonical puncturing.
