# Result

The fully proportional generic residue now has explicit reconstructions of
both quotient constants and a discriminant weld. Its conic content is one
printed polynomial `F_N` in `b,q`; `d` is only a root of `P_4`. No role
packet is excluded.
