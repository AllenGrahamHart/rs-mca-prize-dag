# Claim contract

| field | value |
|---|---|
| claim | On the fully proportional quotient chart, the conic equations reconstruct `R_0` and leave one explicit polynomial `F_N`; the role equations reconstruct `S_0` and weld the discriminants of `P_4` and the selected role quadratic. |
| status | PROVED |
| dependency | `l1_mersenne_hnf_m8_order_one_cubic_three_two_one_generic_doubly_singular_quadratic_quotient_router` |
| consumer | `l1_mixed_petal_amplification` |
| equivalence guards | Inherited `q*d*(q-d)*D*K_6*R*S!=0`; generic `Delta*W!=0`; proved `b=4x-15!=0`; selected role has `c_0*delta_Phi!=0`. |
| open residue | Unit or retained-point verdict for (FPR5), separately for all 21 role packets, and every arithmetic-lift filter. |
| nonclaim | The discriminant weld is not a prime-field square-class contradiction because the coefficient variables are not forced into `F_p`. |
