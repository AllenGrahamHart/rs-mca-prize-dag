# Audit

The division by `b=4x-15` occurs only after `b=0` is excluded directly from
`N_1=0`. The integer 630 is a unit on every official characteristic.

The role reduction is packet-specific. Its coefficients
`(c_2,c_1,c_0)` come from one of the 21 alternative irreducible quadratics;
they must not be mixed or imposed simultaneously.

The discriminant weld records equality of square classes in the ambient
quadratic field. It is not an `F_p` nonsquare contradiction, and no
Frobenius conclusion follows without an additional base-field or norm
transport theorem.

Equation (FPR5) retains `P_4=0`, all generic nonzero factors, and every
arithmetic lift. Its equation count is not an emptiness certificate.
