# Claim contract

## Input

One exact `m'`-coordinate support in the ten-dimensional residual
correction space, with loopless evaluation matroid and generalized-MDS
common-zero bound `|Z(H)|<=K'-dim(H)`.

## Output

The joint nine-shadow resource inequality

```text
sum_d C(d+2,2) I_d / C(K'-d-9,2) <= C(m',9).
```

## Nonclaim

No complete kernel payment, affine-owner payment, rank-eleven payment, or
prize closure is asserted.
