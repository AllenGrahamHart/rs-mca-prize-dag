# Lineage

The positive-valuation tangent argument localized roots with a monomial
factor. At zero valuation the Euler factorization instead localizes every
tangent root directly on one level set of `H`. Comparing that degree with the
exact derivative deficit closes the two middle eliminant cases when `b` is
nonzero.
