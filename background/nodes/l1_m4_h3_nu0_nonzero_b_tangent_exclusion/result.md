# Result

On the nonzero-`b` arm of `nu=0`, eliminant degrees one and two are empty.
Degree zero has an exact eliminant value and one equation for `R(0)`, while
degree three has only two or three distinct tangent roots, all lying on
`H=kappa`. The zero-`b` arm and both endpoint degrees remain open, so L1 is
not promoted.
