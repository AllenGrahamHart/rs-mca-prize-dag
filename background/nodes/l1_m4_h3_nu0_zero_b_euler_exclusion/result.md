# Result

The complete zero-`b`, zero-valuation endpoint is empty on every official
`m=4,h=3` row. On the only two rows surviving the value-coset certificate,
the Euler identity forces `a/R(0)^2=-3/2`, contradicting the invariant
`q^2+3q+1=0` outside characteristic five.
