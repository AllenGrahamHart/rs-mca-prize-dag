# Lineage

This node resolves the two characteristic-dependent zero-`b` packets left by
`l1_m4_h3_nu0_zero_b_value_coset_certificate`. It combines the packet
invariant with an exact value of the low-degree Euler eliminant on the full
split fiber `R=0`.
