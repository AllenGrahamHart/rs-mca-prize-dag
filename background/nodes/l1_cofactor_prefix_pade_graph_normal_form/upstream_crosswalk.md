# Upstream crosswalk - L1 cofactor-prefix Pade graph

## Finite Q refinement

At scalar cofactor `e=0`, the graph is exactly one upstream locator-Q target.
For `e>0`, L1 does not require an arbitrary union of unrelated Q atoms: its
targets form the explicit graph

```text
Lhat=Uhat/Qhat mod T^(w+e+1).
```

This is a candidate normal form for upstream's separate arbitrary-word
list-interior term, not a theorem about its currently posed active Q atom.

## Shift-pair interface

Two split divisors in the same graph recover cofactors `Q_1,Q_2` and produce
the decorated shift-pair equation already compiled in
`l1_growing_cofactor_decorated_shift_pair_compiler`.  Thus the graph count is
the first moment and that compiler is its exact off-diagonal second moment.

## Portable target

A PR-grade open statement in upstream terminology is:

> After first-match quotient and field-drop payments, bound the intersection
> of the primitive split-divisor prefix image with every realized reciprocal
> Pade graph by the row's printed full-slice constant.

The statement must retain attained-image collapse and the list-interior owner;
it must not be advertised as already following from `prob:row-sharp-q`.
