# Result

`PROVED`. Selected actual-support intersections are transverse, not
tangent. This makes the extremal full locator and split biform coprime. In
the first strict profile, padding first forces any common component to be
`X`-linear and parameter-high; an exact row/fiber incidence count then
excludes it. The paired curve systems are coprime in both profiles.
