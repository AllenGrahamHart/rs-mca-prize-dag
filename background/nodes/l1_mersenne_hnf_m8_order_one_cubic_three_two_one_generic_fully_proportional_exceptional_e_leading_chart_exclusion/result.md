# Result

`PROVED`: the exceptional `a_2=0` leading chart is empty in all four official
characteristics. The fixed conic value and `E_G=0` alone give a nonzero
integer obstruction; `X_*=0` and the structural equations are not needed on
this branch.

The generic exceptional endpoint and its `S_1=0` and `J_*=0` singular charts
remain open. No Modal computation was used.
