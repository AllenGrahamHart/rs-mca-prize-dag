# Result

Two more WCL leaves are fixed-dimensional. The `(1,5)` relation problem is a
three-variable degree-five divisor ideal, and `(2,7)` is a four-variable
degree-seven divisor ideal. Both ideals are the unit ideal over `Q`; complete
integer Nullstellensatz certificates replace raw support or prime sweeps.
