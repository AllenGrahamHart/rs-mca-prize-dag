# Statement

Over `F_2130706433`, the guarded cell-11 positive `433-1b` route is empty for
`xi=3`, residual pairings `1` and `2`, and every required source and target
sign lane. Exact outside-role transport closes the two orbits
`{(3,1),(4,1)}` and `{(3,2),(4,2)}`.

Each pairing has its own complete row and terminal ledger. No other matching
or role cell is claimed.
