# Proof

Any admissible point on the surviving cubic/`J11` route satisfies the full
`J` product identity. The proved logarithmic router is exhaustive: if an
individual constant factor vanishes, the point lies on one of the two
symmetric denominator unions; otherwise logarithmic differentiation is
valid and the degree-67 numerator vanishes.

The denominator-union exclusion proves both unions empty for each target.
The guarded-numerator exclusion proves the complementary numerator branch
empty for each target. Thus every case in the router is empty, so the
surviving cubic/full-`J` route is empty in `F04-R02` and `F04-R20`. QED.
