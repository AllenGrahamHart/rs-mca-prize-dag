# Result

PROVED. A live profile-`(3,4,0)` collision has singleton reduction
multiplicity at most five at `X=1`. A live profile-`(4,2,0)`
collision has singleton reduction multiplicity at most sixteen, equivalently
its two singleton exponents are not congruent modulo 32.

These are exact necessary conditions. Neither first-band profile nor the
route-wide collision ledger is closed.
