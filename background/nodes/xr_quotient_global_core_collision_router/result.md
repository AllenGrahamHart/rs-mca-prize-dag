# Result

`PROVED`. Across the whole agreement induction, globally singleton quotient
fiber cores fit the original `B_quot_ub(A_0)` slot. Every repeated core is an
existing size-`k` collision class, routed to P-A on the generic branch and to
the mismatch bridge otherwise. No independent quotient-image computation is
needed.
