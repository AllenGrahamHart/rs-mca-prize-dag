# Audit

1. The quartic descent is derived as a resultant, not inferred from samples.
2. The forced product is reduced from its protected numerator/denominator.
3. All three forced-type orbits and both signs are covered.
4. The signed-pair shortcut is used only where its hypotheses hold.
5. Primary projection zeros at `DF` indices `6,7,8` are discharged by the
   original three-equation deployed-field ideals.
6. The independent verifier changes the shared resultant equation and
   covers all 54 retained sign/matching cases.
7. Positive loop exchange has `c'=c/b` and no horizontal minus sign; both
   differences from the negative-sign transport are checked explicitly.
8. The conclusion is only a complete-product row deletion.
