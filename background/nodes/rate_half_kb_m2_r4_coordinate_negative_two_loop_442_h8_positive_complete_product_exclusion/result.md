# Result

PROVED.  Both positive-sign eighth-root common rows have no complete
paired-product lift.  The live `442` frontier is exactly the two `H6` rows,
12 invariant cells, and matching-subcase cap 156.
