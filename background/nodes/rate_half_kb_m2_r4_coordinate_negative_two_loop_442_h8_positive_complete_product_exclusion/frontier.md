# Frontier

Analyze `H6,tau=-1` and `H6,tau=+1`.  Their row equations are quadratic in
`b`, so reduce the involution and forced product directly over those two
quadratic algebras, eliminate the same three forced-type residual sextics,
and keep the signs separate because their locators and protected norms
differ.  Do not carry any H8 row into full interpolation.
