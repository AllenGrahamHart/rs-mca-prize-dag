# Result

The distance-three residual generator is no longer a free biform. Its `e`
internal fibers interpolate it exactly from a perfect matching of the
exceptional roots, `e` distinct internal slopes, and `e` nonzero scalars.
The resulting pair-Lagrange formula also supplies explicit parameter-root
patterns and an intrinsic minimal separated basis of size `e+1`.

The external classification problem is now concrete: determine when this
normal form has exactly `3e` additional completely split fibers outside the
canonical support, subject to the near-saturated outside-row ledger and the
remaining Hankel/reciprocal identities.
