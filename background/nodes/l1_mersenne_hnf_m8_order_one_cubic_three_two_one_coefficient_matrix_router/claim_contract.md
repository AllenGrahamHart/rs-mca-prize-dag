# Claim contract

| field | value |
|---|---|
| claim | The scaled fourth/fifth coefficient equations form an exact two-by-two linear system with an explicit generic/singular determinant split; the singular `J=0` branch reduces to three equations in `(q,d)`. |
| status | PROVED |
| dependency | `l1_mersenne_hnf_m8_order_one_cubic_three_two_one_scaled_quadratic_core_compiler` |
| consumer | `l1_mixed_petal_amplification` |
| equivalence guards | `D(q-d)K_6!=0`; `Delta` and `J` are split without division. |
| open residue | Unit verdicts for every routed branch and all exact-fiber, norm, cyclotomic, Frobenius-converse, and inner-lift filters. |
