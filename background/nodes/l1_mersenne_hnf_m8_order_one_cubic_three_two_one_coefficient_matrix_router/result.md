# Result

The official `3+2+1` coefficient core now has an exact determinant router.
The generic branch has two Cramer equations, the determinant-zero branch is
split without lost components, and its `J=0` arm is reduced to a small
three-equation system in `(q,d)` before the remaining arithmetic filters.
