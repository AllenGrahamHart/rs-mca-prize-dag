# Result

`PROVED`: for each official role packet, the J-zero chart's role and `P_4`
layer reduces exactly to two additional univariate filters of degrees at
most 24 and 34, followed by an explicit two-branch reconstruction of `d`.

Common roots, reconstructed-branch saturations, arithmetic lifts, other
`h=7` shapes, and the critical consumer remain open.
