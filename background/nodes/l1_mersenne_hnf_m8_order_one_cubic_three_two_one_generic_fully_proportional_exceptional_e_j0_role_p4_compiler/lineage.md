# Lineage

The fully proportional parameter reduction had already shown that the
selected role quadratic and `P_4` have a zero remainder precisely when two
role-proportionality coefficients vanish. The preceding J-zero structural
compiler makes every coefficient in those two equations rational in `b`.

This node combines the two facts: it compiles the role remainder to two
univariate filters and uses a root of the fixed role quadratic to reconstruct
the formerly retained parameter `d`.
