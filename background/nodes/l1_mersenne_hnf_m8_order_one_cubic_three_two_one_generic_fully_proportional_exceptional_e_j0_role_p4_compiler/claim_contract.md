# Claim contract

| field | value |
|---|---|
| claim | For each fixed official role quadratic on the J-zero chart, two role filters become univariate of degrees at most 24 and 34; either role root reconstructs `d`, and the exact quotient identity then recovers `P_4=0`. |
| status | PROVED |
| dependency | `l1_mersenne_hnf_m8_order_one_cubic_three_two_one_generic_fully_proportional_exceptional_e_j0_structural_consistency_compiler` |
| consumer | `l1_mixed_petal_amplification` |
| equivalence guards | Inherited `b*(b+3)*D_*T*q*R_j*c_0*delta_Phi!=0` and every earlier saturation. |
| alternatives | The 21 official irreducible role quadratics are disjunctive packets; each has two ambient roots `eta`, both retained until saturations and lifts are checked. |
| complete compiled endpoint | The six coefficient/structural filters plus `Lhat_Phi=What_Phi=0`, with `q=5bM/T` and `d=3(eta R_j-S_j)/q`. |
| open residue | Common roots for each role packet, every inherited saturation on the reconstructed variables, arithmetic lifts, and the remaining h=7 shapes. |
| nonclaim | No eight-filter gcd is asserted unit, no reconstructed branch is asserted to pass a lift, and no role packet, chart, or critical node is asserted empty or closed. |
| replay | `verify.py` constructs sparse rational numerator representatives and checks the degree/DAG packet; `verify_audit.py` independently checks the role/P4 quotient identity on rational samples. Both are intentionally unexecuted locally under the Modal-only computation rule. |
