# Lineage

The all-infinity chart closed first. A complete seven-mask pilot then showed
that every chart with exactly one finite root follows the same forbidden
`b=-1` boundary, while every chart with two or three finite roots remains a
computational obstruction.
