# Frontier

The remaining masks are `FFF`, `FFI`, `FIF`, and `IFF`. Direct resultants,
direct Rabinowitsch inversion, and an eight-variable sparse kernel lift have
all timed out. The next route must use a structural common-root reduction or
a different algebra engine on one mask; no four-mask Singular rerun is
authorized.
