# Statement

For case

```text
(cell=3,S0,sigma_o=-1,epsilon=(-1,-1),xi=2,pairing=0),
```

all three projective chart masks with exactly one finite common root,
`FII`, `IFI`, and `IIF`, are empty after the ordinary guards and rank-cofactor
saturation.
