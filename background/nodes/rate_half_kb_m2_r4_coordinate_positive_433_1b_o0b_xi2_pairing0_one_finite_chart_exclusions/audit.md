# Audit

The checker pins all seven remaining chart masks and accepts complete unit,
complete nonunit, or exact timeout rows without favoring the outcome. Its
hostile audit rejects an incomplete collection, wrong row order, and a
missing chart equation. The node verifier separately requires that precisely
`FII`, `IFI`, and `IIF` are unit and checks each printed `b+1` transition.
