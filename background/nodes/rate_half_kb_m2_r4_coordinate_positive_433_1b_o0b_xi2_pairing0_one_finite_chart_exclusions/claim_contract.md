# Claim contract

- The three results are exact over `F_2130706433` and its algebraic closure.
- Exactly masks `FII`, `IFI`, and `IIF` are closed.
- The all-infinity chart is closed by a separate node.
- Masks `FFF`, `FFI`, `FIF`, and `IFF` remain open; their timeouts have no
  mathematical status.
- Unit after saturation proves guarded emptiness, not raw ideal emptiness.
