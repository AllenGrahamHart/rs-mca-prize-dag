# Result

Every pair of actual quadratic-packet error supports has union at least
`rho+2`. The complete coefficient rank is one lower than in the core-free
case, cutting any assigned-center line to roughly half the prior cap and
forcing about half the supported slopes to expand every fixed locator pair.
