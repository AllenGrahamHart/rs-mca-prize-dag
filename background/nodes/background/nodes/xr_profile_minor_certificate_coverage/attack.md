# ATTACK - xr_profile_minor_certificate_coverage

Do not run E32 locally on the WSL laptop.

This is now an assembly node. The remaining obligation is
`xr_profile_minor_record_inventory_payload`, not determinant semantics. For
each budget-meeting unpaid non-boundary light profile, find one of:

- a triangular specialization of the MDS normal-form matrix whose diagonal
  entries are nonzero and whose off-triangular forced entries vanish; or
- a profile-dependent monomial in a maximal minor that cannot cancel; or
- a complete remote certificate table listing a nonzero maximal-minor
  specialization for every profile in the intended range.

Toy E32-MERGED/E32-COORD data remains evidence only. It does not by itself
cover the uniform profile quantifier.
