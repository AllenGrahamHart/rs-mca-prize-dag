# Claim contract - PMA sigma-one low-defect payment

## Proves

1. A field-independent exact upper formula for every finite `d<=2` source
   cell, after restoring missed-core and background choices.
2. The uniform per-sunflower bound `B_low<n^5/1024` on all official rows.
3. Safe compatibility with the prior exact-periodic owner by overcount.

## Does not prove

- an arbitrary-word sunflower atlas or its chart multiplicity;
- the final mixed-branch allowance;
- any `d>=3` source bound;
- the growing-reserve `GROW union RES` theorem.

## Consumer effect

On the finite track, replace the ambient B11 `J/A2` census by `(L1)`. The
remaining source theorem is exactly `GROW={d>=3}`. The later
`pma_sigma_one_first_layout_domination` theorem removes the finite chart
multiplier; this node alone does not do so.
