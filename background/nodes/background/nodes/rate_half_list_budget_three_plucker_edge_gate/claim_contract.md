# Claim contract

- **claim:** every B*=3 split-pencil witness satisfies the bounded Plucker
  identity `(PG1)`, rank-two parameterization `(PG2)`, and two-generator
  subgroup factorization `(PG3)`;
- **scope:** the complete six-type normal form over any field and distinct
  evaluation set, with `(PG3)` specialized to the official multiplicative
  coset through its vanishing polynomial;
- **requirement:** `rate_half_list_budget_three_split_pencil_normal_form`;
- **consumer:** the B*=3 branch of `rate_half_list_adjacent_crossing`;
- **new attack order:** classify degree-at-most-two Plucker edge sextuples,
  then the two remaining large block locators;
- **nonclaim:** no edge pattern or two-generator factorization is excluded,
  and no adjacent list crossing is promoted;
- **falsifier:** a valid split-pencil system violating `(PG1)`, any minor or
  coordinate of `(PG2)`, `(PG3)`, or the bound `deg R<=8`.
