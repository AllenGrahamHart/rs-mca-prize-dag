# Result

The quartic separated-factor boundary is impossible: its three omitted
domain rows alone would consume at least `3(e-1)` units of dominant-component
capacity, while the exact ledger permits only `e`.

Thus every profile on the official core-one maximal-degree sharp cap has
`K!=0`. The nonzero-weld coupled matrix factorization remains open. No Modal
computation was used.
