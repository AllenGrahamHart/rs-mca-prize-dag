# ATTACK - m720_residual_off_laptop_zero_certificates

The residual metadata is proved, but it exposes two over-ceiling complete-window
cells: `n=32,h=16` at `q_exp in {2,3}`. Do not run these locally.

Preferred closure routes:

- retrieve completed off-laptop logs for those two cells;
- produce compact certificates for their anchored non-toral counts;
- correct the certificate policy if over-ceiling complete-window cells are not
  meant to be complete certificate obligations.
