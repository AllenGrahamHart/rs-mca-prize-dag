# petal_residue_kernel_linear_bound

- **status:** PROVED
- **closure:** proof

## Statement

In the L1 coset-chart full-petal residue bridge, the residue-line kernel

```text
K_{I,d} = ker(pi_{>d} R_{I,d})
```

satisfies the proved Lemma 13 bound

```text
dim K_{I,d} <= c + 1,    c = d - ell.
```

## Falsifier

A coset-chart residue instance violating `dim K_{I,d} <= c+1`.
