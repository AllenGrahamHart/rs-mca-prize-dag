# conditional: aqb_c2_average_family

This node is conditional on:

- `aqb_family_certificate_semantics`
- `aqb_c2_family_certificate_payload`

The first dependency is proved. The second must provide the concrete
averaged-family certificate: finite member parametrization, shared
quotient/fiber data, reusable charge data, and transfer maps to list
witnesses at `sigma*`.
