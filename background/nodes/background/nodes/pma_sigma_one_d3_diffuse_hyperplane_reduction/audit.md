# Audit - PMA sigma-one diffuse hyperplane reduction

1. The five points are in distinct petals because the residual has no full
   petal. Distinct planted scalars are load-bearing in the nonzero-hyperplane
   proof.
2. Barycentric weights are nonzero because all evaluation points are distinct.
3. The certificate uses the first five agreements, so codewords with more
   agreements are not multiplied.
4. The split-locator count allows arbitrary core sets and all characteristics;
   it uses no division by 2, 3, or 6 inside the field.
5. The global bound has order `n^7`. The later reciprocal-quadratic
   construction realizes that exponent with actual primitive source objects,
   and the index-two near-core theorem supplies the required global owner. Any
   reduced count must explicitly restrict to its complement.
6. Small-field census growth and the official large-field rows must remain
   separate. Neither one alone decides the aggregate hyperplane incidence.
