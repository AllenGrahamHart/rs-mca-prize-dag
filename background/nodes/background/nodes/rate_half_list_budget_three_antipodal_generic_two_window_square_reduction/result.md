# Result

The nested generic secondary calculation has collapsed to one direct square
congruence between two length-`h` windows of `E^(-1/4)`. The tail is also
governed by a first-order differential equation with linear forcing. Future
attacks should use `(TWS5)--(TWS6)` before constructing `B`, `C`, or the full
canonical span data.

This is a compression theorem, not a boundary exclusion. The official
dyadic two-window square problem and every surviving canonical-span/Mobius
certificate remain open.

The bounded order-128 census is banked under
`experiments/prize_resolution/rate_half_list_order128_two_window_result.*`.
No further raw fixed-order sweep is indicated. The exact parity/univariate
reduction of its deleted-antipodal-pair orbits is now proved in
`rate_half_list_budget_three_antipodal_generic_deleted_pair_parity_reduction`;
its common-root problem remains open.
