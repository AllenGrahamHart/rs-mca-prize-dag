# Result

The all-linear `K_4` edge chamber is a line on `Gr(2,4)`. Its four large
locators satisfy one nondegenerate base-field constant relation with all
coefficients nonzero, while their product misses the subgroup polynomial by
only eight roots. The remaining `K_4` problem is therefore a four-term
split-unit equation, not a six-edge coupling search.
