# Result

The core-one dominant component now has its own exact norm-power and
complementary factorizations. Its only possible omitted slope root is at the
single middle-Hankel regular zero, its norm residual degree is
`e-5b-1+D_*`, and at least `14m+5b` residual domain fibers split completely.

This is the precise mixed high-rank object still requiring exclusion. No
Modal computation was used.
