# Result

For every official rate-half row with `B* in {1,2}`, the full common-support
interleaved list challenge is determined for every arity:

```text
a_L=3n/4,       delta_L=1/4.
```

The safe coordinate is preserved by sub-square-root projection collapse, and
the unsafe predecessor is preserved by diagonal embedding. Larger budgets
remain open.
