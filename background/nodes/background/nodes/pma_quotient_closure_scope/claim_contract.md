# Claim contract - PMA quotient-closure scope

## Proved output

1. The precise folded-source injection is contained in the global
   exact-periodic owner.
2. Low-defect error supports have a chart-free predicate, canonical
   first-match order, exact boundary filter, and reserve-preserving periodic
   multisequence reduction.
3. Boundary stripping plus only the raw word-free periodic candidate count
   cannot fit the official first-scale `719Q_2` line.
4. The PMA router may set `QCLOSE` empty without losing exhaustiveness,
   provided the direct residual statement retains every low-defect survivor.

## Not proved

- an actual low-defect fixed-syndrome payment;
- a bound for algebraically folded codewords against nonfolded receivers;
- finite B11 threshold choices;
- the `GROW union RES` theorem or its final allowance.

## Promotion impact

This packet removes a routing ambiguity and narrows the red PMA leaf. It is a
proved supplier to the amber mixed-petal implication and evidence for the red
leaf, not a decomposition of the red leaf into new conjectures.
