# Result

The explicit antipodal component no longer hides inside a large locator
search. After the four-point quotient, it is exactly a two-dimensional
base-field pencil of four pairwise-coprime degree-`2^37-1` factors partitioning
the order-`2^39` subgroup polynomial apart from four named roots.

This is the object that a finite split-pencil census must exclude or
construct. The reduction uses no official-scale computation and is directly
portable to the upstream base-field-normalized split-pencil program.
