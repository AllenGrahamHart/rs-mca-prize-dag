# Result

The two live active systems now have an exact incidence-only reconstruction
gate. Their clean and saturated root partitions determine all fiber scalars
through a connected nonincidence graph; cycle consistency plus `r+1`
Reed--Solomon membership tests reconstructs the unique compatible biform.
The same packet determines its exact separation rank from either locator
coefficient matrix or the core value matrix; every live packet has rank at
least five, and the `b=0` branch has rank `e+1`. The remaining classification
can therefore reject incidence packets before introducing Hankel or
elimination variables. The packet includes a standalone prime-field pilot
checker, an exact `X^3-t` fixture over `F_97`, and three negative controls.
