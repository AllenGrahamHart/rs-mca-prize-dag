# Result

The small dominant norm residual is exactly the product of the bad-row
degree-defect quotients. In the trace-free branch every bad-domain factor
divides both `A,B`; the apparent `W` allocation contradicts its own
exceptional-root degree.

The exceptional slope has two exact allocations, and the full system reduces
to the complement square `(TFA7)`. The active-trace and reduced-square
branches remain open. No Modal computation was used.
