# Claim contract - PMA sigma-one paired-core abundance obstruction

## Claim

The exact mean number of paired cores is `(PA2)`. At one certified official
rate-half cyclic row this mean exceeds `n^6`, so unweighted enumeration of all
paired cores cannot prove the Post allowance.

## Consumes

- `pma_sigma_one_paired_core_normalization`: the paired-core quotient map and
  two-anchor uniqueness;
- elementary interpolation, set partitions, and first-moment averaging;
- an exact prime/divisibility and big-integer certificate for the finite row.

## Serves

- `pma_wide_residual`: forbids replacing the weighted sum `sum_C #E_C` by a
  bare count of all paired cores; the subsequent first-layout domination
  theorem supplies the selected bypass;
- `petal_mixed_amplification`: pins the needed theorem to Post-carrying mass,
  empty-core classification, chart-free ownership, or first-layout
  domination.

## Nonclaims

No Post codeword lower bound, `imgfib` counterexample, generated-field
entropy transfer, or infinite-row asymptotic is claimed.
