# ATTACK - m720_complete_calibration_certificates

Status: conditional.

The certificate semantics are proved in `m720_certificate_semantics`. Do not
run the full scan locally.

Use off-laptop execution for complete cells. A valid certificate must include
the cell parameters, `complete=true` flag, active-core count, and a clear
distinction between full cells and window slices.
