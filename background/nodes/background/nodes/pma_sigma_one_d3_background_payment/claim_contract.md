# Claim contract - PMA sigma-one defect-three background payment

## Proves

1. The exact source-pinned upper charge for `(d,r)=(3,1)`.
2. Its field-independent `n^6/9216` payment on every official row.
3. A combined `n^6/8192` per-sunflower payment with all `d<=2` cells.

## Does not prove

- payment of `(d,r)=(3,0)` or any `d>=4` cell;
- global chart composition, which the later first-layout theorem resolves;
- an asymptotic-reserve theorem.

## Consumer effect

The finite source residual begins at `(d,r)=(3,0)`, together with all higher
defects. This node does not remove the chart multiplier; the later
first-layout theorem does.
