# Dependency sub-DAG

```text
rate_half_list_budget_three_quadratic_scroll_full_rank [PROVED] --req-->
  rate_half_list_budget_three_quadratic_scroll_primitive_module [PROVED]
    --ev--> rate_half_list_adjacent_crossing [TARGET]
```

The parent supplies the invertible balanced basis and exact chamber data. The
new node proves coprimality, exact generator degrees, and directness. It
remains evidence because the resulting official-subgroup factorization is
not excluded.
