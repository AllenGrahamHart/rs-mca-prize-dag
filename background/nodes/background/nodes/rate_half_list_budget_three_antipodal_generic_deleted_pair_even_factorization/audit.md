# Audit

The conclusion uses the full canonical span identity, not merely the primary
and secondary gaps. The order-128 deleted-pair packets fail before that point
and are not positive fixtures for `(DEF3)`.

The exact degree statement uses `deg Cbar=h-3`, which belongs to the generic
floor boundary. If `Cbar` drops degree, the candidate lies on a different
stratum and `(4)` cannot be reused unchanged.

The two factors are not asserted to be subgroup cosets. The proved primitive
quotient gate already excludes the direct coset construction; `(DEF3)` is a
more general square-pencil partition and remains to be classified.
