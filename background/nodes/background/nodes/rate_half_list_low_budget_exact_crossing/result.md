# Result

The rate-half ordinary-list crossing is now exact on the complete budget
branches `B*=1` and `B*=2`:

```text
a_L(C)=3n/4,       delta_L(C)=1/4.
```

The safe side is the exact-integer Johnson theorem. The predecessor is paid
by explicit lists of two and three Reed-Solomon codewords. Budgets `B*>=3`
remain open, so `rate_half_list_adjacent_crossing` remains `TARGET`.
