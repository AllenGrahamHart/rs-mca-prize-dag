# Result

The last unstructured half-distance Hankel family now has an all-degree
router after separating its fixed core `s=0,1,2`. Each residual generator is
a rational normal curve, and every counterexample lies in an exact
core/degree/slack chamber with matching norm and component ledgers.

At the first live degree, only `3+4+4` slack strata remain and all retain a
positive saturation guarantee. No stratum is excluded and no Modal
computation was used.
