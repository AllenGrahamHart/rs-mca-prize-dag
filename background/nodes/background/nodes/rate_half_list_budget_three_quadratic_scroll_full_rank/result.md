# Result

All four quadratic B*=3 chambers are full-rank balanced scrolls. Their
coefficient determinant is explicit and nonzero, so the quadratic
rank-deficient split-unit branch is empty. The sole quadratic arithmetic
object is the standard module `(alpha,Xalpha,beta,Xbeta)`.
