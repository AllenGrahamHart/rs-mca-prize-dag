# Audit

- `L_13` means the coefficient of `X`, even when `b_13` is quadratic. The
  balancing parameter cancels from the determinant exactly.
- The pendant proof uses only the tight linear factors `b_12,b_03`; it does
  not assume anything about a slack quotient root.
- The `K_4-e` proof uses exact quadratic degree of `b_23`, not merely its
  degree upper bound.
- Every division is by the nonzero constant `b_01` or nonzero leading
  coefficient of the tight factor `b_03`.
- The conclusion removes only the rank-deficient scroll subbranch. It does
  not turn the full-rank module into a split-unit equation.
