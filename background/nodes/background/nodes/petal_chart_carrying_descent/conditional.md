# petal_chart_carrying_descent — CONDITIONAL packet (2026-07-13)

- **status:** CONDITIONAL (amber) — Claims 1 (structure theorem S1-S6),
  2 (uniqueness U0-U2, margin exactly 1), 3-L0 (K_2 reduction) PROVED
  (csp packet, audited PACKET-SOUND: every step re-derived, 4 fresh cells,
  7 mutation controls); 3-C conditional on the SINGLE hypothesis below.
- **the open hypothesis (G1 coverage, wired req from petal_g1_layer_maps):**
  for the descended word u1 of each fiber-aligned sigma=1 chart word at an
  official row (k even), G1 supplies per received word a finite atlas
  A'(u1) of reading-A band charts (W, P) at the quotient row — W subset
  core'' = Z' union {y_nf} with y_nf in W, 1 <= |W| <= k'-1, |P| <=
  (k'-|W|)+2 — covering every contributor class S' with y_nf in S' and
  k'+1 <= |S'| <= k'+2, within G1's weighted budget (#131: ~4e4
  charts/word at fiber-rich cells). By S2, u1 is itself a concrete
  sunflower chart word — this is G1's quotient-closed clause, NOT a new
  demand; coverage is satisfiable in principle (each S'-minimal chart is
  automatically a band chart).
- **#136 (consumption map):** Claim 3-C does NOT consume the tail-collapse
  chain — those three edges feed only obligation (ii)'s m'+1 safety line.
- **#137 (annihilating-drift hardening):** layer-normalized labels EMPTY
  the descended census — downstream checkers MUST assert census
  nonemptiness or pin counts (a wrong-normalization "no violations" pass
  is vacuous).
- **scoped out by name:** sigma > 1; odd k; direct M > 2 (replaced by the
  L0 reduction, #130).
- **re-surgery:** the coverage hypothesis falsified (a class no atlas
  covers within budget); any battery cell contradicting Claims 1/2; the
  reading-A pin re-read.
