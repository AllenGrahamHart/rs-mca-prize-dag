# Refutation - pma_wide_residual

The finite claim required `#Post<=B_post`, where
`B_post=n^6-N_top` at rate half. The PROVED node
`pma_sigma_one_d4_generic_source_obstruction` supplies a valid official source
word with more than `n^6` primitive post-owner maximal-source codewords.

The exact agreement supports have size `k+1`, hence trivial stabilizer; their
balanced core keeps them outside the growing dyadic near-coset owner; and the
source is not an odd lift. Therefore all of them remain in the disjoint
`Top union Post` class. Since `#Top<=N_top`,

```text
#Post > n^6-N_top = B_post.
```

This is the node's literal falsifier. First-layout domination remains true;
it shows that the counterexample mass is already present in one layout rather
than arising from chart multiplication.
