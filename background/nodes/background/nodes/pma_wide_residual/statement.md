## MASTER ORIGINAL STATEMENT (preserved at wave-8 import)

> RESTATED after the pullback counterexample (verified): split L(D) = L_quot u L_lowdef u L_prim. The quotient-pullback stratum is REAL and exponential-in-M (pma_pullback_lists) — charged to the quotient-profile budget (M = O(log n) at the cutoff => poly-but-budgeted). The low-defect stratum is covered by the fixed-excess machinery. The corrected target: THE PRIMITIVE RESIDUAL — non-pullback, non-low-defect wide sub-Johnson lists are polynomial. The third appearance of strip-quotient-first (after XR's E2 caveat and F's scale recursion).

---

# pma_wide_residual

- **status:** REFUTED
- **role:** retired finite `n^6` post-top-band allocation
- **consumer:** `petal_mixed_amplification`

## Refutation

`pma_sigma_one_d4_generic_source_obstruction` constructs, by an exact
first-moment argument at the valid official row

```text
(q,n,k)=(65537^2,65536,32768),
```

a maximal source word with more than `n^6` primitive post-owner exact-defect-
four codewords. The proved Top/Post ledger would cap their union by `n^6` if
the finite clause below held. Hence `#Post<=B_post` and the stronger
one-layout residual inequality are false.

The remainder of this file records the route that was falsified. It is not a
live proof target. The replacement frontier is the re-posed
`petal_mixed_amplification`, which asks for a correct polynomial/profile
allocation and explicitly retains this generic defect-four floor.

## Refuted statement

Fix an official-row received/source word and first-match its
maximal-sunflower contributors by carried source layout. The aggregate class
outside the proved layout-anchored top-band atlas consists of below-floor
full-petal contributions and mixed/diffuse partial-petal contributions. For
each carried source layout use the auxiliary instance supplied by
`pma_aux_list_reduction`: disjoint petals `T_i`, a defect locator `L_D` of
degree `d`, scalars `c_i`, background `R_P`, auxiliary word

```text
U*(x)=c_i L_D(x) on T_i,  U*(x)=0 on R_P,
```

and required agreement `N_aux=d+1+sigma-|R_P|`. Work in the wide
sub-Johnson regime `N_aux^2<=d|T|`. Put `ell=sigma+1` and use the exact
source-to-weighted-RS dictionary of `pma_source_paving_bridge`.

The proved `pma_exact_periodic_owner` is applied before any sunflower chart:
all source codewords whose full agreement set has nontrivial exact stabilizer
receive one natural-scale owner. On the official `sigma=1` grid they are
charged by one global first-scale line; at other reserves the complete profile
envelope must pay the theorem's explicit scale sum.

On the official finite track, the proved
`pma_sigma_one_dyadic_near_coset_owner` is applied next. Writing `n=2^j` and
`s_n=floor(n/(32j))`, it canonically owns every remaining codeword missing at
most `s_n` points of any dyadic coset of size at least `k-s_n`. Its global
count is below `Q_2(k+2)`; together with the
exact-periodic owner it uses less than `8(1+2^-690)Q_2(k+2)` of the existing
`719` line. This class contains the entire reciprocal-quadratic construction
and has no chart multiplier. This finite owner is not imported at general
asymptotic reserve.

Apply `pma_sigma_one_odd_lift_boundary_owner` after those two owners. For a
scale-two source `U(x)=(x-a)V(x^2)`, the split point is unique. The complete
exact `k+1` and `k+3` odd-lift strata from catch #171 therefore have global
count

```text
binom(n/2,k/2+1) < 3Q_2(k+2).
```

Together the three owners use fewer than
`11(1+2^-690)Q_2(k+2)` of the existing `719` line and have no chart
multiplier. Higher odd-lift levels and non-lift below-floor full petals are
not claimed paid.

The proved `pma_quotient_closure_scope` resolves the remaining routing choice.
The precise folded-source class is already exact-periodic. Low-defect error
supports have a chart-free stripping predicate, but its raw word-free
`d=2,beta=1` route exceeds the official `719Q_2` line. The campaign therefore
keeps `QCLOSE=empty`; the dyadic near-coset owner is a separate finite
source predicate, not a claim that arbitrary low-defect closures are paid.
Every survivor of both global owners, including every low-defect survivor,
enters the B11 router and remains in its direct class unless `J/A2/AR` pays it.

The `sigma=1` B11 threshold problem is now closed by
`pma_sigma_one_b11_scope`. With

```text
(E,V_2,V_R)=(0,2,0),
```

the finite partition is `d<=2` in `J/A2` and `d>=3` in `GROW`, with
`AR=RES=empty`. However, the resulting certificate
`J(n,k)+11 binom((n-k)/2,2)q^4` already exceeds the whole `n^6` primitive
allocation at rates `1/8` and `1/16` for every generated field containing the
domain. Thus the threshold collapse is not a finite payment.

`pma_sigma_one_low_defect_payment` repairs that finite cell at the source.
After summing all missed-core sets and both exact background states, every
realized `d<=2` extra in one maximal sunflower is bounded by

```text
B_low(n,k)<n^5/1024,
```

independently of `q`. This payment includes periodic members as harmless
overcount, so it composes safely after `QOWN_per`.

`pma_sigma_one_d3_background_payment` pays the next complete source cell.
For defect three with the unique background retained, the pinned-basis charge
is

```text
binom(k-1,3) floor(binom(n-k,3)/4) < n^6/9216.
```

Together with the `d<=2` theorem, all finite source objects with `d<=2` or
`(d,r)=(3,1)` contribute less than `n^6/8192` per maximal sunflower. The
next theorem pays the part of `(d,r)=(3,0)` containing a full petal by

```text
binom(k-1,3)((n-k)/2)binom(n-k-2,2)<n^6/1536.
```

All currently paid finite source cells together are below `n^6/1024` per
maximal sunflower. The exact defect-three remainder has at most one agreement
in each of at least five petals. Exact small-row censuses show that this
diffuse class is populated, primitive, and consistent with `n^7` rather than
`n^6` scaling; this is route evidence, not an official-row lower theorem.

`pma_sigma_one_d3_diffuse_hyperplane_reduction` gives the exact algebraic
normal form of this residual. The first five singleton agreements define a
nonzero divided-difference hyperplane in the three elementary symmetric
coefficients of `L_D`; each fixed hyperplane contains fewer than
`(k-1)^2/2` split cubic core locators. The resulting direct aggregate is

```text
#DIFF_30 < 16(k-1)^2 binom((n-k)/2,5),
```

which has order `n^7`. The subsequent
`pma_sigma_one_d3_reciprocal_quadratic_obstruction` proves that this is not
only an upper-bound artifact. At rate one half it constructs actual primitive
diffuse source codewords with lower bound

```text
floor((M-15)(n/2-6)(n/2-3)binom(M,5)/(6M)),
```

which exceeds `n^6` from `n=2^22` onward. The complete diffuse class therefore
has no universal per-sunflower `n^6` payment. The dyadic growing-defect theorem
now assigns this entire family, and every analogous eligible near-coset core
with at most `s_n` misses, a global owner below `Q_2(k+2)`. The finite endpoint
is the far-from-coset complementary diffuse incidence after that owner.

`pma_sigma_one_paired_core_normalization` gives the exact global chart index.
For every `(k-1)`-core `C`, interpolate `Q_C=U|_C` and put

```text
phi_C(x)=(U(x)-Q_C(x))/L_C(x),       x outside C.
```

An official maximal source layout over `C` exists exactly when the fibers of
`phi_C` have signature `(2,...,2,1)`. Those fibers uniquely recover the
background, every petal, and every label. Hence, after first match,

```text
#Post(U)=sum_(C paired) #E_C,
```

where `E_C` is the Post class first assigned to that paired core. The known
finite source payments bound individual summands; they do not bound this
weighted paired-core sum.

The exact first moment in `pma_sigma_one_paired_core_abundance` shows that the
distinction is necessary. At the valid official rate-half row
`(q,n,k)=(65537,65536,32768)`, some received word has more than `n^6` paired
cores. This does not produce more than `n^6` Post codewords because a paired
core may have `E_C=empty`; it rules out any proof that first bounds all paired
cores by the primitive allowance. The open theorem must retain `#E_C`, prove
most paired cores empty after ownership, or replace the local terms by
chart-free source owners.

`pma_sigma_one_first_layout_domination` supplies the missing source-global
composition theorem. Order the paired cores and let `C_1` be first. Universal
core-defect carriage says that every listed codeword not planted in the
`C_1` layout is already carried there. Hence every Post codeword first
carried by a later core is one of the first layout's `M=(n-k)/2` planted
anchors, and

```text
#Post(U) <= #E_C1+M.
```

The exponentially large paired-core index therefore creates no finite
multiplier. Evaluating the three proved source payments only in the first
layout gives

```text
B_paid=B_low+B_31+B_30^full,
#Post(U) <= B_paid+M+#R_first(U),
B_paid+M<n^6/1023
```

on every official row. Here `R_first` is the unowned remainder in that one
layout.

`pma_sigma_one_post_top_allowance` now supplies the exact finite common
consumer ledger. After all global profile owners are removed, first carried
source layout gives a disjoint primitive partition `Top disjoint_union Post`.
At rate `1/2`,

```text
N_top  = 2((n-k)/2+1) sum_(j=0)^3 binom(k-1,j),
B_post = n^6-N_top,
```

and `#Top<=N_top`; at rates `1/4`, `1/8`, and `1/16`, the top band is empty
and `B_post=n^6`. Thus the finite open inequality has reduced to the single
first-layout target

```text
#R_first(U) <= B_post-B_paid-M.
```

This uses the exact `N_top` census, not subtraction of the coarse
`(121/128)n^6` certificate. First-match the exact-periodic, dyadic near-coset,
and first-two-level odd-lift owners, then pay in the first layout the remaining
below-floor full-petal class, the post-owner diffuse complement, and every
defect outside the growing near-coset window within the displayed allowance.
No weighted paired-core census remains. On the asymptotic-reserve track,
separately pay the general `GROW union RES` remainder after fixed finite B11
gates.

Apply the disjoint first-match order to source-admissible extras, represented
injectively by their auxiliary polynomials:

```text
QOWN_per; QOWN_cosgrow; QOWN_odd2; J; A2; AR; GROW; RES,
```

where `J,A2,AR,GROW,RES` are the cells of
`pma_b11_first_match_router`, `QOWN_per` is the proved exact-periodic owner,
`QOWN_cosgrow` is the finite all-dyadic near-coset owner, and `QOWN_odd2` is
the finite first-two-level scale-two odd-lift owner. The named payments are
charged once. The remaining `GROW union RES` source extras, explicitly
including all surviving low-defect quotient closures, have cardinality within
`B_post-B_paid-M` in the first layout on the finite track and within the
separately printed allowance on the asymptotic track.
The same allowance also pays every below-floor full-petal source not already
selected by a global owner. The full auxiliary list may be used as an upper
bound through
`pma_source_paving_bridge`; surjectivity onto the fixed source owner is not
assumed. On the finite track, universal first-layout domination replaces the
sum over paired cores by the one-layout residual above. Consequently the
complete post-top-band maximal-sunflower
contribution fits the `n^B` allowance consumed by `imgfib`.

## Falsifier

A transported post-owner family of source-admissible below-floor full-petal
objects, far-from-coset diffuse `(3,0)` extras, or finite defects outside the
growing owner whose composed count exceeds `B_post`; an asymptotic
`GROW union RES` family exceeding its separately printed residual allowance;
a first-layout residual exceeding `B_post-B_paid-M`; or an overlap/gap in the
`QOWN_per`/`QOWN_cosgrow`/`QOWN_odd2`/B11 first-match ledger.

Toy instances or arbitrary correlated targets outside the source dictionary
are route diagnostics, not counterexamples to this statement. The top-band
full-petal theorem is not a payment for below-floor or mixed-petal cells.
