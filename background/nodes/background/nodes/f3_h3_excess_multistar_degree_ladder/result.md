# Result

The cutoff excess now controls the strength of its own ideal certificate:

```text
e=P-18       minimum small vectors       guaranteed center weight
1..2         8                           4
3..6         9 or 10                     6
7..10        11 or 12                    8
```

The pattern continues by `(EML3)` and `(EML4)`. The only fibers not forced
into the weight-six sieve are the diagonal boundary profiles `(19,1)` and
`(20,2)`. This separates the low-excess edge of C36' from its increasingly
overdetermined high-excess tail.
