# ATTACK - m720_h3_large_gate_replay

Closed by a memory-controlled exact local replay. The verifier avoids the
large bucket table by sorting compact signature records.

Historical routes were:

- cite a completed off-laptop or Modal log containing both exact rows;
- import a compact certificate for the anchored-core counts;
- devise a lighter exact h=3 counting proof that avoids large local tables.
