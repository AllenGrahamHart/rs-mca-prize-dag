# Result

The collision directions through every member of the rank-five base core
are not an arbitrary collection in projective four-space. They are the
vertices of a simple four-dimensional star configuration, with exact
degree-`d` evaluation distance `C(m-d,4)`.

In particular, the reused directions in the three `r=2` cores cannot lie on
any nonzero hypersurface of degree at most `4,4,2`. This gives a
constants-explicit direction-rigidity input for the finite split-pencil chart
audit. It does not by itself convert local rich lines into the required
global point count.

The complete Modal verifier fleet passed `160/160` in
`ap-RTaElVAnr87RAE0ODo2EDm`. All five integration gates passed in
`ap-x1Udk5Zy8GTcDqFUAZREmS`.
