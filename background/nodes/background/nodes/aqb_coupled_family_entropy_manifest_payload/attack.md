# ATTACK - aqb_coupled_family_entropy_manifest_payload

This is now the live AQB leaf.

Needed output:

- define the canonical finite member set for the `c=2` quotient-box geometry
  at `sigma*`;
- identify the shared quotient/fiber coordinates reused by all members;
- record the reusable box-charge datum rather than charging one full box per
  member;
- give the transfer map from each member to the list witness used by
  `aqb_average_member_transfer`;
- provide exact or interval-certified entropy ledger entries for that same
  family;
- prove the certified net lower bound is at least `429,645,547` bits.

Use only light local checks. A broad WSL search for the family is not
appropriate on this machine.
