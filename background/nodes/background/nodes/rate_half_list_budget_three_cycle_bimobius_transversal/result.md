# Result

The 4-cycle chamber is a two-complete-fiber/two-Mobius-graph rational map. At
official scale its block pencil has exactly two fully split members, so the
cycle route should attack that primitive graph coupling rather than search for
a hidden third constant fiber.
