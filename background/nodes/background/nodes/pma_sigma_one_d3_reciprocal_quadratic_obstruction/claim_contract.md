# Claim contract - reciprocal-quadratic PMA obstruction

## Claim

At rate one half, the explicit multiplicative-core sunflower construction
contains at least `(RQ-LB)` actual primitive diffuse `(d,r)=(3,0)` extras, and
this exceeds `n^6` at the official row.

## Dependencies

- the official `sigma=1` maximal-sunflower conventions;
- the diffuse defect-three object isolated by the prior PMA reductions.

The construction and count are otherwise self-contained.

## Falsifier

An algebraic failure of `(1)` in `proof.md`, repeated or zero labels, a
counted word with wrong degree/core/background profile, a full petal or
nontrivial stabilizer surviving the matching filters, or failure of the exact
official-row inequality.

## Consumer effect

`pma_wide_residual` may not seek a complete `n^6` per-sunflower payment after
the fixed-hyperplane reduction. The proved index-two near-core successor routes
this named family to a global owner; the red leaf must restrict its next
incidence theorem to the complement.
