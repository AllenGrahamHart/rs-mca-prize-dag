# proof: subcore_richline_classification (Pro Brief A; replayed)

## Resolution of the definitional caveat
The repo's operative "affine-net" class IS the broad F3/U3 class (mixed b=2
degree-1 pullback cells): f3_net_absorption.md, PROVED for the P1/P3
fixed-subcore affine-plane model under the unified pullback strip, verifier
replayed green (14 cases, 342 rich points charged, 726 pair trades, the
original P3 fixture 65/65). Pro's conditional therefore discharges.

## The two-case exhaustiveness proof (Pro; refereed)
Fixed (k-1)-subcore R: each off-core point x contributes an affine branch
L_x(z) = alpha_x + beta_x z in the (z,a) plane (the P1 reduction); a rich
parameter P = (z0,a0) has >= t+1 incident branches.

CASE 1 (all incident branches one direction): distinct parallel affine lines
cannot meet; branches through a common point with equal slope are duplicates
of one line — tangent-pencil/duplicate bookkeeping, stripped.

CASE 2 (>= 2 directions): pick a pivot L_0 and any different-direction L_i;
both pass through P, so the unordered pair is the quadratic block
Y^2 - e_1(Z)Y + e_2(Z) with e_1(z0) = 2a0, e_2(z0) = a0^2 — a mixed b=2
degree-1 pullback cell; a spanning tree from the pivot covers the block by
|I(P)| - 1 such cells — exactly the F3 absorption lemma, stripped.

The two cases are exhaustive. Stabilizer consistency: nontrivial toral
stabilizers are cyclic/dihedral (Laurent-Ritt, granted); the trivial-
stabilizer multi-direction case is Case 2. Hence NO FIFTH CLASS, and the
post-strip rich-parameter residue is EMPTY: C_rich = 0 — stronger than the
uniform-cap form requested.
