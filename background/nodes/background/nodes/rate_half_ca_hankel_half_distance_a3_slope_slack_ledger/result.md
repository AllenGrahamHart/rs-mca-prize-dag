# Result

The half-distance `A=3` residual now has the same all-degree rational-normal,
slope-slack, norm, and component-degree router as the strict branch, with the
correct transposed Hankel dimensions and target `T<=2^39+1`.

At its first live degree `e=2^37+1`, only four slack strata remain and every
one forces at least `3*2^37+2` saturated domain rows and `3*2^37+6` completely
split generic columns. The separate `A=1` profile remains open. No Modal
computation was used.
