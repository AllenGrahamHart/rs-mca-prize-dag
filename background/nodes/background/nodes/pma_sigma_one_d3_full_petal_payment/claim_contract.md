# Claim contract - PMA sigma-one defect-three full-petal payment

## Inputs

1. The exact source dictionary of `pma_source_paving_bridge`.
2. The official `sigma=1` maximal-sunflower identities `L=n-k`, `M=L/2`.
3. The prior finite payments for `d<=2` and `(d,r)=(3,1)`.

## Output

The full-petal portion of `(d,r)=(3,0)` is below `n^6/1536` per sunflower,
and all currently paid finite source cells together are below `n^6/1024`.

## Nonclaims

- no payment of the diffuse five-singleton class;
- no global chart census;
- no asymptotic-reserve theorem;
- no lower bound inferred from the experimental census.

