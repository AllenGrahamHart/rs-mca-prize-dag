# Result

The fixed-order H3 candidate union is sharpened from divisors of one
low-distance collision norm to divisors of one normalized two-generator ideal
norm. Every rich fiber supplies two low-distance collisions with a common
center among coefficient vectors of squared norm at most three, and the row
prime divides their ideal norm and their normalized principal-norm gcd.

Distance-two collisions have 2-power norm and cannot occur at the odd row
prime. The selected seven-vector graph therefore has at least six low-distance
edges, strengthening the earlier four-edge packing count.

The H3 critical leaf remains `TARGET`. The next computational or algebraic
route must classify rooted ideal stars; extending the single-norm census is no
longer selected.

The pinned exact boundary certificate provides a non-load-bearing positive
check. In both ten-pair rich fibers at `(n,p)=(8192,67657729)`, the chosen
reference pair is at squared distance six from every other pair. Its first two
collision norms have gcd `4p`, so ideal batching identifies the normalized
two-generator ideal with the selected prime ideal and gives norm exactly `p`.
This does not assert uniform saturation.
