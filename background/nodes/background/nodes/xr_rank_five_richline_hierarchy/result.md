# Result

The P-A base residual now carries a complete multiplicity-sensitive peeling
hierarchy.  It is not enough for a counterexample to reuse almost all bases:
after passing to a suitable nonempty core, every point lies on at least

```text
123, 120, 27
```

lines containing three or more selected points.  Other thresholds are given
by the exact formula in the statement; positive forced cores persist through
line populations `74`, `40`, and `9` on the three rows.

This selects a rich-line or line-arrangement theorem as the next base-branch
input.  Pair-only constructions no longer model a counterexample, but the
hierarchy alone does not bound the surviving arrangements.

Focused Modal replay: `ap-J4bSjL4ndNIY8sETZZ4Yha` (`58 MB`). The replay
includes a non-vacuous finite core and two DAG mutation controls. The complete
fleet passed `153/153` in `ap-5RKj0HRUK4A0W7GARxWAZj`; all five integration
gates passed in `ap-XmGc6nmUjNkxMsYn3BpCeS`.
