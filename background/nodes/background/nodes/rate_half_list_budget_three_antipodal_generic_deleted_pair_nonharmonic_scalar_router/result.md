# Result

The outer ratio has been removed from the remaining deleted-pair search. For
each lift ratio `r`, there are three named scalar identities; a passing
identity determines `q_out+q_out^(-1)`, and a 38-step trace proves or rejects
the exact torsion condition. The final polynomial-square verdict is invariant
under the only remaining ambiguity `q_out<->q_out^(-1)`.

CR-002 can therefore request three one-variable certifiers rather than a
two-variable torsion search. No harmonic shard, point-matching enumeration,
free outer ratio, free `V_0`, or common-scale variable remains.

Uniform rejection of those three certifiers is still open. Any expensive
implementation belongs in the deferred compute ledger until it has a
compressed representation and a measured resource ceiling.
