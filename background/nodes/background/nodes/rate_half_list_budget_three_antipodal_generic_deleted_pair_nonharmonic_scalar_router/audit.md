# Audit

The symbol `q_out` is the outer ratio `mu/lambda`, not the field size. The
field is the already-proved split quadratic branch, while all data used by
this router lie in its prime subfield `F_p`.

The scalar identity `(NSR4)` alone is not sufficient. The trace gate is what
recovers the exact `N`-torsion condition, and the polynomial-square test is
what recovers the missing pencil direction. Prefix agreement or a fourth-
power test on `T` would be weaker.

The trace endpoint `y_38=2` is exact, not a probable-order test. Its converse
uses `N|p-1`, so all roots of `X^N-1` lie in `F_p`.

No arbitrary square-root choice is hidden in `(NSR6)`. The reciprocal roots
change the candidate by multiplication by `q_out`, and `q_out` is a fourth
power because `4N|p-1`.

The node is an exact router, not a nonexistence result. A contributor
classifier must still represent the ODE-generated `U_0`, the Euclidean
quotient and remainder, and the square test without materializing arrays of
length `N`.
