# Result

The Möbius stage of the remaining deleted-pair branch is now finite over its
single lift ratio. For each `r`, the outer ratio `mu/lambda` has at most three
unordered possibilities, one for each pullback of the antipodal target
pairing, and that ratio must itself be an `N`th root of unity. No permutation
census or unrestricted outer-ratio variable is needed.

Together with the constant ODE, a symbolic classifier may now generate the
unique possible monic `U_0` from the deleted quadratic, choose one of three
outer-ratio branches, and only then solve for `V_0` and the remaining common
outer scale.

The node does not exclude those three systems. Their compatibility with the
half-degree factorization of `x^(8M)-1` remains open.
