# Audit

## Scope checks

- The thirteen rows are necessary degree chambers, not existence claims.
- The leading-coefficient test uses the exact degree of the nonzero pair
  difference; it does not assume generic leading coefficients.
- A split-fiber row is printed only when all three `delta` values vanish, so
  all three coefficients in `(SF2)` are constants.
- Pairwise coprimality follows from disjoint incidence blocks, not from an
  unproved genericity assertion.
- The cap `(SF3)` counts fully split members of one fixed constant pencil. It
  is not a census of all split pencils.
- The `d-1` cap is four only for `d>=6`; the small scales remain explicitly
  outside that sharpening.

## Route consequence

Five incidence types now expose one or more three-member split pencils. At
large official scales each pencil has room for at most one further fully
split member. The unproved content is the coupling or residual-member
classification. The 4-cycle is isolated because each of its four triangles
contains one of the two slack edges.

No unsafe witness, safe-side list bound, or adjacent crossing follows from
this audit.
