# Claim contract

## Inputs

- the official C36' row and its shifted subgroup;
- the proved identity-deficit energy compiler;
- a proposed estimate for the multiplicative energy of the full shift `H-1`.

## Conclusion

The full-shift estimate `(ZN2)` is a sufficient C36' premise, and a centered
source theorem may spend the explicit allowance `(ZN4)`.

## Nonclaims

No full-shift energy estimate is proved here. The node corrects conventions
and constants only; it does not turn an implicit `O(n^2 log n)` theorem into
the required finite estimate.
