# Result

The first exact failure point of each nonharmonic scalar branch is now one
field equation. It can be checked from `t`, the source trace `chi`, and one
terminal reversed-quotient coefficient `sigma`; no outer ratio, full
remainder identity, gcd, or fourth root is needed.

CR-002 should attack `(CCG3)` before every later deleted-pair gate. A useful
contributor result is a compressed recurrence or diagonal formula for
`sigma`, followed by a uniform nonvanishing proof for the three displayed
equations. A raw length-`2^36` recurrence is not the requested endpoint.

The equations are not yet uniformly excluded, so the deleted-pair sublane
remains open.
