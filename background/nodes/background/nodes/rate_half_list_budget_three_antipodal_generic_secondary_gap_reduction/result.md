# Result

On the maximal generic floor, both pencil directions are now canonical up to
the expected scale: `U` comes from a fourth-root truncation, and `V` comes from
a nested square-root truncation. Existence requires two separate two-term
coefficient gaps at the printed indices.

The generic boundary and the rate-half adjacent crossing remain open.
