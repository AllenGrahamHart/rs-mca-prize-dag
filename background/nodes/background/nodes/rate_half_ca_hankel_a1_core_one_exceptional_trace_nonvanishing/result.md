# Result

The zero-exceptional active system is impossible: its degree-`r-1` fiber
would require an `A` quotient one degree larger than the global complement
allows.

Only the `D_*=0` system and the `D_*=1` system with active exceptional trace
remain. No Modal computation was used.
