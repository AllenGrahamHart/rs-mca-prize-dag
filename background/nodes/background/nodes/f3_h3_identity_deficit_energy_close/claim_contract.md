# Claim contract

## Inputs

- an official order `n=2^s`, `13<=s<=41`;
- an official subgroup `H<=F_p^*` of order `n`;
- `A=(1-H)\{0}` and the product and quotient multiplicities from the
  critical C36' contract;
- the proved one-shift Stepanov estimate `P(1)<4n^(2/3)`.

## Conclusion

The single additional estimate

```text
4 E_x(A)<=145(n-1)^2
```

implies the original C36' three-to-one bound on every official order.

## Nonclaims

This node does not prove the displayed energy estimate, the cutoff-18
weighted Mobius claim, or C36' unconditionally. It does not promote any
critical node. Its purpose is to expose the weakest currently proved clean
energy endpoint and the source-specific deficit that pays the difference.
