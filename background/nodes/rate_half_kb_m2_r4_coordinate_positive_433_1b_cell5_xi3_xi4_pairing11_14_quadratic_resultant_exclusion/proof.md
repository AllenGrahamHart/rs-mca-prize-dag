# Proof

Delete the `DF` record and put `m=df`, `S=(d+f)^2`, `z=1/d`,
`y=z^2`, and `q=de`. The missing Vieta equation is

```text
M(y)=1+(2m-S)y+m^2y^2=0.
```

For pairing 11, the first two residual pair equations are quadratic in
`q`. Their exact Sylvester resultant is degree eight in `z`. Reduce it
modulo `M(z^2)`, split the cubic remainder into even and odd parts, and
multiply the two sign conjugates. The resulting polynomial in `y` has a
linear remainder modulo `M`; its division-free common-root cut is normed
through the exact cell-5 basis `1,t,b,bt`.

A syntax-tree adapter reuses the audited cell-4 compiler function unchanged
and supplies the cell-5 row-6 tower and compact kernel. All eight
source-sign/`sigma_c` rows terminate. The packet contains 52 target
roots, 108 complete candidates, 120 guarded source routes, 24
compatible `(z,q)` lifts, and 48 final colored lanes. Its boundary
partition has 40 `r` guards, 32 `t` guards, and no leading-boundary case.

A separate SymPy/Galois-tools reconstruction covers 45 exact polynomial
profiles, 172 deployed roots, and degrees through 3432. The direct audit
rebuilds every candidate union, source-kernel value, missing reciprocal
relation, `d/e/f` lift, and all three paired equations. Every one of the
48 final values is nonzero; there is no witness or unresolved branch.

The universal label quotient transports `(3,11)` exactly across
`{(3,11),(3,14),(4,11),(4,14)}`. Therefore all four labels are empty. QED.
