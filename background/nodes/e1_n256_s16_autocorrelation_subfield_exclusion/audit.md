# Audit

Date: 2026-07-27.

The proof separates three fields and norms explicitly: `alpha` lies in the
degree-128 field, `beta=alpha conjugate(alpha)` lies in the degree-32
subfield only under the autocorrelation-support hypothesis, and the two
rational norms satisfy `R^2=N^4` by the tower law.

The local verifier checks the field degrees, the four recurrence values that
force `L<=22`, the strict integer inequality `60^32<2^250`, the DAG wiring,
and the exact weighted-Schur value 2718 of the subgroup configuration removed
by the theorem.

No norm census, factorization, floating approximation, or Modal result is
load-bearing.
