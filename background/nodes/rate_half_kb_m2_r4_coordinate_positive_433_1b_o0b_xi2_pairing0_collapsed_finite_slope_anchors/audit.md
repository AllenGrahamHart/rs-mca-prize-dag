# Audit

The hostile audit checks three necessary hypotheses:

1. Flipping the `q5` sign makes its record equal the anchor.
2. Dropping `A2(lambda) != 0` admits an identically proportional pair with
   a different record while the pinned equation still vanishes.
3. Dropping the `b^2-d^2 != 0` separation admits `b=d` in the `q4`
   record.

These mutations are rejected and prevent interpreting the determinant as an
unconditionally exact replacement outside the stated locus.
