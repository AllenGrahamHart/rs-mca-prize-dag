# Claim contract

- The result applies only after the proved `z2=z5=0` collapse.
- It covers finite pairs `q4,q5` in `FFI` and `q4,q6` in `FIF`.
- It uses the pinned `q3` equation, the printed `a2m != 0` guard, odd
  characteristic, and printed target nonzero/square-distinct guards.
- It proves exact root elimination for these finite pairs, not chart
  emptiness.
- It does not apply to `FFF` or `IFF`, where `z2=z5=0` has not been
  established.
