# Result

The exact `FFI` and `FIF` systems may eliminate all finite-root variables.
For each finite pair, retain its coefficient determinant and invert the first
linear coefficient. This is an equivalence, not merely a necessary
relaxation.
