# Frontier

Use the exact determinant presentation with the two first-slope guards on
`FFI` first. A unit certificate closes that chart. A timeout or nonunit
result has no proof status and should trigger factor/branch analysis, not a
broader repeat.
