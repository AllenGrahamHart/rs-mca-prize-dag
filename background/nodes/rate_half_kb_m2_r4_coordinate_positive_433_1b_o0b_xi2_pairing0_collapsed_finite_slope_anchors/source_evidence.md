# Source evidence

- The cached outside compiler prints `q3`, records, matching coefficients,
  target guards, and `a2m`.
- The collapse node proves `z2=z5=0` in exactly the two claimed masks.
- The standalone verifier checks the generic anchor identity, all three
  signed record separations, and the mask ledger in characteristic
  `2130706433`.
