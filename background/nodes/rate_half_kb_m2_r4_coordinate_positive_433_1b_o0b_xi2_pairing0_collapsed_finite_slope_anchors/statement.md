# Statement

In the canonical `xi=2,pairing=0` S0 case, apply the proved
`z2=z5=0` collapse to chart `FFI` or `FIF`. For every finite matching
pair `qj` in that chart,

```text
mjp1 != 0.
```

Thus the two linear matching polynomials have a finite common root exactly
when their `2 x 2` coefficient determinant vanishes.
