# Result

`PROVED`. The false one-slope near-rational payment has a uniform
same-witness replacement:

```text
N_near-rational,support-wise-MCA-bad <= 2w
```

under `w>=1` and `3w<=n-K`. This is a local route cut, not a complete MCA
ledger.
