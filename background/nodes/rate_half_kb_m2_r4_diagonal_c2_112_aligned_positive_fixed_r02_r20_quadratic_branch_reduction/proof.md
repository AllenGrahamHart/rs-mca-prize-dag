# Proof

The exact quadratic compatibility identity and reconstruction are supplied
by the pinned fixed-orbit compression theorem. Direct factorization of
`R,E2,E3` for the four representatives removes only factors of `V` or
transported named units. The retained `R` product has factor degrees
`3,3,12`. Exact deployed-prime bases and localizer reductions give the
recorded cubic classifications; all degree-12 requests terminate only at the
declared cap and are not promoted.

For `(KBGR-1)`, reconstruct the source pair and `z` from the pinned atlas.
The coefficient-0 projective mismatch is computed from each factor's
constant and leading coefficients, so no omitted middle term can affect the
identity. Its rational denominator consists entirely of named factors. After
removing those factors, exact `c,d -> s,p` descent and factorization gives the
five displayed factors.

Each of the eight `F04` intersections is then rebuilt literally. Substitution
`w=-U/V` is denominator-cleared, factors inverted on the generic chart are
removed, and the selected full-`J` factor is adjoined to the surviving cubic
q-slice ideal. Six intersections have localizer nilpotence index one. The two
`J11` intersections retain dimension-one support. This proves the reduction
and no stronger closure. QED.
