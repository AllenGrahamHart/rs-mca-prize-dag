# Attack surface

The generic frontier has two qualitatively different pieces:

1. The surviving cubic plus `J11` curve is now closed downstream for both
   `F04` targets by the logarithmic router, its denominator exclusions, and
   homogeneous degree-67 numerator reduction. Replay that exact route
   literally for `F05`, then its fingerprint companions; do not infer
   complete-system transport from fingerprint equality.
2. The degree-12 resultant factor. Seek a low-degree eliminant, syzygy, or
   factor-specific full-identity reduction, respecting the separate
   nonnamed leading-drop branches in the degree-12 decomposition node.

Rank drop is already proved empty in a separate node. For `R02`, every
degree-12 branch is also closed; a literal replay of the cubic route is the
only remaining work in `F05/F06/F07-R02`. For `R20`, the degree-12 branch
remains independently open.
