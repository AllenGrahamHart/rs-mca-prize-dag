# Claim contract - L1 joint Plotkin-boundary payment

## Inputs

- the joint core/background cross-determinant intersection bound;
- canonical equal-size background agreement subsets;
- fixed petal polarity and the L1 lower cutoff;
- the official maximal-sunflower source equation for `(PK8)--(PK9)`.

## Output

Every exact support cell with `N+b<=4ell` has at most `2(N+b)` contributors.
The bounded-polarity residual satisfies the strict official gates
`(PK7)--(PK9)`.

## Consumer rule

Consumers may discard the full equality regime and impose `(PK7)`. At the
first official scale they must also impose `ell>r(g+1)`. They may not apply
the support-pattern aggregate to growing petal polarity.

## Falsifier

More than `2(N+b)` compatible contributors in one exact support cell with
`N+b<=4ell`, or an official source violating the equivalence of
`(PK7)--(PK9)`.
