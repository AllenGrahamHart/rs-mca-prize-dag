# Audit - L1 joint Plotkin-boundary payment

## Checked axes

1. The combined sets use disjoint core and background universes.
2. Every combined set has the same weight `r+ell`.
3. The shared intersection budget gives Hamming distance at least `2ell`.
4. The sign-vector inner product is `V-2` times Hamming distance.
5. Equality `V=4ell` is retained in the paid region.
6. The Rankin lemma proof handles zero inner products and positive-dependence
   blocks, not only a strictly obtuse family.
7. The aggregate counts exact support patterns and defect degrees but not
   background subsets.
8. The official source translation preserves all additive constants.
9. Growing petal polarity and cross-chart ownership remain open.

## Remaining attack

The bounded-polarity per-chart tail now has `N+b>4ell` and obeys the joint
sub-Johnson inequality. At the first official scale it also has the strict
large-background condition `ell>r(g+1)`. Future computation must enforce
all three conditions.
