# Audit

1. The safe theorem is applied at the same agreement as the explicit
   predecessor witness; there is no radius shift between objects.
2. The sub-square-root hypothesis is strict and immediate:
   `L_1^2<=B*^2<=4<q`.
3. The projection theorem controls every `m>=1` uniformly; no bounded-arity
   sweep or unproved regularity assumption is consumed.
4. Diagonal tuples preserve the common support exactly, not merely its size.
5. The denominator remains `q`, so the integer threshold is the same `B*` for
   ordinary and interleaved lists.
6. The conclusion is branchwise. It does not promote the global grand node or
   the remaining ordinary rate-half leaf.

The focused verifier checks the exact DAG dependencies and endpoint
arithmetic. The independent audit replays the integer inequalities for both
budgets and hostile endpoint mutations.
