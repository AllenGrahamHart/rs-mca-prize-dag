# Frontier

Compile the complete positive Vieta and product equations only for the
thirteen routed records `(KBPRW-4)`.  Start with `433-1a -> O0b`, whose
common defect already saturates the budget and whose outside graph is
unique.  Treat the two zero-common rows separately because only they can
carry an outside loop.
