# Audit - L1 background-quotient Johnson bound

## Checked axes

1. The fixed interpolant is subtracted before division by the petal locator.
2. The derived target on the background is independent of the contributor.
3. Every contributor has at least `u=ell-s` background agreements by the
   exact list threshold.
4. Agreement sets are canonically truncated to equal size before applying
   the constant-weight Johnson bound.
5. Strict denominator positivity is retained.
6. The positive fixture attains `(BQ3)` exactly.
7. The zero-denominator fixture is an exact saturated L1 chart with two
   contributors.
8. The aggregate sums support patterns and defect degrees but not background
   subsets.
9. Non-intrinsic chart multiplicity remains open.

## Remaining attack

The bounded-polarity per-chart residue satisfies both `(BQ5)` and `(BQ6)`.
It needs a higher moment, split-pencil restriction, or natural-scale owner;
another ordinary second-moment argument cannot cross the zero denominator.
