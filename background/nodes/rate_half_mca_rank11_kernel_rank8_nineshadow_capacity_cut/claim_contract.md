# Claim contract

The claim is the exact interval `10<=K'<=17608`, with first method wall
`K'=17609`. It depends on:

- the existing individual ambient/record caps;
- the rank-preserving nine-shadow resource;
- the proved rank-eight full-containment coefficient.

The finite scan is exact rational arithmetic. It is a local route cut and
does not pay the rank-eight owner lane, the remaining kernel interval,
error rank eleven, or either prize.
