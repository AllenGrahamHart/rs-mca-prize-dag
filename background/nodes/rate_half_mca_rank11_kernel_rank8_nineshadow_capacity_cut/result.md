# Result

The rank-eight extension deficit moves the exact dominant-kernel cutoff
from `K'=15670` to

```text
K'=17608.
```

The first open row is `K'=17609`. The boundary optimizer uses coranks one
through four: one and three are capped, two and four are resource-tight,
and five through nine vanish.
