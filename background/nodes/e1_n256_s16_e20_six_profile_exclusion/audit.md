# Audit

Modal app `ap-iegnpEhh0V6js6IkkcDEWr` completed all 154 templates with two
exact 256 MB engines and 60-second task timeouts, using 952.816 aggregate dual
worker-seconds. App `ap-18QcozEhrOO0MkhBtr6sTU` independently repeated the
complete dual census and collected the full-conductor residue in 964.815
aggregate dual worker-seconds. Checkpoint files were written throughout.

The count and collection checkers validate source and atlas hashes, all 154
rows, exact engine equality, profile/conductor totals, two content
fingerprints, and every retained vector. Modal app
`ap-WGuf8zhWii313tpUoMUdWG` computed the exact norm ledger in two batches per
engine. The norm checker validates source/collection hashes, vector ordering,
exact FLINT/PARI agreement, positivity, distinct count, profile maxima, global
maximum, threshold count, maximizing indices, and a hostile mutation.
