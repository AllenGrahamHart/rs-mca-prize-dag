# Result

`PROVED`: retaining the common-zero count replaces the separated support
factor by the exact one-dimensional envelope `(CZ1)`.

The uniform support walls roughly double at the first residual ranks.  Most
notably, Mersenne transformed rank `5` is paid through `e=124471>d`, so a
surviving rank-five family is forced well into the former middle-support
cell rather than merely beyond the sparse punctured-list range.
