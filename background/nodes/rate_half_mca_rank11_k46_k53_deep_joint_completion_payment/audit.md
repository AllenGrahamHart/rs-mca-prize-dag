# Audit

1. Every exact defect pair in `0..q` is included.
2. Every other support retains all terminal/fallback alternatives.
3. Every cap is intersected with inherited caps.
4. The joint cap is used exactly on `s_4+s_5<q`.
5. All `8640` other branches are generated before deduplication.
6. Pareto compression removes only componentwise dominated vectors.
7. Both implementations find `1182` unique and `9` maximal vectors per row.
8. Every deficit weight, kernel corank, and rank-nine mark is retained.
9. All eight safe signs and the adjacent wall sign are replayed exactly.
10. Demand subtracts `C(n,11)` exactly once.
