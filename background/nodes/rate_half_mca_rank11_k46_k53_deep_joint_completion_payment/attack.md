# Attack record

The coarse `K'=46` all-fallback leaf allowed only terminal support-four and
support-five pairs to use their joint carrier.  Exact deeper defects reveal
that the worst pair still satisfies `s_4+s_5<q`.  Reusing the proved joint
charge collapses the wall and, without another premise, closes eight rows.
At `K'=54` the same balanced deep pair is the first arithmetic wall.
