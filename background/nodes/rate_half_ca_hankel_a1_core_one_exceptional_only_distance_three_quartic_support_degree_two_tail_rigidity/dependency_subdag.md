# Dependency sub-DAG

```text
degree_two_three_subgroup_reduction [PROVED] --+
                                                  +--req--> degree_two_tail_rigidity [PROVED]
antiweight_absorption [PROVED] ------------------+                         |
                                                                            +--ev--> rate_half_band_closure [UNPROVED]
```

The earlier `40` is retained as a valid normalization loss, but no longer
as the true number of degree-two support tails.
