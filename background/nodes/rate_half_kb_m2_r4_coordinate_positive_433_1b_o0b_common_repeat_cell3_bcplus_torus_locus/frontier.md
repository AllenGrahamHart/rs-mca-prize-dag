# Frontier

Compile the 105 outside source-label assignments on `H_epsilon=0`. Quotient
first by the exact cell-3/cell-6 transport and by outside-record exchanges;
then use one-variable resultants on the explicit torus curve to exclude or
classify survivors. The `BC-` compact curves and owner blocks `[1,2]` and
`[11,14]` remain separate child tasks.
