# Audit

- All logarithmic comparisons use rational atanh-series intervals; floating
  point is route-planning evidence only.
- `verify.py` reconstructs the chord inequality, all 2,339 universal layer
  profiles, their exact `Phi` maxima, and every Hermite interpolant.
- The primary direct-convolution frontier is Modal app
  `ap-2SrwvNtxlhfJ2USNVd5yvn`; the independent folded audit is
  `ap-uGeZVekDHwri0h2YdtA6yH`.
- Both cover all `320292000` normalized vectors, return all 32 shards, and
  agree exactly in every chamber through `V=194`. Aggregate worker times were
  94.92468 and 58.20173 seconds.
- The exact field-floor comparison is made after raising to the fifth power.
- Normalized counts are used only for existence and are not interpreted as
  weighted class-pair counts.
