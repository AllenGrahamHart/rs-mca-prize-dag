# Claim contract

- **Claim:** the complete rank-eleven component target is empty at
  `K'=23`.
- **Resources:** every kernel corank, every core offset, both completion
  branches, and one global rank-nine mark budget.
- **Endpoint:** both the floor-row gap and record coefficient are positive.
- **Boundary:** `K'=24` is an explicit failure of this payment, not a
  counterexample to its lemmas.
- **Nonclaim:** no rank-eight chronology, active-v4 movement, or prize
  closure.
