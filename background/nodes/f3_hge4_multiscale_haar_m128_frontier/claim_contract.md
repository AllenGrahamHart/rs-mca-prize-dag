# Claim contract

| field | value |
|---|---|
| claim | Balance contributes one additional factor of two to every nonzero dyadic norm in the multiscale Haar product; the resulting exact gate closes all `m=128` widths `h>=12`. |
| status | PROVED |
| dependencies | `f3_hge4_multiscale_haar_m64_level_close`, `f3_hge4_cyclotomic_norm_quarter_width_exclusion`, `f3_hge4_nonfull_complement_third_gate` |
| exact output | `E_h^prim(128,p)=0` for `12<=h<=64`; precise Haar-pattern pruning at `h=9,10,11`. |
| open residue | `m=128`, `4<=h<=11` after `(MX4)`; every level `m>=256`; the aggregate. |
| consumer | `f3_hge4_norm_gate_count` by evidence edge. |

The node does not promote the critical consumer and does not claim that a
surviving Haar pattern is realized.
