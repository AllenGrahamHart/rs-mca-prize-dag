# Result

`PROVED`. Balance strengthens the multiscale Haar norm product by the exact
factor `2^(1+|S|)`. At the first previously unpaid exact ratio level this
closes every width `12<=h<=64`; the remaining `m=128` problem is confined to
`4<=h<=11`, with only one Haar pattern left at `h=11` and two at `h=10`.
