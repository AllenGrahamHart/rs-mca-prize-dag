# Audit

1. Every discarded vector is componentwise dominated before nonnegative
   weighting.
2. The conservative frontier and geometry lanes form a disjoint exhaustive
   partition of the implemented atlas.
3. Above-ceiling filtering uses exact integers and strict `>`.
4. The 36 rerouted tuples are unique and all have `M_3>M_2>0`.
5. Pairwise carrier alternatives are combined by Cartesian product.
6. Joint support-4/5 caps are intersected with, not substituted for, all
   inherited componentwise caps.
7. The lane maximum values are each below the global conservative maximum.
8. Floor placement in the final capacity is unchanged from the parent row.
9. Positive premium margin and positive demand-capacity gap are both checked.
10. Remote replay uses bounded lane workers; no local computation is needed.
