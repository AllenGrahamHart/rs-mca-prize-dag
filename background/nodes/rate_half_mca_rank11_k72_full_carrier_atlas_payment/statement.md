# Full carrier atlas closes `K'=72`

- **status:** PROVED
- **closed row:** `K'=72`
- **new closed prefix:** `K'=10..72`

At `K'=72`, `q=62` and `m=67544`.  Exact support-two through support-five
defects, all support-six through support-nine terminal/fallback caps, the
full-completion pairwise carrier atlas, and the fixed-union flat-coupled
support-4/5 charge give the global completion premium

```text
P_72=41089877204729279662874647920595743958596178333.  (P72)
```

The exact safe premium ceiling is

```text
41100317939998934447573065780978817075733675000,
```

so the premium margin is

```text
10440735269654784698417860383073117137496667.          (M72)
```

The resulting required-component-incidence minus complete-capacity gap is

```text
52200017935756118667066163970702686810349690944821612538425>0. (G72)
```

Thus the rank-nine component row `K'=72` is closed.  The next unproved row
in this prefix program is `K'=73`.

## Falsifier

An omitted defect or carrier-position leaf; a geometry lane above `(P72)`;
a conservative cell above the ceiling absent from the printed 36-cell
reroute list; a rerouted cell above the ceiling; or an exact replay with
nonpositive `(M72)` or `(G72)`.
