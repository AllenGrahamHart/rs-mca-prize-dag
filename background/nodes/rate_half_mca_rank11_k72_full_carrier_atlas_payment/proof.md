# Proof

Start with the complete exact-defect cap system used through `K'=71`.
Componentwise dominated cap vectors may be discarded because every deficit
weight is nonnegative and every later operation takes componentwise minima.
The support-two/support-three projective-position theorem removes 992
impossible pairs and leaves 675 maximal ordinary vectors, together with the
full-completion lanes recorded in the contract.

The streamed conservative replay covers 7,991,221 leaves.  Exactly 36
distinct support-two through support-five defect tuples have conservative
premium above the exact safe ceiling.  The largest conservative leaf already
below the ceiling is

```text
s2=35/s3=32/s4=33/s5=31/c6F/c7F/c8F/c9F,
```

with premium `(P72)` and margin `(M72)`.

For each of the 36 exceptional tuples, apply the exhaustive `T/A/F`
position split from the full-completion pairwise carrier atlas independently
at supports three, four, and five.  Every resulting fixed union of dimension
at least five receives the flat-coupled support-4/5 charge, intersected with
all exact same-support and cross-support caps.  The 8,057 resulting exact
evaluations are all safe.  Their maximum is

```text
31430973522237705446704558558267588754434271701,
```

below the ceiling by
`9669344417761229000868507222711228321299403299`.

The branches routed before the conservative filter are replayed in seven
disjoint geometry lanes.  Their exact evaluation counts sum to 113,124,235.
The largest geometry premium is the one-step value

```text
35086385884924358787108064569034674887022445357,
```

below the ceiling by
`6013932055074575660465001211944142188711229643`.
All other lane maxima are smaller.  Therefore the global premium is the
already-safe conservative leaf `(P72)`.

Let `R` be the unchanged record floor, `G` the rank-nine marks, and `Kern`
the inherited kernel capacity.  Exact integral arithmetic gives

```text
Cap_full=floor((G+R P_72)/55),
Cap_total=Kern+Cap_full,
Demand=R C(67544,11)-C(1048648,11).
```

Substitution gives `(G72)`, so demand exceeds complete capacity and the row
closes. QED.
