# Claim contract

- Exact row only: `K'=72`, `q=62`, `m=67544`, `n=1048648`.
- The inherited rank-nine marks, kernel capacity, record floor, and
  support-six through support-nine caps are unchanged from the proved
  `K'=71` payment chain.
- Conservative leaves are retained unless their exact premium exceeds the
  safe ceiling; exactly those 36 defect tuples are rerouted.
- The `T/A/F` router is exhaustive on every rerouted tuple.
- Every fixed union uses the joint support-4/support-5 charge on the original
  outside evaluation matroid.
- Lane computations are exact integer enumerations, not samples.
- This node closes `K'=72`; it does not claim `K'=73` or the full rank-nine
  interval.
