# Provenance

Statement U was posed by pilot `r36_hrlow` (round 36, bank 3) and REFUTED
by pilot `r37_urand` (round 37, bank 1) within the same day; the floor and
cap machinery is from pilot `r38_urate_genericity` (round 38, bank 3).
All three banks are coordinator-audited addenda on
`critical/nodes/rate_half_band_crossing_location/statement.md`.

The round-37 draft package (`notes/pilots_20260811/r37_mint_drafts/
statement_u/`) was written against the round-36 pin hours before the
refutation landed; its staleness was recognised at drafting and the
re-draft recorded as a wiring pre-condition in task #41. This node is that
re-draft: coordinator-authored 2026-08-11, keeping the draft's PROVED
arithmetic components (razor integers, (FIB) forms, U-sym surplus, the
binomial-step negative) and replacing U's TARGET framing with the
refutation ledger plus the round-38 floor/cap state.

Razor constants independently banked at
`notes/pilots_20260811/r36_hrlow/f4_results.txt:29-45`. No Modal
computation or external library was used.
