# Proof (of the PROVED components; the ledger's open items are TARGETs)

## 1. The (FIB) pigeonhole

Every fibre (structural) slope is witnessed by an `r`-subset of
`W = S_1 u S_2`, `|W| = r + f`, and two distinct fibre slopes cannot share
a locator. There are at most `floor((r+f)/f)` pairwise-distinct `r`-subsets
with the property that each omits at least one point some other subset
contains (each slope must "own" at least `f` points across the family
budget `r + f`); the sharp form is the standard pigeonhole
`T_fib <= floor((r+f)/f) = floor(r/f) + 1` — the two displayed forms are
the same integer for every `f >= 1` since `(r+f) = f*(r//f + 1) + (r mod f)`.
Equality `T_fib = r+1` requires `f = 1` and `chi` injective, which is the
LB1 configuration (`d = 1`), where the structural floor `ceil((r+1)/d)`
meets the cap. Nothing is imported from the type-2 ledger, which is
vacuous by sign on this bracket.

## 2. The U-sym razor kill

On a `mu_M`-invariant domain, an orbit-invariant locator carrier
annihilates the orbits the support misses and leaves `ceil(rho/M)` genuine
conditions on the single unknown slope (the mechanism node
`rate_half_far_ca_negation_closure_excess_fence` proves the `M = 2` case
exactly; the general `M` count is the same orbit computation). Survival
needs `ceil(rho/M) <= 1`, i.e. `M >= rho = 2^34`; at `M = 2` the surplus is
`2^33 - 1` conditions, `q`-free. Round 38 closed the carrier-completeness
residue: at odd `r`, degree parity forces the symmetric carrier family
`(X - x_0)P(X^2)`, so no unlisted symmetric mode exists.

## 3. The refutation and the floor (cited, proved on A1)

Statement U's refutation, the `T = (r+1) + j` construction, the exhaustive
censuses, the `j = 126` double-cover design with kernel dimension exactly
2, the rank half of R-GENERICITY, and the exchange law are proved in the
Round-37 U-rand and Round-38 URATE/genericity addenda of
`critical/nodes/rate_half_band_crossing_location/statement.md`
(coordinator-audited, with hand-checks recorded there). This node's
verifiers replay every EXACT-ARITHMETIC consequence: the razor integers,
`126*rho = 2r`, `2(r+1) - 126*rho = 2`, the floor value
`r + 1 + 126 = 1082331758719`, the six-decimal bit-invariance, the
exchange law's `delta = 0` optimum at razor, and the `mu_26` cell's
in-normal-form cap `18`.

## 4. What is NOT proved here

The exact value of `B_ca^far(k + 2^34)`. Open residues of record: (i)
chi-injectivity inside forced multi-edges, (ii) column-farness Case B (the
floor's two residues), and (iii) the cap's shared-`A*` normal-form
conditionality, whose safety margin is pigeonhole (`17.17x`), not
algebraic. The C3 `T = 19` exhibit shows the conditionality is real at
small rho.
