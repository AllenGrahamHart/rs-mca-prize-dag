# Result

The far-CA question at the crossing offset is no longer "is the count
exactly `r+1`" (Statement U — refuted) but "what is the constant in
`r + 1 + Theta(n/rho)`". The state of record: constructive floor
`r + 1 + 126`, proved modulo two named residues (in-block chi-injectivity;
column-far Case B); cap the same `126`, a theorem only inside the
shared-`A*` normal form with a `17.17x` pigeonhole margin; in bits nothing
moves (`2^39.977280` to six decimals either way), so the prize question is
untouched by the refutation.

For consumers: do not spend on `T <= r+1` at razor shape (unprovable); do
not transport per-slope condition counts into caps (the joint rank of
`Phi` is the correct object); the symmetric mode is closed and the
fibre mode bracketed — all remaining uncertainty lives in T_rand's two
residues and the normal-form conditionality.
