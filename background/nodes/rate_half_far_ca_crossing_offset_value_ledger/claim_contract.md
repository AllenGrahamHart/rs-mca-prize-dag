# Claim contract

- **Claim (ledger):** at razor shape, Statement U (every bad slope of a
  column-far pencil admits a locator inside `W`) is REFUTED;
  `B_ca^far(k + 2^34) = r + 1 + Theta(n/rho)` with constructive floor
  `r + 1 + 126 = 1082331758719` (modulo two named residues) and
  normal-form-conditional cap the same value; in bits both ends are
  `2^39.977280` to six decimals. The (FIB) pigeonhole bracket and the
  U-sym razor kill are PROVED components.
- **Dependencies:** the Round-37 U-rand and Round-38 URATE/genericity
  addenda on `rate_half_band_crossing_location` (proofs of the refutation,
  the rank half, and the exchange law); sibling nodes
  `rate_half_far_ca_negation_closure_excess_fence` (the T_sym carrier
  mechanism), `rate_half_far_ca_hr_dictionary_common_support` (LB1, the
  `f = 1` floor case), `rate_half_type2_ledger_vacuous_by_sign_fence`
  (why the cap is a from-scratch pigeonhole).
- **Output:** the far-CA residual map at the crossing offset in one place:
  what is refuted, what is proved, the two floor residues, the cap's
  conditionality, the named breakers, and the transport warning.
- **Consumer:** `rate_half_band_crossing_location`.
- **Nonclaims:** no unconditional exact value; no razor-scale
  measurement; the C3 `T = 19` exhibit and the pigeonhole margin are cited
  from the bank, not recomputed.
- **Falsifier:** an in-normal-form razor design with `T > r + 1 + 126`; a
  proof or refutation of either floor residue; an algebraic family with
  `m > 1.7e10`; or an arithmetic error in the replayed integers.
- **Replay:** `tools/ramguard tiny -- python3
  background/nodes/rate_half_far_ca_crossing_offset_value_ledger/verify.py`
  and the float-free independent audit `verify_audit.py`.
