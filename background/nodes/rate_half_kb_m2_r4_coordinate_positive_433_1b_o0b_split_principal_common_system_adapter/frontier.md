# Frontier

Build the exact split-lane symmetry quotient before appending outside rows.
The first candidate is simultaneous `B<->C`, `E<->F`, which fixes `S0` and
exchanges `SDE` with `SDF`. Do not rerun common-locus computations already
owned by the O0a cells.
