# Claim contract

The node imports only common-system equations and their scoped O0a geometry.
It does not import any O0a outside exclusion. O0b has a different outside
multigraph, so its six lane packets and all 105 labels per common row remain
to be paid.
