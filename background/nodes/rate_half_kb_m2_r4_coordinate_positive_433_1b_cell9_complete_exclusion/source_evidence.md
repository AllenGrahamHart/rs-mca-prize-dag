# Source Evidence

The exact owner list is encoded in `verify.py`. Its inputs are the 19
`requires` edges in `node.json`: the endpoint theorem, the universal orbit
quotient, and 17 terminal exclusion packets.

The orbit partition is reconstructed from
`rate_half_kb_positive_433_1b_universal_generic_label_orbit_router.py` rather
than copied from prose. Each algebraic owner retains its own digest-pinned
primary packet and independent audit.
