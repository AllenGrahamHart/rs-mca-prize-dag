# Dependency sub-DAG

```text
cell4_genus1_plane_kernel_reduction ------+
                                           +--> cell4_exceptional_scale_chart_exclusion --ev--> rate_half_band_closure
common_root_sign_symmetry_quotient --------+
```

Both incoming edges are `req`; the outgoing edge is evidence only because the
main genus-one chart remains open.
