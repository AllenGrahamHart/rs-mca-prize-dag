# Claim contract

## Claim

No pair-feasible `N=256` folded-profile `(3,4,0)` vector has `V=56`.

## Inputs consumed

- the exact eight-profile E28 reduction;
- the complete eight-profile joint exclusion.

## Output supplied

The live positive even variance frontier advances from `V<=56` to `V<=54`.

## Scope exclusions

- no exclusion at `V<=54`;
- no closure of folded profile `(4,2,0)`;
- no closure of later `N=256` or `N=512` bands;
- no route-wide pair-incidence theorem.
