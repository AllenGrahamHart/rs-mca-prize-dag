# Frontier

There are now only two cross-type compatibility geometries:

1. one globally normalized rational atom on every canonical pairwise weld;
2. all pair-type vectors in one rank-two `F(X)` pencil.

Extend the global atom to every owned record by one-swap packets. For the
rank-two branch, derive a base-field/degree ledger for the rational
coefficients or identify it with the upstream split-pencil census.
