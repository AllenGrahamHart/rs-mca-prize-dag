# Result

Official field arithmetic forces `p>=3583` and leaves at least 3,175
received-word excess layers in which every exact-shell fixed-cofactor
locator-prefix cell is exactly a power-sum prefix cell. If `p>=n-k`, this
coordinate transport covers every received word. The remaining task is the
row-sharp Q/Pade-graph bound, not a small-characteristic Newton repair.
