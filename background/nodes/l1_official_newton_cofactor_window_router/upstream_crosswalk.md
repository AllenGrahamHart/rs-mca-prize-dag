# Upstream crosswalk - official Newton cofactor window

## Przemek `(Q)` terminology

The first-form quotient/prefix map in `prob:capfpr-Q` records the signed
elementary-symmetric locator prefix. Its normalized-recursion/Fourier route
uses the first power sums and invokes Newton identities when the depth is
below the characteristic.

This router proves that exact compatibility for L1 fixed-cofactor cells. In
the declared official window, each local locator-prefix target is one
power-sum target with identical fiber size. At scalar cofactor this is the
literal top-shell `(Q)` atom; at positive cofactor the targets form the
already proved Pade graph and must still be controlled collectively.

## Portable contribution

The field-cap arithmetic is finite and exact: `p>=3583` and
`p-ell_0>=3174`. A PR can therefore advertise 3,175 uniformly
characteristic-safe received-word excess layers, and all layers on rows with
`p>=n-k`, without claiming the missing max-fiber estimate. This removes the
small-characteristic cycle caveat from that scope but does not identify the
special F2 growing-order Myerson statement with general `(Q)`.
