# Audit

- The proof counts distinct transformed explanations before applying the
  separate ratio-fiber slope multiplicity; it does not confuse slopes with
  codewords.
- Puncturing preserves distinct codewords because `N-e>=K` throughout
  `e<d`.
- Pairwise agreement is at most `K-1`, including over extension fields.
- The strict denominator sign is checked at every paid support and at the
  adjacent unsupported support.
- The coarse official scan is exact integer arithmetic over at most 65,236
  supports and uses negligible memory.
- The theorem stops when the Johnson denominator fails.  Failure of this
  proof method is not reported as an unsafe row or a counterexample.
- The node uses no statement from the refuted affine-span incidence
  compiler.
