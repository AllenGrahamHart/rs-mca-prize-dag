# Claim contract

## Proved

1. The deficit range `1<=h_a<=e` and owner cap `floor(e/h_a)`.
2. The punctured ordinary-list Johnson cap `(PJ1)` whenever its denominator
   is positive.
3. The cumulative profile `(PJ2)` and coarse payment `(PJ3)`.
4. The complete official support prefixes `e<=63908` and `e<=65236`, with
   exact endpoint arithmetic and adjacent denominator failures.

## Not proved

1. Any estimate after the Johnson denominator becomes nonpositive.
2. Necessity or sharpness of either printed wall.
3. A payment of the remaining middle-support cells.
4. A complete MCA or prize theorem.

## Promotion rule

Keep `PROVED` only while both verifiers accept the source pins, formulas,
official constants, complete finite-prefix budget scan, and hostile
mutations.
