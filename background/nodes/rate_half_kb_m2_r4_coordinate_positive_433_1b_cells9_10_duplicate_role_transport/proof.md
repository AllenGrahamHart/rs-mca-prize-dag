# Proof

Cell 9 has singleton `BC+` and matching `(LA,AB),(AC,BC-)`; cell 10 has singleton `BC+` and matching `(LA,AC),(AB,BC-)`. Exchanging `B` and `C` takes cell 9 to cell 10 while fixing the singleton. The `BC-` sum changes sign, so flipping `epsilon_2` preserves its signed `q` row; `r`, `t`, and `epsilon_1` remain fixed. Direct symbolic expansion verifies all five common product, signed-sum, and root-square triples in every source-sign row.

For the outside atlas, gauge `(D,E,F)` by `sigma_c`. The first five outside records remain fixed and the final two records `BF` and `sigma_c CF` exchange. Products and squared sums agree identically, and the complete nonzero and signed-distinctness guard set is invariant.

Deleting any outside role leaves six records. The induced outside permutation maps every perfect matching bijectively onto all 15 perfect matchings of the new residual set. Hence it bijects all 105 labels in each target lane. Four source rows and four target lanes give 1,680 principal systems. The complementary product-rankdrop branch is already excluded by the required global theorem.
