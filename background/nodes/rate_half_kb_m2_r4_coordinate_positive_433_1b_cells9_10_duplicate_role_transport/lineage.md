# Lineage

This is the `[9,10]` analogue of the proved `[12,13]` duplicate-role transport. Both exchange `B,C` and gauge the outside variables; `[9,10]` fixes the singleton parameter and flips the second source sign, while `[12,13]` fixes source signs and sends its singleton parameter to its negative.
