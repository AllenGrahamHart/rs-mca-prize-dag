# Audit - L1 exact-shell balanced shifted-lattice reduction

## Checked axes

1. The complement locator has degree `omega=n-m`, not agreement degree `m`.
2. The shifted degree uses `deg N-(k-1)`.
3. `W|N` is derived only for squarefree split `W|Omega`.
4. The low-degree branch may be empty.
5. Its nonempty support mass is one codeword's binomial family.
6. Minimum-distance uniqueness uses the band inequality.
7. The strict `m+1` agreement lower bound uses the `-1` in `(BL3)`.
8. Exact-shell emptiness is distinguished from unguarded-census emptiness.
9. Balanced coefficient caps and dimension use predictable degrees.
10. The complement gcd guard is retained after cancellation.
11. No smoothness or base-field count is inferred from the normal form.

## Route effect

The L1 band no longer owes any payment for near-rational support
multiplicity.  Its complete-agreement shells live entirely in the balanced
split-pencil locus already isolated by upstream.
