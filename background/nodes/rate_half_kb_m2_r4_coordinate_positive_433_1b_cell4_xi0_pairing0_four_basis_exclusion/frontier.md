# Frontier

Exchange of the two identical positive `de` records should transport this
result to `xi=1`, matching `0`, without further computation.  The next new
algebraic case is `xi=2`, matching `0`, whose residual positive `de,de` pair
uses a different target-free specialization.

After the three parallel-`DE` missing roles are understood at matching `0`,
test whether the same matching transports or quotient identities used in
cell `3` pay matchings `1` and `2`.  Do not launch the remaining `104` slices
as independent norm jobs before these symmetries are exhausted.
