# Statement

## Claim `(KBP1B4-X0P0-1)`

In the guarded principal branch of deployed positive `433-1b -> O0a` role
cell `4`, fix missing outside record `xi=0` (the first positive `de` record)
and canonical residual matching `0`.  Every one of the

```text
4 source-sign rows x 4 target-sign lanes = 16
```

raw cases is empty.

More precisely, in the exact four-basis common quotient algebra the missing
record is `m=B(-t^2)/A(-t^2)`.  The residual opposite `de,-de` pair gives one
target-free element.  Its norm to `F_2130706433(r)` has numerator degree `298`
with `265` terms and denominator degree `156` with `157` terms.  In each
source-sign row the numerator has exactly five deployed-field roots.  Adding
all roots of the four inverse guards and every denominator gives exactly seven
candidate `r` values.  Every candidate is excluded by a printed source guard:
five at the `r` stage and two after lifting `t`.  There are no route-open
source points, witnesses, free fibers, or unresolved boundaries.

The target-free equation does not contain either target-lane sign, so the
four-lane payment is exact.  No other missing record, matching, cell, orbit,
K3 row, LIST row, MCA row, or Prize result is claimed.
