# Attack

This closes only the four `R02` `B0` leading charts with `K8!=0`. The
exhaustive next split in each is `K8=0`.

On `K8=0`, use the `A0` pseudo-remainder chart when its nonnamed leading
factor `K10` is nonzero. Keep the residual `K8=K10=0` intersection as a
separate leaf. Replay that cover for `F05-R02`, `F06-R02`, and `F07-R02`; it
is already proved for `F04-R02`.

The `R20` replay is a different route: after the same saturation its core
ideal remains one-dimensional. Do not infer a finite-field exclusion there.
