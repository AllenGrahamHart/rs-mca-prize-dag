# Attack

This closes only the `B0` leading chart `K8!=0`. The exhaustive next split
is `K8=0`.

On `K8=0`, use the `A0` pseudo-remainder chart when its irreducible
degree-10 leading factor `K10` is nonzero. Keep the residual
`K8=K10=0` intersection as a separate leaf. Do not infer either branch from
the present saturation because `K8` was explicitly inverted here.
