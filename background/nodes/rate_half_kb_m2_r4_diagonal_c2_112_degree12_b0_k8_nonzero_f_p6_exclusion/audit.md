# Audit

- Eight-cell replay app `ap-O4QtW6BMk9vvf8hNos3Vak` completed with eight
  process passes and no timeout or remote error. The four `R02` jobs took
  `161.9`--`194.8` seconds and at most `0.56 GB` peak child RSS. Their
  saturated basis sizes are `25,25,26,26`, all with dimension zero.
- In each `R02` quotient, the exact degree-six field equations leave a proper
  ideal, but the complete open product reduces to zero at factor 18. This is
  a boundary exclusion, not a claim that the unlocalized field ideal is the
  unit ideal.
- The same replay deliberately records that all four `R20` saturations have
  dimension one. No Frobenius closure or emptiness claim is made for them.
- The detailed `F04-R02` saturation retains the exact degree-30 `s`-eliminant
  and factor ledger as an independent route.
- Linear-fiber app `ap-OsdVI9zfrrdj6ZkpADRPW7` ran the five `F04-R02` roots
  in parallel. Every case completed in `29.1`--`38.1` seconds at about
  `0.39 GB` peak child RSS.
- The quartic/quintic exclusion uses irreducibility and the elementary
  subfield criterion `d | 6`; it is not a numerical root search.
- Every cancelled or inverted factor is printed in the claim contract.
