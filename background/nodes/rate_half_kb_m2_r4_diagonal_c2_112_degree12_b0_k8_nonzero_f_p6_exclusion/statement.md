# Statement

Let `p0=2130706433`. For each literal cell

```text
F04-R02, F05-R02, F06-R02, F07-R02,
```

let

```text
I_F = (R12, C_A,F, C_B,F) subset F_p0[x,s,pvar]
```

be its `B0` pseudo-remainder core ideal. Let `K8` be the nonnamed factor of
the `B0` leading coefficient. Then `I_F` has no `F_(p0^6)` point on the
complete named-open chart with `K8!=0`.

More precisely, after saturating each `I_F` by

```text
V K8 (s^2-4pvar),
```

the four quotient dimensions are zero and their basis sizes are

```text
25, 25, 26, 26.
```

Adjoining the exact degree-six field equations and reducing the complete
named-open product gives zero in every cell.

Independently, the `F04-R02` elimination to `F_p0[s]` is generated by a
degree-30 polynomial whose irreducible factor degrees are

```text
1,1,1,1,1,4,4,5.
```

The quartic and quintic factors cannot vanish in `F_(p0^6)`. Exact fiber
checks at all five linear roots also force the complete open product to zero.
