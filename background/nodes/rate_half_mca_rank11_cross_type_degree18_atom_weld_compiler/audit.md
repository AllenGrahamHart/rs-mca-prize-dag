# Audit

- Recomputed every packet row from `b=17-3t` and `r=31-3t`.
- Checked availability separately: anchors/counterparts use at most 18
  records and other basis types use exactly their guaranteed three.
- Checked that the represented type set, not record multiplicity, determines
  the common recovered core.
- Checked degree 18 from 18 anchor roots plus an off-line counterpart record.
- Reused only the pure-locator and common-pole arguments whose hypotheses
  survive the changed multiplicities.
- Checked every overlap against the independent atom-identity margin.
- Kept cross-weld coherence and aggregate payment open.
