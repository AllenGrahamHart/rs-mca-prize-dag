# Frontier

Substitute `A=0`, `B=0`, and, for the moving-moving template, `C=0` into
the reconstructed negative source form before applying `(KBQS-1)`. Factor
the q-slice mismatch separately on those loci. All negative assignments off
their union are already deleted. Positive candidates remain unrestricted by
this node and should be attacked directly with the q-slice gate.
