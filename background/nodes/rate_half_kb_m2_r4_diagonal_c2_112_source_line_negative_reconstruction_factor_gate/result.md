# Result

PROVED. The twelve negative internal assignments reduce to two normalized
templates with exact augmented-determinant factors. The apparent `A` locus
forces `z=-1` and is impossible. Only `B=0`, and `C=0` in the moving-moving
template, proceed to the q-slice gate. No full packet or row is deleted.
