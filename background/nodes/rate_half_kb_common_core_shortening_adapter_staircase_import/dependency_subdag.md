# Dependency subdag

- **Ingredients (textual):**
  - the upstream maximal-support slope-degree theorem (source of the
    `c < k` clause in the non-affine case; cited, not re-proved);
  - `v13_2_near_rational_supportwise_two_anchor_payment` — the `2d`
    near-rational bound referenced by the upstream packet; used here
    only as a control (the `#1160` construction is globally affine and
    is not deleted by the adapter). No requires edge: this node's
    proved content stands without it.
- **Requires edges:** none (self-contained given the cited source
  clause; all arithmetic replayed locally).
- **Evidence for:** `rate_half_band_crossing_location` (row-level
  route-cut context for the official KoalaBear crossing bracket).
- **Consumers downstream:** any future S/A/E ledger regeneration must
  route the common-core branch through this adapter's typed interface
  or supply the missing whole-line selector recorded here.
