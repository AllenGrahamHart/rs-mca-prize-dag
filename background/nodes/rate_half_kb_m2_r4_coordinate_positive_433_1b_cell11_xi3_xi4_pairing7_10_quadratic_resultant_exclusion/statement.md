# Statement

Over `F_2130706433`, the guarded source-role cell-11 positive `433-1b`
route is empty for `xi=3`, residual pairing `7`, and every required
source and target sign lane. Exact outside-role transport closes the orbit
`{(3,7),(3,10),(4,7),(4,10)}`.

This closes exactly four active labels. The 30 endpoint labels with
`xi in {5,6}` are not claimed.
