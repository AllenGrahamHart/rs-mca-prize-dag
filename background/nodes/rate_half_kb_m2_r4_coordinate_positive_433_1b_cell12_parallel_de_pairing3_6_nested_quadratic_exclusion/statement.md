# Statement

Over `F_p`, `p=2130706433`, the guarded cell-12 positive `433-1b` route is
empty for parallel-`DE` missing roles `xi=0,2` at residual matching `3`.
The exact generic label quotient transports this to six labels:

```text
{(0,3),(0,6),(1,3),(1,6)}, {(2,3),(2,6)}.
```

These form two generic orbits. No other matching is decided.
