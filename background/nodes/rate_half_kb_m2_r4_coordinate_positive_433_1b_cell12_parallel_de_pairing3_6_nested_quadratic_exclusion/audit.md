# Audit

Run `verify.py` and `verify_audit.py`. The independent audit validates the
AST adapter and separate root reconstruction, replays each candidate-root
union and leading-boundary exit, rebuilds the source kernel, and directly
checks all first-pair quadratics, missing-sum equations, `f` lifts, and final
colored-pair values.
