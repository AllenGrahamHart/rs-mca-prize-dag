# Rank-eleven large shared pair-core payment

- **status:** PROVED
- **closure:** exact conditional branch payment
- **scope:** deployed KoalaBear MCA row, post-near error rank eleven

## Statement

Use the intrinsic near deletion and reversible rank-eleven gauge. For each
post-near selected record with support-local margin `theta<=387`, fix the
actual minimizing pair `(a_gamma,b_gamma)` as in the rank-eleven pair-core
route cut, and put

```text
H_(a,b)={x in D:r_0(x)=a(x), r_1(x)=b(x)}.
```

Assume there is one set `J subset D` with

```text
|J| >= K-4922 = 1043654
```

contained in `H_(a,b)` for every distinct minimizing pair used by those
low-margin records. Then the complete received line has at most

```text
274790217213713313
```

bad challenge slopes, including the disjoint `2w=134944` near-rational
charge. This is below

```text
B_*=274980728111395087
```

by `190510897681774`.

Thus an unsafe rank-eleven line cannot have all of its `theta<=387`
minimizing pair cores share `K-4922` coordinates. The deployed twelve-owner
construction in the sibling multi-owner fence has common core `K-9`, so it
is paid by this theorem.

## Nonclaims

Nothing is asserted when the common intersection of the low-margin pair
cores has size below `K-4922`. In particular this does not pay rank eleven,
construct an S/A/E chronology owner, or close KoalaBear.

## Falsifier

A line satisfying the stated common-core premise whose high-margin charge,
low-margin distinct-pair count, fixed-pair multiplicity, and near charge sum
above the printed cap; or failure of the shortened Johnson or interleaving
hypotheses at any residual dimension `1<=d<=4922`.
