# Result

`PROVED`. The four automatic source candidates behave as follows:

```text
G residual:   canonical section of O_C(2B),
P_F residual: the same section by the Pade syzygy,
G_t, G_X:     do not descend through D_mand.
```

The direct second-section route is therefore retired. A future use of
`h^0(O_C(2B))=1` must first construct a new source combination carrying all
mandatory zeros. The active shape-A route returns to the concentrated excess
norm and scalar weld.
