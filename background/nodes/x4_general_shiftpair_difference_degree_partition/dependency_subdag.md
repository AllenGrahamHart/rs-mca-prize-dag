# Dependency sub-DAG

```text
x4_general_shiftpair_difference_degree_partition [PROVED]
  --ev--> x4_primitive_star_u1_coverage [TARGET]
```

The theorem changes the attack coordinates but does not supply either local
degree bound, so the edge is evidence rather than a requirement.
