# Lineage

- Local source: `star_pte_lemma` and
  `x4_maxfiber_local_shiftpair_equivalence` supply the common-core and local
  graph viewpoints.
- Upstream alignment: `rs-mca` v13.2,
  `prob:capg-active-shiftpairs`, and the experimental SP route named
  "Low-degree difference incidence."
- New content here: the exact `d=0` iff F-4-minimal dictionary and the
  disjoint local partition separating all nonminimal general records.
