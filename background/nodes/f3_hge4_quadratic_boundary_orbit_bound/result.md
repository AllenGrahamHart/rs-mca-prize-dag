# Result

The dyadic quadratic third-line boundary contributes fewer than
`(2/3)m^2` primitive ordered scaling orbits. Both `e=1` and `e=2` are now
paid by explicit upper bounds; all remaining exact-level widths have defect
at least four. These bounds must be debited from the retained-width budget.
