# Audit

1. The loop edge is the singleton; the two doubled signed product pairs are
   exactly the two source antipodal pairs.
2. Target representative changes cover all four relative square-root signs.
3. The first factor route uses `b!=+/-1` and `b!=c` explicitly.
4. Family B also uses `b^2!=1` and `r!=1`; these are source/target guards,
   not genericity assumptions.
5. Both necessity branches and both converses are checked in the deployed
   characteristic.
6. The two families survive common-`K`; no deletion is inferred.
