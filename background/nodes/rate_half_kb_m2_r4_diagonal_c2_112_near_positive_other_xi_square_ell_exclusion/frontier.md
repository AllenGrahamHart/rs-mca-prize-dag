# Frontier

Both square allocations in the other relative `xi` orbit are closed.
Classify its mixed allocation, then move to the moving-moving template,
negative loci, and homogenized `w=0` boundary.
