# Attack

Do not seek a uniform contradiction from one rank-eight chart using only:

```text
rank(ev_B)=8,
owners in one affine U^2 flat,
exact supports and pair noncontainment,
rank-ten component extensions,
error affine rank <=3,
the current distinct or weighted selector demand.
```

The explicit `K'=11` construction satisfies all of these and exceeds both
demands.  A live payment must retain information discarded before the fixed
chart was printed.  The main candidates are:

1. the full ten-dimensional normalized-deviation span and its eighteen
   dense-anchor provenance;
2. compatibility of the same records and extension weights across many
   nine-subset charts;
3. a chronology-correct aggregate owner which couples different points of
   the affine `U^2` flat;
4. a global incidence identity linking owner petals, coordinate-pair loads,
   and the rank-three error form.

Every proposed local repair should first be tested against the eight-petal
model and should print the exact additional ancestor hypothesis that
excludes it.
