# Claim contract

## Input

Only the local rank-eight output of the fixed nine-subset target router:
one rank-eight nine-subset, owners in its affine `U^2` flat, exact rich
supports, rank-ten affine-owner component extensions, support-wise pair
noncontainment, and error affine rank at most three.  For the weighted
comparison, retain the component-extension unit of the weighted
concentrator.

## Output

An explicit `K'=11` residual realization, with a reversible official-row
lift, containing `4070352` distinct records and
`9265216597693056` marked `(record,T)` incidences.  These exceed the exact
distinct and weighted selector demands at that shortening.

## Nonclaim

The realization does not reproduce the full dense-anchor ancestor packet,
the normalized deviation-span condition, an unsafe line, a chronology
owner, a rank-eleven payment, or a prize closure.
