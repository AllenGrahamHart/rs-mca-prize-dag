# Fixed rank-eight chart local-cap fence

- **status:** PROVED
- **scope:** the local rank-eight conclusions of the fixed nine-subset target
  router, including the weighted component-extension demand
- **witness shortening:** `K'=11`

On the residual row

```text
(n',K',m')=(1048587,11,67483)
```

there is a ten-dimensional RS correction space `V'`, a nine-subset `B`
with `rank(ev_B|V')=8`, and a same-support pair-noncontained rank-eight
owner-flat family with

```text
4070352 > 2578110
```

distinct finite slopes.  Every selected record has exact support `m'`,
error affine rank at most two, and

```text
C(67473,2)=2276269128
```

rank-ten component eleven-subsets extending `B`.  Consequently its marked
component weight is

```text
9265216597693056 > 5869376383979174,
```

the exact weighted-selector demand at `K'=11`.  The construction lifts
reversibly to the official row, where every support and every owner core
acquires the same `K-11` coordinates.

Thus neither the distinct-record target nor its weighted refinement can be
paid from the current fixed-chart rank-eight conclusions alone.  A payment
must retain additional ancestor information or couple charts and owners
through a global chronology/incidence argument.

## Nonclaim

The construction is not an unsafe KoalaBear line and does not instantiate
the dense-anchor deck, the ten-dimensional normalized-deviation span, or
the complete pre-selector component census.  It fences only arguments that
treat the printed fixed-chart rank-eight output as their complete hypothesis
set.
