# Statement

On each F04 target `R02,R20`, intersect the surviving cubic/J11 route with
the two symmetric unions obtained from the nonnamed denominator pairs

```text
(D7(c,d)=0 or D7(d,c)=0),
(D11(c,d)=0 or D11(d,c)=0).
```

After symmetric descent, `w=-U/V` reconstruction, and removal of named
factors, the four essential union equations have degrees

```text
R02: 25, 60;    R20: 28, 62.
```

For every one of the four intersections, the complete transported named
localizer is zero modulo the exact deployed-prime Groebner basis. Hence all
four union branches are empty over `F_2130706433` and every extension.
