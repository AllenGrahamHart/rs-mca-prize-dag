# Dependency sub-DAG

```text
stratification_partition_thm [PROVED] --req-->
  ww_paid_residual_partition_adapter [PROVED]
    --ev--> ww_row_envelope_clause [TARGET, RETIRED]
```

The dependency supplies the arbitrary-predicate first-match lemma. The
adapter instantiates it on W3 list words and adds the integer budget identity.
That implication survives even though W3 is no longer consumed.
