# Result

The pair-Lagrange data now determine the entire contracted moment pencil.
Every exceptional-root coefficient is a nonzero constant times
`z-xi_i`, with that constant printed explicitly from the MDS circuit; the
three off-locator coefficients retain their barycentric formula. The top
coefficient `q_bar` and every external circuit scalar are explicit as well.

The external MDS circuits also force the reconstructed moment pencil to have
`Q` as its Hankel kernel identically. The exceptional/ordinary ranks,
first-order crossing, and pinned adjugate follow. CR-003 no longer needs
source-weight, endpoint-moment, Hankel-chain, rank, or cofactor variables on
the distance-three branch; the corrected reciprocal square and original
column-far lift remain genuine gates.
