# Result

All lower reciprocal complement coefficients are now represented by one
identity `FU+P_clL=R_X`. The external classifier can choose `F,U`, test one
coefficientwise divisibility by `P_cl`, and recover `L`, while retaining the
separate `E`-divisibility, unit, Hankel, and splitting gates.
