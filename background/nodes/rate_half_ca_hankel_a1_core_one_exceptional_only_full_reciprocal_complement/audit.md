# Audit

- Reversed the corrected complement at degree `D_0`, so the degree-`D_0-1`
  right side acquires exactly one factor of `Y`.
- Removed the proved constant coefficient of `A_vee` before dividing by
  `Y`; no Laurent term is introduced.
- Grouped `j_infF+EG=YL` with its `P_cl` factor before cancelling `Y`.
- Checked that the constant coefficient is the preceding Bezout identity
  and that the resultant recovers the two-sided certificate.
- Did not treat the descended identity as sufficient: reconstruction of
  `G` requires the separate divisibility `E | (YL-j_infF)`.
