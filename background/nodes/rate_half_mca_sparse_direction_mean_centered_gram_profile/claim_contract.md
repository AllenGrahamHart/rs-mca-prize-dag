# Claim contract

## Proved

1. The PSD mean-centered block cap `(MC1)` under the three exact sign
   hypotheses `(MC0)`.
2. The suffix-minimum exact deficit profile `(MC2)`.
3. Every KoalaBear support `64038<=e<=64047` and every Mersenne support
   `65419<=e<=65454` is paid.
4. The exact endpoint and adjacent profile or denominator records.

## Not proved

1. A payment at `e=64048` or `e=65455`.
2. Optimality of the chord or trace-rank estimate.
3. Any theorem for the remaining middle-support cells.
4. A complete MCA or prize theorem.

## Promotion rule

Keep `PROVED` only while both verifiers accept the source pins, PSD rank
argument, chord inequality, suffix-minimum compiler, exact strip scans, and
hostile mutations.
