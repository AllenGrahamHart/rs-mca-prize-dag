# Claim contract

- **claim:** `(AS1)--(AS4)` for every RS code, received word, agreement
  threshold, and affine codeword flat in the stated range.
- **source object:** upstream `thm:affine-span-list` and
  `thm:rank-flat-list` at `b13de811`.
- **consumer effect:** supplies a field-independent list cap inside a known
  affine flat and supports the rate-half affine-rank rigidity corollary.
- **nonclaim:** the locator Grassmann lines in the budget-three atlas are not
  thereby identified with affine flats of codewords.
- **falsifier:** an explicit RS list and affine flat exceeding `(AS1)` or
  `(AS3)`, or a direction subcode violating `(AS4)`.
