# Result

Przemek's affine-span and generalized-weight list compilers are independently
replayed locally. At the rate-half budget-three predecessor, `(AS2)` caps a
codeword pencil at two, while `(AS1)` gives a raw rank-two cap of four. The
separate affine-rank rigidity node resolves the equality case.
