# Claim contract

## Proved

1. Exact terminal-deficit explanations lie on one affine codeword line when
   `e>=K`.
2. Their outside agreement sets, after deleting a common core of size at
   most `K-1`, are disjoint, proving `(TL1)`.
3. The prefix-plus-terminal profile `(TL2)`.
4. KoalaBear support `e=64048` and Mersenne support `e=65455` are paid.
5. The exact endpoint and adjacent stopping records.

## Not proved

1. A payment at `e=64049` or `e=65456`.
2. An affine-line cap for exact deficits below `e`.
3. Any theorem for the remaining middle-support cells.
4. A complete MCA or prize theorem.

## Promotion rule

Keep `PROVED` only while both verifiers accept the source pins, exceptional
coordinate saturation, restriction injectivity, outside packing, exact
profile scans, and hostile mutations.
