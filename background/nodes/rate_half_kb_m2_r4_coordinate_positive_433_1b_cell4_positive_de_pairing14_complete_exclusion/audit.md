# Audit

- The exact computation covers `xi=0`; only identical positive-copy
  transport supplies `xi=1`.
- Matching 14 is pinned as `((0,5),(1,4),(2,3))`.
- The scalar replay uses `Pair(-de,bf)` and `Pair(de,sigma_c*cf)` in the
  same order as the symbolic resultant.
- The common-`f` resultant is division-free and retains quadratic
  leading-degree drops.
- Norm numerator, norm denominator, and every rational inversion numerator
  and denominator are included in the candidate-root union.
- Every candidate root reaches a guard, no-lift, or directly replayed finite
  terminal. No free fiber is accepted as excluded.
- The verifier independently recomputes both quadratic root sets, their
  intersection, all roots of the even quartic, and the final colored cut.
- No matching or missing role outside the printed positive-`DE` scope is
  claimed.
