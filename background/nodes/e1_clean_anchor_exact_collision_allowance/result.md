# Result

PROVED. The vague E1 density ask is compiled to six exact finite
collision-pair allowances. The live clean cells have quotient orders `256` and
`512`, not `128` and `256`; those smaller numbers are folded dimensions.

This narrows the route but pays no row. The next theorem must bound the actual
reduced-value collision pairs below the relevant printed allowance, certify
more than `B*` values directly, or abandon E1 for another `V`/`M` supplier.
Rows with quotient-generated field of size at most `B*` are now ruled out of
E1 unconditionally and belong to the latter branch.
