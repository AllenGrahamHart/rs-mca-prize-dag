# Claim contract

- Exact row only: `K'=80`, `q=70`, `m=67552`, `n=1048656`.
- Exactly the 26,104 above-ceiling tuples represented by the canonical digest
  are rerouted through the complete pairwise atlas.
- Every carrier, high-support, and geometry branch is retained.
- Counts and comparisons are exact integers; no sample is used.
- This node closes `K'=80`; it makes no claim about `K'=81`.
