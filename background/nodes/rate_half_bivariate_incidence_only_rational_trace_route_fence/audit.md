# Audit

1. `W` is a minimum pair union, not an arbitrary support: all 36 pair unions
   are at least 12.
2. The unique deficient point is outside `W`, so every matrix column in the
   witness is a highest clone and the rational kernel is blockwise nonzero.
3. The witness exercises `T=rho+2` and the exact one-deficit incidence
   identity, unlike the earlier `T=3` realizability censuses.
4. It does not exercise coefficientwise `X`-degree at most `rho`; that gate
   fails and is stated prominently.
5. Rank `M_W=11` follows both from the explicit barycentric kernel and direct
   elimination. Rank `C_W=12` is independently recomputed.
6. The Modal run was one deterministic 256 MB task. The retained local
   verifiers require only the Python standard library and the result packet.
