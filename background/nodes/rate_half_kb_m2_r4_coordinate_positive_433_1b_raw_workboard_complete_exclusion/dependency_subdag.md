# Dependency sub-DAG

```text
common compiler + signed atlas
              |
      nine disjoint role owners ----+
                                     +--> raw workboard empty (PROVED)
      global rank-drop exclusion ---+
```

The next edge is not automatic:

```text
raw workboard empty --[balanced-core / genuine-pencil compiler OPEN]-->
distinct affine-slope K3 census
```
