# Claim contract

- **claim id:** `f3_h3_dsp8_smooth_quotient_cap_compiler`
- **proved claim:** the pointwise Mattarei quotient cap turns the live smooth
  DSP8 residual into the exact sufficient unweighted estimates `(SQC4)` and
  `(SQC5)`
- **normalization:** `U_sm^c` forgets only `(z,w)`; it retains the normalized
  product pair, smooth trace, richness, class, split, and signed-disjointness
  predicates
- **dependencies:** Mattarei affine-pair bound, unit-product trace normal
  form, and exact smooth residual target
- **nonclaims:** no `O(n^(4/3))` unweighted theorem, no marginal elliptic
  point bound, no full shifted-energy payment, and no DSP8 close
- **falsifier:** a base tuple whose quotient multiplicity is not `R(t)`, a
  retained `t` making the forms in `(2)` proportional, or failure of the
  exact rational implication `(SQC4)`
