# Result

The base-field half of the `h=7` conic attack is finite. It is empty in
characteristics `8191` and `131071`; in characteristics `524287` and
`2147483647`, only the two signs in (BCR2) need later exact replay.

The non-base-field conic parameter branch and all other L1 chambers remain
open.
