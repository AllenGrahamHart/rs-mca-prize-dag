# Statement

## Claim `(KBP1B3-XI1P0-1)`

On the guarded deployed positive `433-1b -> O0a` role-cell-3 branch, the
outside systems with canonical matching `pairing=0` and missing indices
`xi=0` and `xi=1` are identical after swapping the first two parallel
positive `DE` records.  The swap preserves the product record `de`, its
squared-sum record `(d+e)^2`, every target guard, every source sign, and every
target-sign lane.

Consequently `(KBP1B3-XI0P0-1)` transports to `xi=1`: all

```text
4 source signs * 4 target lanes = 16
```

raw `xi=1,pairing=0` outside cases are empty.

The claim does not cover `xi=2,...,6`, another matching, complete cell-3 or
positive `433-1b` closure, K3, LIST, MCA, or either Prize result.
