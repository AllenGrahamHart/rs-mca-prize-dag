# Frontier

The orbit-distinct negative `DE` record `xi=2` is now excluded by
`(KBP1B3-XI2P0-1)`.  The next missing record is `xi=3`, carrying `df` and
`(d+f)^2`.

At `pairing=0`, deleting `df` retains `paired(de,de)` as the first residual
cut, so first test whether the proved `xi=2` target-free root ledger can be
reused.  The missing product and squared-sum equations change, and must be
rederived before a finite solver is run.
