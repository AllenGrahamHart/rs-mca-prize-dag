# Claim contract

## Proved

1. The official profile-`(4,4,S=20)` cofactor ceiling is exactly `1707433`.
2. Every collision has local valuation in the fourteen-value set printed in
   `(P44-1)`.
3. Local reciprocity forces `m/2^mu=1 mod 256`.
4. The global residue-degree condition leaves exactly `1133` necessary-sieve
   cofactor values, with the printed count by valuation.
5. Fourteen pure-power branches survive those necessary conditions.

## Not proved

- No listed cofactor is asserted to be realized by a profile-`(4,4)` vector.
- No collision orbit, norm quotient, or ideal family is counted.
- The one-orbit-per-fixed-ideal height collapse at square mass `18` is not
  imported at square mass `20`.
- The cap of seven orbits and the parent pair-budget target remain open.

The comparison `14>7` is a route fence for the existing necessary-sieve
method, not a lower bound of fourteen collision orbits.
