# Claim contract

- **Claim ID:** `rate_half_kb_m2_r4_diagonal_c2_112_source_line_q_slice_resultant_gate`
- **Input:** one reconstructed saturated source-line `(1,1,2)` candidate.
- **Output:** the necessary degree-eight identity `(KBQS-1)` with the aligned
  or near-aligned target quadratic in `(KBQS-2)`.
- **Deletion interface:** a coefficient mismatch in `(KBQS-1)` rejects that
  reconstructed candidate before the full colored quotient calculation.
- **Non-output:** no sufficiency claim, universal candidate deletion,
  interpolation, endpoint, owner, payment, row, or Prize close.
