# Audit

The verifier reconstructs all fifteen cells and the duplicate-role action,
checks the exact source-root assignment and identical target rows, and
enumerates the `4*2*2*105` system census. Controls alter one duplicate target
row and one partner cell.
