# Lineage

This node refines the quadratic paired-product interface after complete
`F_29` squared-sum replay showed that every product-compatible lift in the
surviving cell-5 lane fails each residual sum row.  It replaces outside
target variables by two exact triangle identities without promoting the
finite-field pattern to a universal classification.
