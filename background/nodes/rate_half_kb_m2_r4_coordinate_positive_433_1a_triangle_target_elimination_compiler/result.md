# Result

PROVED.  Two residual matching templates with missing-mate product `ef`
now have target-variable-free product chains and compact squared-sum cuts.
The outside representatives `d,e,f` disappear entirely, leaving only the
common rational maps and three outside source deck labels.  The repaired
chains have four product equations: the added cross relation makes the
target-product elimination complete.

This is a compiler theorem, not a template exclusion or route closure.
