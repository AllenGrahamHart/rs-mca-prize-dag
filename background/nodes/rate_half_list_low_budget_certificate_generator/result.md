# Result

The two fully proved rate-half list branches now have an executable exact map
from public row data to a prize-facing certificate. For both `B*=1` and `2`,
the generated ordinary and all-arity certificates report

```text
a*=3n/4,       delta*=1/4,
```

and certify that adding one to the radius numerator is unsafe. The generator
fails closed on all presently unproved rate-half budgets.
