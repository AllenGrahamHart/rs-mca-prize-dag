# Claim contract

## Consumed content

- The profile-(2,10) payment supplies the prior residual edge budget.
- The profile-(1,14) router supplies the exact payment implication and the
  138-orbit envelope conditional only on cofactor-1028 energy-four emptiness.
- The cubic exclusion discharges that final premise.
- The weighted-kernel dictionary supplies the profile multiplicities and the
  oriented-to-unordered factor one half.

## New proved content

1. Profile `(1,14,S=18)` has exact worst-case charge
   `20391647614756836040054426763033478955008`.
2. The residual edge budget is
   `2231339193048374054995899432498611923367`.
3. The next maximum-weight profile is `(0,18,S=18)`.
4. Its tight residual uniform oriented cap is `3994`.

## Guards

- All 128 possible cofactor-514 ideal families are charged; no cofactor-514
  emptiness is assumed.
- Cofactor 1028 is removed only after all five possible energies are proved
  impossible.
- Every orbit restores all 256 oriented shifts/signs before division by two.
- No later profile is paid or declared empty.
- The parent E1 target remains open.
