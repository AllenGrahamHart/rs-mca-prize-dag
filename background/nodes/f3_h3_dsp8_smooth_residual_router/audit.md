# Audit

1. `G_sm` and `G_sing` are raw trace-curve records, not point counts and not
   the `K`-normalized shift-pair counts.
2. The factor is `G=4K`; omitting or doubling it changes the residual by a
   factor of four or two.
3. The class weights `10,17` remain inside both pieces. No class discount is
   inferred from smoothness.
4. The strict nodal bounds permit a weak `<=` smooth residual at the printed
   endpoint and still give a strict total inequality.
5. The one-root and three-root residuals are different. Only the smaller
   `36086/25` value is uniform.
6. The nodal payment is subtracted once. The high-tail domination router is
   not an additional credit against `(SRR4)`.

