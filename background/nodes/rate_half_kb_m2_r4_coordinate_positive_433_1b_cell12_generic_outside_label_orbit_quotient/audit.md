# Audit

Run `verify.py` and `verify_audit.py` in this directory. The first checks the
pinned router and DAG edges; the second reconstructs the action without
importing the router and checks every orbit and hostile count mutation.
