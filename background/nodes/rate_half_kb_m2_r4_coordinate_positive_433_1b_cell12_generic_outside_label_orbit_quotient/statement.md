# Statement

The generic cell-12 outside ledger has `7*15=105` labels.  Let `P` exchange
the two identical positive `DE` records and let `Q` be the universal `D/E`
outside transport, which exchanges `DF` with `sigma_o EF` after the standard
gauge.  Both preserve cell `12`, all source and target equations, target
lanes, and guards.

The group generated by `P,Q` partitions the 105 labels into 36 orbits:

```text
3 orbits of size 1,
15 orbits of size 2,
18 orbits of size 4.
```

Consequently one generic exclusion per orbit suffices for all 105 labels.
No representative is excluded by this node.
