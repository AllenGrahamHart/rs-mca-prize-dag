# Upstream crosswalk

- **upstream object:** draft PR #1102, `M31 depth-32 shell counterexample`
- **relation:** `IDENTICAL` at the pinned support-level profile
- **independence:** the local proof reconstructs the labels and all support
  checks directly; it does not import the PR's recorded enumeration values.
- **scope fence:** no received-word, first-match, `U_Q`, row, or Prize claim
  is identified.
