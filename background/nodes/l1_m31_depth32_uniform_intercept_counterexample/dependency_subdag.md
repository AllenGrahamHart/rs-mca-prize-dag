# Dependency sub-DAG

```text
explicit M31 quotient labels + T16/T64 class arithmetic
       |
       v
l1_m31_depth32_uniform_intercept_counterexample [PROVED]
       |
       +--ev--> l1_mixed_petal_amplification [TARGET]
```

This is a route fence only. It removes one false uniform shell cap and is not
a premise of a replacement upper-bound route.
