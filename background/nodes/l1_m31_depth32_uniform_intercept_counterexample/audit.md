# Audit

1. The object is the auxiliary Mersenne-31 `2^-100` list profile, not either
   Grand Challenge row at `2^-128`.
2. The primary replay reconstructs all quotient labels, the class partitions,
   all twelve mixed supports, and the whole-class family argument.
3. The independent replay uses sequential odd powers and a truncated
   elementary-symmetric recurrence rather than per-label exponentiation and
   full locator multiplication.
4. The `1225` whole-class supports are proved by constant-translate locator
   factors; their individual prefixes are not accepted as recorded data.
5. The twelve mixed supports are checked individually. Their separation from
   the bulk family is certified by partial `T_64` occupancy.
6. The compiler arithmetic is calibration only. The node banks no upper
   payment and changes no critical status.

Verdict: the counterexample is valid at its stated support-level scope.
