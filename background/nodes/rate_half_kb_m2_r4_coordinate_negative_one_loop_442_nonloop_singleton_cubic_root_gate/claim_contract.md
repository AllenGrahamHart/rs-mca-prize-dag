# Claim contract

| field | content |
|---|---|
| claim | Every guarded common solution in cells `[9,10,12,13]` lies on the explicit sign-dependent cubic `(KB41C-3)`, and `c` is given by `(KB41C-4)`. |
| assumptions | The negative one-loop 442 common product equations and q welds in odd deployed characteristic. |
| exact interface | One regular product solve followed by the four-row q factor table `(2)`. |
| nonclaim | Classification or deletion of the cubic roots, the remaining common equations, outside realization, coordinate or row closure, or either Prize theorem. |
