# Upstream crosswalk - coarse p-free entropy reserve

Przemek's `(Q)` programme distinguishes ambient entropy from max-fiber
flatness. At the official finite rows, a p-free formulation reaching the
characteristic may forget up to 23 elementary coordinates. The present
theorem shows that this loss is already prepaid by at least 3,175 additional
prefix depths: the coarse ambient average is below `2^-28276`.

An upstream theorem may therefore target ambient max-to-average inflation
`K_d<=q 2^28148` on checkpoint depths, rather than pay `q^r` conditioned
fibers by a union bound. This packet does not identify that theorem with the
special F2 Myerson summit or with an image-normalized `(Q)` constant.

There is nevertheless an exact constants bridge. If a theorem on the L1
coarse fiber supplied an exact structured owner and an extras estimate
`max_s Exc_d(s)<=2^(15(d-r))mu_free(d)`, the official generated-field
arithmetic would force every extras count below one, hence to zero. This
matches the F2 campaign's printed 15-bit per-condition endpoint numerically.
A portable proof must still replace F2's zero target and Frobenius-sector
owner by a uniform arbitrary-target subtraction with the received-word/Pade
first owner used here. No requirement edge from the open F2 summit is
asserted.
