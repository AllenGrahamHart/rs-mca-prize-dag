# Result

Forgetting all Frobenius checkpoints does not create an entropy barrier on
official checkpoint depths. The first such depth is at least 3,175 layers
past the canonical reserve, and its coarse p-free ambient average is below
`2^-28276`. The finite target therefore permits max-to-average inflation up
to `q 2^28148` on this branch. The remaining purchase is an actual coarse
max-fiber or owner-sensitive Pade intersection theorem, not a checkpoint
census. Using the generated-field degree and canonical depth more sharply,
the F2 campaign's `2^15` per-condition endpoint would leave an exactly owner-
pruned extras residual below `2^-3393`, forcing it to be empty; `2^16` is not
certified by the same worst-case inequalities. Structured fibers remain in
their paid owners. Hence constants are compatible and the live F2-to-L1 gap
is the arbitrary-target map, exact structured subtraction, and first-owner
transport.
