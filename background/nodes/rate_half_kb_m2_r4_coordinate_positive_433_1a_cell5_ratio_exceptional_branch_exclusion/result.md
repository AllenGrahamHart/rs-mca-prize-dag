# Result

PROVED.  The denominator branch of the deployed cell-5 ratio chart is empty.
Every admissible common survivor in this chart has `a1!=0`, hence

```text
b=-a0/a1
```

and is governed by the two generic equations in `(x,r,t)`.  Outside
elimination remains open.
