# Lineage

The ratio-chart parent retained `a1=0` because its rational reconstruction
could not divide there.  A single exact deployed-field saturation of the
complete branch returned `{1}`.  This node records that deletion separately
so the parent's branch-complete algebra remains auditable.
