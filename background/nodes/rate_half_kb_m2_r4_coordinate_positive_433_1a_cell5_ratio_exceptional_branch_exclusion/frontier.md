# Frontier

Do not carry `a0=a1=0` in subsequent cell-5 computations.  Work only in the
generic ratio function field:

1. reconstruct `b=-a0/a1`;
2. reduce the four universal outside product equations modulo the two common
   eliminants;
3. only then append the seven squared-sum equations;
4. replay unsquared `q` signs before claiming a cell deletion.
