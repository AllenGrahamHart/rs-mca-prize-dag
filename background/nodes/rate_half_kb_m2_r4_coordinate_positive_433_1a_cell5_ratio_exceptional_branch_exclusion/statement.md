# KoalaBear positive 433-1a cell-5 ratio exceptional-branch exclusion

- **status:** PROVED
- **scope:** deployed-field cell `5`, signs `(-1,-1)`, common chart `C1`,
  exceptional branch of the ratio compiler
- **dependency:**
  `rate_half_kb_m2_r4_coordinate_positive_433_1a_cell5_ratio_chart_compiler`
- **consumer:** `rate_half_band_closure`

In the notation of the ratio-chart parent, the branch on which generic
reconstruction cannot divide by `a1` is

```text
a0=a1=L1=L2=0.                                  (KBREB-1)
```

After translating `c=bx`, retain all 22 atomic guards:

```text
r,t;  t+-1,r+-1,r+-iota,t+-r,t+-iota*r,t+-iota;
b,x;  b+-1,bx+-1,x+-1.                          (KBREB-2)
```

Over `F_2130706433`, adjoin `z` and the inverse equation
`z*product(KBREB-2)-1=0`.  The saturated ideal generated by `(KBREB-1)`
has reduced standard basis `{1}`.  Therefore the exceptional branch has no
point even over the algebraic closure of the deployed field.

Consequently every admissible common survivor in this fixed cell/sign/chart
lies on the generic ratio presentation `b=-a0/a1` and its two equations in
`(x,r,t)`.  No outside equation or unsquared `q` sign is imposed, so cell `5`,
`433-1a -> O0b`, K3, and both Prize results remain open.

## Falsifier

An admissible deployed-field solution of `(KBREB-1)`, a missing translated
guard, or an exact replay whose saturated basis is not `{1}`.
