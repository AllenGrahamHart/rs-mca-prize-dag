# Audit

1. The saturation contains all translated guards, including `bx+-1` from
   `c+-1` and `x+-1` from `b+-c`.
2. The unit basis is over the deployed field and therefore excludes points
   over its algebraic closure; it is stronger than a base-field point scan.
3. Equation shapes are `14,14,40,40` terms before saturation; no equation is
   omitted from the exceptional ideal.
4. Empty exceptional reconstruction does not make the generic common curve
   empty and does not test an outside record.
