# Result

`PROVED`. Two near-rational slopes force a common codeword-pair
explanation on at least `n-2w` coordinates. This is a structural router,
not a support-wise MCA payment, and changes no critical status.
