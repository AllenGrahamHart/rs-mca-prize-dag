# lean_tier1

- **status:** PROVED
- **closure:** Lean build certificate

The vendored stdlib-only Lean package kernel-checks the Tier-1 finite-row
arithmetic used by the partial dossier: the two positive addition witnesses
for `6*2^128 < 17^32 < 7*2^128`, the floor `B*=6`, tangent-cap inclusion,
the `506/507` staircase values, and the normalized endpoint conversion
`5/512 < 6/512 = 3/256`.

This node does not promote the package's explicitly typed analytic targets.

