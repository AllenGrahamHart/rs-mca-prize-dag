# Lineage

This turns the degree-three/degree-twelve common parameter gate into the
same finite forced-mate interface used by the earlier complete two-loop
product classifiers, without expanding any outside matching yet.
