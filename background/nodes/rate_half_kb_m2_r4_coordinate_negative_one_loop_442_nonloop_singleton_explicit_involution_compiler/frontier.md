# Frontier

For each of `S0,S1,S2`, choose which outside product equals `m`, enumerate
the fifteen pairings of the remaining six values, and reduce the three
`Phi` equations in the finite common quotient before imposing outside sums.
