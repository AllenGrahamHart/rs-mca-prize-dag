# Audit

- Direct literal numerator proofs exist for `F05-R02`, `F07-R02`, and
  `F06-R20`; `F05-R02` independently checks the transported `F04` result.
- Every transported case uses the complete-system inversion node. Neither
  q-slice fingerprints nor unproved generic orbit covariance is used.
- This node composes proved dependencies only and introduces no new
  computation.
