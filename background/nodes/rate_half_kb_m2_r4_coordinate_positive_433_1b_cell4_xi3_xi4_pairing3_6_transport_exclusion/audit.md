# Audit

The two transports act on different ledger coordinates but are not assumed
to pay multiplicity beyond their exact images. The accounting explicitly
enumerates the four labels `(3,3),(3,6),(4,3),(4,6)` and multiplies each by
the unchanged `4*4` source-sign/target-lane census.

The verifier reruns the universal outside-role certificate and the complete
15-matching exchange calculation. It also requires all three parent nodes to
be PROVED and checks the three requirement edges in the compiled DAG.
