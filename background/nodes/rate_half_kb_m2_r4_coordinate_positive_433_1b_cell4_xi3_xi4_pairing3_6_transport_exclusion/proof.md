# Proof

The direct parent proves `(xi,pairing)=(3,3)` empty.

For every missing role `xi in {2,3,4,5,6}`, the proved exchange of the two
identical positive `DE` records fixes `xi`, all source signs and target
lanes, role cell 4, and target guards. Its canonical matching action contains
the orbit `{3,6}`. It therefore transports `(3,3)` to `(3,6)`.

The proved outside-coordinate involution transports `xi=3` to `xi=4` while
fixing role cell 4, every source sign and target lane, target guards, and the
canonical matching index. Applying it at matching 3 gives `(4,3)`; applying
it to the already transported matching-6 slice gives `(4,6)`.

Both maps are exact system bijections. They preserve emptiness and do not
change raw-case multiplicity. Each labeled slice contains `4*4=16` raw
cases, so the four-label block contains 64. QED.
