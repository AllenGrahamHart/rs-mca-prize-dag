# Frontier

The local shifted-inversion frontier is now

```text
tau!=0,       lambda=kappa/tau^2 notin {0,1},
I(tau,kappa)>=8740.
```

Blind additional sampling has low value after the 6,144-parameter probe.
The next useful step is a pointwise dense-cyclotomy theorem for this open
parameter locus or a heavy-ruling owner argument that prices the two global
quotient classes without classifying every shifted pencil.
