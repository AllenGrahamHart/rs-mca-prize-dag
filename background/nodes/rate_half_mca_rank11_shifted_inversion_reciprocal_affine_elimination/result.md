# Result

The reciprocal degeneration of the shifted quadratic frontier is empty. A
4,370-fiber shifted survivor must have
`lambda=kappa/tau^2` outside `{0,1}`: zero was excluded by nonsingularity,
and one is the affine-reflection chart excluded here. Antipodal,
constant-product, general shifted, nonquadratic, and high-complexity outputs
remain unpaid.
