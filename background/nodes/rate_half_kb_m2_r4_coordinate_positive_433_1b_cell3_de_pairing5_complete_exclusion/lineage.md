# Lineage

- The pairing-4 FLINT function-field backend supplied the exact
  six-dimensional arithmetic and exceptional-root replay.
- Matching 5 keeps the `u=df` cut but replaces `(second_de,bf)` with
  `(second_de,sigma_c cf)` and moves `(sigma_o ef,bf)` to the final check.
- The apparent `b,c` exchange sends role cell 3 to cell 6 and therefore
  does not close the cell-3 matching.
- The degree-eight `f` eliminant remains practical: all final source rows
  complete below 180 seconds.
- Norm work is shared across the two `sigma_o` lanes at fixed `sigma_c`.
- Exact parallel-edge transport adds the second positive `DE` deletion.
