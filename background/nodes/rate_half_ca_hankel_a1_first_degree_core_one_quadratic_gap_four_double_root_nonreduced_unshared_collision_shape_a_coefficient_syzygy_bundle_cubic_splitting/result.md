# Result

`PROVED`. The Shape-A parameter coefficient linear series has no minimal
syzygy of degree four or higher. Its complete syzygy bundle is specified by
three counts `(c_1,c_2,c_3)` satisfying `(CSB4)--(CSB6)`.

At the earlier one-third rank boundary only two conditional profiles occur;
the locator-interpolation theorem subsequently excludes that boundary. This
remains a classification router for the live upper-half rank interval.
