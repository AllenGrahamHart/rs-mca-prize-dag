# Result - M31 source-head infinity-owner route cut

The source head is a genuine projective coordinate, but the existing deletion
recurrence pays only one fixed label at that coordinate. It cannot aggregate
all head colors, and the current per-head/per-core caps alone have no finite
sum. The next theorem must couple infinity to finite source data or provide a
new cross-head/core first owner.
