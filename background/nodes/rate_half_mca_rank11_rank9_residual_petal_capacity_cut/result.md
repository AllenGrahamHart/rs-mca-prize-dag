# Result

**PROVED.** Exact residual petal-pair capacity eliminates the fixed
rank-nine target on `15635<=K'<=20617`. The gap at the first closed row is
`3381772318665`; the cap still wins at `15634` by `1881744358235`.

Combined with the repaired high-row weighted theorem, rank nine now remains
only on `10<=K'<=15634`.
