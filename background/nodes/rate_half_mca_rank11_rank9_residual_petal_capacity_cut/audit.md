# Audit

1. `J subset Z(u)` gives the residual bound `j<=K'-1` without lifting.
2. A full-rank extension must use at least one coordinate outside `Z(u)`.
3. Coordinates outside `Z(u)` belong to at most one owner petal.
4. The exact pair count is `s(j-9)+C(s,2)`; replacing it by `s(m'-10)`
   loses the improvement.
5. The owner multiplicity `981105` and petal disjointness are used in the
   same `(record,T)` unit as the weighted demand.
6. The common-core envelope is increasing in `j` throughout the claimed
   interval, so `j=K'-1` is the correct worst case.
7. The adjacent row `15634` is an honest method failure.
8. No original-row pair-core theorem appears in the proof.

No Modal computation is used.
