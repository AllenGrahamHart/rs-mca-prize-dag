# Claim contract

## Input

The fixed rank-nine typed chart, its marked component-extension demand, the
rank-nine owner-plane decomposition, fixed-owner record cap, residual RS
root bound, and support-wise pair noncontainment.

## Output

The rank-nine alternative is empty on `15635<=K'<=20617`. Together with
the existing weighted high-row cut, it is empty for every `K'>=15635`.

## Load-bearing scope

The common core `J` and support size `m'` are both residual objects. No
original-row intersection floor is transported through reverse shortening.

## Nonclaim

Rows `10<=K'<=15634`, rank eight, chronology, rank eleven, and both prize
problems remain open.
