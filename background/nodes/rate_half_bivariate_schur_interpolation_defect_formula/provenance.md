# Provenance

This node expands the Schur complement proved in
`rate_half_bivariate_top_vandermonde_schur_reduction`. Its motivation is the
contrast between the exact `m=1` rank defect and the bounded `m=2`
bad-pattern campaign.

The proof is symbolic Lagrange interpolation and elementary symmetric
polynomials. No numerical search is used to establish the formulas.
