# Result

`PROVED`: the new maximum-weight prize profile is reduced from an unrestricted
support-9 search to twelve exact cofactor classes with finite even-variance
windows. Exact children now exclude `m=1538`, `m=1024`, `m=1028`, `m=512`,
and `m=514` completely. Seven cofactors remain. The capped product child
contracts every residual endpoint. The energy-adaptive child contracts
`m=256` further to `V<=46`; this is now the shortest live window.

The profile is not yet excluded. The next attack should consume the proved
capped windows, starting with `m=256` through `E=23` and its multiplicity-eight
structure.
