# Result

`PROVED`: the new maximum-weight prize profile is reduced from an unrestricted
support-9 search to twelve exact cofactor classes with finite even-variance
windows. Exact children now exclude `m=1538`, `m=1024`, `m=1028`, and
`m=512` completely. Eight cofactors remain. The capped product child contracts
every residual endpoint; `m=514`, ending at `V=34`, is now the shortest live
window.

The profile is not yet excluded. The next attack should consume the proved
capped windows, starting with `m=514` through `V=34` and its required factor
257.
