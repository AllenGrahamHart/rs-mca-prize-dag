# Result

`PROVED`: the new maximum-weight prize profile is reduced from an unrestricted
support-9 search to twelve exact cofactor classes with finite even-variance
windows. The first exact child now excludes `m=1538` completely. Eleven
cofactors remain; the `m=1024` and `m=1028` branches both end at 34.

The profile is not yet excluded. The next attack should classify the distinct
`mu=10` and `mu=2` low-variance geometries before any norm computation.
