# Audit

The trace polynomial is split into all four factors from the dependency.
Lower-order sixteenth roots are not discarded when the primitive quartic is
checked.

The primitive-factor certificate uses the exact pseudo-remainder formula
(5), with nonzero `L` in both packets. Every printed integer is reduced
modulo the official characteristic `8191`; characteristic-zero nonvanishing
alone would not suffice.

This proof supersedes the queued `CR-L1-H15-COLOR0` computation. No Modal
execution is needed for this chamber.
