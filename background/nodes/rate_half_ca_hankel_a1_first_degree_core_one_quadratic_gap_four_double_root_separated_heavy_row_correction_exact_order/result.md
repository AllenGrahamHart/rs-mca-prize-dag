# Result

For each correction root `tau`,

```text
ord_tau G(t,x_*)=2-ord_tau Lambda=ord_tau H,
T_j(tau)!=0.
```

Thus the free overlap polynomial is coprime to the correction quadratic.
The remaining separated wall concerns its roots away from `S_B` and the
global barycentric remainder constraints.
