# Statement

## Lemma (KBP1B9-TOWER-1)

The four guarded cell-9 common curves are covered by six exact charts.  On
each chart their coordinate algebra over `F_p(r)` has basis `1,t,b,tb`, with
quadratic relations for `t` and `b` and linear recovery of `c`.

No outside-label exclusion is asserted.
