# Result

The three exact nonreduced collision profiles now have concrete global
names. They are selected by whether the split-biform row derivative
`G_X(t,x_*)` is nonzero at the correction, vanishes simply in parameter,
or vanishes to parameter order at least two. Equivalently, they are the
zeroth and first derivative moments of the second divided locator row.
