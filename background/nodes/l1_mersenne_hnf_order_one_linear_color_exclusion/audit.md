# Audit

The argument uses the actual pointwise identity `E(x)=x^(p+1)`, not merely
the weaker congruence `E^m=1 mod L`. It therefore preserves the assignment
between each reduced root and its color.

The congruence `p=-1 mod m` is checked separately on all five official rows.
The root count is valid because `L` divides the separable polynomial
`W^(m(p+1))-1`, and `H=m-2` is at least six.

The result excludes degree exactly one. It does not merge the constant and
linear strata, and the constant `m=16,h=15` chamber remains open.
