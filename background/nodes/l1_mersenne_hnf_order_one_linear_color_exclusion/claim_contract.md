# Claim contract

| field | value |
|---|---|
| claim | The rootwise colored Frobenius interpolant on the reduced order-one roots cannot have degree one. |
| status | PROVED |
| dependency | `l1_mersenne_hnf_order_one_frobenius_gate` |
| consumer | `l1_mixed_petal_amplification` |
| exact rows | Four `(m,h)=(8,7)` rows and one `(m,h)=(16,15)` row with `n=m(p+1)` and `p=-1 mod m`. |
| excluded stratum | `deg E=1` on the `H=m-2` reduced roots. |
| open residue | Constant color at `m=16,h=15`, every degree at least two, actual cyclotomic divisibility, and inner lifts. |
| nonclaim | No converse to the bounded colored identities and no L1 promotion. |
