# Attack record

The one-level near-saturation theorem closed `K'=22` but left `K'=23`
above capacity.  Repeating the same span argument shows that a defect-`s`
completion family needs at most `s` further circuit labels.  The exact
union-size inequality determines, rather than guesses, how many levels
each support size permits.
