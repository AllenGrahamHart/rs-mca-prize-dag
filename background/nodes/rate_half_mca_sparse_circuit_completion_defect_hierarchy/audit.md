# Audit

1. A `q-s` completion family has dimension exactly `q-s`.
2. The added labels span only the support-`c` label space; no claim is made
   about larger supports.
3. The worst carrier uses all `s` added labels.
4. The final comparison includes one further circuit support.
5. Support five is unchanged because its depth is zero.
6. The fallback keeps a maximum over every allowed completion count.
