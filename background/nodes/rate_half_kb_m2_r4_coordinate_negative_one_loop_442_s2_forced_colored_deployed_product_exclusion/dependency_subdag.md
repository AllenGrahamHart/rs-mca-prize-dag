# Dependency sub-DAG

```text
binary-sextic forced-cell census --+
all-row common product data -------+--> S2 forced-colored exclusion
```

The exclusion supplies product-level evidence to `rate_half_band_closure`.
