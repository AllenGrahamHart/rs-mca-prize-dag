# Result

`PROVED`. The unique large coordinate-clone exception is no longer an
undifferentiated positive-dimensional component. Its rank-one denominator
branch is absorbed into one fixed rational owner except at the exact
`|C|=m` locator remainder. Only that boundary and genuine moving-denominator
rank two remain.
