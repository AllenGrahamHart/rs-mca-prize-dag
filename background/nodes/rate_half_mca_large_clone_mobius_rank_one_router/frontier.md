# Frontier

Attack the two exact residuals separately.

1. `EXACT_M_CLONE_LOCATOR_REMAINDER`: after division by the scalar slope
   factor, keep the term `mu Lambda_C/ell(gamma)` explicitly. Determine
   whether same-support noncontainment sends it to the pure-locator or
   imperfect-quotient first-match cell. Do not call it near-codeword merely
   because it has a one-pole slope parameter.
2. `MOVING_DENOMINATOR_RANK_TWO`: use the independent denominator pair as
   the two-dimensional correction object. Seek a proper-intersection or
   base-field split-pencil payment; do not collapse the two polynomial
   directions pointwise.
