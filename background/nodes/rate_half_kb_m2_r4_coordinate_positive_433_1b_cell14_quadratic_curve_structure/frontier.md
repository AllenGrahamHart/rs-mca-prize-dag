# Frontier

One exact branch remains before cell `14` can close: exclude the final
all-mixed outside ledger on the quadratic cover. The linear-pair, rank-one
target-projection, and fixed-a chain theorems remove a disjoint
`144+960+432=1536` of its 1680 raw cases. The retained 144 cases have a
missing record in `de,de,-de` and matching index `6`, `7`, or `8`. The
normalized kernel denominator is proved nonzero everywhere on the guarded
curve, so future work may use the global `(-1,1)` normalization without a
denominator-boundary branch.

Do not launch the existing dense five-variable outside prototype in bulk:
one sampled ideal timed out before its initial standard basis.  Quotient,
successive-resultant, and projective latent-root samples also reached bounded
caps; none should be scaled in their current forms. The next implementation
must keep the fixed `de` value and use the rank-one relation. The next exact
route is a double resultant from the four mixed two-target projections.
