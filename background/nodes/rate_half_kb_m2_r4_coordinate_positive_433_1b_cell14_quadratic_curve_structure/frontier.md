# Frontier

One exact branch remains before cell `14` can close: exclude the outside
ledger on the quadratic cover.  The normalized kernel denominator is now
proved nonzero everywhere on the guarded curve, so future work may use the
global `(-1,1)` normalization without a denominator-boundary branch.

Do not launch the existing dense five-variable outside prototype in bulk:
one sampled ideal timed out before its initial standard basis.  Quotient,
successive-resultant, and projective latent-root samples also reached bounded
caps; none should be scaled in their current forms.  The next implementation
must use the quadratic relation and the linear target-record structure before
forming a joint standard basis.
