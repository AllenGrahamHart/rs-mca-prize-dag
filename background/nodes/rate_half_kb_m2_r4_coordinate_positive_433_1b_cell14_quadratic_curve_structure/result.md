# Result

Cell `14` is reduced from 24 dense common-minor charts to one exact
quadratic-cover model per source-sign pair.  The rational `t` coordinate is
global on the guarded curve.  The rational `c` coordinate covers a dense
stratum whose basis-four boundary intersection becomes unit after route-guard
saturation.  Exact kernels for all four source-sign curves annihilate all ten
Vieta rows.  The guarded principal common locus therefore has one global
quadratic-cover model per source-sign pair.

This is a proved decomposition, not a cell exclusion.
