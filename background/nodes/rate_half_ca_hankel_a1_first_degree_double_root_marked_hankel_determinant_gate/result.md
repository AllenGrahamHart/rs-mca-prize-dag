# Result

The retained scalar double-root packets now have explicit Hankel-matrix
tests. Core-free row evaluations are bordered maximal-minor determinants;
the core-one quadratic double root forces a rank-one updated middle-Hankel
determinant to factor as `D_1 g_*^2` times the sixth power of a quadratic,
up to the printed base-field constant.
