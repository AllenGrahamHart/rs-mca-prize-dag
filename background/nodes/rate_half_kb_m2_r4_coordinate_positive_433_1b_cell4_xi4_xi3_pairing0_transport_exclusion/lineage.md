# Lineage

Wave 64 proved the outside-role transport uniformly but deliberately paid no
labels. The direct matching-0 reciprocal-square theorem supplies the first
new empty `xi=3` block in cell 4. This node is the first cell-4 application
of that universal transport and pays the paired `xi=4` label.

Future matching payments should retain this two-node pattern: prove `xi=3`
directly, then record the `xi=4` corollary with no duplicate computation.
