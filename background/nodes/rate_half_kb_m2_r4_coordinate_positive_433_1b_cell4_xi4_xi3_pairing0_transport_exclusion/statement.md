# Statement

## Claim (KBP1B4-XI4P0-TR-1)

On the guarded positive deployed 433-1b role-cell-4 branch, every atlas case
with missing record `xi=4` (the `sigma_o ef` record), canonical matching
index `0`, any source sign `epsilon in {+-1}^2`, and any target lane
`sigma in {+-1}^2` is empty.

The exact universal outside-role transport maps these systems bijectively to
the PROVED cell-4 `xi=3`, pairing-0 systems. Thus this node pays
`4 source signs * 4 target lanes = 16` raw cases.

No other matching, missing role, complete cell-4 exclusion, rate-half band,
LIST, MCA, or Prize result is claimed.
