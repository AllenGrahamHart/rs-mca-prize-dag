# Frontier

Cell 4 retains `xi in {3,4}` at matching indices 1 through 14. By the same
universal transport, only the 14 `xi=3` labels require independent proof;
each one will pay its `xi=4` partner automatically.

The next structural task is a certified matching-orbit classification for
the residual six-row list. This determines whether the 14 labels reduce to
the eight remaining matching-exchange orbits already identified by the
campaign accounting.
