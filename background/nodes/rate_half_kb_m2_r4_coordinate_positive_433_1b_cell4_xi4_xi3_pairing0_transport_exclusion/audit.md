# Audit

This is a logical corollary, not a second experimental census. The verifier
reexecutes the pinned universal transport certificate and checks that both
parents and both required DAG edges are PROVED.

Scope alignment is exact: the transport theorem is universal over role
cells and matching indices, while the exclusion parent fixes role cell 4
and matching 0. The corollary narrows the universal theorem to precisely
that direct parent's scope before changing only the missing role.

The 16-case payment counts source signs and target lanes once. It does not
multiply by the three overlapping rational branches used internally by the
direct parent.
