# Claim contract

| field | value |
|---|---|
| claim | On the fully proportional coefficient-zero locus, `M_1=M_0=0` reconstruct `D,Q_0` from `(b,q)`, while `C_1=C_0=0` become two explicit affine equations in `G_2`; the generic and singly exceptional charts reconstruct `G_2,H,Y`. |
| status | PROVED |
| dependency | `l1_mersenne_hnf_m8_order_one_cubic_three_two_one_generic_fully_proportional_bivariate_factorization` |
| consumer | `l1_mixed_petal_amplification` |
| equivalence guards | Inherited `b*q*D*R!=0`, `A=-(b+3)/2!=0`, and the printed `E_G` or `J_G` chart guards. |
| open residue | Unit or retained-point verdict for the bivariate generic compatibility system, the `E_G=0` charts, the role-discriminant weld, `P_4`, and every arithmetic-lift filter. |
| nonclaim | No coefficient chart is asserted empty, and no ambient-field square-class conclusion is used. |
