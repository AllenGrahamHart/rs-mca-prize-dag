# Result

The four parameter-only coefficient equations reduce to rational
reconstruction from `(b,q)` plus one bivariate compatibility equation on
the generic chart. The only chart retaining `G_2` is explicitly isolated
by `E_G=J_G=L_G=0` and `Q_0=q^2/3`. No chart is excluded.
