# Audit

The reconstruction of `D` uses `b!=0`; `D_*!=0` follows from the inherited
`D!=0`. The reconstruction of `Q_0` therefore loses no component.

The identity `H+G_2=ell` is derived from the original definitions, not
added as an independent assumption. The division by `A` is valid because
the inherited nonzero role numerator `R=A(3Y^2+2xY+G_2)` forces `A!=0`.

The `E_G=0` chart is retained. It forces `Q_0=q^2/3` and then splits on
`J_G`; the doubly exceptional `J_G=L_G=0` endpoint is not discarded.

All formulas remain coupled to `F_b`, the structural identities, role and
`P_4` equations, and arithmetic-lift filters. The router proves no
emptiness or prize-row payment.
