# Statement

## Claim `(KBP1B4-XI3-P7-QRSF-1)`

On the guarded positive deployed `433-1b -> O0a` role-cell-4 branch, every
case with missing `df` record `xi=3` and canonical matching index 7 is empty
for all four source-sign pairs and all four target lanes.

After deleting `xi=3`, the residual list and matching are

```text
de, de, -de, sigma_o ef, bf, sigma_c cf,
(de,sigma_o ef), (de,bf), (-de,sigma_c cf).
```

The claim pays 16 raw atlas cases and one labeled slice. It claims no other
matching, missing role, role cell, complete positive branch, or Prize result.
