# Frontier

Composing this theorem with the proved parallel-`DE` matching exchange pays
`xi=3`, pairing 10. Composing both with the universal `xi=4 <-> xi=3`
outside-role transport pays their two `xi=4` partners.

After that separate composition, cell 4 has two matching-exchange pairs
left: `{8,13}` and `{11,14}`.
