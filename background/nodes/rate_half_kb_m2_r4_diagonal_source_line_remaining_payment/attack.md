# Attack

1. Mint exact workboards for `(1,0,4)`, `(0,1,4)`, and the near-aligned
   `(0,0,6)` row.
2. Keep the exceptional unsaturated `(1,1,2)` orbit separate from the
   saturated c112 packet.
3. Reuse reciprocal source-line normal forms only after checking every row's
   ramification and guard hypotheses.
4. Close each packet algebraically where possible; otherwise attach an exact
   slope compiler and bridge multiplicity.
