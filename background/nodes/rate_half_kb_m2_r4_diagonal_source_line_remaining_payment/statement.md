# Diagonal source-line remaining payment

The exact source-line mixing census has five rows. The `(2,0,2)` row and the
saturated `(1,1,2)` workboard are proved empty. Close or pay the remaining
active images:

```text
(1,0,4)
(0,1,4)
(0,0,6) near-aligned survivor
(1,1,2) exceptional unsaturated orbit KBDM-10
```

Print their disjoint exact total `U_source_line` in distinct affine slopes,
including bridge fibers and active first-match chronology.

## Falsifier

An omitted mixing row or exceptional orbit, a false emptiness claim, or a
nonzero payment with incorrect owner or multiplicity.
