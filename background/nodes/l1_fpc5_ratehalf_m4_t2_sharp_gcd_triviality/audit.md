# Audit

1. The two easy members only show that a common divisor divides `L_0`; the
   interpolation member is required to remove the roots of `L_0`.
2. Interpolation uses `ell-3` values with degree at most `ell-4`, inside the
   allowed degree `ell-3` cofactor space.
3. Pairwise disjointness makes `L_1` and `L_2` units at every background
   root.
4. Nonzero `c_1` is needed in the displayed evaluation; distinctness makes
   the locator formula well-defined.
5. Flat-wide `gcd(V_F)=1` is distinct from candidate-wise
   `gcd(F,W_F)=1`.
