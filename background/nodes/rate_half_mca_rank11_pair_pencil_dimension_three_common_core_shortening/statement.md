# Dimension-three common-core shortening

- **status:** PROVED
- **scope:** the scalar-dimension-three quotient pair-pencil branch

Every affine plane in the scalar polynomial space contains at most

```text
floor(15*(n-(K-1))/((m-2)-(K-1)))=233.              (D3-1)
```

selected quotient types. Therefore every coordinate outside the scalar-space
common gcd lies in at most 233 pair cores. If `J` is the common received-pair
core of all 520 types, then

```text
520*(m-2)<=520|J|+233*(n-|J|),
|J|>=319539.                                         (D3-2)
```

Subtracting the common pair and dividing punctured received values and
explanations by the locator of `J` gives the same reversible shortening as
in dimension two. At the minimum core floor,

```text
n'=1777613,       K'=729037,       m'=796509,
pair-core size s'=796507,
233n'-520s'=189.                                    (D3-3)
```

Thus the shortened residual core-incidence design is within 189 slots of
full 233-fold occupancy.

## Falsifier

An affine scalar plane with 234 selected types; a dimension-three common core
below 319539; failure of reversible shortening; or incorrect residual
parameters or incidence slack.
