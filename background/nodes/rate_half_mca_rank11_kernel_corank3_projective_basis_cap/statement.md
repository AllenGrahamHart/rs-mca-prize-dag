# Statement

In the complete shortened corank-three kernel chart

```text
(n,K,m,s)=(1048579,3,67475,3),
```

the `m` normals incident with each selected record are distinct projective
points in `PG(3)`, no three are collinear, and they span the ambient
projective space.  Every record consequently owns at least

```text
4(m-1)(m-2)(m-3)=1228711865141376
```

ordered linearly independent coordinate quadruples.  Since an independent
quadruple determines at most one parameter point, the record count is at
most

```text
floor((n)_fall_4/(4(m-1)(m-2)(m-3)))=983902549.
```

This improves the generic support-local transversality cap `3935435218` by
`2951532669` records.
