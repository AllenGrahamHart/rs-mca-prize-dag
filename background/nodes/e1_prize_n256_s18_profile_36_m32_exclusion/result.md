# Result

`PROVED`: cofactor `32` is impossible in the prize profile `(3,6,S=18)`.
Four profile cofactors remain:

```text
2, 4, 8, 16.
```

The next branch is `m=16`, with multiplicity four and a possible
all-one-parity support branch that must be handled explicitly.
