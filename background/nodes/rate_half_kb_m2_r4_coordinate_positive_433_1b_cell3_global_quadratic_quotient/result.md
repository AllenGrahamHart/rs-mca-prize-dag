# Result

`PROVED`: the guarded positive deployed `433-1b -> O0a` cell-3 common curve
has an exact chart-global presentation by one `(t,r)` base equation, one
palindromic quadratic equation for `b`, and one linear recovery equation for
`c`.  This replaces the dense polynomial-kernel outside interface by a
two-coordinate quadratic pair algebra; the outside ledger remains open.
