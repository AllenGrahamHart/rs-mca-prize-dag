# Lineage

- `(KBP1B3-CURVE-1)` proved the compact three-determinant common curve, its
  route-open kernel normalization, and its irreducible `(t,r)` projection.
- A block-lex experiment asked whether the remaining coordinates could be
  recovered over that projection.
- The full 24-chart census found the same three selected basis relations in
  every cofactor chart for each sign row.
- Boundary saturation and 240 exact reductions promote that observation to
  the localized ideal equality `(KBP1B3-QUOT-1)`.
- The child changes the computational interface, not the cell-3 truth status:
  every outside obligation remains live.
