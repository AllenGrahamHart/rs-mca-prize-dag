# Claim contract

- **claim:** minimum support makes the constant-to-leading ratio of
  `J_supp` equal to the explicit scalar `Xi`, and `Xi` has order dividing
  `N/2`.
- **scope:** complete canonical candidates in the official one-antipodal
  `c=2` chamber attaining barycentric support `3H+1`.
- **dependencies:** the split-divisor support compiler and the
  minimum-support degree conclusion `deg C=H-3`.
- **falsifier:** a covered minimum-support packet violating `(ETG2)--(ETG4)`.
- **nonclaims:** the scalar torsion gate is not proved empty, no official
  coefficient is evaluated, and no larger-support case is classified.
