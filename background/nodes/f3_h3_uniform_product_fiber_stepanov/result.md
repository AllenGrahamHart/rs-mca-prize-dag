# Result

The individual shifted-product fiber has the uniform official-corridor bound

```text
P(tau)<33 n^(2/3),             tau!=0,1.
```

The subgroup-preserving change `(x,y)->(U,V)=(x,x/y)` turns the fiber into an
irreducible bidegree-`(2,1)` curve with a two-term lowest homogeneous part,
making the constants-explicit Konyagin--Shparlinski--Vyugin construction
applicable.

This is actual subgroup-specific control, unlike an abstract profile fence,
but it remains marginal. It does not control simultaneous product and
quotient richness, so the critical C36 leaf remains open. No Modal resource
is used.

The theorem now has one exact auxiliary use: it reduces the quotient
separator exponent in `f3_h3_double_accident_derivative_ideal`. The verifier
allows precisely that required consumer while continuing to reject any use
as a direct critical premise.
