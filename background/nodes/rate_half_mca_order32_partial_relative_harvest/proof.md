# Proof

This node imports the exact theorem and proof from the immutable upstream
source identified in `source_pin.json`.

The slope-degree interval is upstream Corollary `cor:degree-18`. The
two-cover-complexity floor is the contrapositive of the full scalar-locator
profile extraction theorem `cor:full-profile`. The global alternative is the
composition of order-32 coherence with the exact 31-overlap near-sunflower
corollary. These are precisely the three citations in the upstream proof of
`thm:partial-relative`.

At the two rows, direct substitution gives

```text
3*1116048-1048576+3 = 2299571,
3*1116024-1048576+3 = 2299499.
```

The independent verifier recomputes these constants and the printed
near-sunflower floors. No conditional statement from the upstream completion
section is imported as proved. QED.
