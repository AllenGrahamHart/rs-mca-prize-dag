# Audit

The theorem classifies one selected 32-tuple. It does not provide exhaustive
same-owner routing of every exceptional local classification. In particular,
the coherence conclusion is conditional on every residual 32-subset already
being affine or rational; otherwise the primitive spread branch remains.

No wording from the later conditional adjacent-row theorem is promoted.
