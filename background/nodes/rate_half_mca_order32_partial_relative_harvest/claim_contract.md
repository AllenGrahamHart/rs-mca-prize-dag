# Claim contract

## Proved

The exact scope and alternatives of upstream `thm:partial-relative`, at the
immutable commit and blob in `source_pin.json`.

## Not proved

- complete spread-component routing `(S)`;
- the exclusive large-owner image bound `(A)`;
- complete exception routing `(E)`;
- any MCA safe row or adjacent threshold.
