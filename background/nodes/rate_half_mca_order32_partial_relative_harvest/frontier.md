# Frontier

Use a source-bound one-per-slope support selector to feed this theorem, then
attack the three exact upstream terminals: `(S)` spread-component routing,
`(A)` large-owner image control, and `(E)` exception routing.
