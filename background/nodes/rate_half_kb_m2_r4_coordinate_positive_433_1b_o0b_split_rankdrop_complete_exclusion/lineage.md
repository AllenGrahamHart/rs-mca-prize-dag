# Lineage

This reuses the exact sixteen-point split-common rank-drop classifier from
the closed `O0a` campaign but replaces its outside graph with all six `O0b`
split lanes.  It is a branch closure, not a transport of the `O0a` outside
conclusion.
