# Frontier

The cell-0 route is closed; do not append `BE` or revisit its 267 formal
cases. Among the six remaining representatives, cells `3` and `9` have
the smallest common basis size (23). Run one lex/branch reduction for cell
`3`; if its kernel normalizes comparably, test the same necessary
`DE+/DE-` pair before any colored edge.
