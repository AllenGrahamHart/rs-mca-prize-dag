# Audit

1. Both roots of `b^2-6b+1` are computed.
2. On each branch the numerator and denominator of `C_b(t)` have gcd one;
   no denominator-zero component is omitted.
3. The cancelled common scale is nonzero under the original common guards.
4. `A_2(z_j^2)` is saturated only because the proved complete-packet
   interface supplies leading support at every source label.
5. `DE+` and `DE-` must use distinct labels; otherwise nonzero `de=-de`.
6. Target-root nonzero/distinctness constraints are omitted from the ideal,
   so the unit calculation is stronger than necessary.
7. The pair equations are necessary but need not be sufficient; unit
   necessity is enough for exclusion.
8. Both branch ideals and projected elimination ideals are `<1>`.
9. The generic four-equation timeout is not used.
10. Six other common orbits remain open.
