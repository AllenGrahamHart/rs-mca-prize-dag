# Proof

Repeat the forced-`EF` construction from the `tau_1=+1` node with
`delta=+1`.  The forced equation and denominator clearing are unchanged;
only the signed `DF` factor changes, giving `(KB41EM-1)`.

Sparse quotient multiplication gives three 17-term equations for each
forced `EF` sign.  Projecting to both cubic common fields and replaying the
six quotient relations, exact grevlex Buchberger reduction completes after
435 S-pairs in every case.  Each basis contains the one-term monic
polynomial `e`.  Saturation by the required nonzero representative `e`
therefore gives the unit ideal.  Both cells are guard-empty. QED.
