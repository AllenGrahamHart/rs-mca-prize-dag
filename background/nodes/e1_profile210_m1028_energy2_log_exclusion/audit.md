# Audit

- Integrality gives `sum |A_d|<=E` and hence the two-sided deviation bound
  `|x_u|<=4` at energy two.
- The derivative of the logarithm minorant has only `-11/4` and `0` as
  critical points on the audited interval.
- The endpoint logarithm uses a positive atanh series and an explicit
  geometric tail; its rational margin is `1/23058`.
- The final field-ceiling comparison uses `exp(-z)>1-z` and exact integer
  cross-multiplication.
- Energies three and four remain outside this claim.
