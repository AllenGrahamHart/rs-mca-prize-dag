# Claim contract

- The action is restricted to `S0`; no `d`-sign quotient is claimed in the
  repeated `SDE` or `SDF` lanes.
- The common rows and source normalization are fixed by `d->-d`.
- The two opposite-signed `BC` records remain distinct and are not swapped.
- The 16,020 count is a representative workload, not an exclusion count.
- No common row, outside label, retained raw row, route, K3 case, or Prize
  endpoint is closed here.
