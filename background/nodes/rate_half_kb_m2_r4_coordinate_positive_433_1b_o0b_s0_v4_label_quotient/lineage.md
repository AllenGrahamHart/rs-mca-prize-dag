# Lineage

The D-sign label router was originally scoped to repeated-BC cell `3` and
could not by itself reduce the principal split workboard. The common-system
adapter and all-cell B/C--E/F theorem expose the exact compatibility needed
to lift that router uniformly across `S0` and compose the two actions.
