# Provenance

- Local input: the proved plane-218 pure-power router.
- New contribution: equality propagation to a symmetric configuration,
  exact leave and Gram ledgers, and pointwise saturation consequences.
- Computation: integer replay only; no field search or Modal result is used.
- External context was consulted only to assess attack routes and is not a
  load-bearing lemma.
- Upstream alignment: `(Q)` quotient-periodic classification/payment and
  the exact base-field-normalized split-pencil ledger.
