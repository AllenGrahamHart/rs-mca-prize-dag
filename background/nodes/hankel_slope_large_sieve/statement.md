# hankel_slope_large_sieve
- **status:** TARGET
## Statement
Supported-slope estimate (Pro J): after tangent/Luroth/extension pricing,
#Sigma_unp(u,v) = #{row-full generic slopes Z with a nonempty unpaid fiber}
<= n^{C_Sigma} max(1, C(n,j) q^{-(t-1)}). Locator-side equivalent: the number
of unpaid supports T with H_u ell_T proportional to H_v ell_T obeys the same
bound. HONESTY: this is R2's factorized form (R2 <=> sieve, up to the now-
PROVED Conjecture-F fiber factor) — a factorization, not a difficulty
reduction; its content is the XR exchange/Johnson-expansion wall. CLOSED
subcases (unconditional): vertical rank-drop slopes (poly many, principal
kernels); deficiency D=1 (univariate eliminant); fixed D = O(1) (Bezout
tower, n^{O(D)}). The in-band growing-D row-full case is the wall. Do NOT
cite the scroll degree as bounding slopes (components != points).
