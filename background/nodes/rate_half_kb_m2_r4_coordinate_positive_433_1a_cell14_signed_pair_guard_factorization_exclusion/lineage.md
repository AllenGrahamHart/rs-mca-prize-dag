# Lineage

This node follows the global common-curve, product-rank, paired-product, and
root-sign symmetry theorems.  A 106-fiber scout first identified the stable
guard product.  The proof does not consume that scout: it derives the
compact kernel exactly, proves the global quotient identity, factors the
complete leading norm, and replays every compact-chart scale.
