# Upstream crosswalk - band complement-dimension packing

## BC profile effect

Paper D's balanced split-pencil coefficient dimension is
`omega-w+1`.  For exact L1 shells its projectivization has dimension
`s=omega-w`, and complete-support uniqueness gives the uniform cap `(CP2)`
before any base-field BC estimate.

## Paid range

This theorem removes the fixed- and reserve-sublinear-dimensional transition
strip from the active BC census.  It is compatible with the strict interior
convention `d_1>=w+2` but does not depend on the individual `d_1` profile.

## Remaining input

The deployed frontier has linear `s`; there the exact cap is exponential and
an owner-pruned `BC_exact_guarded` theorem remains necessary.  Paper D's raw
base-field support floor routes separately to higher-shell Q.
