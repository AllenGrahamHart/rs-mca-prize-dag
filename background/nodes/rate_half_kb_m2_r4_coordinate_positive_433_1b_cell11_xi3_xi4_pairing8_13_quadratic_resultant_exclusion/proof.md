# Proof

Delete the `DF` record and put `m=df`, `S=(d+f)^2`, `z=1/d`,
`y=z^2`, and `q=de`. The missing Vieta equation is

```text
M(y)=1+(2m-S)y+m^2y^2=0.
```

For pairing 8, the first two residual pair equations are quadratic in
`q`. Their exact Sylvester resultant is degree eight in `z`. Reduce it
modulo `M(z^2)`, split the cubic remainder into even and odd parts, and
multiply the two sign conjugates. The resulting polynomial in `y` has a
linear remainder modulo `M`; its division-free common-root cut is normed
through the exact cell-11 basis `1,t,b,bt`.

A syntax-tree adapter reuses the audited cell-4 compiler function unchanged
and supplies the cell-11 row-6 tower and compact kernel. All eight
source-sign/`sigma_c` rows terminate. The packet contains 88 target
roots, 124 complete candidates, 144 guarded source routes, 64 compatible
`(z,q)` lifts, and 128 final colored lanes. Its terminal partition has 112
checked finite points, 16 missing-impossible exits, 16 target-product
boundaries, 40 `r` guards, 16 `t` guards, 16 cell-11 leading boundaries,
and 68 no-`b` exits.

A separate python-flint reconstruction covers a shared 61-profile packet
with 302 deployed roots and degrees through 5192. The pairing-8 slice has 45
profiles and 206 roots. The direct audit
rebuilds every candidate union, source-kernel value, missing reciprocal
relation, `d/e/f` lift, and all three paired equations. Every one of the
128 final values is nonzero; there is no witness or unresolved branch.

The universal label quotient transports `(3,8)` exactly across
`{(3,8),(3,13),(4,8),(4,13)}`. Therefore all four labels are empty. QED.
