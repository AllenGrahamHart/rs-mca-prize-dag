# Dependency sub-DAG - PMA quotient-closure scope

```text
pma_exact_periodic_owner --------- req ---+
                                           |
qa22_m_le_t_extension ------------ req ---> pma_quotient_closure_scope
                                                    |
                                                    +-- req --> petal_mixed_amplification
                                                    |
                                                    +-- ev ---> pma_wide_residual
```

The first requirement supplies the already-proved exact-scale owner consumed
by folded-source subsumption. The second supplies the official `Q_2(k+2)` and
`719` profile convention. Boundary stripping is reproved in this packet from
the syndrome moments.

No requirement edge enters the red leaf. The red leaf remains the direct
quantitative `GROW union RES` theorem after the now-explicit empty-`QCLOSE`
route.
