# Result

The clean endpoint now has a complete two-axis intersection ledger. The
dual complement meets the irreducible locator curve once in the affine
surface and otherwise osculates the full parameter-infinity fibre to order
`T+b`. Equivalently, the ambient line bundle `O(N,-T)` restricts to the
single-point divisor `P_*`.

This turns the geometric endpoint into an exact cohomological rank defect,
rather than an open-ended cyclic-norm classification.
