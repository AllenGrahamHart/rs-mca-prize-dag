# Claim contract

This node closes the positive-dimensional component target at the single
residual row `K'=11`.  It depends on four proved interfaces:

1. dense-locator incidence gives at least `990810934/10^9` of all
   `(record,T)` incidences;
2. `V'<=RS_{<11}` has dimension ten, so every eleven-set has rank ten;
3. rank-nine owner charts have the residual common-core/petal decomposition;
4. the core-offset selected-support theorem caps each rank-nine chart.

The circuit representation uses the one global hyperplane functional
cutting out `V'`.  Low circuits coalesce because the union of two supports of
size at most five has size at most ten, within the Vandermonde independence
range.

No row `K'>=12`, dense-owner chronology assignment, active-v4 ledger
movement, or prize closure is claimed.
