# Attack

The exact rank-nine lower endpoint is now `K'=12`.  At that row, `V'` is a
ten-dimensional codimension-two subspace of `RS_{<12}`.  The next attack
should replace the single circuit functional by its two-dimensional quotient
space:

1. classify sparse quotient supports whose pairwise unions stay within the
   twelve-point Vandermonde independence range;
2. count how many rank-nine shadows each quotient-support rank forces;
3. extend the selected-support offset cap to `j in {9,10,11}`;
4. preserve the global quotient label before summing chart capacities.

Do not return to a chart-local distinct-record cap; the `K'=11` fence already
shows that route is insufficient.
