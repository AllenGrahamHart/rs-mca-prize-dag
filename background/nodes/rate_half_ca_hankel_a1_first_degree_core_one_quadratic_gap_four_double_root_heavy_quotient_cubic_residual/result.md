# Result

On the squarefree correction locus disjoint from `g_*`, the double-heavy
quotient satisfies `M U=D_1C` for a cubic parameter-vector `C`, with
`P_F(t,x_*)=D_1C_0` and an exact Hankel recurrence. Each of the two
correction roots belongs to one Smith chain of length two.
