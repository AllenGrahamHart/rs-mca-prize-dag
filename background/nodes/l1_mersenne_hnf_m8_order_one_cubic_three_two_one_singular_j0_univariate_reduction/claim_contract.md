# Claim contract

| field | value |
|---|---|
| claim | The determinant-singular `J=0` coefficient endpoint is exactly one degree-7/degree-10 univariate common-root test in `q`, with unique reconstruction of `d`. |
| status | PROVED |
| dependency | `l1_mersenne_hnf_m8_order_one_cubic_three_two_one_coefficient_matrix_router` |
| consumer | `l1_mixed_petal_amplification` |
| equivalence guard | `q!=0`, which forces `A(q)!=0` on `F_J=0`. |
| open residue | Four official modular gcd verdicts; for nonunit rows, `E_6`, role, exact-fiber, norm, cyclotomic, Frobenius-converse, and inner-lift filters. |
