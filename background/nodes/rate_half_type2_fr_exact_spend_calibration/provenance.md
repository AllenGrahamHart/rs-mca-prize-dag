# Provenance

- **derived:** 2026-08-10 in the Codex prize-resolution worktree;
- **trigger:** theorem-led audit after the zero-power `m=2` placement sweep;
- **inputs:** Round-31 `(NEWCAP)`, the exact `(AO1)` capacity term, and
  `rate_half_ca_hankel_endpoint_saturation_rigidity`;
- **compute:** exact integer arithmetic only, replayed under `ramguard tiny`;
- **upstream:** no imported theorem and no upstream status claim.
