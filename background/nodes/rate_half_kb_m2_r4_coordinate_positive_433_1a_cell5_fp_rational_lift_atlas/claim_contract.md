# Claim contract

| field | value |
|---|---|
| assumptions | Deployed field, cell 5, sign row `(-1,-1)`, and every common/source guard. |
| claim | Every guarded common `F_p` point has unique `r,c` coordinates over its `(b,t)` projection, using one `r` chart and a four-chart `c` atlas. |
| exact engine | Ordinary deployed-characteristic elimination plus a low-degree Frobenius/Bezout certificate. |
| excluded | Surjectivity from `P=0`, geometric closure over extensions, signed-family/colored-edge emptiness, cell/route/K3/Prize closure. |
