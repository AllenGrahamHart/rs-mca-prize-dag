# Lineage

This node follows the deployed reciprocal projection.  It was prompted by a
failed single-denominator reconstruction: the first `c` formula has genuine
extra zeros.  Retaining all four linear elimination equations reduces their
common guarded obstruction to an irreducible cubic with no deployed-field
root.
