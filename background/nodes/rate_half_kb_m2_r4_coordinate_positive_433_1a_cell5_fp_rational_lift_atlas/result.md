# Result

PROVED.  On guarded deployed-field points the reciprocal `(b,t)` projection
has degree four in `b` and loses no common-locus information: `r` has one
guard-unit formula and `c` is covered by four linear formulas.  Their only
simultaneous denominator locus is the forbidden `t=+-i` boundary plus a
cubic with no `F_p` root.
