# Audit

- Homogeneous forms include infinity and prevent hidden affine cancellation.
- Two complete active fibers exist in every live row because `n>=2`; their
  split locators descend a target transform without assuming `h` is already
  defined over the challenge field.
- Degree 30 is routed to degree six, not declared empty.
- The degree-12 residue is unique only because `A` splits over `K` and fifth
  power is bijective in each factor.
- The carrier cardinality and stabilizer statements are explicitly
  conditional on a missing same-record parameter-to-carrier bridge.
- `verify.py` and `verify_audit.py` independently replay the finite profile,
  partition, field, and carrier arithmetic.
