# Result

PROVED: the full-lift walls advance from `95943` to `96150` on KoalaBear and
from `97908` to `98229` on Mersenne-31. The next KoalaBear cell is a clean
mean-centered denominator wall; the next Mersenne cell is legal but exceeds
budget by `638658`.
