# Claim contract

## Input

The canonical complete shortened explanation-dimension-two chart, exact
support size `m=67474`, the support-local MDS transversality stages, and
same-support pair noncontainment.

## Output

An exact field-independent cap of `84416263` selected parameter records in
that chart.

## Nonclaims

The theorem neither changes record caps outside explanation dimension two
nor pays the aggregate rank-eleven kernel lane by itself.
