# Statement

In the complete shortened corank-two kernel chart

```text
(n,K,m,s)=(1048578,2,67474,2),
```

the `m` normals incident with each selected record are distinct projective
points in a three-dimensional normal space and are not all collinear.  Every
record consequently owns at least

```text
3(m-1)(m-2)=13657614768
```

ordered linearly independent coordinate triples.  Since an independent
triple determines at most one parameter point, the record count is at most

```text
floor(n(n-1)(n-2)/(3(m-1)(m-2)))=84416263.
```

This improves the generic support-local transversality cap `253241283` by
`168825020` records.
