# Proof: ImgFib Conjecture-F route retirement

## 1. The historical line had no callable interface

The old `imgfib/proof.md` contained only the bullet

```text
conj_f (plane sections of D_j have poly unpaid points).
```

It supplied none of the data required to invoke Conjecture F: no branch, no
linear space, no punctured domain, no locator degree or projective dimension,
no root threshold, and no first-owner or multiplicity map. The full-locator
Pade section developed later is polynomial, not linear. Thus the bullet
cannot be used as an unnamed LIST theorem.

## 2. The direct support partition is exhaustive

First-match totality assigns every contributor to one carried source. By
definition its petal support is either full petal or mixed/partial petal.
These cases are disjoint.

For a full-petal contributor, `pma_full_petal_band_composition` proves that
the top band `d>=ell(M-2)` is paid by `petal_growth`. In the below-band
complement, every cell with `t>=2M-4` has root excess `e=0` and is paid by the
root-pinning aggregate. The exact remaining full-petal branch is FPC5:

```text
M>=4,  d<ell(M-2),  t<2M-4,  e=max(0,2d+1-t ell)->infinity.
```

That branch is now named directly by `l1_full_petal_fpc5_payment`.
Mixed/diffuse partial-petal contributors are named directly by
`l1_mixed_petal_amplification`.

The live `imgfib` manifest retains the ordinary-Johnson, exact-periodic,
profile-evaluation, and taxonomy requirements. Hence every branch formerly
hidden behind the prose assembly either has a proved payment/interface or is
one of the two direct red petal leaves.

## 3. Conclusion and scope

No live LIST branch is sent to Conjecture F, so deleting the strict
`conj_f -> imgfib` edge loses no claimed payment. The genuine SPI/Hankel
consumer remains strict and unchanged.

This proof certifies an exhaustive route partition and retirement of an
untyped edge. It does not prove either red L1 leaf and does not promote
Conjecture F. The exact root-free rational-Q projective cell remains a valid
proved subfamily for possible future routes, but is not needed by this
assembly. QED.
