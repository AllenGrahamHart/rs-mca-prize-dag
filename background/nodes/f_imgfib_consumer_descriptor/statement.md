# ImgFib Conjecture-F route retirement

- **status:** PROVED
- **consumer:** `f_prize_consumer_flat_scope`

The historical `imgfib` proof listed `conj_f` as a hypothesis but printed no
LIST branch, flat, punctured domain, locator degree, dimension, root
threshold, owner, or multiplicity transport. The repaired assembly does not
interpret that prose line as a theorem.

```text
full petal
  = proved top-band or fixed-e payment
    disjoint union FPC5 direct target;

mixed or diffuse partial petal
  = mixed-petal direct target.
```

The proved `pma_full_petal_band_composition` identifies the first residual
exactly as

```text
M>=4,  d<ell(M-2),  t<2M-4,
max(0,2d+1-t ell)->infinity.                         (FPC5)
```

It is now the direct red `l1_full_petal_fpc5_payment`. The second residual is
the direct red `l1_mixed_petal_amplification`. The remaining ordinary-Johnson,
exact-periodic, quotient-profile, and taxonomy interfaces stay explicitly
wired in `imgfib`.

Therefore no current LIST branch is passed to Conjecture F. Removing the
`conj_f -> imgfib` requirement loses no claimed payment and avoids treating
the nonlinear full Pade section as a flat. This is route retirement, not a
proof of FPC5, mixed-petal amplification, or Conjecture F.

## Falsifier

A current `imgfib` branch whose only stated payment is Conjecture F; a
full-petal branch outside the proved payments and FPC5; or overlap between
FPC5 and the mixed/partial target.
