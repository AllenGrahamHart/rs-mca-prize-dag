# Closed attack record

1. Start from the `imgfib` assembly branches, not from family names in the
   old Conjecture-F notes.
2. The audit found no typed `conj_f` invocation. The apparent call was one
   prose bullet in a superseded statement-level assembly.
3. The exact full-petal audit exposed FPC5 rather than a linear flat. It is
   now `l1_full_petal_fpc5_payment`.
4. Mixed/partial petals remain the separate
   `l1_mixed_petal_amplification` target.
5. The verifier pins the repaired strict caller inventory and fails if LIST
   silently reacquires a `conj_f` edge.
