# Proof

Split the literal cell into `V=0` and `V!=0`. The fixed rank-drop exclusion
proves the first chart empty.

On `V!=0`, exact quadratic reconstruction reduces the resultant core to
three retained factors of degrees `3,3,12`. The generic route reduction
proves one cubic branch empty and reduces the other to the full-`J` route.
The complete `F04-R02` cubic/full-`J` exclusion proves that second cubic
branch empty. The complete literal `F04-R02` degree-12 exclusion proves the
remaining factor empty.

These charts and factors are exhaustive, so the whole literal cell is
empty over the deployed extension field. QED.
