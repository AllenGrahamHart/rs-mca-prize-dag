# Result

The surviving nonreduced collision is now a concrete test on the unique
paired scalar weld. Its outside value row has an exact double parameter
zero, while two derivative-row functionals select `[4]`, `[1,3]`, or
`[2,2]`. The correction parameter is outside every supported row-root set,
so all summands in the zeroth value and derivative equations are nonzero
before their forced cancellations.
