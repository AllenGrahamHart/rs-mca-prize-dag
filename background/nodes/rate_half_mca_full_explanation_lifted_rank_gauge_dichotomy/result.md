# Result

`PROVED`: every full-explanation-rank residual splits into a rank-drop branch
of lifted/error rank `K` and a no-drop branch of lifted/error rank `K+1`.

The rank-drop branch inherits the penultimate-rank corrected occupancy wall:
`e>=992852` on KoalaBear and `e>=1037876` on Mersenne-31.  The full-lift
middle-support cells remain open.
