# Proof

The E21 router proves that every `V=42` candidate lies in one of seven
profiles on 111 templates. The seven-profile exclusion proves that every
proper-conductor vector is removed by the conductor theorem and every
full-conductor norm is positive and below `2^250`. These branches exhaust the
router, so the entire `V=42` chamber is empty. Variance is even, hence the
next live positive endpoint is at most 40.
