# Frontier

Enumerate the two one-loop signed common-`K` atlases using `(KBN1W-4)` before
constructing any outside source-facet records.  Classify symbolic survivor
loci rather than launching arbitrary assignment searches.
