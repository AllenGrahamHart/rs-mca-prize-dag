# Proof

The fixed rank-drop exclusion proves the complete `V=0` chart empty for
`F07-R02`. On `V!=0`, exact quadratic reconstruction and resultant
factorization are exhaustive and give two cubic factors and one degree-12
factor.

The literal cubic replay proves factor 1 empty after complete named
localization and identifies factor 0 as the surviving route. The all-literal
cubic/full-`J` route node proves factor 0 empty. The four-cell `R02`
degree-12 node proves the selected degree-12 branch empty by its exhaustive
`s,L6,K8,K10` decomposition. Every branch of both reconstruction charts is
therefore empty. QED.
