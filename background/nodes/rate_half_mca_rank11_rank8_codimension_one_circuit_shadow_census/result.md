# Result

The first open residual row now has an exact structural partition:

```text
K'=11, c=9:   B is the only rank-eight shadow; 54 shadows have rank nine.
K'=11, c<=8:  at least two rank-eight shadows occur, and
               L_(C_B)RS_{<11-c} has dimension at least three.
```

Every `c=2,...,9` is possible at the correction-space and chart-rank level;
linear algebra alone removes none of them. The rank-eight row remains open;
no active-v4 atom moves. The next argument must retain component-extension
multiplicity while coupling neighboring charts to one chronology-correct
owner or exploiting the larger locator ideal in the `c<=8` branch.
