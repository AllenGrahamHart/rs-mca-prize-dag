# Dependency sub-DAG

```text
dense-root saturation + minimal-shortening exclusion
  -> at the first open row K'=11, V' is a hyperplane of RS_{<11}

rank-eight fixed-chart target
  -> rank(ev_B|V')=8
  -> ker(ev_B|V')=L_B RS_{<2}

maximal-shortening normalization + at least two fixed-chart slopes
  -> an evaluation loop would make E_b(Z) identically zero
  -> no circuit of size one on B

hyperplane functional factors through ev_B
  -> one fixed circuit C_B subset B, 2<=|C_B|<=9
  -> every extension T has that same unique circuit
  -> exact 55-shadow and ten-basis census                 [PROVED]
  -> locator ideal L_(C_B) RS_{<11-|C_B|} lies in V'     [PROVED]

finite-field hyperplanes for every c=2,...,9
  -> local linear algebra excludes no circuit size          [PROVED FENCE]

next split:
  c=9   -> 54 rank-nine shadows, eight-petal control
  c<=8  -> extra rank-eight shadows and ideal dimension >=3
```
