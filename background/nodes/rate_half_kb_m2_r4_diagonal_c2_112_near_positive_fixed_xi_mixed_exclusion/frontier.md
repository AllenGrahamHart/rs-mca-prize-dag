# Frontier

The three residual allocations in the fixed-moving `xi=a` affine template
are closed. Move to the other relative `xi` orbits, then the moving-moving
template, negative factor loci, and the homogenized `w=0` boundary.
