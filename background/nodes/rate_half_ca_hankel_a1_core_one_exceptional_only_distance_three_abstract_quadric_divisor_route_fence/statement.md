# Distance-three abstract quadric-divisor route fence

- **status:** PROVED
- **closure:** explicit construction
- **consumer:** `rate_half_band_closure`
- **dependency:**
  `rate_half_ca_hankel_a1_core_one_exceptional_only_distance_three_dihedral_pair_complement_quadratic_trace`

Let `e>=3` and let the field have odd characteristic and at least `3e`
distinct elements. There is a squarefree split polynomial `P` of degree
`3e`, a four-dimensional vector space `W` of polynomials of degree at most
`e`, and a rank-three quadric cone in `P(W)` containing

```text
6e+1                                                   (AQF1)
```

distinct projective classes of split degree-`e` divisors of `P`.

Consequently, no upper bound below `3e-3` can follow merely from:

```text
P squarefree and split of degree 3e,
K | P split of degree e,
[K] in one P^3 and on one rank-three quadric cone.     (AQF2)
```

The pair-complement trace must use its calibrated interpolation weights,
orbit coordinates, or external-incidence relations beyond the abstract data
in `(AQF2)`. This is a route fence, not a distance-three counterexample.
