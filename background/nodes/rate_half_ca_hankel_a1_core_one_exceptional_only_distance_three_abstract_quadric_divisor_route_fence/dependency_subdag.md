# Dependency sub-DAG

```text
pair_complement_quadratic_trace [PROVED]
  --req--> abstract_quadric_divisor_route_fence [PROVED]
  --ev---> rate_half_band_closure [UNPROVED]
```

The fence removes an insufficient abstract route and preserves the calibrated
pair-complement trace as the live theorem interface.
