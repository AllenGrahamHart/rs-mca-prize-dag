# Result

The double-root norm test no longer contains an unknown rational cube. The
full resultant is exactly `H^d` times a linear or quadratic cube, with one
explicit exponent-three correction in the ordinary cubic packet. This is a
bounded symbolic target independent of the official integer sizes.
