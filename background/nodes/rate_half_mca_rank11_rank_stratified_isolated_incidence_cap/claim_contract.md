# Claim contract

The theorem uses the normalized coordinate equations already proved in the
dense-locator component-incidence node.  It also uses the retained-slope
condition `q(gamma)!=0` and distinctness of record slopes.

It sharpens only the count of actual isolated `(record,T)` incidences.  It
does not bound projective intersection multiplicity, identify components,
pay either component lane, assign an owner, or close rank eleven by itself.
