# Result

The residual slot `(ell,w)=(2,6)` now has an exact simultaneous-moment
router. After normalizing three selected roots, the other three are the
roots of a cubic with one free coefficient. Their product lies in the
subgroup and removes that coefficient. Two coupled doubling recurrences then
test subgroup membership with ten squarings at order `1024`.

This replaces the blind `185,569,028`-class weight-six norm route by at
most `1,550,336` pair-product candidates, each carrying two explicit
cyclotomic norm obstructions. Prime factors shared with `Norm(1+x+y)` may
be removed exactly, because the paid weight-three theorem excludes that
branch at every conjugate embedding. The node does not assert that the
remaining saturated gcds avoid official characteristics, so WCL-ZONE and
its five residual slots remain open.

A deterministic Modal route probe evaluated four candidates at each
`M=64,128,256,512,1024`. No obstruction pair was identically zero. At
`M=1024`, raw gcds reached `183,078` bits, but weight-three saturation
reduced the maximum to `11,367` bits. All `19` nonunit saturated gcds in
the full ladder factored completely over `16` primes, with maximum
`v_2(p-1)=10`. This is strong route evidence only. The four production-size
norm pairs took `28.2` to `44.7` seconds each, so a blind full sweep is
not yet the selected next step.
