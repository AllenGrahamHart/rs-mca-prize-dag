# Claim contract - L1 bounded Plotkin-excess payment

## Inputs

- the combined-set distance bound and Rankin equality payment;
- fixed petal polarity and the L1 lower cutoff;
- the official source equation for `(PX5)`.

## Output

Every exact support cell has the exponential-in-excess but otherwise
field-independent bound `(PX2)`. Every fixed `(p,E)` box is polynomial per
source chart, forcing `(PX4)` on a counterfamily.

## Consumer rule

Consumers may discard every bounded Plotkin-excess layer. They must not call
`2^E` polynomial when `E` is unbounded, or apply the support-pattern aggregate
to growing petal polarity.

## Falsifier

A length-`4ell+E` combined-set code of distance at least `2ell` exceeding
`(PX2)`, or failure of the exact official identity `(PX5)`.
