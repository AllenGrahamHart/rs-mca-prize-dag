# Audit - L1 bounded Plotkin-excess payment

## Checked axes

1. Exactly `E=V-4ell` coordinates are fixed before puncturing.
2. Same-pattern puncturing preserves, rather than merely lower-bounds, every
   distance inside a class.
3. The punctured length is exactly `4ell`.
4. The Rankin equality bound is applied separately to every pattern class.
5. The factor is `2^E`, not a field-size factor.
6. The aggregate sums exact support patterns and defect degrees only.
7. The exact `F_23` chart populates the `E=1` layer but is polynomially paid.
8. The official slack identity preserves the additive `r(g+1)` term.
9. Unbounded Plotkin excess and growing petal polarity remain open.

## Remaining attack

The bounded-polarity per-chart residual now has joint sub-Johnson slack and
Plotkin excess both unbounded. A useful next reduction must exploit the
marked split-pencil geometry or identify a natural-scale owner in that
double-growing region.
