# Lineage - L1 tame fixed-petal refinement census

`l1_fixed_source_quotient_partition_anchor_census` proved that one whole
source petal fixes a degree-`ell` quotient partition. The live local residual
also allows that petal to be a union of smaller complete fibers. Factoring its
locator as `F(P)` and reading the top `s-1` coefficients proves uniqueness at
every tame divisor. The additive `X^(p^2)-X` fixture pins the exact point at
which that triangular argument can fail.
