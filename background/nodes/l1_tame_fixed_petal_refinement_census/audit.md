# Audit - L1 tame fixed-petal refinement census

## Checked axes

1. Complete degree-`s` fibers, not dense partial fibers, imply `(TR2)`.
2. Both the right component and outer factor are monic.
3. Distinct fiber labels make the outer factor squarefree.
4. Lower outer powers cannot reach the top `s-1` coefficient positions.
5. Division by `r` is used only under the explicit tame guard.
6. The undetermined right-component constant is exactly additive-shift
   equivalence.
7. Least-petal ownership and `s|ell` give the count in `(TR4)`.
8. The theorem counts maps only; it does not hide `3^(n/s)` role entropy.
9. The `F_9` fixture has four distinct normalized degree-three right
   components of `X^9-X`.
10. The fixture is an algebraic route fence, not an official-domain
    counterexample.

## Remaining attack

Pay role/support polarity collectively at small tame refinement scales;
classify or exclude wild right components using multiplicative-domain
geometry; and retain the unanchored and arbitrary-locator branches.
