# Result

The remaining determinant-singular `J!=0` branch no longer requires a raw
four-variable elimination. Its `x=0` part is a two-variable system with a
linear `d` relation, and its `x!=0` part is an exact five-equation system in
`(x,Y,q)` after rational reconstruction of `d`. No branch is declared empty.
