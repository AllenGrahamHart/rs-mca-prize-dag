# Claim contract

| field | value |
|---|---|
| claim | The determinant-singular `J!=0` coefficient core splits exactly into an `x=0` two-variable endpoint with a linear relation in `d`, and an `x!=0` three-variable chart with `d` reconstructed rationally. |
| status | PROVED |
| dependency | `l1_mersenne_hnf_m8_order_one_cubic_three_two_one_coefficient_matrix_router` |
| consumer | `l1_mixed_petal_amplification` |
| equivalence guards | Inherited `q*d*(q-d)*D*K_6*R*S!=0`; `J!=0`; on the second chart `xG_2NZP(q^2x-P)!=0`. |
| open residue | Unit or retained-point verdict for both charts; the generic `Delta!=0` chart; every exact-fiber, norm, Frobenius-converse, cyclotomic, and inner-lift filter. |
| nonclaim | The endpoint variables are not forced into `F_p`; irreducibility of an official role quadratic alone does not exclude either chart. |
