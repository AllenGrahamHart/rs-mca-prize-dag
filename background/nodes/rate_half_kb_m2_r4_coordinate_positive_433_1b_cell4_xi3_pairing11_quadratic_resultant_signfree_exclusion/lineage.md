# Lineage

- Wave 68 introduced nested sign elimination in `q` and then `z` to close
  representative matching 4 and its orbit partner 9.
- Wave 69 applies the same exact architecture with the colored target sign
  entering the second pair to close representative matching 5.
- Wave 70 replaces the nested `q` elimination by the exact quadratic
  Sylvester resultant for the two non-lane pairs at representative matching
  7, then retains the same exact `z` sign elimination and finite replay.
- Wave 71 applies the same resultant architecture after exchanging the
  `bf` and `sigma_c cf` partners at representative matching 8.
- Wave 72 applies the same exact architecture to the common-left-`q`
  matching 11 residual and closes the last live matching-exchange orbit.
