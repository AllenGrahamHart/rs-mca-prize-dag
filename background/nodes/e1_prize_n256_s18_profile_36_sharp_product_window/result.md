# Result

`PROVED`: the profile-`(3,6,S=18)` cofactor branches `1024` and `1028`
now have the exact residual window

```text
V in {4,6,8,10,12}.
```

The previous upper endpoint was `34`. Eleven profile cofactors remain, but
the two shortest surviving branches are now both in the low-energy range of
the exact mod-four/XOR classifier.
