# Audit

- The extremum argument maximizes over all positive real 64-tuples with the
  two moments in the proof. It is therefore an upper relaxation of the
  cyclotomic problem, not an unproved realizability assertion.
- Every interior extremum has at most two values because the multiplier
  equation is quadratic. Every boundary point has product zero.
- The certificate uses no floating-point comparison. Square roots are
  enclosed with integer square roots at denominator `2^192`, and products
  are compared as exact rational numbers.
- The ledger checks all 649 feasible `(V,j)` chambers for even
  `14<=V<=34`. The closest chamber is `(14,63)`.
- A lower interval at `(12,63)` proves that the same method cannot silently
  remove the printed boundary.
- Route probe Modal app: `ap-SMIqDy9GbBH2jIbHwnE07t`.
- Exact certificate Modal app: `ap-VUumIAvKygC1l5swGhdI4j`.
