# `A=1` distance-three quartic support degree-four reducible deck router

- **status:** PROVED
- **closure:** proof plus exact Newton-support certificate
- **consumer:** `rate_half_band_closure`
- **dependency:**
  `rate_half_ca_hankel_a1_core_one_exceptional_only_distance_three_quartic_support_degree_four_irreducible_router`

Retain a degree-four common-fiber survivor whose off-diagonal coincidence
divisor is geometrically reducible. Then `psi` has a nonidentity special
deck automorphism over the base field and, after scaling coordinates, has
one of the forms

```text
psi(X)=F(X^h),              h in {2,4},              (Q4D1)
psi(X)=F(X+c/X),            c in mu_N,               (Q4D2)
```

where `F` is rational of degree `4/h` in `(Q4D1)` and degree two in
`(Q4D2)`. In the cyclic case the deck scalar lies in `mu_N` and has order
`h`; in the inversion case the displayed `c` lies in `mu_N`.

Thus the complete degree-four support frontier is:

1. the absolutely irreducible Laurent-end curve from the preceding router;
   or
2. one of the cyclic/dihedral pullbacks `(Q4D1)--(Q4D2)`.

No generic geometrically reducible quartic map remains. This node does not
exclude the displayed pullbacks or show that the support matching itself is
the deck-orbit matching.
