# Frontier

For each of the four residual loci, compute the common kernel and discard
the `beta=0` or leading-coefficient branches.  Then compile the unique
outside skeleton into seven guarded edge ideals; seek a low-degree
resultant or unit-ideal deletion before any finite-field search.
