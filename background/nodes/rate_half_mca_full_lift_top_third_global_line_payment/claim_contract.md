# Claim contract

## Proved

1. Triple overlap synchronizes the union of all top-third exact layers onto
   one affine codeword line.
2. The union, not each layer separately, costs at most `N-m+1` slopes.
3. The global-line profile `(GL2)`.
4. KoalaBear is paid through `e=95943` and Mersenne-31 through `e=97908`.
5. Both adjacent denominator records.

## Not proved

1. A payment at `e=95944` or `e=97909`.
2. A post-Johnson prefix theorem below the current agreement threshold.
3. A complete MCA or prize theorem.

## Promotion rule

Keep `PROVED` only while both verifiers accept source pins, cross-layer
triple overlap, global affine-line synchronization, total-core packing,
exact strips, adjacent denominator signs, and hostile mutations.
