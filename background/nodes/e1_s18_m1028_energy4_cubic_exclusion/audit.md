# Audit

- Both integral square partitions of energy four are considered; the
  all-even one is removed by local multiplicity.
- The finite-field screen checks every four-lag set and all 16 sign patterns.
- Root `3` is checked to have exact order 256 modulo `257`.
- The 8,385-row ledger is pinned by count and SHA-256 digest.
- Ordered-triple and relation-formula computations agree for every retained
  type.
- The cubic logarithm inequality is global on the full positivity interval
  `t>-1`.
- The final field-floor comparison is exact rational arithmetic.
- No profile-specific coefficient realization is assumed.
