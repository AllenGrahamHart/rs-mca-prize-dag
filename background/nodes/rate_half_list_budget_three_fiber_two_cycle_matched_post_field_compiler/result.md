# Result

The doubled-order matched parity branch is now nonharmonic and has a complete
post-field classifier through the ODE, scalar, final square, constant, and
gcd stages. The exact final test is

```text
T/q_out=W^4,
```

which is invariant under `q_out<->q_out^(-1)`. This repairs the old unscaled
fourth-power test in the nonsplit field class. The next theorem task is the
parameter transfer of the trace-Jacobi and cyclotomic-norm endpoint; only
after that transfer can CR-002-C become a contributor-ready run.
