# Dependency sub-DAG

```text
matched lift field router ---------------------------+
parameter-uniform constant ODE ----------------------+--> matched post-field compiler [PROVED]
old harmonic levels 1..38 + new level 39 certificate+                |
                                                                      +--ev--> rate_half_list_adjacent_crossing [TARGET]
```

The exact nonharmonic endpoint is `T/q_out=W^4`, not unconditionally
`T=W^4`. The latter loses the nonsplit field class when `q_out` has exact
order `N`. The transfer stops before the trace-Jacobi and cyclotomic-norm
scale audit.
