# Result

The high-distance endpoint has no free `W_vee`, `A_W`, or `B_W` allocation.
Clean first jets determine `W_0`; a triangular sequence of affine remainder
tests then uniquely reconstructs both corrections or rejects the packet.
Only deterministic unit-division and Hankel checks remain on this interface.
