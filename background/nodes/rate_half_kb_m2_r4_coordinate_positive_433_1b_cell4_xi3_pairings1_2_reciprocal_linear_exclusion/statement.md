# Statement

## Claim (KBP1B4-XI3P12-RL-1)

On the guarded positive deployed 433-1b role-cell-4 branch, every atlas case
with missing record `xi=3` (the `df` record), canonical matching index in
`{1,2}`, any source sign `epsilon in {+-1}^2`, and any target lane
`sigma in {+-1}^2` is empty.

For each source point, `paired(de,de)=0` has exactly the three rational
branch types `q=B_i/A_i`, `i in {0,1,2}`, subject to denominator replay.
Twelve matching-1 rows and 24 matching-2 rows cover all source signs and
target lanes. The branches are internal alternatives, so these rows pay 32
raw atlas cases, not 96.

No other matching, missing role, complete cell-4 exclusion, rate-half band,
LIST, MCA, or Prize result is claimed.
