# Lineage

Wave 65 closed matching 0 by a quadratic/quadratic cut in `y=1/d^2`.
Matchings 1 and 2 retain the same factored first pair but replace the outside
second pair by a common-endpoint pair. The natural variable is therefore
`z=1/d`, giving the quartic/quadratic reciprocal-linear route used here.

The packet keeps direct `xi=3` exclusion separate from the universal
`xi=4` transport application so both proof obligations remain auditable.
