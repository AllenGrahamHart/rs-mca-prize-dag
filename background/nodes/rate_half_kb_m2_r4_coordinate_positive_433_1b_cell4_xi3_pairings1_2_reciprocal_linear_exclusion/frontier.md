# Frontier

This node closes only `xi=3` at matchings 1 and 2. The immediate exact
consequence is the corresponding pair of `xi=4` exclusions through the
already-PROVED universal outside-role transport.

After transport, cell 4 retains the six matching-exchange pairs
`{3,6}`, `{4,9}`, `{5,12}`, `{7,10}`, `{8,13}`, and `{11,14}` for each of
`xi in {3,4}`. Only one `xi=3` proof per matching index is independent.
