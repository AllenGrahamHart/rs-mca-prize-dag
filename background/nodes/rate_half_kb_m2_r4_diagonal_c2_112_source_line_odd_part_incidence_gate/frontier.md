# Frontier

Evaluate `(KBOI-3)` on the aligned and near-aligned quotient records before
building any full interpolation matrix. After normalizing the nonzero odd
part, surviving incidences retain two positive-sign or one negative-sign
affine `U` parameters after imposing `U(a,z)=0`; combine that residue with
the appropriate colored quotient system. Attack the ramified forced-square
branch separately.
