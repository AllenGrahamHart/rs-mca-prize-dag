# Result

Rank-two block-extension compatibility is now an exact finite packing ledger.
Reusing an active zero point and sharing an outside extension point consume
the printed pair slack `z_i+z_j-d-1`; summing gives a global reuse/collision
budget with exact outside multiplicities.

The unresolved rank-two work is to count coefficient-compatible support
families and solutions of this packing ledger, assign first-core ownership,
and pay the cross-core slope aggregate.
