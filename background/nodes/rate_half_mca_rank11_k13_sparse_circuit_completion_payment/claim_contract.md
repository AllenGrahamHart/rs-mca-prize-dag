# Claim contract

This node closes the complete positive-dimensional component population at
the single residual row `K'=13`.

It partitions all incidences into:

1. rank-deficient eleven-sets, paid by both nonzero canonical-basis corank
   terms;
2. full-rank circuits of size at least six, paid by rank-nine shadows; and
3. full-rank circuits of size at most five, paid recordwise by the
   codimension-three completion theorem.

The empty global common zero hypothesis used by the sparse theorem is the
proved property already used to exclude rank zero in the canonical-basis
globalizer.  No row `K'>=14`, quotient dimension above three, chronology
owner, active-v4 movement, or complete prize closure is claimed.
