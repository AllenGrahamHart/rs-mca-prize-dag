# Audit

1. The rank-deficient payment retains coranks one and two with extension
   factors three and one.
2. The rank-nine chart maximum includes every honest core size
   `j=9,10,11,12`.
3. The one-dimensional quotient intersection on a full-rank eleven-set is
   used both for circuit uniqueness and for the 55-shadow partition.
4. The sparse theorem is applied only after invoking the proved empty global
   common zero property of the correction space.
5. Structured and two-completion sparse branches have already been maximized
   inside the separate theorem; the larger official cap is used here.
6. Kernel and high-circuit capacities are independent of `R_actual`.
7. The demand-minus-low coefficient is positive, so checking the minimum
   record population is sufficient.
8. The exact gap is positive but materially smaller than at `K'=12`; every
   printed floor is replayed with integer arithmetic.
