# Frontier

The next cofactor-`514` object is a finite signed autocorrelation target
screen over 17 magnitude profiles. Apply, in this order:

```text
1. local multiplicity two on the parity autocorrelation polynomial;
2. one fixed primitive-root equation modulo 257;
3. diagonal Galois canonicalization;
4. exact CRT resultants only for compatible types.
```

Energies twelve and thirteen now have only all-unit profiles, but their raw
lag-combination spaces are still too large for unstructured enumeration.
Exploit the local multiplicity and finite-field equation before launching any
remote census.
