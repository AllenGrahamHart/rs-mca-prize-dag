# Claim contract

## Consumed content

- The cofactor-514 outer-energy leaf reduces the branch to `E=5,...,13`.
- The conductor-256 autocorrelation dictionary supplies integral
  autocorrelations, conjugate moments, and the deviation bound by their `L1`.
- The split-prime router supplies the official field interval.

## New proved content

1. The integral square partitions of energies five through thirteen give 32
   possible magnitude profiles before logarithmic filtering.
2. The profile `(E;n_1,n_2,n_3)=(5;1,1,0)` lies above the field ceiling.
3. Five shape-sensitive upper envelopes remove 14 further profiles below the
   field floor.
4. Exactly the 17 profiles printed in the statement survive.

## Guards

- The result classifies autocorrelation magnitude profiles, not signed lag
  configurations or coefficient supports.
- A surviving profile is not asserted realizable.
- Local multiplicity one and the mod-`257` root condition are not yet used.
- No prime-ideal orbit is removed and the parent E1 target remains open.
