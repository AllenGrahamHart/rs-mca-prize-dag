# Result

The exact external norm supplies a support-only prefilter. Every matched
exceptional pair and every pair of triple points yields an explicit
normalized derivative ratio that must be an `e`th root of unity. No internal
slope, fiber scalar, external block, moment, or complement variable is needed
to evaluate these gates.

The remaining design search should apply these boundary gates before any
pair-Lagrange interpolation or split-fiber classification.
