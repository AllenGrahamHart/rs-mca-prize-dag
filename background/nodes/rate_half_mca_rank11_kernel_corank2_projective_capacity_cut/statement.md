# Conditional corank-two projective capacity cut

Assume the open uniform proposition

```text
M_2<=84416263
```

for every official corank-two canonical-basis chart.  Together with the
proved uniform corank-one cap, the other individual caps, and all 28
multi-step shadow inequalities, this excludes domination for

```text
377674<=K'<=568338.
```

The predecessor excludes `10<=K'<=377673`, so the conditional combined
closure is `10<=K'<=568338`.  The exact conditional comparison reverses at
`K'=568339`.

The cap `84416263` is currently proved only in the complete chart.  The
valid integer-gap envelope gives `253238254` at `K'=377674`, so this node is
not an unconditional kernel payment.
