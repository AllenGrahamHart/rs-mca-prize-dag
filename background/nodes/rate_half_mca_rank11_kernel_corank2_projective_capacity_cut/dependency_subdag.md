# Dependency sub-DAG

```text
projective-pair capacity cut ---------\
uniform corank-two cap ---------------+-> proved capacity cut
multi-step shadow hierarchy ---------/
```

The predecessor banks all rows through `377673`.  The proved uniform-cap
parent supplies the corank-two resource; the hierarchy parent supplies all
28 exact rank-step inequalities.  The exact replay composes them through
`568338`.
