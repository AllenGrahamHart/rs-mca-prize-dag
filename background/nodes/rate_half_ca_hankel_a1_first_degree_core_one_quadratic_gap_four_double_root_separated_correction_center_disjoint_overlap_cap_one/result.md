# Result

On the separated double-root locus,

```text
gcd(S_B,Lambda)=1,
j=deg gcd(Lambda,g_*S_B^2)<=1.
```

Every correction root is off-center and occurs with exact multiplicity two
in the nonzero heavy row. The remaining overlap polynomial is constant or
linear and introduces at most two scalar coefficients.
