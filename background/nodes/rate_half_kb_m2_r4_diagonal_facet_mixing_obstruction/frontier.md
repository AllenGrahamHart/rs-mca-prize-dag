# Frontier

Split the diagonal coefficient attack by the five rows `(KBDM-3)`. Start
with `c=2`, where four endpoint labels remain in each preserved side and the
common-`K` support restrictions are strongest. Combine `(KBDM-4)` with the
8/7-dimensional reciprocal norm forms in the source-line branch and with the
split cubic resolvent in the biquadratic branch. Do not impose coordinate-style
`I,J` invariance or descend the colored divisor through the source deck map.

The aligned `c=6` row is gone. For its near-aligned survivor, use the proved
quotient identities `(KBDM-7)` directly: its colored quadratic is the
positive reciprocal locator of the pair `{xi,tau(xi)}`. Do not enumerate
arbitrary four-edge colored divisors in this row.

For `c=2`, begin with `(a,b)=(2,0)`: impose the forced square fiber
`R_(k_*)~P_(J_1)^2` and the four-fiber norm
`product_(K_0)R_k~P_(J_0)^4` on the reciprocal norm or split-resolvent
branch. Then treat the saturated `(1,1)` rows. Keep the exceptional
near-aligned row with `eta,tau(eta) in J_0` separate; it has only the
crossing-incidence interval `6..8`.
