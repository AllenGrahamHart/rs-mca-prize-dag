# Proof

The proved generic signed-pair colored exclusion is valid whenever all
registered guard/chart norms and certificate denominators specialize
regularly.  The proved router census computes the exact complement as a
69-value set with canonical digest

```text
bd64dc238bb3dcc4491d7d7b856078871336571cbdd5df3343014f8198cfe1d4.
```

The first finite packet excludes 23 values.  Exact cancellation of the old
coordinate-map poles followed by dynamic primitive replay excludes a
disjoint set of 38 values.  Direct raw-basis replay excludes the remaining
eight values, including the dimension-23 vertical fiber.  The assembly
checker reconstructs the router independently and verifies

```text
23+38+8=69
```

as a disjoint set partition.  Thus the generic theorem covers every value
outside the router and the three finite theorems cover every value inside
it.  The sign row is empty for every deployed `t`. QED.
