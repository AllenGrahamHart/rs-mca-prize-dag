# Frontier

The positive residual route `433-1a -> O0b` is closed.  Do not return to its
signed target lanes, common matching cells, root-sign rows, compact curves,
or signed-pair projections.

Continue with a different unresolved route in `rate_half_band_closure`.
