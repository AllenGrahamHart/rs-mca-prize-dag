# Result

The generic Euler gate now has a one-scalar prefilter. In every field branch
with a nontrivial cubic character, `t_1^2V(tau)` must be a cube before the
full quotient-algebra remainder is evaluated.

The test is necessary only and is vacuous when cubing is bijective.
