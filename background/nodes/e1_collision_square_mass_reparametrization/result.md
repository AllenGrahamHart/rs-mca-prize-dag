# Result

PROVED, with a correction to the canonical precursor. Square mass `S`, not raw
representative distance, is the norm-visible collision coordinate. At fixed
official parameters it satisfies the exact finite bounds `S<=260` or
`S<=132`; it is not literally unbounded.

All three additional `N=256,S=16` splits `(2,8)`, `(1,12)`, and `(0,16)` are
realized by official-size class pairs and survive the current norm test. The
existing `(3,4)` variance closures stand, but continuing that descent alone
cannot close the route-uniform E1 target. No row, endpoint, or prize terminal
is closed by this node.
