# Result

Quotient weights may be canonicalized on the six-value anharmonic target
orbit. Antipodal targets admit the additional exact twin identity

```text
(P(t),R(t))=(P(t/(t-1)),R(t/(t-1))).
```

This can halve nonfixed antipodal richness/weight evaluation while retaining
both target multiplicities. The class-A cutoff is exactly `P>=26`, or
`M_a>=24`. The signed-disjoint edge totals must still be computed separately.
