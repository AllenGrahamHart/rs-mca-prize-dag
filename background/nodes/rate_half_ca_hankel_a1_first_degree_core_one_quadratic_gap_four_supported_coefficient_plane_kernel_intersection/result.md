# Result

At every ordinary rank-one loss slope, the coefficient plane meets the
specialized Hankel kernel only in the primitive locator line, and its
evaluation on the actual contracted support has exact rank `e`. Rank-two
loss permits at most one additional kernel direction.
