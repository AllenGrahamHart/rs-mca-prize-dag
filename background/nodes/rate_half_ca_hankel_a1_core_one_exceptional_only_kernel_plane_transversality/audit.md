# Audit

- Used the specialized minimal apolar generator, not merely the specialized
  generic generator. Equality follows because both have exact degree `r-1`.
- Checked the complete-intersection boundary: the other generator has degree
  `r+3`, so no additional degree-`r` kernel vector exists.
- Used the literal middle catalecticant, whose degree-`r` kernel is the
  degree-`r` apolar space; arbitrary symmetric pencils need not have the two
  shift vectors.
- Derived both zero pairings from the first kernel-lift equation and
  symmetry. They are not extra assumptions.
- Derived `v^TM_1v!=0` from the proved nonzero linear cofactor quotient;
  generic rank recovery alone could occur at higher order and would be too
  weak.
- The local coordinate `z=E/H` requires `H(gamma_0)!=0`; rescaling by a
  local unit preserves every zero/nonzero assertion.
