# Provenance

Proved by pilot `r36_lawcount_geom` (round 36, bank 1; Round-36 (LA-EQ)
addendum, coordinator hand-checked). Drafted as a standalone package by
pilot `r37_mint_drafts` (round 37, bank 2, package 6 of 10), which caught
the duplication hazard (the `Z^m - X^{2m}` family was already banked on
the saturation fence node's coordinator addendum) and recorded the (RIC3)
wiring gap.

Wired 2026-08-11 by the coordinator in the task-#41 mint session: requires
edge to `rate_half_layer_a_saturation_count_route_fence` added; the
duplication handled as cite-not-reclaim per the draft's own warning; the
(RIC3) citations added on both sides (the fence node's wave-59 addendum +
this node's explicit references); the wiring audit contributed the first
local replay of the banked `m = 4` fence cell and a deterministic H1 build
at the fresh field `q = 577`. No Modal computation or external library was
used.
