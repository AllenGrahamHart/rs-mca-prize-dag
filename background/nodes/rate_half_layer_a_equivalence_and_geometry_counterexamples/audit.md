# Audit

1. The duplication hazard the draft itself flagged is enforced structurally:
   the `m = 2` member and the family measurements are consumed through a
   REQUIRES edge on the PROVED fence node, and the statement says
   "cite, not duplicate" in so many words.
2. `verify.py` builds the H1 census (random draws, two fields), the H1+H2
   exhibit, and the fresh `m = 3` fence cell; `verify_audit.py` shares no
   code path — a DETERMINISTIC H1 build at the fresh field `q = 577`
   (incidences by whole-field quadratic scan), and the banked-but-never-
   locally-verified `m = 4` cell (108 x 80, nullity 8), with independently
   written construction and elimination (t-major column order).
3. (LA-PADE)/(LA-DEG) is carried at POSED with its mechanism attributed to
   the PROVED (RIC3) by citation; the 9/9 agreement is the source's, not
   re-claimed.
4. The (LA-EQ) reading is priced honestly as a reading (five lines from
   proved inputs), not as new mathematics.
5. The scope section preserves every sample-vs-bound distinction the draft
   made (the `O` minima; the two-field H1+H2 refutation's exact
   quantifier reach).
6. Edge character: evidence to the crossing node + one requires edge to
   the fence node. No critical requirement discharged.
