# Frontier

The degree-24 packet satisfies the algebraic input of the existing
partial-relative extraction more strongly than the generic degree-18 seed.
The next exact adapter should apply that extraction at this branch-local
scope and preserve ruling orientation and first ownership in each output:

```text
pure locator | scalar-locator rational | chi>=2299571.
```

The route closes only if these packet outputs can be aggregated without
counting one original slope under multiple local order-32 seeds.
