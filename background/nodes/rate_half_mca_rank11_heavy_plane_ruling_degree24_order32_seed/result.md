# Result

Every heavy ruling orientation contains an actual order-32 packet with
residual dimension at least three and slope degree `24..31`. This improves
the generic dense-pair seed's `18..31` lower degree inside the heavy Segre
branch and reaches the same partial-relative algebraic interface without
assuming that a large punctured-domain incidence floor survives.

The remaining obstacle is aggregation: the packet must be assigned to a
chronology-safe partial-relative/S-A-E owner or paid directly.
