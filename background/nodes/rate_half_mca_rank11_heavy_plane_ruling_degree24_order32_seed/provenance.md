# Provenance

Derived locally on 2026-08-18 from the heavy-plane low-margin Segre ruling
router. The component-span core recovery follows the same exact linear
mechanism as the existing heavy-pair order-32 compiler, but all constants,
pair types, and the degree-24 pin are reconstructed at the smaller
four-dimensional ruling interface.

The upstream comparison target is PR `#1173` at `2788d5ec3`, whose next
terminal is factor-flag synchronization. The packet also overlaps the
primitive shift-pair interface; no upstream payment is imported.
