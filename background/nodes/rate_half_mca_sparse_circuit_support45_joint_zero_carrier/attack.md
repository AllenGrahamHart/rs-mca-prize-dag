# Attack record

The independent support caps at `K'=45` left a nested support-five defect
two / support-four defect three branch above the safe premium.  Treating
those defects separately discarded the fact that both completion families
live in one annihilator.

The first attempted shortcut was to assert that their label spaces span the
whole annihilator.  That is false in general: their sum can have codimension
one or two.  Closing the intersection vanishing space under all common zeros
retains exactly this residual as `delta`; the root bound then controls every
support-four completion carrier relative to the same set `B`.
