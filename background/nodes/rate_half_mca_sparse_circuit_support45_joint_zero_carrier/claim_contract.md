# Claim contract

This node is a dimension-ten polynomial-subspace theorem.  It uses exact
completion maxima at supports four and five, the no-common-zero hypothesis,
Grassmann dimension, and the common-root bound.

The set `B` is the complete common-zero closure inside the evaluation set,
not merely the displayed union of two completion carriers.  The quotient
defect `delta` is defined only after that closure.

The node supplies no incidence count or component payment.  Those are
separate consumers of the outside-completion bound.
