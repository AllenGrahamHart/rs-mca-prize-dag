# Audit - L1 marked common-pencil CRT fiber bound

## Checked axes

1. CRT is applied to the selected support locators `S_i`, not the full
   marked locators `P-a_i`.
2. Monicity and the reduced representative prevent cancellation of the
   leading term in `W_0+SH`.
3. The singleton conclusion uses both the next-strip margin and `v<=p`.
4. At `t=m`, the strict background cap supplies `eta<=p-1`; dropping
   maximality loses one degree.
5. The count is per fixed defect locator and marks. It does not count those
   locators or support patterns.
6. Polynomiality uses fixed `p<=P` and the generated-field hypothesis.

## Remaining attack

Bound or profile-own the squarefree core-locator points in each finite
Forney stratum, with first-match multiplicity across source charts.
