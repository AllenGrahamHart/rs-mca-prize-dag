# KoalaBear m2 r4 positive 433-1a universal target elimination compiler

- **status:** PROVED
- **scope:** all seven outside product and squared-sum records in every
  formal positive `433-1a -> O0b` case
- **dependency:**
  `rate_half_kb_m2_r4_coordinate_positive_433_1a_outside_case_symmetry_quotient`
- **consumer:** `rate_half_band_closure`

For a fixed cycle sign `sigma in {+1,-1}`, write the seven nonzero target
product records as

```text
X_DE+=de,       X_DE-=-de,
X_DF+=df,       X_DF-=-df,
X_EF=sigma ef,  X_BE=be,       X_CF=cf.           (KBUTE-1)
```

Their complete target-product elimination ideal over the supported
`b,c` stratum is generated by four binomials:

```text
X_DE-+X_DE+=0,
X_DF-+X_DF+=0,
X_BE X_CF-sigma b c X_EF=0,
b X_DE+ X_CF-c X_DF+ X_BE=0.                      (KBUTE-2)
```

These equations are necessary and sufficient for target-product
realizability.  Explicitly, reconstruct

```text
e=X_BE/b,       f=X_CF/c,       d=b X_DE+/X_BE.   (KBUTE-3)
```

The last equation of `(KBUTE-2)` gives the prescribed `X_DF+`, the third
gives `X_EF`, and the first two give the negative records.

Let `H_R` denote the source-side value
`H(kappa_R)=kappa_R B_1(kappa_R)^2/A_2(kappa_R)^2` at the source label
carrying record `R`.  All seven target squared sums eliminate without
`d,e,f`:

```text
b^2 X_BE^2 H_DE+ -(b^2 X_DE+ +X_BE^2)^2=0,
b^2 X_BE^2 H_DE- -(b^2 X_DE+ -X_BE^2)^2=0,

c^2 X_CF^2 H_DF+ -(c^2 X_DF+ +X_CF^2)^2=0,
c^2 X_CF^2 H_DF- -(c^2 X_DF+ -X_CF^2)^2=0,

b^2 c^2 H_EF-(c X_BE+sigma b X_CF)^2=0,
b^2 H_BE-(b^2+X_BE)^2=0,
c^2 H_CF-(c^2+X_CF)^2=0.                         (KBUTE-4)
```

Conversely, after reconstruction `(KBUTE-3)`, equations `(KBUTE-4)` say
exactly that the seven `H_R` equal

```text
(d+e)^2,(d-e)^2,(d+f)^2,(d-f)^2,
(e+sigma f)^2,(b+e)^2,(c+f)^2.                   (KBUTE-5)
```

Thus `(KBUTE-2)--(KBUTE-4)` eliminate the outside target representatives
`d,e,f` exactly, not merely necessarily.

For each of the 267 residual target-gauge representatives, put the `xi`
record at `F(xi)` and the three matched record pairs at
`(F(u),F(-u))`, `(F(v),F(-v))`, and `(F(w),F(-w))`.  This substitution in
`(KBUTE-2)--(KBUTE-4)` is a complete target-free compiler for every formal
case.  No separate target-monomial exponent-lattice classification or
case-specific triangle derivation is needed.

In particular, the first version of the two triangle templates omitted the
fourth binomial in `(KBUTE-2)`.  Their statements have been repaired with
the specializations

```text
A: b F(u)F(-v)=c F(w)F(-w),
B: b F(u)F(-u)=c F(-v)F(-w).                     (KBUTE-6)
```

This theorem does not impose the common rational-map equations, source
label distinctness, denominator guards, or unsquared `q` signs.  It does
not prove any compiled source system empty, delete an alignment branch or
`433-1a -> O0b`, close positive coordinate parity, K3, a Prize row, or
either Prize result.

## Falsifier

Nonzero target products satisfying `(KBUTE-2)` but admitting no
reconstruction `(KBUTE-3)`, an actual target sum violating `(KBUTE-4)`, a
formal symmetry representative for which the record-to-source substitution
is incomplete, or a valid target realization omitted by the compiler.
