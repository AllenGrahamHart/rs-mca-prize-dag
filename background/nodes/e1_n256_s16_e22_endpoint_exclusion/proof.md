# Proof

The E22 router proves that every `V=44` candidate lies in one of eight
profiles on 1,321 templates.  The eight-profile exclusion proves that every
proper-conductor vector is removed by the conductor theorem and every
full-conductor norm is positive and below `2^250`.  These branches exhaust the
router, so the entire `V=44` chamber is empty.  Variance is even, hence the
next live positive endpoint is at most 42.
