# frontier: e22_minimal_scale_column_evaluation

Closed.

The selected classes are disjointly partitioned by minimal dyadic scale, and
the raw-to-selected triangular accounting is proved. The residual filter has
now been proved through the exact weighted lower-scale intersections, so the
minimal-scale column evaluation is proved.
