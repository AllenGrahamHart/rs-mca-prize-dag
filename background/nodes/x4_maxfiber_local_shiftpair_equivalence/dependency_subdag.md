# Dependency sub-DAG

```text
v13_prefix_collision_ledger [PROVED]
  --req--> x4_maxfiber_local_shiftpair_equivalence [PROVED]
  --ev----> x4_primitive_star_u1_coverage [TARGET]
```

The upstream collision ledger supplies the split-pair dictionary. The present
adapter changes the live quantifier from the global pair sum to the maximum
local same-prefix degree. The separate target must still cover those general
local records by the exact `u1` ledger.
