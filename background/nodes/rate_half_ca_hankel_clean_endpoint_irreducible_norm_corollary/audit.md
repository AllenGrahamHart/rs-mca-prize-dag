# Audit

1. `O=0` is an explicit branch hypothesis, not a conclusion for every
   endpoint failure.
2. The two uses of a letter `b` in the parent results are separated here:
   the residual component degree is zero, while the number of nonsaturated
   domain columns is one.
3. Absolute irreducibility follows because the parent factorization is over
   the algebraic closure, is squarefree, and has no residual component.
4. The linear norm factor `S` may vanish at a supported slope; no stronger
   transversality claim is made beyond the banked `3m+1` count.
5. The official `m=2^37` lies in the stated `m>1` scope.
