# Result

The clean branch has no residual component to classify. Its sole remaining
geometric object is an absolutely irreducible bidegree-`(4m-1,m)` curve whose
norm over the order-`16m` evaluation subgroup is a `rho`th power times one
linear form.

This rules out returning to reducible, product-of-rational-maps, or multiple
moving-root-branch constructions on `O=0`. The unresolved task is the
irreducible near-perfect cyclic norm itself.
