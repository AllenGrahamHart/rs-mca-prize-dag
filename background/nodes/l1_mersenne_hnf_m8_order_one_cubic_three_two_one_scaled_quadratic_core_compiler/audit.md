# Audit

The symbols `R,S` in this node are the common-quadratic role numerator and
complement value after multiplying both by `d^3`; their ratio is unchanged.
They are not the unscaled symbols in (TRW1).

Equation `E_5` clears `q-d`, so the inherited `q-d!=0` saturation is
essential. The implication `D!=0` uses both `E_6=0` and `K_6!=0`; do not use
the transported role equation without those conditions.

The 21 role equations remain a disjunction. The compiler removes the
complement value from the fourth, fifth, and role equations, but does not
prove any system empty or reconstruct a colored split-pencil witness from a
retained point.
