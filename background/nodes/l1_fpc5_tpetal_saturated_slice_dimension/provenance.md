# Provenance

The cross-determinant proof was isolated and coordinator-replayed in
`notes/pilots_20260808/t_petal_lemma/`, where it was left as a mint candidate.
This node prints the implicit hypotheses `e>=0` and `h>d`, adds the locator
projection proof, and specializes the result only after the exact large-source
prefilter.
