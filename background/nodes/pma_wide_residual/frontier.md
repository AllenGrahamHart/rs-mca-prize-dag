# Frontier — pma_wide_residual

## Status

REFUTED. The exact generic defect-four source obstruction realizes the finite
falsifier with more than `n^6` post-owner primitive codewords. The historical
frontier below explains the reductions that led to the false endpoint; the
live frontier has moved to `petal_mixed_amplification`.

## Proved around the leaf

- The core-defect normal form and auxiliary-list injection are proved.
- The Johnson-safe range is proved.
- The background quotient, interpolation-owner charge, automatic
  transversality, and source-to-PR-`#764` chart bridge are proved.
- Exact-periodic source codewords now have a canonical chart-free owner. On
  the official `sigma=1` grid their union over every nontrivial dyadic scale
  fits one global first-scale quotient-profile line.
- Every listed codeword missing at most
  `s_n=floor(n/(32 log_2 n))` points of any eligible dyadic coset now has a
  second canonical chart-free owner. Its complete global count
  is below `Q_2(k+2)` on every official row; together with exact periodicity it
  consumes less than `8(1+2^-690)Q_2(k+2)` of the existing `719` line.
- Catch #171's scale-two source has a unique odd-lift split point.  At exact
  agreement `k+1` and `k+3`, support injection gives the complete global
  owner
  `binom(n/2,k/2+1)<3Q_2(k+2)`.  All three global owners together consume
  fewer than eleven of the `719` units before chart selection.
- The B11 Johnson, two-petal-anchor, and background-petal-anchor gates have an
  exact disjoint first-match router and explicit finite bound.
- The primitive finite consumer now has a common carried-layout ledger. At
  rate half the exact top census is
  `N_top=2((n-k)/2+1)sum_(j=0)^3 binom(k-1,j)` and leaves
  `B_post=n^6-N_top`; at the lower rates the top band is empty and
  `B_post=n^6`. This proves the allowance, not the Post count.
- Every official `sigma=1` maximal source layout now has a canonical global
  index. A `(k-1)`-core `C` carries a layout exactly when
  `(U-Q_C)/L_C` off `C` has fiber signature `(2,...,2,1)`; the fibers uniquely
  recover the background and petals, and any two planted anchors recover the
  core. Thus the finite composition wall is the exact weighted sum over
  paired cores, with no hidden pairing or normalization multiplicity.
- A bare count of those cores is provably unusable. Their exact first moment is
  explicit, and at `(q,n,k)=(65537,65536,32768)` some word has more than
  `n^6` paired cores. This is not a Post lower bound: it pins the required
  saving to the weights `#E_C`, empty-core forcing, or chart-free ownership.
- Universal first-layout domination now removes that weighted-census wall.
  Every Post codeword first carried after the first paired core is one of the
  first layout's `M` planted anchors, so `#Post<=#E_C1+M`. Applying the three
  proved finite source payments only to `C_1` gives
  `#Post<=B_paid+M+#R_first` with `B_paid+M<n^6/1023` on every official row.
- On the finite `sigma=1` track, `(E,V_2,V_R)=(0,2,0)` makes `RES` empty and
  leaves exactly `GROW={d>=3}` after `J/A2`. The corresponding ambient
  certificate fails even the generous full `n^6` primitive ceiling at rates
  `1/8` and `1/16`, so it is a route diagnostic rather than a payment.
- The source-pinned interpolation charge replaces that failed certificate:
  after restoring all defect sets and both exact background states, the
  complete finite `d<=2` class is below `n^5/1024` per maximal sunflower,
  independently of the field.
- The same source-pinned method pays the complete `(d,r)=(3,1)` cell by
  `n^6/9216`. Together, `d<=2` and `(3,1)` contribute less than `n^6/8192`
  per maximal sunflower.
- A canonical four-point certificate pays the part of `(d,r)=(3,0)` with a
  full petal by `n^6/1536`. All paid finite source cells together are below
  `n^6/1024` per maximal sunflower.
- The remaining defect-three class has at most one agreement in each of at
  least five petals. Exact censuses at `n=32,40,48,56` found only trivial-
  stabilizer objects in this class and counts consistent with an `n^7` scale.
  This is a route warning, not an asymptotic or official-row lower bound.
- The canonical first five agreements now give a nonzero divided-difference
  hyperplane in `(e_1(D),e_2(D),e_3(D))`. Every fixed hyperplane contains
  fewer than `(k-1)^2/2` split cubic locators. The direct aggregate
  `16(k-1)^2 binom((n-k)/2,5)` is still order `n^7`.
- That exponent is real. At rate one half the reciprocal-quadratic labels
  `c(x)=x^(-1)+rx` on an index-two multiplicative-core sunflower give at
  least `(M-15)(N-6)(N-3)binom(M,5)/(6M)` actual primitive diffuse extras.
  The lower bound first exceeds `n^6` at `n=2^22`, so it overwhelms the
  official `n=2^41` row. This is a direct-payment route cut, not merely
  census evidence.
- The reciprocal-quadratic family is now paid globally: every constructed
  codeword agrees on `G\({b} union D)` and therefore belongs to the proved
  all-dyadic growing-defect owner. Chart multiplicity no longer exposes this
  family to the per-sunflower `n^6` line.
- Fixed-excess and growing-excess full-petal layers have separate proved
  payments, but those theorems do not pay mixed petals.
- Quotient-pullback families are real and must remain separately priced.
- Exact stabilizer does not exhaust folded and low-defect quotient closures:
  a folded polynomial against a nonfolded receiver and low-defect error
  supports can have stabilizer one.
- The precise folded-source theorem is now proved to be subsumed by the global
  exact-periodic owner. The low-defect boundary-stripping predicate is exact,
  but its raw first-scale candidate count cannot fit `719Q_2`.

## Refuted remaining line

Pay every maximal-sunflower contribution outside the proved top-band atlas at
the actual source scale and within the downstream exponent budget, using the
auxiliary list only as an injected upper bound. This includes non-lift and
higher-level odd-lift below-floor full petals as well as mixed/diffuse partial
petals. At `sigma=1`, it is the one-layout inequality
`#R_first<=B_post-B_paid-M`, including the post-owner far-from-coset diffuse
background-free `(d,r)=(3,0)` complement and every finite defect outside the
growing owner. At asymptotic reserve it is the general `GROW union RES` cell.

## Retired next increment

Prove `#R_first<=B_post-B_paid-M` inside one first source layout. Seek the
needed incidence saving only on the complement of
`QOWN_per union QOWN_cosgrow union QOWN_odd2`, classify the remaining
below-floor full-petal sources, and pay the later defects without any chart
multiplier. Another complete auxiliary-list or
fixed-certificate estimate is formally route-cut. The separate asymptotic
target remains a source-uniform `GROW union RES` payment.
