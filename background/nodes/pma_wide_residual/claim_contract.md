# Claim contract — pma_wide_residual

**Status: REFUTED.** The conclusion below is false by
`pma_sigma_one_d4_generic_source_obstruction`. This contract is retained to
pin the failed allowance and prevent its accidental reuse.

## Object

For a fixed official received/source word, the objects being paid are the
aggregate first-matched maximal-sunflower extras outside the proved
layout-anchored top-band atlas: below-floor full-petal extras and
mixed/diffuse partial-petal extras. Each carried chart injects into an
auxiliary Reed-Solomon list. The auxiliary list is an upper bound, not a
claimed bijection with the fixed source owner. It is not an arbitrary RS
received word and does not reopen the proved top-band chart.

## Hypotheses

1. The exact `pma_aux_list_reduction` source dictionary holds.
2. `N_aux^2<=d|T|`; the Johnson-safe complement is out of scope.
3. `pma_source_paving_bridge` supplies the exact background quotient and
   weighted-RS chart, including same-slope witnesses.
4. Exact-periodic members are removed globally by
   `pma_exact_periodic_owner`. `pma_quotient_closure_scope` proves that the
   precise folded-source class is already included and fixes `QCLOSE=empty`;
   every low-defect exact-stabilizer-one survivor is retained in the direct
   residual theorem.
5. On the finite `sigma=1` track,
   `pma_sigma_one_dyadic_near_coset_owner` next removes every codeword missing
   at most `s_n=floor(n/(32 log_2 n))` points of an eligible dyadic coset. It
   contains the complete reciprocal-quadratic family and every analogous
   growing-defect near-coset core;
   together with exact periodicity it fits the existing first-scale `719` line
   without chart multiplicity.
6. `pma_sigma_one_odd_lift_boundary_owner` then removes the complete first
   two exact levels of the scale-two odd-lift family from catch #171.  The
   source split point is unique, so this global owner costs less than three
   more `Q_2(k+2)` units and introduces no chart multiplier.
7. `pma_sigma_one_b11_scope` fixes `(E,V_2,V_R)=(0,2,0)`, proves
   `AR=RES=empty` on the finite track, and cuts the raw `q^4` payment route.
8. `pma_sigma_one_low_defect_payment` pays every finite `d<=2` source cell
   by less than `n^5/1024` per maximal sunflower after restoring `D` and
   background choices.
9. `pma_sigma_one_d3_background_payment` pays the finite `(d,r)=(3,1)`
   source cell by less than `n^6/9216`; together with item 8 this is less
   than `n^6/8192` per maximal sunflower.
10. `pma_sigma_one_d3_full_petal_payment` pays the full-petal part of
   `(d,r)=(3,0)` by less than `n^6/1536`; all paid finite source cells are
   below `n^6/1024` per maximal sunflower.
11. `pma_sigma_one_d3_diffuse_hyperplane_reduction` converts the remaining
   defect-three class into an aggregate incidence between explicit nonzero
   five-point hyperplanes and split cubic core locators. Its direct sum is
   order `n^7` and is not treated as paid.
12. `pma_sigma_one_d3_reciprocal_quadratic_obstruction` proves that actual
    primitive source codewords realize order `n^7` and exceed `n^6` on the
    official rate-half row. Item 5 supplies its global owner, so only the
    complementary diffuse incidence remains.
13. At asymptotic reserve, `J`, `A2`, and `AR` are removed by the proved
   general B11 first-match router.
14. On the finite track, `pma_sigma_one_post_top_allowance` supplies the
    source-level disjoint Top/Post partition and the exact unborrowed target
    `B_post=n^6-N_top` at rate half or `B_post=n^6` at the lower rates.
15. `pma_sigma_one_paired_core_normalization` canonically indexes every
    maximal source layout by a `(k-1)`-core with quotient-fiber signature
    `(2,...,2,1)`. The first-match identity is
    `#Post=sum_C #E_C`; no unproved layout or pairing multiplicity remains
    hidden in the notation.
16. `pma_sigma_one_paired_core_abundance` proves that the unweighted number of
    paired cores can exceed `n^6` on a valid official row. No premise may
    replace the Post-weighted sum by a bare core census.
17. `pma_sigma_one_first_layout_domination` proves that all later first-match
    Post groups lie in the first layout's `M` planted anchors. It reduces the
    finite target to `#R_first<=B_post-B_paid-M`, with
    `B_paid+M<n^6/1023`; no paired-core multiplier remains.
18. Domain, generated field, reserve, and row are the ones consumed by
    `imgfib`.

## Conclusion

The finite first-layout remainder (the unowned below-floor full-petal class,
the post-owner far-from-coset complementary `(d,r)=(3,0)` incidence, and every
defect outside the growing owner) has size at most
`B_post-B_paid-M`. The asymptotic non-quotient `GROW union RES` residual fits
its separately printed downstream allowance.

## Promotion evidence

Promotion is forbidden: the finite conclusion has a proved counterexample.
The following list is the retired pre-refutation gate.

Promotion requires:

1. a theorem proving the source-uniform primitive bound;
2. an exhaustiveness/disjointness proof for `QOWN_per`, `QOWN_cosgrow`, and
   `QOWN_odd2` followed by B11, with the direct residual theorem explicitly
   quantified over below-floor full petals and low-defect survivors;
3. the one-layout finite residual bound within `B_post-B_paid-M`, plus the
   separate asymptotic allowance and its `imgfib` consumer bridge;
4. a verifier for every finite arithmetic line;
5. a consumer-backward audit showing no arbitrary-word or field drift.

Numerical survival alone cannot promote the node.
