# Audit

- Integer autocorrelations give `sum |A_d|<=E`; no sparsity pattern is
  assumed.
- The upper deviation bound is `2E<=12`. The lower endpoint comes from
  positivity of every conjugate square in a nonzero norm.
- The derivative of the logarithm majorant has only the printed critical
  points on `(-18,12]`.
- The endpoint logarithm is bounded by a positive rational series with an
  explicit geometric tail.
- The final exponential comparison uses a lower Taylor polynomial with
  positive omitted terms and exact integer cross-multiplication.
- Energies two, three, and four remain outside this claim.
