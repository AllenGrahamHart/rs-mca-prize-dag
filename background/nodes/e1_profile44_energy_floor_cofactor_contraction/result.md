# Result

**PROVED.** The low-energy floor and an energy-adaptive product majorant
exclude all profile-`(4,4)` cofactors above `932364`, reducing the exact
frontier from `1133` values to `657` and removing the pure `2^20` branch.
