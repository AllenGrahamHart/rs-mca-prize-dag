# Audit

Two independent verifiers check:

- the energy-adaptive cap and quadratic-log derivative signs;
- monotonicity of `V/C(V)`;
- exact degree-`37` rational bounds for `e^16`;
- both sides of the sharp integer threshold `932364/932365`;
- reconstruction of all `1133` parent cofactors and the exact `657`-value
  contracted list;
- the thirteen surviving pure powers and exclusion of `2^20`;
- hostile mutations of the energy floor, threshold, and cofactor list.

The audit explicitly rejects treating `657` cofactor values as `657`
collision orbits. This node is a norm-size contraction only.
