# Audit

1. The upstream Python verifier passes its 11 semantic mutations and all
   five generic quadratic identities.
2. All twelve literal compiler shards were rerun independently under
   official SageMath 10.9 with one CPU and 8 GB caps.
3. Every shard matched the pinned resultant and terminal `U,V,Z` core hashes.
4. The six groups are exact two-element partitions, not inferred covariance.
5. Both `V!=0` and `U=V=0` remain open; no emptiness or owner is claimed.
6. A route sweep confirms `(0,1)` is the unique quadratic row pair for both
   balanced representatives: 2 expected passes and 10 exact degree-guard
   rejections, with no failures or timeouts.
7. The theorem moves no owner, charge, row, or Prize bound.
