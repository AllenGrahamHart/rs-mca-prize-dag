# Result

**PROVED.** The twelve remaining fixed aligned-positive cells are exactly
reduced to six literal fingerprint orbits, each split into a generic
`V!=0` branch and a retained rank-drop `U=V=0` branch. No cell closes yet.
