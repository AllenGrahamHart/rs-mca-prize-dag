# Dependency sub-DAG

```text
f3_h3_disjoint_distance_six_split_pencil_router ----+
                                                     |
f3_h3_dsp8_primitive_shift_pair_adapter -------------+
                                                     |
f3_h3_dsp8_global_overlap_cover_payment -------------+
                                                     v
f3_h3_dsp8_rich_factorial_moment_compiler [PROVED]
                                                     |
                                                     v (load-bearing evidence)
f3_h3_dsp8_correlation_bound [TARGET]
```

The compiler introduces no new required leaf. The older `FM69` target is one
strictly stronger possible input, not a dependency of the proved implication.
