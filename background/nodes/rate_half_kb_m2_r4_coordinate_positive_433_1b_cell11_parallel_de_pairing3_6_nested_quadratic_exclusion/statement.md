# Statement

## Theorem (KBP1B11-DE3)

Every guarded cell-11 principal system in the two parallel-`DE` generic label
orbits represented by `(xi,pairing)=(0,3)` and `(2,3)` is empty. Universal
label transport therefore pays the six labels with `xi in {0,1,2}` and
pairing in `{3,6}`.
