# frontier: aqb_c2_average_family

Conditional.

The certificate semantics are proved. The current repository still lacks the
actual averaged `c=2` family certificate. The live family leaf is
`aqb_c2_family_certificate_payload`.
