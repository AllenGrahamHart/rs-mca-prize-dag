# Result

The official pure-quartic harmonic lift locus is empty in characteristic
zero. Its entire finite-characteristic obstruction is now isolated before
the Fermat decomposition as a fixed sparse unit ideal with `126` variables,
`127` cubic-or-lower equations, and an integer bad-characteristic
certificate.

No certificate is computed and no official characteristic is excluded, so
the rate-half list target remains open.
