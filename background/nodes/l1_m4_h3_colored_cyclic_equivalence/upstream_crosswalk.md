# Upstream crosswalk

This is a base-field-normalized split-pencil reduction. It replaces the
three-value census on each multiplicity-four Mersenne row by an exact
prime-field cyclic-code two-Schur section. An upstream implementation should
retain both coefficientwise code constraints and the three equal color
counts; dropping the square constraint changes the problem.
