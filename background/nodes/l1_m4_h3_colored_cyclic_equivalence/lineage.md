# Lineage

- The checkpoint atlas leaves four rows at multiplicity four.
- Maximal-value polynomial abc removes `h=4`, leaving `h=2,3`.
- The embedded-family parity theorem settles its constructed pencils in
  `h=2` and motivates isolating the nonembedded cubic branch.
- This node turns that branch into a simultaneous cyclic-code/Schur-square
  condition without adding a conjectural dependency.
