# Claim contract

- **claim id:** `l1_m4_h3_colored_cyclic_equivalence`
- **status:** PROVED
- **scope:** exactly the four official rows `n=4(p+1)` and split-value degree
  `h=3` at first-checkpoint width `t=p`
- **proved content:** equivalence between three complete fibers and the
  simultaneous code constraints `b,b^[2] in C_M`, including the exact color
  counts and `p+4` zero complement
- **dependencies used:** the Mersenne closure formula and maximal `h=4`
  emptiness
- **nonclaim:** no section emptiness, component count, nonembedded `h=2`
  classification, `m=8,16` statement, higher-width result, or L1 promotion
