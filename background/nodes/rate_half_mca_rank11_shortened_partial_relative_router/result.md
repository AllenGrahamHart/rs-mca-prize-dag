# Result

```text
maximal common core:       c<K
residual slope degree:     18..31
denominator degree:        <=67472
residual low-complexity:   pure locator or scalar locator
residual high-complexity:  chi'>=3(m-c)-(K-c)+3
original high-complexity:  chi>=2299571
certificate lift:          exact, same Q and affine locator scalars
```

The rank-eleven seed now reaches the standard local partial-relative
interfaces. The next route action is to connect the lifted scalar-locator
branch to the existing pole-tolerant `(E)` packet and the high-complexity
branch to a deployed same-owner spread-abundance target.
