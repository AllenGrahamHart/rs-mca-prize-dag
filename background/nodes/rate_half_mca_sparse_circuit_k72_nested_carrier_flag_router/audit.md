# Audit

1. `B_3 subset B_4,B_5` is assumed only for the maximal-overlap cell.
2. The impossible branch uses ground-set inclusion, not merely span nesting.
3. A support-five circuit cannot contain four points in a rank-three flat.
4. One shared residual point forces span nesting only when it lies outside
   `F_3`.
5. The inside-`F_3` branch retains both levels of the vanishing-space flag.
6. Grassmann is taken inside `H_3`, giving five rather than three.
7. The payment values are recomputed by an import-based primary script and
   an independent implementation of the incidence formula.
8. No K'=72 closure is claimed.
