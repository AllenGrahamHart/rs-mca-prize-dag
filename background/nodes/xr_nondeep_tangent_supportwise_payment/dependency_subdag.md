# Dependency sub-DAG

```text
xr_strip_classification_rungs [PROVED algebra/arithmetic, ev]
       |
       v
xr_nondeep_tangent_supportwise_payment [REFUTED]
       |
       | ev
       v
xr_tangent_support_mismatch_bridge [TARGET]
```

The refutation prevents the XR consumer from treating the existence of one
global joint support as a complete support-wise tangent payment outside the
deep regime.
