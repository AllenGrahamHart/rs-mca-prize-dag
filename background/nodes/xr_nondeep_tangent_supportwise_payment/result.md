# Result

`REFUTED`. Outside the deep range, a globally tangent pair need not fit in
the `n-A+1` support-wise tangent slot. The smooth rate-`1/4` `F_17` fixture
has eight exact-`A` bad slopes against a slot of six. The official XR route
therefore retains `xr_tangent_support_mismatch_bridge` as a critical target.
