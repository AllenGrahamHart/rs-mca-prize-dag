# Statement

## Claim (KBP1B3-XI3-P1114-FM-1)

On the guarded positive deployed 433-1b role-cell-3 branch, every atlas case
with missing record `xi=3` (the `df` record), canonical matching index in
`{11,14}`, any source sign `epsilon in {+-1}^2`, and any target lane
`sigma in {+-1}^2` is empty.

Sixteen exact source rows compute matching 11, one for each source sign and
target lane. Canonical matching 14 is its image under exchange of the two
identical positive `de` records, so the computed 16 raw cases transport
value-for-value to 16 further raw cases. Thus all 32 raw cases in the stated
scope are empty.

No other matching, missing role, complete cell-3 exclusion, rate-half band,
LIST, MCA, or Prize result is claimed.
