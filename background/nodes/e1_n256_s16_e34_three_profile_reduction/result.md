# Result

PROVED. Every pair-feasible profile-`(3,4,0)` collision at `V=68` is reduced
to exactly three autocorrelation magnitude profiles:

```text
(6,7),       (9,4,1),       (12,1,2).
```

All have `L=20`. The other 21 integer profiles are excluded by an abstract
cap, complete quotient certificates, or the exact inner-`4Z` support census.
The `V=68` slice remains open on the three displayed profiles.
