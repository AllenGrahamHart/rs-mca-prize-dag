# Lineage

- The source-only endpoint cut reduced `xi=5` from 240 raw cases to 24 exact
  source points but could not exclude those points by itself.
- Fixing `f=m/b` at each point converts the remaining problem to 1440 small
  bivariate systems over the deployed field.
- The primary census eliminates `v`; the independent census eliminates `u`.
  Both completed in under one quarter-second per source on Modal.
- Both directions enumerate the same 2208 outer roots and find no inner root.
- No global quotient resultant or dense symbolic expansion is needed for this
  slice.
- The cell-3 frontier moves to `xi=4` only.
- The result remains background evidence toward `rate_half_band_closure`.
